// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const trace_processor = @import("src/gen/protos/perfetto/trace_processor/trace_processor.proto.zig");
// structs
const BigtraceQueryArgsWire = struct {
    const TRACES_WIRE: gremlin.ProtoWireNumber = 1;
    const SQL_QUERY_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const BigtraceQueryArgs = struct {
    // fields
    traces: ?[]const ?[]const u8 = null,
    sql_query: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const BigtraceQueryArgs) usize {
        var res: usize = 0;
        if (self.traces) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(BigtraceQueryArgsWire.TRACES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.sql_query) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(BigtraceQueryArgsWire.SQL_QUERY_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const BigtraceQueryArgs, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BigtraceQueryArgs, target: *gremlin.Writer) void {
        if (self.traces) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(BigtraceQueryArgsWire.TRACES_WIRE, v);
                } else {
                    target.appendBytesTag(BigtraceQueryArgsWire.TRACES_WIRE, 0);
                }
            }
        }
        if (self.sql_query) |v| {
            if (v.len > 0) {
                target.appendBytes(BigtraceQueryArgsWire.SQL_QUERY_WIRE, v);
            }
        }
    }
};
pub const BigtraceQueryArgsReader = struct {
    buf: gremlin.Reader,
    _traces_offset: ?usize = null,
    _traces_last_offset: ?usize = null,
    _traces_cnt: usize = 0,
    _sql_query: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!BigtraceQueryArgsReader {
        const buf = gremlin.Reader.init(src);
        var res = BigtraceQueryArgsReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BigtraceQueryArgsWire.TRACES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._traces_offset == null) {
                        res._traces_offset = offset - result.size;
                    }
                    res._traces_last_offset = offset;
                    res._traces_cnt += 1;
                },
                BigtraceQueryArgsWire.SQL_QUERY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._sql_query = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn tracesCount(self: *const BigtraceQueryArgsReader) usize {
        return self._traces_cnt;
    }
    pub fn tracesNext(self: *BigtraceQueryArgsReader) ?[]const u8 {
        if (self._traces_offset == null) return null;
        const current_offset = self._traces_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._traces_last_offset != null and current_offset >= self._traces_last_offset.?) {
            self._traces_offset = null;
            return result.value;
        }
        if (self._traces_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._traces_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == BigtraceQueryArgsWire.TRACES_WIRE) {
                self._traces_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._traces_offset = null;
        return result.value;
    }
    pub inline fn getSqlQuery(self: *const BigtraceQueryArgsReader) []const u8 {
        return self._sql_query orelse &[_]u8{};
    }
};
const BigtraceQueryResponseWire = struct {
    const TRACE_WIRE: gremlin.ProtoWireNumber = 1;
    const RESULT_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const BigtraceQueryResponse = struct {
    // fields
    trace: ?[]const u8 = null,
    result: ?[]const ?trace_processor.QueryResult = null,
    pub fn calcProtobufSize(self: *const BigtraceQueryResponse) usize {
        var res: usize = 0;
        if (self.trace) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(BigtraceQueryResponseWire.TRACE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.result) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(BigtraceQueryResponseWire.RESULT_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const BigtraceQueryResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BigtraceQueryResponse, target: *gremlin.Writer) void {
        if (self.trace) |v| {
            if (v.len > 0) {
                target.appendBytes(BigtraceQueryResponseWire.TRACE_WIRE, v);
            }
        }
        if (self.result) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(BigtraceQueryResponseWire.RESULT_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(BigtraceQueryResponseWire.RESULT_WIRE, 0);
                }
            }
        }
    }
};
pub const BigtraceQueryResponseReader = struct {
    buf: gremlin.Reader,
    _trace: ?[]const u8 = null,
    _result_offset: ?usize = null,
    _result_last_offset: ?usize = null,
    _result_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!BigtraceQueryResponseReader {
        const buf = gremlin.Reader.init(src);
        var res = BigtraceQueryResponseReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BigtraceQueryResponseWire.TRACE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._trace = result.value;
                },
                BigtraceQueryResponseWire.RESULT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._result_offset == null) {
                        res._result_offset = offset - result.size;
                    }
                    res._result_last_offset = offset;
                    res._result_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTrace(self: *const BigtraceQueryResponseReader) []const u8 {
        return self._trace orelse &[_]u8{};
    }
    pub fn resultCount(self: *const BigtraceQueryResponseReader) usize {
        return self._result_cnt;
    }
    pub fn resultNext(self: *BigtraceQueryResponseReader) ?trace_processor.QueryResultReader {
        if (self._result_offset == null) return null;
        const current_offset = self._result_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = trace_processor.QueryResultReader.init(result.value) catch return null;
        if (self._result_last_offset != null and current_offset >= self._result_last_offset.?) {
            self._result_offset = null;
            return msg;
        }
        if (self._result_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._result_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == BigtraceQueryResponseWire.RESULT_WIRE) {
                self._result_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._result_offset = null;
        return msg;
    }
};
