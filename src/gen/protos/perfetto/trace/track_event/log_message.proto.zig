// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const LogMessageWire = struct {
    const SOURCE_LOCATION_IID_WIRE: gremlin.ProtoWireNumber = 1;
    const BODY_IID_WIRE: gremlin.ProtoWireNumber = 2;
    const PRIO_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const LogMessage = struct {
    // nested enums
    pub const Priority = enum(i32) {
        PRIO_UNSPECIFIED = 0,
        PRIO_UNUSED = 1,
        PRIO_VERBOSE = 2,
        PRIO_DEBUG = 3,
        PRIO_INFO = 4,
        PRIO_WARN = 5,
        PRIO_ERROR = 6,
        PRIO_FATAL = 7,
    };
    // fields
    source_location_iid: u64 = 0,
    body_iid: u64 = 0,
    prio: LogMessage.Priority = @enumFromInt(0),
    pub fn calcProtobufSize(self: *const LogMessage) usize {
        var res: usize = 0;
        if (self.source_location_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(LogMessageWire.SOURCE_LOCATION_IID_WIRE) + gremlin.sizes.sizeU64(self.source_location_iid);
        }
        if (self.body_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(LogMessageWire.BODY_IID_WIRE) + gremlin.sizes.sizeU64(self.body_iid);
        }
        if (@intFromEnum(self.prio) != 0) {
            res += gremlin.sizes.sizeWireNumber(LogMessageWire.PRIO_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.prio));
        }
        return res;
    }
    pub fn encode(self: *const LogMessage, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const LogMessage, target: *gremlin.Writer) void {
        if (self.source_location_iid != 0) {
            target.appendUint64(LogMessageWire.SOURCE_LOCATION_IID_WIRE, self.source_location_iid);
        }
        if (self.body_iid != 0) {
            target.appendUint64(LogMessageWire.BODY_IID_WIRE, self.body_iid);
        }
        if (@intFromEnum(self.prio) != 0) {
            target.appendInt32(LogMessageWire.PRIO_WIRE, @intFromEnum(self.prio));
        }
    }
};
pub const LogMessageReader = struct {
    buf: gremlin.Reader,
    _source_location_iid: u64 = 0,
    _body_iid: u64 = 0,
    _prio: LogMessage.Priority = @enumFromInt(0),
    pub fn init(src: []const u8) gremlin.Error!LogMessageReader {
        const buf = gremlin.Reader.init(src);
        var res = LogMessageReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                LogMessageWire.SOURCE_LOCATION_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._source_location_iid = result.value;
                },
                LogMessageWire.BODY_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._body_iid = result.value;
                },
                LogMessageWire.PRIO_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._prio = @enumFromInt(result.value);
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getSourceLocationIid(self: *const LogMessageReader) u64 {
        return self._source_location_iid;
    }
    pub inline fn getBodyIid(self: *const LogMessageReader) u64 {
        return self._body_iid;
    }
    pub inline fn getPrio(self: *const LogMessageReader) LogMessage.Priority {
        return self._prio;
    }
};
const LogMessageBodyWire = struct {
    const IID_WIRE: gremlin.ProtoWireNumber = 1;
    const BODY_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const LogMessageBody = struct {
    // fields
    iid: u64 = 0,
    body: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const LogMessageBody) usize {
        var res: usize = 0;
        if (self.iid != 0) {
            res += gremlin.sizes.sizeWireNumber(LogMessageBodyWire.IID_WIRE) + gremlin.sizes.sizeU64(self.iid);
        }
        if (self.body) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(LogMessageBodyWire.BODY_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const LogMessageBody, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const LogMessageBody, target: *gremlin.Writer) void {
        if (self.iid != 0) {
            target.appendUint64(LogMessageBodyWire.IID_WIRE, self.iid);
        }
        if (self.body) |v| {
            if (v.len > 0) {
                target.appendBytes(LogMessageBodyWire.BODY_WIRE, v);
            }
        }
    }
};
pub const LogMessageBodyReader = struct {
    buf: gremlin.Reader,
    _iid: u64 = 0,
    _body: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!LogMessageBodyReader {
        const buf = gremlin.Reader.init(src);
        var res = LogMessageBodyReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                LogMessageBodyWire.IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._iid = result.value;
                },
                LogMessageBodyWire.BODY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._body = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIid(self: *const LogMessageBodyReader) u64 {
        return self._iid;
    }
    pub inline fn getBody(self: *const LogMessageBodyReader) []const u8 {
        return self._body orelse &[_]u8{};
    }
};
