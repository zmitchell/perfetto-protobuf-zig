// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeLegacyIpcWire = struct {
    const MESSAGE_CLASS_WIRE: gremlin.ProtoWireNumber = 1;
    const MESSAGE_LINE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const ChromeLegacyIpc = struct {
    // nested enums
    pub const MessageClass = enum(i32) {
        CLASS_UNSPECIFIED = 0,
        CLASS_AUTOMATION = 1,
        CLASS_FRAME = 2,
        CLASS_PAGE = 3,
        CLASS_VIEW = 4,
        CLASS_WIDGET = 5,
        CLASS_INPUT = 6,
        CLASS_TEST = 7,
        CLASS_WORKER = 8,
        CLASS_NACL = 9,
        CLASS_GPU_CHANNEL = 10,
        CLASS_MEDIA = 11,
        CLASS_PPAPI = 12,
        CLASS_CHROME = 13,
        CLASS_DRAG = 14,
        CLASS_PRINT = 15,
        CLASS_EXTENSION = 16,
        CLASS_TEXT_INPUT_CLIENT = 17,
        CLASS_BLINK_TEST = 18,
        CLASS_ACCESSIBILITY = 19,
        CLASS_PRERENDER = 20,
        CLASS_CHROMOTING = 21,
        CLASS_BROWSER_PLUGIN = 22,
        CLASS_ANDROID_WEB_VIEW = 23,
        CLASS_NACL_HOST = 24,
        CLASS_ENCRYPTED_MEDIA = 25,
        CLASS_CAST = 26,
        CLASS_GIN_JAVA_BRIDGE = 27,
        CLASS_CHROME_UTILITY_PRINTING = 28,
        CLASS_OZONE_GPU = 29,
        CLASS_WEB_TEST = 30,
        CLASS_NETWORK_HINTS = 31,
        CLASS_EXTENSIONS_GUEST_VIEW = 32,
        CLASS_GUEST_VIEW = 33,
        CLASS_MEDIA_PLAYER_DELEGATE = 34,
        CLASS_EXTENSION_WORKER = 35,
        CLASS_SUBRESOURCE_FILTER = 36,
        CLASS_UNFREEZABLE_FRAME = 37,
    };
    // fields
    message_class: ChromeLegacyIpc.MessageClass = @enumFromInt(0),
    message_line: u32 = 0,
    pub fn calcProtobufSize(self: *const ChromeLegacyIpc) usize {
        var res: usize = 0;
        if (@intFromEnum(self.message_class) != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeLegacyIpcWire.MESSAGE_CLASS_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.message_class));
        }
        if (self.message_line != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeLegacyIpcWire.MESSAGE_LINE_WIRE) + gremlin.sizes.sizeU32(self.message_line);
        }
        return res;
    }
    pub fn encode(self: *const ChromeLegacyIpc, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeLegacyIpc, target: *gremlin.Writer) void {
        if (@intFromEnum(self.message_class) != 0) {
            target.appendInt32(ChromeLegacyIpcWire.MESSAGE_CLASS_WIRE, @intFromEnum(self.message_class));
        }
        if (self.message_line != 0) {
            target.appendUint32(ChromeLegacyIpcWire.MESSAGE_LINE_WIRE, self.message_line);
        }
    }
};
pub const ChromeLegacyIpcReader = struct {
    buf: gremlin.Reader,
    _message_class: ChromeLegacyIpc.MessageClass = @enumFromInt(0),
    _message_line: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeLegacyIpcReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeLegacyIpcReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeLegacyIpcWire.MESSAGE_CLASS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._message_class = @enumFromInt(result.value);
                },
                ChromeLegacyIpcWire.MESSAGE_LINE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._message_line = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getMessageClass(self: *const ChromeLegacyIpcReader) ChromeLegacyIpc.MessageClass {
        return self._message_class;
    }
    pub inline fn getMessageLine(self: *const ChromeLegacyIpcReader) u32 {
        return self._message_line;
    }
};
