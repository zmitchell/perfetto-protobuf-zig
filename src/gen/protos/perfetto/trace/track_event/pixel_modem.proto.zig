// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PixelModemEventInsightWire = struct {
    const DETOKENIZED_MESSAGE_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const PixelModemEventInsight = struct {
    // fields
    detokenized_message: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const PixelModemEventInsight) usize {
        var res: usize = 0;
        if (self.detokenized_message) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(PixelModemEventInsightWire.DETOKENIZED_MESSAGE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const PixelModemEventInsight, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PixelModemEventInsight, target: *gremlin.Writer) void {
        if (self.detokenized_message) |v| {
            if (v.len > 0) {
                target.appendBytes(PixelModemEventInsightWire.DETOKENIZED_MESSAGE_WIRE, v);
            }
        }
    }
};
pub const PixelModemEventInsightReader = struct {
    buf: gremlin.Reader,
    _detokenized_message: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!PixelModemEventInsightReader {
        const buf = gremlin.Reader.init(src);
        var res = PixelModemEventInsightReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PixelModemEventInsightWire.DETOKENIZED_MESSAGE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._detokenized_message = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDetokenizedMessage(self: *const PixelModemEventInsightReader) []const u8 {
        return self._detokenized_message orelse &[_]u8{};
    }
};
