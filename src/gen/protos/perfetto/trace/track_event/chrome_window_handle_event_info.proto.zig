// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeWindowHandleEventInfoWire = struct {
    const DPI_WIRE: gremlin.ProtoWireNumber = 1;
    const MESSAGE_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const HWND_PTR_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ChromeWindowHandleEventInfo = struct {
    // fields
    dpi: u32 = 0,
    message_id: u32 = 0,
    hwnd_ptr: u64 = 0,
    pub fn calcProtobufSize(self: *const ChromeWindowHandleEventInfo) usize {
        var res: usize = 0;
        if (self.dpi != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeWindowHandleEventInfoWire.DPI_WIRE) + gremlin.sizes.sizeU32(self.dpi);
        }
        if (self.message_id != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeWindowHandleEventInfoWire.MESSAGE_ID_WIRE) + gremlin.sizes.sizeU32(self.message_id);
        }
        if (self.hwnd_ptr != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeWindowHandleEventInfoWire.HWND_PTR_WIRE) + gremlin.sizes.sizeFixed64(self.hwnd_ptr);
        }
        return res;
    }
    pub fn encode(self: *const ChromeWindowHandleEventInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeWindowHandleEventInfo, target: *gremlin.Writer) void {
        if (self.dpi != 0) {
            target.appendUint32(ChromeWindowHandleEventInfoWire.DPI_WIRE, self.dpi);
        }
        if (self.message_id != 0) {
            target.appendUint32(ChromeWindowHandleEventInfoWire.MESSAGE_ID_WIRE, self.message_id);
        }
        if (self.hwnd_ptr != 0) {
            target.appendFixed64(ChromeWindowHandleEventInfoWire.HWND_PTR_WIRE, self.hwnd_ptr);
        }
    }
};
pub const ChromeWindowHandleEventInfoReader = struct {
    buf: gremlin.Reader,
    _dpi: u32 = 0,
    _message_id: u32 = 0,
    _hwnd_ptr: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeWindowHandleEventInfoReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeWindowHandleEventInfoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeWindowHandleEventInfoWire.DPI_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._dpi = result.value;
                },
                ChromeWindowHandleEventInfoWire.MESSAGE_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._message_id = result.value;
                },
                ChromeWindowHandleEventInfoWire.HWND_PTR_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._hwnd_ptr = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDpi(self: *const ChromeWindowHandleEventInfoReader) u32 {
        return self._dpi;
    }
    pub inline fn getMessageId(self: *const ChromeWindowHandleEventInfoReader) u32 {
        return self._message_id;
    }
    pub inline fn getHwndPtr(self: *const ChromeWindowHandleEventInfoReader) u64 {
        return self._hwnd_ptr;
    }
};
