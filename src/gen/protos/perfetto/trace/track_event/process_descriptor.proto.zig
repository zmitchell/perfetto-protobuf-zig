// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ProcessDescriptorWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
    const CMDLINE_WIRE: gremlin.ProtoWireNumber = 2;
    const PROCESS_NAME_WIRE: gremlin.ProtoWireNumber = 6;
    const PROCESS_PRIORITY_WIRE: gremlin.ProtoWireNumber = 5;
    const START_TIMESTAMP_NS_WIRE: gremlin.ProtoWireNumber = 7;
    const CHROME_PROCESS_TYPE_WIRE: gremlin.ProtoWireNumber = 4;
    const LEGACY_SORT_INDEX_WIRE: gremlin.ProtoWireNumber = 3;
    const PROCESS_LABELS_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const ProcessDescriptor = struct {
    // nested enums
    pub const ChromeProcessType = enum(i32) {
        PROCESS_UNSPECIFIED = 0,
        PROCESS_BROWSER = 1,
        PROCESS_RENDERER = 2,
        PROCESS_UTILITY = 3,
        PROCESS_ZYGOTE = 4,
        PROCESS_SANDBOX_HELPER = 5,
        PROCESS_GPU = 6,
        PROCESS_PPAPI_PLUGIN = 7,
        PROCESS_PPAPI_BROKER = 8,
    };
    // fields
    pid: i32 = 0,
    cmdline: ?[]const ?[]const u8 = null,
    process_name: ?[]const u8 = null,
    process_priority: i32 = 0,
    start_timestamp_ns: i64 = 0,
    chrome_process_type: ProcessDescriptor.ChromeProcessType = @enumFromInt(0),
    legacy_sort_index: i32 = 0,
    process_labels: ?[]const ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ProcessDescriptor) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(ProcessDescriptorWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.cmdline) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ProcessDescriptorWire.CMDLINE_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.process_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ProcessDescriptorWire.PROCESS_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.process_priority != 0) {
            res += gremlin.sizes.sizeWireNumber(ProcessDescriptorWire.PROCESS_PRIORITY_WIRE) + gremlin.sizes.sizeI32(self.process_priority);
        }
        if (self.start_timestamp_ns != 0) {
            res += gremlin.sizes.sizeWireNumber(ProcessDescriptorWire.START_TIMESTAMP_NS_WIRE) + gremlin.sizes.sizeI64(self.start_timestamp_ns);
        }
        if (@intFromEnum(self.chrome_process_type) != 0) {
            res += gremlin.sizes.sizeWireNumber(ProcessDescriptorWire.CHROME_PROCESS_TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.chrome_process_type));
        }
        if (self.legacy_sort_index != 0) {
            res += gremlin.sizes.sizeWireNumber(ProcessDescriptorWire.LEGACY_SORT_INDEX_WIRE) + gremlin.sizes.sizeI32(self.legacy_sort_index);
        }
        if (self.process_labels) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ProcessDescriptorWire.PROCESS_LABELS_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ProcessDescriptor, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ProcessDescriptor, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendInt32(ProcessDescriptorWire.PID_WIRE, self.pid);
        }
        if (self.cmdline) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(ProcessDescriptorWire.CMDLINE_WIRE, v);
                } else {
                    target.appendBytesTag(ProcessDescriptorWire.CMDLINE_WIRE, 0);
                }
            }
        }
        if (self.process_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ProcessDescriptorWire.PROCESS_NAME_WIRE, v);
            }
        }
        if (self.process_priority != 0) {
            target.appendInt32(ProcessDescriptorWire.PROCESS_PRIORITY_WIRE, self.process_priority);
        }
        if (self.start_timestamp_ns != 0) {
            target.appendInt64(ProcessDescriptorWire.START_TIMESTAMP_NS_WIRE, self.start_timestamp_ns);
        }
        if (@intFromEnum(self.chrome_process_type) != 0) {
            target.appendInt32(ProcessDescriptorWire.CHROME_PROCESS_TYPE_WIRE, @intFromEnum(self.chrome_process_type));
        }
        if (self.legacy_sort_index != 0) {
            target.appendInt32(ProcessDescriptorWire.LEGACY_SORT_INDEX_WIRE, self.legacy_sort_index);
        }
        if (self.process_labels) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(ProcessDescriptorWire.PROCESS_LABELS_WIRE, v);
                } else {
                    target.appendBytesTag(ProcessDescriptorWire.PROCESS_LABELS_WIRE, 0);
                }
            }
        }
    }
};
pub const ProcessDescriptorReader = struct {
    buf: gremlin.Reader,
    _pid: i32 = 0,
    _cmdline_offset: ?usize = null,
    _cmdline_last_offset: ?usize = null,
    _cmdline_cnt: usize = 0,
    _process_name: ?[]const u8 = null,
    _process_priority: i32 = 0,
    _start_timestamp_ns: i64 = 0,
    _chrome_process_type: ProcessDescriptor.ChromeProcessType = @enumFromInt(0),
    _legacy_sort_index: i32 = 0,
    _process_labels_offset: ?usize = null,
    _process_labels_last_offset: ?usize = null,
    _process_labels_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ProcessDescriptorReader {
        const buf = gremlin.Reader.init(src);
        var res = ProcessDescriptorReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ProcessDescriptorWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                ProcessDescriptorWire.CMDLINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._cmdline_offset == null) {
                        res._cmdline_offset = offset - result.size;
                    }
                    res._cmdline_last_offset = offset;
                    res._cmdline_cnt += 1;
                },
                ProcessDescriptorWire.PROCESS_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._process_name = result.value;
                },
                ProcessDescriptorWire.PROCESS_PRIORITY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._process_priority = result.value;
                },
                ProcessDescriptorWire.START_TIMESTAMP_NS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._start_timestamp_ns = result.value;
                },
                ProcessDescriptorWire.CHROME_PROCESS_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._chrome_process_type = @enumFromInt(result.value);
                },
                ProcessDescriptorWire.LEGACY_SORT_INDEX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._legacy_sort_index = result.value;
                },
                ProcessDescriptorWire.PROCESS_LABELS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._process_labels_offset == null) {
                        res._process_labels_offset = offset - result.size;
                    }
                    res._process_labels_last_offset = offset;
                    res._process_labels_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const ProcessDescriptorReader) i32 {
        return self._pid;
    }
    pub fn cmdlineCount(self: *const ProcessDescriptorReader) usize {
        return self._cmdline_cnt;
    }
    pub fn cmdlineNext(self: *ProcessDescriptorReader) ?[]const u8 {
        if (self._cmdline_offset == null) return null;
        const current_offset = self._cmdline_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._cmdline_last_offset != null and current_offset >= self._cmdline_last_offset.?) {
            self._cmdline_offset = null;
            return result.value;
        }
        if (self._cmdline_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._cmdline_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ProcessDescriptorWire.CMDLINE_WIRE) {
                self._cmdline_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._cmdline_offset = null;
        return result.value;
    }
    pub inline fn getProcessName(self: *const ProcessDescriptorReader) []const u8 {
        return self._process_name orelse &[_]u8{};
    }
    pub inline fn getProcessPriority(self: *const ProcessDescriptorReader) i32 {
        return self._process_priority;
    }
    pub inline fn getStartTimestampNs(self: *const ProcessDescriptorReader) i64 {
        return self._start_timestamp_ns;
    }
    pub inline fn getChromeProcessType(self: *const ProcessDescriptorReader) ProcessDescriptor.ChromeProcessType {
        return self._chrome_process_type;
    }
    pub inline fn getLegacySortIndex(self: *const ProcessDescriptorReader) i32 {
        return self._legacy_sort_index;
    }
    pub fn processLabelsCount(self: *const ProcessDescriptorReader) usize {
        return self._process_labels_cnt;
    }
    pub fn processLabelsNext(self: *ProcessDescriptorReader) ?[]const u8 {
        if (self._process_labels_offset == null) return null;
        const current_offset = self._process_labels_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._process_labels_last_offset != null and current_offset >= self._process_labels_last_offset.?) {
            self._process_labels_offset = null;
            return result.value;
        }
        if (self._process_labels_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._process_labels_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ProcessDescriptorWire.PROCESS_LABELS_WIRE) {
                self._process_labels_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._process_labels_offset = null;
        return result.value;
    }
};
