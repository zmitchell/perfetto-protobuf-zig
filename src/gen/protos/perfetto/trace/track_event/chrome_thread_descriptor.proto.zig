// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeThreadDescriptorWire = struct {
    const THREAD_TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const LEGACY_SORT_INDEX_WIRE: gremlin.ProtoWireNumber = 2;
    const IS_SANDBOXED_TID_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ChromeThreadDescriptor = struct {
    // fields
    thread_type: i32 = 0,
    legacy_sort_index: i32 = 0,
    is_sandboxed_tid: bool = false,
    pub fn calcProtobufSize(self: *const ChromeThreadDescriptor) usize {
        var res: usize = 0;
        if (self.thread_type != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeThreadDescriptorWire.THREAD_TYPE_WIRE) + gremlin.sizes.sizeI32(self.thread_type);
        }
        if (self.legacy_sort_index != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeThreadDescriptorWire.LEGACY_SORT_INDEX_WIRE) + gremlin.sizes.sizeI32(self.legacy_sort_index);
        }
        if (self.is_sandboxed_tid != false) {
            res += gremlin.sizes.sizeWireNumber(ChromeThreadDescriptorWire.IS_SANDBOXED_TID_WIRE) + gremlin.sizes.sizeBool(self.is_sandboxed_tid);
        }
        return res;
    }
    pub fn encode(self: *const ChromeThreadDescriptor, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeThreadDescriptor, target: *gremlin.Writer) void {
        if (self.thread_type != 0) {
            target.appendInt32(ChromeThreadDescriptorWire.THREAD_TYPE_WIRE, self.thread_type);
        }
        if (self.legacy_sort_index != 0) {
            target.appendInt32(ChromeThreadDescriptorWire.LEGACY_SORT_INDEX_WIRE, self.legacy_sort_index);
        }
        if (self.is_sandboxed_tid != false) {
            target.appendBool(ChromeThreadDescriptorWire.IS_SANDBOXED_TID_WIRE, self.is_sandboxed_tid);
        }
    }
};
pub const ChromeThreadDescriptorReader = struct {
    buf: gremlin.Reader,
    _thread_type: i32 = 0,
    _legacy_sort_index: i32 = 0,
    _is_sandboxed_tid: bool = false,
    pub fn init(src: []const u8) gremlin.Error!ChromeThreadDescriptorReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeThreadDescriptorReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeThreadDescriptorWire.THREAD_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._thread_type = result.value;
                },
                ChromeThreadDescriptorWire.LEGACY_SORT_INDEX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._legacy_sort_index = result.value;
                },
                ChromeThreadDescriptorWire.IS_SANDBOXED_TID_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._is_sandboxed_tid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getThreadType(self: *const ChromeThreadDescriptorReader) i32 {
        return self._thread_type;
    }
    pub inline fn getLegacySortIndex(self: *const ChromeThreadDescriptorReader) i32 {
        return self._legacy_sort_index;
    }
    pub inline fn getIsSandboxedTid(self: *const ChromeThreadDescriptorReader) bool {
        return self._is_sandboxed_tid;
    }
};
