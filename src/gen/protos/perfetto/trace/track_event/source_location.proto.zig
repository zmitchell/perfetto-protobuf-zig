// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const UnsymbolizedSourceLocationWire = struct {
    const IID_WIRE: gremlin.ProtoWireNumber = 1;
    const MAPPING_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const REL_PC_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const UnsymbolizedSourceLocation = struct {
    // fields
    iid: u64 = 0,
    mapping_id: u64 = 0,
    rel_pc: u64 = 0,
    pub fn calcProtobufSize(self: *const UnsymbolizedSourceLocation) usize {
        var res: usize = 0;
        if (self.iid != 0) {
            res += gremlin.sizes.sizeWireNumber(UnsymbolizedSourceLocationWire.IID_WIRE) + gremlin.sizes.sizeU64(self.iid);
        }
        if (self.mapping_id != 0) {
            res += gremlin.sizes.sizeWireNumber(UnsymbolizedSourceLocationWire.MAPPING_ID_WIRE) + gremlin.sizes.sizeU64(self.mapping_id);
        }
        if (self.rel_pc != 0) {
            res += gremlin.sizes.sizeWireNumber(UnsymbolizedSourceLocationWire.REL_PC_WIRE) + gremlin.sizes.sizeU64(self.rel_pc);
        }
        return res;
    }
    pub fn encode(self: *const UnsymbolizedSourceLocation, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const UnsymbolizedSourceLocation, target: *gremlin.Writer) void {
        if (self.iid != 0) {
            target.appendUint64(UnsymbolizedSourceLocationWire.IID_WIRE, self.iid);
        }
        if (self.mapping_id != 0) {
            target.appendUint64(UnsymbolizedSourceLocationWire.MAPPING_ID_WIRE, self.mapping_id);
        }
        if (self.rel_pc != 0) {
            target.appendUint64(UnsymbolizedSourceLocationWire.REL_PC_WIRE, self.rel_pc);
        }
    }
};
pub const UnsymbolizedSourceLocationReader = struct {
    buf: gremlin.Reader,
    _iid: u64 = 0,
    _mapping_id: u64 = 0,
    _rel_pc: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!UnsymbolizedSourceLocationReader {
        const buf = gremlin.Reader.init(src);
        var res = UnsymbolizedSourceLocationReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                UnsymbolizedSourceLocationWire.IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._iid = result.value;
                },
                UnsymbolizedSourceLocationWire.MAPPING_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._mapping_id = result.value;
                },
                UnsymbolizedSourceLocationWire.REL_PC_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._rel_pc = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIid(self: *const UnsymbolizedSourceLocationReader) u64 {
        return self._iid;
    }
    pub inline fn getMappingId(self: *const UnsymbolizedSourceLocationReader) u64 {
        return self._mapping_id;
    }
    pub inline fn getRelPc(self: *const UnsymbolizedSourceLocationReader) u64 {
        return self._rel_pc;
    }
};
const SourceLocationWire = struct {
    const IID_WIRE: gremlin.ProtoWireNumber = 1;
    const FILE_NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const FUNCTION_NAME_WIRE: gremlin.ProtoWireNumber = 3;
    const LINE_NUMBER_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const SourceLocation = struct {
    // fields
    iid: u64 = 0,
    file_name: ?[]const u8 = null,
    function_name: ?[]const u8 = null,
    line_number: u32 = 0,
    pub fn calcProtobufSize(self: *const SourceLocation) usize {
        var res: usize = 0;
        if (self.iid != 0) {
            res += gremlin.sizes.sizeWireNumber(SourceLocationWire.IID_WIRE) + gremlin.sizes.sizeU64(self.iid);
        }
        if (self.file_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SourceLocationWire.FILE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.function_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SourceLocationWire.FUNCTION_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.line_number != 0) {
            res += gremlin.sizes.sizeWireNumber(SourceLocationWire.LINE_NUMBER_WIRE) + gremlin.sizes.sizeU32(self.line_number);
        }
        return res;
    }
    pub fn encode(self: *const SourceLocation, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SourceLocation, target: *gremlin.Writer) void {
        if (self.iid != 0) {
            target.appendUint64(SourceLocationWire.IID_WIRE, self.iid);
        }
        if (self.file_name) |v| {
            if (v.len > 0) {
                target.appendBytes(SourceLocationWire.FILE_NAME_WIRE, v);
            }
        }
        if (self.function_name) |v| {
            if (v.len > 0) {
                target.appendBytes(SourceLocationWire.FUNCTION_NAME_WIRE, v);
            }
        }
        if (self.line_number != 0) {
            target.appendUint32(SourceLocationWire.LINE_NUMBER_WIRE, self.line_number);
        }
    }
};
pub const SourceLocationReader = struct {
    buf: gremlin.Reader,
    _iid: u64 = 0,
    _file_name: ?[]const u8 = null,
    _function_name: ?[]const u8 = null,
    _line_number: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!SourceLocationReader {
        const buf = gremlin.Reader.init(src);
        var res = SourceLocationReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SourceLocationWire.IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._iid = result.value;
                },
                SourceLocationWire.FILE_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._file_name = result.value;
                },
                SourceLocationWire.FUNCTION_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._function_name = result.value;
                },
                SourceLocationWire.LINE_NUMBER_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._line_number = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIid(self: *const SourceLocationReader) u64 {
        return self._iid;
    }
    pub inline fn getFileName(self: *const SourceLocationReader) []const u8 {
        return self._file_name orelse &[_]u8{};
    }
    pub inline fn getFunctionName(self: *const SourceLocationReader) []const u8 {
        return self._function_name orelse &[_]u8{};
    }
    pub inline fn getLineNumber(self: *const SourceLocationReader) u32 {
        return self._line_number;
    }
};
