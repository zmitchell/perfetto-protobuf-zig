// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeApplicationStateInfoWire = struct {
    const APPLICATION_STATE_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ChromeApplicationStateInfo = struct {
    // nested enums
    pub const ChromeApplicationState = enum(i32) {
        APPLICATION_STATE_UNKNOWN = 0,
        APPLICATION_STATE_HAS_RUNNING_ACTIVITIES = 1,
        APPLICATION_STATE_HAS_PAUSED_ACTIVITIES = 2,
        APPLICATION_STATE_HAS_STOPPED_ACTIVITIES = 3,
        APPLICATION_STATE_HAS_DESTROYED_ACTIVITIES = 4,
    };
    // fields
    application_state: ChromeApplicationStateInfo.ChromeApplicationState = @enumFromInt(0),
    pub fn calcProtobufSize(self: *const ChromeApplicationStateInfo) usize {
        var res: usize = 0;
        if (@intFromEnum(self.application_state) != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeApplicationStateInfoWire.APPLICATION_STATE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.application_state));
        }
        return res;
    }
    pub fn encode(self: *const ChromeApplicationStateInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeApplicationStateInfo, target: *gremlin.Writer) void {
        if (@intFromEnum(self.application_state) != 0) {
            target.appendInt32(ChromeApplicationStateInfoWire.APPLICATION_STATE_WIRE, @intFromEnum(self.application_state));
        }
    }
};
pub const ChromeApplicationStateInfoReader = struct {
    buf: gremlin.Reader,
    _application_state: ChromeApplicationStateInfo.ChromeApplicationState = @enumFromInt(0),
    pub fn init(src: []const u8) gremlin.Error!ChromeApplicationStateInfoReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeApplicationStateInfoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeApplicationStateInfoWire.APPLICATION_STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._application_state = @enumFromInt(result.value);
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getApplicationState(self: *const ChromeApplicationStateInfoReader) ChromeApplicationStateInfo.ChromeApplicationState {
        return self._application_state;
    }
};
