// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeActiveProcessesWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ChromeActiveProcesses = struct {
    // fields
    pid: ?[]const i32 = null,
    pub fn calcProtobufSize(self: *const ChromeActiveProcesses) usize {
        var res: usize = 0;
        if (self.pid) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(ChromeActiveProcessesWire.PID_WIRE) + gremlin.sizes.sizeI32(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI32(v);
                }
                res += gremlin.sizes.sizeWireNumber(ChromeActiveProcessesWire.PID_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        return res;
    }
    pub fn encode(self: *const ChromeActiveProcesses, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeActiveProcesses, target: *gremlin.Writer) void {
        if (self.pid) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendInt32(ChromeActiveProcessesWire.PID_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI32(v);
                }
                target.appendBytesTag(ChromeActiveProcessesWire.PID_WIRE, packed_size);
                for (arr) |v| {
                    target.appendInt32WithoutTag(v);
                }
            }
        }
    }
};
pub const ChromeActiveProcessesReader = struct {
    buf: gremlin.Reader,
    _pid_offset: ?usize = null,
    _pid_last_offset: ?usize = null,
    _pid_packed: bool = false,
    pub fn init(src: []const u8) gremlin.Error!ChromeActiveProcessesReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeActiveProcessesReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeActiveProcessesWire.PID_WIRE => {
                    if (res._pid_offset == null) {
                        res._pid_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._pid_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._pid_offset = offset + length_result.size;
                        res._pid_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._pid_last_offset.?;
                    } else {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._pid_last_offset = offset;
                    }
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn pidNext(self: *ChromeActiveProcessesReader) gremlin.Error!?i32 {
        if (self._pid_offset == null) return null;
        const current_offset = self._pid_offset.?;
        if (current_offset >= self._pid_last_offset.?) {
            self._pid_offset = null;
            return null;
        }
        if (self._pid_packed) {
            const value_result = try self.buf.readInt32(current_offset);
            self._pid_offset = current_offset + value_result.size;
            if (self._pid_offset.? >= self._pid_last_offset.?) {
                self._pid_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readInt32(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._pid_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == ChromeActiveProcessesWire.PID_WIRE) {
                    self._pid_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._pid_offset = null;
            return value_result.value;
        }
    }
};
