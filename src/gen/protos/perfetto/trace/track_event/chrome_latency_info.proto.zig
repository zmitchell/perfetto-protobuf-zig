// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeLatencyInfoWire = struct {
    const TRACE_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const STEP_WIRE: gremlin.ProtoWireNumber = 2;
    const FRAME_TREE_NODE_ID_WIRE: gremlin.ProtoWireNumber = 3;
    const COMPONENT_INFO_WIRE: gremlin.ProtoWireNumber = 4;
    const IS_COALESCED_WIRE: gremlin.ProtoWireNumber = 5;
    const GESTURE_SCROLL_ID_WIRE: gremlin.ProtoWireNumber = 6;
    const TOUCH_ID_WIRE: gremlin.ProtoWireNumber = 7;
    const INPUT_TYPE_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const ChromeLatencyInfo = struct {
    // nested enums
    pub const Step = enum(i32) {
        STEP_UNSPECIFIED = 0,
        STEP_SEND_INPUT_EVENT_UI = 3,
        STEP_HANDLE_INPUT_EVENT_IMPL = 5,
        STEP_DID_HANDLE_INPUT_AND_OVERSCROLL = 8,
        STEP_HANDLE_INPUT_EVENT_MAIN = 4,
        STEP_MAIN_THREAD_SCROLL_UPDATE = 2,
        STEP_HANDLE_INPUT_EVENT_MAIN_COMMIT = 1,
        STEP_HANDLED_INPUT_EVENT_MAIN_OR_IMPL = 9,
        STEP_HANDLED_INPUT_EVENT_IMPL = 10,
        STEP_SWAP_BUFFERS = 6,
        STEP_DRAW_AND_SWAP = 7,
        STEP_FINISHED_SWAP_BUFFERS = 11,
    };
    pub const LatencyComponentType = enum(i32) {
        COMPONENT_UNSPECIFIED = 0,
        COMPONENT_INPUT_EVENT_LATENCY_BEGIN_RWH = 1,
        COMPONENT_INPUT_EVENT_LATENCY_SCROLL_UPDATE_ORIGINAL = 2,
        COMPONENT_INPUT_EVENT_LATENCY_FIRST_SCROLL_UPDATE_ORIGINAL = 3,
        COMPONENT_INPUT_EVENT_LATENCY_ORIGINAL = 4,
        COMPONENT_INPUT_EVENT_LATENCY_UI = 5,
        COMPONENT_INPUT_EVENT_LATENCY_RENDERER_MAIN = 6,
        COMPONENT_INPUT_EVENT_LATENCY_RENDERING_SCHEDULED_MAIN = 7,
        COMPONENT_INPUT_EVENT_LATENCY_RENDERING_SCHEDULED_IMPL = 8,
        COMPONENT_INPUT_EVENT_LATENCY_SCROLL_UPDATE_LAST_EVENT = 9,
        COMPONENT_INPUT_EVENT_LATENCY_ACK_RWH = 10,
        COMPONENT_INPUT_EVENT_LATENCY_RENDERER_SWAP = 11,
        COMPONENT_DISPLAY_COMPOSITOR_RECEIVED_FRAME = 12,
        COMPONENT_INPUT_EVENT_GPU_SWAP_BUFFER = 13,
        COMPONENT_INPUT_EVENT_LATENCY_FRAME_SWAP = 14,
    };
    pub const InputType = enum(i32) {
        UNSPECIFIED_OR_OTHER = 0,
        TOUCH_MOVED = 1,
        GESTURE_SCROLL_BEGIN = 2,
        GESTURE_SCROLL_UPDATE = 3,
        GESTURE_SCROLL_END = 4,
        GESTURE_TAP = 5,
        GESTURE_TAP_CANCEL = 6,
    };
    // nested structs
    const ComponentInfoWire = struct {
        const COMPONENT_TYPE_WIRE: gremlin.ProtoWireNumber = 1;
        const TIME_US_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const ComponentInfo = struct {
        // fields
        component_type: ChromeLatencyInfo.LatencyComponentType = @enumFromInt(0),
        time_us: u64 = 0,
        pub fn calcProtobufSize(self: *const ChromeLatencyInfo.ComponentInfo) usize {
            var res: usize = 0;
            if (@intFromEnum(self.component_type) != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfo.ComponentInfoWire.COMPONENT_TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.component_type));
            }
            if (self.time_us != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfo.ComponentInfoWire.TIME_US_WIRE) + gremlin.sizes.sizeU64(self.time_us);
            }
            return res;
        }
        pub fn encode(self: *const ChromeLatencyInfo.ComponentInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ChromeLatencyInfo.ComponentInfo, target: *gremlin.Writer) void {
            if (@intFromEnum(self.component_type) != 0) {
                target.appendInt32(ChromeLatencyInfo.ComponentInfoWire.COMPONENT_TYPE_WIRE, @intFromEnum(self.component_type));
            }
            if (self.time_us != 0) {
                target.appendUint64(ChromeLatencyInfo.ComponentInfoWire.TIME_US_WIRE, self.time_us);
            }
        }
    };
    pub const ComponentInfoReader = struct {
        buf: gremlin.Reader,
        _component_type: ChromeLatencyInfo.LatencyComponentType = @enumFromInt(0),
        _time_us: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!ChromeLatencyInfo.ComponentInfoReader {
            const buf = gremlin.Reader.init(src);
            var res = ChromeLatencyInfo.ComponentInfoReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ChromeLatencyInfo.ComponentInfoWire.COMPONENT_TYPE_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._component_type = @enumFromInt(result.value);
                    },
                    ChromeLatencyInfo.ComponentInfoWire.TIME_US_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._time_us = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getComponentType(self: *const ChromeLatencyInfo.ComponentInfoReader) ChromeLatencyInfo.LatencyComponentType {
            return self._component_type;
        }
        pub inline fn getTimeUs(self: *const ChromeLatencyInfo.ComponentInfoReader) u64 {
            return self._time_us;
        }
    };
    // fields
    trace_id: i64 = 0,
    step: ChromeLatencyInfo.Step = @enumFromInt(0),
    frame_tree_node_id: i32 = 0,
    component_info: ?[]const ?ChromeLatencyInfo.ComponentInfo = null,
    is_coalesced: bool = false,
    gesture_scroll_id: i64 = 0,
    touch_id: i64 = 0,
    input_type: ChromeLatencyInfo.InputType = @enumFromInt(0),
    pub fn calcProtobufSize(self: *const ChromeLatencyInfo) usize {
        var res: usize = 0;
        if (self.trace_id != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfoWire.TRACE_ID_WIRE) + gremlin.sizes.sizeI64(self.trace_id);
        }
        if (@intFromEnum(self.step) != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfoWire.STEP_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.step));
        }
        if (self.frame_tree_node_id != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfoWire.FRAME_TREE_NODE_ID_WIRE) + gremlin.sizes.sizeI32(self.frame_tree_node_id);
        }
        if (self.component_info) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfoWire.COMPONENT_INFO_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.is_coalesced != false) {
            res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfoWire.IS_COALESCED_WIRE) + gremlin.sizes.sizeBool(self.is_coalesced);
        }
        if (self.gesture_scroll_id != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfoWire.GESTURE_SCROLL_ID_WIRE) + gremlin.sizes.sizeI64(self.gesture_scroll_id);
        }
        if (self.touch_id != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfoWire.TOUCH_ID_WIRE) + gremlin.sizes.sizeI64(self.touch_id);
        }
        if (@intFromEnum(self.input_type) != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeLatencyInfoWire.INPUT_TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.input_type));
        }
        return res;
    }
    pub fn encode(self: *const ChromeLatencyInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeLatencyInfo, target: *gremlin.Writer) void {
        if (self.trace_id != 0) {
            target.appendInt64(ChromeLatencyInfoWire.TRACE_ID_WIRE, self.trace_id);
        }
        if (@intFromEnum(self.step) != 0) {
            target.appendInt32(ChromeLatencyInfoWire.STEP_WIRE, @intFromEnum(self.step));
        }
        if (self.frame_tree_node_id != 0) {
            target.appendInt32(ChromeLatencyInfoWire.FRAME_TREE_NODE_ID_WIRE, self.frame_tree_node_id);
        }
        if (self.component_info) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ChromeLatencyInfoWire.COMPONENT_INFO_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ChromeLatencyInfoWire.COMPONENT_INFO_WIRE, 0);
                }
            }
        }
        if (self.is_coalesced != false) {
            target.appendBool(ChromeLatencyInfoWire.IS_COALESCED_WIRE, self.is_coalesced);
        }
        if (self.gesture_scroll_id != 0) {
            target.appendInt64(ChromeLatencyInfoWire.GESTURE_SCROLL_ID_WIRE, self.gesture_scroll_id);
        }
        if (self.touch_id != 0) {
            target.appendInt64(ChromeLatencyInfoWire.TOUCH_ID_WIRE, self.touch_id);
        }
        if (@intFromEnum(self.input_type) != 0) {
            target.appendInt32(ChromeLatencyInfoWire.INPUT_TYPE_WIRE, @intFromEnum(self.input_type));
        }
    }
};
pub const ChromeLatencyInfoReader = struct {
    buf: gremlin.Reader,
    _trace_id: i64 = 0,
    _step: ChromeLatencyInfo.Step = @enumFromInt(0),
    _frame_tree_node_id: i32 = 0,
    _component_info_offset: ?usize = null,
    _component_info_last_offset: ?usize = null,
    _component_info_cnt: usize = 0,
    _is_coalesced: bool = false,
    _gesture_scroll_id: i64 = 0,
    _touch_id: i64 = 0,
    _input_type: ChromeLatencyInfo.InputType = @enumFromInt(0),
    pub fn init(src: []const u8) gremlin.Error!ChromeLatencyInfoReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeLatencyInfoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeLatencyInfoWire.TRACE_ID_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._trace_id = result.value;
                },
                ChromeLatencyInfoWire.STEP_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._step = @enumFromInt(result.value);
                },
                ChromeLatencyInfoWire.FRAME_TREE_NODE_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._frame_tree_node_id = result.value;
                },
                ChromeLatencyInfoWire.COMPONENT_INFO_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._component_info_offset == null) {
                        res._component_info_offset = offset - result.size;
                    }
                    res._component_info_last_offset = offset;
                    res._component_info_cnt += 1;
                },
                ChromeLatencyInfoWire.IS_COALESCED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._is_coalesced = result.value;
                },
                ChromeLatencyInfoWire.GESTURE_SCROLL_ID_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._gesture_scroll_id = result.value;
                },
                ChromeLatencyInfoWire.TOUCH_ID_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._touch_id = result.value;
                },
                ChromeLatencyInfoWire.INPUT_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._input_type = @enumFromInt(result.value);
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTraceId(self: *const ChromeLatencyInfoReader) i64 {
        return self._trace_id;
    }
    pub inline fn getStep(self: *const ChromeLatencyInfoReader) ChromeLatencyInfo.Step {
        return self._step;
    }
    pub inline fn getFrameTreeNodeId(self: *const ChromeLatencyInfoReader) i32 {
        return self._frame_tree_node_id;
    }
    pub fn componentInfoCount(self: *const ChromeLatencyInfoReader) usize {
        return self._component_info_cnt;
    }
    pub fn componentInfoNext(self: *ChromeLatencyInfoReader) ?ChromeLatencyInfo.ComponentInfoReader {
        if (self._component_info_offset == null) return null;
        const current_offset = self._component_info_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ChromeLatencyInfo.ComponentInfoReader.init(result.value) catch return null;
        if (self._component_info_last_offset != null and current_offset >= self._component_info_last_offset.?) {
            self._component_info_offset = null;
            return msg;
        }
        if (self._component_info_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._component_info_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ChromeLatencyInfoWire.COMPONENT_INFO_WIRE) {
                self._component_info_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._component_info_offset = null;
        return msg;
    }
    pub inline fn getIsCoalesced(self: *const ChromeLatencyInfoReader) bool {
        return self._is_coalesced;
    }
    pub inline fn getGestureScrollId(self: *const ChromeLatencyInfoReader) i64 {
        return self._gesture_scroll_id;
    }
    pub inline fn getTouchId(self: *const ChromeLatencyInfoReader) i64 {
        return self._touch_id;
    }
    pub inline fn getInputType(self: *const ChromeLatencyInfoReader) ChromeLatencyInfo.InputType {
        return self._input_type;
    }
};
