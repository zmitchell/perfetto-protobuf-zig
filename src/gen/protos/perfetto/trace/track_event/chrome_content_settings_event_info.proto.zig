// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeContentSettingsEventInfoWire = struct {
    const NUMBER_OF_EXCEPTIONS_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ChromeContentSettingsEventInfo = struct {
    // fields
    number_of_exceptions: u32 = 0,
    pub fn calcProtobufSize(self: *const ChromeContentSettingsEventInfo) usize {
        var res: usize = 0;
        if (self.number_of_exceptions != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeContentSettingsEventInfoWire.NUMBER_OF_EXCEPTIONS_WIRE) + gremlin.sizes.sizeU32(self.number_of_exceptions);
        }
        return res;
    }
    pub fn encode(self: *const ChromeContentSettingsEventInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeContentSettingsEventInfo, target: *gremlin.Writer) void {
        if (self.number_of_exceptions != 0) {
            target.appendUint32(ChromeContentSettingsEventInfoWire.NUMBER_OF_EXCEPTIONS_WIRE, self.number_of_exceptions);
        }
    }
};
pub const ChromeContentSettingsEventInfoReader = struct {
    buf: gremlin.Reader,
    _number_of_exceptions: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeContentSettingsEventInfoReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeContentSettingsEventInfoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeContentSettingsEventInfoWire.NUMBER_OF_EXCEPTIONS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._number_of_exceptions = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getNumberOfExceptions(self: *const ChromeContentSettingsEventInfoReader) u32 {
        return self._number_of_exceptions;
    }
};
