// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ScreenshotWire = struct {
    const JPG_IMAGE_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const Screenshot = struct {
    // fields
    jpg_image: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const Screenshot) usize {
        var res: usize = 0;
        if (self.jpg_image) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ScreenshotWire.JPG_IMAGE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const Screenshot, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const Screenshot, target: *gremlin.Writer) void {
        if (self.jpg_image) |v| {
            if (v.len > 0) {
                target.appendBytes(ScreenshotWire.JPG_IMAGE_WIRE, v);
            }
        }
    }
};
pub const ScreenshotReader = struct {
    buf: gremlin.Reader,
    _jpg_image: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ScreenshotReader {
        const buf = gremlin.Reader.init(src);
        var res = ScreenshotReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ScreenshotWire.JPG_IMAGE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._jpg_image = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getJpgImage(self: *const ScreenshotReader) []const u8 {
        return self._jpg_image orelse &[_]u8{};
    }
};
