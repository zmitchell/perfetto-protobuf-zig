// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// enums
pub const ChromeRAILMode = enum(i32) {
    RAIL_MODE_NONE = 0,
    RAIL_MODE_RESPONSE = 1,
    RAIL_MODE_ANIMATION = 2,
    RAIL_MODE_IDLE = 3,
    RAIL_MODE_LOAD = 4,
};
// structs
const ChromeRendererSchedulerStateWire = struct {
    const RAIL_MODE_WIRE: gremlin.ProtoWireNumber = 1;
    const IS_BACKGROUNDED_WIRE: gremlin.ProtoWireNumber = 2;
    const IS_HIDDEN_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ChromeRendererSchedulerState = struct {
    // fields
    rail_mode: ChromeRAILMode = @enumFromInt(0),
    is_backgrounded: bool = false,
    is_hidden: bool = false,
    pub fn calcProtobufSize(self: *const ChromeRendererSchedulerState) usize {
        var res: usize = 0;
        if (@intFromEnum(self.rail_mode) != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeRendererSchedulerStateWire.RAIL_MODE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.rail_mode));
        }
        if (self.is_backgrounded != false) {
            res += gremlin.sizes.sizeWireNumber(ChromeRendererSchedulerStateWire.IS_BACKGROUNDED_WIRE) + gremlin.sizes.sizeBool(self.is_backgrounded);
        }
        if (self.is_hidden != false) {
            res += gremlin.sizes.sizeWireNumber(ChromeRendererSchedulerStateWire.IS_HIDDEN_WIRE) + gremlin.sizes.sizeBool(self.is_hidden);
        }
        return res;
    }
    pub fn encode(self: *const ChromeRendererSchedulerState, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeRendererSchedulerState, target: *gremlin.Writer) void {
        if (@intFromEnum(self.rail_mode) != 0) {
            target.appendInt32(ChromeRendererSchedulerStateWire.RAIL_MODE_WIRE, @intFromEnum(self.rail_mode));
        }
        if (self.is_backgrounded != false) {
            target.appendBool(ChromeRendererSchedulerStateWire.IS_BACKGROUNDED_WIRE, self.is_backgrounded);
        }
        if (self.is_hidden != false) {
            target.appendBool(ChromeRendererSchedulerStateWire.IS_HIDDEN_WIRE, self.is_hidden);
        }
    }
};
pub const ChromeRendererSchedulerStateReader = struct {
    buf: gremlin.Reader,
    _rail_mode: ChromeRAILMode = @enumFromInt(0),
    _is_backgrounded: bool = false,
    _is_hidden: bool = false,
    pub fn init(src: []const u8) gremlin.Error!ChromeRendererSchedulerStateReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeRendererSchedulerStateReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeRendererSchedulerStateWire.RAIL_MODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._rail_mode = @enumFromInt(result.value);
                },
                ChromeRendererSchedulerStateWire.IS_BACKGROUNDED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._is_backgrounded = result.value;
                },
                ChromeRendererSchedulerStateWire.IS_HIDDEN_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._is_hidden = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getRailMode(self: *const ChromeRendererSchedulerStateReader) ChromeRAILMode {
        return self._rail_mode;
    }
    pub inline fn getIsBackgrounded(self: *const ChromeRendererSchedulerStateReader) bool {
        return self._is_backgrounded;
    }
    pub inline fn getIsHidden(self: *const ChromeRendererSchedulerStateReader) bool {
        return self._is_hidden;
    }
};
