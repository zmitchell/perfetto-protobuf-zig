// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const CounterDescriptorWire = struct {
    const TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const CATEGORIES_WIRE: gremlin.ProtoWireNumber = 2;
    const UNIT_WIRE: gremlin.ProtoWireNumber = 3;
    const UNIT_NAME_WIRE: gremlin.ProtoWireNumber = 6;
    const UNIT_MULTIPLIER_WIRE: gremlin.ProtoWireNumber = 4;
    const IS_INCREMENTAL_WIRE: gremlin.ProtoWireNumber = 5;
    const Y_AXIS_SHARE_KEY_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const CounterDescriptor = struct {
    // nested enums
    pub const BuiltinCounterType = enum(i32) {
        COUNTER_UNSPECIFIED = 0,
        COUNTER_THREAD_TIME_NS = 1,
        COUNTER_THREAD_INSTRUCTION_COUNT = 2,
    };
    pub const Unit = enum(i32) {
        UNIT_UNSPECIFIED = 0,
        UNIT_TIME_NS = 1,
        UNIT_COUNT = 2,
        UNIT_SIZE_BYTES = 3,
    };
    // fields
    type: CounterDescriptor.BuiltinCounterType = @enumFromInt(0),
    categories: ?[]const ?[]const u8 = null,
    unit: CounterDescriptor.Unit = @enumFromInt(0),
    unit_name: ?[]const u8 = null,
    unit_multiplier: i64 = 0,
    is_incremental: bool = false,
    y_axis_share_key: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CounterDescriptor) usize {
        var res: usize = 0;
        if (@intFromEnum(self.type) != 0) {
            res += gremlin.sizes.sizeWireNumber(CounterDescriptorWire.TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.type));
        }
        if (self.categories) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(CounterDescriptorWire.CATEGORIES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (@intFromEnum(self.unit) != 0) {
            res += gremlin.sizes.sizeWireNumber(CounterDescriptorWire.UNIT_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.unit));
        }
        if (self.unit_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CounterDescriptorWire.UNIT_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.unit_multiplier != 0) {
            res += gremlin.sizes.sizeWireNumber(CounterDescriptorWire.UNIT_MULTIPLIER_WIRE) + gremlin.sizes.sizeI64(self.unit_multiplier);
        }
        if (self.is_incremental != false) {
            res += gremlin.sizes.sizeWireNumber(CounterDescriptorWire.IS_INCREMENTAL_WIRE) + gremlin.sizes.sizeBool(self.is_incremental);
        }
        if (self.y_axis_share_key) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CounterDescriptorWire.Y_AXIS_SHARE_KEY_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CounterDescriptor, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CounterDescriptor, target: *gremlin.Writer) void {
        if (@intFromEnum(self.type) != 0) {
            target.appendInt32(CounterDescriptorWire.TYPE_WIRE, @intFromEnum(self.type));
        }
        if (self.categories) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(CounterDescriptorWire.CATEGORIES_WIRE, v);
                } else {
                    target.appendBytesTag(CounterDescriptorWire.CATEGORIES_WIRE, 0);
                }
            }
        }
        if (@intFromEnum(self.unit) != 0) {
            target.appendInt32(CounterDescriptorWire.UNIT_WIRE, @intFromEnum(self.unit));
        }
        if (self.unit_name) |v| {
            if (v.len > 0) {
                target.appendBytes(CounterDescriptorWire.UNIT_NAME_WIRE, v);
            }
        }
        if (self.unit_multiplier != 0) {
            target.appendInt64(CounterDescriptorWire.UNIT_MULTIPLIER_WIRE, self.unit_multiplier);
        }
        if (self.is_incremental != false) {
            target.appendBool(CounterDescriptorWire.IS_INCREMENTAL_WIRE, self.is_incremental);
        }
        if (self.y_axis_share_key) |v| {
            if (v.len > 0) {
                target.appendBytes(CounterDescriptorWire.Y_AXIS_SHARE_KEY_WIRE, v);
            }
        }
    }
};
pub const CounterDescriptorReader = struct {
    buf: gremlin.Reader,
    _type: CounterDescriptor.BuiltinCounterType = @enumFromInt(0),
    _categories_offset: ?usize = null,
    _categories_last_offset: ?usize = null,
    _categories_cnt: usize = 0,
    _unit: CounterDescriptor.Unit = @enumFromInt(0),
    _unit_name: ?[]const u8 = null,
    _unit_multiplier: i64 = 0,
    _is_incremental: bool = false,
    _y_axis_share_key: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CounterDescriptorReader {
        const buf = gremlin.Reader.init(src);
        var res = CounterDescriptorReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CounterDescriptorWire.TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._type = @enumFromInt(result.value);
                },
                CounterDescriptorWire.CATEGORIES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._categories_offset == null) {
                        res._categories_offset = offset - result.size;
                    }
                    res._categories_last_offset = offset;
                    res._categories_cnt += 1;
                },
                CounterDescriptorWire.UNIT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._unit = @enumFromInt(result.value);
                },
                CounterDescriptorWire.UNIT_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._unit_name = result.value;
                },
                CounterDescriptorWire.UNIT_MULTIPLIER_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._unit_multiplier = result.value;
                },
                CounterDescriptorWire.IS_INCREMENTAL_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._is_incremental = result.value;
                },
                CounterDescriptorWire.Y_AXIS_SHARE_KEY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._y_axis_share_key = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getType(self: *const CounterDescriptorReader) CounterDescriptor.BuiltinCounterType {
        return self._type;
    }
    pub fn categoriesCount(self: *const CounterDescriptorReader) usize {
        return self._categories_cnt;
    }
    pub fn categoriesNext(self: *CounterDescriptorReader) ?[]const u8 {
        if (self._categories_offset == null) return null;
        const current_offset = self._categories_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._categories_last_offset != null and current_offset >= self._categories_last_offset.?) {
            self._categories_offset = null;
            return result.value;
        }
        if (self._categories_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._categories_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == CounterDescriptorWire.CATEGORIES_WIRE) {
                self._categories_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._categories_offset = null;
        return result.value;
    }
    pub inline fn getUnit(self: *const CounterDescriptorReader) CounterDescriptor.Unit {
        return self._unit;
    }
    pub inline fn getUnitName(self: *const CounterDescriptorReader) []const u8 {
        return self._unit_name orelse &[_]u8{};
    }
    pub inline fn getUnitMultiplier(self: *const CounterDescriptorReader) i64 {
        return self._unit_multiplier;
    }
    pub inline fn getIsIncremental(self: *const CounterDescriptorReader) bool {
        return self._is_incremental;
    }
    pub inline fn getYAxisShareKey(self: *const CounterDescriptorReader) []const u8 {
        return self._y_axis_share_key orelse &[_]u8{};
    }
};
