// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ThreadDescriptorWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
    const TID_WIRE: gremlin.ProtoWireNumber = 2;
    const THREAD_NAME_WIRE: gremlin.ProtoWireNumber = 5;
    const CHROME_THREAD_TYPE_WIRE: gremlin.ProtoWireNumber = 4;
    const REFERENCE_TIMESTAMP_US_WIRE: gremlin.ProtoWireNumber = 6;
    const REFERENCE_THREAD_TIME_US_WIRE: gremlin.ProtoWireNumber = 7;
    const REFERENCE_THREAD_INSTRUCTION_COUNT_WIRE: gremlin.ProtoWireNumber = 8;
    const LEGACY_SORT_INDEX_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ThreadDescriptor = struct {
    // nested enums
    pub const ChromeThreadType = enum(i32) {
        CHROME_THREAD_UNSPECIFIED = 0,
        CHROME_THREAD_MAIN = 1,
        CHROME_THREAD_IO = 2,
        CHROME_THREAD_POOL_BG_WORKER = 3,
        CHROME_THREAD_POOL_FG_WORKER = 4,
        CHROME_THREAD_POOL_FB_BLOCKING = 5,
        CHROME_THREAD_POOL_BG_BLOCKING = 6,
        CHROME_THREAD_POOL_SERVICE = 7,
        CHROME_THREAD_COMPOSITOR = 8,
        CHROME_THREAD_VIZ_COMPOSITOR = 9,
        CHROME_THREAD_COMPOSITOR_WORKER = 10,
        CHROME_THREAD_SERVICE_WORKER = 11,
        CHROME_THREAD_MEMORY_INFRA = 50,
        CHROME_THREAD_SAMPLING_PROFILER = 51,
    };
    // fields
    pid: i32 = 0,
    tid: i32 = 0,
    thread_name: ?[]const u8 = null,
    chrome_thread_type: ThreadDescriptor.ChromeThreadType = @enumFromInt(0),
    reference_timestamp_us: i64 = 0,
    reference_thread_time_us: i64 = 0,
    reference_thread_instruction_count: i64 = 0,
    legacy_sort_index: i32 = 0,
    pub fn calcProtobufSize(self: *const ThreadDescriptor) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(ThreadDescriptorWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.tid != 0) {
            res += gremlin.sizes.sizeWireNumber(ThreadDescriptorWire.TID_WIRE) + gremlin.sizes.sizeI32(self.tid);
        }
        if (self.thread_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ThreadDescriptorWire.THREAD_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (@intFromEnum(self.chrome_thread_type) != 0) {
            res += gremlin.sizes.sizeWireNumber(ThreadDescriptorWire.CHROME_THREAD_TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.chrome_thread_type));
        }
        if (self.reference_timestamp_us != 0) {
            res += gremlin.sizes.sizeWireNumber(ThreadDescriptorWire.REFERENCE_TIMESTAMP_US_WIRE) + gremlin.sizes.sizeI64(self.reference_timestamp_us);
        }
        if (self.reference_thread_time_us != 0) {
            res += gremlin.sizes.sizeWireNumber(ThreadDescriptorWire.REFERENCE_THREAD_TIME_US_WIRE) + gremlin.sizes.sizeI64(self.reference_thread_time_us);
        }
        if (self.reference_thread_instruction_count != 0) {
            res += gremlin.sizes.sizeWireNumber(ThreadDescriptorWire.REFERENCE_THREAD_INSTRUCTION_COUNT_WIRE) + gremlin.sizes.sizeI64(self.reference_thread_instruction_count);
        }
        if (self.legacy_sort_index != 0) {
            res += gremlin.sizes.sizeWireNumber(ThreadDescriptorWire.LEGACY_SORT_INDEX_WIRE) + gremlin.sizes.sizeI32(self.legacy_sort_index);
        }
        return res;
    }
    pub fn encode(self: *const ThreadDescriptor, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ThreadDescriptor, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendInt32(ThreadDescriptorWire.PID_WIRE, self.pid);
        }
        if (self.tid != 0) {
            target.appendInt32(ThreadDescriptorWire.TID_WIRE, self.tid);
        }
        if (self.thread_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ThreadDescriptorWire.THREAD_NAME_WIRE, v);
            }
        }
        if (@intFromEnum(self.chrome_thread_type) != 0) {
            target.appendInt32(ThreadDescriptorWire.CHROME_THREAD_TYPE_WIRE, @intFromEnum(self.chrome_thread_type));
        }
        if (self.reference_timestamp_us != 0) {
            target.appendInt64(ThreadDescriptorWire.REFERENCE_TIMESTAMP_US_WIRE, self.reference_timestamp_us);
        }
        if (self.reference_thread_time_us != 0) {
            target.appendInt64(ThreadDescriptorWire.REFERENCE_THREAD_TIME_US_WIRE, self.reference_thread_time_us);
        }
        if (self.reference_thread_instruction_count != 0) {
            target.appendInt64(ThreadDescriptorWire.REFERENCE_THREAD_INSTRUCTION_COUNT_WIRE, self.reference_thread_instruction_count);
        }
        if (self.legacy_sort_index != 0) {
            target.appendInt32(ThreadDescriptorWire.LEGACY_SORT_INDEX_WIRE, self.legacy_sort_index);
        }
    }
};
pub const ThreadDescriptorReader = struct {
    buf: gremlin.Reader,
    _pid: i32 = 0,
    _tid: i32 = 0,
    _thread_name: ?[]const u8 = null,
    _chrome_thread_type: ThreadDescriptor.ChromeThreadType = @enumFromInt(0),
    _reference_timestamp_us: i64 = 0,
    _reference_thread_time_us: i64 = 0,
    _reference_thread_instruction_count: i64 = 0,
    _legacy_sort_index: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!ThreadDescriptorReader {
        const buf = gremlin.Reader.init(src);
        var res = ThreadDescriptorReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ThreadDescriptorWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                ThreadDescriptorWire.TID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._tid = result.value;
                },
                ThreadDescriptorWire.THREAD_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._thread_name = result.value;
                },
                ThreadDescriptorWire.CHROME_THREAD_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._chrome_thread_type = @enumFromInt(result.value);
                },
                ThreadDescriptorWire.REFERENCE_TIMESTAMP_US_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._reference_timestamp_us = result.value;
                },
                ThreadDescriptorWire.REFERENCE_THREAD_TIME_US_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._reference_thread_time_us = result.value;
                },
                ThreadDescriptorWire.REFERENCE_THREAD_INSTRUCTION_COUNT_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._reference_thread_instruction_count = result.value;
                },
                ThreadDescriptorWire.LEGACY_SORT_INDEX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._legacy_sort_index = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const ThreadDescriptorReader) i32 {
        return self._pid;
    }
    pub inline fn getTid(self: *const ThreadDescriptorReader) i32 {
        return self._tid;
    }
    pub inline fn getThreadName(self: *const ThreadDescriptorReader) []const u8 {
        return self._thread_name orelse &[_]u8{};
    }
    pub inline fn getChromeThreadType(self: *const ThreadDescriptorReader) ThreadDescriptor.ChromeThreadType {
        return self._chrome_thread_type;
    }
    pub inline fn getReferenceTimestampUs(self: *const ThreadDescriptorReader) i64 {
        return self._reference_timestamp_us;
    }
    pub inline fn getReferenceThreadTimeUs(self: *const ThreadDescriptorReader) i64 {
        return self._reference_thread_time_us;
    }
    pub inline fn getReferenceThreadInstructionCount(self: *const ThreadDescriptorReader) i64 {
        return self._reference_thread_instruction_count;
    }
    pub inline fn getLegacySortIndex(self: *const ThreadDescriptorReader) i32 {
        return self._legacy_sort_index;
    }
};
