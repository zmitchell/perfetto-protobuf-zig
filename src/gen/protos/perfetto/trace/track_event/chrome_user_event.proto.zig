// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeUserEventWire = struct {
    const ACTION_WIRE: gremlin.ProtoWireNumber = 1;
    const ACTION_HASH_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const ChromeUserEvent = struct {
    // fields
    action: ?[]const u8 = null,
    action_hash: u64 = 0,
    pub fn calcProtobufSize(self: *const ChromeUserEvent) usize {
        var res: usize = 0;
        if (self.action) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeUserEventWire.ACTION_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.action_hash != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeUserEventWire.ACTION_HASH_WIRE) + gremlin.sizes.sizeU64(self.action_hash);
        }
        return res;
    }
    pub fn encode(self: *const ChromeUserEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeUserEvent, target: *gremlin.Writer) void {
        if (self.action) |v| {
            if (v.len > 0) {
                target.appendBytes(ChromeUserEventWire.ACTION_WIRE, v);
            }
        }
        if (self.action_hash != 0) {
            target.appendUint64(ChromeUserEventWire.ACTION_HASH_WIRE, self.action_hash);
        }
    }
};
pub const ChromeUserEventReader = struct {
    buf: gremlin.Reader,
    _action: ?[]const u8 = null,
    _action_hash: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeUserEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeUserEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeUserEventWire.ACTION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._action = result.value;
                },
                ChromeUserEventWire.ACTION_HASH_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._action_hash = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getAction(self: *const ChromeUserEventReader) []const u8 {
        return self._action orelse &[_]u8{};
    }
    pub inline fn getActionHash(self: *const ChromeUserEventReader) u64 {
        return self._action_hash;
    }
};
