// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const HistogramNameWire = struct {
    const IID_WIRE: gremlin.ProtoWireNumber = 1;
    const NAME_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const HistogramName = struct {
    // fields
    iid: u64 = 0,
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const HistogramName) usize {
        var res: usize = 0;
        if (self.iid != 0) {
            res += gremlin.sizes.sizeWireNumber(HistogramNameWire.IID_WIRE) + gremlin.sizes.sizeU64(self.iid);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(HistogramNameWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const HistogramName, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const HistogramName, target: *gremlin.Writer) void {
        if (self.iid != 0) {
            target.appendUint64(HistogramNameWire.IID_WIRE, self.iid);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(HistogramNameWire.NAME_WIRE, v);
            }
        }
    }
};
pub const HistogramNameReader = struct {
    buf: gremlin.Reader,
    _iid: u64 = 0,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!HistogramNameReader {
        const buf = gremlin.Reader.init(src);
        var res = HistogramNameReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                HistogramNameWire.IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._iid = result.value;
                },
                HistogramNameWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIid(self: *const HistogramNameReader) u64 {
        return self._iid;
    }
    pub inline fn getName(self: *const HistogramNameReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const ChromeHistogramSampleWire = struct {
    const NAME_HASH_WIRE: gremlin.ProtoWireNumber = 1;
    const NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const SAMPLE_WIRE: gremlin.ProtoWireNumber = 3;
    const NAME_IID_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const ChromeHistogramSample = struct {
    // fields
    name_hash: u64 = 0,
    name: ?[]const u8 = null,
    sample: i64 = 0,
    name_iid: u64 = 0,
    pub fn calcProtobufSize(self: *const ChromeHistogramSample) usize {
        var res: usize = 0;
        if (self.name_hash != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeHistogramSampleWire.NAME_HASH_WIRE) + gremlin.sizes.sizeU64(self.name_hash);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeHistogramSampleWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.sample != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeHistogramSampleWire.SAMPLE_WIRE) + gremlin.sizes.sizeI64(self.sample);
        }
        if (self.name_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeHistogramSampleWire.NAME_IID_WIRE) + gremlin.sizes.sizeU64(self.name_iid);
        }
        return res;
    }
    pub fn encode(self: *const ChromeHistogramSample, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeHistogramSample, target: *gremlin.Writer) void {
        if (self.name_hash != 0) {
            target.appendUint64(ChromeHistogramSampleWire.NAME_HASH_WIRE, self.name_hash);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(ChromeHistogramSampleWire.NAME_WIRE, v);
            }
        }
        if (self.sample != 0) {
            target.appendInt64(ChromeHistogramSampleWire.SAMPLE_WIRE, self.sample);
        }
        if (self.name_iid != 0) {
            target.appendUint64(ChromeHistogramSampleWire.NAME_IID_WIRE, self.name_iid);
        }
    }
};
pub const ChromeHistogramSampleReader = struct {
    buf: gremlin.Reader,
    _name_hash: u64 = 0,
    _name: ?[]const u8 = null,
    _sample: i64 = 0,
    _name_iid: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeHistogramSampleReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeHistogramSampleReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeHistogramSampleWire.NAME_HASH_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._name_hash = result.value;
                },
                ChromeHistogramSampleWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                ChromeHistogramSampleWire.SAMPLE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._sample = result.value;
                },
                ChromeHistogramSampleWire.NAME_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._name_iid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getNameHash(self: *const ChromeHistogramSampleReader) u64 {
        return self._name_hash;
    }
    pub inline fn getName(self: *const ChromeHistogramSampleReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getSample(self: *const ChromeHistogramSampleReader) i64 {
        return self._sample;
    }
    pub inline fn getNameIid(self: *const ChromeHistogramSampleReader) u64 {
        return self._name_iid;
    }
};
