// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TaskExecutionWire = struct {
    const POSTED_FROM_IID_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const TaskExecution = struct {
    // fields
    posted_from_iid: u64 = 0,
    pub fn calcProtobufSize(self: *const TaskExecution) usize {
        var res: usize = 0;
        if (self.posted_from_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(TaskExecutionWire.POSTED_FROM_IID_WIRE) + gremlin.sizes.sizeU64(self.posted_from_iid);
        }
        return res;
    }
    pub fn encode(self: *const TaskExecution, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TaskExecution, target: *gremlin.Writer) void {
        if (self.posted_from_iid != 0) {
            target.appendUint64(TaskExecutionWire.POSTED_FROM_IID_WIRE, self.posted_from_iid);
        }
    }
};
pub const TaskExecutionReader = struct {
    buf: gremlin.Reader,
    _posted_from_iid: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!TaskExecutionReader {
        const buf = gremlin.Reader.init(src);
        var res = TaskExecutionReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TaskExecutionWire.POSTED_FROM_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._posted_from_iid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPostedFromIid(self: *const TaskExecutionReader) u64 {
        return self._posted_from_iid;
    }
};
