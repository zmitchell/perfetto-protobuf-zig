// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeMessagePumpWire = struct {
    const SENT_MESSAGES_IN_QUEUE_WIRE: gremlin.ProtoWireNumber = 1;
    const IO_HANDLER_LOCATION_IID_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const ChromeMessagePump = struct {
    // fields
    sent_messages_in_queue: bool = false,
    io_handler_location_iid: u64 = 0,
    pub fn calcProtobufSize(self: *const ChromeMessagePump) usize {
        var res: usize = 0;
        if (self.sent_messages_in_queue != false) {
            res += gremlin.sizes.sizeWireNumber(ChromeMessagePumpWire.SENT_MESSAGES_IN_QUEUE_WIRE) + gremlin.sizes.sizeBool(self.sent_messages_in_queue);
        }
        if (self.io_handler_location_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeMessagePumpWire.IO_HANDLER_LOCATION_IID_WIRE) + gremlin.sizes.sizeU64(self.io_handler_location_iid);
        }
        return res;
    }
    pub fn encode(self: *const ChromeMessagePump, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeMessagePump, target: *gremlin.Writer) void {
        if (self.sent_messages_in_queue != false) {
            target.appendBool(ChromeMessagePumpWire.SENT_MESSAGES_IN_QUEUE_WIRE, self.sent_messages_in_queue);
        }
        if (self.io_handler_location_iid != 0) {
            target.appendUint64(ChromeMessagePumpWire.IO_HANDLER_LOCATION_IID_WIRE, self.io_handler_location_iid);
        }
    }
};
pub const ChromeMessagePumpReader = struct {
    buf: gremlin.Reader,
    _sent_messages_in_queue: bool = false,
    _io_handler_location_iid: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeMessagePumpReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeMessagePumpReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeMessagePumpWire.SENT_MESSAGES_IN_QUEUE_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._sent_messages_in_queue = result.value;
                },
                ChromeMessagePumpWire.IO_HANDLER_LOCATION_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._io_handler_location_iid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getSentMessagesInQueue(self: *const ChromeMessagePumpReader) bool {
        return self._sent_messages_in_queue;
    }
    pub inline fn getIoHandlerLocationIid(self: *const ChromeMessagePumpReader) u64 {
        return self._io_handler_location_iid;
    }
};
