// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeProcessDescriptorWire = struct {
    const PROCESS_TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const PROCESS_PRIORITY_WIRE: gremlin.ProtoWireNumber = 2;
    const LEGACY_SORT_INDEX_WIRE: gremlin.ProtoWireNumber = 3;
    const HOST_APP_PACKAGE_NAME_WIRE: gremlin.ProtoWireNumber = 4;
    const CRASH_TRACE_ID_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const ChromeProcessDescriptor = struct {
    // fields
    process_type: i32 = 0,
    process_priority: i32 = 0,
    legacy_sort_index: i32 = 0,
    host_app_package_name: ?[]const u8 = null,
    crash_trace_id: u64 = 0,
    pub fn calcProtobufSize(self: *const ChromeProcessDescriptor) usize {
        var res: usize = 0;
        if (self.process_type != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeProcessDescriptorWire.PROCESS_TYPE_WIRE) + gremlin.sizes.sizeI32(self.process_type);
        }
        if (self.process_priority != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeProcessDescriptorWire.PROCESS_PRIORITY_WIRE) + gremlin.sizes.sizeI32(self.process_priority);
        }
        if (self.legacy_sort_index != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeProcessDescriptorWire.LEGACY_SORT_INDEX_WIRE) + gremlin.sizes.sizeI32(self.legacy_sort_index);
        }
        if (self.host_app_package_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeProcessDescriptorWire.HOST_APP_PACKAGE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.crash_trace_id != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeProcessDescriptorWire.CRASH_TRACE_ID_WIRE) + gremlin.sizes.sizeU64(self.crash_trace_id);
        }
        return res;
    }
    pub fn encode(self: *const ChromeProcessDescriptor, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeProcessDescriptor, target: *gremlin.Writer) void {
        if (self.process_type != 0) {
            target.appendInt32(ChromeProcessDescriptorWire.PROCESS_TYPE_WIRE, self.process_type);
        }
        if (self.process_priority != 0) {
            target.appendInt32(ChromeProcessDescriptorWire.PROCESS_PRIORITY_WIRE, self.process_priority);
        }
        if (self.legacy_sort_index != 0) {
            target.appendInt32(ChromeProcessDescriptorWire.LEGACY_SORT_INDEX_WIRE, self.legacy_sort_index);
        }
        if (self.host_app_package_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ChromeProcessDescriptorWire.HOST_APP_PACKAGE_NAME_WIRE, v);
            }
        }
        if (self.crash_trace_id != 0) {
            target.appendUint64(ChromeProcessDescriptorWire.CRASH_TRACE_ID_WIRE, self.crash_trace_id);
        }
    }
};
pub const ChromeProcessDescriptorReader = struct {
    buf: gremlin.Reader,
    _process_type: i32 = 0,
    _process_priority: i32 = 0,
    _legacy_sort_index: i32 = 0,
    _host_app_package_name: ?[]const u8 = null,
    _crash_trace_id: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeProcessDescriptorReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeProcessDescriptorReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeProcessDescriptorWire.PROCESS_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._process_type = result.value;
                },
                ChromeProcessDescriptorWire.PROCESS_PRIORITY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._process_priority = result.value;
                },
                ChromeProcessDescriptorWire.LEGACY_SORT_INDEX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._legacy_sort_index = result.value;
                },
                ChromeProcessDescriptorWire.HOST_APP_PACKAGE_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._host_app_package_name = result.value;
                },
                ChromeProcessDescriptorWire.CRASH_TRACE_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._crash_trace_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getProcessType(self: *const ChromeProcessDescriptorReader) i32 {
        return self._process_type;
    }
    pub inline fn getProcessPriority(self: *const ChromeProcessDescriptorReader) i32 {
        return self._process_priority;
    }
    pub inline fn getLegacySortIndex(self: *const ChromeProcessDescriptorReader) i32 {
        return self._legacy_sort_index;
    }
    pub inline fn getHostAppPackageName(self: *const ChromeProcessDescriptorReader) []const u8 {
        return self._host_app_package_name orelse &[_]u8{};
    }
    pub inline fn getCrashTraceId(self: *const ChromeProcessDescriptorReader) u64 {
        return self._crash_trace_id;
    }
};
