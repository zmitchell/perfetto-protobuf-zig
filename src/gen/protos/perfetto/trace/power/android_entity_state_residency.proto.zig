// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const EntityStateResidencyWire = struct {
    const POWER_ENTITY_STATE_WIRE: gremlin.ProtoWireNumber = 1;
    const RESIDENCY_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const EntityStateResidency = struct {
    // nested structs
    const PowerEntityStateWire = struct {
        const ENTITY_INDEX_WIRE: gremlin.ProtoWireNumber = 1;
        const STATE_INDEX_WIRE: gremlin.ProtoWireNumber = 2;
        const ENTITY_NAME_WIRE: gremlin.ProtoWireNumber = 3;
        const STATE_NAME_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const PowerEntityState = struct {
        // fields
        entity_index: i32 = 0,
        state_index: i32 = 0,
        entity_name: ?[]const u8 = null,
        state_name: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const EntityStateResidency.PowerEntityState) usize {
            var res: usize = 0;
            if (self.entity_index != 0) {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidency.PowerEntityStateWire.ENTITY_INDEX_WIRE) + gremlin.sizes.sizeI32(self.entity_index);
            }
            if (self.state_index != 0) {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidency.PowerEntityStateWire.STATE_INDEX_WIRE) + gremlin.sizes.sizeI32(self.state_index);
            }
            if (self.entity_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(EntityStateResidency.PowerEntityStateWire.ENTITY_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.state_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(EntityStateResidency.PowerEntityStateWire.STATE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const EntityStateResidency.PowerEntityState, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const EntityStateResidency.PowerEntityState, target: *gremlin.Writer) void {
            if (self.entity_index != 0) {
                target.appendInt32(EntityStateResidency.PowerEntityStateWire.ENTITY_INDEX_WIRE, self.entity_index);
            }
            if (self.state_index != 0) {
                target.appendInt32(EntityStateResidency.PowerEntityStateWire.STATE_INDEX_WIRE, self.state_index);
            }
            if (self.entity_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(EntityStateResidency.PowerEntityStateWire.ENTITY_NAME_WIRE, v);
                }
            }
            if (self.state_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(EntityStateResidency.PowerEntityStateWire.STATE_NAME_WIRE, v);
                }
            }
        }
    };
    pub const PowerEntityStateReader = struct {
        buf: gremlin.Reader,
        _entity_index: i32 = 0,
        _state_index: i32 = 0,
        _entity_name: ?[]const u8 = null,
        _state_name: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!EntityStateResidency.PowerEntityStateReader {
            const buf = gremlin.Reader.init(src);
            var res = EntityStateResidency.PowerEntityStateReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    EntityStateResidency.PowerEntityStateWire.ENTITY_INDEX_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._entity_index = result.value;
                    },
                    EntityStateResidency.PowerEntityStateWire.STATE_INDEX_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._state_index = result.value;
                    },
                    EntityStateResidency.PowerEntityStateWire.ENTITY_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._entity_name = result.value;
                    },
                    EntityStateResidency.PowerEntityStateWire.STATE_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._state_name = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getEntityIndex(self: *const EntityStateResidency.PowerEntityStateReader) i32 {
            return self._entity_index;
        }
        pub inline fn getStateIndex(self: *const EntityStateResidency.PowerEntityStateReader) i32 {
            return self._state_index;
        }
        pub inline fn getEntityName(self: *const EntityStateResidency.PowerEntityStateReader) []const u8 {
            return self._entity_name orelse &[_]u8{};
        }
        pub inline fn getStateName(self: *const EntityStateResidency.PowerEntityStateReader) []const u8 {
            return self._state_name orelse &[_]u8{};
        }
    };
    const StateResidencyWire = struct {
        const ENTITY_INDEX_WIRE: gremlin.ProtoWireNumber = 1;
        const STATE_INDEX_WIRE: gremlin.ProtoWireNumber = 2;
        const TOTAL_TIME_IN_STATE_MS_WIRE: gremlin.ProtoWireNumber = 3;
        const TOTAL_STATE_ENTRY_COUNT_WIRE: gremlin.ProtoWireNumber = 4;
        const LAST_ENTRY_TIMESTAMP_MS_WIRE: gremlin.ProtoWireNumber = 5;
    };
    pub const StateResidency = struct {
        // fields
        entity_index: i32 = 0,
        state_index: i32 = 0,
        total_time_in_state_ms: u64 = 0,
        total_state_entry_count: u64 = 0,
        last_entry_timestamp_ms: u64 = 0,
        pub fn calcProtobufSize(self: *const EntityStateResidency.StateResidency) usize {
            var res: usize = 0;
            if (self.entity_index != 0) {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidency.StateResidencyWire.ENTITY_INDEX_WIRE) + gremlin.sizes.sizeI32(self.entity_index);
            }
            if (self.state_index != 0) {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidency.StateResidencyWire.STATE_INDEX_WIRE) + gremlin.sizes.sizeI32(self.state_index);
            }
            if (self.total_time_in_state_ms != 0) {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidency.StateResidencyWire.TOTAL_TIME_IN_STATE_MS_WIRE) + gremlin.sizes.sizeU64(self.total_time_in_state_ms);
            }
            if (self.total_state_entry_count != 0) {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidency.StateResidencyWire.TOTAL_STATE_ENTRY_COUNT_WIRE) + gremlin.sizes.sizeU64(self.total_state_entry_count);
            }
            if (self.last_entry_timestamp_ms != 0) {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidency.StateResidencyWire.LAST_ENTRY_TIMESTAMP_MS_WIRE) + gremlin.sizes.sizeU64(self.last_entry_timestamp_ms);
            }
            return res;
        }
        pub fn encode(self: *const EntityStateResidency.StateResidency, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const EntityStateResidency.StateResidency, target: *gremlin.Writer) void {
            if (self.entity_index != 0) {
                target.appendInt32(EntityStateResidency.StateResidencyWire.ENTITY_INDEX_WIRE, self.entity_index);
            }
            if (self.state_index != 0) {
                target.appendInt32(EntityStateResidency.StateResidencyWire.STATE_INDEX_WIRE, self.state_index);
            }
            if (self.total_time_in_state_ms != 0) {
                target.appendUint64(EntityStateResidency.StateResidencyWire.TOTAL_TIME_IN_STATE_MS_WIRE, self.total_time_in_state_ms);
            }
            if (self.total_state_entry_count != 0) {
                target.appendUint64(EntityStateResidency.StateResidencyWire.TOTAL_STATE_ENTRY_COUNT_WIRE, self.total_state_entry_count);
            }
            if (self.last_entry_timestamp_ms != 0) {
                target.appendUint64(EntityStateResidency.StateResidencyWire.LAST_ENTRY_TIMESTAMP_MS_WIRE, self.last_entry_timestamp_ms);
            }
        }
    };
    pub const StateResidencyReader = struct {
        buf: gremlin.Reader,
        _entity_index: i32 = 0,
        _state_index: i32 = 0,
        _total_time_in_state_ms: u64 = 0,
        _total_state_entry_count: u64 = 0,
        _last_entry_timestamp_ms: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!EntityStateResidency.StateResidencyReader {
            const buf = gremlin.Reader.init(src);
            var res = EntityStateResidency.StateResidencyReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    EntityStateResidency.StateResidencyWire.ENTITY_INDEX_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._entity_index = result.value;
                    },
                    EntityStateResidency.StateResidencyWire.STATE_INDEX_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._state_index = result.value;
                    },
                    EntityStateResidency.StateResidencyWire.TOTAL_TIME_IN_STATE_MS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._total_time_in_state_ms = result.value;
                    },
                    EntityStateResidency.StateResidencyWire.TOTAL_STATE_ENTRY_COUNT_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._total_state_entry_count = result.value;
                    },
                    EntityStateResidency.StateResidencyWire.LAST_ENTRY_TIMESTAMP_MS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._last_entry_timestamp_ms = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getEntityIndex(self: *const EntityStateResidency.StateResidencyReader) i32 {
            return self._entity_index;
        }
        pub inline fn getStateIndex(self: *const EntityStateResidency.StateResidencyReader) i32 {
            return self._state_index;
        }
        pub inline fn getTotalTimeInStateMs(self: *const EntityStateResidency.StateResidencyReader) u64 {
            return self._total_time_in_state_ms;
        }
        pub inline fn getTotalStateEntryCount(self: *const EntityStateResidency.StateResidencyReader) u64 {
            return self._total_state_entry_count;
        }
        pub inline fn getLastEntryTimestampMs(self: *const EntityStateResidency.StateResidencyReader) u64 {
            return self._last_entry_timestamp_ms;
        }
    };
    // fields
    power_entity_state: ?[]const ?EntityStateResidency.PowerEntityState = null,
    residency: ?[]const ?EntityStateResidency.StateResidency = null,
    pub fn calcProtobufSize(self: *const EntityStateResidency) usize {
        var res: usize = 0;
        if (self.power_entity_state) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidencyWire.POWER_ENTITY_STATE_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.residency) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(EntityStateResidencyWire.RESIDENCY_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const EntityStateResidency, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const EntityStateResidency, target: *gremlin.Writer) void {
        if (self.power_entity_state) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(EntityStateResidencyWire.POWER_ENTITY_STATE_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(EntityStateResidencyWire.POWER_ENTITY_STATE_WIRE, 0);
                }
            }
        }
        if (self.residency) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(EntityStateResidencyWire.RESIDENCY_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(EntityStateResidencyWire.RESIDENCY_WIRE, 0);
                }
            }
        }
    }
};
pub const EntityStateResidencyReader = struct {
    buf: gremlin.Reader,
    _power_entity_state_offset: ?usize = null,
    _power_entity_state_last_offset: ?usize = null,
    _power_entity_state_cnt: usize = 0,
    _residency_offset: ?usize = null,
    _residency_last_offset: ?usize = null,
    _residency_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!EntityStateResidencyReader {
        const buf = gremlin.Reader.init(src);
        var res = EntityStateResidencyReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                EntityStateResidencyWire.POWER_ENTITY_STATE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._power_entity_state_offset == null) {
                        res._power_entity_state_offset = offset - result.size;
                    }
                    res._power_entity_state_last_offset = offset;
                    res._power_entity_state_cnt += 1;
                },
                EntityStateResidencyWire.RESIDENCY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._residency_offset == null) {
                        res._residency_offset = offset - result.size;
                    }
                    res._residency_last_offset = offset;
                    res._residency_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn powerEntityStateCount(self: *const EntityStateResidencyReader) usize {
        return self._power_entity_state_cnt;
    }
    pub fn powerEntityStateNext(self: *EntityStateResidencyReader) ?EntityStateResidency.PowerEntityStateReader {
        if (self._power_entity_state_offset == null) return null;
        const current_offset = self._power_entity_state_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = EntityStateResidency.PowerEntityStateReader.init(result.value) catch return null;
        if (self._power_entity_state_last_offset != null and current_offset >= self._power_entity_state_last_offset.?) {
            self._power_entity_state_offset = null;
            return msg;
        }
        if (self._power_entity_state_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._power_entity_state_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == EntityStateResidencyWire.POWER_ENTITY_STATE_WIRE) {
                self._power_entity_state_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._power_entity_state_offset = null;
        return msg;
    }
    pub fn residencyCount(self: *const EntityStateResidencyReader) usize {
        return self._residency_cnt;
    }
    pub fn residencyNext(self: *EntityStateResidencyReader) ?EntityStateResidency.StateResidencyReader {
        if (self._residency_offset == null) return null;
        const current_offset = self._residency_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = EntityStateResidency.StateResidencyReader.init(result.value) catch return null;
        if (self._residency_last_offset != null and current_offset >= self._residency_last_offset.?) {
            self._residency_offset = null;
            return msg;
        }
        if (self._residency_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._residency_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == EntityStateResidencyWire.RESIDENCY_WIRE) {
                self._residency_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._residency_offset = null;
        return msg;
    }
};
