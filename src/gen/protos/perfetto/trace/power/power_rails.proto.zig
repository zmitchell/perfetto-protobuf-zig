// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PowerRailsWire = struct {
    const RAIL_DESCRIPTOR_WIRE: gremlin.ProtoWireNumber = 1;
    const ENERGY_DATA_WIRE: gremlin.ProtoWireNumber = 2;
    const SESSION_UUID_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const PowerRails = struct {
    // nested structs
    const RailDescriptorWire = struct {
        const INDEX_WIRE: gremlin.ProtoWireNumber = 1;
        const RAIL_NAME_WIRE: gremlin.ProtoWireNumber = 2;
        const SUBSYS_NAME_WIRE: gremlin.ProtoWireNumber = 3;
        const SAMPLING_RATE_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const RailDescriptor = struct {
        // fields
        index: u32 = 0,
        rail_name: ?[]const u8 = null,
        subsys_name: ?[]const u8 = null,
        sampling_rate: u32 = 0,
        pub fn calcProtobufSize(self: *const PowerRails.RailDescriptor) usize {
            var res: usize = 0;
            if (self.index != 0) {
                res += gremlin.sizes.sizeWireNumber(PowerRails.RailDescriptorWire.INDEX_WIRE) + gremlin.sizes.sizeU32(self.index);
            }
            if (self.rail_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(PowerRails.RailDescriptorWire.RAIL_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.subsys_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(PowerRails.RailDescriptorWire.SUBSYS_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.sampling_rate != 0) {
                res += gremlin.sizes.sizeWireNumber(PowerRails.RailDescriptorWire.SAMPLING_RATE_WIRE) + gremlin.sizes.sizeU32(self.sampling_rate);
            }
            return res;
        }
        pub fn encode(self: *const PowerRails.RailDescriptor, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const PowerRails.RailDescriptor, target: *gremlin.Writer) void {
            if (self.index != 0) {
                target.appendUint32(PowerRails.RailDescriptorWire.INDEX_WIRE, self.index);
            }
            if (self.rail_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(PowerRails.RailDescriptorWire.RAIL_NAME_WIRE, v);
                }
            }
            if (self.subsys_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(PowerRails.RailDescriptorWire.SUBSYS_NAME_WIRE, v);
                }
            }
            if (self.sampling_rate != 0) {
                target.appendUint32(PowerRails.RailDescriptorWire.SAMPLING_RATE_WIRE, self.sampling_rate);
            }
        }
    };
    pub const RailDescriptorReader = struct {
        buf: gremlin.Reader,
        _index: u32 = 0,
        _rail_name: ?[]const u8 = null,
        _subsys_name: ?[]const u8 = null,
        _sampling_rate: u32 = 0,
        pub fn init(src: []const u8) gremlin.Error!PowerRails.RailDescriptorReader {
            const buf = gremlin.Reader.init(src);
            var res = PowerRails.RailDescriptorReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    PowerRails.RailDescriptorWire.INDEX_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._index = result.value;
                    },
                    PowerRails.RailDescriptorWire.RAIL_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._rail_name = result.value;
                    },
                    PowerRails.RailDescriptorWire.SUBSYS_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._subsys_name = result.value;
                    },
                    PowerRails.RailDescriptorWire.SAMPLING_RATE_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._sampling_rate = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getIndex(self: *const PowerRails.RailDescriptorReader) u32 {
            return self._index;
        }
        pub inline fn getRailName(self: *const PowerRails.RailDescriptorReader) []const u8 {
            return self._rail_name orelse &[_]u8{};
        }
        pub inline fn getSubsysName(self: *const PowerRails.RailDescriptorReader) []const u8 {
            return self._subsys_name orelse &[_]u8{};
        }
        pub inline fn getSamplingRate(self: *const PowerRails.RailDescriptorReader) u32 {
            return self._sampling_rate;
        }
    };
    const EnergyDataWire = struct {
        const INDEX_WIRE: gremlin.ProtoWireNumber = 1;
        const TIMESTAMP_MS_WIRE: gremlin.ProtoWireNumber = 2;
        const ENERGY_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const EnergyData = struct {
        // fields
        index: u32 = 0,
        timestamp_ms: u64 = 0,
        energy: u64 = 0,
        pub fn calcProtobufSize(self: *const PowerRails.EnergyData) usize {
            var res: usize = 0;
            if (self.index != 0) {
                res += gremlin.sizes.sizeWireNumber(PowerRails.EnergyDataWire.INDEX_WIRE) + gremlin.sizes.sizeU32(self.index);
            }
            if (self.timestamp_ms != 0) {
                res += gremlin.sizes.sizeWireNumber(PowerRails.EnergyDataWire.TIMESTAMP_MS_WIRE) + gremlin.sizes.sizeU64(self.timestamp_ms);
            }
            if (self.energy != 0) {
                res += gremlin.sizes.sizeWireNumber(PowerRails.EnergyDataWire.ENERGY_WIRE) + gremlin.sizes.sizeU64(self.energy);
            }
            return res;
        }
        pub fn encode(self: *const PowerRails.EnergyData, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const PowerRails.EnergyData, target: *gremlin.Writer) void {
            if (self.index != 0) {
                target.appendUint32(PowerRails.EnergyDataWire.INDEX_WIRE, self.index);
            }
            if (self.timestamp_ms != 0) {
                target.appendUint64(PowerRails.EnergyDataWire.TIMESTAMP_MS_WIRE, self.timestamp_ms);
            }
            if (self.energy != 0) {
                target.appendUint64(PowerRails.EnergyDataWire.ENERGY_WIRE, self.energy);
            }
        }
    };
    pub const EnergyDataReader = struct {
        buf: gremlin.Reader,
        _index: u32 = 0,
        _timestamp_ms: u64 = 0,
        _energy: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!PowerRails.EnergyDataReader {
            const buf = gremlin.Reader.init(src);
            var res = PowerRails.EnergyDataReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    PowerRails.EnergyDataWire.INDEX_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._index = result.value;
                    },
                    PowerRails.EnergyDataWire.TIMESTAMP_MS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._timestamp_ms = result.value;
                    },
                    PowerRails.EnergyDataWire.ENERGY_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._energy = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getIndex(self: *const PowerRails.EnergyDataReader) u32 {
            return self._index;
        }
        pub inline fn getTimestampMs(self: *const PowerRails.EnergyDataReader) u64 {
            return self._timestamp_ms;
        }
        pub inline fn getEnergy(self: *const PowerRails.EnergyDataReader) u64 {
            return self._energy;
        }
    };
    // fields
    rail_descriptor: ?[]const ?PowerRails.RailDescriptor = null,
    energy_data: ?[]const ?PowerRails.EnergyData = null,
    session_uuid: u64 = 0,
    pub fn calcProtobufSize(self: *const PowerRails) usize {
        var res: usize = 0;
        if (self.rail_descriptor) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(PowerRailsWire.RAIL_DESCRIPTOR_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.energy_data) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(PowerRailsWire.ENERGY_DATA_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.session_uuid != 0) {
            res += gremlin.sizes.sizeWireNumber(PowerRailsWire.SESSION_UUID_WIRE) + gremlin.sizes.sizeU64(self.session_uuid);
        }
        return res;
    }
    pub fn encode(self: *const PowerRails, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PowerRails, target: *gremlin.Writer) void {
        if (self.rail_descriptor) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(PowerRailsWire.RAIL_DESCRIPTOR_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(PowerRailsWire.RAIL_DESCRIPTOR_WIRE, 0);
                }
            }
        }
        if (self.energy_data) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(PowerRailsWire.ENERGY_DATA_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(PowerRailsWire.ENERGY_DATA_WIRE, 0);
                }
            }
        }
        if (self.session_uuid != 0) {
            target.appendUint64(PowerRailsWire.SESSION_UUID_WIRE, self.session_uuid);
        }
    }
};
pub const PowerRailsReader = struct {
    buf: gremlin.Reader,
    _rail_descriptor_offset: ?usize = null,
    _rail_descriptor_last_offset: ?usize = null,
    _rail_descriptor_cnt: usize = 0,
    _energy_data_offset: ?usize = null,
    _energy_data_last_offset: ?usize = null,
    _energy_data_cnt: usize = 0,
    _session_uuid: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!PowerRailsReader {
        const buf = gremlin.Reader.init(src);
        var res = PowerRailsReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PowerRailsWire.RAIL_DESCRIPTOR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._rail_descriptor_offset == null) {
                        res._rail_descriptor_offset = offset - result.size;
                    }
                    res._rail_descriptor_last_offset = offset;
                    res._rail_descriptor_cnt += 1;
                },
                PowerRailsWire.ENERGY_DATA_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._energy_data_offset == null) {
                        res._energy_data_offset = offset - result.size;
                    }
                    res._energy_data_last_offset = offset;
                    res._energy_data_cnt += 1;
                },
                PowerRailsWire.SESSION_UUID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._session_uuid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn railDescriptorCount(self: *const PowerRailsReader) usize {
        return self._rail_descriptor_cnt;
    }
    pub fn railDescriptorNext(self: *PowerRailsReader) ?PowerRails.RailDescriptorReader {
        if (self._rail_descriptor_offset == null) return null;
        const current_offset = self._rail_descriptor_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = PowerRails.RailDescriptorReader.init(result.value) catch return null;
        if (self._rail_descriptor_last_offset != null and current_offset >= self._rail_descriptor_last_offset.?) {
            self._rail_descriptor_offset = null;
            return msg;
        }
        if (self._rail_descriptor_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._rail_descriptor_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == PowerRailsWire.RAIL_DESCRIPTOR_WIRE) {
                self._rail_descriptor_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._rail_descriptor_offset = null;
        return msg;
    }
    pub fn energyDataCount(self: *const PowerRailsReader) usize {
        return self._energy_data_cnt;
    }
    pub fn energyDataNext(self: *PowerRailsReader) ?PowerRails.EnergyDataReader {
        if (self._energy_data_offset == null) return null;
        const current_offset = self._energy_data_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = PowerRails.EnergyDataReader.init(result.value) catch return null;
        if (self._energy_data_last_offset != null and current_offset >= self._energy_data_last_offset.?) {
            self._energy_data_offset = null;
            return msg;
        }
        if (self._energy_data_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._energy_data_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == PowerRailsWire.ENERGY_DATA_WIRE) {
                self._energy_data_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._energy_data_offset = null;
        return msg;
    }
    pub inline fn getSessionUuid(self: *const PowerRailsReader) u64 {
        return self._session_uuid;
    }
};
