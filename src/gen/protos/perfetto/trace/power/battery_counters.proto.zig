// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const BatteryCountersWire = struct {
    const CHARGE_COUNTER_UAH_WIRE: gremlin.ProtoWireNumber = 1;
    const CAPACITY_PERCENT_WIRE: gremlin.ProtoWireNumber = 2;
    const CURRENT_UA_WIRE: gremlin.ProtoWireNumber = 3;
    const CURRENT_AVG_UA_WIRE: gremlin.ProtoWireNumber = 4;
    const NAME_WIRE: gremlin.ProtoWireNumber = 5;
    const ENERGY_COUNTER_UWH_WIRE: gremlin.ProtoWireNumber = 6;
    const VOLTAGE_UV_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const BatteryCounters = struct {
    // fields
    charge_counter_uah: i64 = 0,
    capacity_percent: f32 = 0.0,
    current_ua: i64 = 0,
    current_avg_ua: i64 = 0,
    name: ?[]const u8 = null,
    energy_counter_uwh: i64 = 0,
    voltage_uv: i64 = 0,
    pub fn calcProtobufSize(self: *const BatteryCounters) usize {
        var res: usize = 0;
        if (self.charge_counter_uah != 0) {
            res += gremlin.sizes.sizeWireNumber(BatteryCountersWire.CHARGE_COUNTER_UAH_WIRE) + gremlin.sizes.sizeI64(self.charge_counter_uah);
        }
        if (self.capacity_percent != 0.0) {
            res += gremlin.sizes.sizeWireNumber(BatteryCountersWire.CAPACITY_PERCENT_WIRE) + gremlin.sizes.sizeFloat(self.capacity_percent);
        }
        if (self.current_ua != 0) {
            res += gremlin.sizes.sizeWireNumber(BatteryCountersWire.CURRENT_UA_WIRE) + gremlin.sizes.sizeI64(self.current_ua);
        }
        if (self.current_avg_ua != 0) {
            res += gremlin.sizes.sizeWireNumber(BatteryCountersWire.CURRENT_AVG_UA_WIRE) + gremlin.sizes.sizeI64(self.current_avg_ua);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(BatteryCountersWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.energy_counter_uwh != 0) {
            res += gremlin.sizes.sizeWireNumber(BatteryCountersWire.ENERGY_COUNTER_UWH_WIRE) + gremlin.sizes.sizeI64(self.energy_counter_uwh);
        }
        if (self.voltage_uv != 0) {
            res += gremlin.sizes.sizeWireNumber(BatteryCountersWire.VOLTAGE_UV_WIRE) + gremlin.sizes.sizeI64(self.voltage_uv);
        }
        return res;
    }
    pub fn encode(self: *const BatteryCounters, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BatteryCounters, target: *gremlin.Writer) void {
        if (self.charge_counter_uah != 0) {
            target.appendInt64(BatteryCountersWire.CHARGE_COUNTER_UAH_WIRE, self.charge_counter_uah);
        }
        if (self.capacity_percent != 0.0) {
            target.appendFloat32(BatteryCountersWire.CAPACITY_PERCENT_WIRE, self.capacity_percent);
        }
        if (self.current_ua != 0) {
            target.appendInt64(BatteryCountersWire.CURRENT_UA_WIRE, self.current_ua);
        }
        if (self.current_avg_ua != 0) {
            target.appendInt64(BatteryCountersWire.CURRENT_AVG_UA_WIRE, self.current_avg_ua);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(BatteryCountersWire.NAME_WIRE, v);
            }
        }
        if (self.energy_counter_uwh != 0) {
            target.appendInt64(BatteryCountersWire.ENERGY_COUNTER_UWH_WIRE, self.energy_counter_uwh);
        }
        if (self.voltage_uv != 0) {
            target.appendInt64(BatteryCountersWire.VOLTAGE_UV_WIRE, self.voltage_uv);
        }
    }
};
pub const BatteryCountersReader = struct {
    buf: gremlin.Reader,
    _charge_counter_uah: i64 = 0,
    _capacity_percent: f32 = 0.0,
    _current_ua: i64 = 0,
    _current_avg_ua: i64 = 0,
    _name: ?[]const u8 = null,
    _energy_counter_uwh: i64 = 0,
    _voltage_uv: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!BatteryCountersReader {
        const buf = gremlin.Reader.init(src);
        var res = BatteryCountersReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BatteryCountersWire.CHARGE_COUNTER_UAH_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._charge_counter_uah = result.value;
                },
                BatteryCountersWire.CAPACITY_PERCENT_WIRE => {
                    const result = try buf.readFloat32(offset);
                    offset += result.size;
                    res._capacity_percent = result.value;
                },
                BatteryCountersWire.CURRENT_UA_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._current_ua = result.value;
                },
                BatteryCountersWire.CURRENT_AVG_UA_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._current_avg_ua = result.value;
                },
                BatteryCountersWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                BatteryCountersWire.ENERGY_COUNTER_UWH_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._energy_counter_uwh = result.value;
                },
                BatteryCountersWire.VOLTAGE_UV_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._voltage_uv = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getChargeCounterUah(self: *const BatteryCountersReader) i64 {
        return self._charge_counter_uah;
    }
    pub inline fn getCapacityPercent(self: *const BatteryCountersReader) f32 {
        return self._capacity_percent;
    }
    pub inline fn getCurrentUa(self: *const BatteryCountersReader) i64 {
        return self._current_ua;
    }
    pub inline fn getCurrentAvgUa(self: *const BatteryCountersReader) i64 {
        return self._current_avg_ua;
    }
    pub inline fn getName(self: *const BatteryCountersReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getEnergyCounterUwh(self: *const BatteryCountersReader) i64 {
        return self._energy_counter_uwh;
    }
    pub inline fn getVoltageUv(self: *const BatteryCountersReader) i64 {
        return self._voltage_uv;
    }
};
