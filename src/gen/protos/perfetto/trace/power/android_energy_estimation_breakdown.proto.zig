// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const android_energy_consumer_descriptor = @import("src/gen/protos/perfetto/common/android_energy_consumer_descriptor.proto.zig");
// structs
const AndroidEnergyEstimationBreakdownWire = struct {
    const ENERGY_CONSUMER_DESCRIPTOR_WIRE: gremlin.ProtoWireNumber = 1;
    const ENERGY_CONSUMER_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const ENERGY_UWS_WIRE: gremlin.ProtoWireNumber = 3;
    const PER_UID_BREAKDOWN_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const AndroidEnergyEstimationBreakdown = struct {
    // nested structs
    const EnergyUidBreakdownWire = struct {
        const UID_WIRE: gremlin.ProtoWireNumber = 1;
        const ENERGY_UWS_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const EnergyUidBreakdown = struct {
        // fields
        uid: i32 = 0,
        energy_uws: i64 = 0,
        pub fn calcProtobufSize(self: *const AndroidEnergyEstimationBreakdown.EnergyUidBreakdown) usize {
            var res: usize = 0;
            if (self.uid != 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidEnergyEstimationBreakdown.EnergyUidBreakdownWire.UID_WIRE) + gremlin.sizes.sizeI32(self.uid);
            }
            if (self.energy_uws != 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidEnergyEstimationBreakdown.EnergyUidBreakdownWire.ENERGY_UWS_WIRE) + gremlin.sizes.sizeI64(self.energy_uws);
            }
            return res;
        }
        pub fn encode(self: *const AndroidEnergyEstimationBreakdown.EnergyUidBreakdown, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const AndroidEnergyEstimationBreakdown.EnergyUidBreakdown, target: *gremlin.Writer) void {
            if (self.uid != 0) {
                target.appendInt32(AndroidEnergyEstimationBreakdown.EnergyUidBreakdownWire.UID_WIRE, self.uid);
            }
            if (self.energy_uws != 0) {
                target.appendInt64(AndroidEnergyEstimationBreakdown.EnergyUidBreakdownWire.ENERGY_UWS_WIRE, self.energy_uws);
            }
        }
    };
    pub const EnergyUidBreakdownReader = struct {
        buf: gremlin.Reader,
        _uid: i32 = 0,
        _energy_uws: i64 = 0,
        pub fn init(src: []const u8) gremlin.Error!AndroidEnergyEstimationBreakdown.EnergyUidBreakdownReader {
            const buf = gremlin.Reader.init(src);
            var res = AndroidEnergyEstimationBreakdown.EnergyUidBreakdownReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    AndroidEnergyEstimationBreakdown.EnergyUidBreakdownWire.UID_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._uid = result.value;
                    },
                    AndroidEnergyEstimationBreakdown.EnergyUidBreakdownWire.ENERGY_UWS_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._energy_uws = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getUid(self: *const AndroidEnergyEstimationBreakdown.EnergyUidBreakdownReader) i32 {
            return self._uid;
        }
        pub inline fn getEnergyUws(self: *const AndroidEnergyEstimationBreakdown.EnergyUidBreakdownReader) i64 {
            return self._energy_uws;
        }
    };
    // fields
    energy_consumer_descriptor: ?android_energy_consumer_descriptor.AndroidEnergyConsumerDescriptor = null,
    energy_consumer_id: i32 = 0,
    energy_uws: i64 = 0,
    per_uid_breakdown: ?[]const ?AndroidEnergyEstimationBreakdown.EnergyUidBreakdown = null,
    pub fn calcProtobufSize(self: *const AndroidEnergyEstimationBreakdown) usize {
        var res: usize = 0;
        if (self.energy_consumer_descriptor) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidEnergyEstimationBreakdownWire.ENERGY_CONSUMER_DESCRIPTOR_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.energy_consumer_id != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidEnergyEstimationBreakdownWire.ENERGY_CONSUMER_ID_WIRE) + gremlin.sizes.sizeI32(self.energy_consumer_id);
        }
        if (self.energy_uws != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidEnergyEstimationBreakdownWire.ENERGY_UWS_WIRE) + gremlin.sizes.sizeI64(self.energy_uws);
        }
        if (self.per_uid_breakdown) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(AndroidEnergyEstimationBreakdownWire.PER_UID_BREAKDOWN_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const AndroidEnergyEstimationBreakdown, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidEnergyEstimationBreakdown, target: *gremlin.Writer) void {
        if (self.energy_consumer_descriptor) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidEnergyEstimationBreakdownWire.ENERGY_CONSUMER_DESCRIPTOR_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.energy_consumer_id != 0) {
            target.appendInt32(AndroidEnergyEstimationBreakdownWire.ENERGY_CONSUMER_ID_WIRE, self.energy_consumer_id);
        }
        if (self.energy_uws != 0) {
            target.appendInt64(AndroidEnergyEstimationBreakdownWire.ENERGY_UWS_WIRE, self.energy_uws);
        }
        if (self.per_uid_breakdown) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(AndroidEnergyEstimationBreakdownWire.PER_UID_BREAKDOWN_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(AndroidEnergyEstimationBreakdownWire.PER_UID_BREAKDOWN_WIRE, 0);
                }
            }
        }
    }
};
pub const AndroidEnergyEstimationBreakdownReader = struct {
    buf: gremlin.Reader,
    _energy_consumer_descriptor_buf: ?[]const u8 = null,
    _energy_consumer_id: i32 = 0,
    _energy_uws: i64 = 0,
    _per_uid_breakdown_offset: ?usize = null,
    _per_uid_breakdown_last_offset: ?usize = null,
    _per_uid_breakdown_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidEnergyEstimationBreakdownReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidEnergyEstimationBreakdownReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidEnergyEstimationBreakdownWire.ENERGY_CONSUMER_DESCRIPTOR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._energy_consumer_descriptor_buf = result.value;
                },
                AndroidEnergyEstimationBreakdownWire.ENERGY_CONSUMER_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._energy_consumer_id = result.value;
                },
                AndroidEnergyEstimationBreakdownWire.ENERGY_UWS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._energy_uws = result.value;
                },
                AndroidEnergyEstimationBreakdownWire.PER_UID_BREAKDOWN_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._per_uid_breakdown_offset == null) {
                        res._per_uid_breakdown_offset = offset - result.size;
                    }
                    res._per_uid_breakdown_last_offset = offset;
                    res._per_uid_breakdown_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getEnergyConsumerDescriptor(self: *const AndroidEnergyEstimationBreakdownReader) gremlin.Error!android_energy_consumer_descriptor.AndroidEnergyConsumerDescriptorReader {
        if (self._energy_consumer_descriptor_buf) |buf| {
            return try android_energy_consumer_descriptor.AndroidEnergyConsumerDescriptorReader.init(buf);
        }
        return try android_energy_consumer_descriptor.AndroidEnergyConsumerDescriptorReader.init(&[_]u8{});
    }
    pub inline fn getEnergyConsumerId(self: *const AndroidEnergyEstimationBreakdownReader) i32 {
        return self._energy_consumer_id;
    }
    pub inline fn getEnergyUws(self: *const AndroidEnergyEstimationBreakdownReader) i64 {
        return self._energy_uws;
    }
    pub fn perUidBreakdownCount(self: *const AndroidEnergyEstimationBreakdownReader) usize {
        return self._per_uid_breakdown_cnt;
    }
    pub fn perUidBreakdownNext(self: *AndroidEnergyEstimationBreakdownReader) ?AndroidEnergyEstimationBreakdown.EnergyUidBreakdownReader {
        if (self._per_uid_breakdown_offset == null) return null;
        const current_offset = self._per_uid_breakdown_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = AndroidEnergyEstimationBreakdown.EnergyUidBreakdownReader.init(result.value) catch return null;
        if (self._per_uid_breakdown_last_offset != null and current_offset >= self._per_uid_breakdown_last_offset.?) {
            self._per_uid_breakdown_offset = null;
            return msg;
        }
        if (self._per_uid_breakdown_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._per_uid_breakdown_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == AndroidEnergyEstimationBreakdownWire.PER_UID_BREAKDOWN_WIRE) {
                self._per_uid_breakdown_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._per_uid_breakdown_offset = null;
        return msg;
    }
};
