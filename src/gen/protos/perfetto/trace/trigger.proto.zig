// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TriggerWire = struct {
    const TRIGGER_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const PRODUCER_NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const TRUSTED_PRODUCER_UID_WIRE: gremlin.ProtoWireNumber = 3;
    const STOP_DELAY_MS_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const Trigger = struct {
    // fields
    trigger_name: ?[]const u8 = null,
    producer_name: ?[]const u8 = null,
    trusted_producer_uid: i32 = 0,
    stop_delay_ms: u64 = 0,
    pub fn calcProtobufSize(self: *const Trigger) usize {
        var res: usize = 0;
        if (self.trigger_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TriggerWire.TRIGGER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.producer_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TriggerWire.PRODUCER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.trusted_producer_uid != 0) {
            res += gremlin.sizes.sizeWireNumber(TriggerWire.TRUSTED_PRODUCER_UID_WIRE) + gremlin.sizes.sizeI32(self.trusted_producer_uid);
        }
        if (self.stop_delay_ms != 0) {
            res += gremlin.sizes.sizeWireNumber(TriggerWire.STOP_DELAY_MS_WIRE) + gremlin.sizes.sizeU64(self.stop_delay_ms);
        }
        return res;
    }
    pub fn encode(self: *const Trigger, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const Trigger, target: *gremlin.Writer) void {
        if (self.trigger_name) |v| {
            if (v.len > 0) {
                target.appendBytes(TriggerWire.TRIGGER_NAME_WIRE, v);
            }
        }
        if (self.producer_name) |v| {
            if (v.len > 0) {
                target.appendBytes(TriggerWire.PRODUCER_NAME_WIRE, v);
            }
        }
        if (self.trusted_producer_uid != 0) {
            target.appendInt32(TriggerWire.TRUSTED_PRODUCER_UID_WIRE, self.trusted_producer_uid);
        }
        if (self.stop_delay_ms != 0) {
            target.appendUint64(TriggerWire.STOP_DELAY_MS_WIRE, self.stop_delay_ms);
        }
    }
};
pub const TriggerReader = struct {
    buf: gremlin.Reader,
    _trigger_name: ?[]const u8 = null,
    _producer_name: ?[]const u8 = null,
    _trusted_producer_uid: i32 = 0,
    _stop_delay_ms: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!TriggerReader {
        const buf = gremlin.Reader.init(src);
        var res = TriggerReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TriggerWire.TRIGGER_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._trigger_name = result.value;
                },
                TriggerWire.PRODUCER_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._producer_name = result.value;
                },
                TriggerWire.TRUSTED_PRODUCER_UID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._trusted_producer_uid = result.value;
                },
                TriggerWire.STOP_DELAY_MS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._stop_delay_ms = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTriggerName(self: *const TriggerReader) []const u8 {
        return self._trigger_name orelse &[_]u8{};
    }
    pub inline fn getProducerName(self: *const TriggerReader) []const u8 {
        return self._producer_name orelse &[_]u8{};
    }
    pub inline fn getTrustedProducerUid(self: *const TriggerReader) i32 {
        return self._trusted_producer_uid;
    }
    pub inline fn getStopDelayMs(self: *const TriggerReader) u64 {
        return self._stop_delay_ms;
    }
};
