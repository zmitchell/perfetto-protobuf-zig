// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const descriptor = @import("src/gen/protos/perfetto/common/descriptor.proto.zig");
// structs
const ExtensionDescriptorWire = struct {
    const EXTENSION_SET_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ExtensionDescriptor = struct {
    // fields
    extension_set: ?descriptor.FileDescriptorSet = null,
    pub fn calcProtobufSize(self: *const ExtensionDescriptor) usize {
        var res: usize = 0;
        if (self.extension_set) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(ExtensionDescriptorWire.EXTENSION_SET_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const ExtensionDescriptor, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ExtensionDescriptor, target: *gremlin.Writer) void {
        if (self.extension_set) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(ExtensionDescriptorWire.EXTENSION_SET_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const ExtensionDescriptorReader = struct {
    buf: gremlin.Reader,
    _extension_set_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ExtensionDescriptorReader {
        const buf = gremlin.Reader.init(src);
        var res = ExtensionDescriptorReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ExtensionDescriptorWire.EXTENSION_SET_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._extension_set_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getExtensionSet(self: *const ExtensionDescriptorReader) gremlin.Error!descriptor.FileDescriptorSetReader {
        if (self._extension_set_buf) |buf| {
            return try descriptor.FileDescriptorSetReader.init(buf);
        }
        return try descriptor.FileDescriptorSetReader.init(&[_]u8{});
    }
};
