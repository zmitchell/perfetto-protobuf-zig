// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TraceUuidWire = struct {
    const MSB_WIRE: gremlin.ProtoWireNumber = 1;
    const LSB_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const TraceUuid = struct {
    // fields
    msb: i64 = 0,
    lsb: i64 = 0,
    pub fn calcProtobufSize(self: *const TraceUuid) usize {
        var res: usize = 0;
        if (self.msb != 0) {
            res += gremlin.sizes.sizeWireNumber(TraceUuidWire.MSB_WIRE) + gremlin.sizes.sizeI64(self.msb);
        }
        if (self.lsb != 0) {
            res += gremlin.sizes.sizeWireNumber(TraceUuidWire.LSB_WIRE) + gremlin.sizes.sizeI64(self.lsb);
        }
        return res;
    }
    pub fn encode(self: *const TraceUuid, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TraceUuid, target: *gremlin.Writer) void {
        if (self.msb != 0) {
            target.appendInt64(TraceUuidWire.MSB_WIRE, self.msb);
        }
        if (self.lsb != 0) {
            target.appendInt64(TraceUuidWire.LSB_WIRE, self.lsb);
        }
    }
};
pub const TraceUuidReader = struct {
    buf: gremlin.Reader,
    _msb: i64 = 0,
    _lsb: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!TraceUuidReader {
        const buf = gremlin.Reader.init(src);
        var res = TraceUuidReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TraceUuidWire.MSB_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._msb = result.value;
                },
                TraceUuidWire.LSB_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._lsb = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getMsb(self: *const TraceUuidReader) i64 {
        return self._msb;
    }
    pub inline fn getLsb(self: *const TraceUuidReader) i64 {
        return self._lsb;
    }
};
