// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const CpuInfoWire = struct {
    const CPUS_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const CpuInfo = struct {
    // nested structs
    const ArmCpuIdentifierWire = struct {
        const IMPLEMENTER_WIRE: gremlin.ProtoWireNumber = 1;
        const ARCHITECTURE_WIRE: gremlin.ProtoWireNumber = 2;
        const VARIANT_WIRE: gremlin.ProtoWireNumber = 3;
        const PART_WIRE: gremlin.ProtoWireNumber = 4;
        const REVISION_WIRE: gremlin.ProtoWireNumber = 5;
    };
    pub const ArmCpuIdentifier = struct {
        // fields
        implementer: u32 = 0,
        architecture: u32 = 0,
        variant: u32 = 0,
        part: u32 = 0,
        revision: u32 = 0,
        pub fn calcProtobufSize(self: *const CpuInfo.ArmCpuIdentifier) usize {
            var res: usize = 0;
            if (self.implementer != 0) {
                res += gremlin.sizes.sizeWireNumber(CpuInfo.ArmCpuIdentifierWire.IMPLEMENTER_WIRE) + gremlin.sizes.sizeU32(self.implementer);
            }
            if (self.architecture != 0) {
                res += gremlin.sizes.sizeWireNumber(CpuInfo.ArmCpuIdentifierWire.ARCHITECTURE_WIRE) + gremlin.sizes.sizeU32(self.architecture);
            }
            if (self.variant != 0) {
                res += gremlin.sizes.sizeWireNumber(CpuInfo.ArmCpuIdentifierWire.VARIANT_WIRE) + gremlin.sizes.sizeU32(self.variant);
            }
            if (self.part != 0) {
                res += gremlin.sizes.sizeWireNumber(CpuInfo.ArmCpuIdentifierWire.PART_WIRE) + gremlin.sizes.sizeU32(self.part);
            }
            if (self.revision != 0) {
                res += gremlin.sizes.sizeWireNumber(CpuInfo.ArmCpuIdentifierWire.REVISION_WIRE) + gremlin.sizes.sizeU32(self.revision);
            }
            return res;
        }
        pub fn encode(self: *const CpuInfo.ArmCpuIdentifier, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const CpuInfo.ArmCpuIdentifier, target: *gremlin.Writer) void {
            if (self.implementer != 0) {
                target.appendUint32(CpuInfo.ArmCpuIdentifierWire.IMPLEMENTER_WIRE, self.implementer);
            }
            if (self.architecture != 0) {
                target.appendUint32(CpuInfo.ArmCpuIdentifierWire.ARCHITECTURE_WIRE, self.architecture);
            }
            if (self.variant != 0) {
                target.appendUint32(CpuInfo.ArmCpuIdentifierWire.VARIANT_WIRE, self.variant);
            }
            if (self.part != 0) {
                target.appendUint32(CpuInfo.ArmCpuIdentifierWire.PART_WIRE, self.part);
            }
            if (self.revision != 0) {
                target.appendUint32(CpuInfo.ArmCpuIdentifierWire.REVISION_WIRE, self.revision);
            }
        }
    };
    pub const ArmCpuIdentifierReader = struct {
        buf: gremlin.Reader,
        _implementer: u32 = 0,
        _architecture: u32 = 0,
        _variant: u32 = 0,
        _part: u32 = 0,
        _revision: u32 = 0,
        pub fn init(src: []const u8) gremlin.Error!CpuInfo.ArmCpuIdentifierReader {
            const buf = gremlin.Reader.init(src);
            var res = CpuInfo.ArmCpuIdentifierReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    CpuInfo.ArmCpuIdentifierWire.IMPLEMENTER_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._implementer = result.value;
                    },
                    CpuInfo.ArmCpuIdentifierWire.ARCHITECTURE_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._architecture = result.value;
                    },
                    CpuInfo.ArmCpuIdentifierWire.VARIANT_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._variant = result.value;
                    },
                    CpuInfo.ArmCpuIdentifierWire.PART_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._part = result.value;
                    },
                    CpuInfo.ArmCpuIdentifierWire.REVISION_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._revision = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getImplementer(self: *const CpuInfo.ArmCpuIdentifierReader) u32 {
            return self._implementer;
        }
        pub inline fn getArchitecture(self: *const CpuInfo.ArmCpuIdentifierReader) u32 {
            return self._architecture;
        }
        pub inline fn getVariant(self: *const CpuInfo.ArmCpuIdentifierReader) u32 {
            return self._variant;
        }
        pub inline fn getPart(self: *const CpuInfo.ArmCpuIdentifierReader) u32 {
            return self._part;
        }
        pub inline fn getRevision(self: *const CpuInfo.ArmCpuIdentifierReader) u32 {
            return self._revision;
        }
    };
    const CpuWire = struct {
        const PROCESSOR_WIRE: gremlin.ProtoWireNumber = 1;
        const FREQUENCIES_WIRE: gremlin.ProtoWireNumber = 2;
        const CAPACITY_WIRE: gremlin.ProtoWireNumber = 3;
        const FEATURES_WIRE: gremlin.ProtoWireNumber = 5;
        const ARM_IDENTIFIER_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const Cpu = struct {
        // fields
        processor: ?[]const u8 = null,
        frequencies: ?[]const u32 = null,
        capacity: u32 = 0,
        features: u64 = 0,
        arm_identifier: ?CpuInfo.ArmCpuIdentifier = null,
        pub fn calcProtobufSize(self: *const CpuInfo.Cpu) usize {
            var res: usize = 0;
            if (self.processor) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(CpuInfo.CpuWire.PROCESSOR_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.frequencies) |arr| {
                if (arr.len == 0) {} else if (arr.len == 1) {
                    res += gremlin.sizes.sizeWireNumber(CpuInfo.CpuWire.FREQUENCIES_WIRE) + gremlin.sizes.sizeU32(arr[0]);
                } else {
                    var packed_size: usize = 0;
                    for (arr) |v| {
                        packed_size += gremlin.sizes.sizeU32(v);
                    }
                    res += gremlin.sizes.sizeWireNumber(CpuInfo.CpuWire.FREQUENCIES_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
                }
            }
            if (self.capacity != 0) {
                res += gremlin.sizes.sizeWireNumber(CpuInfo.CpuWire.CAPACITY_WIRE) + gremlin.sizes.sizeU32(self.capacity);
            }
            if (self.features != 0) {
                res += gremlin.sizes.sizeWireNumber(CpuInfo.CpuWire.FEATURES_WIRE) + gremlin.sizes.sizeU64(self.features);
            }
            if (self.arm_identifier) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(CpuInfo.CpuWire.ARM_IDENTIFIER_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            return res;
        }
        pub fn encode(self: *const CpuInfo.Cpu, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const CpuInfo.Cpu, target: *gremlin.Writer) void {
            if (self.processor) |v| {
                if (v.len > 0) {
                    target.appendBytes(CpuInfo.CpuWire.PROCESSOR_WIRE, v);
                }
            }
            if (self.frequencies) |arr| {
                if (arr.len == 0) {} else if (arr.len == 1) {
                    target.appendUint32(CpuInfo.CpuWire.FREQUENCIES_WIRE, arr[0]);
                } else {
                    var packed_size: usize = 0;
                    for (arr) |v| {
                        packed_size += gremlin.sizes.sizeU32(v);
                    }
                    target.appendBytesTag(CpuInfo.CpuWire.FREQUENCIES_WIRE, packed_size);
                    for (arr) |v| {
                        target.appendUint32WithoutTag(v);
                    }
                }
            }
            if (self.capacity != 0) {
                target.appendUint32(CpuInfo.CpuWire.CAPACITY_WIRE, self.capacity);
            }
            if (self.features != 0) {
                target.appendUint64(CpuInfo.CpuWire.FEATURES_WIRE, self.features);
            }
            if (self.arm_identifier) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(CpuInfo.CpuWire.ARM_IDENTIFIER_WIRE, size);
                    v.encodeTo(target);
                }
            }
        }
    };
    pub const CpuReader = struct {
        buf: gremlin.Reader,
        _processor: ?[]const u8 = null,
        _frequencies_offset: ?usize = null,
        _frequencies_last_offset: ?usize = null,
        _frequencies_packed: bool = false,
        _capacity: u32 = 0,
        _features: u64 = 0,
        _arm_identifier_buf: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!CpuInfo.CpuReader {
            const buf = gremlin.Reader.init(src);
            var res = CpuInfo.CpuReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    CpuInfo.CpuWire.PROCESSOR_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._processor = result.value;
                    },
                    CpuInfo.CpuWire.FREQUENCIES_WIRE => {
                        if (res._frequencies_offset == null) {
                            res._frequencies_offset = offset;
                        }
                        if (tag.wire == gremlin.ProtoWireType.bytes) {
                            res._frequencies_packed = true;
                            const length_result = try buf.readVarInt(offset);
                            res._frequencies_offset = offset + length_result.size;
                            res._frequencies_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                            offset = res._frequencies_last_offset.?;
                        } else {
                            const result = try buf.readUInt32(offset);
                            offset += result.size;
                            res._frequencies_last_offset = offset;
                        }
                    },
                    CpuInfo.CpuWire.CAPACITY_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._capacity = result.value;
                    },
                    CpuInfo.CpuWire.FEATURES_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._features = result.value;
                    },
                    CpuInfo.CpuWire.ARM_IDENTIFIER_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._arm_identifier_buf = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getProcessor(self: *const CpuInfo.CpuReader) []const u8 {
            return self._processor orelse &[_]u8{};
        }
        pub fn frequenciesNext(self: *CpuInfo.CpuReader) gremlin.Error!?u32 {
            if (self._frequencies_offset == null) return null;
            const current_offset = self._frequencies_offset.?;
            if (current_offset >= self._frequencies_last_offset.?) {
                self._frequencies_offset = null;
                return null;
            }
            if (self._frequencies_packed) {
                const value_result = try self.buf.readUInt32(current_offset);
                self._frequencies_offset = current_offset + value_result.size;
                if (self._frequencies_offset.? >= self._frequencies_last_offset.?) {
                    self._frequencies_offset = null;
                }
                return value_result.value;
            } else {
                const value_result = try self.buf.readUInt32(current_offset);
                var next_offset = current_offset + value_result.size;
                const max_offset = self._frequencies_last_offset.?;
                // Search for the next occurrence of this field
                while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                    const next_tag = try self.buf.readTagAt(next_offset);
                    next_offset += next_tag.size;
                    if (next_tag.number == CpuInfo.CpuWire.FREQUENCIES_WIRE) {
                        self._frequencies_offset = next_offset;
                        return value_result.value;
                    } else {
                        next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                    }
                }
                self._frequencies_offset = null;
                return value_result.value;
            }
        }
        pub inline fn getCapacity(self: *const CpuInfo.CpuReader) u32 {
            return self._capacity;
        }
        pub inline fn getFeatures(self: *const CpuInfo.CpuReader) u64 {
            return self._features;
        }
        pub fn getArmIdentifier(self: *const CpuInfo.CpuReader) gremlin.Error!CpuInfo.ArmCpuIdentifierReader {
            if (self._arm_identifier_buf) |buf| {
                return try CpuInfo.ArmCpuIdentifierReader.init(buf);
            }
            return try CpuInfo.ArmCpuIdentifierReader.init(&[_]u8{});
        }
    };
    // fields
    cpus: ?[]const ?CpuInfo.Cpu = null,
    pub fn calcProtobufSize(self: *const CpuInfo) usize {
        var res: usize = 0;
        if (self.cpus) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(CpuInfoWire.CPUS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const CpuInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CpuInfo, target: *gremlin.Writer) void {
        if (self.cpus) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(CpuInfoWire.CPUS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(CpuInfoWire.CPUS_WIRE, 0);
                }
            }
        }
    }
};
pub const CpuInfoReader = struct {
    buf: gremlin.Reader,
    _cpus_offset: ?usize = null,
    _cpus_last_offset: ?usize = null,
    _cpus_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!CpuInfoReader {
        const buf = gremlin.Reader.init(src);
        var res = CpuInfoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CpuInfoWire.CPUS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._cpus_offset == null) {
                        res._cpus_offset = offset - result.size;
                    }
                    res._cpus_last_offset = offset;
                    res._cpus_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn cpusCount(self: *const CpuInfoReader) usize {
        return self._cpus_cnt;
    }
    pub fn cpusNext(self: *CpuInfoReader) ?CpuInfo.CpuReader {
        if (self._cpus_offset == null) return null;
        const current_offset = self._cpus_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = CpuInfo.CpuReader.init(result.value) catch return null;
        if (self._cpus_last_offset != null and current_offset >= self._cpus_last_offset.?) {
            self._cpus_offset = null;
            return msg;
        }
        if (self._cpus_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._cpus_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == CpuInfoWire.CPUS_WIRE) {
                self._cpus_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._cpus_offset = null;
        return msg;
    }
};
