// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const UiStateWire = struct {
    const TIMELINE_START_TS_WIRE: gremlin.ProtoWireNumber = 1;
    const TIMELINE_END_TS_WIRE: gremlin.ProtoWireNumber = 2;
    const HIGHLIGHT_PROCESS_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const UiState = struct {
    // nested structs
    const HighlightProcessWire = struct {
        const PID_WIRE: gremlin.ProtoWireNumber = 1;
        const CMDLINE_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const HighlightProcess = struct {
        // fields
        pid: u32 = 0,
        cmdline: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const UiState.HighlightProcess) usize {
            var res: usize = 0;
            if (self.pid != 0) {
                res += gremlin.sizes.sizeWireNumber(UiState.HighlightProcessWire.PID_WIRE) + gremlin.sizes.sizeU32(self.pid);
            }
            if (self.cmdline) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(UiState.HighlightProcessWire.CMDLINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const UiState.HighlightProcess, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const UiState.HighlightProcess, target: *gremlin.Writer) void {
            if (self.pid != 0) {
                target.appendUint32(UiState.HighlightProcessWire.PID_WIRE, self.pid);
            }
            if (self.cmdline) |v| {
                if (v.len > 0) {
                    target.appendBytes(UiState.HighlightProcessWire.CMDLINE_WIRE, v);
                }
            }
        }
    };
    pub const HighlightProcessReader = struct {
        buf: gremlin.Reader,
        _pid: u32 = 0,
        _cmdline: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!UiState.HighlightProcessReader {
            const buf = gremlin.Reader.init(src);
            var res = UiState.HighlightProcessReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    UiState.HighlightProcessWire.PID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._pid = result.value;
                    },
                    UiState.HighlightProcessWire.CMDLINE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._cmdline = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getPid(self: *const UiState.HighlightProcessReader) u32 {
            return self._pid;
        }
        pub inline fn getCmdline(self: *const UiState.HighlightProcessReader) []const u8 {
            return self._cmdline orelse &[_]u8{};
        }
    };
    // fields
    timeline_start_ts: i64 = 0,
    timeline_end_ts: i64 = 0,
    highlight_process: ?UiState.HighlightProcess = null,
    pub fn calcProtobufSize(self: *const UiState) usize {
        var res: usize = 0;
        if (self.timeline_start_ts != 0) {
            res += gremlin.sizes.sizeWireNumber(UiStateWire.TIMELINE_START_TS_WIRE) + gremlin.sizes.sizeI64(self.timeline_start_ts);
        }
        if (self.timeline_end_ts != 0) {
            res += gremlin.sizes.sizeWireNumber(UiStateWire.TIMELINE_END_TS_WIRE) + gremlin.sizes.sizeI64(self.timeline_end_ts);
        }
        if (self.highlight_process) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(UiStateWire.HIGHLIGHT_PROCESS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const UiState, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const UiState, target: *gremlin.Writer) void {
        if (self.timeline_start_ts != 0) {
            target.appendInt64(UiStateWire.TIMELINE_START_TS_WIRE, self.timeline_start_ts);
        }
        if (self.timeline_end_ts != 0) {
            target.appendInt64(UiStateWire.TIMELINE_END_TS_WIRE, self.timeline_end_ts);
        }
        if (self.highlight_process) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(UiStateWire.HIGHLIGHT_PROCESS_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const UiStateReader = struct {
    buf: gremlin.Reader,
    _timeline_start_ts: i64 = 0,
    _timeline_end_ts: i64 = 0,
    _highlight_process_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!UiStateReader {
        const buf = gremlin.Reader.init(src);
        var res = UiStateReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                UiStateWire.TIMELINE_START_TS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._timeline_start_ts = result.value;
                },
                UiStateWire.TIMELINE_END_TS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._timeline_end_ts = result.value;
                },
                UiStateWire.HIGHLIGHT_PROCESS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._highlight_process_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTimelineStartTs(self: *const UiStateReader) i64 {
        return self._timeline_start_ts;
    }
    pub inline fn getTimelineEndTs(self: *const UiStateReader) i64 {
        return self._timeline_end_ts;
    }
    pub fn getHighlightProcess(self: *const UiStateReader) gremlin.Error!UiState.HighlightProcessReader {
        if (self._highlight_process_buf) |buf| {
            return try UiState.HighlightProcessReader.init(buf);
        }
        return try UiState.HighlightProcessReader.init(&[_]u8{});
    }
};
