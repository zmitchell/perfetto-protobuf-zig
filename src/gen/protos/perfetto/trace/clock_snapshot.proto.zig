// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const builtin_clock = @import("src/gen/protos/perfetto/common/builtin_clock.proto.zig");
// structs
const ClockSnapshotWire = struct {
    const CLOCKS_WIRE: gremlin.ProtoWireNumber = 1;
    const PRIMARY_TRACE_CLOCK_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const ClockSnapshot = struct {
    // nested structs
    const ClockWire = struct {
        const CLOCK_ID_WIRE: gremlin.ProtoWireNumber = 1;
        const TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 2;
        const IS_INCREMENTAL_WIRE: gremlin.ProtoWireNumber = 3;
        const UNIT_MULTIPLIER_NS_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const Clock = struct {
        // nested enums
        pub const BuiltinClocks = enum(i32) {
            UNKNOWN = 0,
            REALTIME = 1,
            REALTIME_COARSE = 2,
            MONOTONIC = 3,
            MONOTONIC_COARSE = 4,
            MONOTONIC_RAW = 5,
            BOOTTIME = 6,
            BUILTIN_CLOCK_MAX_ID = 63,
        };
        // fields
        clock_id: u32 = 0,
        timestamp: u64 = 0,
        is_incremental: bool = false,
        unit_multiplier_ns: u64 = 0,
        pub fn calcProtobufSize(self: *const ClockSnapshot.Clock) usize {
            var res: usize = 0;
            if (self.clock_id != 0) {
                res += gremlin.sizes.sizeWireNumber(ClockSnapshot.ClockWire.CLOCK_ID_WIRE) + gremlin.sizes.sizeU32(self.clock_id);
            }
            if (self.timestamp != 0) {
                res += gremlin.sizes.sizeWireNumber(ClockSnapshot.ClockWire.TIMESTAMP_WIRE) + gremlin.sizes.sizeU64(self.timestamp);
            }
            if (self.is_incremental != false) {
                res += gremlin.sizes.sizeWireNumber(ClockSnapshot.ClockWire.IS_INCREMENTAL_WIRE) + gremlin.sizes.sizeBool(self.is_incremental);
            }
            if (self.unit_multiplier_ns != 0) {
                res += gremlin.sizes.sizeWireNumber(ClockSnapshot.ClockWire.UNIT_MULTIPLIER_NS_WIRE) + gremlin.sizes.sizeU64(self.unit_multiplier_ns);
            }
            return res;
        }
        pub fn encode(self: *const ClockSnapshot.Clock, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ClockSnapshot.Clock, target: *gremlin.Writer) void {
            if (self.clock_id != 0) {
                target.appendUint32(ClockSnapshot.ClockWire.CLOCK_ID_WIRE, self.clock_id);
            }
            if (self.timestamp != 0) {
                target.appendUint64(ClockSnapshot.ClockWire.TIMESTAMP_WIRE, self.timestamp);
            }
            if (self.is_incremental != false) {
                target.appendBool(ClockSnapshot.ClockWire.IS_INCREMENTAL_WIRE, self.is_incremental);
            }
            if (self.unit_multiplier_ns != 0) {
                target.appendUint64(ClockSnapshot.ClockWire.UNIT_MULTIPLIER_NS_WIRE, self.unit_multiplier_ns);
            }
        }
    };
    pub const ClockReader = struct {
        buf: gremlin.Reader,
        _clock_id: u32 = 0,
        _timestamp: u64 = 0,
        _is_incremental: bool = false,
        _unit_multiplier_ns: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!ClockSnapshot.ClockReader {
            const buf = gremlin.Reader.init(src);
            var res = ClockSnapshot.ClockReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ClockSnapshot.ClockWire.CLOCK_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._clock_id = result.value;
                    },
                    ClockSnapshot.ClockWire.TIMESTAMP_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._timestamp = result.value;
                    },
                    ClockSnapshot.ClockWire.IS_INCREMENTAL_WIRE => {
                        const result = try buf.readBool(offset);
                        offset += result.size;
                        res._is_incremental = result.value;
                    },
                    ClockSnapshot.ClockWire.UNIT_MULTIPLIER_NS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._unit_multiplier_ns = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getClockId(self: *const ClockSnapshot.ClockReader) u32 {
            return self._clock_id;
        }
        pub inline fn getTimestamp(self: *const ClockSnapshot.ClockReader) u64 {
            return self._timestamp;
        }
        pub inline fn getIsIncremental(self: *const ClockSnapshot.ClockReader) bool {
            return self._is_incremental;
        }
        pub inline fn getUnitMultiplierNs(self: *const ClockSnapshot.ClockReader) u64 {
            return self._unit_multiplier_ns;
        }
    };
    // fields
    clocks: ?[]const ?ClockSnapshot.Clock = null,
    primary_trace_clock: builtin_clock.BuiltinClock = @enumFromInt(0),
    pub fn calcProtobufSize(self: *const ClockSnapshot) usize {
        var res: usize = 0;
        if (self.clocks) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ClockSnapshotWire.CLOCKS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (@intFromEnum(self.primary_trace_clock) != 0) {
            res += gremlin.sizes.sizeWireNumber(ClockSnapshotWire.PRIMARY_TRACE_CLOCK_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.primary_trace_clock));
        }
        return res;
    }
    pub fn encode(self: *const ClockSnapshot, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ClockSnapshot, target: *gremlin.Writer) void {
        if (self.clocks) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ClockSnapshotWire.CLOCKS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ClockSnapshotWire.CLOCKS_WIRE, 0);
                }
            }
        }
        if (@intFromEnum(self.primary_trace_clock) != 0) {
            target.appendInt32(ClockSnapshotWire.PRIMARY_TRACE_CLOCK_WIRE, @intFromEnum(self.primary_trace_clock));
        }
    }
};
pub const ClockSnapshotReader = struct {
    buf: gremlin.Reader,
    _clocks_offset: ?usize = null,
    _clocks_last_offset: ?usize = null,
    _clocks_cnt: usize = 0,
    _primary_trace_clock: builtin_clock.BuiltinClock = @enumFromInt(0),
    pub fn init(src: []const u8) gremlin.Error!ClockSnapshotReader {
        const buf = gremlin.Reader.init(src);
        var res = ClockSnapshotReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ClockSnapshotWire.CLOCKS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._clocks_offset == null) {
                        res._clocks_offset = offset - result.size;
                    }
                    res._clocks_last_offset = offset;
                    res._clocks_cnt += 1;
                },
                ClockSnapshotWire.PRIMARY_TRACE_CLOCK_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._primary_trace_clock = @enumFromInt(result.value);
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn clocksCount(self: *const ClockSnapshotReader) usize {
        return self._clocks_cnt;
    }
    pub fn clocksNext(self: *ClockSnapshotReader) ?ClockSnapshot.ClockReader {
        if (self._clocks_offset == null) return null;
        const current_offset = self._clocks_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ClockSnapshot.ClockReader.init(result.value) catch return null;
        if (self._clocks_last_offset != null and current_offset >= self._clocks_last_offset.?) {
            self._clocks_offset = null;
            return msg;
        }
        if (self._clocks_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._clocks_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ClockSnapshotWire.CLOCKS_WIRE) {
                self._clocks_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._clocks_offset = null;
        return msg;
    }
    pub inline fn getPrimaryTraceClock(self: *const ClockSnapshotReader) builtin_clock.BuiltinClock {
        return self._primary_trace_clock;
    }
};
