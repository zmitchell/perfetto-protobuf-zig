// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const KernelWakelockDataWire = struct {
    const WAKELOCK_WIRE: gremlin.ProtoWireNumber = 1;
    const WAKELOCK_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const TIME_HELD_MILLIS_WIRE: gremlin.ProtoWireNumber = 3;
    const ERROR_FLAGS_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const KernelWakelockData = struct {
    // nested structs
    const WakelockWire = struct {
        const WAKELOCK_ID_WIRE: gremlin.ProtoWireNumber = 1;
        const WAKELOCK_NAME_WIRE: gremlin.ProtoWireNumber = 2;
        const WAKELOCK_TYPE_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const Wakelock = struct {
        // nested enums
        pub const Type = enum(i32) {
            WAKELOCK_TYPE_UNKNOWN = 0,
            WAKELOCK_TYPE_KERNEL = 1,
            WAKELOCK_TYPE_NATIVE = 2,
        };
        // fields
        wakelock_id: u32 = 0,
        wakelock_name: ?[]const u8 = null,
        wakelock_type: KernelWakelockData.Wakelock.Type = @enumFromInt(0),
        pub fn calcProtobufSize(self: *const KernelWakelockData.Wakelock) usize {
            var res: usize = 0;
            if (self.wakelock_id != 0) {
                res += gremlin.sizes.sizeWireNumber(KernelWakelockData.WakelockWire.WAKELOCK_ID_WIRE) + gremlin.sizes.sizeU32(self.wakelock_id);
            }
            if (self.wakelock_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(KernelWakelockData.WakelockWire.WAKELOCK_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (@intFromEnum(self.wakelock_type) != 0) {
                res += gremlin.sizes.sizeWireNumber(KernelWakelockData.WakelockWire.WAKELOCK_TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.wakelock_type));
            }
            return res;
        }
        pub fn encode(self: *const KernelWakelockData.Wakelock, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const KernelWakelockData.Wakelock, target: *gremlin.Writer) void {
            if (self.wakelock_id != 0) {
                target.appendUint32(KernelWakelockData.WakelockWire.WAKELOCK_ID_WIRE, self.wakelock_id);
            }
            if (self.wakelock_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(KernelWakelockData.WakelockWire.WAKELOCK_NAME_WIRE, v);
                }
            }
            if (@intFromEnum(self.wakelock_type) != 0) {
                target.appendInt32(KernelWakelockData.WakelockWire.WAKELOCK_TYPE_WIRE, @intFromEnum(self.wakelock_type));
            }
        }
    };
    pub const WakelockReader = struct {
        buf: gremlin.Reader,
        _wakelock_id: u32 = 0,
        _wakelock_name: ?[]const u8 = null,
        _wakelock_type: KernelWakelockData.Wakelock.Type = @enumFromInt(0),
        pub fn init(src: []const u8) gremlin.Error!KernelWakelockData.WakelockReader {
            const buf = gremlin.Reader.init(src);
            var res = KernelWakelockData.WakelockReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    KernelWakelockData.WakelockWire.WAKELOCK_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._wakelock_id = result.value;
                    },
                    KernelWakelockData.WakelockWire.WAKELOCK_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._wakelock_name = result.value;
                    },
                    KernelWakelockData.WakelockWire.WAKELOCK_TYPE_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._wakelock_type = @enumFromInt(result.value);
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getWakelockId(self: *const KernelWakelockData.WakelockReader) u32 {
            return self._wakelock_id;
        }
        pub inline fn getWakelockName(self: *const KernelWakelockData.WakelockReader) []const u8 {
            return self._wakelock_name orelse &[_]u8{};
        }
        pub inline fn getWakelockType(self: *const KernelWakelockData.WakelockReader) KernelWakelockData.Wakelock.Type {
            return self._wakelock_type;
        }
    };
    // fields
    wakelock: ?[]const ?KernelWakelockData.Wakelock = null,
    wakelock_id: ?[]const u32 = null,
    time_held_millis: ?[]const u64 = null,
    error_flags: u64 = 0,
    pub fn calcProtobufSize(self: *const KernelWakelockData) usize {
        var res: usize = 0;
        if (self.wakelock) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(KernelWakelockDataWire.WAKELOCK_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.wakelock_id) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(KernelWakelockDataWire.WAKELOCK_ID_WIRE) + gremlin.sizes.sizeU32(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU32(v);
                }
                res += gremlin.sizes.sizeWireNumber(KernelWakelockDataWire.WAKELOCK_ID_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.time_held_millis) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(KernelWakelockDataWire.TIME_HELD_MILLIS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(KernelWakelockDataWire.TIME_HELD_MILLIS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.error_flags != 0) {
            res += gremlin.sizes.sizeWireNumber(KernelWakelockDataWire.ERROR_FLAGS_WIRE) + gremlin.sizes.sizeU64(self.error_flags);
        }
        return res;
    }
    pub fn encode(self: *const KernelWakelockData, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const KernelWakelockData, target: *gremlin.Writer) void {
        if (self.wakelock) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(KernelWakelockDataWire.WAKELOCK_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(KernelWakelockDataWire.WAKELOCK_WIRE, 0);
                }
            }
        }
        if (self.wakelock_id) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint32(KernelWakelockDataWire.WAKELOCK_ID_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU32(v);
                }
                target.appendBytesTag(KernelWakelockDataWire.WAKELOCK_ID_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint32WithoutTag(v);
                }
            }
        }
        if (self.time_held_millis) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(KernelWakelockDataWire.TIME_HELD_MILLIS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(KernelWakelockDataWire.TIME_HELD_MILLIS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
        if (self.error_flags != 0) {
            target.appendUint64(KernelWakelockDataWire.ERROR_FLAGS_WIRE, self.error_flags);
        }
    }
};
pub const KernelWakelockDataReader = struct {
    buf: gremlin.Reader,
    _wakelock_offset: ?usize = null,
    _wakelock_last_offset: ?usize = null,
    _wakelock_cnt: usize = 0,
    _wakelock_id_offset: ?usize = null,
    _wakelock_id_last_offset: ?usize = null,
    _wakelock_id_packed: bool = false,
    _time_held_millis_offset: ?usize = null,
    _time_held_millis_last_offset: ?usize = null,
    _time_held_millis_packed: bool = false,
    _error_flags: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!KernelWakelockDataReader {
        const buf = gremlin.Reader.init(src);
        var res = KernelWakelockDataReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                KernelWakelockDataWire.WAKELOCK_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._wakelock_offset == null) {
                        res._wakelock_offset = offset - result.size;
                    }
                    res._wakelock_last_offset = offset;
                    res._wakelock_cnt += 1;
                },
                KernelWakelockDataWire.WAKELOCK_ID_WIRE => {
                    if (res._wakelock_id_offset == null) {
                        res._wakelock_id_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._wakelock_id_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._wakelock_id_offset = offset + length_result.size;
                        res._wakelock_id_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._wakelock_id_last_offset.?;
                    } else {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._wakelock_id_last_offset = offset;
                    }
                },
                KernelWakelockDataWire.TIME_HELD_MILLIS_WIRE => {
                    if (res._time_held_millis_offset == null) {
                        res._time_held_millis_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._time_held_millis_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._time_held_millis_offset = offset + length_result.size;
                        res._time_held_millis_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._time_held_millis_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._time_held_millis_last_offset = offset;
                    }
                },
                KernelWakelockDataWire.ERROR_FLAGS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._error_flags = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn wakelockCount(self: *const KernelWakelockDataReader) usize {
        return self._wakelock_cnt;
    }
    pub fn wakelockNext(self: *KernelWakelockDataReader) ?KernelWakelockData.WakelockReader {
        if (self._wakelock_offset == null) return null;
        const current_offset = self._wakelock_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = KernelWakelockData.WakelockReader.init(result.value) catch return null;
        if (self._wakelock_last_offset != null and current_offset >= self._wakelock_last_offset.?) {
            self._wakelock_offset = null;
            return msg;
        }
        if (self._wakelock_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._wakelock_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == KernelWakelockDataWire.WAKELOCK_WIRE) {
                self._wakelock_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._wakelock_offset = null;
        return msg;
    }
    pub fn wakelockIdNext(self: *KernelWakelockDataReader) gremlin.Error!?u32 {
        if (self._wakelock_id_offset == null) return null;
        const current_offset = self._wakelock_id_offset.?;
        if (current_offset >= self._wakelock_id_last_offset.?) {
            self._wakelock_id_offset = null;
            return null;
        }
        if (self._wakelock_id_packed) {
            const value_result = try self.buf.readUInt32(current_offset);
            self._wakelock_id_offset = current_offset + value_result.size;
            if (self._wakelock_id_offset.? >= self._wakelock_id_last_offset.?) {
                self._wakelock_id_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt32(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._wakelock_id_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == KernelWakelockDataWire.WAKELOCK_ID_WIRE) {
                    self._wakelock_id_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._wakelock_id_offset = null;
            return value_result.value;
        }
    }
    pub fn timeHeldMillisNext(self: *KernelWakelockDataReader) gremlin.Error!?u64 {
        if (self._time_held_millis_offset == null) return null;
        const current_offset = self._time_held_millis_offset.?;
        if (current_offset >= self._time_held_millis_last_offset.?) {
            self._time_held_millis_offset = null;
            return null;
        }
        if (self._time_held_millis_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._time_held_millis_offset = current_offset + value_result.size;
            if (self._time_held_millis_offset.? >= self._time_held_millis_last_offset.?) {
                self._time_held_millis_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._time_held_millis_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == KernelWakelockDataWire.TIME_HELD_MILLIS_WIRE) {
                    self._time_held_millis_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._time_held_millis_offset = null;
            return value_result.value;
        }
    }
    pub inline fn getErrorFlags(self: *const KernelWakelockDataReader) u64 {
        return self._error_flags;
    }
};
