// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const InitialDisplayStateWire = struct {
    const DISPLAY_STATE_WIRE: gremlin.ProtoWireNumber = 1;
    const BRIGHTNESS_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const InitialDisplayState = struct {
    // fields
    display_state: i32 = 0,
    brightness: f64 = 0.0,
    pub fn calcProtobufSize(self: *const InitialDisplayState) usize {
        var res: usize = 0;
        if (self.display_state != 0) {
            res += gremlin.sizes.sizeWireNumber(InitialDisplayStateWire.DISPLAY_STATE_WIRE) + gremlin.sizes.sizeI32(self.display_state);
        }
        if (self.brightness != 0.0) {
            res += gremlin.sizes.sizeWireNumber(InitialDisplayStateWire.BRIGHTNESS_WIRE) + gremlin.sizes.sizeDouble(self.brightness);
        }
        return res;
    }
    pub fn encode(self: *const InitialDisplayState, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InitialDisplayState, target: *gremlin.Writer) void {
        if (self.display_state != 0) {
            target.appendInt32(InitialDisplayStateWire.DISPLAY_STATE_WIRE, self.display_state);
        }
        if (self.brightness != 0.0) {
            target.appendFloat64(InitialDisplayStateWire.BRIGHTNESS_WIRE, self.brightness);
        }
    }
};
pub const InitialDisplayStateReader = struct {
    buf: gremlin.Reader,
    _display_state: i32 = 0,
    _brightness: f64 = 0.0,
    pub fn init(src: []const u8) gremlin.Error!InitialDisplayStateReader {
        const buf = gremlin.Reader.init(src);
        var res = InitialDisplayStateReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InitialDisplayStateWire.DISPLAY_STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._display_state = result.value;
                },
                InitialDisplayStateWire.BRIGHTNESS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._brightness = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDisplayState(self: *const InitialDisplayStateReader) i32 {
        return self._display_state;
    }
    pub inline fn getBrightness(self: *const InitialDisplayStateReader) f64 {
        return self._brightness;
    }
};
