// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PackagesListWire = struct {
    const PACKAGES_WIRE: gremlin.ProtoWireNumber = 1;
    const PARSE_ERROR_WIRE: gremlin.ProtoWireNumber = 2;
    const READ_ERROR_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const PackagesList = struct {
    // nested structs
    const PackageInfoWire = struct {
        const NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const UID_WIRE: gremlin.ProtoWireNumber = 2;
        const DEBUGGABLE_WIRE: gremlin.ProtoWireNumber = 3;
        const PROFILEABLE_FROM_SHELL_WIRE: gremlin.ProtoWireNumber = 4;
        const VERSION_CODE_WIRE: gremlin.ProtoWireNumber = 5;
    };
    pub const PackageInfo = struct {
        // fields
        name: ?[]const u8 = null,
        uid: u64 = 0,
        debuggable: bool = false,
        profileable_from_shell: bool = false,
        version_code: i64 = 0,
        pub fn calcProtobufSize(self: *const PackagesList.PackageInfo) usize {
            var res: usize = 0;
            if (self.name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(PackagesList.PackageInfoWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.uid != 0) {
                res += gremlin.sizes.sizeWireNumber(PackagesList.PackageInfoWire.UID_WIRE) + gremlin.sizes.sizeU64(self.uid);
            }
            if (self.debuggable != false) {
                res += gremlin.sizes.sizeWireNumber(PackagesList.PackageInfoWire.DEBUGGABLE_WIRE) + gremlin.sizes.sizeBool(self.debuggable);
            }
            if (self.profileable_from_shell != false) {
                res += gremlin.sizes.sizeWireNumber(PackagesList.PackageInfoWire.PROFILEABLE_FROM_SHELL_WIRE) + gremlin.sizes.sizeBool(self.profileable_from_shell);
            }
            if (self.version_code != 0) {
                res += gremlin.sizes.sizeWireNumber(PackagesList.PackageInfoWire.VERSION_CODE_WIRE) + gremlin.sizes.sizeI64(self.version_code);
            }
            return res;
        }
        pub fn encode(self: *const PackagesList.PackageInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const PackagesList.PackageInfo, target: *gremlin.Writer) void {
            if (self.name) |v| {
                if (v.len > 0) {
                    target.appendBytes(PackagesList.PackageInfoWire.NAME_WIRE, v);
                }
            }
            if (self.uid != 0) {
                target.appendUint64(PackagesList.PackageInfoWire.UID_WIRE, self.uid);
            }
            if (self.debuggable != false) {
                target.appendBool(PackagesList.PackageInfoWire.DEBUGGABLE_WIRE, self.debuggable);
            }
            if (self.profileable_from_shell != false) {
                target.appendBool(PackagesList.PackageInfoWire.PROFILEABLE_FROM_SHELL_WIRE, self.profileable_from_shell);
            }
            if (self.version_code != 0) {
                target.appendInt64(PackagesList.PackageInfoWire.VERSION_CODE_WIRE, self.version_code);
            }
        }
    };
    pub const PackageInfoReader = struct {
        buf: gremlin.Reader,
        _name: ?[]const u8 = null,
        _uid: u64 = 0,
        _debuggable: bool = false,
        _profileable_from_shell: bool = false,
        _version_code: i64 = 0,
        pub fn init(src: []const u8) gremlin.Error!PackagesList.PackageInfoReader {
            const buf = gremlin.Reader.init(src);
            var res = PackagesList.PackageInfoReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    PackagesList.PackageInfoWire.NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._name = result.value;
                    },
                    PackagesList.PackageInfoWire.UID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._uid = result.value;
                    },
                    PackagesList.PackageInfoWire.DEBUGGABLE_WIRE => {
                        const result = try buf.readBool(offset);
                        offset += result.size;
                        res._debuggable = result.value;
                    },
                    PackagesList.PackageInfoWire.PROFILEABLE_FROM_SHELL_WIRE => {
                        const result = try buf.readBool(offset);
                        offset += result.size;
                        res._profileable_from_shell = result.value;
                    },
                    PackagesList.PackageInfoWire.VERSION_CODE_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._version_code = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getName(self: *const PackagesList.PackageInfoReader) []const u8 {
            return self._name orelse &[_]u8{};
        }
        pub inline fn getUid(self: *const PackagesList.PackageInfoReader) u64 {
            return self._uid;
        }
        pub inline fn getDebuggable(self: *const PackagesList.PackageInfoReader) bool {
            return self._debuggable;
        }
        pub inline fn getProfileableFromShell(self: *const PackagesList.PackageInfoReader) bool {
            return self._profileable_from_shell;
        }
        pub inline fn getVersionCode(self: *const PackagesList.PackageInfoReader) i64 {
            return self._version_code;
        }
    };
    // fields
    packages: ?[]const ?PackagesList.PackageInfo = null,
    parse_error: bool = false,
    read_error: bool = false,
    pub fn calcProtobufSize(self: *const PackagesList) usize {
        var res: usize = 0;
        if (self.packages) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(PackagesListWire.PACKAGES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.parse_error != false) {
            res += gremlin.sizes.sizeWireNumber(PackagesListWire.PARSE_ERROR_WIRE) + gremlin.sizes.sizeBool(self.parse_error);
        }
        if (self.read_error != false) {
            res += gremlin.sizes.sizeWireNumber(PackagesListWire.READ_ERROR_WIRE) + gremlin.sizes.sizeBool(self.read_error);
        }
        return res;
    }
    pub fn encode(self: *const PackagesList, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PackagesList, target: *gremlin.Writer) void {
        if (self.packages) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(PackagesListWire.PACKAGES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(PackagesListWire.PACKAGES_WIRE, 0);
                }
            }
        }
        if (self.parse_error != false) {
            target.appendBool(PackagesListWire.PARSE_ERROR_WIRE, self.parse_error);
        }
        if (self.read_error != false) {
            target.appendBool(PackagesListWire.READ_ERROR_WIRE, self.read_error);
        }
    }
};
pub const PackagesListReader = struct {
    buf: gremlin.Reader,
    _packages_offset: ?usize = null,
    _packages_last_offset: ?usize = null,
    _packages_cnt: usize = 0,
    _parse_error: bool = false,
    _read_error: bool = false,
    pub fn init(src: []const u8) gremlin.Error!PackagesListReader {
        const buf = gremlin.Reader.init(src);
        var res = PackagesListReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PackagesListWire.PACKAGES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._packages_offset == null) {
                        res._packages_offset = offset - result.size;
                    }
                    res._packages_last_offset = offset;
                    res._packages_cnt += 1;
                },
                PackagesListWire.PARSE_ERROR_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._parse_error = result.value;
                },
                PackagesListWire.READ_ERROR_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._read_error = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn packagesCount(self: *const PackagesListReader) usize {
        return self._packages_cnt;
    }
    pub fn packagesNext(self: *PackagesListReader) ?PackagesList.PackageInfoReader {
        if (self._packages_offset == null) return null;
        const current_offset = self._packages_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = PackagesList.PackageInfoReader.init(result.value) catch return null;
        if (self._packages_last_offset != null and current_offset >= self._packages_last_offset.?) {
            self._packages_offset = null;
            return msg;
        }
        if (self._packages_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._packages_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == PackagesListWire.PACKAGES_WIRE) {
                self._packages_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._packages_offset = null;
        return msg;
    }
    pub inline fn getParseError(self: *const PackagesListReader) bool {
        return self._parse_error;
    }
    pub inline fn getReadError(self: *const PackagesListReader) bool {
        return self._read_error;
    }
};
