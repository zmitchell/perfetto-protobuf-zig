// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const AndroidSystemPropertyWire = struct {
    const VALUES_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const AndroidSystemProperty = struct {
    // nested structs
    const PropertyValueWire = struct {
        const NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const VALUE_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const PropertyValue = struct {
        // fields
        name: ?[]const u8 = null,
        value: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const AndroidSystemProperty.PropertyValue) usize {
            var res: usize = 0;
            if (self.name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(AndroidSystemProperty.PropertyValueWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.value) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(AndroidSystemProperty.PropertyValueWire.VALUE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const AndroidSystemProperty.PropertyValue, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const AndroidSystemProperty.PropertyValue, target: *gremlin.Writer) void {
            if (self.name) |v| {
                if (v.len > 0) {
                    target.appendBytes(AndroidSystemProperty.PropertyValueWire.NAME_WIRE, v);
                }
            }
            if (self.value) |v| {
                if (v.len > 0) {
                    target.appendBytes(AndroidSystemProperty.PropertyValueWire.VALUE_WIRE, v);
                }
            }
        }
    };
    pub const PropertyValueReader = struct {
        buf: gremlin.Reader,
        _name: ?[]const u8 = null,
        _value: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!AndroidSystemProperty.PropertyValueReader {
            const buf = gremlin.Reader.init(src);
            var res = AndroidSystemProperty.PropertyValueReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    AndroidSystemProperty.PropertyValueWire.NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._name = result.value;
                    },
                    AndroidSystemProperty.PropertyValueWire.VALUE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._value = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getName(self: *const AndroidSystemProperty.PropertyValueReader) []const u8 {
            return self._name orelse &[_]u8{};
        }
        pub inline fn getValue(self: *const AndroidSystemProperty.PropertyValueReader) []const u8 {
            return self._value orelse &[_]u8{};
        }
    };
    // fields
    values: ?[]const ?AndroidSystemProperty.PropertyValue = null,
    pub fn calcProtobufSize(self: *const AndroidSystemProperty) usize {
        var res: usize = 0;
        if (self.values) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(AndroidSystemPropertyWire.VALUES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const AndroidSystemProperty, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidSystemProperty, target: *gremlin.Writer) void {
        if (self.values) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(AndroidSystemPropertyWire.VALUES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(AndroidSystemPropertyWire.VALUES_WIRE, 0);
                }
            }
        }
    }
};
pub const AndroidSystemPropertyReader = struct {
    buf: gremlin.Reader,
    _values_offset: ?usize = null,
    _values_last_offset: ?usize = null,
    _values_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidSystemPropertyReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidSystemPropertyReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidSystemPropertyWire.VALUES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._values_offset == null) {
                        res._values_offset = offset - result.size;
                    }
                    res._values_last_offset = offset;
                    res._values_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn valuesCount(self: *const AndroidSystemPropertyReader) usize {
        return self._values_cnt;
    }
    pub fn valuesNext(self: *AndroidSystemPropertyReader) ?AndroidSystemProperty.PropertyValueReader {
        if (self._values_offset == null) return null;
        const current_offset = self._values_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = AndroidSystemProperty.PropertyValueReader.init(result.value) catch return null;
        if (self._values_last_offset != null and current_offset >= self._values_last_offset.?) {
            self._values_offset = null;
            return msg;
        }
        if (self._values_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._values_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == AndroidSystemPropertyWire.VALUES_WIRE) {
                self._values_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._values_offset = null;
        return msg;
    }
};
