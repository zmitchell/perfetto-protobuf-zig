// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const inputmethodmanager = @import("src/gen/protos/perfetto/trace/android/view/inputmethod/inputmethodmanager.proto.zig");
const viewrootimpl = @import("src/gen/protos/perfetto/trace/android/view/viewrootimpl.proto.zig");
const insetscontroller = @import("src/gen/protos/perfetto/trace/android/view/insetscontroller.proto.zig");
const imeinsetssourceconsumer = @import("src/gen/protos/perfetto/trace/android/view/imeinsetssourceconsumer.proto.zig");
const editorinfo = @import("src/gen/protos/perfetto/trace/android/view/inputmethod/editorinfo.proto.zig");
const imefocuscontroller = @import("src/gen/protos/perfetto/trace/android/view/imefocuscontroller.proto.zig");
const inputconnection = @import("src/gen/protos/perfetto/trace/android/view/inputmethod/inputconnection.proto.zig");
const inputmethodservice = @import("src/gen/protos/perfetto/trace/android/inputmethodservice/inputmethodservice.proto.zig");
const inputmethodmanagerservice = @import("src/gen/protos/perfetto/trace/android/server/inputmethod/inputmethodmanagerservice.proto.zig");
// structs
const InputMethodClientsTraceProtoWire = struct {
    const ELAPSED_REALTIME_NANOS_WIRE: gremlin.ProtoWireNumber = 1;
    const WHERE_WIRE: gremlin.ProtoWireNumber = 2;
    const CLIENT_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const InputMethodClientsTraceProto = struct {
    // nested structs
    const ClientSideProtoWire = struct {
        const DISPLAY_ID_WIRE: gremlin.ProtoWireNumber = 1;
        const INPUT_METHOD_MANAGER_WIRE: gremlin.ProtoWireNumber = 2;
        const VIEW_ROOT_IMPL_WIRE: gremlin.ProtoWireNumber = 3;
        const INSETS_CONTROLLER_WIRE: gremlin.ProtoWireNumber = 4;
        const IME_INSETS_SOURCE_CONSUMER_WIRE: gremlin.ProtoWireNumber = 5;
        const EDITOR_INFO_WIRE: gremlin.ProtoWireNumber = 6;
        const IME_FOCUS_CONTROLLER_WIRE: gremlin.ProtoWireNumber = 7;
        const INPUT_CONNECTION_WIRE: gremlin.ProtoWireNumber = 8;
        const INPUT_CONNECTION_CALL_WIRE: gremlin.ProtoWireNumber = 9;
    };
    pub const ClientSideProto = struct {
        // fields
        display_id: i32 = 0,
        input_method_manager: ?inputmethodmanager.InputMethodManagerProto = null,
        view_root_impl: ?viewrootimpl.ViewRootImplProto = null,
        insets_controller: ?insetscontroller.InsetsControllerProto = null,
        ime_insets_source_consumer: ?imeinsetssourceconsumer.ImeInsetsSourceConsumerProto = null,
        editor_info: ?editorinfo.EditorInfoProto = null,
        ime_focus_controller: ?imefocuscontroller.ImeFocusControllerProto = null,
        input_connection: ?inputconnection.InputConnectionProto = null,
        input_connection_call: ?inputconnection.InputConnectionCallProto = null,
        pub fn calcProtobufSize(self: *const InputMethodClientsTraceProto.ClientSideProto) usize {
            var res: usize = 0;
            if (self.display_id != 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.DISPLAY_ID_WIRE) + gremlin.sizes.sizeI32(self.display_id);
            }
            if (self.input_method_manager) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_METHOD_MANAGER_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            if (self.view_root_impl) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.VIEW_ROOT_IMPL_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            if (self.insets_controller) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.INSETS_CONTROLLER_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            if (self.ime_insets_source_consumer) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.IME_INSETS_SOURCE_CONSUMER_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            if (self.editor_info) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.EDITOR_INFO_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            if (self.ime_focus_controller) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.IME_FOCUS_CONTROLLER_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            if (self.input_connection) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_CONNECTION_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            if (self.input_connection_call) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_CONNECTION_CALL_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            return res;
        }
        pub fn encode(self: *const InputMethodClientsTraceProto.ClientSideProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const InputMethodClientsTraceProto.ClientSideProto, target: *gremlin.Writer) void {
            if (self.display_id != 0) {
                target.appendInt32(InputMethodClientsTraceProto.ClientSideProtoWire.DISPLAY_ID_WIRE, self.display_id);
            }
            if (self.input_method_manager) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_METHOD_MANAGER_WIRE, size);
                    v.encodeTo(target);
                }
            }
            if (self.view_root_impl) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(InputMethodClientsTraceProto.ClientSideProtoWire.VIEW_ROOT_IMPL_WIRE, size);
                    v.encodeTo(target);
                }
            }
            if (self.insets_controller) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(InputMethodClientsTraceProto.ClientSideProtoWire.INSETS_CONTROLLER_WIRE, size);
                    v.encodeTo(target);
                }
            }
            if (self.ime_insets_source_consumer) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(InputMethodClientsTraceProto.ClientSideProtoWire.IME_INSETS_SOURCE_CONSUMER_WIRE, size);
                    v.encodeTo(target);
                }
            }
            if (self.editor_info) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(InputMethodClientsTraceProto.ClientSideProtoWire.EDITOR_INFO_WIRE, size);
                    v.encodeTo(target);
                }
            }
            if (self.ime_focus_controller) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(InputMethodClientsTraceProto.ClientSideProtoWire.IME_FOCUS_CONTROLLER_WIRE, size);
                    v.encodeTo(target);
                }
            }
            if (self.input_connection) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_CONNECTION_WIRE, size);
                    v.encodeTo(target);
                }
            }
            if (self.input_connection_call) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_CONNECTION_CALL_WIRE, size);
                    v.encodeTo(target);
                }
            }
        }
    };
    pub const ClientSideProtoReader = struct {
        buf: gremlin.Reader,
        _display_id: i32 = 0,
        _input_method_manager_buf: ?[]const u8 = null,
        _view_root_impl_buf: ?[]const u8 = null,
        _insets_controller_buf: ?[]const u8 = null,
        _ime_insets_source_consumer_buf: ?[]const u8 = null,
        _editor_info_buf: ?[]const u8 = null,
        _ime_focus_controller_buf: ?[]const u8 = null,
        _input_connection_buf: ?[]const u8 = null,
        _input_connection_call_buf: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!InputMethodClientsTraceProto.ClientSideProtoReader {
            const buf = gremlin.Reader.init(src);
            var res = InputMethodClientsTraceProto.ClientSideProtoReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    InputMethodClientsTraceProto.ClientSideProtoWire.DISPLAY_ID_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._display_id = result.value;
                    },
                    InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_METHOD_MANAGER_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._input_method_manager_buf = result.value;
                    },
                    InputMethodClientsTraceProto.ClientSideProtoWire.VIEW_ROOT_IMPL_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._view_root_impl_buf = result.value;
                    },
                    InputMethodClientsTraceProto.ClientSideProtoWire.INSETS_CONTROLLER_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._insets_controller_buf = result.value;
                    },
                    InputMethodClientsTraceProto.ClientSideProtoWire.IME_INSETS_SOURCE_CONSUMER_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._ime_insets_source_consumer_buf = result.value;
                    },
                    InputMethodClientsTraceProto.ClientSideProtoWire.EDITOR_INFO_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._editor_info_buf = result.value;
                    },
                    InputMethodClientsTraceProto.ClientSideProtoWire.IME_FOCUS_CONTROLLER_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._ime_focus_controller_buf = result.value;
                    },
                    InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_CONNECTION_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._input_connection_buf = result.value;
                    },
                    InputMethodClientsTraceProto.ClientSideProtoWire.INPUT_CONNECTION_CALL_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._input_connection_call_buf = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getDisplayId(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) i32 {
            return self._display_id;
        }
        pub fn getInputMethodManager(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) gremlin.Error!inputmethodmanager.InputMethodManagerProtoReader {
            if (self._input_method_manager_buf) |buf| {
                return try inputmethodmanager.InputMethodManagerProtoReader.init(buf);
            }
            return try inputmethodmanager.InputMethodManagerProtoReader.init(&[_]u8{});
        }
        pub fn getViewRootImpl(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) gremlin.Error!viewrootimpl.ViewRootImplProtoReader {
            if (self._view_root_impl_buf) |buf| {
                return try viewrootimpl.ViewRootImplProtoReader.init(buf);
            }
            return try viewrootimpl.ViewRootImplProtoReader.init(&[_]u8{});
        }
        pub fn getInsetsController(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) gremlin.Error!insetscontroller.InsetsControllerProtoReader {
            if (self._insets_controller_buf) |buf| {
                return try insetscontroller.InsetsControllerProtoReader.init(buf);
            }
            return try insetscontroller.InsetsControllerProtoReader.init(&[_]u8{});
        }
        pub fn getImeInsetsSourceConsumer(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) gremlin.Error!imeinsetssourceconsumer.ImeInsetsSourceConsumerProtoReader {
            if (self._ime_insets_source_consumer_buf) |buf| {
                return try imeinsetssourceconsumer.ImeInsetsSourceConsumerProtoReader.init(buf);
            }
            return try imeinsetssourceconsumer.ImeInsetsSourceConsumerProtoReader.init(&[_]u8{});
        }
        pub fn getEditorInfo(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) gremlin.Error!editorinfo.EditorInfoProtoReader {
            if (self._editor_info_buf) |buf| {
                return try editorinfo.EditorInfoProtoReader.init(buf);
            }
            return try editorinfo.EditorInfoProtoReader.init(&[_]u8{});
        }
        pub fn getImeFocusController(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) gremlin.Error!imefocuscontroller.ImeFocusControllerProtoReader {
            if (self._ime_focus_controller_buf) |buf| {
                return try imefocuscontroller.ImeFocusControllerProtoReader.init(buf);
            }
            return try imefocuscontroller.ImeFocusControllerProtoReader.init(&[_]u8{});
        }
        pub fn getInputConnection(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) gremlin.Error!inputconnection.InputConnectionProtoReader {
            if (self._input_connection_buf) |buf| {
                return try inputconnection.InputConnectionProtoReader.init(buf);
            }
            return try inputconnection.InputConnectionProtoReader.init(&[_]u8{});
        }
        pub fn getInputConnectionCall(self: *const InputMethodClientsTraceProto.ClientSideProtoReader) gremlin.Error!inputconnection.InputConnectionCallProtoReader {
            if (self._input_connection_call_buf) |buf| {
                return try inputconnection.InputConnectionCallProtoReader.init(buf);
            }
            return try inputconnection.InputConnectionCallProtoReader.init(&[_]u8{});
        }
    };
    // fields
    elapsed_realtime_nanos: u64 = 0,
    where: ?[]const u8 = null,
    client: ?InputMethodClientsTraceProto.ClientSideProto = null,
    pub fn calcProtobufSize(self: *const InputMethodClientsTraceProto) usize {
        var res: usize = 0;
        if (self.elapsed_realtime_nanos != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE) + gremlin.sizes.sizeFixed64(self.elapsed_realtime_nanos);
        }
        if (self.where) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProtoWire.WHERE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.client) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceProtoWire.CLIENT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const InputMethodClientsTraceProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InputMethodClientsTraceProto, target: *gremlin.Writer) void {
        if (self.elapsed_realtime_nanos != 0) {
            target.appendFixed64(InputMethodClientsTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE, self.elapsed_realtime_nanos);
        }
        if (self.where) |v| {
            if (v.len > 0) {
                target.appendBytes(InputMethodClientsTraceProtoWire.WHERE_WIRE, v);
            }
        }
        if (self.client) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InputMethodClientsTraceProtoWire.CLIENT_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const InputMethodClientsTraceProtoReader = struct {
    buf: gremlin.Reader,
    _elapsed_realtime_nanos: u64 = 0,
    _where: ?[]const u8 = null,
    _client_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!InputMethodClientsTraceProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InputMethodClientsTraceProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InputMethodClientsTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._elapsed_realtime_nanos = result.value;
                },
                InputMethodClientsTraceProtoWire.WHERE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._where = result.value;
                },
                InputMethodClientsTraceProtoWire.CLIENT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._client_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getElapsedRealtimeNanos(self: *const InputMethodClientsTraceProtoReader) u64 {
        return self._elapsed_realtime_nanos;
    }
    pub inline fn getWhere(self: *const InputMethodClientsTraceProtoReader) []const u8 {
        return self._where orelse &[_]u8{};
    }
    pub fn getClient(self: *const InputMethodClientsTraceProtoReader) gremlin.Error!InputMethodClientsTraceProto.ClientSideProtoReader {
        if (self._client_buf) |buf| {
            return try InputMethodClientsTraceProto.ClientSideProtoReader.init(buf);
        }
        return try InputMethodClientsTraceProto.ClientSideProtoReader.init(&[_]u8{});
    }
};
const InputMethodServiceTraceProtoWire = struct {
    const ELAPSED_REALTIME_NANOS_WIRE: gremlin.ProtoWireNumber = 1;
    const WHERE_WIRE: gremlin.ProtoWireNumber = 2;
    const INPUT_METHOD_SERVICE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const InputMethodServiceTraceProto = struct {
    // fields
    elapsed_realtime_nanos: u64 = 0,
    where: ?[]const u8 = null,
    input_method_service: ?inputmethodservice.InputMethodServiceProto = null,
    pub fn calcProtobufSize(self: *const InputMethodServiceTraceProto) usize {
        var res: usize = 0;
        if (self.elapsed_realtime_nanos != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodServiceTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE) + gremlin.sizes.sizeFixed64(self.elapsed_realtime_nanos);
        }
        if (self.where) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodServiceTraceProtoWire.WHERE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.input_method_service) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodServiceTraceProtoWire.INPUT_METHOD_SERVICE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const InputMethodServiceTraceProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InputMethodServiceTraceProto, target: *gremlin.Writer) void {
        if (self.elapsed_realtime_nanos != 0) {
            target.appendFixed64(InputMethodServiceTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE, self.elapsed_realtime_nanos);
        }
        if (self.where) |v| {
            if (v.len > 0) {
                target.appendBytes(InputMethodServiceTraceProtoWire.WHERE_WIRE, v);
            }
        }
        if (self.input_method_service) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InputMethodServiceTraceProtoWire.INPUT_METHOD_SERVICE_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const InputMethodServiceTraceProtoReader = struct {
    buf: gremlin.Reader,
    _elapsed_realtime_nanos: u64 = 0,
    _where: ?[]const u8 = null,
    _input_method_service_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!InputMethodServiceTraceProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InputMethodServiceTraceProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InputMethodServiceTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._elapsed_realtime_nanos = result.value;
                },
                InputMethodServiceTraceProtoWire.WHERE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._where = result.value;
                },
                InputMethodServiceTraceProtoWire.INPUT_METHOD_SERVICE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._input_method_service_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getElapsedRealtimeNanos(self: *const InputMethodServiceTraceProtoReader) u64 {
        return self._elapsed_realtime_nanos;
    }
    pub inline fn getWhere(self: *const InputMethodServiceTraceProtoReader) []const u8 {
        return self._where orelse &[_]u8{};
    }
    pub fn getInputMethodService(self: *const InputMethodServiceTraceProtoReader) gremlin.Error!inputmethodservice.InputMethodServiceProtoReader {
        if (self._input_method_service_buf) |buf| {
            return try inputmethodservice.InputMethodServiceProtoReader.init(buf);
        }
        return try inputmethodservice.InputMethodServiceProtoReader.init(&[_]u8{});
    }
};
const InputMethodManagerServiceTraceProtoWire = struct {
    const ELAPSED_REALTIME_NANOS_WIRE: gremlin.ProtoWireNumber = 1;
    const WHERE_WIRE: gremlin.ProtoWireNumber = 2;
    const INPUT_METHOD_MANAGER_SERVICE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const InputMethodManagerServiceTraceProto = struct {
    // fields
    elapsed_realtime_nanos: u64 = 0,
    where: ?[]const u8 = null,
    input_method_manager_service: ?inputmethodmanagerservice.InputMethodManagerServiceProto = null,
    pub fn calcProtobufSize(self: *const InputMethodManagerServiceTraceProto) usize {
        var res: usize = 0;
        if (self.elapsed_realtime_nanos != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodManagerServiceTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE) + gremlin.sizes.sizeFixed64(self.elapsed_realtime_nanos);
        }
        if (self.where) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodManagerServiceTraceProtoWire.WHERE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.input_method_manager_service) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodManagerServiceTraceProtoWire.INPUT_METHOD_MANAGER_SERVICE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const InputMethodManagerServiceTraceProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InputMethodManagerServiceTraceProto, target: *gremlin.Writer) void {
        if (self.elapsed_realtime_nanos != 0) {
            target.appendFixed64(InputMethodManagerServiceTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE, self.elapsed_realtime_nanos);
        }
        if (self.where) |v| {
            if (v.len > 0) {
                target.appendBytes(InputMethodManagerServiceTraceProtoWire.WHERE_WIRE, v);
            }
        }
        if (self.input_method_manager_service) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InputMethodManagerServiceTraceProtoWire.INPUT_METHOD_MANAGER_SERVICE_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const InputMethodManagerServiceTraceProtoReader = struct {
    buf: gremlin.Reader,
    _elapsed_realtime_nanos: u64 = 0,
    _where: ?[]const u8 = null,
    _input_method_manager_service_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!InputMethodManagerServiceTraceProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InputMethodManagerServiceTraceProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InputMethodManagerServiceTraceProtoWire.ELAPSED_REALTIME_NANOS_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._elapsed_realtime_nanos = result.value;
                },
                InputMethodManagerServiceTraceProtoWire.WHERE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._where = result.value;
                },
                InputMethodManagerServiceTraceProtoWire.INPUT_METHOD_MANAGER_SERVICE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._input_method_manager_service_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getElapsedRealtimeNanos(self: *const InputMethodManagerServiceTraceProtoReader) u64 {
        return self._elapsed_realtime_nanos;
    }
    pub inline fn getWhere(self: *const InputMethodManagerServiceTraceProtoReader) []const u8 {
        return self._where orelse &[_]u8{};
    }
    pub fn getInputMethodManagerService(self: *const InputMethodManagerServiceTraceProtoReader) gremlin.Error!inputmethodmanagerservice.InputMethodManagerServiceProtoReader {
        if (self._input_method_manager_service_buf) |buf| {
            return try inputmethodmanagerservice.InputMethodManagerServiceProtoReader.init(buf);
        }
        return try inputmethodmanagerservice.InputMethodManagerServiceProtoReader.init(&[_]u8{});
    }
};
const InputMethodClientsTraceFileProtoWire = struct {
    const MAGIC_NUMBER_WIRE: gremlin.ProtoWireNumber = 1;
    const ENTRY_WIRE: gremlin.ProtoWireNumber = 2;
    const REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const InputMethodClientsTraceFileProto = struct {
    // nested enums
    pub const MagicNumber = enum(i32) {
        INVALID = 0,
        MAGIC_NUMBER_L = 1413696841,
        MAGIC_NUMBER_H = 1162035538,
    };
    // fields
    magic_number: u64 = 0,
    entry: ?[]const ?InputMethodClientsTraceProto = null,
    real_to_elapsed_time_offset_nanos: u64 = 0,
    pub fn calcProtobufSize(self: *const InputMethodClientsTraceFileProto) usize {
        var res: usize = 0;
        if (self.magic_number != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceFileProtoWire.MAGIC_NUMBER_WIRE) + gremlin.sizes.sizeFixed64(self.magic_number);
        }
        if (self.entry) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceFileProtoWire.ENTRY_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.real_to_elapsed_time_offset_nanos != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodClientsTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE) + gremlin.sizes.sizeFixed64(self.real_to_elapsed_time_offset_nanos);
        }
        return res;
    }
    pub fn encode(self: *const InputMethodClientsTraceFileProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InputMethodClientsTraceFileProto, target: *gremlin.Writer) void {
        if (self.magic_number != 0) {
            target.appendFixed64(InputMethodClientsTraceFileProtoWire.MAGIC_NUMBER_WIRE, self.magic_number);
        }
        if (self.entry) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(InputMethodClientsTraceFileProtoWire.ENTRY_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(InputMethodClientsTraceFileProtoWire.ENTRY_WIRE, 0);
                }
            }
        }
        if (self.real_to_elapsed_time_offset_nanos != 0) {
            target.appendFixed64(InputMethodClientsTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE, self.real_to_elapsed_time_offset_nanos);
        }
    }
};
pub const InputMethodClientsTraceFileProtoReader = struct {
    buf: gremlin.Reader,
    _magic_number: u64 = 0,
    _entry_offset: ?usize = null,
    _entry_last_offset: ?usize = null,
    _entry_cnt: usize = 0,
    _real_to_elapsed_time_offset_nanos: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!InputMethodClientsTraceFileProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InputMethodClientsTraceFileProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InputMethodClientsTraceFileProtoWire.MAGIC_NUMBER_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._magic_number = result.value;
                },
                InputMethodClientsTraceFileProtoWire.ENTRY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._entry_offset == null) {
                        res._entry_offset = offset - result.size;
                    }
                    res._entry_last_offset = offset;
                    res._entry_cnt += 1;
                },
                InputMethodClientsTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._real_to_elapsed_time_offset_nanos = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getMagicNumber(self: *const InputMethodClientsTraceFileProtoReader) u64 {
        return self._magic_number;
    }
    pub fn entryCount(self: *const InputMethodClientsTraceFileProtoReader) usize {
        return self._entry_cnt;
    }
    pub fn entryNext(self: *InputMethodClientsTraceFileProtoReader) ?InputMethodClientsTraceProtoReader {
        if (self._entry_offset == null) return null;
        const current_offset = self._entry_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = InputMethodClientsTraceProtoReader.init(result.value) catch return null;
        if (self._entry_last_offset != null and current_offset >= self._entry_last_offset.?) {
            self._entry_offset = null;
            return msg;
        }
        if (self._entry_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._entry_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == InputMethodClientsTraceFileProtoWire.ENTRY_WIRE) {
                self._entry_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._entry_offset = null;
        return msg;
    }
    pub inline fn getRealToElapsedTimeOffsetNanos(self: *const InputMethodClientsTraceFileProtoReader) u64 {
        return self._real_to_elapsed_time_offset_nanos;
    }
};
const InputMethodServiceTraceFileProtoWire = struct {
    const MAGIC_NUMBER_WIRE: gremlin.ProtoWireNumber = 1;
    const ENTRY_WIRE: gremlin.ProtoWireNumber = 2;
    const REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const InputMethodServiceTraceFileProto = struct {
    // nested enums
    pub const MagicNumber = enum(i32) {
        INVALID = 0,
        MAGIC_NUMBER_L = 1414745417,
        MAGIC_NUMBER_H = 1162035538,
    };
    // fields
    magic_number: u64 = 0,
    entry: ?[]const ?InputMethodServiceTraceProto = null,
    real_to_elapsed_time_offset_nanos: u64 = 0,
    pub fn calcProtobufSize(self: *const InputMethodServiceTraceFileProto) usize {
        var res: usize = 0;
        if (self.magic_number != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodServiceTraceFileProtoWire.MAGIC_NUMBER_WIRE) + gremlin.sizes.sizeFixed64(self.magic_number);
        }
        if (self.entry) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(InputMethodServiceTraceFileProtoWire.ENTRY_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.real_to_elapsed_time_offset_nanos != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodServiceTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE) + gremlin.sizes.sizeFixed64(self.real_to_elapsed_time_offset_nanos);
        }
        return res;
    }
    pub fn encode(self: *const InputMethodServiceTraceFileProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InputMethodServiceTraceFileProto, target: *gremlin.Writer) void {
        if (self.magic_number != 0) {
            target.appendFixed64(InputMethodServiceTraceFileProtoWire.MAGIC_NUMBER_WIRE, self.magic_number);
        }
        if (self.entry) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(InputMethodServiceTraceFileProtoWire.ENTRY_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(InputMethodServiceTraceFileProtoWire.ENTRY_WIRE, 0);
                }
            }
        }
        if (self.real_to_elapsed_time_offset_nanos != 0) {
            target.appendFixed64(InputMethodServiceTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE, self.real_to_elapsed_time_offset_nanos);
        }
    }
};
pub const InputMethodServiceTraceFileProtoReader = struct {
    buf: gremlin.Reader,
    _magic_number: u64 = 0,
    _entry_offset: ?usize = null,
    _entry_last_offset: ?usize = null,
    _entry_cnt: usize = 0,
    _real_to_elapsed_time_offset_nanos: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!InputMethodServiceTraceFileProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InputMethodServiceTraceFileProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InputMethodServiceTraceFileProtoWire.MAGIC_NUMBER_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._magic_number = result.value;
                },
                InputMethodServiceTraceFileProtoWire.ENTRY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._entry_offset == null) {
                        res._entry_offset = offset - result.size;
                    }
                    res._entry_last_offset = offset;
                    res._entry_cnt += 1;
                },
                InputMethodServiceTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._real_to_elapsed_time_offset_nanos = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getMagicNumber(self: *const InputMethodServiceTraceFileProtoReader) u64 {
        return self._magic_number;
    }
    pub fn entryCount(self: *const InputMethodServiceTraceFileProtoReader) usize {
        return self._entry_cnt;
    }
    pub fn entryNext(self: *InputMethodServiceTraceFileProtoReader) ?InputMethodServiceTraceProtoReader {
        if (self._entry_offset == null) return null;
        const current_offset = self._entry_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = InputMethodServiceTraceProtoReader.init(result.value) catch return null;
        if (self._entry_last_offset != null and current_offset >= self._entry_last_offset.?) {
            self._entry_offset = null;
            return msg;
        }
        if (self._entry_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._entry_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == InputMethodServiceTraceFileProtoWire.ENTRY_WIRE) {
                self._entry_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._entry_offset = null;
        return msg;
    }
    pub inline fn getRealToElapsedTimeOffsetNanos(self: *const InputMethodServiceTraceFileProtoReader) u64 {
        return self._real_to_elapsed_time_offset_nanos;
    }
};
const InputMethodManagerServiceTraceFileProtoWire = struct {
    const MAGIC_NUMBER_WIRE: gremlin.ProtoWireNumber = 1;
    const ENTRY_WIRE: gremlin.ProtoWireNumber = 2;
    const REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const InputMethodManagerServiceTraceFileProto = struct {
    // nested enums
    pub const MagicNumber = enum(i32) {
        INVALID = 0,
        MAGIC_NUMBER_L = 1414352201,
        MAGIC_NUMBER_H = 1162035538,
    };
    // fields
    magic_number: u64 = 0,
    entry: ?[]const ?InputMethodManagerServiceTraceProto = null,
    real_to_elapsed_time_offset_nanos: u64 = 0,
    pub fn calcProtobufSize(self: *const InputMethodManagerServiceTraceFileProto) usize {
        var res: usize = 0;
        if (self.magic_number != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodManagerServiceTraceFileProtoWire.MAGIC_NUMBER_WIRE) + gremlin.sizes.sizeFixed64(self.magic_number);
        }
        if (self.entry) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(InputMethodManagerServiceTraceFileProtoWire.ENTRY_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.real_to_elapsed_time_offset_nanos != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodManagerServiceTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE) + gremlin.sizes.sizeFixed64(self.real_to_elapsed_time_offset_nanos);
        }
        return res;
    }
    pub fn encode(self: *const InputMethodManagerServiceTraceFileProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InputMethodManagerServiceTraceFileProto, target: *gremlin.Writer) void {
        if (self.magic_number != 0) {
            target.appendFixed64(InputMethodManagerServiceTraceFileProtoWire.MAGIC_NUMBER_WIRE, self.magic_number);
        }
        if (self.entry) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(InputMethodManagerServiceTraceFileProtoWire.ENTRY_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(InputMethodManagerServiceTraceFileProtoWire.ENTRY_WIRE, 0);
                }
            }
        }
        if (self.real_to_elapsed_time_offset_nanos != 0) {
            target.appendFixed64(InputMethodManagerServiceTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE, self.real_to_elapsed_time_offset_nanos);
        }
    }
};
pub const InputMethodManagerServiceTraceFileProtoReader = struct {
    buf: gremlin.Reader,
    _magic_number: u64 = 0,
    _entry_offset: ?usize = null,
    _entry_last_offset: ?usize = null,
    _entry_cnt: usize = 0,
    _real_to_elapsed_time_offset_nanos: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!InputMethodManagerServiceTraceFileProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InputMethodManagerServiceTraceFileProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InputMethodManagerServiceTraceFileProtoWire.MAGIC_NUMBER_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._magic_number = result.value;
                },
                InputMethodManagerServiceTraceFileProtoWire.ENTRY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._entry_offset == null) {
                        res._entry_offset = offset - result.size;
                    }
                    res._entry_last_offset = offset;
                    res._entry_cnt += 1;
                },
                InputMethodManagerServiceTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._real_to_elapsed_time_offset_nanos = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getMagicNumber(self: *const InputMethodManagerServiceTraceFileProtoReader) u64 {
        return self._magic_number;
    }
    pub fn entryCount(self: *const InputMethodManagerServiceTraceFileProtoReader) usize {
        return self._entry_cnt;
    }
    pub fn entryNext(self: *InputMethodManagerServiceTraceFileProtoReader) ?InputMethodManagerServiceTraceProtoReader {
        if (self._entry_offset == null) return null;
        const current_offset = self._entry_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = InputMethodManagerServiceTraceProtoReader.init(result.value) catch return null;
        if (self._entry_last_offset != null and current_offset >= self._entry_last_offset.?) {
            self._entry_offset = null;
            return msg;
        }
        if (self._entry_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._entry_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == InputMethodManagerServiceTraceFileProtoWire.ENTRY_WIRE) {
                self._entry_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._entry_offset = null;
        return msg;
    }
    pub inline fn getRealToElapsedTimeOffsetNanos(self: *const InputMethodManagerServiceTraceFileProtoReader) u64 {
        return self._real_to_elapsed_time_offset_nanos;
    }
};
