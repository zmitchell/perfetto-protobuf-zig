// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const rect = @import("src/gen/protos/perfetto/trace/android/graphics/rect.proto.zig");
// structs
const WindowConfigurationProtoWire = struct {
    const APP_BOUNDS_WIRE: gremlin.ProtoWireNumber = 1;
    const WINDOWING_MODE_WIRE: gremlin.ProtoWireNumber = 2;
    const ACTIVITY_TYPE_WIRE: gremlin.ProtoWireNumber = 3;
    const BOUNDS_WIRE: gremlin.ProtoWireNumber = 4;
    const MAX_BOUNDS_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const WindowConfigurationProto = struct {
    // fields
    app_bounds: ?rect.RectProto = null,
    windowing_mode: i32 = 0,
    activity_type: i32 = 0,
    bounds: ?rect.RectProto = null,
    max_bounds: ?rect.RectProto = null,
    pub fn calcProtobufSize(self: *const WindowConfigurationProto) usize {
        var res: usize = 0;
        if (self.app_bounds) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(WindowConfigurationProtoWire.APP_BOUNDS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.windowing_mode != 0) {
            res += gremlin.sizes.sizeWireNumber(WindowConfigurationProtoWire.WINDOWING_MODE_WIRE) + gremlin.sizes.sizeI32(self.windowing_mode);
        }
        if (self.activity_type != 0) {
            res += gremlin.sizes.sizeWireNumber(WindowConfigurationProtoWire.ACTIVITY_TYPE_WIRE) + gremlin.sizes.sizeI32(self.activity_type);
        }
        if (self.bounds) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(WindowConfigurationProtoWire.BOUNDS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.max_bounds) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(WindowConfigurationProtoWire.MAX_BOUNDS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const WindowConfigurationProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const WindowConfigurationProto, target: *gremlin.Writer) void {
        if (self.app_bounds) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(WindowConfigurationProtoWire.APP_BOUNDS_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.windowing_mode != 0) {
            target.appendInt32(WindowConfigurationProtoWire.WINDOWING_MODE_WIRE, self.windowing_mode);
        }
        if (self.activity_type != 0) {
            target.appendInt32(WindowConfigurationProtoWire.ACTIVITY_TYPE_WIRE, self.activity_type);
        }
        if (self.bounds) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(WindowConfigurationProtoWire.BOUNDS_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.max_bounds) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(WindowConfigurationProtoWire.MAX_BOUNDS_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const WindowConfigurationProtoReader = struct {
    buf: gremlin.Reader,
    _app_bounds_buf: ?[]const u8 = null,
    _windowing_mode: i32 = 0,
    _activity_type: i32 = 0,
    _bounds_buf: ?[]const u8 = null,
    _max_bounds_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!WindowConfigurationProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = WindowConfigurationProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                WindowConfigurationProtoWire.APP_BOUNDS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._app_bounds_buf = result.value;
                },
                WindowConfigurationProtoWire.WINDOWING_MODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._windowing_mode = result.value;
                },
                WindowConfigurationProtoWire.ACTIVITY_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._activity_type = result.value;
                },
                WindowConfigurationProtoWire.BOUNDS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._bounds_buf = result.value;
                },
                WindowConfigurationProtoWire.MAX_BOUNDS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._max_bounds_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getAppBounds(self: *const WindowConfigurationProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._app_bounds_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub inline fn getWindowingMode(self: *const WindowConfigurationProtoReader) i32 {
        return self._windowing_mode;
    }
    pub inline fn getActivityType(self: *const WindowConfigurationProtoReader) i32 {
        return self._activity_type;
    }
    pub fn getBounds(self: *const WindowConfigurationProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._bounds_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getMaxBounds(self: *const WindowConfigurationProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._max_bounds_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
};
