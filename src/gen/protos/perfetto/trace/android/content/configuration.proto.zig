// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const locale = @import("locale.proto.zig");
const window_configuration = @import("src/gen/protos/perfetto/trace/android/app/window_configuration.proto.zig");
// structs
const ConfigurationProtoWire = struct {
    const FONT_SCALE_WIRE: gremlin.ProtoWireNumber = 1;
    const MCC_WIRE: gremlin.ProtoWireNumber = 2;
    const MNC_WIRE: gremlin.ProtoWireNumber = 3;
    const LOCALES_WIRE: gremlin.ProtoWireNumber = 4;
    const SCREEN_LAYOUT_WIRE: gremlin.ProtoWireNumber = 5;
    const COLOR_MODE_WIRE: gremlin.ProtoWireNumber = 6;
    const TOUCHSCREEN_WIRE: gremlin.ProtoWireNumber = 7;
    const KEYBOARD_WIRE: gremlin.ProtoWireNumber = 8;
    const KEYBOARD_HIDDEN_WIRE: gremlin.ProtoWireNumber = 9;
    const HARD_KEYBOARD_HIDDEN_WIRE: gremlin.ProtoWireNumber = 10;
    const NAVIGATION_WIRE: gremlin.ProtoWireNumber = 11;
    const NAVIGATION_HIDDEN_WIRE: gremlin.ProtoWireNumber = 12;
    const ORIENTATION_WIRE: gremlin.ProtoWireNumber = 13;
    const UI_MODE_WIRE: gremlin.ProtoWireNumber = 14;
    const SCREEN_WIDTH_DP_WIRE: gremlin.ProtoWireNumber = 15;
    const SCREEN_HEIGHT_DP_WIRE: gremlin.ProtoWireNumber = 16;
    const SMALLEST_SCREEN_WIDTH_DP_WIRE: gremlin.ProtoWireNumber = 17;
    const DENSITY_DPI_WIRE: gremlin.ProtoWireNumber = 18;
    const WINDOW_CONFIGURATION_WIRE: gremlin.ProtoWireNumber = 19;
    const LOCALE_LIST_WIRE: gremlin.ProtoWireNumber = 20;
    const FONT_WEIGHT_ADJUSTMENT_WIRE: gremlin.ProtoWireNumber = 21;
    const GRAMMATICAL_GENDER_WIRE: gremlin.ProtoWireNumber = 22;
};
pub const ConfigurationProto = struct {
    // fields
    font_scale: f32 = 0.0,
    mcc: u32 = 0,
    mnc: u32 = 0,
    locales: ?[]const ?locale.LocaleProto = null,
    screen_layout: u32 = 0,
    color_mode: u32 = 0,
    touchscreen: u32 = 0,
    keyboard: u32 = 0,
    keyboard_hidden: u32 = 0,
    hard_keyboard_hidden: u32 = 0,
    navigation: u32 = 0,
    navigation_hidden: u32 = 0,
    orientation: u32 = 0,
    ui_mode: u32 = 0,
    screen_width_dp: u32 = 0,
    screen_height_dp: u32 = 0,
    smallest_screen_width_dp: u32 = 0,
    density_dpi: u32 = 0,
    window_configuration: ?window_configuration.WindowConfigurationProto = null,
    locale_list: ?[]const u8 = null,
    font_weight_adjustment: u32 = 0,
    grammatical_gender: u32 = 0,
    pub fn calcProtobufSize(self: *const ConfigurationProto) usize {
        var res: usize = 0;
        if (self.font_scale != 0.0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.FONT_SCALE_WIRE) + gremlin.sizes.sizeFloat(self.font_scale);
        }
        if (self.mcc != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.MCC_WIRE) + gremlin.sizes.sizeU32(self.mcc);
        }
        if (self.mnc != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.MNC_WIRE) + gremlin.sizes.sizeU32(self.mnc);
        }
        if (self.locales) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.LOCALES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.screen_layout != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.SCREEN_LAYOUT_WIRE) + gremlin.sizes.sizeU32(self.screen_layout);
        }
        if (self.color_mode != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.COLOR_MODE_WIRE) + gremlin.sizes.sizeU32(self.color_mode);
        }
        if (self.touchscreen != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.TOUCHSCREEN_WIRE) + gremlin.sizes.sizeU32(self.touchscreen);
        }
        if (self.keyboard != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.KEYBOARD_WIRE) + gremlin.sizes.sizeU32(self.keyboard);
        }
        if (self.keyboard_hidden != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.KEYBOARD_HIDDEN_WIRE) + gremlin.sizes.sizeU32(self.keyboard_hidden);
        }
        if (self.hard_keyboard_hidden != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.HARD_KEYBOARD_HIDDEN_WIRE) + gremlin.sizes.sizeU32(self.hard_keyboard_hidden);
        }
        if (self.navigation != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.NAVIGATION_WIRE) + gremlin.sizes.sizeU32(self.navigation);
        }
        if (self.navigation_hidden != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.NAVIGATION_HIDDEN_WIRE) + gremlin.sizes.sizeU32(self.navigation_hidden);
        }
        if (self.orientation != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.ORIENTATION_WIRE) + gremlin.sizes.sizeU32(self.orientation);
        }
        if (self.ui_mode != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.UI_MODE_WIRE) + gremlin.sizes.sizeU32(self.ui_mode);
        }
        if (self.screen_width_dp != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.SCREEN_WIDTH_DP_WIRE) + gremlin.sizes.sizeU32(self.screen_width_dp);
        }
        if (self.screen_height_dp != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.SCREEN_HEIGHT_DP_WIRE) + gremlin.sizes.sizeU32(self.screen_height_dp);
        }
        if (self.smallest_screen_width_dp != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.SMALLEST_SCREEN_WIDTH_DP_WIRE) + gremlin.sizes.sizeU32(self.smallest_screen_width_dp);
        }
        if (self.density_dpi != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.DENSITY_DPI_WIRE) + gremlin.sizes.sizeU32(self.density_dpi);
        }
        if (self.window_configuration) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.WINDOW_CONFIGURATION_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.locale_list) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.LOCALE_LIST_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.font_weight_adjustment != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.FONT_WEIGHT_ADJUSTMENT_WIRE) + gremlin.sizes.sizeU32(self.font_weight_adjustment);
        }
        if (self.grammatical_gender != 0) {
            res += gremlin.sizes.sizeWireNumber(ConfigurationProtoWire.GRAMMATICAL_GENDER_WIRE) + gremlin.sizes.sizeU32(self.grammatical_gender);
        }
        return res;
    }
    pub fn encode(self: *const ConfigurationProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ConfigurationProto, target: *gremlin.Writer) void {
        if (self.font_scale != 0.0) {
            target.appendFloat32(ConfigurationProtoWire.FONT_SCALE_WIRE, self.font_scale);
        }
        if (self.mcc != 0) {
            target.appendUint32(ConfigurationProtoWire.MCC_WIRE, self.mcc);
        }
        if (self.mnc != 0) {
            target.appendUint32(ConfigurationProtoWire.MNC_WIRE, self.mnc);
        }
        if (self.locales) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ConfigurationProtoWire.LOCALES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ConfigurationProtoWire.LOCALES_WIRE, 0);
                }
            }
        }
        if (self.screen_layout != 0) {
            target.appendUint32(ConfigurationProtoWire.SCREEN_LAYOUT_WIRE, self.screen_layout);
        }
        if (self.color_mode != 0) {
            target.appendUint32(ConfigurationProtoWire.COLOR_MODE_WIRE, self.color_mode);
        }
        if (self.touchscreen != 0) {
            target.appendUint32(ConfigurationProtoWire.TOUCHSCREEN_WIRE, self.touchscreen);
        }
        if (self.keyboard != 0) {
            target.appendUint32(ConfigurationProtoWire.KEYBOARD_WIRE, self.keyboard);
        }
        if (self.keyboard_hidden != 0) {
            target.appendUint32(ConfigurationProtoWire.KEYBOARD_HIDDEN_WIRE, self.keyboard_hidden);
        }
        if (self.hard_keyboard_hidden != 0) {
            target.appendUint32(ConfigurationProtoWire.HARD_KEYBOARD_HIDDEN_WIRE, self.hard_keyboard_hidden);
        }
        if (self.navigation != 0) {
            target.appendUint32(ConfigurationProtoWire.NAVIGATION_WIRE, self.navigation);
        }
        if (self.navigation_hidden != 0) {
            target.appendUint32(ConfigurationProtoWire.NAVIGATION_HIDDEN_WIRE, self.navigation_hidden);
        }
        if (self.orientation != 0) {
            target.appendUint32(ConfigurationProtoWire.ORIENTATION_WIRE, self.orientation);
        }
        if (self.ui_mode != 0) {
            target.appendUint32(ConfigurationProtoWire.UI_MODE_WIRE, self.ui_mode);
        }
        if (self.screen_width_dp != 0) {
            target.appendUint32(ConfigurationProtoWire.SCREEN_WIDTH_DP_WIRE, self.screen_width_dp);
        }
        if (self.screen_height_dp != 0) {
            target.appendUint32(ConfigurationProtoWire.SCREEN_HEIGHT_DP_WIRE, self.screen_height_dp);
        }
        if (self.smallest_screen_width_dp != 0) {
            target.appendUint32(ConfigurationProtoWire.SMALLEST_SCREEN_WIDTH_DP_WIRE, self.smallest_screen_width_dp);
        }
        if (self.density_dpi != 0) {
            target.appendUint32(ConfigurationProtoWire.DENSITY_DPI_WIRE, self.density_dpi);
        }
        if (self.window_configuration) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(ConfigurationProtoWire.WINDOW_CONFIGURATION_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.locale_list) |v| {
            if (v.len > 0) {
                target.appendBytes(ConfigurationProtoWire.LOCALE_LIST_WIRE, v);
            }
        }
        if (self.font_weight_adjustment != 0) {
            target.appendUint32(ConfigurationProtoWire.FONT_WEIGHT_ADJUSTMENT_WIRE, self.font_weight_adjustment);
        }
        if (self.grammatical_gender != 0) {
            target.appendUint32(ConfigurationProtoWire.GRAMMATICAL_GENDER_WIRE, self.grammatical_gender);
        }
    }
};
pub const ConfigurationProtoReader = struct {
    buf: gremlin.Reader,
    _font_scale: f32 = 0.0,
    _mcc: u32 = 0,
    _mnc: u32 = 0,
    _locales_offset: ?usize = null,
    _locales_last_offset: ?usize = null,
    _locales_cnt: usize = 0,
    _screen_layout: u32 = 0,
    _color_mode: u32 = 0,
    _touchscreen: u32 = 0,
    _keyboard: u32 = 0,
    _keyboard_hidden: u32 = 0,
    _hard_keyboard_hidden: u32 = 0,
    _navigation: u32 = 0,
    _navigation_hidden: u32 = 0,
    _orientation: u32 = 0,
    _ui_mode: u32 = 0,
    _screen_width_dp: u32 = 0,
    _screen_height_dp: u32 = 0,
    _smallest_screen_width_dp: u32 = 0,
    _density_dpi: u32 = 0,
    _window_configuration_buf: ?[]const u8 = null,
    _locale_list: ?[]const u8 = null,
    _font_weight_adjustment: u32 = 0,
    _grammatical_gender: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!ConfigurationProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = ConfigurationProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ConfigurationProtoWire.FONT_SCALE_WIRE => {
                    const result = try buf.readFloat32(offset);
                    offset += result.size;
                    res._font_scale = result.value;
                },
                ConfigurationProtoWire.MCC_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._mcc = result.value;
                },
                ConfigurationProtoWire.MNC_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._mnc = result.value;
                },
                ConfigurationProtoWire.LOCALES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._locales_offset == null) {
                        res._locales_offset = offset - result.size;
                    }
                    res._locales_last_offset = offset;
                    res._locales_cnt += 1;
                },
                ConfigurationProtoWire.SCREEN_LAYOUT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._screen_layout = result.value;
                },
                ConfigurationProtoWire.COLOR_MODE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._color_mode = result.value;
                },
                ConfigurationProtoWire.TOUCHSCREEN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._touchscreen = result.value;
                },
                ConfigurationProtoWire.KEYBOARD_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._keyboard = result.value;
                },
                ConfigurationProtoWire.KEYBOARD_HIDDEN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._keyboard_hidden = result.value;
                },
                ConfigurationProtoWire.HARD_KEYBOARD_HIDDEN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._hard_keyboard_hidden = result.value;
                },
                ConfigurationProtoWire.NAVIGATION_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._navigation = result.value;
                },
                ConfigurationProtoWire.NAVIGATION_HIDDEN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._navigation_hidden = result.value;
                },
                ConfigurationProtoWire.ORIENTATION_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._orientation = result.value;
                },
                ConfigurationProtoWire.UI_MODE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._ui_mode = result.value;
                },
                ConfigurationProtoWire.SCREEN_WIDTH_DP_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._screen_width_dp = result.value;
                },
                ConfigurationProtoWire.SCREEN_HEIGHT_DP_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._screen_height_dp = result.value;
                },
                ConfigurationProtoWire.SMALLEST_SCREEN_WIDTH_DP_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._smallest_screen_width_dp = result.value;
                },
                ConfigurationProtoWire.DENSITY_DPI_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._density_dpi = result.value;
                },
                ConfigurationProtoWire.WINDOW_CONFIGURATION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._window_configuration_buf = result.value;
                },
                ConfigurationProtoWire.LOCALE_LIST_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._locale_list = result.value;
                },
                ConfigurationProtoWire.FONT_WEIGHT_ADJUSTMENT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._font_weight_adjustment = result.value;
                },
                ConfigurationProtoWire.GRAMMATICAL_GENDER_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._grammatical_gender = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFontScale(self: *const ConfigurationProtoReader) f32 {
        return self._font_scale;
    }
    pub inline fn getMcc(self: *const ConfigurationProtoReader) u32 {
        return self._mcc;
    }
    pub inline fn getMnc(self: *const ConfigurationProtoReader) u32 {
        return self._mnc;
    }
    pub fn localesCount(self: *const ConfigurationProtoReader) usize {
        return self._locales_cnt;
    }
    pub fn localesNext(self: *ConfigurationProtoReader) ?locale.LocaleProtoReader {
        if (self._locales_offset == null) return null;
        const current_offset = self._locales_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = locale.LocaleProtoReader.init(result.value) catch return null;
        if (self._locales_last_offset != null and current_offset >= self._locales_last_offset.?) {
            self._locales_offset = null;
            return msg;
        }
        if (self._locales_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._locales_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ConfigurationProtoWire.LOCALES_WIRE) {
                self._locales_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._locales_offset = null;
        return msg;
    }
    pub inline fn getScreenLayout(self: *const ConfigurationProtoReader) u32 {
        return self._screen_layout;
    }
    pub inline fn getColorMode(self: *const ConfigurationProtoReader) u32 {
        return self._color_mode;
    }
    pub inline fn getTouchscreen(self: *const ConfigurationProtoReader) u32 {
        return self._touchscreen;
    }
    pub inline fn getKeyboard(self: *const ConfigurationProtoReader) u32 {
        return self._keyboard;
    }
    pub inline fn getKeyboardHidden(self: *const ConfigurationProtoReader) u32 {
        return self._keyboard_hidden;
    }
    pub inline fn getHardKeyboardHidden(self: *const ConfigurationProtoReader) u32 {
        return self._hard_keyboard_hidden;
    }
    pub inline fn getNavigation(self: *const ConfigurationProtoReader) u32 {
        return self._navigation;
    }
    pub inline fn getNavigationHidden(self: *const ConfigurationProtoReader) u32 {
        return self._navigation_hidden;
    }
    pub inline fn getOrientation(self: *const ConfigurationProtoReader) u32 {
        return self._orientation;
    }
    pub inline fn getUiMode(self: *const ConfigurationProtoReader) u32 {
        return self._ui_mode;
    }
    pub inline fn getScreenWidthDp(self: *const ConfigurationProtoReader) u32 {
        return self._screen_width_dp;
    }
    pub inline fn getScreenHeightDp(self: *const ConfigurationProtoReader) u32 {
        return self._screen_height_dp;
    }
    pub inline fn getSmallestScreenWidthDp(self: *const ConfigurationProtoReader) u32 {
        return self._smallest_screen_width_dp;
    }
    pub inline fn getDensityDpi(self: *const ConfigurationProtoReader) u32 {
        return self._density_dpi;
    }
    pub fn getWindowConfiguration(self: *const ConfigurationProtoReader) gremlin.Error!window_configuration.WindowConfigurationProtoReader {
        if (self._window_configuration_buf) |buf| {
            return try window_configuration.WindowConfigurationProtoReader.init(buf);
        }
        return try window_configuration.WindowConfigurationProtoReader.init(&[_]u8{});
    }
    pub inline fn getLocaleList(self: *const ConfigurationProtoReader) []const u8 {
        return self._locale_list orelse &[_]u8{};
    }
    pub inline fn getFontWeightAdjustment(self: *const ConfigurationProtoReader) u32 {
        return self._font_weight_adjustment;
    }
    pub inline fn getGrammaticalGender(self: *const ConfigurationProtoReader) u32 {
        return self._grammatical_gender;
    }
};
const ResourcesConfigurationProtoWire = struct {
    const CONFIGURATION_WIRE: gremlin.ProtoWireNumber = 1;
    const SDK_VERSION_WIRE: gremlin.ProtoWireNumber = 2;
    const SCREEN_WIDTH_PX_WIRE: gremlin.ProtoWireNumber = 3;
    const SCREEN_HEIGHT_PX_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const ResourcesConfigurationProto = struct {
    // fields
    configuration: ?ConfigurationProto = null,
    sdk_version: u32 = 0,
    screen_width_px: u32 = 0,
    screen_height_px: u32 = 0,
    pub fn calcProtobufSize(self: *const ResourcesConfigurationProto) usize {
        var res: usize = 0;
        if (self.configuration) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(ResourcesConfigurationProtoWire.CONFIGURATION_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.sdk_version != 0) {
            res += gremlin.sizes.sizeWireNumber(ResourcesConfigurationProtoWire.SDK_VERSION_WIRE) + gremlin.sizes.sizeU32(self.sdk_version);
        }
        if (self.screen_width_px != 0) {
            res += gremlin.sizes.sizeWireNumber(ResourcesConfigurationProtoWire.SCREEN_WIDTH_PX_WIRE) + gremlin.sizes.sizeU32(self.screen_width_px);
        }
        if (self.screen_height_px != 0) {
            res += gremlin.sizes.sizeWireNumber(ResourcesConfigurationProtoWire.SCREEN_HEIGHT_PX_WIRE) + gremlin.sizes.sizeU32(self.screen_height_px);
        }
        return res;
    }
    pub fn encode(self: *const ResourcesConfigurationProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ResourcesConfigurationProto, target: *gremlin.Writer) void {
        if (self.configuration) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(ResourcesConfigurationProtoWire.CONFIGURATION_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.sdk_version != 0) {
            target.appendUint32(ResourcesConfigurationProtoWire.SDK_VERSION_WIRE, self.sdk_version);
        }
        if (self.screen_width_px != 0) {
            target.appendUint32(ResourcesConfigurationProtoWire.SCREEN_WIDTH_PX_WIRE, self.screen_width_px);
        }
        if (self.screen_height_px != 0) {
            target.appendUint32(ResourcesConfigurationProtoWire.SCREEN_HEIGHT_PX_WIRE, self.screen_height_px);
        }
    }
};
pub const ResourcesConfigurationProtoReader = struct {
    buf: gremlin.Reader,
    _configuration_buf: ?[]const u8 = null,
    _sdk_version: u32 = 0,
    _screen_width_px: u32 = 0,
    _screen_height_px: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!ResourcesConfigurationProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = ResourcesConfigurationProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ResourcesConfigurationProtoWire.CONFIGURATION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._configuration_buf = result.value;
                },
                ResourcesConfigurationProtoWire.SDK_VERSION_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._sdk_version = result.value;
                },
                ResourcesConfigurationProtoWire.SCREEN_WIDTH_PX_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._screen_width_px = result.value;
                },
                ResourcesConfigurationProtoWire.SCREEN_HEIGHT_PX_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._screen_height_px = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getConfiguration(self: *const ResourcesConfigurationProtoReader) gremlin.Error!ConfigurationProtoReader {
        if (self._configuration_buf) |buf| {
            return try ConfigurationProtoReader.init(buf);
        }
        return try ConfigurationProtoReader.init(&[_]u8{});
    }
    pub inline fn getSdkVersion(self: *const ResourcesConfigurationProtoReader) u32 {
        return self._sdk_version;
    }
    pub inline fn getScreenWidthPx(self: *const ResourcesConfigurationProtoReader) u32 {
        return self._screen_width_px;
    }
    pub inline fn getScreenHeightPx(self: *const ResourcesConfigurationProtoReader) u32 {
        return self._screen_height_px;
    }
};
const DeviceConfigurationProtoWire = struct {
    const STABLE_SCREEN_WIDTH_PX_WIRE: gremlin.ProtoWireNumber = 1;
    const STABLE_SCREEN_HEIGHT_PX_WIRE: gremlin.ProtoWireNumber = 2;
    const STABLE_DENSITY_DPI_WIRE: gremlin.ProtoWireNumber = 3;
    const TOTAL_RAM_WIRE: gremlin.ProtoWireNumber = 4;
    const LOW_RAM_WIRE: gremlin.ProtoWireNumber = 5;
    const MAX_CORES_WIRE: gremlin.ProtoWireNumber = 6;
    const HAS_SECURE_SCREEN_LOCK_WIRE: gremlin.ProtoWireNumber = 7;
    const OPENGL_VERSION_WIRE: gremlin.ProtoWireNumber = 8;
    const OPENGL_EXTENSIONS_WIRE: gremlin.ProtoWireNumber = 9;
    const SHARED_LIBRARIES_WIRE: gremlin.ProtoWireNumber = 10;
    const FEATURES_WIRE: gremlin.ProtoWireNumber = 11;
    const CPU_ARCHITECTURES_WIRE: gremlin.ProtoWireNumber = 12;
};
pub const DeviceConfigurationProto = struct {
    // fields
    stable_screen_width_px: u32 = 0,
    stable_screen_height_px: u32 = 0,
    stable_density_dpi: u32 = 0,
    total_ram: u64 = 0,
    low_ram: bool = false,
    max_cores: u32 = 0,
    has_secure_screen_lock: bool = false,
    opengl_version: u32 = 0,
    opengl_extensions: ?[]const ?[]const u8 = null,
    shared_libraries: ?[]const ?[]const u8 = null,
    features: ?[]const ?[]const u8 = null,
    cpu_architectures: ?[]const ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const DeviceConfigurationProto) usize {
        var res: usize = 0;
        if (self.stable_screen_width_px != 0) {
            res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.STABLE_SCREEN_WIDTH_PX_WIRE) + gremlin.sizes.sizeU32(self.stable_screen_width_px);
        }
        if (self.stable_screen_height_px != 0) {
            res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.STABLE_SCREEN_HEIGHT_PX_WIRE) + gremlin.sizes.sizeU32(self.stable_screen_height_px);
        }
        if (self.stable_density_dpi != 0) {
            res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.STABLE_DENSITY_DPI_WIRE) + gremlin.sizes.sizeU32(self.stable_density_dpi);
        }
        if (self.total_ram != 0) {
            res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.TOTAL_RAM_WIRE) + gremlin.sizes.sizeU64(self.total_ram);
        }
        if (self.low_ram != false) {
            res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.LOW_RAM_WIRE) + gremlin.sizes.sizeBool(self.low_ram);
        }
        if (self.max_cores != 0) {
            res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.MAX_CORES_WIRE) + gremlin.sizes.sizeU32(self.max_cores);
        }
        if (self.has_secure_screen_lock != false) {
            res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.HAS_SECURE_SCREEN_LOCK_WIRE) + gremlin.sizes.sizeBool(self.has_secure_screen_lock);
        }
        if (self.opengl_version != 0) {
            res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.OPENGL_VERSION_WIRE) + gremlin.sizes.sizeU32(self.opengl_version);
        }
        if (self.opengl_extensions) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.OPENGL_EXTENSIONS_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.shared_libraries) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.SHARED_LIBRARIES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.features) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.FEATURES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.cpu_architectures) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(DeviceConfigurationProtoWire.CPU_ARCHITECTURES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const DeviceConfigurationProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DeviceConfigurationProto, target: *gremlin.Writer) void {
        if (self.stable_screen_width_px != 0) {
            target.appendUint32(DeviceConfigurationProtoWire.STABLE_SCREEN_WIDTH_PX_WIRE, self.stable_screen_width_px);
        }
        if (self.stable_screen_height_px != 0) {
            target.appendUint32(DeviceConfigurationProtoWire.STABLE_SCREEN_HEIGHT_PX_WIRE, self.stable_screen_height_px);
        }
        if (self.stable_density_dpi != 0) {
            target.appendUint32(DeviceConfigurationProtoWire.STABLE_DENSITY_DPI_WIRE, self.stable_density_dpi);
        }
        if (self.total_ram != 0) {
            target.appendUint64(DeviceConfigurationProtoWire.TOTAL_RAM_WIRE, self.total_ram);
        }
        if (self.low_ram != false) {
            target.appendBool(DeviceConfigurationProtoWire.LOW_RAM_WIRE, self.low_ram);
        }
        if (self.max_cores != 0) {
            target.appendUint32(DeviceConfigurationProtoWire.MAX_CORES_WIRE, self.max_cores);
        }
        if (self.has_secure_screen_lock != false) {
            target.appendBool(DeviceConfigurationProtoWire.HAS_SECURE_SCREEN_LOCK_WIRE, self.has_secure_screen_lock);
        }
        if (self.opengl_version != 0) {
            target.appendUint32(DeviceConfigurationProtoWire.OPENGL_VERSION_WIRE, self.opengl_version);
        }
        if (self.opengl_extensions) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(DeviceConfigurationProtoWire.OPENGL_EXTENSIONS_WIRE, v);
                } else {
                    target.appendBytesTag(DeviceConfigurationProtoWire.OPENGL_EXTENSIONS_WIRE, 0);
                }
            }
        }
        if (self.shared_libraries) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(DeviceConfigurationProtoWire.SHARED_LIBRARIES_WIRE, v);
                } else {
                    target.appendBytesTag(DeviceConfigurationProtoWire.SHARED_LIBRARIES_WIRE, 0);
                }
            }
        }
        if (self.features) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(DeviceConfigurationProtoWire.FEATURES_WIRE, v);
                } else {
                    target.appendBytesTag(DeviceConfigurationProtoWire.FEATURES_WIRE, 0);
                }
            }
        }
        if (self.cpu_architectures) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(DeviceConfigurationProtoWire.CPU_ARCHITECTURES_WIRE, v);
                } else {
                    target.appendBytesTag(DeviceConfigurationProtoWire.CPU_ARCHITECTURES_WIRE, 0);
                }
            }
        }
    }
};
pub const DeviceConfigurationProtoReader = struct {
    buf: gremlin.Reader,
    _stable_screen_width_px: u32 = 0,
    _stable_screen_height_px: u32 = 0,
    _stable_density_dpi: u32 = 0,
    _total_ram: u64 = 0,
    _low_ram: bool = false,
    _max_cores: u32 = 0,
    _has_secure_screen_lock: bool = false,
    _opengl_version: u32 = 0,
    _opengl_extensions_offset: ?usize = null,
    _opengl_extensions_last_offset: ?usize = null,
    _opengl_extensions_cnt: usize = 0,
    _shared_libraries_offset: ?usize = null,
    _shared_libraries_last_offset: ?usize = null,
    _shared_libraries_cnt: usize = 0,
    _features_offset: ?usize = null,
    _features_last_offset: ?usize = null,
    _features_cnt: usize = 0,
    _cpu_architectures_offset: ?usize = null,
    _cpu_architectures_last_offset: ?usize = null,
    _cpu_architectures_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!DeviceConfigurationProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = DeviceConfigurationProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DeviceConfigurationProtoWire.STABLE_SCREEN_WIDTH_PX_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._stable_screen_width_px = result.value;
                },
                DeviceConfigurationProtoWire.STABLE_SCREEN_HEIGHT_PX_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._stable_screen_height_px = result.value;
                },
                DeviceConfigurationProtoWire.STABLE_DENSITY_DPI_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._stable_density_dpi = result.value;
                },
                DeviceConfigurationProtoWire.TOTAL_RAM_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._total_ram = result.value;
                },
                DeviceConfigurationProtoWire.LOW_RAM_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._low_ram = result.value;
                },
                DeviceConfigurationProtoWire.MAX_CORES_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._max_cores = result.value;
                },
                DeviceConfigurationProtoWire.HAS_SECURE_SCREEN_LOCK_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._has_secure_screen_lock = result.value;
                },
                DeviceConfigurationProtoWire.OPENGL_VERSION_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._opengl_version = result.value;
                },
                DeviceConfigurationProtoWire.OPENGL_EXTENSIONS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._opengl_extensions_offset == null) {
                        res._opengl_extensions_offset = offset - result.size;
                    }
                    res._opengl_extensions_last_offset = offset;
                    res._opengl_extensions_cnt += 1;
                },
                DeviceConfigurationProtoWire.SHARED_LIBRARIES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._shared_libraries_offset == null) {
                        res._shared_libraries_offset = offset - result.size;
                    }
                    res._shared_libraries_last_offset = offset;
                    res._shared_libraries_cnt += 1;
                },
                DeviceConfigurationProtoWire.FEATURES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._features_offset == null) {
                        res._features_offset = offset - result.size;
                    }
                    res._features_last_offset = offset;
                    res._features_cnt += 1;
                },
                DeviceConfigurationProtoWire.CPU_ARCHITECTURES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._cpu_architectures_offset == null) {
                        res._cpu_architectures_offset = offset - result.size;
                    }
                    res._cpu_architectures_last_offset = offset;
                    res._cpu_architectures_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getStableScreenWidthPx(self: *const DeviceConfigurationProtoReader) u32 {
        return self._stable_screen_width_px;
    }
    pub inline fn getStableScreenHeightPx(self: *const DeviceConfigurationProtoReader) u32 {
        return self._stable_screen_height_px;
    }
    pub inline fn getStableDensityDpi(self: *const DeviceConfigurationProtoReader) u32 {
        return self._stable_density_dpi;
    }
    pub inline fn getTotalRam(self: *const DeviceConfigurationProtoReader) u64 {
        return self._total_ram;
    }
    pub inline fn getLowRam(self: *const DeviceConfigurationProtoReader) bool {
        return self._low_ram;
    }
    pub inline fn getMaxCores(self: *const DeviceConfigurationProtoReader) u32 {
        return self._max_cores;
    }
    pub inline fn getHasSecureScreenLock(self: *const DeviceConfigurationProtoReader) bool {
        return self._has_secure_screen_lock;
    }
    pub inline fn getOpenglVersion(self: *const DeviceConfigurationProtoReader) u32 {
        return self._opengl_version;
    }
    pub fn openglExtensionsCount(self: *const DeviceConfigurationProtoReader) usize {
        return self._opengl_extensions_cnt;
    }
    pub fn openglExtensionsNext(self: *DeviceConfigurationProtoReader) ?[]const u8 {
        if (self._opengl_extensions_offset == null) return null;
        const current_offset = self._opengl_extensions_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._opengl_extensions_last_offset != null and current_offset >= self._opengl_extensions_last_offset.?) {
            self._opengl_extensions_offset = null;
            return result.value;
        }
        if (self._opengl_extensions_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._opengl_extensions_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == DeviceConfigurationProtoWire.OPENGL_EXTENSIONS_WIRE) {
                self._opengl_extensions_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._opengl_extensions_offset = null;
        return result.value;
    }
    pub fn sharedLibrariesCount(self: *const DeviceConfigurationProtoReader) usize {
        return self._shared_libraries_cnt;
    }
    pub fn sharedLibrariesNext(self: *DeviceConfigurationProtoReader) ?[]const u8 {
        if (self._shared_libraries_offset == null) return null;
        const current_offset = self._shared_libraries_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._shared_libraries_last_offset != null and current_offset >= self._shared_libraries_last_offset.?) {
            self._shared_libraries_offset = null;
            return result.value;
        }
        if (self._shared_libraries_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._shared_libraries_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == DeviceConfigurationProtoWire.SHARED_LIBRARIES_WIRE) {
                self._shared_libraries_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._shared_libraries_offset = null;
        return result.value;
    }
    pub fn featuresCount(self: *const DeviceConfigurationProtoReader) usize {
        return self._features_cnt;
    }
    pub fn featuresNext(self: *DeviceConfigurationProtoReader) ?[]const u8 {
        if (self._features_offset == null) return null;
        const current_offset = self._features_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._features_last_offset != null and current_offset >= self._features_last_offset.?) {
            self._features_offset = null;
            return result.value;
        }
        if (self._features_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._features_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == DeviceConfigurationProtoWire.FEATURES_WIRE) {
                self._features_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._features_offset = null;
        return result.value;
    }
    pub fn cpuArchitecturesCount(self: *const DeviceConfigurationProtoReader) usize {
        return self._cpu_architectures_cnt;
    }
    pub fn cpuArchitecturesNext(self: *DeviceConfigurationProtoReader) ?[]const u8 {
        if (self._cpu_architectures_offset == null) return null;
        const current_offset = self._cpu_architectures_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._cpu_architectures_last_offset != null and current_offset >= self._cpu_architectures_last_offset.?) {
            self._cpu_architectures_offset = null;
            return result.value;
        }
        if (self._cpu_architectures_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._cpu_architectures_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == DeviceConfigurationProtoWire.CPU_ARCHITECTURES_WIRE) {
                self._cpu_architectures_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._cpu_architectures_offset = null;
        return result.value;
    }
};
const GlobalConfigurationProtoWire = struct {
    const RESOURCES_WIRE: gremlin.ProtoWireNumber = 1;
    const DEVICE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const GlobalConfigurationProto = struct {
    // fields
    resources: ?ResourcesConfigurationProto = null,
    device: ?DeviceConfigurationProto = null,
    pub fn calcProtobufSize(self: *const GlobalConfigurationProto) usize {
        var res: usize = 0;
        if (self.resources) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GlobalConfigurationProtoWire.RESOURCES_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.device) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GlobalConfigurationProtoWire.DEVICE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const GlobalConfigurationProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GlobalConfigurationProto, target: *gremlin.Writer) void {
        if (self.resources) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GlobalConfigurationProtoWire.RESOURCES_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.device) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GlobalConfigurationProtoWire.DEVICE_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const GlobalConfigurationProtoReader = struct {
    buf: gremlin.Reader,
    _resources_buf: ?[]const u8 = null,
    _device_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!GlobalConfigurationProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = GlobalConfigurationProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GlobalConfigurationProtoWire.RESOURCES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._resources_buf = result.value;
                },
                GlobalConfigurationProtoWire.DEVICE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._device_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getResources(self: *const GlobalConfigurationProtoReader) gremlin.Error!ResourcesConfigurationProtoReader {
        if (self._resources_buf) |buf| {
            return try ResourcesConfigurationProtoReader.init(buf);
        }
        return try ResourcesConfigurationProtoReader.init(&[_]u8{});
    }
    pub fn getDevice(self: *const GlobalConfigurationProtoReader) gremlin.Error!DeviceConfigurationProtoReader {
        if (self._device_buf) |buf| {
            return try DeviceConfigurationProtoReader.init(buf);
        }
        return try DeviceConfigurationProtoReader.init(&[_]u8{});
    }
};
