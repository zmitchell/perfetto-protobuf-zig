// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const LocaleProtoWire = struct {
    const LANGUAGE_WIRE: gremlin.ProtoWireNumber = 1;
    const COUNTRY_WIRE: gremlin.ProtoWireNumber = 2;
    const VARIANT_WIRE: gremlin.ProtoWireNumber = 3;
    const SCRIPT_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const LocaleProto = struct {
    // fields
    language: ?[]const u8 = null,
    country: ?[]const u8 = null,
    variant: ?[]const u8 = null,
    script: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const LocaleProto) usize {
        var res: usize = 0;
        if (self.language) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(LocaleProtoWire.LANGUAGE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.country) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(LocaleProtoWire.COUNTRY_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.variant) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(LocaleProtoWire.VARIANT_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.script) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(LocaleProtoWire.SCRIPT_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const LocaleProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const LocaleProto, target: *gremlin.Writer) void {
        if (self.language) |v| {
            if (v.len > 0) {
                target.appendBytes(LocaleProtoWire.LANGUAGE_WIRE, v);
            }
        }
        if (self.country) |v| {
            if (v.len > 0) {
                target.appendBytes(LocaleProtoWire.COUNTRY_WIRE, v);
            }
        }
        if (self.variant) |v| {
            if (v.len > 0) {
                target.appendBytes(LocaleProtoWire.VARIANT_WIRE, v);
            }
        }
        if (self.script) |v| {
            if (v.len > 0) {
                target.appendBytes(LocaleProtoWire.SCRIPT_WIRE, v);
            }
        }
    }
};
pub const LocaleProtoReader = struct {
    buf: gremlin.Reader,
    _language: ?[]const u8 = null,
    _country: ?[]const u8 = null,
    _variant: ?[]const u8 = null,
    _script: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!LocaleProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = LocaleProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                LocaleProtoWire.LANGUAGE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._language = result.value;
                },
                LocaleProtoWire.COUNTRY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._country = result.value;
                },
                LocaleProtoWire.VARIANT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._variant = result.value;
                },
                LocaleProtoWire.SCRIPT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._script = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getLanguage(self: *const LocaleProtoReader) []const u8 {
        return self._language orelse &[_]u8{};
    }
    pub inline fn getCountry(self: *const LocaleProtoReader) []const u8 {
        return self._country orelse &[_]u8{};
    }
    pub inline fn getVariant(self: *const LocaleProtoReader) []const u8 {
        return self._variant orelse &[_]u8{};
    }
    pub inline fn getScript(self: *const LocaleProtoReader) []const u8 {
        return self._script orelse &[_]u8{};
    }
};
