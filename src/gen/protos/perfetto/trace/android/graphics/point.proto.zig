// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PointProtoWire = struct {
    const X_WIRE: gremlin.ProtoWireNumber = 1;
    const Y_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const PointProto = struct {
    // fields
    x: i32 = 0,
    y: i32 = 0,
    pub fn calcProtobufSize(self: *const PointProto) usize {
        var res: usize = 0;
        if (self.x != 0) {
            res += gremlin.sizes.sizeWireNumber(PointProtoWire.X_WIRE) + gremlin.sizes.sizeI32(self.x);
        }
        if (self.y != 0) {
            res += gremlin.sizes.sizeWireNumber(PointProtoWire.Y_WIRE) + gremlin.sizes.sizeI32(self.y);
        }
        return res;
    }
    pub fn encode(self: *const PointProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PointProto, target: *gremlin.Writer) void {
        if (self.x != 0) {
            target.appendInt32(PointProtoWire.X_WIRE, self.x);
        }
        if (self.y != 0) {
            target.appendInt32(PointProtoWire.Y_WIRE, self.y);
        }
    }
};
pub const PointProtoReader = struct {
    buf: gremlin.Reader,
    _x: i32 = 0,
    _y: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!PointProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = PointProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PointProtoWire.X_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._x = result.value;
                },
                PointProtoWire.Y_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._y = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getX(self: *const PointProtoReader) i32 {
        return self._x;
    }
    pub inline fn getY(self: *const PointProtoReader) i32 {
        return self._y;
    }
};
