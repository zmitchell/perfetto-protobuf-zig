// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const InsetsProtoWire = struct {
    const LEFT_WIRE: gremlin.ProtoWireNumber = 1;
    const TOP_WIRE: gremlin.ProtoWireNumber = 2;
    const RIGHT_WIRE: gremlin.ProtoWireNumber = 3;
    const BOTTOM_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const InsetsProto = struct {
    // fields
    left: i32 = 0,
    top: i32 = 0,
    right: i32 = 0,
    bottom: i32 = 0,
    pub fn calcProtobufSize(self: *const InsetsProto) usize {
        var res: usize = 0;
        if (self.left != 0) {
            res += gremlin.sizes.sizeWireNumber(InsetsProtoWire.LEFT_WIRE) + gremlin.sizes.sizeI32(self.left);
        }
        if (self.top != 0) {
            res += gremlin.sizes.sizeWireNumber(InsetsProtoWire.TOP_WIRE) + gremlin.sizes.sizeI32(self.top);
        }
        if (self.right != 0) {
            res += gremlin.sizes.sizeWireNumber(InsetsProtoWire.RIGHT_WIRE) + gremlin.sizes.sizeI32(self.right);
        }
        if (self.bottom != 0) {
            res += gremlin.sizes.sizeWireNumber(InsetsProtoWire.BOTTOM_WIRE) + gremlin.sizes.sizeI32(self.bottom);
        }
        return res;
    }
    pub fn encode(self: *const InsetsProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InsetsProto, target: *gremlin.Writer) void {
        if (self.left != 0) {
            target.appendInt32(InsetsProtoWire.LEFT_WIRE, self.left);
        }
        if (self.top != 0) {
            target.appendInt32(InsetsProtoWire.TOP_WIRE, self.top);
        }
        if (self.right != 0) {
            target.appendInt32(InsetsProtoWire.RIGHT_WIRE, self.right);
        }
        if (self.bottom != 0) {
            target.appendInt32(InsetsProtoWire.BOTTOM_WIRE, self.bottom);
        }
    }
};
pub const InsetsProtoReader = struct {
    buf: gremlin.Reader,
    _left: i32 = 0,
    _top: i32 = 0,
    _right: i32 = 0,
    _bottom: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!InsetsProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InsetsProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InsetsProtoWire.LEFT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._left = result.value;
                },
                InsetsProtoWire.TOP_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._top = result.value;
                },
                InsetsProtoWire.RIGHT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._right = result.value;
                },
                InsetsProtoWire.BOTTOM_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._bottom = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getLeft(self: *const InsetsProtoReader) i32 {
        return self._left;
    }
    pub inline fn getTop(self: *const InsetsProtoReader) i32 {
        return self._top;
    }
    pub inline fn getRight(self: *const InsetsProtoReader) i32 {
        return self._right;
    }
    pub inline fn getBottom(self: *const InsetsProtoReader) i32 {
        return self._bottom;
    }
};
