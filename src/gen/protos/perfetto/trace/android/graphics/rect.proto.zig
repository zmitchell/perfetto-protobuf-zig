// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const RectProtoWire = struct {
    const LEFT_WIRE: gremlin.ProtoWireNumber = 1;
    const TOP_WIRE: gremlin.ProtoWireNumber = 2;
    const RIGHT_WIRE: gremlin.ProtoWireNumber = 3;
    const BOTTOM_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const RectProto = struct {
    // fields
    left: i32 = 0,
    top: i32 = 0,
    right: i32 = 0,
    bottom: i32 = 0,
    pub fn calcProtobufSize(self: *const RectProto) usize {
        var res: usize = 0;
        if (self.left != 0) {
            res += gremlin.sizes.sizeWireNumber(RectProtoWire.LEFT_WIRE) + gremlin.sizes.sizeI32(self.left);
        }
        if (self.top != 0) {
            res += gremlin.sizes.sizeWireNumber(RectProtoWire.TOP_WIRE) + gremlin.sizes.sizeI32(self.top);
        }
        if (self.right != 0) {
            res += gremlin.sizes.sizeWireNumber(RectProtoWire.RIGHT_WIRE) + gremlin.sizes.sizeI32(self.right);
        }
        if (self.bottom != 0) {
            res += gremlin.sizes.sizeWireNumber(RectProtoWire.BOTTOM_WIRE) + gremlin.sizes.sizeI32(self.bottom);
        }
        return res;
    }
    pub fn encode(self: *const RectProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RectProto, target: *gremlin.Writer) void {
        if (self.left != 0) {
            target.appendInt32(RectProtoWire.LEFT_WIRE, self.left);
        }
        if (self.top != 0) {
            target.appendInt32(RectProtoWire.TOP_WIRE, self.top);
        }
        if (self.right != 0) {
            target.appendInt32(RectProtoWire.RIGHT_WIRE, self.right);
        }
        if (self.bottom != 0) {
            target.appendInt32(RectProtoWire.BOTTOM_WIRE, self.bottom);
        }
    }
};
pub const RectProtoReader = struct {
    buf: gremlin.Reader,
    _left: i32 = 0,
    _top: i32 = 0,
    _right: i32 = 0,
    _bottom: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!RectProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = RectProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RectProtoWire.LEFT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._left = result.value;
                },
                RectProtoWire.TOP_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._top = result.value;
                },
                RectProtoWire.RIGHT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._right = result.value;
                },
                RectProtoWire.BOTTOM_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._bottom = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getLeft(self: *const RectProtoReader) i32 {
        return self._left;
    }
    pub inline fn getTop(self: *const RectProtoReader) i32 {
        return self._top;
    }
    pub inline fn getRight(self: *const RectProtoReader) i32 {
        return self._right;
    }
    pub inline fn getBottom(self: *const RectProtoReader) i32 {
        return self._bottom;
    }
};
