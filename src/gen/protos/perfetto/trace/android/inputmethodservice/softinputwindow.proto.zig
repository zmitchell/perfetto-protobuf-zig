// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const SoftInputWindowProtoWire = struct {
    const WINDOW_STATE_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const SoftInputWindowProto = struct {
    // fields
    window_state: i32 = 0,
    pub fn calcProtobufSize(self: *const SoftInputWindowProto) usize {
        var res: usize = 0;
        if (self.window_state != 0) {
            res += gremlin.sizes.sizeWireNumber(SoftInputWindowProtoWire.WINDOW_STATE_WIRE) + gremlin.sizes.sizeI32(self.window_state);
        }
        return res;
    }
    pub fn encode(self: *const SoftInputWindowProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SoftInputWindowProto, target: *gremlin.Writer) void {
        if (self.window_state != 0) {
            target.appendInt32(SoftInputWindowProtoWire.WINDOW_STATE_WIRE, self.window_state);
        }
    }
};
pub const SoftInputWindowProtoReader = struct {
    buf: gremlin.Reader,
    _window_state: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!SoftInputWindowProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = SoftInputWindowProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SoftInputWindowProtoWire.WINDOW_STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._window_state = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getWindowState(self: *const SoftInputWindowProtoReader) i32 {
        return self._window_state;
    }
};
