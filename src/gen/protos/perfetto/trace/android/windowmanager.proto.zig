// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const windowmanagerservice = @import("src/gen/protos/perfetto/trace/android/server/windowmanagerservice.proto.zig");
// structs
const WindowManagerTraceFileProtoWire = struct {
    const MAGIC_NUMBER_WIRE: gremlin.ProtoWireNumber = 1;
    const ENTRY_WIRE: gremlin.ProtoWireNumber = 2;
    const REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const WindowManagerTraceFileProto = struct {
    // nested enums
    pub const MagicNumber = enum(i32) {
        INVALID = 0,
        MAGIC_NUMBER_L = 1414416727,
        MAGIC_NUMBER_H = 1162035538,
    };
    // fields
    magic_number: u64 = 0,
    entry: ?[]const ?WindowManagerTraceEntry = null,
    real_to_elapsed_time_offset_nanos: u64 = 0,
    pub fn calcProtobufSize(self: *const WindowManagerTraceFileProto) usize {
        var res: usize = 0;
        if (self.magic_number != 0) {
            res += gremlin.sizes.sizeWireNumber(WindowManagerTraceFileProtoWire.MAGIC_NUMBER_WIRE) + gremlin.sizes.sizeFixed64(self.magic_number);
        }
        if (self.entry) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(WindowManagerTraceFileProtoWire.ENTRY_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.real_to_elapsed_time_offset_nanos != 0) {
            res += gremlin.sizes.sizeWireNumber(WindowManagerTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE) + gremlin.sizes.sizeFixed64(self.real_to_elapsed_time_offset_nanos);
        }
        return res;
    }
    pub fn encode(self: *const WindowManagerTraceFileProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const WindowManagerTraceFileProto, target: *gremlin.Writer) void {
        if (self.magic_number != 0) {
            target.appendFixed64(WindowManagerTraceFileProtoWire.MAGIC_NUMBER_WIRE, self.magic_number);
        }
        if (self.entry) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(WindowManagerTraceFileProtoWire.ENTRY_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(WindowManagerTraceFileProtoWire.ENTRY_WIRE, 0);
                }
            }
        }
        if (self.real_to_elapsed_time_offset_nanos != 0) {
            target.appendFixed64(WindowManagerTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE, self.real_to_elapsed_time_offset_nanos);
        }
    }
};
pub const WindowManagerTraceFileProtoReader = struct {
    buf: gremlin.Reader,
    _magic_number: u64 = 0,
    _entry_offset: ?usize = null,
    _entry_last_offset: ?usize = null,
    _entry_cnt: usize = 0,
    _real_to_elapsed_time_offset_nanos: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!WindowManagerTraceFileProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = WindowManagerTraceFileProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                WindowManagerTraceFileProtoWire.MAGIC_NUMBER_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._magic_number = result.value;
                },
                WindowManagerTraceFileProtoWire.ENTRY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._entry_offset == null) {
                        res._entry_offset = offset - result.size;
                    }
                    res._entry_last_offset = offset;
                    res._entry_cnt += 1;
                },
                WindowManagerTraceFileProtoWire.REAL_TO_ELAPSED_TIME_OFFSET_NANOS_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._real_to_elapsed_time_offset_nanos = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getMagicNumber(self: *const WindowManagerTraceFileProtoReader) u64 {
        return self._magic_number;
    }
    pub fn entryCount(self: *const WindowManagerTraceFileProtoReader) usize {
        return self._entry_cnt;
    }
    pub fn entryNext(self: *WindowManagerTraceFileProtoReader) ?WindowManagerTraceEntryReader {
        if (self._entry_offset == null) return null;
        const current_offset = self._entry_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = WindowManagerTraceEntryReader.init(result.value) catch return null;
        if (self._entry_last_offset != null and current_offset >= self._entry_last_offset.?) {
            self._entry_offset = null;
            return msg;
        }
        if (self._entry_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._entry_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == WindowManagerTraceFileProtoWire.ENTRY_WIRE) {
                self._entry_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._entry_offset = null;
        return msg;
    }
    pub inline fn getRealToElapsedTimeOffsetNanos(self: *const WindowManagerTraceFileProtoReader) u64 {
        return self._real_to_elapsed_time_offset_nanos;
    }
};
const WindowManagerTraceEntryWire = struct {
    const ELAPSED_REALTIME_NANOS_WIRE: gremlin.ProtoWireNumber = 1;
    const WHERE_WIRE: gremlin.ProtoWireNumber = 2;
    const WINDOW_MANAGER_SERVICE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const WindowManagerTraceEntry = struct {
    // fields
    elapsed_realtime_nanos: u64 = 0,
    where: ?[]const u8 = null,
    window_manager_service: ?windowmanagerservice.WindowManagerServiceDumpProto = null,
    pub fn calcProtobufSize(self: *const WindowManagerTraceEntry) usize {
        var res: usize = 0;
        if (self.elapsed_realtime_nanos != 0) {
            res += gremlin.sizes.sizeWireNumber(WindowManagerTraceEntryWire.ELAPSED_REALTIME_NANOS_WIRE) + gremlin.sizes.sizeFixed64(self.elapsed_realtime_nanos);
        }
        if (self.where) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(WindowManagerTraceEntryWire.WHERE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.window_manager_service) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(WindowManagerTraceEntryWire.WINDOW_MANAGER_SERVICE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const WindowManagerTraceEntry, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const WindowManagerTraceEntry, target: *gremlin.Writer) void {
        if (self.elapsed_realtime_nanos != 0) {
            target.appendFixed64(WindowManagerTraceEntryWire.ELAPSED_REALTIME_NANOS_WIRE, self.elapsed_realtime_nanos);
        }
        if (self.where) |v| {
            if (v.len > 0) {
                target.appendBytes(WindowManagerTraceEntryWire.WHERE_WIRE, v);
            }
        }
        if (self.window_manager_service) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(WindowManagerTraceEntryWire.WINDOW_MANAGER_SERVICE_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const WindowManagerTraceEntryReader = struct {
    buf: gremlin.Reader,
    _elapsed_realtime_nanos: u64 = 0,
    _where: ?[]const u8 = null,
    _window_manager_service_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!WindowManagerTraceEntryReader {
        const buf = gremlin.Reader.init(src);
        var res = WindowManagerTraceEntryReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                WindowManagerTraceEntryWire.ELAPSED_REALTIME_NANOS_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._elapsed_realtime_nanos = result.value;
                },
                WindowManagerTraceEntryWire.WHERE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._where = result.value;
                },
                WindowManagerTraceEntryWire.WINDOW_MANAGER_SERVICE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._window_manager_service_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getElapsedRealtimeNanos(self: *const WindowManagerTraceEntryReader) u64 {
        return self._elapsed_realtime_nanos;
    }
    pub inline fn getWhere(self: *const WindowManagerTraceEntryReader) []const u8 {
        return self._where orelse &[_]u8{};
    }
    pub fn getWindowManagerService(self: *const WindowManagerTraceEntryReader) gremlin.Error!windowmanagerservice.WindowManagerServiceDumpProtoReader {
        if (self._window_manager_service_buf) |buf| {
            return try windowmanagerservice.WindowManagerServiceDumpProtoReader.init(buf);
        }
        return try windowmanagerservice.WindowManagerServiceDumpProtoReader.init(&[_]u8{});
    }
};
