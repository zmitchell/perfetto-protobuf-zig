// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const rect = @import("src/gen/protos/perfetto/trace/android/graphics/rect.proto.zig");
const insets = @import("src/gen/protos/perfetto/trace/android/graphics/insets.proto.zig");
// structs
const InsetsSourceProtoWire = struct {
    const TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const FRAME_WIRE: gremlin.ProtoWireNumber = 2;
    const VISIBLE_FRAME_WIRE: gremlin.ProtoWireNumber = 3;
    const VISIBLE_WIRE: gremlin.ProtoWireNumber = 4;
    const TYPE_NUMBER_WIRE: gremlin.ProtoWireNumber = 5;
    const ATTACHED_INSETS_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const InsetsSourceProto = struct {
    // fields
    type: ?[]const u8 = null,
    frame: ?rect.RectProto = null,
    visible_frame: ?rect.RectProto = null,
    visible: bool = false,
    type_number: i32 = 0,
    attached_insets: ?insets.InsetsProto = null,
    pub fn calcProtobufSize(self: *const InsetsSourceProto) usize {
        var res: usize = 0;
        if (self.type) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceProtoWire.TYPE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceProtoWire.FRAME_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.visible_frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceProtoWire.VISIBLE_FRAME_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.visible != false) {
            res += gremlin.sizes.sizeWireNumber(InsetsSourceProtoWire.VISIBLE_WIRE) + gremlin.sizes.sizeBool(self.visible);
        }
        if (self.type_number != 0) {
            res += gremlin.sizes.sizeWireNumber(InsetsSourceProtoWire.TYPE_NUMBER_WIRE) + gremlin.sizes.sizeI32(self.type_number);
        }
        if (self.attached_insets) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceProtoWire.ATTACHED_INSETS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const InsetsSourceProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InsetsSourceProto, target: *gremlin.Writer) void {
        if (self.type) |v| {
            if (v.len > 0) {
                target.appendBytes(InsetsSourceProtoWire.TYPE_WIRE, v);
            }
        }
        if (self.frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsSourceProtoWire.FRAME_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.visible_frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsSourceProtoWire.VISIBLE_FRAME_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.visible != false) {
            target.appendBool(InsetsSourceProtoWire.VISIBLE_WIRE, self.visible);
        }
        if (self.type_number != 0) {
            target.appendInt32(InsetsSourceProtoWire.TYPE_NUMBER_WIRE, self.type_number);
        }
        if (self.attached_insets) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsSourceProtoWire.ATTACHED_INSETS_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const InsetsSourceProtoReader = struct {
    buf: gremlin.Reader,
    _type: ?[]const u8 = null,
    _frame_buf: ?[]const u8 = null,
    _visible_frame_buf: ?[]const u8 = null,
    _visible: bool = false,
    _type_number: i32 = 0,
    _attached_insets_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!InsetsSourceProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InsetsSourceProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InsetsSourceProtoWire.TYPE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                InsetsSourceProtoWire.FRAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._frame_buf = result.value;
                },
                InsetsSourceProtoWire.VISIBLE_FRAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._visible_frame_buf = result.value;
                },
                InsetsSourceProtoWire.VISIBLE_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._visible = result.value;
                },
                InsetsSourceProtoWire.TYPE_NUMBER_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._type_number = result.value;
                },
                InsetsSourceProtoWire.ATTACHED_INSETS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._attached_insets_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getType(self: *const InsetsSourceProtoReader) []const u8 {
        return self._type orelse &[_]u8{};
    }
    pub fn getFrame(self: *const InsetsSourceProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._frame_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getVisibleFrame(self: *const InsetsSourceProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._visible_frame_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub inline fn getVisible(self: *const InsetsSourceProtoReader) bool {
        return self._visible;
    }
    pub inline fn getTypeNumber(self: *const InsetsSourceProtoReader) i32 {
        return self._type_number;
    }
    pub fn getAttachedInsets(self: *const InsetsSourceProtoReader) gremlin.Error!insets.InsetsProtoReader {
        if (self._attached_insets_buf) |buf| {
            return try insets.InsetsProtoReader.init(buf);
        }
        return try insets.InsetsProtoReader.init(&[_]u8{});
    }
};
