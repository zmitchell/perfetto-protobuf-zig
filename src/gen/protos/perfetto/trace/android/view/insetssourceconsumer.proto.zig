// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const insetssourcecontrol = @import("insetssourcecontrol.proto.zig");
const rect = @import("src/gen/protos/perfetto/trace/android/graphics/rect.proto.zig");
// structs
const InsetsSourceConsumerProtoWire = struct {
    const INTERNAL_INSETS_TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const HAS_WINDOW_FOCUS_WIRE: gremlin.ProtoWireNumber = 2;
    const IS_REQUESTED_VISIBLE_WIRE: gremlin.ProtoWireNumber = 3;
    const SOURCE_CONTROL_WIRE: gremlin.ProtoWireNumber = 4;
    const PENDING_FRAME_WIRE: gremlin.ProtoWireNumber = 5;
    const PENDING_VISIBLE_FRAME_WIRE: gremlin.ProtoWireNumber = 6;
    const ANIMATION_STATE_WIRE: gremlin.ProtoWireNumber = 7;
    const TYPE_NUMBER_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const InsetsSourceConsumerProto = struct {
    // fields
    internal_insets_type: ?[]const u8 = null,
    has_window_focus: bool = false,
    is_requested_visible: bool = false,
    source_control: ?insetssourcecontrol.InsetsSourceControlProto = null,
    pending_frame: ?rect.RectProto = null,
    pending_visible_frame: ?rect.RectProto = null,
    animation_state: i32 = 0,
    type_number: i32 = 0,
    pub fn calcProtobufSize(self: *const InsetsSourceConsumerProto) usize {
        var res: usize = 0;
        if (self.internal_insets_type) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceConsumerProtoWire.INTERNAL_INSETS_TYPE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.has_window_focus != false) {
            res += gremlin.sizes.sizeWireNumber(InsetsSourceConsumerProtoWire.HAS_WINDOW_FOCUS_WIRE) + gremlin.sizes.sizeBool(self.has_window_focus);
        }
        if (self.is_requested_visible != false) {
            res += gremlin.sizes.sizeWireNumber(InsetsSourceConsumerProtoWire.IS_REQUESTED_VISIBLE_WIRE) + gremlin.sizes.sizeBool(self.is_requested_visible);
        }
        if (self.source_control) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceConsumerProtoWire.SOURCE_CONTROL_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.pending_frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceConsumerProtoWire.PENDING_FRAME_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.pending_visible_frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceConsumerProtoWire.PENDING_VISIBLE_FRAME_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.animation_state != 0) {
            res += gremlin.sizes.sizeWireNumber(InsetsSourceConsumerProtoWire.ANIMATION_STATE_WIRE) + gremlin.sizes.sizeI32(self.animation_state);
        }
        if (self.type_number != 0) {
            res += gremlin.sizes.sizeWireNumber(InsetsSourceConsumerProtoWire.TYPE_NUMBER_WIRE) + gremlin.sizes.sizeI32(self.type_number);
        }
        return res;
    }
    pub fn encode(self: *const InsetsSourceConsumerProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InsetsSourceConsumerProto, target: *gremlin.Writer) void {
        if (self.internal_insets_type) |v| {
            if (v.len > 0) {
                target.appendBytes(InsetsSourceConsumerProtoWire.INTERNAL_INSETS_TYPE_WIRE, v);
            }
        }
        if (self.has_window_focus != false) {
            target.appendBool(InsetsSourceConsumerProtoWire.HAS_WINDOW_FOCUS_WIRE, self.has_window_focus);
        }
        if (self.is_requested_visible != false) {
            target.appendBool(InsetsSourceConsumerProtoWire.IS_REQUESTED_VISIBLE_WIRE, self.is_requested_visible);
        }
        if (self.source_control) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsSourceConsumerProtoWire.SOURCE_CONTROL_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.pending_frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsSourceConsumerProtoWire.PENDING_FRAME_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.pending_visible_frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsSourceConsumerProtoWire.PENDING_VISIBLE_FRAME_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.animation_state != 0) {
            target.appendInt32(InsetsSourceConsumerProtoWire.ANIMATION_STATE_WIRE, self.animation_state);
        }
        if (self.type_number != 0) {
            target.appendInt32(InsetsSourceConsumerProtoWire.TYPE_NUMBER_WIRE, self.type_number);
        }
    }
};
pub const InsetsSourceConsumerProtoReader = struct {
    buf: gremlin.Reader,
    _internal_insets_type: ?[]const u8 = null,
    _has_window_focus: bool = false,
    _is_requested_visible: bool = false,
    _source_control_buf: ?[]const u8 = null,
    _pending_frame_buf: ?[]const u8 = null,
    _pending_visible_frame_buf: ?[]const u8 = null,
    _animation_state: i32 = 0,
    _type_number: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!InsetsSourceConsumerProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InsetsSourceConsumerProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InsetsSourceConsumerProtoWire.INTERNAL_INSETS_TYPE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._internal_insets_type = result.value;
                },
                InsetsSourceConsumerProtoWire.HAS_WINDOW_FOCUS_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._has_window_focus = result.value;
                },
                InsetsSourceConsumerProtoWire.IS_REQUESTED_VISIBLE_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._is_requested_visible = result.value;
                },
                InsetsSourceConsumerProtoWire.SOURCE_CONTROL_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._source_control_buf = result.value;
                },
                InsetsSourceConsumerProtoWire.PENDING_FRAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._pending_frame_buf = result.value;
                },
                InsetsSourceConsumerProtoWire.PENDING_VISIBLE_FRAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._pending_visible_frame_buf = result.value;
                },
                InsetsSourceConsumerProtoWire.ANIMATION_STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._animation_state = result.value;
                },
                InsetsSourceConsumerProtoWire.TYPE_NUMBER_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._type_number = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getInternalInsetsType(self: *const InsetsSourceConsumerProtoReader) []const u8 {
        return self._internal_insets_type orelse &[_]u8{};
    }
    pub inline fn getHasWindowFocus(self: *const InsetsSourceConsumerProtoReader) bool {
        return self._has_window_focus;
    }
    pub inline fn getIsRequestedVisible(self: *const InsetsSourceConsumerProtoReader) bool {
        return self._is_requested_visible;
    }
    pub fn getSourceControl(self: *const InsetsSourceConsumerProtoReader) gremlin.Error!insetssourcecontrol.InsetsSourceControlProtoReader {
        if (self._source_control_buf) |buf| {
            return try insetssourcecontrol.InsetsSourceControlProtoReader.init(buf);
        }
        return try insetssourcecontrol.InsetsSourceControlProtoReader.init(&[_]u8{});
    }
    pub fn getPendingFrame(self: *const InsetsSourceConsumerProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._pending_frame_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getPendingVisibleFrame(self: *const InsetsSourceConsumerProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._pending_visible_frame_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub inline fn getAnimationState(self: *const InsetsSourceConsumerProtoReader) i32 {
        return self._animation_state;
    }
    pub inline fn getTypeNumber(self: *const InsetsSourceConsumerProtoReader) i32 {
        return self._type_number;
    }
};
