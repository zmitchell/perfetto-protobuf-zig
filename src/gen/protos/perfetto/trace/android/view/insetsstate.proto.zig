// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const insetssource = @import("insetssource.proto.zig");
const rect = @import("src/gen/protos/perfetto/trace/android/graphics/rect.proto.zig");
const displaycutout = @import("displaycutout.proto.zig");
// structs
const InsetsStateProtoWire = struct {
    const SOURCES_WIRE: gremlin.ProtoWireNumber = 1;
    const DISPLAY_FRAME_WIRE: gremlin.ProtoWireNumber = 2;
    const DISPLAY_CUTOUT_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const InsetsStateProto = struct {
    // fields
    sources: ?[]const ?insetssource.InsetsSourceProto = null,
    display_frame: ?rect.RectProto = null,
    display_cutout: ?displaycutout.DisplayCutoutProto = null,
    pub fn calcProtobufSize(self: *const InsetsStateProto) usize {
        var res: usize = 0;
        if (self.sources) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(InsetsStateProtoWire.SOURCES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.display_frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsStateProtoWire.DISPLAY_FRAME_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.display_cutout) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsStateProtoWire.DISPLAY_CUTOUT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const InsetsStateProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InsetsStateProto, target: *gremlin.Writer) void {
        if (self.sources) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(InsetsStateProtoWire.SOURCES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(InsetsStateProtoWire.SOURCES_WIRE, 0);
                }
            }
        }
        if (self.display_frame) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsStateProtoWire.DISPLAY_FRAME_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.display_cutout) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsStateProtoWire.DISPLAY_CUTOUT_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const InsetsStateProtoReader = struct {
    buf: gremlin.Reader,
    _sources_offset: ?usize = null,
    _sources_last_offset: ?usize = null,
    _sources_cnt: usize = 0,
    _display_frame_buf: ?[]const u8 = null,
    _display_cutout_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!InsetsStateProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InsetsStateProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InsetsStateProtoWire.SOURCES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._sources_offset == null) {
                        res._sources_offset = offset - result.size;
                    }
                    res._sources_last_offset = offset;
                    res._sources_cnt += 1;
                },
                InsetsStateProtoWire.DISPLAY_FRAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._display_frame_buf = result.value;
                },
                InsetsStateProtoWire.DISPLAY_CUTOUT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._display_cutout_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn sourcesCount(self: *const InsetsStateProtoReader) usize {
        return self._sources_cnt;
    }
    pub fn sourcesNext(self: *InsetsStateProtoReader) ?insetssource.InsetsSourceProtoReader {
        if (self._sources_offset == null) return null;
        const current_offset = self._sources_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = insetssource.InsetsSourceProtoReader.init(result.value) catch return null;
        if (self._sources_last_offset != null and current_offset >= self._sources_last_offset.?) {
            self._sources_offset = null;
            return msg;
        }
        if (self._sources_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._sources_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == InsetsStateProtoWire.SOURCES_WIRE) {
                self._sources_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._sources_offset = null;
        return msg;
    }
    pub fn getDisplayFrame(self: *const InsetsStateProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._display_frame_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getDisplayCutout(self: *const InsetsStateProtoReader) gremlin.Error!displaycutout.DisplayCutoutProtoReader {
        if (self._display_cutout_buf) |buf| {
            return try displaycutout.DisplayCutoutProtoReader.init(buf);
        }
        return try displaycutout.DisplayCutoutProtoReader.init(&[_]u8{});
    }
};
