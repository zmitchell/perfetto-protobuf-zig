// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const point = @import("src/gen/protos/perfetto/trace/android/graphics/point.proto.zig");
const surfacecontrol = @import("surfacecontrol.proto.zig");
// structs
const InsetsSourceControlProtoWire = struct {
    const TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const POSITION_WIRE: gremlin.ProtoWireNumber = 2;
    const LEASH_WIRE: gremlin.ProtoWireNumber = 3;
    const TYPE_NUMBER_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const InsetsSourceControlProto = struct {
    // fields
    type: ?[]const u8 = null,
    position: ?point.PointProto = null,
    leash: ?surfacecontrol.SurfaceControlProto = null,
    type_number: i32 = 0,
    pub fn calcProtobufSize(self: *const InsetsSourceControlProto) usize {
        var res: usize = 0;
        if (self.type) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceControlProtoWire.TYPE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.position) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceControlProtoWire.POSITION_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.leash) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsSourceControlProtoWire.LEASH_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.type_number != 0) {
            res += gremlin.sizes.sizeWireNumber(InsetsSourceControlProtoWire.TYPE_NUMBER_WIRE) + gremlin.sizes.sizeI32(self.type_number);
        }
        return res;
    }
    pub fn encode(self: *const InsetsSourceControlProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InsetsSourceControlProto, target: *gremlin.Writer) void {
        if (self.type) |v| {
            if (v.len > 0) {
                target.appendBytes(InsetsSourceControlProtoWire.TYPE_WIRE, v);
            }
        }
        if (self.position) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsSourceControlProtoWire.POSITION_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.leash) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsSourceControlProtoWire.LEASH_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.type_number != 0) {
            target.appendInt32(InsetsSourceControlProtoWire.TYPE_NUMBER_WIRE, self.type_number);
        }
    }
};
pub const InsetsSourceControlProtoReader = struct {
    buf: gremlin.Reader,
    _type: ?[]const u8 = null,
    _position_buf: ?[]const u8 = null,
    _leash_buf: ?[]const u8 = null,
    _type_number: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!InsetsSourceControlProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InsetsSourceControlProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InsetsSourceControlProtoWire.TYPE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                InsetsSourceControlProtoWire.POSITION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._position_buf = result.value;
                },
                InsetsSourceControlProtoWire.LEASH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._leash_buf = result.value;
                },
                InsetsSourceControlProtoWire.TYPE_NUMBER_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._type_number = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getType(self: *const InsetsSourceControlProtoReader) []const u8 {
        return self._type orelse &[_]u8{};
    }
    pub fn getPosition(self: *const InsetsSourceControlProtoReader) gremlin.Error!point.PointProtoReader {
        if (self._position_buf) |buf| {
            return try point.PointProtoReader.init(buf);
        }
        return try point.PointProtoReader.init(&[_]u8{});
    }
    pub fn getLeash(self: *const InsetsSourceControlProtoReader) gremlin.Error!surfacecontrol.SurfaceControlProtoReader {
        if (self._leash_buf) |buf| {
            return try surfacecontrol.SurfaceControlProtoReader.init(buf);
        }
        return try surfacecontrol.SurfaceControlProtoReader.init(&[_]u8{});
    }
    pub inline fn getTypeNumber(self: *const InsetsSourceControlProtoReader) i32 {
        return self._type_number;
    }
};
