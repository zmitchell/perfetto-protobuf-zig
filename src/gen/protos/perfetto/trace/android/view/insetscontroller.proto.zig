// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const insetsstate = @import("insetsstate.proto.zig");
const insetsanimationcontrolimpl = @import("insetsanimationcontrolimpl.proto.zig");
// structs
const InsetsControllerProtoWire = struct {
    const STATE_WIRE: gremlin.ProtoWireNumber = 1;
    const CONTROL_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const InsetsControllerProto = struct {
    // fields
    state: ?insetsstate.InsetsStateProto = null,
    control: ?[]const ?insetsanimationcontrolimpl.InsetsAnimationControlImplProto = null,
    pub fn calcProtobufSize(self: *const InsetsControllerProto) usize {
        var res: usize = 0;
        if (self.state) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InsetsControllerProtoWire.STATE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.control) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(InsetsControllerProtoWire.CONTROL_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const InsetsControllerProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InsetsControllerProto, target: *gremlin.Writer) void {
        if (self.state) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InsetsControllerProtoWire.STATE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.control) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(InsetsControllerProtoWire.CONTROL_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(InsetsControllerProtoWire.CONTROL_WIRE, 0);
                }
            }
        }
    }
};
pub const InsetsControllerProtoReader = struct {
    buf: gremlin.Reader,
    _state_buf: ?[]const u8 = null,
    _control_offset: ?usize = null,
    _control_last_offset: ?usize = null,
    _control_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!InsetsControllerProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InsetsControllerProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InsetsControllerProtoWire.STATE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._state_buf = result.value;
                },
                InsetsControllerProtoWire.CONTROL_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._control_offset == null) {
                        res._control_offset = offset - result.size;
                    }
                    res._control_last_offset = offset;
                    res._control_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getState(self: *const InsetsControllerProtoReader) gremlin.Error!insetsstate.InsetsStateProtoReader {
        if (self._state_buf) |buf| {
            return try insetsstate.InsetsStateProtoReader.init(buf);
        }
        return try insetsstate.InsetsStateProtoReader.init(&[_]u8{});
    }
    pub fn controlCount(self: *const InsetsControllerProtoReader) usize {
        return self._control_cnt;
    }
    pub fn controlNext(self: *InsetsControllerProtoReader) ?insetsanimationcontrolimpl.InsetsAnimationControlImplProtoReader {
        if (self._control_offset == null) return null;
        const current_offset = self._control_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = insetsanimationcontrolimpl.InsetsAnimationControlImplProtoReader.init(result.value) catch return null;
        if (self._control_last_offset != null and current_offset >= self._control_last_offset.?) {
            self._control_offset = null;
            return msg;
        }
        if (self._control_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._control_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == InsetsControllerProtoWire.CONTROL_WIRE) {
                self._control_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._control_offset = null;
        return msg;
    }
};
