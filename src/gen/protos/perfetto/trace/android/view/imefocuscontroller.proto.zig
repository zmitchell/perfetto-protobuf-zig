// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ImeFocusControllerProtoWire = struct {
    const HAS_IME_FOCUS_WIRE: gremlin.ProtoWireNumber = 1;
    const SERVED_VIEW_WIRE: gremlin.ProtoWireNumber = 2;
    const NEXT_SERVED_VIEW_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ImeFocusControllerProto = struct {
    // fields
    has_ime_focus: bool = false,
    served_view: ?[]const u8 = null,
    next_served_view: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ImeFocusControllerProto) usize {
        var res: usize = 0;
        if (self.has_ime_focus != false) {
            res += gremlin.sizes.sizeWireNumber(ImeFocusControllerProtoWire.HAS_IME_FOCUS_WIRE) + gremlin.sizes.sizeBool(self.has_ime_focus);
        }
        if (self.served_view) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ImeFocusControllerProtoWire.SERVED_VIEW_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.next_served_view) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ImeFocusControllerProtoWire.NEXT_SERVED_VIEW_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const ImeFocusControllerProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ImeFocusControllerProto, target: *gremlin.Writer) void {
        if (self.has_ime_focus != false) {
            target.appendBool(ImeFocusControllerProtoWire.HAS_IME_FOCUS_WIRE, self.has_ime_focus);
        }
        if (self.served_view) |v| {
            if (v.len > 0) {
                target.appendBytes(ImeFocusControllerProtoWire.SERVED_VIEW_WIRE, v);
            }
        }
        if (self.next_served_view) |v| {
            if (v.len > 0) {
                target.appendBytes(ImeFocusControllerProtoWire.NEXT_SERVED_VIEW_WIRE, v);
            }
        }
    }
};
pub const ImeFocusControllerProtoReader = struct {
    buf: gremlin.Reader,
    _has_ime_focus: bool = false,
    _served_view: ?[]const u8 = null,
    _next_served_view: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ImeFocusControllerProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = ImeFocusControllerProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ImeFocusControllerProtoWire.HAS_IME_FOCUS_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._has_ime_focus = result.value;
                },
                ImeFocusControllerProtoWire.SERVED_VIEW_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._served_view = result.value;
                },
                ImeFocusControllerProtoWire.NEXT_SERVED_VIEW_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._next_served_view = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getHasImeFocus(self: *const ImeFocusControllerProtoReader) bool {
        return self._has_ime_focus;
    }
    pub inline fn getServedView(self: *const ImeFocusControllerProtoReader) []const u8 {
        return self._served_view orelse &[_]u8{};
    }
    pub inline fn getNextServedView(self: *const ImeFocusControllerProtoReader) []const u8 {
        return self._next_served_view orelse &[_]u8{};
    }
};
