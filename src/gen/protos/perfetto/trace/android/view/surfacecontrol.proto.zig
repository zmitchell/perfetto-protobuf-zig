// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const SurfaceControlProtoWire = struct {
    const HASH_CODE_WIRE: gremlin.ProtoWireNumber = 1;
    const NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const LAYER_ID_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const SurfaceControlProto = struct {
    // fields
    hash_code: i32 = 0,
    name: ?[]const u8 = null,
    layer_id: i32 = 0,
    pub fn calcProtobufSize(self: *const SurfaceControlProto) usize {
        var res: usize = 0;
        if (self.hash_code != 0) {
            res += gremlin.sizes.sizeWireNumber(SurfaceControlProtoWire.HASH_CODE_WIRE) + gremlin.sizes.sizeI32(self.hash_code);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SurfaceControlProtoWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.layer_id != 0) {
            res += gremlin.sizes.sizeWireNumber(SurfaceControlProtoWire.LAYER_ID_WIRE) + gremlin.sizes.sizeI32(self.layer_id);
        }
        return res;
    }
    pub fn encode(self: *const SurfaceControlProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SurfaceControlProto, target: *gremlin.Writer) void {
        if (self.hash_code != 0) {
            target.appendInt32(SurfaceControlProtoWire.HASH_CODE_WIRE, self.hash_code);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(SurfaceControlProtoWire.NAME_WIRE, v);
            }
        }
        if (self.layer_id != 0) {
            target.appendInt32(SurfaceControlProtoWire.LAYER_ID_WIRE, self.layer_id);
        }
    }
};
pub const SurfaceControlProtoReader = struct {
    buf: gremlin.Reader,
    _hash_code: i32 = 0,
    _name: ?[]const u8 = null,
    _layer_id: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!SurfaceControlProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = SurfaceControlProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SurfaceControlProtoWire.HASH_CODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._hash_code = result.value;
                },
                SurfaceControlProtoWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                SurfaceControlProtoWire.LAYER_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._layer_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getHashCode(self: *const SurfaceControlProtoReader) i32 {
        return self._hash_code;
    }
    pub inline fn getName(self: *const SurfaceControlProtoReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getLayerId(self: *const SurfaceControlProtoReader) i32 {
        return self._layer_id;
    }
};
