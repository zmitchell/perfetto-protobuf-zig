// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const displaycutout = @import("displaycutout.proto.zig");
// structs
const DisplayInfoProtoWire = struct {
    const LOGICAL_WIDTH_WIRE: gremlin.ProtoWireNumber = 1;
    const LOGICAL_HEIGHT_WIRE: gremlin.ProtoWireNumber = 2;
    const APP_WIDTH_WIRE: gremlin.ProtoWireNumber = 3;
    const APP_HEIGHT_WIRE: gremlin.ProtoWireNumber = 4;
    const NAME_WIRE: gremlin.ProtoWireNumber = 5;
    const FLAGS_WIRE: gremlin.ProtoWireNumber = 6;
    const CUTOUT_WIRE: gremlin.ProtoWireNumber = 7;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const DisplayInfoProto = struct {
    // fields
    logical_width: i32 = 0,
    logical_height: i32 = 0,
    app_width: i32 = 0,
    app_height: i32 = 0,
    name: ?[]const u8 = null,
    flags: i32 = 0,
    cutout: ?displaycutout.DisplayCutoutProto = null,
    type: i32 = 0,
    pub fn calcProtobufSize(self: *const DisplayInfoProto) usize {
        var res: usize = 0;
        if (self.logical_width != 0) {
            res += gremlin.sizes.sizeWireNumber(DisplayInfoProtoWire.LOGICAL_WIDTH_WIRE) + gremlin.sizes.sizeI32(self.logical_width);
        }
        if (self.logical_height != 0) {
            res += gremlin.sizes.sizeWireNumber(DisplayInfoProtoWire.LOGICAL_HEIGHT_WIRE) + gremlin.sizes.sizeI32(self.logical_height);
        }
        if (self.app_width != 0) {
            res += gremlin.sizes.sizeWireNumber(DisplayInfoProtoWire.APP_WIDTH_WIRE) + gremlin.sizes.sizeI32(self.app_width);
        }
        if (self.app_height != 0) {
            res += gremlin.sizes.sizeWireNumber(DisplayInfoProtoWire.APP_HEIGHT_WIRE) + gremlin.sizes.sizeI32(self.app_height);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DisplayInfoProtoWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.flags != 0) {
            res += gremlin.sizes.sizeWireNumber(DisplayInfoProtoWire.FLAGS_WIRE) + gremlin.sizes.sizeI32(self.flags);
        }
        if (self.cutout) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(DisplayInfoProtoWire.CUTOUT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(DisplayInfoProtoWire.TYPE_WIRE) + gremlin.sizes.sizeI32(self.type);
        }
        return res;
    }
    pub fn encode(self: *const DisplayInfoProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DisplayInfoProto, target: *gremlin.Writer) void {
        if (self.logical_width != 0) {
            target.appendInt32(DisplayInfoProtoWire.LOGICAL_WIDTH_WIRE, self.logical_width);
        }
        if (self.logical_height != 0) {
            target.appendInt32(DisplayInfoProtoWire.LOGICAL_HEIGHT_WIRE, self.logical_height);
        }
        if (self.app_width != 0) {
            target.appendInt32(DisplayInfoProtoWire.APP_WIDTH_WIRE, self.app_width);
        }
        if (self.app_height != 0) {
            target.appendInt32(DisplayInfoProtoWire.APP_HEIGHT_WIRE, self.app_height);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(DisplayInfoProtoWire.NAME_WIRE, v);
            }
        }
        if (self.flags != 0) {
            target.appendInt32(DisplayInfoProtoWire.FLAGS_WIRE, self.flags);
        }
        if (self.cutout) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(DisplayInfoProtoWire.CUTOUT_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.type != 0) {
            target.appendInt32(DisplayInfoProtoWire.TYPE_WIRE, self.type);
        }
    }
};
pub const DisplayInfoProtoReader = struct {
    buf: gremlin.Reader,
    _logical_width: i32 = 0,
    _logical_height: i32 = 0,
    _app_width: i32 = 0,
    _app_height: i32 = 0,
    _name: ?[]const u8 = null,
    _flags: i32 = 0,
    _cutout_buf: ?[]const u8 = null,
    _type: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DisplayInfoProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = DisplayInfoProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DisplayInfoProtoWire.LOGICAL_WIDTH_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._logical_width = result.value;
                },
                DisplayInfoProtoWire.LOGICAL_HEIGHT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._logical_height = result.value;
                },
                DisplayInfoProtoWire.APP_WIDTH_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._app_width = result.value;
                },
                DisplayInfoProtoWire.APP_HEIGHT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._app_height = result.value;
                },
                DisplayInfoProtoWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                DisplayInfoProtoWire.FLAGS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._flags = result.value;
                },
                DisplayInfoProtoWire.CUTOUT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cutout_buf = result.value;
                },
                DisplayInfoProtoWire.TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getLogicalWidth(self: *const DisplayInfoProtoReader) i32 {
        return self._logical_width;
    }
    pub inline fn getLogicalHeight(self: *const DisplayInfoProtoReader) i32 {
        return self._logical_height;
    }
    pub inline fn getAppWidth(self: *const DisplayInfoProtoReader) i32 {
        return self._app_width;
    }
    pub inline fn getAppHeight(self: *const DisplayInfoProtoReader) i32 {
        return self._app_height;
    }
    pub inline fn getName(self: *const DisplayInfoProtoReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getFlags(self: *const DisplayInfoProtoReader) i32 {
        return self._flags;
    }
    pub fn getCutout(self: *const DisplayInfoProtoReader) gremlin.Error!displaycutout.DisplayCutoutProtoReader {
        if (self._cutout_buf) |buf| {
            return try displaycutout.DisplayCutoutProtoReader.init(buf);
        }
        return try displaycutout.DisplayCutoutProtoReader.init(&[_]u8{});
    }
    pub inline fn getType(self: *const DisplayInfoProtoReader) i32 {
        return self._type;
    }
};
