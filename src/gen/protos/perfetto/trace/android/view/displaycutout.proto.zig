// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const rect = @import("src/gen/protos/perfetto/trace/android/graphics/rect.proto.zig");
// structs
const DisplayCutoutProtoWire = struct {
    const INSETS_WIRE: gremlin.ProtoWireNumber = 1;
    const BOUND_LEFT_WIRE: gremlin.ProtoWireNumber = 3;
    const BOUND_TOP_WIRE: gremlin.ProtoWireNumber = 4;
    const BOUND_RIGHT_WIRE: gremlin.ProtoWireNumber = 5;
    const BOUND_BOTTOM_WIRE: gremlin.ProtoWireNumber = 6;
    const WATERFALL_INSETS_WIRE: gremlin.ProtoWireNumber = 7;
    const SIDE_OVERRIDES_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const DisplayCutoutProto = struct {
    // fields
    insets: ?rect.RectProto = null,
    bound_left: ?rect.RectProto = null,
    bound_top: ?rect.RectProto = null,
    bound_right: ?rect.RectProto = null,
    bound_bottom: ?rect.RectProto = null,
    waterfall_insets: ?rect.RectProto = null,
    side_overrides: ?[]const i32 = null,
    pub fn calcProtobufSize(self: *const DisplayCutoutProto) usize {
        var res: usize = 0;
        if (self.insets) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(DisplayCutoutProtoWire.INSETS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.bound_left) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(DisplayCutoutProtoWire.BOUND_LEFT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.bound_top) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(DisplayCutoutProtoWire.BOUND_TOP_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.bound_right) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(DisplayCutoutProtoWire.BOUND_RIGHT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.bound_bottom) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(DisplayCutoutProtoWire.BOUND_BOTTOM_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.waterfall_insets) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(DisplayCutoutProtoWire.WATERFALL_INSETS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.side_overrides) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(DisplayCutoutProtoWire.SIDE_OVERRIDES_WIRE) + gremlin.sizes.sizeI32(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI32(v);
                }
                res += gremlin.sizes.sizeWireNumber(DisplayCutoutProtoWire.SIDE_OVERRIDES_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        return res;
    }
    pub fn encode(self: *const DisplayCutoutProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DisplayCutoutProto, target: *gremlin.Writer) void {
        if (self.insets) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(DisplayCutoutProtoWire.INSETS_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.bound_left) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(DisplayCutoutProtoWire.BOUND_LEFT_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.bound_top) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(DisplayCutoutProtoWire.BOUND_TOP_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.bound_right) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(DisplayCutoutProtoWire.BOUND_RIGHT_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.bound_bottom) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(DisplayCutoutProtoWire.BOUND_BOTTOM_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.waterfall_insets) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(DisplayCutoutProtoWire.WATERFALL_INSETS_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.side_overrides) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendInt32(DisplayCutoutProtoWire.SIDE_OVERRIDES_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI32(v);
                }
                target.appendBytesTag(DisplayCutoutProtoWire.SIDE_OVERRIDES_WIRE, packed_size);
                for (arr) |v| {
                    target.appendInt32WithoutTag(v);
                }
            }
        }
    }
};
pub const DisplayCutoutProtoReader = struct {
    buf: gremlin.Reader,
    _insets_buf: ?[]const u8 = null,
    _bound_left_buf: ?[]const u8 = null,
    _bound_top_buf: ?[]const u8 = null,
    _bound_right_buf: ?[]const u8 = null,
    _bound_bottom_buf: ?[]const u8 = null,
    _waterfall_insets_buf: ?[]const u8 = null,
    _side_overrides_offset: ?usize = null,
    _side_overrides_last_offset: ?usize = null,
    _side_overrides_packed: bool = false,
    pub fn init(src: []const u8) gremlin.Error!DisplayCutoutProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = DisplayCutoutProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DisplayCutoutProtoWire.INSETS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._insets_buf = result.value;
                },
                DisplayCutoutProtoWire.BOUND_LEFT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._bound_left_buf = result.value;
                },
                DisplayCutoutProtoWire.BOUND_TOP_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._bound_top_buf = result.value;
                },
                DisplayCutoutProtoWire.BOUND_RIGHT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._bound_right_buf = result.value;
                },
                DisplayCutoutProtoWire.BOUND_BOTTOM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._bound_bottom_buf = result.value;
                },
                DisplayCutoutProtoWire.WATERFALL_INSETS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._waterfall_insets_buf = result.value;
                },
                DisplayCutoutProtoWire.SIDE_OVERRIDES_WIRE => {
                    if (res._side_overrides_offset == null) {
                        res._side_overrides_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._side_overrides_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._side_overrides_offset = offset + length_result.size;
                        res._side_overrides_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._side_overrides_last_offset.?;
                    } else {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._side_overrides_last_offset = offset;
                    }
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getInsets(self: *const DisplayCutoutProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._insets_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getBoundLeft(self: *const DisplayCutoutProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._bound_left_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getBoundTop(self: *const DisplayCutoutProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._bound_top_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getBoundRight(self: *const DisplayCutoutProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._bound_right_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getBoundBottom(self: *const DisplayCutoutProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._bound_bottom_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn getWaterfallInsets(self: *const DisplayCutoutProtoReader) gremlin.Error!rect.RectProtoReader {
        if (self._waterfall_insets_buf) |buf| {
            return try rect.RectProtoReader.init(buf);
        }
        return try rect.RectProtoReader.init(&[_]u8{});
    }
    pub fn sideOverridesNext(self: *DisplayCutoutProtoReader) gremlin.Error!?i32 {
        if (self._side_overrides_offset == null) return null;
        const current_offset = self._side_overrides_offset.?;
        if (current_offset >= self._side_overrides_last_offset.?) {
            self._side_overrides_offset = null;
            return null;
        }
        if (self._side_overrides_packed) {
            const value_result = try self.buf.readInt32(current_offset);
            self._side_overrides_offset = current_offset + value_result.size;
            if (self._side_overrides_offset.? >= self._side_overrides_last_offset.?) {
                self._side_overrides_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readInt32(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._side_overrides_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == DisplayCutoutProtoWire.SIDE_OVERRIDES_WIRE) {
                    self._side_overrides_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._side_overrides_offset = null;
            return value_result.value;
        }
    }
};
