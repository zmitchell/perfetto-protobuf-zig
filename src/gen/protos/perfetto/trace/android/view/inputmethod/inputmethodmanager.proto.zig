// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const InputMethodManagerProtoWire = struct {
    const CUR_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const FULLSCREEN_MODE_WIRE: gremlin.ProtoWireNumber = 2;
    const DISPLAY_ID_WIRE: gremlin.ProtoWireNumber = 3;
    const ACTIVE_WIRE: gremlin.ProtoWireNumber = 4;
    const SERVED_CONNECTING_WIRE: gremlin.ProtoWireNumber = 5;
    const SERVED_VIEW_WIRE: gremlin.ProtoWireNumber = 6;
    const NEXT_SERVED_VIEW_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const InputMethodManagerProto = struct {
    // fields
    cur_id: ?[]const u8 = null,
    fullscreen_mode: bool = false,
    display_id: i32 = 0,
    active: bool = false,
    served_connecting: bool = false,
    served_view: ?[]const u8 = null,
    next_served_view: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const InputMethodManagerProto) usize {
        var res: usize = 0;
        if (self.cur_id) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodManagerProtoWire.CUR_ID_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.fullscreen_mode != false) {
            res += gremlin.sizes.sizeWireNumber(InputMethodManagerProtoWire.FULLSCREEN_MODE_WIRE) + gremlin.sizes.sizeBool(self.fullscreen_mode);
        }
        if (self.display_id != 0) {
            res += gremlin.sizes.sizeWireNumber(InputMethodManagerProtoWire.DISPLAY_ID_WIRE) + gremlin.sizes.sizeI32(self.display_id);
        }
        if (self.active != false) {
            res += gremlin.sizes.sizeWireNumber(InputMethodManagerProtoWire.ACTIVE_WIRE) + gremlin.sizes.sizeBool(self.active);
        }
        if (self.served_connecting != false) {
            res += gremlin.sizes.sizeWireNumber(InputMethodManagerProtoWire.SERVED_CONNECTING_WIRE) + gremlin.sizes.sizeBool(self.served_connecting);
        }
        if (self.served_view) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodManagerProtoWire.SERVED_VIEW_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.next_served_view) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InputMethodManagerProtoWire.NEXT_SERVED_VIEW_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const InputMethodManagerProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InputMethodManagerProto, target: *gremlin.Writer) void {
        if (self.cur_id) |v| {
            if (v.len > 0) {
                target.appendBytes(InputMethodManagerProtoWire.CUR_ID_WIRE, v);
            }
        }
        if (self.fullscreen_mode != false) {
            target.appendBool(InputMethodManagerProtoWire.FULLSCREEN_MODE_WIRE, self.fullscreen_mode);
        }
        if (self.display_id != 0) {
            target.appendInt32(InputMethodManagerProtoWire.DISPLAY_ID_WIRE, self.display_id);
        }
        if (self.active != false) {
            target.appendBool(InputMethodManagerProtoWire.ACTIVE_WIRE, self.active);
        }
        if (self.served_connecting != false) {
            target.appendBool(InputMethodManagerProtoWire.SERVED_CONNECTING_WIRE, self.served_connecting);
        }
        if (self.served_view) |v| {
            if (v.len > 0) {
                target.appendBytes(InputMethodManagerProtoWire.SERVED_VIEW_WIRE, v);
            }
        }
        if (self.next_served_view) |v| {
            if (v.len > 0) {
                target.appendBytes(InputMethodManagerProtoWire.NEXT_SERVED_VIEW_WIRE, v);
            }
        }
    }
};
pub const InputMethodManagerProtoReader = struct {
    buf: gremlin.Reader,
    _cur_id: ?[]const u8 = null,
    _fullscreen_mode: bool = false,
    _display_id: i32 = 0,
    _active: bool = false,
    _served_connecting: bool = false,
    _served_view: ?[]const u8 = null,
    _next_served_view: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!InputMethodManagerProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = InputMethodManagerProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InputMethodManagerProtoWire.CUR_ID_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cur_id = result.value;
                },
                InputMethodManagerProtoWire.FULLSCREEN_MODE_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._fullscreen_mode = result.value;
                },
                InputMethodManagerProtoWire.DISPLAY_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._display_id = result.value;
                },
                InputMethodManagerProtoWire.ACTIVE_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._active = result.value;
                },
                InputMethodManagerProtoWire.SERVED_CONNECTING_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._served_connecting = result.value;
                },
                InputMethodManagerProtoWire.SERVED_VIEW_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._served_view = result.value;
                },
                InputMethodManagerProtoWire.NEXT_SERVED_VIEW_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._next_served_view = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCurId(self: *const InputMethodManagerProtoReader) []const u8 {
        return self._cur_id orelse &[_]u8{};
    }
    pub inline fn getFullscreenMode(self: *const InputMethodManagerProtoReader) bool {
        return self._fullscreen_mode;
    }
    pub inline fn getDisplayId(self: *const InputMethodManagerProtoReader) i32 {
        return self._display_id;
    }
    pub inline fn getActive(self: *const InputMethodManagerProtoReader) bool {
        return self._active;
    }
    pub inline fn getServedConnecting(self: *const InputMethodManagerProtoReader) bool {
        return self._served_connecting;
    }
    pub inline fn getServedView(self: *const InputMethodManagerProtoReader) []const u8 {
        return self._served_view orelse &[_]u8{};
    }
    pub inline fn getNextServedView(self: *const InputMethodManagerProtoReader) []const u8 {
        return self._next_served_view orelse &[_]u8{};
    }
};
