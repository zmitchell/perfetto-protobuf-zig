// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const AppWakelockInfoWire = struct {
    const IID_WIRE: gremlin.ProtoWireNumber = 1;
    const TAG_WIRE: gremlin.ProtoWireNumber = 2;
    const FLAGS_WIRE: gremlin.ProtoWireNumber = 3;
    const OWNER_PID_WIRE: gremlin.ProtoWireNumber = 4;
    const OWNER_UID_WIRE: gremlin.ProtoWireNumber = 5;
    const WORK_UID_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const AppWakelockInfo = struct {
    // fields
    iid: i32 = 0,
    tag: ?[]const u8 = null,
    flags: i32 = 0,
    owner_pid: i32 = 0,
    owner_uid: i32 = 0,
    work_uid: i32 = 0,
    pub fn calcProtobufSize(self: *const AppWakelockInfo) usize {
        var res: usize = 0;
        if (self.iid != 0) {
            res += gremlin.sizes.sizeWireNumber(AppWakelockInfoWire.IID_WIRE) + gremlin.sizes.sizeI32(self.iid);
        }
        if (self.tag) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AppWakelockInfoWire.TAG_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.flags != 0) {
            res += gremlin.sizes.sizeWireNumber(AppWakelockInfoWire.FLAGS_WIRE) + gremlin.sizes.sizeI32(self.flags);
        }
        if (self.owner_pid != 0) {
            res += gremlin.sizes.sizeWireNumber(AppWakelockInfoWire.OWNER_PID_WIRE) + gremlin.sizes.sizeI32(self.owner_pid);
        }
        if (self.owner_uid != 0) {
            res += gremlin.sizes.sizeWireNumber(AppWakelockInfoWire.OWNER_UID_WIRE) + gremlin.sizes.sizeI32(self.owner_uid);
        }
        if (self.work_uid != 0) {
            res += gremlin.sizes.sizeWireNumber(AppWakelockInfoWire.WORK_UID_WIRE) + gremlin.sizes.sizeI32(self.work_uid);
        }
        return res;
    }
    pub fn encode(self: *const AppWakelockInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AppWakelockInfo, target: *gremlin.Writer) void {
        if (self.iid != 0) {
            target.appendInt32(AppWakelockInfoWire.IID_WIRE, self.iid);
        }
        if (self.tag) |v| {
            if (v.len > 0) {
                target.appendBytes(AppWakelockInfoWire.TAG_WIRE, v);
            }
        }
        if (self.flags != 0) {
            target.appendInt32(AppWakelockInfoWire.FLAGS_WIRE, self.flags);
        }
        if (self.owner_pid != 0) {
            target.appendInt32(AppWakelockInfoWire.OWNER_PID_WIRE, self.owner_pid);
        }
        if (self.owner_uid != 0) {
            target.appendInt32(AppWakelockInfoWire.OWNER_UID_WIRE, self.owner_uid);
        }
        if (self.work_uid != 0) {
            target.appendInt32(AppWakelockInfoWire.WORK_UID_WIRE, self.work_uid);
        }
    }
};
pub const AppWakelockInfoReader = struct {
    buf: gremlin.Reader,
    _iid: i32 = 0,
    _tag: ?[]const u8 = null,
    _flags: i32 = 0,
    _owner_pid: i32 = 0,
    _owner_uid: i32 = 0,
    _work_uid: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!AppWakelockInfoReader {
        const buf = gremlin.Reader.init(src);
        var res = AppWakelockInfoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AppWakelockInfoWire.IID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._iid = result.value;
                },
                AppWakelockInfoWire.TAG_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._tag = result.value;
                },
                AppWakelockInfoWire.FLAGS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._flags = result.value;
                },
                AppWakelockInfoWire.OWNER_PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._owner_pid = result.value;
                },
                AppWakelockInfoWire.OWNER_UID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._owner_uid = result.value;
                },
                AppWakelockInfoWire.WORK_UID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._work_uid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIid(self: *const AppWakelockInfoReader) i32 {
        return self._iid;
    }
    pub inline fn getTag(self: *const AppWakelockInfoReader) []const u8 {
        return self._tag orelse &[_]u8{};
    }
    pub inline fn getFlags(self: *const AppWakelockInfoReader) i32 {
        return self._flags;
    }
    pub inline fn getOwnerPid(self: *const AppWakelockInfoReader) i32 {
        return self._owner_pid;
    }
    pub inline fn getOwnerUid(self: *const AppWakelockInfoReader) i32 {
        return self._owner_uid;
    }
    pub inline fn getWorkUid(self: *const AppWakelockInfoReader) i32 {
        return self._work_uid;
    }
};
const AppWakelockBundleWire = struct {
    const INTERN_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const ENCODED_TS_WIRE: gremlin.ProtoWireNumber = 2;
    const INFO_WIRE: gremlin.ProtoWireNumber = 3;
    const ACQUIRED_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const AppWakelockBundle = struct {
    // fields
    intern_id: ?[]const u32 = null,
    encoded_ts: ?[]const u64 = null,
    info: ?AppWakelockInfo = null,
    acquired: bool = false,
    pub fn calcProtobufSize(self: *const AppWakelockBundle) usize {
        var res: usize = 0;
        if (self.intern_id) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AppWakelockBundleWire.INTERN_ID_WIRE) + gremlin.sizes.sizeU32(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU32(v);
                }
                res += gremlin.sizes.sizeWireNumber(AppWakelockBundleWire.INTERN_ID_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.encoded_ts) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AppWakelockBundleWire.ENCODED_TS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AppWakelockBundleWire.ENCODED_TS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AppWakelockBundleWire.INFO_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.acquired != false) {
            res += gremlin.sizes.sizeWireNumber(AppWakelockBundleWire.ACQUIRED_WIRE) + gremlin.sizes.sizeBool(self.acquired);
        }
        return res;
    }
    pub fn encode(self: *const AppWakelockBundle, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AppWakelockBundle, target: *gremlin.Writer) void {
        if (self.intern_id) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint32(AppWakelockBundleWire.INTERN_ID_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU32(v);
                }
                target.appendBytesTag(AppWakelockBundleWire.INTERN_ID_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint32WithoutTag(v);
                }
            }
        }
        if (self.encoded_ts) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(AppWakelockBundleWire.ENCODED_TS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(AppWakelockBundleWire.ENCODED_TS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
        if (self.info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AppWakelockBundleWire.INFO_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.acquired != false) {
            target.appendBool(AppWakelockBundleWire.ACQUIRED_WIRE, self.acquired);
        }
    }
};
pub const AppWakelockBundleReader = struct {
    buf: gremlin.Reader,
    _intern_id_offset: ?usize = null,
    _intern_id_last_offset: ?usize = null,
    _intern_id_packed: bool = false,
    _encoded_ts_offset: ?usize = null,
    _encoded_ts_last_offset: ?usize = null,
    _encoded_ts_packed: bool = false,
    _info_buf: ?[]const u8 = null,
    _acquired: bool = false,
    pub fn init(src: []const u8) gremlin.Error!AppWakelockBundleReader {
        const buf = gremlin.Reader.init(src);
        var res = AppWakelockBundleReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AppWakelockBundleWire.INTERN_ID_WIRE => {
                    if (res._intern_id_offset == null) {
                        res._intern_id_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._intern_id_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._intern_id_offset = offset + length_result.size;
                        res._intern_id_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._intern_id_last_offset.?;
                    } else {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._intern_id_last_offset = offset;
                    }
                },
                AppWakelockBundleWire.ENCODED_TS_WIRE => {
                    if (res._encoded_ts_offset == null) {
                        res._encoded_ts_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._encoded_ts_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._encoded_ts_offset = offset + length_result.size;
                        res._encoded_ts_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._encoded_ts_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._encoded_ts_last_offset = offset;
                    }
                },
                AppWakelockBundleWire.INFO_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._info_buf = result.value;
                },
                AppWakelockBundleWire.ACQUIRED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._acquired = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn internIdNext(self: *AppWakelockBundleReader) gremlin.Error!?u32 {
        if (self._intern_id_offset == null) return null;
        const current_offset = self._intern_id_offset.?;
        if (current_offset >= self._intern_id_last_offset.?) {
            self._intern_id_offset = null;
            return null;
        }
        if (self._intern_id_packed) {
            const value_result = try self.buf.readUInt32(current_offset);
            self._intern_id_offset = current_offset + value_result.size;
            if (self._intern_id_offset.? >= self._intern_id_last_offset.?) {
                self._intern_id_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt32(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._intern_id_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AppWakelockBundleWire.INTERN_ID_WIRE) {
                    self._intern_id_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._intern_id_offset = null;
            return value_result.value;
        }
    }
    pub fn encodedTsNext(self: *AppWakelockBundleReader) gremlin.Error!?u64 {
        if (self._encoded_ts_offset == null) return null;
        const current_offset = self._encoded_ts_offset.?;
        if (current_offset >= self._encoded_ts_last_offset.?) {
            self._encoded_ts_offset = null;
            return null;
        }
        if (self._encoded_ts_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._encoded_ts_offset = current_offset + value_result.size;
            if (self._encoded_ts_offset.? >= self._encoded_ts_last_offset.?) {
                self._encoded_ts_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._encoded_ts_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AppWakelockBundleWire.ENCODED_TS_WIRE) {
                    self._encoded_ts_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._encoded_ts_offset = null;
            return value_result.value;
        }
    }
    pub fn getInfo(self: *const AppWakelockBundleReader) gremlin.Error!AppWakelockInfoReader {
        if (self._info_buf) |buf| {
            return try AppWakelockInfoReader.init(buf);
        }
        return try AppWakelockInfoReader.init(&[_]u8{});
    }
    pub inline fn getAcquired(self: *const AppWakelockBundleReader) bool {
        return self._acquired;
    }
};
