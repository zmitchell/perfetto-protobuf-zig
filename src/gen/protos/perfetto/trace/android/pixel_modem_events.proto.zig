// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PixelModemEventsWire = struct {
    const EVENTS_WIRE: gremlin.ProtoWireNumber = 1;
    const EVENT_TIME_NANOS_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const PixelModemEvents = struct {
    // fields
    events: ?[]const ?[]const u8 = null,
    event_time_nanos: ?[]const u64 = null,
    pub fn calcProtobufSize(self: *const PixelModemEvents) usize {
        var res: usize = 0;
        if (self.events) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(PixelModemEventsWire.EVENTS_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.event_time_nanos) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(PixelModemEventsWire.EVENT_TIME_NANOS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(PixelModemEventsWire.EVENT_TIME_NANOS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        return res;
    }
    pub fn encode(self: *const PixelModemEvents, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PixelModemEvents, target: *gremlin.Writer) void {
        if (self.events) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(PixelModemEventsWire.EVENTS_WIRE, v);
                } else {
                    target.appendBytesTag(PixelModemEventsWire.EVENTS_WIRE, 0);
                }
            }
        }
        if (self.event_time_nanos) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(PixelModemEventsWire.EVENT_TIME_NANOS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(PixelModemEventsWire.EVENT_TIME_NANOS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
    }
};
pub const PixelModemEventsReader = struct {
    buf: gremlin.Reader,
    _events_offset: ?usize = null,
    _events_last_offset: ?usize = null,
    _events_cnt: usize = 0,
    _event_time_nanos_offset: ?usize = null,
    _event_time_nanos_last_offset: ?usize = null,
    _event_time_nanos_packed: bool = false,
    pub fn init(src: []const u8) gremlin.Error!PixelModemEventsReader {
        const buf = gremlin.Reader.init(src);
        var res = PixelModemEventsReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PixelModemEventsWire.EVENTS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._events_offset == null) {
                        res._events_offset = offset - result.size;
                    }
                    res._events_last_offset = offset;
                    res._events_cnt += 1;
                },
                PixelModemEventsWire.EVENT_TIME_NANOS_WIRE => {
                    if (res._event_time_nanos_offset == null) {
                        res._event_time_nanos_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._event_time_nanos_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._event_time_nanos_offset = offset + length_result.size;
                        res._event_time_nanos_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._event_time_nanos_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._event_time_nanos_last_offset = offset;
                    }
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn eventsCount(self: *const PixelModemEventsReader) usize {
        return self._events_cnt;
    }
    pub fn eventsNext(self: *PixelModemEventsReader) ?[]const u8 {
        if (self._events_offset == null) return null;
        const current_offset = self._events_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._events_last_offset != null and current_offset >= self._events_last_offset.?) {
            self._events_offset = null;
            return result.value;
        }
        if (self._events_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._events_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == PixelModemEventsWire.EVENTS_WIRE) {
                self._events_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._events_offset = null;
        return result.value;
    }
    pub fn eventTimeNanosNext(self: *PixelModemEventsReader) gremlin.Error!?u64 {
        if (self._event_time_nanos_offset == null) return null;
        const current_offset = self._event_time_nanos_offset.?;
        if (current_offset >= self._event_time_nanos_last_offset.?) {
            self._event_time_nanos_offset = null;
            return null;
        }
        if (self._event_time_nanos_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._event_time_nanos_offset = current_offset + value_result.size;
            if (self._event_time_nanos_offset.? >= self._event_time_nanos_last_offset.?) {
                self._event_time_nanos_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._event_time_nanos_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == PixelModemEventsWire.EVENT_TIME_NANOS_WIRE) {
                    self._event_time_nanos_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._event_time_nanos_offset = null;
            return value_result.value;
        }
    }
};
const PixelModemTokenDatabaseWire = struct {
    const DATABASE_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const PixelModemTokenDatabase = struct {
    // fields
    database: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const PixelModemTokenDatabase) usize {
        var res: usize = 0;
        if (self.database) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(PixelModemTokenDatabaseWire.DATABASE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const PixelModemTokenDatabase, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PixelModemTokenDatabase, target: *gremlin.Writer) void {
        if (self.database) |v| {
            if (v.len > 0) {
                target.appendBytes(PixelModemTokenDatabaseWire.DATABASE_WIRE, v);
            }
        }
    }
};
pub const PixelModemTokenDatabaseReader = struct {
    buf: gremlin.Reader,
    _database: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!PixelModemTokenDatabaseReader {
        const buf = gremlin.Reader.init(src);
        var res = PixelModemTokenDatabaseReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PixelModemTokenDatabaseWire.DATABASE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._database = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDatabase(self: *const PixelModemTokenDatabaseReader) []const u8 {
        return self._database orelse &[_]u8{};
    }
};
