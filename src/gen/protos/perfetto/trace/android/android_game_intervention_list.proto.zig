// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const AndroidGameInterventionListWire = struct {
    const GAME_PACKAGES_WIRE: gremlin.ProtoWireNumber = 1;
    const PARSE_ERROR_WIRE: gremlin.ProtoWireNumber = 2;
    const READ_ERROR_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const AndroidGameInterventionList = struct {
    // nested structs
    const GameModeInfoWire = struct {
        const MODE_WIRE: gremlin.ProtoWireNumber = 1;
        const USE_ANGLE_WIRE: gremlin.ProtoWireNumber = 2;
        const RESOLUTION_DOWNSCALE_WIRE: gremlin.ProtoWireNumber = 3;
        const FPS_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const GameModeInfo = struct {
        // fields
        mode: u32 = 0,
        use_angle: bool = false,
        resolution_downscale: f32 = 0.0,
        fps: f32 = 0.0,
        pub fn calcProtobufSize(self: *const AndroidGameInterventionList.GameModeInfo) usize {
            var res: usize = 0;
            if (self.mode != 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionList.GameModeInfoWire.MODE_WIRE) + gremlin.sizes.sizeU32(self.mode);
            }
            if (self.use_angle != false) {
                res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionList.GameModeInfoWire.USE_ANGLE_WIRE) + gremlin.sizes.sizeBool(self.use_angle);
            }
            if (self.resolution_downscale != 0.0) {
                res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionList.GameModeInfoWire.RESOLUTION_DOWNSCALE_WIRE) + gremlin.sizes.sizeFloat(self.resolution_downscale);
            }
            if (self.fps != 0.0) {
                res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionList.GameModeInfoWire.FPS_WIRE) + gremlin.sizes.sizeFloat(self.fps);
            }
            return res;
        }
        pub fn encode(self: *const AndroidGameInterventionList.GameModeInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const AndroidGameInterventionList.GameModeInfo, target: *gremlin.Writer) void {
            if (self.mode != 0) {
                target.appendUint32(AndroidGameInterventionList.GameModeInfoWire.MODE_WIRE, self.mode);
            }
            if (self.use_angle != false) {
                target.appendBool(AndroidGameInterventionList.GameModeInfoWire.USE_ANGLE_WIRE, self.use_angle);
            }
            if (self.resolution_downscale != 0.0) {
                target.appendFloat32(AndroidGameInterventionList.GameModeInfoWire.RESOLUTION_DOWNSCALE_WIRE, self.resolution_downscale);
            }
            if (self.fps != 0.0) {
                target.appendFloat32(AndroidGameInterventionList.GameModeInfoWire.FPS_WIRE, self.fps);
            }
        }
    };
    pub const GameModeInfoReader = struct {
        buf: gremlin.Reader,
        _mode: u32 = 0,
        _use_angle: bool = false,
        _resolution_downscale: f32 = 0.0,
        _fps: f32 = 0.0,
        pub fn init(src: []const u8) gremlin.Error!AndroidGameInterventionList.GameModeInfoReader {
            const buf = gremlin.Reader.init(src);
            var res = AndroidGameInterventionList.GameModeInfoReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    AndroidGameInterventionList.GameModeInfoWire.MODE_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._mode = result.value;
                    },
                    AndroidGameInterventionList.GameModeInfoWire.USE_ANGLE_WIRE => {
                        const result = try buf.readBool(offset);
                        offset += result.size;
                        res._use_angle = result.value;
                    },
                    AndroidGameInterventionList.GameModeInfoWire.RESOLUTION_DOWNSCALE_WIRE => {
                        const result = try buf.readFloat32(offset);
                        offset += result.size;
                        res._resolution_downscale = result.value;
                    },
                    AndroidGameInterventionList.GameModeInfoWire.FPS_WIRE => {
                        const result = try buf.readFloat32(offset);
                        offset += result.size;
                        res._fps = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getMode(self: *const AndroidGameInterventionList.GameModeInfoReader) u32 {
            return self._mode;
        }
        pub inline fn getUseAngle(self: *const AndroidGameInterventionList.GameModeInfoReader) bool {
            return self._use_angle;
        }
        pub inline fn getResolutionDownscale(self: *const AndroidGameInterventionList.GameModeInfoReader) f32 {
            return self._resolution_downscale;
        }
        pub inline fn getFps(self: *const AndroidGameInterventionList.GameModeInfoReader) f32 {
            return self._fps;
        }
    };
    const GamePackageInfoWire = struct {
        const NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const UID_WIRE: gremlin.ProtoWireNumber = 2;
        const CURRENT_MODE_WIRE: gremlin.ProtoWireNumber = 3;
        const GAME_MODE_INFO_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const GamePackageInfo = struct {
        // fields
        name: ?[]const u8 = null,
        uid: u64 = 0,
        current_mode: u32 = 0,
        game_mode_info: ?[]const ?AndroidGameInterventionList.GameModeInfo = null,
        pub fn calcProtobufSize(self: *const AndroidGameInterventionList.GamePackageInfo) usize {
            var res: usize = 0;
            if (self.name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionList.GamePackageInfoWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.uid != 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionList.GamePackageInfoWire.UID_WIRE) + gremlin.sizes.sizeU64(self.uid);
            }
            if (self.current_mode != 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionList.GamePackageInfoWire.CURRENT_MODE_WIRE) + gremlin.sizes.sizeU32(self.current_mode);
            }
            if (self.game_mode_info) |arr| {
                for (arr) |maybe_v| {
                    res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionList.GamePackageInfoWire.GAME_MODE_INFO_WIRE);
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        res += gremlin.sizes.sizeUsize(size) + size;
                    } else {
                        res += gremlin.sizes.sizeUsize(0);
                    }
                }
            }
            return res;
        }
        pub fn encode(self: *const AndroidGameInterventionList.GamePackageInfo, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const AndroidGameInterventionList.GamePackageInfo, target: *gremlin.Writer) void {
            if (self.name) |v| {
                if (v.len > 0) {
                    target.appendBytes(AndroidGameInterventionList.GamePackageInfoWire.NAME_WIRE, v);
                }
            }
            if (self.uid != 0) {
                target.appendUint64(AndroidGameInterventionList.GamePackageInfoWire.UID_WIRE, self.uid);
            }
            if (self.current_mode != 0) {
                target.appendUint32(AndroidGameInterventionList.GamePackageInfoWire.CURRENT_MODE_WIRE, self.current_mode);
            }
            if (self.game_mode_info) |arr| {
                for (arr) |maybe_v| {
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        target.appendBytesTag(AndroidGameInterventionList.GamePackageInfoWire.GAME_MODE_INFO_WIRE, size);
                        v.encodeTo(target);
                    } else {
                        target.appendBytesTag(AndroidGameInterventionList.GamePackageInfoWire.GAME_MODE_INFO_WIRE, 0);
                    }
                }
            }
        }
    };
    pub const GamePackageInfoReader = struct {
        buf: gremlin.Reader,
        _name: ?[]const u8 = null,
        _uid: u64 = 0,
        _current_mode: u32 = 0,
        _game_mode_info_offset: ?usize = null,
        _game_mode_info_last_offset: ?usize = null,
        _game_mode_info_cnt: usize = 0,
        pub fn init(src: []const u8) gremlin.Error!AndroidGameInterventionList.GamePackageInfoReader {
            const buf = gremlin.Reader.init(src);
            var res = AndroidGameInterventionList.GamePackageInfoReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    AndroidGameInterventionList.GamePackageInfoWire.NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._name = result.value;
                    },
                    AndroidGameInterventionList.GamePackageInfoWire.UID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._uid = result.value;
                    },
                    AndroidGameInterventionList.GamePackageInfoWire.CURRENT_MODE_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._current_mode = result.value;
                    },
                    AndroidGameInterventionList.GamePackageInfoWire.GAME_MODE_INFO_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        if (res._game_mode_info_offset == null) {
                            res._game_mode_info_offset = offset - result.size;
                        }
                        res._game_mode_info_last_offset = offset;
                        res._game_mode_info_cnt += 1;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getName(self: *const AndroidGameInterventionList.GamePackageInfoReader) []const u8 {
            return self._name orelse &[_]u8{};
        }
        pub inline fn getUid(self: *const AndroidGameInterventionList.GamePackageInfoReader) u64 {
            return self._uid;
        }
        pub inline fn getCurrentMode(self: *const AndroidGameInterventionList.GamePackageInfoReader) u32 {
            return self._current_mode;
        }
        pub fn gameModeInfoCount(self: *const AndroidGameInterventionList.GamePackageInfoReader) usize {
            return self._game_mode_info_cnt;
        }
        pub fn gameModeInfoNext(self: *AndroidGameInterventionList.GamePackageInfoReader) ?AndroidGameInterventionList.GameModeInfoReader {
            if (self._game_mode_info_offset == null) return null;
            const current_offset = self._game_mode_info_offset.?;
            const result = self.buf.readBytes(current_offset) catch return null;
            const msg = AndroidGameInterventionList.GameModeInfoReader.init(result.value) catch return null;
            if (self._game_mode_info_last_offset != null and current_offset >= self._game_mode_info_last_offset.?) {
                self._game_mode_info_offset = null;
                return msg;
            }
            if (self._game_mode_info_last_offset == null) unreachable;
            var next_offset = current_offset + result.size;
            const max_offset = self._game_mode_info_last_offset.?;
            while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
                const tag = self.buf.readTagAt(next_offset) catch break;
                next_offset += tag.size;
                if (tag.number == AndroidGameInterventionList.GamePackageInfoWire.GAME_MODE_INFO_WIRE) {
                    self._game_mode_info_offset = next_offset;
                    return msg;
                } else {
                    next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
                }
            }
            self._game_mode_info_offset = null;
            return msg;
        }
    };
    // fields
    game_packages: ?[]const ?AndroidGameInterventionList.GamePackageInfo = null,
    parse_error: bool = false,
    read_error: bool = false,
    pub fn calcProtobufSize(self: *const AndroidGameInterventionList) usize {
        var res: usize = 0;
        if (self.game_packages) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionListWire.GAME_PACKAGES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.parse_error != false) {
            res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionListWire.PARSE_ERROR_WIRE) + gremlin.sizes.sizeBool(self.parse_error);
        }
        if (self.read_error != false) {
            res += gremlin.sizes.sizeWireNumber(AndroidGameInterventionListWire.READ_ERROR_WIRE) + gremlin.sizes.sizeBool(self.read_error);
        }
        return res;
    }
    pub fn encode(self: *const AndroidGameInterventionList, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidGameInterventionList, target: *gremlin.Writer) void {
        if (self.game_packages) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(AndroidGameInterventionListWire.GAME_PACKAGES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(AndroidGameInterventionListWire.GAME_PACKAGES_WIRE, 0);
                }
            }
        }
        if (self.parse_error != false) {
            target.appendBool(AndroidGameInterventionListWire.PARSE_ERROR_WIRE, self.parse_error);
        }
        if (self.read_error != false) {
            target.appendBool(AndroidGameInterventionListWire.READ_ERROR_WIRE, self.read_error);
        }
    }
};
pub const AndroidGameInterventionListReader = struct {
    buf: gremlin.Reader,
    _game_packages_offset: ?usize = null,
    _game_packages_last_offset: ?usize = null,
    _game_packages_cnt: usize = 0,
    _parse_error: bool = false,
    _read_error: bool = false,
    pub fn init(src: []const u8) gremlin.Error!AndroidGameInterventionListReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidGameInterventionListReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidGameInterventionListWire.GAME_PACKAGES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._game_packages_offset == null) {
                        res._game_packages_offset = offset - result.size;
                    }
                    res._game_packages_last_offset = offset;
                    res._game_packages_cnt += 1;
                },
                AndroidGameInterventionListWire.PARSE_ERROR_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._parse_error = result.value;
                },
                AndroidGameInterventionListWire.READ_ERROR_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._read_error = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn gamePackagesCount(self: *const AndroidGameInterventionListReader) usize {
        return self._game_packages_cnt;
    }
    pub fn gamePackagesNext(self: *AndroidGameInterventionListReader) ?AndroidGameInterventionList.GamePackageInfoReader {
        if (self._game_packages_offset == null) return null;
        const current_offset = self._game_packages_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = AndroidGameInterventionList.GamePackageInfoReader.init(result.value) catch return null;
        if (self._game_packages_last_offset != null and current_offset >= self._game_packages_last_offset.?) {
            self._game_packages_offset = null;
            return msg;
        }
        if (self._game_packages_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._game_packages_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == AndroidGameInterventionListWire.GAME_PACKAGES_WIRE) {
                self._game_packages_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._game_packages_offset = null;
        return msg;
    }
    pub inline fn getParseError(self: *const AndroidGameInterventionListReader) bool {
        return self._parse_error;
    }
    pub inline fn getReadError(self: *const AndroidGameInterventionListReader) bool {
        return self._read_error;
    }
};
