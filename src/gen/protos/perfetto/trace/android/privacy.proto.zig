// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// enums
pub const Destination = enum(i32) {
    DEST_LOCAL = 0,
    DEST_EXPLICIT = 100,
    DEST_AUTOMATIC = 200,
    DEST_UNSET = 255,
};
// structs
const PrivacyFlagsWire = struct {
    const DEST_WIRE: gremlin.ProtoWireNumber = 1;
    const PATTERNS_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const PrivacyFlags = struct {
    // fields
    dest: Destination = @enumFromInt(0),
    patterns: ?[]const ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const PrivacyFlags) usize {
        var res: usize = 0;
        if (self.dest != Destination.DEST_UNSET) {
            res += gremlin.sizes.sizeWireNumber(PrivacyFlagsWire.DEST_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.dest));
        }
        if (self.patterns) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(PrivacyFlagsWire.PATTERNS_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const PrivacyFlags, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PrivacyFlags, target: *gremlin.Writer) void {
        if (self.dest != Destination.DEST_UNSET) {
            target.appendInt32(PrivacyFlagsWire.DEST_WIRE, @intFromEnum(self.dest));
        }
        if (self.patterns) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(PrivacyFlagsWire.PATTERNS_WIRE, v);
                } else {
                    target.appendBytesTag(PrivacyFlagsWire.PATTERNS_WIRE, 0);
                }
            }
        }
    }
};
pub const PrivacyFlagsReader = struct {
    buf: gremlin.Reader,
    _dest: Destination = Destination.DEST_UNSET,
    _patterns_offset: ?usize = null,
    _patterns_last_offset: ?usize = null,
    _patterns_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!PrivacyFlagsReader {
        const buf = gremlin.Reader.init(src);
        var res = PrivacyFlagsReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PrivacyFlagsWire.DEST_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dest = @enumFromInt(result.value);
                },
                PrivacyFlagsWire.PATTERNS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._patterns_offset == null) {
                        res._patterns_offset = offset - result.size;
                    }
                    res._patterns_last_offset = offset;
                    res._patterns_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDest(self: *const PrivacyFlagsReader) Destination {
        return self._dest;
    }
    pub fn patternsCount(self: *const PrivacyFlagsReader) usize {
        return self._patterns_cnt;
    }
    pub fn patternsNext(self: *PrivacyFlagsReader) ?[]const u8 {
        if (self._patterns_offset == null) return null;
        const current_offset = self._patterns_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._patterns_last_offset != null and current_offset >= self._patterns_last_offset.?) {
            self._patterns_offset = null;
            return result.value;
        }
        if (self._patterns_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._patterns_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == PrivacyFlagsWire.PATTERNS_WIRE) {
                self._patterns_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._patterns_offset = null;
        return result.value;
    }
};
