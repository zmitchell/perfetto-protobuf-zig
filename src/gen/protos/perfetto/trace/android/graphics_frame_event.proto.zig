// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const GraphicsFrameEventWire = struct {
    const BUFFER_EVENT_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const GraphicsFrameEvent = struct {
    // nested enums
    pub const BufferEventType = enum(i32) {
        UNSPECIFIED = 0,
        DEQUEUE = 1,
        QUEUE = 2,
        POST = 3,
        ACQUIRE_FENCE = 4,
        LATCH = 5,
        HWC_COMPOSITION_QUEUED = 6,
        FALLBACK_COMPOSITION = 7,
        PRESENT_FENCE = 8,
        RELEASE_FENCE = 9,
        MODIFY = 10,
        DETACH = 11,
        ATTACH = 12,
        CANCEL = 13,
    };
    // nested structs
    const BufferEventWire = struct {
        const FRAME_NUMBER_WIRE: gremlin.ProtoWireNumber = 1;
        const TYPE_WIRE: gremlin.ProtoWireNumber = 2;
        const LAYER_NAME_WIRE: gremlin.ProtoWireNumber = 3;
        const DURATION_NS_WIRE: gremlin.ProtoWireNumber = 4;
        const BUFFER_ID_WIRE: gremlin.ProtoWireNumber = 5;
    };
    pub const BufferEvent = struct {
        // fields
        frame_number: u32 = 0,
        type: GraphicsFrameEvent.BufferEventType = @enumFromInt(0),
        layer_name: ?[]const u8 = null,
        duration_ns: u64 = 0,
        buffer_id: u32 = 0,
        pub fn calcProtobufSize(self: *const GraphicsFrameEvent.BufferEvent) usize {
            var res: usize = 0;
            if (self.frame_number != 0) {
                res += gremlin.sizes.sizeWireNumber(GraphicsFrameEvent.BufferEventWire.FRAME_NUMBER_WIRE) + gremlin.sizes.sizeU32(self.frame_number);
            }
            if (@intFromEnum(self.type) != 0) {
                res += gremlin.sizes.sizeWireNumber(GraphicsFrameEvent.BufferEventWire.TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.type));
            }
            if (self.layer_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(GraphicsFrameEvent.BufferEventWire.LAYER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.duration_ns != 0) {
                res += gremlin.sizes.sizeWireNumber(GraphicsFrameEvent.BufferEventWire.DURATION_NS_WIRE) + gremlin.sizes.sizeU64(self.duration_ns);
            }
            if (self.buffer_id != 0) {
                res += gremlin.sizes.sizeWireNumber(GraphicsFrameEvent.BufferEventWire.BUFFER_ID_WIRE) + gremlin.sizes.sizeU32(self.buffer_id);
            }
            return res;
        }
        pub fn encode(self: *const GraphicsFrameEvent.BufferEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GraphicsFrameEvent.BufferEvent, target: *gremlin.Writer) void {
            if (self.frame_number != 0) {
                target.appendUint32(GraphicsFrameEvent.BufferEventWire.FRAME_NUMBER_WIRE, self.frame_number);
            }
            if (@intFromEnum(self.type) != 0) {
                target.appendInt32(GraphicsFrameEvent.BufferEventWire.TYPE_WIRE, @intFromEnum(self.type));
            }
            if (self.layer_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(GraphicsFrameEvent.BufferEventWire.LAYER_NAME_WIRE, v);
                }
            }
            if (self.duration_ns != 0) {
                target.appendUint64(GraphicsFrameEvent.BufferEventWire.DURATION_NS_WIRE, self.duration_ns);
            }
            if (self.buffer_id != 0) {
                target.appendUint32(GraphicsFrameEvent.BufferEventWire.BUFFER_ID_WIRE, self.buffer_id);
            }
        }
    };
    pub const BufferEventReader = struct {
        buf: gremlin.Reader,
        _frame_number: u32 = 0,
        _type: GraphicsFrameEvent.BufferEventType = @enumFromInt(0),
        _layer_name: ?[]const u8 = null,
        _duration_ns: u64 = 0,
        _buffer_id: u32 = 0,
        pub fn init(src: []const u8) gremlin.Error!GraphicsFrameEvent.BufferEventReader {
            const buf = gremlin.Reader.init(src);
            var res = GraphicsFrameEvent.BufferEventReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GraphicsFrameEvent.BufferEventWire.FRAME_NUMBER_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._frame_number = result.value;
                    },
                    GraphicsFrameEvent.BufferEventWire.TYPE_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._type = @enumFromInt(result.value);
                    },
                    GraphicsFrameEvent.BufferEventWire.LAYER_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._layer_name = result.value;
                    },
                    GraphicsFrameEvent.BufferEventWire.DURATION_NS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._duration_ns = result.value;
                    },
                    GraphicsFrameEvent.BufferEventWire.BUFFER_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._buffer_id = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getFrameNumber(self: *const GraphicsFrameEvent.BufferEventReader) u32 {
            return self._frame_number;
        }
        pub inline fn getType(self: *const GraphicsFrameEvent.BufferEventReader) GraphicsFrameEvent.BufferEventType {
            return self._type;
        }
        pub inline fn getLayerName(self: *const GraphicsFrameEvent.BufferEventReader) []const u8 {
            return self._layer_name orelse &[_]u8{};
        }
        pub inline fn getDurationNs(self: *const GraphicsFrameEvent.BufferEventReader) u64 {
            return self._duration_ns;
        }
        pub inline fn getBufferId(self: *const GraphicsFrameEvent.BufferEventReader) u32 {
            return self._buffer_id;
        }
    };
    // fields
    buffer_event: ?GraphicsFrameEvent.BufferEvent = null,
    pub fn calcProtobufSize(self: *const GraphicsFrameEvent) usize {
        var res: usize = 0;
        if (self.buffer_event) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GraphicsFrameEventWire.BUFFER_EVENT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const GraphicsFrameEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GraphicsFrameEvent, target: *gremlin.Writer) void {
        if (self.buffer_event) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GraphicsFrameEventWire.BUFFER_EVENT_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const GraphicsFrameEventReader = struct {
    buf: gremlin.Reader,
    _buffer_event_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!GraphicsFrameEventReader {
        const buf = gremlin.Reader.init(src);
        var res = GraphicsFrameEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GraphicsFrameEventWire.BUFFER_EVENT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._buffer_event_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getBufferEvent(self: *const GraphicsFrameEventReader) gremlin.Error!GraphicsFrameEvent.BufferEventReader {
        if (self._buffer_event_buf) |buf| {
            return try GraphicsFrameEvent.BufferEventReader.init(buf);
        }
        return try GraphicsFrameEvent.BufferEventReader.init(&[_]u8{});
    }
};
