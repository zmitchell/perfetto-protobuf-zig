// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// enums
pub const BluetoothTracePacketType = enum(i32) {
    HCI_CMD = 1,
    HCI_EVT = 2,
    HCI_ACL_RX = 3,
    HCI_ACL_TX = 4,
    HCI_SCO_RX = 5,
    HCI_SCO_TX = 6,
    HCI_ISO_RX = 7,
    HCI_ISO_TX = 8,
    _PROTOBUF_UNKNOWN = 0,
};
// structs
const BluetoothTraceEventWire = struct {
    const PACKET_TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const COUNT_WIRE: gremlin.ProtoWireNumber = 2;
    const LENGTH_WIRE: gremlin.ProtoWireNumber = 3;
    const DURATION_WIRE: gremlin.ProtoWireNumber = 4;
    const OP_CODE_WIRE: gremlin.ProtoWireNumber = 5;
    const EVENT_CODE_WIRE: gremlin.ProtoWireNumber = 6;
    const SUBEVENT_CODE_WIRE: gremlin.ProtoWireNumber = 7;
    const CONNECTION_HANDLE_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const BluetoothTraceEvent = struct {
    // fields
    packet_type: BluetoothTracePacketType = @enumFromInt(0),
    count: u32 = 0,
    length: u32 = 0,
    duration: u32 = 0,
    op_code: u32 = 0,
    event_code: u32 = 0,
    subevent_code: u32 = 0,
    connection_handle: u32 = 0,
    pub fn calcProtobufSize(self: *const BluetoothTraceEvent) usize {
        var res: usize = 0;
        if (@intFromEnum(self.packet_type) != 0) {
            res += gremlin.sizes.sizeWireNumber(BluetoothTraceEventWire.PACKET_TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.packet_type));
        }
        if (self.count != 0) {
            res += gremlin.sizes.sizeWireNumber(BluetoothTraceEventWire.COUNT_WIRE) + gremlin.sizes.sizeU32(self.count);
        }
        if (self.length != 0) {
            res += gremlin.sizes.sizeWireNumber(BluetoothTraceEventWire.LENGTH_WIRE) + gremlin.sizes.sizeU32(self.length);
        }
        if (self.duration != 0) {
            res += gremlin.sizes.sizeWireNumber(BluetoothTraceEventWire.DURATION_WIRE) + gremlin.sizes.sizeU32(self.duration);
        }
        if (self.op_code != 0) {
            res += gremlin.sizes.sizeWireNumber(BluetoothTraceEventWire.OP_CODE_WIRE) + gremlin.sizes.sizeU32(self.op_code);
        }
        if (self.event_code != 0) {
            res += gremlin.sizes.sizeWireNumber(BluetoothTraceEventWire.EVENT_CODE_WIRE) + gremlin.sizes.sizeU32(self.event_code);
        }
        if (self.subevent_code != 0) {
            res += gremlin.sizes.sizeWireNumber(BluetoothTraceEventWire.SUBEVENT_CODE_WIRE) + gremlin.sizes.sizeU32(self.subevent_code);
        }
        if (self.connection_handle != 0) {
            res += gremlin.sizes.sizeWireNumber(BluetoothTraceEventWire.CONNECTION_HANDLE_WIRE) + gremlin.sizes.sizeU32(self.connection_handle);
        }
        return res;
    }
    pub fn encode(self: *const BluetoothTraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BluetoothTraceEvent, target: *gremlin.Writer) void {
        if (@intFromEnum(self.packet_type) != 0) {
            target.appendInt32(BluetoothTraceEventWire.PACKET_TYPE_WIRE, @intFromEnum(self.packet_type));
        }
        if (self.count != 0) {
            target.appendUint32(BluetoothTraceEventWire.COUNT_WIRE, self.count);
        }
        if (self.length != 0) {
            target.appendUint32(BluetoothTraceEventWire.LENGTH_WIRE, self.length);
        }
        if (self.duration != 0) {
            target.appendUint32(BluetoothTraceEventWire.DURATION_WIRE, self.duration);
        }
        if (self.op_code != 0) {
            target.appendUint32(BluetoothTraceEventWire.OP_CODE_WIRE, self.op_code);
        }
        if (self.event_code != 0) {
            target.appendUint32(BluetoothTraceEventWire.EVENT_CODE_WIRE, self.event_code);
        }
        if (self.subevent_code != 0) {
            target.appendUint32(BluetoothTraceEventWire.SUBEVENT_CODE_WIRE, self.subevent_code);
        }
        if (self.connection_handle != 0) {
            target.appendUint32(BluetoothTraceEventWire.CONNECTION_HANDLE_WIRE, self.connection_handle);
        }
    }
};
pub const BluetoothTraceEventReader = struct {
    buf: gremlin.Reader,
    _packet_type: BluetoothTracePacketType = @enumFromInt(0),
    _count: u32 = 0,
    _length: u32 = 0,
    _duration: u32 = 0,
    _op_code: u32 = 0,
    _event_code: u32 = 0,
    _subevent_code: u32 = 0,
    _connection_handle: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!BluetoothTraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BluetoothTraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BluetoothTraceEventWire.PACKET_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._packet_type = @enumFromInt(result.value);
                },
                BluetoothTraceEventWire.COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._count = result.value;
                },
                BluetoothTraceEventWire.LENGTH_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._length = result.value;
                },
                BluetoothTraceEventWire.DURATION_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._duration = result.value;
                },
                BluetoothTraceEventWire.OP_CODE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._op_code = result.value;
                },
                BluetoothTraceEventWire.EVENT_CODE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._event_code = result.value;
                },
                BluetoothTraceEventWire.SUBEVENT_CODE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._subevent_code = result.value;
                },
                BluetoothTraceEventWire.CONNECTION_HANDLE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._connection_handle = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPacketType(self: *const BluetoothTraceEventReader) BluetoothTracePacketType {
        return self._packet_type;
    }
    pub inline fn getCount(self: *const BluetoothTraceEventReader) u32 {
        return self._count;
    }
    pub inline fn getLength(self: *const BluetoothTraceEventReader) u32 {
        return self._length;
    }
    pub inline fn getDuration(self: *const BluetoothTraceEventReader) u32 {
        return self._duration;
    }
    pub inline fn getOpCode(self: *const BluetoothTraceEventReader) u32 {
        return self._op_code;
    }
    pub inline fn getEventCode(self: *const BluetoothTraceEventReader) u32 {
        return self._event_code;
    }
    pub inline fn getSubeventCode(self: *const BluetoothTraceEventReader) u32 {
        return self._subevent_code;
    }
    pub inline fn getConnectionHandle(self: *const BluetoothTraceEventReader) u32 {
        return self._connection_handle;
    }
};
