// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const track_event = @import("src/gen/protos/perfetto/trace/track_event/track_event.proto.zig");
const debug_annotation = @import("src/gen/protos/perfetto/trace/track_event/debug_annotation.proto.zig");
const task_execution = @import("src/gen/protos/perfetto/trace/track_event/task_execution.proto.zig");
const log_message = @import("src/gen/protos/perfetto/trace/track_event/log_message.proto.zig");
const chrome_compositor_scheduler_state = @import("src/gen/protos/perfetto/trace/track_event/chrome_compositor_scheduler_state.proto.zig");
const chrome_user_event = @import("src/gen/protos/perfetto/trace/track_event/chrome_user_event.proto.zig");
const chrome_keyed_service = @import("src/gen/protos/perfetto/trace/track_event/chrome_keyed_service.proto.zig");
const chrome_legacy_ipc = @import("src/gen/protos/perfetto/trace/track_event/chrome_legacy_ipc.proto.zig");
const chrome_histogram_sample = @import("src/gen/protos/perfetto/trace/track_event/chrome_histogram_sample.proto.zig");
const chrome_latency_info = @import("src/gen/protos/perfetto/trace/track_event/chrome_latency_info.proto.zig");
const chrome_frame_reporter = @import("src/gen/protos/perfetto/trace/track_event/chrome_frame_reporter.proto.zig");
const chrome_application_state_info = @import("src/gen/protos/perfetto/trace/track_event/chrome_application_state_info.proto.zig");
const chrome_renderer_scheduler_state = @import("src/gen/protos/perfetto/trace/track_event/chrome_renderer_scheduler_state.proto.zig");
const chrome_window_handle_event_info = @import("src/gen/protos/perfetto/trace/track_event/chrome_window_handle_event_info.proto.zig");
const chrome_content_settings_event_info = @import("src/gen/protos/perfetto/trace/track_event/chrome_content_settings_event_info.proto.zig");
const chrome_active_processes = @import("src/gen/protos/perfetto/trace/track_event/chrome_active_processes.proto.zig");
const screenshot = @import("src/gen/protos/perfetto/trace/track_event/screenshot.proto.zig");
const chrome_message_pump = @import("src/gen/protos/perfetto/trace/track_event/chrome_message_pump.proto.zig");
const chrome_mojo_event_info = @import("src/gen/protos/perfetto/trace/track_event/chrome_mojo_event_info.proto.zig");
const source_location = @import("src/gen/protos/perfetto/trace/track_event/source_location.proto.zig");
// structs
const AndroidMessageQueueWire = struct {
    const SENDING_THREAD_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const RECEIVING_THREAD_NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const MESSAGE_CODE_WIRE: gremlin.ProtoWireNumber = 3;
    const MESSAGE_DELAY_MS_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const AndroidMessageQueue = struct {
    // fields
    sending_thread_name: ?[]const u8 = null,
    receiving_thread_name: ?[]const u8 = null,
    message_code: i32 = 0,
    message_delay_ms: u64 = 0,
    pub fn calcProtobufSize(self: *const AndroidMessageQueue) usize {
        var res: usize = 0;
        if (self.sending_thread_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidMessageQueueWire.SENDING_THREAD_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.receiving_thread_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidMessageQueueWire.RECEIVING_THREAD_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.message_code != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidMessageQueueWire.MESSAGE_CODE_WIRE) + gremlin.sizes.sizeI32(self.message_code);
        }
        if (self.message_delay_ms != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidMessageQueueWire.MESSAGE_DELAY_MS_WIRE) + gremlin.sizes.sizeU64(self.message_delay_ms);
        }
        return res;
    }
    pub fn encode(self: *const AndroidMessageQueue, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidMessageQueue, target: *gremlin.Writer) void {
        if (self.sending_thread_name) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidMessageQueueWire.SENDING_THREAD_NAME_WIRE, v);
            }
        }
        if (self.receiving_thread_name) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidMessageQueueWire.RECEIVING_THREAD_NAME_WIRE, v);
            }
        }
        if (self.message_code != 0) {
            target.appendInt32(AndroidMessageQueueWire.MESSAGE_CODE_WIRE, self.message_code);
        }
        if (self.message_delay_ms != 0) {
            target.appendUint64(AndroidMessageQueueWire.MESSAGE_DELAY_MS_WIRE, self.message_delay_ms);
        }
    }
};
pub const AndroidMessageQueueReader = struct {
    buf: gremlin.Reader,
    _sending_thread_name: ?[]const u8 = null,
    _receiving_thread_name: ?[]const u8 = null,
    _message_code: i32 = 0,
    _message_delay_ms: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidMessageQueueReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidMessageQueueReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidMessageQueueWire.SENDING_THREAD_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._sending_thread_name = result.value;
                },
                AndroidMessageQueueWire.RECEIVING_THREAD_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._receiving_thread_name = result.value;
                },
                AndroidMessageQueueWire.MESSAGE_CODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._message_code = result.value;
                },
                AndroidMessageQueueWire.MESSAGE_DELAY_MS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._message_delay_ms = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getSendingThreadName(self: *const AndroidMessageQueueReader) []const u8 {
        return self._sending_thread_name orelse &[_]u8{};
    }
    pub inline fn getReceivingThreadName(self: *const AndroidMessageQueueReader) []const u8 {
        return self._receiving_thread_name orelse &[_]u8{};
    }
    pub inline fn getMessageCode(self: *const AndroidMessageQueueReader) i32 {
        return self._message_code;
    }
    pub inline fn getMessageDelayMs(self: *const AndroidMessageQueueReader) u64 {
        return self._message_delay_ms;
    }
};
const AndroidJobSchedulerJobWire = struct {
    const JOB_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const SOURCE_UID_WIRE: gremlin.ProtoWireNumber = 2;
    const PROXY_UID_WIRE: gremlin.ProtoWireNumber = 3;
    const STATE_WIRE: gremlin.ProtoWireNumber = 4;
    const STANDBY_BUCKET_WIRE: gremlin.ProtoWireNumber = 5;
    const REQUESTED_PRIORITY_WIRE: gremlin.ProtoWireNumber = 6;
    const EFFECTIVE_PRIORITY_WIRE: gremlin.ProtoWireNumber = 7;
    const NUM_PREVIOUS_ATTEMPTS_WIRE: gremlin.ProtoWireNumber = 8;
    const DEADLINE_MS_WIRE: gremlin.ProtoWireNumber = 9;
    const DELAY_MS_WIRE: gremlin.ProtoWireNumber = 10;
    const JOB_START_LATENCY_MS_WIRE: gremlin.ProtoWireNumber = 11;
    const NUM_UNCOMPLETED_WORK_ITEMS_WIRE: gremlin.ProtoWireNumber = 12;
    const PROC_STATE_WIRE: gremlin.ProtoWireNumber = 13;
    const PERIODIC_JOB_INTERVAL_MS_WIRE: gremlin.ProtoWireNumber = 14;
    const PERIODIC_JOB_FLEX_INTERVAL_MS_WIRE: gremlin.ProtoWireNumber = 15;
    const NUM_RESCHEDULES_DUE_TO_ABANDONMENT_WIRE: gremlin.ProtoWireNumber = 16;
    const BACK_OFF_POLICY_TYPE_WIRE: gremlin.ProtoWireNumber = 17;
    const INTERNAL_STOP_REASON_WIRE: gremlin.ProtoWireNumber = 18;
    const PUBLIC_STOP_REASON_WIRE: gremlin.ProtoWireNumber = 19;
    const JOB_STATE_FLAGS_WIRE: gremlin.ProtoWireNumber = 20;
};
pub const AndroidJobSchedulerJob = struct {
    // fields
    job_id: i32 = 0,
    source_uid: i32 = 0,
    proxy_uid: i32 = 0,
    state: i32 = 0,
    standby_bucket: i32 = 0,
    requested_priority: i32 = 0,
    effective_priority: i32 = 0,
    num_previous_attempts: i32 = 0,
    deadline_ms: i64 = 0,
    delay_ms: i64 = 0,
    job_start_latency_ms: i64 = 0,
    num_uncompleted_work_items: i32 = 0,
    proc_state: i32 = 0,
    periodic_job_interval_ms: i64 = 0,
    periodic_job_flex_interval_ms: i64 = 0,
    num_reschedules_due_to_abandonment: i32 = 0,
    back_off_policy_type: i32 = 0,
    internal_stop_reason: i32 = 0,
    public_stop_reason: i32 = 0,
    job_state_flags: i64 = 0,
    pub fn calcProtobufSize(self: *const AndroidJobSchedulerJob) usize {
        var res: usize = 0;
        if (self.job_id != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.JOB_ID_WIRE) + gremlin.sizes.sizeI32(self.job_id);
        }
        if (self.source_uid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.SOURCE_UID_WIRE) + gremlin.sizes.sizeI32(self.source_uid);
        }
        if (self.proxy_uid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.PROXY_UID_WIRE) + gremlin.sizes.sizeI32(self.proxy_uid);
        }
        if (self.state != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.STATE_WIRE) + gremlin.sizes.sizeI32(self.state);
        }
        if (self.standby_bucket != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.STANDBY_BUCKET_WIRE) + gremlin.sizes.sizeI32(self.standby_bucket);
        }
        if (self.requested_priority != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.REQUESTED_PRIORITY_WIRE) + gremlin.sizes.sizeI32(self.requested_priority);
        }
        if (self.effective_priority != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.EFFECTIVE_PRIORITY_WIRE) + gremlin.sizes.sizeI32(self.effective_priority);
        }
        if (self.num_previous_attempts != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.NUM_PREVIOUS_ATTEMPTS_WIRE) + gremlin.sizes.sizeI32(self.num_previous_attempts);
        }
        if (self.deadline_ms != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.DEADLINE_MS_WIRE) + gremlin.sizes.sizeI64(self.deadline_ms);
        }
        if (self.delay_ms != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.DELAY_MS_WIRE) + gremlin.sizes.sizeI64(self.delay_ms);
        }
        if (self.job_start_latency_ms != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.JOB_START_LATENCY_MS_WIRE) + gremlin.sizes.sizeI64(self.job_start_latency_ms);
        }
        if (self.num_uncompleted_work_items != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.NUM_UNCOMPLETED_WORK_ITEMS_WIRE) + gremlin.sizes.sizeI32(self.num_uncompleted_work_items);
        }
        if (self.proc_state != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.PROC_STATE_WIRE) + gremlin.sizes.sizeI32(self.proc_state);
        }
        if (self.periodic_job_interval_ms != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.PERIODIC_JOB_INTERVAL_MS_WIRE) + gremlin.sizes.sizeI64(self.periodic_job_interval_ms);
        }
        if (self.periodic_job_flex_interval_ms != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.PERIODIC_JOB_FLEX_INTERVAL_MS_WIRE) + gremlin.sizes.sizeI64(self.periodic_job_flex_interval_ms);
        }
        if (self.num_reschedules_due_to_abandonment != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.NUM_RESCHEDULES_DUE_TO_ABANDONMENT_WIRE) + gremlin.sizes.sizeI32(self.num_reschedules_due_to_abandonment);
        }
        if (self.back_off_policy_type != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.BACK_OFF_POLICY_TYPE_WIRE) + gremlin.sizes.sizeI32(self.back_off_policy_type);
        }
        if (self.internal_stop_reason != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.INTERNAL_STOP_REASON_WIRE) + gremlin.sizes.sizeI32(self.internal_stop_reason);
        }
        if (self.public_stop_reason != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.PUBLIC_STOP_REASON_WIRE) + gremlin.sizes.sizeI32(self.public_stop_reason);
        }
        if (self.job_state_flags != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidJobSchedulerJobWire.JOB_STATE_FLAGS_WIRE) + gremlin.sizes.sizeI64(self.job_state_flags);
        }
        return res;
    }
    pub fn encode(self: *const AndroidJobSchedulerJob, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidJobSchedulerJob, target: *gremlin.Writer) void {
        if (self.job_id != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.JOB_ID_WIRE, self.job_id);
        }
        if (self.source_uid != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.SOURCE_UID_WIRE, self.source_uid);
        }
        if (self.proxy_uid != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.PROXY_UID_WIRE, self.proxy_uid);
        }
        if (self.state != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.STATE_WIRE, self.state);
        }
        if (self.standby_bucket != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.STANDBY_BUCKET_WIRE, self.standby_bucket);
        }
        if (self.requested_priority != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.REQUESTED_PRIORITY_WIRE, self.requested_priority);
        }
        if (self.effective_priority != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.EFFECTIVE_PRIORITY_WIRE, self.effective_priority);
        }
        if (self.num_previous_attempts != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.NUM_PREVIOUS_ATTEMPTS_WIRE, self.num_previous_attempts);
        }
        if (self.deadline_ms != 0) {
            target.appendInt64(AndroidJobSchedulerJobWire.DEADLINE_MS_WIRE, self.deadline_ms);
        }
        if (self.delay_ms != 0) {
            target.appendInt64(AndroidJobSchedulerJobWire.DELAY_MS_WIRE, self.delay_ms);
        }
        if (self.job_start_latency_ms != 0) {
            target.appendInt64(AndroidJobSchedulerJobWire.JOB_START_LATENCY_MS_WIRE, self.job_start_latency_ms);
        }
        if (self.num_uncompleted_work_items != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.NUM_UNCOMPLETED_WORK_ITEMS_WIRE, self.num_uncompleted_work_items);
        }
        if (self.proc_state != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.PROC_STATE_WIRE, self.proc_state);
        }
        if (self.periodic_job_interval_ms != 0) {
            target.appendInt64(AndroidJobSchedulerJobWire.PERIODIC_JOB_INTERVAL_MS_WIRE, self.periodic_job_interval_ms);
        }
        if (self.periodic_job_flex_interval_ms != 0) {
            target.appendInt64(AndroidJobSchedulerJobWire.PERIODIC_JOB_FLEX_INTERVAL_MS_WIRE, self.periodic_job_flex_interval_ms);
        }
        if (self.num_reschedules_due_to_abandonment != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.NUM_RESCHEDULES_DUE_TO_ABANDONMENT_WIRE, self.num_reschedules_due_to_abandonment);
        }
        if (self.back_off_policy_type != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.BACK_OFF_POLICY_TYPE_WIRE, self.back_off_policy_type);
        }
        if (self.internal_stop_reason != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.INTERNAL_STOP_REASON_WIRE, self.internal_stop_reason);
        }
        if (self.public_stop_reason != 0) {
            target.appendInt32(AndroidJobSchedulerJobWire.PUBLIC_STOP_REASON_WIRE, self.public_stop_reason);
        }
        if (self.job_state_flags != 0) {
            target.appendInt64(AndroidJobSchedulerJobWire.JOB_STATE_FLAGS_WIRE, self.job_state_flags);
        }
    }
};
pub const AndroidJobSchedulerJobReader = struct {
    buf: gremlin.Reader,
    _job_id: i32 = 0,
    _source_uid: i32 = 0,
    _proxy_uid: i32 = 0,
    _state: i32 = 0,
    _standby_bucket: i32 = 0,
    _requested_priority: i32 = 0,
    _effective_priority: i32 = 0,
    _num_previous_attempts: i32 = 0,
    _deadline_ms: i64 = 0,
    _delay_ms: i64 = 0,
    _job_start_latency_ms: i64 = 0,
    _num_uncompleted_work_items: i32 = 0,
    _proc_state: i32 = 0,
    _periodic_job_interval_ms: i64 = 0,
    _periodic_job_flex_interval_ms: i64 = 0,
    _num_reschedules_due_to_abandonment: i32 = 0,
    _back_off_policy_type: i32 = 0,
    _internal_stop_reason: i32 = 0,
    _public_stop_reason: i32 = 0,
    _job_state_flags: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidJobSchedulerJobReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidJobSchedulerJobReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidJobSchedulerJobWire.JOB_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._job_id = result.value;
                },
                AndroidJobSchedulerJobWire.SOURCE_UID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._source_uid = result.value;
                },
                AndroidJobSchedulerJobWire.PROXY_UID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._proxy_uid = result.value;
                },
                AndroidJobSchedulerJobWire.STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._state = result.value;
                },
                AndroidJobSchedulerJobWire.STANDBY_BUCKET_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._standby_bucket = result.value;
                },
                AndroidJobSchedulerJobWire.REQUESTED_PRIORITY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._requested_priority = result.value;
                },
                AndroidJobSchedulerJobWire.EFFECTIVE_PRIORITY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._effective_priority = result.value;
                },
                AndroidJobSchedulerJobWire.NUM_PREVIOUS_ATTEMPTS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._num_previous_attempts = result.value;
                },
                AndroidJobSchedulerJobWire.DEADLINE_MS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._deadline_ms = result.value;
                },
                AndroidJobSchedulerJobWire.DELAY_MS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._delay_ms = result.value;
                },
                AndroidJobSchedulerJobWire.JOB_START_LATENCY_MS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._job_start_latency_ms = result.value;
                },
                AndroidJobSchedulerJobWire.NUM_UNCOMPLETED_WORK_ITEMS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._num_uncompleted_work_items = result.value;
                },
                AndroidJobSchedulerJobWire.PROC_STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._proc_state = result.value;
                },
                AndroidJobSchedulerJobWire.PERIODIC_JOB_INTERVAL_MS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._periodic_job_interval_ms = result.value;
                },
                AndroidJobSchedulerJobWire.PERIODIC_JOB_FLEX_INTERVAL_MS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._periodic_job_flex_interval_ms = result.value;
                },
                AndroidJobSchedulerJobWire.NUM_RESCHEDULES_DUE_TO_ABANDONMENT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._num_reschedules_due_to_abandonment = result.value;
                },
                AndroidJobSchedulerJobWire.BACK_OFF_POLICY_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._back_off_policy_type = result.value;
                },
                AndroidJobSchedulerJobWire.INTERNAL_STOP_REASON_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._internal_stop_reason = result.value;
                },
                AndroidJobSchedulerJobWire.PUBLIC_STOP_REASON_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._public_stop_reason = result.value;
                },
                AndroidJobSchedulerJobWire.JOB_STATE_FLAGS_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._job_state_flags = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getJobId(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._job_id;
    }
    pub inline fn getSourceUid(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._source_uid;
    }
    pub inline fn getProxyUid(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._proxy_uid;
    }
    pub inline fn getState(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._state;
    }
    pub inline fn getStandbyBucket(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._standby_bucket;
    }
    pub inline fn getRequestedPriority(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._requested_priority;
    }
    pub inline fn getEffectivePriority(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._effective_priority;
    }
    pub inline fn getNumPreviousAttempts(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._num_previous_attempts;
    }
    pub inline fn getDeadlineMs(self: *const AndroidJobSchedulerJobReader) i64 {
        return self._deadline_ms;
    }
    pub inline fn getDelayMs(self: *const AndroidJobSchedulerJobReader) i64 {
        return self._delay_ms;
    }
    pub inline fn getJobStartLatencyMs(self: *const AndroidJobSchedulerJobReader) i64 {
        return self._job_start_latency_ms;
    }
    pub inline fn getNumUncompletedWorkItems(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._num_uncompleted_work_items;
    }
    pub inline fn getProcState(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._proc_state;
    }
    pub inline fn getPeriodicJobIntervalMs(self: *const AndroidJobSchedulerJobReader) i64 {
        return self._periodic_job_interval_ms;
    }
    pub inline fn getPeriodicJobFlexIntervalMs(self: *const AndroidJobSchedulerJobReader) i64 {
        return self._periodic_job_flex_interval_ms;
    }
    pub inline fn getNumReschedulesDueToAbandonment(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._num_reschedules_due_to_abandonment;
    }
    pub inline fn getBackOffPolicyType(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._back_off_policy_type;
    }
    pub inline fn getInternalStopReason(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._internal_stop_reason;
    }
    pub inline fn getPublicStopReason(self: *const AndroidJobSchedulerJobReader) i32 {
        return self._public_stop_reason;
    }
    pub inline fn getJobStateFlags(self: *const AndroidJobSchedulerJobReader) i64 {
        return self._job_state_flags;
    }
};
const AndroidBitmapWire = struct {
    const SIZE_WIRE: gremlin.ProtoWireNumber = 1;
    const WIDTH_WIRE: gremlin.ProtoWireNumber = 2;
    const HEIGHT_WIRE: gremlin.ProtoWireNumber = 3;
    const DENSITY_WIRE: gremlin.ProtoWireNumber = 4;
    const CONFIG_WIRE: gremlin.ProtoWireNumber = 5;
    const MUTABLE_PIXELS_WIRE: gremlin.ProtoWireNumber = 6;
    const PIXEL_STORAGE_TYPE_WIRE: gremlin.ProtoWireNumber = 7;
    const ID_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const AndroidBitmap = struct {
    // fields
    size: i64 = 0,
    width: i32 = 0,
    height: i32 = 0,
    density: i32 = 0,
    config: i32 = 0,
    mutable_pixels: i32 = 0,
    pixel_storage_type: i32 = 0,
    id: i64 = 0,
    pub fn calcProtobufSize(self: *const AndroidBitmap) usize {
        var res: usize = 0;
        if (self.size != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidBitmapWire.SIZE_WIRE) + gremlin.sizes.sizeI64(self.size);
        }
        if (self.width != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidBitmapWire.WIDTH_WIRE) + gremlin.sizes.sizeI32(self.width);
        }
        if (self.height != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidBitmapWire.HEIGHT_WIRE) + gremlin.sizes.sizeI32(self.height);
        }
        if (self.density != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidBitmapWire.DENSITY_WIRE) + gremlin.sizes.sizeI32(self.density);
        }
        if (self.config != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidBitmapWire.CONFIG_WIRE) + gremlin.sizes.sizeI32(self.config);
        }
        if (self.mutable_pixels != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidBitmapWire.MUTABLE_PIXELS_WIRE) + gremlin.sizes.sizeI32(self.mutable_pixels);
        }
        if (self.pixel_storage_type != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidBitmapWire.PIXEL_STORAGE_TYPE_WIRE) + gremlin.sizes.sizeI32(self.pixel_storage_type);
        }
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidBitmapWire.ID_WIRE) + gremlin.sizes.sizeI64(self.id);
        }
        return res;
    }
    pub fn encode(self: *const AndroidBitmap, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidBitmap, target: *gremlin.Writer) void {
        if (self.size != 0) {
            target.appendInt64(AndroidBitmapWire.SIZE_WIRE, self.size);
        }
        if (self.width != 0) {
            target.appendInt32(AndroidBitmapWire.WIDTH_WIRE, self.width);
        }
        if (self.height != 0) {
            target.appendInt32(AndroidBitmapWire.HEIGHT_WIRE, self.height);
        }
        if (self.density != 0) {
            target.appendInt32(AndroidBitmapWire.DENSITY_WIRE, self.density);
        }
        if (self.config != 0) {
            target.appendInt32(AndroidBitmapWire.CONFIG_WIRE, self.config);
        }
        if (self.mutable_pixels != 0) {
            target.appendInt32(AndroidBitmapWire.MUTABLE_PIXELS_WIRE, self.mutable_pixels);
        }
        if (self.pixel_storage_type != 0) {
            target.appendInt32(AndroidBitmapWire.PIXEL_STORAGE_TYPE_WIRE, self.pixel_storage_type);
        }
        if (self.id != 0) {
            target.appendInt64(AndroidBitmapWire.ID_WIRE, self.id);
        }
    }
};
pub const AndroidBitmapReader = struct {
    buf: gremlin.Reader,
    _size: i64 = 0,
    _width: i32 = 0,
    _height: i32 = 0,
    _density: i32 = 0,
    _config: i32 = 0,
    _mutable_pixels: i32 = 0,
    _pixel_storage_type: i32 = 0,
    _id: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidBitmapReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidBitmapReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidBitmapWire.SIZE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._size = result.value;
                },
                AndroidBitmapWire.WIDTH_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._width = result.value;
                },
                AndroidBitmapWire.HEIGHT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._height = result.value;
                },
                AndroidBitmapWire.DENSITY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._density = result.value;
                },
                AndroidBitmapWire.CONFIG_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._config = result.value;
                },
                AndroidBitmapWire.MUTABLE_PIXELS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._mutable_pixels = result.value;
                },
                AndroidBitmapWire.PIXEL_STORAGE_TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pixel_storage_type = result.value;
                },
                AndroidBitmapWire.ID_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getSize(self: *const AndroidBitmapReader) i64 {
        return self._size;
    }
    pub inline fn getWidth(self: *const AndroidBitmapReader) i32 {
        return self._width;
    }
    pub inline fn getHeight(self: *const AndroidBitmapReader) i32 {
        return self._height;
    }
    pub inline fn getDensity(self: *const AndroidBitmapReader) i32 {
        return self._density;
    }
    pub inline fn getConfig(self: *const AndroidBitmapReader) i32 {
        return self._config;
    }
    pub inline fn getMutablePixels(self: *const AndroidBitmapReader) i32 {
        return self._mutable_pixels;
    }
    pub inline fn getPixelStorageType(self: *const AndroidBitmapReader) i32 {
        return self._pixel_storage_type;
    }
    pub inline fn getId(self: *const AndroidBitmapReader) i64 {
        return self._id;
    }
};
const AndroidTrackEventWire = struct {
    const CATEGORY_IIDS_WIRE: gremlin.ProtoWireNumber = 3;
    const CATEGORIES_WIRE: gremlin.ProtoWireNumber = 22;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 9;
    const TRACK_UUID_WIRE: gremlin.ProtoWireNumber = 11;
    const EXTRA_COUNTER_TRACK_UUIDS_WIRE: gremlin.ProtoWireNumber = 31;
    const EXTRA_COUNTER_VALUES_WIRE: gremlin.ProtoWireNumber = 12;
    const EXTRA_DOUBLE_COUNTER_TRACK_UUIDS_WIRE: gremlin.ProtoWireNumber = 45;
    const EXTRA_DOUBLE_COUNTER_VALUES_WIRE: gremlin.ProtoWireNumber = 46;
    const FLOW_IDS_OLD_WIRE: gremlin.ProtoWireNumber = 36;
    const FLOW_IDS_WIRE: gremlin.ProtoWireNumber = 47;
    const TERMINATING_FLOW_IDS_OLD_WIRE: gremlin.ProtoWireNumber = 42;
    const TERMINATING_FLOW_IDS_WIRE: gremlin.ProtoWireNumber = 48;
    const DEBUG_ANNOTATIONS_WIRE: gremlin.ProtoWireNumber = 4;
    const TASK_EXECUTION_WIRE: gremlin.ProtoWireNumber = 5;
    const LOG_MESSAGE_WIRE: gremlin.ProtoWireNumber = 21;
    const CC_SCHEDULER_STATE_WIRE: gremlin.ProtoWireNumber = 24;
    const CHROME_USER_EVENT_WIRE: gremlin.ProtoWireNumber = 25;
    const CHROME_KEYED_SERVICE_WIRE: gremlin.ProtoWireNumber = 26;
    const CHROME_LEGACY_IPC_WIRE: gremlin.ProtoWireNumber = 27;
    const CHROME_HISTOGRAM_SAMPLE_WIRE: gremlin.ProtoWireNumber = 28;
    const CHROME_LATENCY_INFO_WIRE: gremlin.ProtoWireNumber = 29;
    const CHROME_FRAME_REPORTER_WIRE: gremlin.ProtoWireNumber = 32;
    const CHROME_APPLICATION_STATE_INFO_WIRE: gremlin.ProtoWireNumber = 39;
    const CHROME_RENDERER_SCHEDULER_STATE_WIRE: gremlin.ProtoWireNumber = 40;
    const CHROME_WINDOW_HANDLE_EVENT_INFO_WIRE: gremlin.ProtoWireNumber = 41;
    const CHROME_CONTENT_SETTINGS_EVENT_INFO_WIRE: gremlin.ProtoWireNumber = 43;
    const CHROME_ACTIVE_PROCESSES_WIRE: gremlin.ProtoWireNumber = 49;
    const SCREENSHOT_WIRE: gremlin.ProtoWireNumber = 50;
    const CHROME_MESSAGE_PUMP_WIRE: gremlin.ProtoWireNumber = 35;
    const CHROME_MOJO_EVENT_INFO_WIRE: gremlin.ProtoWireNumber = 38;
    const LEGACY_EVENT_WIRE: gremlin.ProtoWireNumber = 6;
    const NAME_IID_WIRE: gremlin.ProtoWireNumber = 10;
    const NAME_WIRE: gremlin.ProtoWireNumber = 23;
    const COUNTER_VALUE_WIRE: gremlin.ProtoWireNumber = 30;
    const DOUBLE_COUNTER_VALUE_WIRE: gremlin.ProtoWireNumber = 44;
    const CORRELATION_ID_WIRE: gremlin.ProtoWireNumber = 52;
    const CORRELATION_ID_STR_WIRE: gremlin.ProtoWireNumber = 53;
    const CORRELATION_ID_STR_IID_WIRE: gremlin.ProtoWireNumber = 54;
    const CALLSTACK_WIRE: gremlin.ProtoWireNumber = 55;
    const CALLSTACK_IID_WIRE: gremlin.ProtoWireNumber = 56;
    const SOURCE_LOCATION_WIRE: gremlin.ProtoWireNumber = 33;
    const SOURCE_LOCATION_IID_WIRE: gremlin.ProtoWireNumber = 34;
    const TIMESTAMP_DELTA_US_WIRE: gremlin.ProtoWireNumber = 1;
    const TIMESTAMP_ABSOLUTE_US_WIRE: gremlin.ProtoWireNumber = 16;
    const THREAD_TIME_DELTA_US_WIRE: gremlin.ProtoWireNumber = 2;
    const THREAD_TIME_ABSOLUTE_US_WIRE: gremlin.ProtoWireNumber = 17;
    const THREAD_INSTRUCTION_COUNT_DELTA_WIRE: gremlin.ProtoWireNumber = 8;
    const THREAD_INSTRUCTION_COUNT_ABSOLUTE_WIRE: gremlin.ProtoWireNumber = 20;
};
pub const AndroidTrackEvent = struct {
    // fields
    category_iids: ?[]const u64 = null,
    categories: ?[]const ?[]const u8 = null,
    type: track_event.TrackEvent.Type = @enumFromInt(0),
    track_uuid: u64 = 0,
    extra_counter_track_uuids: ?[]const u64 = null,
    extra_counter_values: ?[]const i64 = null,
    extra_double_counter_track_uuids: ?[]const u64 = null,
    extra_double_counter_values: ?[]const f64 = null,
    flow_ids_old: ?[]const u64 = null,
    flow_ids: ?[]const u64 = null,
    terminating_flow_ids_old: ?[]const u64 = null,
    terminating_flow_ids: ?[]const u64 = null,
    debug_annotations: ?[]const ?debug_annotation.DebugAnnotation = null,
    task_execution: ?task_execution.TaskExecution = null,
    log_message: ?log_message.LogMessage = null,
    cc_scheduler_state: ?chrome_compositor_scheduler_state.ChromeCompositorSchedulerState = null,
    chrome_user_event: ?chrome_user_event.ChromeUserEvent = null,
    chrome_keyed_service: ?chrome_keyed_service.ChromeKeyedService = null,
    chrome_legacy_ipc: ?chrome_legacy_ipc.ChromeLegacyIpc = null,
    chrome_histogram_sample: ?chrome_histogram_sample.ChromeHistogramSample = null,
    chrome_latency_info: ?chrome_latency_info.ChromeLatencyInfo = null,
    chrome_frame_reporter: ?chrome_frame_reporter.ChromeFrameReporter = null,
    chrome_application_state_info: ?chrome_application_state_info.ChromeApplicationStateInfo = null,
    chrome_renderer_scheduler_state: ?chrome_renderer_scheduler_state.ChromeRendererSchedulerState = null,
    chrome_window_handle_event_info: ?chrome_window_handle_event_info.ChromeWindowHandleEventInfo = null,
    chrome_content_settings_event_info: ?chrome_content_settings_event_info.ChromeContentSettingsEventInfo = null,
    chrome_active_processes: ?chrome_active_processes.ChromeActiveProcesses = null,
    screenshot: ?screenshot.Screenshot = null,
    chrome_message_pump: ?chrome_message_pump.ChromeMessagePump = null,
    chrome_mojo_event_info: ?chrome_mojo_event_info.ChromeMojoEventInfo = null,
    legacy_event: ?track_event.TrackEvent.LegacyEvent = null,
    name_iid: u64 = 0,
    name: ?[]const u8 = null,
    counter_value: i64 = 0,
    double_counter_value: f64 = 0.0,
    correlation_id: u64 = 0,
    correlation_id_str: ?[]const u8 = null,
    correlation_id_str_iid: u64 = 0,
    callstack: ?track_event.TrackEvent.Callstack = null,
    callstack_iid: u64 = 0,
    source_location: ?source_location.SourceLocation = null,
    source_location_iid: u64 = 0,
    timestamp_delta_us: i64 = 0,
    timestamp_absolute_us: i64 = 0,
    thread_time_delta_us: i64 = 0,
    thread_time_absolute_us: i64 = 0,
    thread_instruction_count_delta: i64 = 0,
    thread_instruction_count_absolute: i64 = 0,
    pub fn calcProtobufSize(self: *const AndroidTrackEvent) usize {
        var res: usize = 0;
        if (self.category_iids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CATEGORY_IIDS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CATEGORY_IIDS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.categories) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CATEGORIES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (@intFromEnum(self.type) != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.type));
        }
        if (self.track_uuid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TRACK_UUID_WIRE) + gremlin.sizes.sizeU64(self.track_uuid);
        }
        if (self.extra_counter_track_uuids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.EXTRA_COUNTER_TRACK_UUIDS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.EXTRA_COUNTER_TRACK_UUIDS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.extra_counter_values) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.EXTRA_COUNTER_VALUES_WIRE) + gremlin.sizes.sizeI64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.EXTRA_COUNTER_VALUES_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.extra_double_counter_track_uuids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_TRACK_UUIDS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_TRACK_UUIDS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.extra_double_counter_values) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_VALUES_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_VALUES_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.flow_ids_old) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.FLOW_IDS_OLD_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.FLOW_IDS_OLD_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.flow_ids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.FLOW_IDS_WIRE) + gremlin.sizes.sizeFixed64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeFixed64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.FLOW_IDS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.terminating_flow_ids_old) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TERMINATING_FLOW_IDS_OLD_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TERMINATING_FLOW_IDS_OLD_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.terminating_flow_ids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TERMINATING_FLOW_IDS_WIRE) + gremlin.sizes.sizeFixed64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeFixed64(v);
                }
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TERMINATING_FLOW_IDS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.debug_annotations) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.DEBUG_ANNOTATIONS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.task_execution) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TASK_EXECUTION_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.log_message) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.LOG_MESSAGE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.cc_scheduler_state) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CC_SCHEDULER_STATE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_user_event) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_USER_EVENT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_keyed_service) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_KEYED_SERVICE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_legacy_ipc) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_LEGACY_IPC_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_histogram_sample) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_HISTOGRAM_SAMPLE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_latency_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_LATENCY_INFO_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_frame_reporter) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_FRAME_REPORTER_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_application_state_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_APPLICATION_STATE_INFO_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_renderer_scheduler_state) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_RENDERER_SCHEDULER_STATE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_window_handle_event_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_WINDOW_HANDLE_EVENT_INFO_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_content_settings_event_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_CONTENT_SETTINGS_EVENT_INFO_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_active_processes) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_ACTIVE_PROCESSES_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.screenshot) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.SCREENSHOT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_message_pump) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_MESSAGE_PUMP_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.chrome_mojo_event_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CHROME_MOJO_EVENT_INFO_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.legacy_event) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.LEGACY_EVENT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.name_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.NAME_IID_WIRE) + gremlin.sizes.sizeU64(self.name_iid);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.counter_value != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.COUNTER_VALUE_WIRE) + gremlin.sizes.sizeI64(self.counter_value);
        }
        if (self.double_counter_value != 0.0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.DOUBLE_COUNTER_VALUE_WIRE) + gremlin.sizes.sizeDouble(self.double_counter_value);
        }
        if (self.correlation_id != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CORRELATION_ID_WIRE) + gremlin.sizes.sizeU64(self.correlation_id);
        }
        if (self.correlation_id_str) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CORRELATION_ID_STR_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.correlation_id_str_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CORRELATION_ID_STR_IID_WIRE) + gremlin.sizes.sizeU64(self.correlation_id_str_iid);
        }
        if (self.callstack) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CALLSTACK_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.callstack_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.CALLSTACK_IID_WIRE) + gremlin.sizes.sizeU64(self.callstack_iid);
        }
        if (self.source_location) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.SOURCE_LOCATION_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.source_location_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.SOURCE_LOCATION_IID_WIRE) + gremlin.sizes.sizeU64(self.source_location_iid);
        }
        if (self.timestamp_delta_us != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TIMESTAMP_DELTA_US_WIRE) + gremlin.sizes.sizeI64(self.timestamp_delta_us);
        }
        if (self.timestamp_absolute_us != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.TIMESTAMP_ABSOLUTE_US_WIRE) + gremlin.sizes.sizeI64(self.timestamp_absolute_us);
        }
        if (self.thread_time_delta_us != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.THREAD_TIME_DELTA_US_WIRE) + gremlin.sizes.sizeI64(self.thread_time_delta_us);
        }
        if (self.thread_time_absolute_us != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.THREAD_TIME_ABSOLUTE_US_WIRE) + gremlin.sizes.sizeI64(self.thread_time_absolute_us);
        }
        if (self.thread_instruction_count_delta != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.THREAD_INSTRUCTION_COUNT_DELTA_WIRE) + gremlin.sizes.sizeI64(self.thread_instruction_count_delta);
        }
        if (self.thread_instruction_count_absolute != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidTrackEventWire.THREAD_INSTRUCTION_COUNT_ABSOLUTE_WIRE) + gremlin.sizes.sizeI64(self.thread_instruction_count_absolute);
        }
        return res;
    }
    pub fn encode(self: *const AndroidTrackEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidTrackEvent, target: *gremlin.Writer) void {
        if (self.category_iids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(AndroidTrackEventWire.CATEGORY_IIDS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.CATEGORY_IIDS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
        if (self.categories) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(AndroidTrackEventWire.CATEGORIES_WIRE, v);
                } else {
                    target.appendBytesTag(AndroidTrackEventWire.CATEGORIES_WIRE, 0);
                }
            }
        }
        if (@intFromEnum(self.type) != 0) {
            target.appendInt32(AndroidTrackEventWire.TYPE_WIRE, @intFromEnum(self.type));
        }
        if (self.track_uuid != 0) {
            target.appendUint64(AndroidTrackEventWire.TRACK_UUID_WIRE, self.track_uuid);
        }
        if (self.extra_counter_track_uuids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(AndroidTrackEventWire.EXTRA_COUNTER_TRACK_UUIDS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.EXTRA_COUNTER_TRACK_UUIDS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
        if (self.extra_counter_values) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendInt64(AndroidTrackEventWire.EXTRA_COUNTER_VALUES_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI64(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.EXTRA_COUNTER_VALUES_WIRE, packed_size);
                for (arr) |v| {
                    target.appendInt64WithoutTag(v);
                }
            }
        }
        if (self.extra_double_counter_track_uuids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_TRACK_UUIDS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_TRACK_UUIDS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
        if (self.extra_double_counter_values) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_VALUES_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_VALUES_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.flow_ids_old) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(AndroidTrackEventWire.FLOW_IDS_OLD_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.FLOW_IDS_OLD_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
        if (self.flow_ids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFixed64(AndroidTrackEventWire.FLOW_IDS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeFixed64(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.FLOW_IDS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFixed64WithoutTag(v);
                }
            }
        }
        if (self.terminating_flow_ids_old) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(AndroidTrackEventWire.TERMINATING_FLOW_IDS_OLD_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.TERMINATING_FLOW_IDS_OLD_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
        if (self.terminating_flow_ids) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFixed64(AndroidTrackEventWire.TERMINATING_FLOW_IDS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeFixed64(v);
                }
                target.appendBytesTag(AndroidTrackEventWire.TERMINATING_FLOW_IDS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFixed64WithoutTag(v);
                }
            }
        }
        if (self.debug_annotations) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(AndroidTrackEventWire.DEBUG_ANNOTATIONS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(AndroidTrackEventWire.DEBUG_ANNOTATIONS_WIRE, 0);
                }
            }
        }
        if (self.task_execution) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.TASK_EXECUTION_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.log_message) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.LOG_MESSAGE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.cc_scheduler_state) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CC_SCHEDULER_STATE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_user_event) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_USER_EVENT_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_keyed_service) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_KEYED_SERVICE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_legacy_ipc) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_LEGACY_IPC_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_histogram_sample) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_HISTOGRAM_SAMPLE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_latency_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_LATENCY_INFO_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_frame_reporter) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_FRAME_REPORTER_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_application_state_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_APPLICATION_STATE_INFO_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_renderer_scheduler_state) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_RENDERER_SCHEDULER_STATE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_window_handle_event_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_WINDOW_HANDLE_EVENT_INFO_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_content_settings_event_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_CONTENT_SETTINGS_EVENT_INFO_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_active_processes) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_ACTIVE_PROCESSES_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.screenshot) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.SCREENSHOT_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_message_pump) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_MESSAGE_PUMP_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.chrome_mojo_event_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CHROME_MOJO_EVENT_INFO_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.legacy_event) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.LEGACY_EVENT_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.name_iid != 0) {
            target.appendUint64(AndroidTrackEventWire.NAME_IID_WIRE, self.name_iid);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidTrackEventWire.NAME_WIRE, v);
            }
        }
        if (self.counter_value != 0) {
            target.appendInt64(AndroidTrackEventWire.COUNTER_VALUE_WIRE, self.counter_value);
        }
        if (self.double_counter_value != 0.0) {
            target.appendFloat64(AndroidTrackEventWire.DOUBLE_COUNTER_VALUE_WIRE, self.double_counter_value);
        }
        if (self.correlation_id != 0) {
            target.appendUint64(AndroidTrackEventWire.CORRELATION_ID_WIRE, self.correlation_id);
        }
        if (self.correlation_id_str) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidTrackEventWire.CORRELATION_ID_STR_WIRE, v);
            }
        }
        if (self.correlation_id_str_iid != 0) {
            target.appendUint64(AndroidTrackEventWire.CORRELATION_ID_STR_IID_WIRE, self.correlation_id_str_iid);
        }
        if (self.callstack) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.CALLSTACK_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.callstack_iid != 0) {
            target.appendUint64(AndroidTrackEventWire.CALLSTACK_IID_WIRE, self.callstack_iid);
        }
        if (self.source_location) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(AndroidTrackEventWire.SOURCE_LOCATION_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.source_location_iid != 0) {
            target.appendUint64(AndroidTrackEventWire.SOURCE_LOCATION_IID_WIRE, self.source_location_iid);
        }
        if (self.timestamp_delta_us != 0) {
            target.appendInt64(AndroidTrackEventWire.TIMESTAMP_DELTA_US_WIRE, self.timestamp_delta_us);
        }
        if (self.timestamp_absolute_us != 0) {
            target.appendInt64(AndroidTrackEventWire.TIMESTAMP_ABSOLUTE_US_WIRE, self.timestamp_absolute_us);
        }
        if (self.thread_time_delta_us != 0) {
            target.appendInt64(AndroidTrackEventWire.THREAD_TIME_DELTA_US_WIRE, self.thread_time_delta_us);
        }
        if (self.thread_time_absolute_us != 0) {
            target.appendInt64(AndroidTrackEventWire.THREAD_TIME_ABSOLUTE_US_WIRE, self.thread_time_absolute_us);
        }
        if (self.thread_instruction_count_delta != 0) {
            target.appendInt64(AndroidTrackEventWire.THREAD_INSTRUCTION_COUNT_DELTA_WIRE, self.thread_instruction_count_delta);
        }
        if (self.thread_instruction_count_absolute != 0) {
            target.appendInt64(AndroidTrackEventWire.THREAD_INSTRUCTION_COUNT_ABSOLUTE_WIRE, self.thread_instruction_count_absolute);
        }
    }
};
pub const AndroidTrackEventReader = struct {
    buf: gremlin.Reader,
    _category_iids_offset: ?usize = null,
    _category_iids_last_offset: ?usize = null,
    _category_iids_packed: bool = false,
    _categories_offset: ?usize = null,
    _categories_last_offset: ?usize = null,
    _categories_cnt: usize = 0,
    _type: track_event.TrackEvent.Type = @enumFromInt(0),
    _track_uuid: u64 = 0,
    _extra_counter_track_uuids_offset: ?usize = null,
    _extra_counter_track_uuids_last_offset: ?usize = null,
    _extra_counter_track_uuids_packed: bool = false,
    _extra_counter_values_offset: ?usize = null,
    _extra_counter_values_last_offset: ?usize = null,
    _extra_counter_values_packed: bool = false,
    _extra_double_counter_track_uuids_offset: ?usize = null,
    _extra_double_counter_track_uuids_last_offset: ?usize = null,
    _extra_double_counter_track_uuids_packed: bool = false,
    _extra_double_counter_values_offset: ?usize = null,
    _extra_double_counter_values_last_offset: ?usize = null,
    _extra_double_counter_values_packed: bool = false,
    _flow_ids_old_offset: ?usize = null,
    _flow_ids_old_last_offset: ?usize = null,
    _flow_ids_old_packed: bool = false,
    _flow_ids_offset: ?usize = null,
    _flow_ids_last_offset: ?usize = null,
    _flow_ids_packed: bool = false,
    _terminating_flow_ids_old_offset: ?usize = null,
    _terminating_flow_ids_old_last_offset: ?usize = null,
    _terminating_flow_ids_old_packed: bool = false,
    _terminating_flow_ids_offset: ?usize = null,
    _terminating_flow_ids_last_offset: ?usize = null,
    _terminating_flow_ids_packed: bool = false,
    _debug_annotations_offset: ?usize = null,
    _debug_annotations_last_offset: ?usize = null,
    _debug_annotations_cnt: usize = 0,
    _task_execution_buf: ?[]const u8 = null,
    _log_message_buf: ?[]const u8 = null,
    _cc_scheduler_state_buf: ?[]const u8 = null,
    _chrome_user_event_buf: ?[]const u8 = null,
    _chrome_keyed_service_buf: ?[]const u8 = null,
    _chrome_legacy_ipc_buf: ?[]const u8 = null,
    _chrome_histogram_sample_buf: ?[]const u8 = null,
    _chrome_latency_info_buf: ?[]const u8 = null,
    _chrome_frame_reporter_buf: ?[]const u8 = null,
    _chrome_application_state_info_buf: ?[]const u8 = null,
    _chrome_renderer_scheduler_state_buf: ?[]const u8 = null,
    _chrome_window_handle_event_info_buf: ?[]const u8 = null,
    _chrome_content_settings_event_info_buf: ?[]const u8 = null,
    _chrome_active_processes_buf: ?[]const u8 = null,
    _screenshot_buf: ?[]const u8 = null,
    _chrome_message_pump_buf: ?[]const u8 = null,
    _chrome_mojo_event_info_buf: ?[]const u8 = null,
    _legacy_event_buf: ?[]const u8 = null,
    _name_iid: u64 = 0,
    _name: ?[]const u8 = null,
    _counter_value: i64 = 0,
    _double_counter_value: f64 = 0.0,
    _correlation_id: u64 = 0,
    _correlation_id_str: ?[]const u8 = null,
    _correlation_id_str_iid: u64 = 0,
    _callstack_buf: ?[]const u8 = null,
    _callstack_iid: u64 = 0,
    _source_location_buf: ?[]const u8 = null,
    _source_location_iid: u64 = 0,
    _timestamp_delta_us: i64 = 0,
    _timestamp_absolute_us: i64 = 0,
    _thread_time_delta_us: i64 = 0,
    _thread_time_absolute_us: i64 = 0,
    _thread_instruction_count_delta: i64 = 0,
    _thread_instruction_count_absolute: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidTrackEventReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidTrackEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidTrackEventWire.CATEGORY_IIDS_WIRE => {
                    if (res._category_iids_offset == null) {
                        res._category_iids_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._category_iids_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._category_iids_offset = offset + length_result.size;
                        res._category_iids_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._category_iids_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._category_iids_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.CATEGORIES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._categories_offset == null) {
                        res._categories_offset = offset - result.size;
                    }
                    res._categories_last_offset = offset;
                    res._categories_cnt += 1;
                },
                AndroidTrackEventWire.TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._type = @enumFromInt(result.value);
                },
                AndroidTrackEventWire.TRACK_UUID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._track_uuid = result.value;
                },
                AndroidTrackEventWire.EXTRA_COUNTER_TRACK_UUIDS_WIRE => {
                    if (res._extra_counter_track_uuids_offset == null) {
                        res._extra_counter_track_uuids_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._extra_counter_track_uuids_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._extra_counter_track_uuids_offset = offset + length_result.size;
                        res._extra_counter_track_uuids_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._extra_counter_track_uuids_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._extra_counter_track_uuids_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.EXTRA_COUNTER_VALUES_WIRE => {
                    if (res._extra_counter_values_offset == null) {
                        res._extra_counter_values_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._extra_counter_values_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._extra_counter_values_offset = offset + length_result.size;
                        res._extra_counter_values_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._extra_counter_values_last_offset.?;
                    } else {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._extra_counter_values_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_TRACK_UUIDS_WIRE => {
                    if (res._extra_double_counter_track_uuids_offset == null) {
                        res._extra_double_counter_track_uuids_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._extra_double_counter_track_uuids_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._extra_double_counter_track_uuids_offset = offset + length_result.size;
                        res._extra_double_counter_track_uuids_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._extra_double_counter_track_uuids_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._extra_double_counter_track_uuids_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_VALUES_WIRE => {
                    if (res._extra_double_counter_values_offset == null) {
                        res._extra_double_counter_values_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._extra_double_counter_values_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._extra_double_counter_values_offset = offset + length_result.size;
                        res._extra_double_counter_values_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._extra_double_counter_values_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._extra_double_counter_values_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.FLOW_IDS_OLD_WIRE => {
                    if (res._flow_ids_old_offset == null) {
                        res._flow_ids_old_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._flow_ids_old_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._flow_ids_old_offset = offset + length_result.size;
                        res._flow_ids_old_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._flow_ids_old_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._flow_ids_old_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.FLOW_IDS_WIRE => {
                    if (res._flow_ids_offset == null) {
                        res._flow_ids_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._flow_ids_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._flow_ids_offset = offset + length_result.size;
                        res._flow_ids_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._flow_ids_last_offset.?;
                    } else {
                        const result = try buf.readFixed64(offset);
                        offset += result.size;
                        res._flow_ids_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.TERMINATING_FLOW_IDS_OLD_WIRE => {
                    if (res._terminating_flow_ids_old_offset == null) {
                        res._terminating_flow_ids_old_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._terminating_flow_ids_old_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._terminating_flow_ids_old_offset = offset + length_result.size;
                        res._terminating_flow_ids_old_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._terminating_flow_ids_old_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._terminating_flow_ids_old_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.TERMINATING_FLOW_IDS_WIRE => {
                    if (res._terminating_flow_ids_offset == null) {
                        res._terminating_flow_ids_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._terminating_flow_ids_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._terminating_flow_ids_offset = offset + length_result.size;
                        res._terminating_flow_ids_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._terminating_flow_ids_last_offset.?;
                    } else {
                        const result = try buf.readFixed64(offset);
                        offset += result.size;
                        res._terminating_flow_ids_last_offset = offset;
                    }
                },
                AndroidTrackEventWire.DEBUG_ANNOTATIONS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._debug_annotations_offset == null) {
                        res._debug_annotations_offset = offset - result.size;
                    }
                    res._debug_annotations_last_offset = offset;
                    res._debug_annotations_cnt += 1;
                },
                AndroidTrackEventWire.TASK_EXECUTION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._task_execution_buf = result.value;
                },
                AndroidTrackEventWire.LOG_MESSAGE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._log_message_buf = result.value;
                },
                AndroidTrackEventWire.CC_SCHEDULER_STATE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cc_scheduler_state_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_USER_EVENT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_user_event_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_KEYED_SERVICE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_keyed_service_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_LEGACY_IPC_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_legacy_ipc_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_HISTOGRAM_SAMPLE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_histogram_sample_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_LATENCY_INFO_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_latency_info_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_FRAME_REPORTER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_frame_reporter_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_APPLICATION_STATE_INFO_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_application_state_info_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_RENDERER_SCHEDULER_STATE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_renderer_scheduler_state_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_WINDOW_HANDLE_EVENT_INFO_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_window_handle_event_info_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_CONTENT_SETTINGS_EVENT_INFO_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_content_settings_event_info_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_ACTIVE_PROCESSES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_active_processes_buf = result.value;
                },
                AndroidTrackEventWire.SCREENSHOT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._screenshot_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_MESSAGE_PUMP_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_message_pump_buf = result.value;
                },
                AndroidTrackEventWire.CHROME_MOJO_EVENT_INFO_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._chrome_mojo_event_info_buf = result.value;
                },
                AndroidTrackEventWire.LEGACY_EVENT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._legacy_event_buf = result.value;
                },
                AndroidTrackEventWire.NAME_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._name_iid = result.value;
                },
                AndroidTrackEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                AndroidTrackEventWire.COUNTER_VALUE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._counter_value = result.value;
                },
                AndroidTrackEventWire.DOUBLE_COUNTER_VALUE_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._double_counter_value = result.value;
                },
                AndroidTrackEventWire.CORRELATION_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._correlation_id = result.value;
                },
                AndroidTrackEventWire.CORRELATION_ID_STR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._correlation_id_str = result.value;
                },
                AndroidTrackEventWire.CORRELATION_ID_STR_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._correlation_id_str_iid = result.value;
                },
                AndroidTrackEventWire.CALLSTACK_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._callstack_buf = result.value;
                },
                AndroidTrackEventWire.CALLSTACK_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._callstack_iid = result.value;
                },
                AndroidTrackEventWire.SOURCE_LOCATION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._source_location_buf = result.value;
                },
                AndroidTrackEventWire.SOURCE_LOCATION_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._source_location_iid = result.value;
                },
                AndroidTrackEventWire.TIMESTAMP_DELTA_US_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._timestamp_delta_us = result.value;
                },
                AndroidTrackEventWire.TIMESTAMP_ABSOLUTE_US_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._timestamp_absolute_us = result.value;
                },
                AndroidTrackEventWire.THREAD_TIME_DELTA_US_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._thread_time_delta_us = result.value;
                },
                AndroidTrackEventWire.THREAD_TIME_ABSOLUTE_US_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._thread_time_absolute_us = result.value;
                },
                AndroidTrackEventWire.THREAD_INSTRUCTION_COUNT_DELTA_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._thread_instruction_count_delta = result.value;
                },
                AndroidTrackEventWire.THREAD_INSTRUCTION_COUNT_ABSOLUTE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._thread_instruction_count_absolute = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn categoryIidsNext(self: *AndroidTrackEventReader) gremlin.Error!?u64 {
        if (self._category_iids_offset == null) return null;
        const current_offset = self._category_iids_offset.?;
        if (current_offset >= self._category_iids_last_offset.?) {
            self._category_iids_offset = null;
            return null;
        }
        if (self._category_iids_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._category_iids_offset = current_offset + value_result.size;
            if (self._category_iids_offset.? >= self._category_iids_last_offset.?) {
                self._category_iids_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._category_iids_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.CATEGORY_IIDS_WIRE) {
                    self._category_iids_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._category_iids_offset = null;
            return value_result.value;
        }
    }
    pub fn categoriesCount(self: *const AndroidTrackEventReader) usize {
        return self._categories_cnt;
    }
    pub fn categoriesNext(self: *AndroidTrackEventReader) ?[]const u8 {
        if (self._categories_offset == null) return null;
        const current_offset = self._categories_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._categories_last_offset != null and current_offset >= self._categories_last_offset.?) {
            self._categories_offset = null;
            return result.value;
        }
        if (self._categories_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._categories_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == AndroidTrackEventWire.CATEGORIES_WIRE) {
                self._categories_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._categories_offset = null;
        return result.value;
    }
    pub inline fn getType(self: *const AndroidTrackEventReader) track_event.TrackEvent.Type {
        return self._type;
    }
    pub inline fn getTrackUuid(self: *const AndroidTrackEventReader) u64 {
        return self._track_uuid;
    }
    pub fn extraCounterTrackUuidsNext(self: *AndroidTrackEventReader) gremlin.Error!?u64 {
        if (self._extra_counter_track_uuids_offset == null) return null;
        const current_offset = self._extra_counter_track_uuids_offset.?;
        if (current_offset >= self._extra_counter_track_uuids_last_offset.?) {
            self._extra_counter_track_uuids_offset = null;
            return null;
        }
        if (self._extra_counter_track_uuids_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._extra_counter_track_uuids_offset = current_offset + value_result.size;
            if (self._extra_counter_track_uuids_offset.? >= self._extra_counter_track_uuids_last_offset.?) {
                self._extra_counter_track_uuids_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._extra_counter_track_uuids_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.EXTRA_COUNTER_TRACK_UUIDS_WIRE) {
                    self._extra_counter_track_uuids_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._extra_counter_track_uuids_offset = null;
            return value_result.value;
        }
    }
    pub fn extraCounterValuesNext(self: *AndroidTrackEventReader) gremlin.Error!?i64 {
        if (self._extra_counter_values_offset == null) return null;
        const current_offset = self._extra_counter_values_offset.?;
        if (current_offset >= self._extra_counter_values_last_offset.?) {
            self._extra_counter_values_offset = null;
            return null;
        }
        if (self._extra_counter_values_packed) {
            const value_result = try self.buf.readInt64(current_offset);
            self._extra_counter_values_offset = current_offset + value_result.size;
            if (self._extra_counter_values_offset.? >= self._extra_counter_values_last_offset.?) {
                self._extra_counter_values_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._extra_counter_values_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.EXTRA_COUNTER_VALUES_WIRE) {
                    self._extra_counter_values_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._extra_counter_values_offset = null;
            return value_result.value;
        }
    }
    pub fn extraDoubleCounterTrackUuidsNext(self: *AndroidTrackEventReader) gremlin.Error!?u64 {
        if (self._extra_double_counter_track_uuids_offset == null) return null;
        const current_offset = self._extra_double_counter_track_uuids_offset.?;
        if (current_offset >= self._extra_double_counter_track_uuids_last_offset.?) {
            self._extra_double_counter_track_uuids_offset = null;
            return null;
        }
        if (self._extra_double_counter_track_uuids_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._extra_double_counter_track_uuids_offset = current_offset + value_result.size;
            if (self._extra_double_counter_track_uuids_offset.? >= self._extra_double_counter_track_uuids_last_offset.?) {
                self._extra_double_counter_track_uuids_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._extra_double_counter_track_uuids_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_TRACK_UUIDS_WIRE) {
                    self._extra_double_counter_track_uuids_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._extra_double_counter_track_uuids_offset = null;
            return value_result.value;
        }
    }
    pub fn extraDoubleCounterValuesNext(self: *AndroidTrackEventReader) gremlin.Error!?f64 {
        if (self._extra_double_counter_values_offset == null) return null;
        const current_offset = self._extra_double_counter_values_offset.?;
        if (current_offset >= self._extra_double_counter_values_last_offset.?) {
            self._extra_double_counter_values_offset = null;
            return null;
        }
        if (self._extra_double_counter_values_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._extra_double_counter_values_offset = current_offset + value_result.size;
            if (self._extra_double_counter_values_offset.? >= self._extra_double_counter_values_last_offset.?) {
                self._extra_double_counter_values_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._extra_double_counter_values_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.EXTRA_DOUBLE_COUNTER_VALUES_WIRE) {
                    self._extra_double_counter_values_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._extra_double_counter_values_offset = null;
            return value_result.value;
        }
    }
    pub fn flowIdsOldNext(self: *AndroidTrackEventReader) gremlin.Error!?u64 {
        if (self._flow_ids_old_offset == null) return null;
        const current_offset = self._flow_ids_old_offset.?;
        if (current_offset >= self._flow_ids_old_last_offset.?) {
            self._flow_ids_old_offset = null;
            return null;
        }
        if (self._flow_ids_old_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._flow_ids_old_offset = current_offset + value_result.size;
            if (self._flow_ids_old_offset.? >= self._flow_ids_old_last_offset.?) {
                self._flow_ids_old_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._flow_ids_old_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.FLOW_IDS_OLD_WIRE) {
                    self._flow_ids_old_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._flow_ids_old_offset = null;
            return value_result.value;
        }
    }
    pub fn flowIdsNext(self: *AndroidTrackEventReader) gremlin.Error!?u64 {
        if (self._flow_ids_offset == null) return null;
        const current_offset = self._flow_ids_offset.?;
        if (current_offset >= self._flow_ids_last_offset.?) {
            self._flow_ids_offset = null;
            return null;
        }
        if (self._flow_ids_packed) {
            const value_result = try self.buf.readFixed64(current_offset);
            self._flow_ids_offset = current_offset + value_result.size;
            if (self._flow_ids_offset.? >= self._flow_ids_last_offset.?) {
                self._flow_ids_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFixed64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._flow_ids_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.FLOW_IDS_WIRE) {
                    self._flow_ids_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._flow_ids_offset = null;
            return value_result.value;
        }
    }
    pub fn terminatingFlowIdsOldNext(self: *AndroidTrackEventReader) gremlin.Error!?u64 {
        if (self._terminating_flow_ids_old_offset == null) return null;
        const current_offset = self._terminating_flow_ids_old_offset.?;
        if (current_offset >= self._terminating_flow_ids_old_last_offset.?) {
            self._terminating_flow_ids_old_offset = null;
            return null;
        }
        if (self._terminating_flow_ids_old_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._terminating_flow_ids_old_offset = current_offset + value_result.size;
            if (self._terminating_flow_ids_old_offset.? >= self._terminating_flow_ids_old_last_offset.?) {
                self._terminating_flow_ids_old_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._terminating_flow_ids_old_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.TERMINATING_FLOW_IDS_OLD_WIRE) {
                    self._terminating_flow_ids_old_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._terminating_flow_ids_old_offset = null;
            return value_result.value;
        }
    }
    pub fn terminatingFlowIdsNext(self: *AndroidTrackEventReader) gremlin.Error!?u64 {
        if (self._terminating_flow_ids_offset == null) return null;
        const current_offset = self._terminating_flow_ids_offset.?;
        if (current_offset >= self._terminating_flow_ids_last_offset.?) {
            self._terminating_flow_ids_offset = null;
            return null;
        }
        if (self._terminating_flow_ids_packed) {
            const value_result = try self.buf.readFixed64(current_offset);
            self._terminating_flow_ids_offset = current_offset + value_result.size;
            if (self._terminating_flow_ids_offset.? >= self._terminating_flow_ids_last_offset.?) {
                self._terminating_flow_ids_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFixed64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._terminating_flow_ids_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == AndroidTrackEventWire.TERMINATING_FLOW_IDS_WIRE) {
                    self._terminating_flow_ids_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._terminating_flow_ids_offset = null;
            return value_result.value;
        }
    }
    pub fn debugAnnotationsCount(self: *const AndroidTrackEventReader) usize {
        return self._debug_annotations_cnt;
    }
    pub fn debugAnnotationsNext(self: *AndroidTrackEventReader) ?debug_annotation.DebugAnnotationReader {
        if (self._debug_annotations_offset == null) return null;
        const current_offset = self._debug_annotations_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = debug_annotation.DebugAnnotationReader.init(result.value) catch return null;
        if (self._debug_annotations_last_offset != null and current_offset >= self._debug_annotations_last_offset.?) {
            self._debug_annotations_offset = null;
            return msg;
        }
        if (self._debug_annotations_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._debug_annotations_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == AndroidTrackEventWire.DEBUG_ANNOTATIONS_WIRE) {
                self._debug_annotations_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._debug_annotations_offset = null;
        return msg;
    }
    pub fn getTaskExecution(self: *const AndroidTrackEventReader) gremlin.Error!task_execution.TaskExecutionReader {
        if (self._task_execution_buf) |buf| {
            return try task_execution.TaskExecutionReader.init(buf);
        }
        return try task_execution.TaskExecutionReader.init(&[_]u8{});
    }
    pub fn getLogMessage(self: *const AndroidTrackEventReader) gremlin.Error!log_message.LogMessageReader {
        if (self._log_message_buf) |buf| {
            return try log_message.LogMessageReader.init(buf);
        }
        return try log_message.LogMessageReader.init(&[_]u8{});
    }
    pub fn getCcSchedulerState(self: *const AndroidTrackEventReader) gremlin.Error!chrome_compositor_scheduler_state.ChromeCompositorSchedulerStateReader {
        if (self._cc_scheduler_state_buf) |buf| {
            return try chrome_compositor_scheduler_state.ChromeCompositorSchedulerStateReader.init(buf);
        }
        return try chrome_compositor_scheduler_state.ChromeCompositorSchedulerStateReader.init(&[_]u8{});
    }
    pub fn getChromeUserEvent(self: *const AndroidTrackEventReader) gremlin.Error!chrome_user_event.ChromeUserEventReader {
        if (self._chrome_user_event_buf) |buf| {
            return try chrome_user_event.ChromeUserEventReader.init(buf);
        }
        return try chrome_user_event.ChromeUserEventReader.init(&[_]u8{});
    }
    pub fn getChromeKeyedService(self: *const AndroidTrackEventReader) gremlin.Error!chrome_keyed_service.ChromeKeyedServiceReader {
        if (self._chrome_keyed_service_buf) |buf| {
            return try chrome_keyed_service.ChromeKeyedServiceReader.init(buf);
        }
        return try chrome_keyed_service.ChromeKeyedServiceReader.init(&[_]u8{});
    }
    pub fn getChromeLegacyIpc(self: *const AndroidTrackEventReader) gremlin.Error!chrome_legacy_ipc.ChromeLegacyIpcReader {
        if (self._chrome_legacy_ipc_buf) |buf| {
            return try chrome_legacy_ipc.ChromeLegacyIpcReader.init(buf);
        }
        return try chrome_legacy_ipc.ChromeLegacyIpcReader.init(&[_]u8{});
    }
    pub fn getChromeHistogramSample(self: *const AndroidTrackEventReader) gremlin.Error!chrome_histogram_sample.ChromeHistogramSampleReader {
        if (self._chrome_histogram_sample_buf) |buf| {
            return try chrome_histogram_sample.ChromeHistogramSampleReader.init(buf);
        }
        return try chrome_histogram_sample.ChromeHistogramSampleReader.init(&[_]u8{});
    }
    pub fn getChromeLatencyInfo(self: *const AndroidTrackEventReader) gremlin.Error!chrome_latency_info.ChromeLatencyInfoReader {
        if (self._chrome_latency_info_buf) |buf| {
            return try chrome_latency_info.ChromeLatencyInfoReader.init(buf);
        }
        return try chrome_latency_info.ChromeLatencyInfoReader.init(&[_]u8{});
    }
    pub fn getChromeFrameReporter(self: *const AndroidTrackEventReader) gremlin.Error!chrome_frame_reporter.ChromeFrameReporterReader {
        if (self._chrome_frame_reporter_buf) |buf| {
            return try chrome_frame_reporter.ChromeFrameReporterReader.init(buf);
        }
        return try chrome_frame_reporter.ChromeFrameReporterReader.init(&[_]u8{});
    }
    pub fn getChromeApplicationStateInfo(self: *const AndroidTrackEventReader) gremlin.Error!chrome_application_state_info.ChromeApplicationStateInfoReader {
        if (self._chrome_application_state_info_buf) |buf| {
            return try chrome_application_state_info.ChromeApplicationStateInfoReader.init(buf);
        }
        return try chrome_application_state_info.ChromeApplicationStateInfoReader.init(&[_]u8{});
    }
    pub fn getChromeRendererSchedulerState(self: *const AndroidTrackEventReader) gremlin.Error!chrome_renderer_scheduler_state.ChromeRendererSchedulerStateReader {
        if (self._chrome_renderer_scheduler_state_buf) |buf| {
            return try chrome_renderer_scheduler_state.ChromeRendererSchedulerStateReader.init(buf);
        }
        return try chrome_renderer_scheduler_state.ChromeRendererSchedulerStateReader.init(&[_]u8{});
    }
    pub fn getChromeWindowHandleEventInfo(self: *const AndroidTrackEventReader) gremlin.Error!chrome_window_handle_event_info.ChromeWindowHandleEventInfoReader {
        if (self._chrome_window_handle_event_info_buf) |buf| {
            return try chrome_window_handle_event_info.ChromeWindowHandleEventInfoReader.init(buf);
        }
        return try chrome_window_handle_event_info.ChromeWindowHandleEventInfoReader.init(&[_]u8{});
    }
    pub fn getChromeContentSettingsEventInfo(self: *const AndroidTrackEventReader) gremlin.Error!chrome_content_settings_event_info.ChromeContentSettingsEventInfoReader {
        if (self._chrome_content_settings_event_info_buf) |buf| {
            return try chrome_content_settings_event_info.ChromeContentSettingsEventInfoReader.init(buf);
        }
        return try chrome_content_settings_event_info.ChromeContentSettingsEventInfoReader.init(&[_]u8{});
    }
    pub fn getChromeActiveProcesses(self: *const AndroidTrackEventReader) gremlin.Error!chrome_active_processes.ChromeActiveProcessesReader {
        if (self._chrome_active_processes_buf) |buf| {
            return try chrome_active_processes.ChromeActiveProcessesReader.init(buf);
        }
        return try chrome_active_processes.ChromeActiveProcessesReader.init(&[_]u8{});
    }
    pub fn getScreenshot(self: *const AndroidTrackEventReader) gremlin.Error!screenshot.ScreenshotReader {
        if (self._screenshot_buf) |buf| {
            return try screenshot.ScreenshotReader.init(buf);
        }
        return try screenshot.ScreenshotReader.init(&[_]u8{});
    }
    pub fn getChromeMessagePump(self: *const AndroidTrackEventReader) gremlin.Error!chrome_message_pump.ChromeMessagePumpReader {
        if (self._chrome_message_pump_buf) |buf| {
            return try chrome_message_pump.ChromeMessagePumpReader.init(buf);
        }
        return try chrome_message_pump.ChromeMessagePumpReader.init(&[_]u8{});
    }
    pub fn getChromeMojoEventInfo(self: *const AndroidTrackEventReader) gremlin.Error!chrome_mojo_event_info.ChromeMojoEventInfoReader {
        if (self._chrome_mojo_event_info_buf) |buf| {
            return try chrome_mojo_event_info.ChromeMojoEventInfoReader.init(buf);
        }
        return try chrome_mojo_event_info.ChromeMojoEventInfoReader.init(&[_]u8{});
    }
    pub fn getLegacyEvent(self: *const AndroidTrackEventReader) gremlin.Error!track_event.TrackEvent.LegacyEventReader {
        if (self._legacy_event_buf) |buf| {
            return try track_event.TrackEvent.LegacyEventReader.init(buf);
        }
        return try track_event.TrackEvent.LegacyEventReader.init(&[_]u8{});
    }
    pub inline fn getNameIid(self: *const AndroidTrackEventReader) u64 {
        return self._name_iid;
    }
    pub inline fn getName(self: *const AndroidTrackEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getCounterValue(self: *const AndroidTrackEventReader) i64 {
        return self._counter_value;
    }
    pub inline fn getDoubleCounterValue(self: *const AndroidTrackEventReader) f64 {
        return self._double_counter_value;
    }
    pub inline fn getCorrelationId(self: *const AndroidTrackEventReader) u64 {
        return self._correlation_id;
    }
    pub inline fn getCorrelationIdStr(self: *const AndroidTrackEventReader) []const u8 {
        return self._correlation_id_str orelse &[_]u8{};
    }
    pub inline fn getCorrelationIdStrIid(self: *const AndroidTrackEventReader) u64 {
        return self._correlation_id_str_iid;
    }
    pub fn getCallstack(self: *const AndroidTrackEventReader) gremlin.Error!track_event.TrackEvent.CallstackReader {
        if (self._callstack_buf) |buf| {
            return try track_event.TrackEvent.CallstackReader.init(buf);
        }
        return try track_event.TrackEvent.CallstackReader.init(&[_]u8{});
    }
    pub inline fn getCallstackIid(self: *const AndroidTrackEventReader) u64 {
        return self._callstack_iid;
    }
    pub fn getSourceLocation(self: *const AndroidTrackEventReader) gremlin.Error!source_location.SourceLocationReader {
        if (self._source_location_buf) |buf| {
            return try source_location.SourceLocationReader.init(buf);
        }
        return try source_location.SourceLocationReader.init(&[_]u8{});
    }
    pub inline fn getSourceLocationIid(self: *const AndroidTrackEventReader) u64 {
        return self._source_location_iid;
    }
    pub inline fn getTimestampDeltaUs(self: *const AndroidTrackEventReader) i64 {
        return self._timestamp_delta_us;
    }
    pub inline fn getTimestampAbsoluteUs(self: *const AndroidTrackEventReader) i64 {
        return self._timestamp_absolute_us;
    }
    pub inline fn getThreadTimeDeltaUs(self: *const AndroidTrackEventReader) i64 {
        return self._thread_time_delta_us;
    }
    pub inline fn getThreadTimeAbsoluteUs(self: *const AndroidTrackEventReader) i64 {
        return self._thread_time_absolute_us;
    }
    pub inline fn getThreadInstructionCountDelta(self: *const AndroidTrackEventReader) i64 {
        return self._thread_instruction_count_delta;
    }
    pub inline fn getThreadInstructionCountAbsolute(self: *const AndroidTrackEventReader) i64 {
        return self._thread_instruction_count_absolute;
    }
};
