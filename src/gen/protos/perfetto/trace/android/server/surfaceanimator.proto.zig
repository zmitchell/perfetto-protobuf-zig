// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const surfacecontrol = @import("src/gen/protos/perfetto/trace/android/view/surfacecontrol.proto.zig");
const animationadapter = @import("animationadapter.proto.zig");
// structs
const SurfaceAnimatorProtoWire = struct {
    const LEASH_WIRE: gremlin.ProtoWireNumber = 1;
    const ANIMATION_START_DELAYED_WIRE: gremlin.ProtoWireNumber = 2;
    const ANIMATION_ADAPTER_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const SurfaceAnimatorProto = struct {
    // fields
    leash: ?surfacecontrol.SurfaceControlProto = null,
    animation_start_delayed: bool = false,
    animation_adapter: ?animationadapter.AnimationAdapterProto = null,
    pub fn calcProtobufSize(self: *const SurfaceAnimatorProto) usize {
        var res: usize = 0;
        if (self.leash) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(SurfaceAnimatorProtoWire.LEASH_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.animation_start_delayed != false) {
            res += gremlin.sizes.sizeWireNumber(SurfaceAnimatorProtoWire.ANIMATION_START_DELAYED_WIRE) + gremlin.sizes.sizeBool(self.animation_start_delayed);
        }
        if (self.animation_adapter) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(SurfaceAnimatorProtoWire.ANIMATION_ADAPTER_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const SurfaceAnimatorProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SurfaceAnimatorProto, target: *gremlin.Writer) void {
        if (self.leash) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(SurfaceAnimatorProtoWire.LEASH_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.animation_start_delayed != false) {
            target.appendBool(SurfaceAnimatorProtoWire.ANIMATION_START_DELAYED_WIRE, self.animation_start_delayed);
        }
        if (self.animation_adapter) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(SurfaceAnimatorProtoWire.ANIMATION_ADAPTER_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const SurfaceAnimatorProtoReader = struct {
    buf: gremlin.Reader,
    _leash_buf: ?[]const u8 = null,
    _animation_start_delayed: bool = false,
    _animation_adapter_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!SurfaceAnimatorProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = SurfaceAnimatorProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SurfaceAnimatorProtoWire.LEASH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._leash_buf = result.value;
                },
                SurfaceAnimatorProtoWire.ANIMATION_START_DELAYED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._animation_start_delayed = result.value;
                },
                SurfaceAnimatorProtoWire.ANIMATION_ADAPTER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._animation_adapter_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getLeash(self: *const SurfaceAnimatorProtoReader) gremlin.Error!surfacecontrol.SurfaceControlProtoReader {
        if (self._leash_buf) |buf| {
            return try surfacecontrol.SurfaceControlProtoReader.init(buf);
        }
        return try surfacecontrol.SurfaceControlProtoReader.init(&[_]u8{});
    }
    pub inline fn getAnimationStartDelayed(self: *const SurfaceAnimatorProtoReader) bool {
        return self._animation_start_delayed;
    }
    pub fn getAnimationAdapter(self: *const SurfaceAnimatorProtoReader) gremlin.Error!animationadapter.AnimationAdapterProtoReader {
        if (self._animation_adapter_buf) |buf| {
            return try animationadapter.AnimationAdapterProtoReader.init(buf);
        }
        return try animationadapter.AnimationAdapterProtoReader.init(&[_]u8{});
    }
};
