// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const surfaceanimator = @import("surfaceanimator.proto.zig");
// structs
const WindowContainerThumbnailProtoWire = struct {
    const WIDTH_WIRE: gremlin.ProtoWireNumber = 1;
    const HEIGHT_WIRE: gremlin.ProtoWireNumber = 2;
    const SURFACE_ANIMATOR_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const WindowContainerThumbnailProto = struct {
    // fields
    width: i32 = 0,
    height: i32 = 0,
    surface_animator: ?surfaceanimator.SurfaceAnimatorProto = null,
    pub fn calcProtobufSize(self: *const WindowContainerThumbnailProto) usize {
        var res: usize = 0;
        if (self.width != 0) {
            res += gremlin.sizes.sizeWireNumber(WindowContainerThumbnailProtoWire.WIDTH_WIRE) + gremlin.sizes.sizeI32(self.width);
        }
        if (self.height != 0) {
            res += gremlin.sizes.sizeWireNumber(WindowContainerThumbnailProtoWire.HEIGHT_WIRE) + gremlin.sizes.sizeI32(self.height);
        }
        if (self.surface_animator) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(WindowContainerThumbnailProtoWire.SURFACE_ANIMATOR_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const WindowContainerThumbnailProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const WindowContainerThumbnailProto, target: *gremlin.Writer) void {
        if (self.width != 0) {
            target.appendInt32(WindowContainerThumbnailProtoWire.WIDTH_WIRE, self.width);
        }
        if (self.height != 0) {
            target.appendInt32(WindowContainerThumbnailProtoWire.HEIGHT_WIRE, self.height);
        }
        if (self.surface_animator) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(WindowContainerThumbnailProtoWire.SURFACE_ANIMATOR_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const WindowContainerThumbnailProtoReader = struct {
    buf: gremlin.Reader,
    _width: i32 = 0,
    _height: i32 = 0,
    _surface_animator_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!WindowContainerThumbnailProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = WindowContainerThumbnailProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                WindowContainerThumbnailProtoWire.WIDTH_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._width = result.value;
                },
                WindowContainerThumbnailProtoWire.HEIGHT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._height = result.value;
                },
                WindowContainerThumbnailProtoWire.SURFACE_ANIMATOR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._surface_animator_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getWidth(self: *const WindowContainerThumbnailProtoReader) i32 {
        return self._width;
    }
    pub inline fn getHeight(self: *const WindowContainerThumbnailProtoReader) i32 {
        return self._height;
    }
    pub fn getSurfaceAnimator(self: *const WindowContainerThumbnailProtoReader) gremlin.Error!surfaceanimator.SurfaceAnimatorProtoReader {
        if (self._surface_animator_buf) |buf| {
            return try surfaceanimator.SurfaceAnimatorProtoReader.init(buf);
        }
        return try surfaceanimator.SurfaceAnimatorProtoReader.init(&[_]u8{});
    }
};
