// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const trace_packet = @import("trace_packet.proto.zig");
// structs
const TraceWire = struct {
    const PACKET_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const Trace = struct {
    // fields
    packet: ?[]const ?trace_packet.TracePacket = null,
    pub fn calcProtobufSize(self: *const Trace) usize {
        var res: usize = 0;
        if (self.packet) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(TraceWire.PACKET_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const Trace, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const Trace, target: *gremlin.Writer) void {
        if (self.packet) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(TraceWire.PACKET_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(TraceWire.PACKET_WIRE, 0);
                }
            }
        }
    }
};
pub const TraceReader = struct {
    buf: gremlin.Reader,
    _packet_offset: ?usize = null,
    _packet_last_offset: ?usize = null,
    _packet_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!TraceReader {
        const buf = gremlin.Reader.init(src);
        var res = TraceReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TraceWire.PACKET_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._packet_offset == null) {
                        res._packet_offset = offset - result.size;
                    }
                    res._packet_last_offset = offset;
                    res._packet_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn packetCount(self: *const TraceReader) usize {
        return self._packet_cnt;
    }
    pub fn packetNext(self: *TraceReader) ?trace_packet.TracePacketReader {
        if (self._packet_offset == null) return null;
        const current_offset = self._packet_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = trace_packet.TracePacketReader.init(result.value) catch return null;
        if (self._packet_last_offset != null and current_offset >= self._packet_last_offset.?) {
            self._packet_offset = null;
            return msg;
        }
        if (self._packet_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._packet_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == TraceWire.PACKET_WIRE) {
                self._packet_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._packet_offset = null;
        return msg;
    }
};
