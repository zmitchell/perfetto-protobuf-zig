// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeTriggerWire = struct {
    const TRIGGER_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const TRIGGER_NAME_HASH_WIRE: gremlin.ProtoWireNumber = 2;
    const FLOW_ID_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ChromeTrigger = struct {
    // fields
    trigger_name: ?[]const u8 = null,
    trigger_name_hash: u32 = 0,
    flow_id: u64 = 0,
    pub fn calcProtobufSize(self: *const ChromeTrigger) usize {
        var res: usize = 0;
        if (self.trigger_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeTriggerWire.TRIGGER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.trigger_name_hash != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeTriggerWire.TRIGGER_NAME_HASH_WIRE) + gremlin.sizes.sizeFixed32(self.trigger_name_hash);
        }
        if (self.flow_id != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeTriggerWire.FLOW_ID_WIRE) + gremlin.sizes.sizeFixed64(self.flow_id);
        }
        return res;
    }
    pub fn encode(self: *const ChromeTrigger, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeTrigger, target: *gremlin.Writer) void {
        if (self.trigger_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ChromeTriggerWire.TRIGGER_NAME_WIRE, v);
            }
        }
        if (self.trigger_name_hash != 0) {
            target.appendFixed32(ChromeTriggerWire.TRIGGER_NAME_HASH_WIRE, self.trigger_name_hash);
        }
        if (self.flow_id != 0) {
            target.appendFixed64(ChromeTriggerWire.FLOW_ID_WIRE, self.flow_id);
        }
    }
};
pub const ChromeTriggerReader = struct {
    buf: gremlin.Reader,
    _trigger_name: ?[]const u8 = null,
    _trigger_name_hash: u32 = 0,
    _flow_id: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeTriggerReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeTriggerReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeTriggerWire.TRIGGER_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._trigger_name = result.value;
                },
                ChromeTriggerWire.TRIGGER_NAME_HASH_WIRE => {
                    const result = try buf.readFixed32(offset);
                    offset += result.size;
                    res._trigger_name_hash = result.value;
                },
                ChromeTriggerWire.FLOW_ID_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._flow_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTriggerName(self: *const ChromeTriggerReader) []const u8 {
        return self._trigger_name orelse &[_]u8{};
    }
    pub inline fn getTriggerNameHash(self: *const ChromeTriggerReader) u32 {
        return self._trigger_name_hash;
    }
    pub inline fn getFlowId(self: *const ChromeTriggerReader) u64 {
        return self._flow_id;
    }
};
