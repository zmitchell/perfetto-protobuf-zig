// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeBenchmarkMetadataWire = struct {
    const BENCHMARK_START_TIME_US_WIRE: gremlin.ProtoWireNumber = 1;
    const STORY_RUN_TIME_US_WIRE: gremlin.ProtoWireNumber = 2;
    const BENCHMARK_NAME_WIRE: gremlin.ProtoWireNumber = 3;
    const BENCHMARK_DESCRIPTION_WIRE: gremlin.ProtoWireNumber = 4;
    const LABEL_WIRE: gremlin.ProtoWireNumber = 5;
    const STORY_NAME_WIRE: gremlin.ProtoWireNumber = 6;
    const STORY_TAGS_WIRE: gremlin.ProtoWireNumber = 7;
    const STORY_RUN_INDEX_WIRE: gremlin.ProtoWireNumber = 8;
    const HAD_FAILURES_WIRE: gremlin.ProtoWireNumber = 9;
};
pub const ChromeBenchmarkMetadata = struct {
    // fields
    benchmark_start_time_us: i64 = 0,
    story_run_time_us: i64 = 0,
    benchmark_name: ?[]const u8 = null,
    benchmark_description: ?[]const u8 = null,
    label: ?[]const u8 = null,
    story_name: ?[]const u8 = null,
    story_tags: ?[]const ?[]const u8 = null,
    story_run_index: i32 = 0,
    had_failures: bool = false,
    pub fn calcProtobufSize(self: *const ChromeBenchmarkMetadata) usize {
        var res: usize = 0;
        if (self.benchmark_start_time_us != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.BENCHMARK_START_TIME_US_WIRE) + gremlin.sizes.sizeI64(self.benchmark_start_time_us);
        }
        if (self.story_run_time_us != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.STORY_RUN_TIME_US_WIRE) + gremlin.sizes.sizeI64(self.story_run_time_us);
        }
        if (self.benchmark_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.BENCHMARK_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.benchmark_description) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.BENCHMARK_DESCRIPTION_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.label) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.LABEL_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.story_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.STORY_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.story_tags) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.STORY_TAGS_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.story_run_index != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.STORY_RUN_INDEX_WIRE) + gremlin.sizes.sizeI32(self.story_run_index);
        }
        if (self.had_failures != false) {
            res += gremlin.sizes.sizeWireNumber(ChromeBenchmarkMetadataWire.HAD_FAILURES_WIRE) + gremlin.sizes.sizeBool(self.had_failures);
        }
        return res;
    }
    pub fn encode(self: *const ChromeBenchmarkMetadata, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeBenchmarkMetadata, target: *gremlin.Writer) void {
        if (self.benchmark_start_time_us != 0) {
            target.appendInt64(ChromeBenchmarkMetadataWire.BENCHMARK_START_TIME_US_WIRE, self.benchmark_start_time_us);
        }
        if (self.story_run_time_us != 0) {
            target.appendInt64(ChromeBenchmarkMetadataWire.STORY_RUN_TIME_US_WIRE, self.story_run_time_us);
        }
        if (self.benchmark_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ChromeBenchmarkMetadataWire.BENCHMARK_NAME_WIRE, v);
            }
        }
        if (self.benchmark_description) |v| {
            if (v.len > 0) {
                target.appendBytes(ChromeBenchmarkMetadataWire.BENCHMARK_DESCRIPTION_WIRE, v);
            }
        }
        if (self.label) |v| {
            if (v.len > 0) {
                target.appendBytes(ChromeBenchmarkMetadataWire.LABEL_WIRE, v);
            }
        }
        if (self.story_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ChromeBenchmarkMetadataWire.STORY_NAME_WIRE, v);
            }
        }
        if (self.story_tags) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(ChromeBenchmarkMetadataWire.STORY_TAGS_WIRE, v);
                } else {
                    target.appendBytesTag(ChromeBenchmarkMetadataWire.STORY_TAGS_WIRE, 0);
                }
            }
        }
        if (self.story_run_index != 0) {
            target.appendInt32(ChromeBenchmarkMetadataWire.STORY_RUN_INDEX_WIRE, self.story_run_index);
        }
        if (self.had_failures != false) {
            target.appendBool(ChromeBenchmarkMetadataWire.HAD_FAILURES_WIRE, self.had_failures);
        }
    }
};
pub const ChromeBenchmarkMetadataReader = struct {
    buf: gremlin.Reader,
    _benchmark_start_time_us: i64 = 0,
    _story_run_time_us: i64 = 0,
    _benchmark_name: ?[]const u8 = null,
    _benchmark_description: ?[]const u8 = null,
    _label: ?[]const u8 = null,
    _story_name: ?[]const u8 = null,
    _story_tags_offset: ?usize = null,
    _story_tags_last_offset: ?usize = null,
    _story_tags_cnt: usize = 0,
    _story_run_index: i32 = 0,
    _had_failures: bool = false,
    pub fn init(src: []const u8) gremlin.Error!ChromeBenchmarkMetadataReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeBenchmarkMetadataReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeBenchmarkMetadataWire.BENCHMARK_START_TIME_US_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._benchmark_start_time_us = result.value;
                },
                ChromeBenchmarkMetadataWire.STORY_RUN_TIME_US_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._story_run_time_us = result.value;
                },
                ChromeBenchmarkMetadataWire.BENCHMARK_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._benchmark_name = result.value;
                },
                ChromeBenchmarkMetadataWire.BENCHMARK_DESCRIPTION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._benchmark_description = result.value;
                },
                ChromeBenchmarkMetadataWire.LABEL_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._label = result.value;
                },
                ChromeBenchmarkMetadataWire.STORY_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._story_name = result.value;
                },
                ChromeBenchmarkMetadataWire.STORY_TAGS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._story_tags_offset == null) {
                        res._story_tags_offset = offset - result.size;
                    }
                    res._story_tags_last_offset = offset;
                    res._story_tags_cnt += 1;
                },
                ChromeBenchmarkMetadataWire.STORY_RUN_INDEX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._story_run_index = result.value;
                },
                ChromeBenchmarkMetadataWire.HAD_FAILURES_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._had_failures = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBenchmarkStartTimeUs(self: *const ChromeBenchmarkMetadataReader) i64 {
        return self._benchmark_start_time_us;
    }
    pub inline fn getStoryRunTimeUs(self: *const ChromeBenchmarkMetadataReader) i64 {
        return self._story_run_time_us;
    }
    pub inline fn getBenchmarkName(self: *const ChromeBenchmarkMetadataReader) []const u8 {
        return self._benchmark_name orelse &[_]u8{};
    }
    pub inline fn getBenchmarkDescription(self: *const ChromeBenchmarkMetadataReader) []const u8 {
        return self._benchmark_description orelse &[_]u8{};
    }
    pub inline fn getLabel(self: *const ChromeBenchmarkMetadataReader) []const u8 {
        return self._label orelse &[_]u8{};
    }
    pub inline fn getStoryName(self: *const ChromeBenchmarkMetadataReader) []const u8 {
        return self._story_name orelse &[_]u8{};
    }
    pub fn storyTagsCount(self: *const ChromeBenchmarkMetadataReader) usize {
        return self._story_tags_cnt;
    }
    pub fn storyTagsNext(self: *ChromeBenchmarkMetadataReader) ?[]const u8 {
        if (self._story_tags_offset == null) return null;
        const current_offset = self._story_tags_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._story_tags_last_offset != null and current_offset >= self._story_tags_last_offset.?) {
            self._story_tags_offset = null;
            return result.value;
        }
        if (self._story_tags_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._story_tags_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ChromeBenchmarkMetadataWire.STORY_TAGS_WIRE) {
                self._story_tags_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._story_tags_offset = null;
        return result.value;
    }
    pub inline fn getStoryRunIndex(self: *const ChromeBenchmarkMetadataReader) i32 {
        return self._story_run_index;
    }
    pub inline fn getHadFailures(self: *const ChromeBenchmarkMetadataReader) bool {
        return self._had_failures;
    }
};
