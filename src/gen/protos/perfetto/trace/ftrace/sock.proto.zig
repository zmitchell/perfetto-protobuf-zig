// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const InetSockSetStateFtraceEventWire = struct {
    const DADDR_WIRE: gremlin.ProtoWireNumber = 1;
    const DPORT_WIRE: gremlin.ProtoWireNumber = 2;
    const FAMILY_WIRE: gremlin.ProtoWireNumber = 3;
    const NEWSTATE_WIRE: gremlin.ProtoWireNumber = 4;
    const OLDSTATE_WIRE: gremlin.ProtoWireNumber = 5;
    const PROTOCOL_WIRE: gremlin.ProtoWireNumber = 6;
    const SADDR_WIRE: gremlin.ProtoWireNumber = 7;
    const SKADDR_WIRE: gremlin.ProtoWireNumber = 8;
    const SPORT_WIRE: gremlin.ProtoWireNumber = 9;
};
pub const InetSockSetStateFtraceEvent = struct {
    // fields
    daddr: u32 = 0,
    dport: u32 = 0,
    family: u32 = 0,
    newstate: i32 = 0,
    oldstate: i32 = 0,
    protocol: u32 = 0,
    saddr: u32 = 0,
    skaddr: u64 = 0,
    sport: u32 = 0,
    pub fn calcProtobufSize(self: *const InetSockSetStateFtraceEvent) usize {
        var res: usize = 0;
        if (self.daddr != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.DADDR_WIRE) + gremlin.sizes.sizeU32(self.daddr);
        }
        if (self.dport != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.DPORT_WIRE) + gremlin.sizes.sizeU32(self.dport);
        }
        if (self.family != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.FAMILY_WIRE) + gremlin.sizes.sizeU32(self.family);
        }
        if (self.newstate != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.NEWSTATE_WIRE) + gremlin.sizes.sizeI32(self.newstate);
        }
        if (self.oldstate != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.OLDSTATE_WIRE) + gremlin.sizes.sizeI32(self.oldstate);
        }
        if (self.protocol != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.PROTOCOL_WIRE) + gremlin.sizes.sizeU32(self.protocol);
        }
        if (self.saddr != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.SADDR_WIRE) + gremlin.sizes.sizeU32(self.saddr);
        }
        if (self.skaddr != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.SKADDR_WIRE) + gremlin.sizes.sizeU64(self.skaddr);
        }
        if (self.sport != 0) {
            res += gremlin.sizes.sizeWireNumber(InetSockSetStateFtraceEventWire.SPORT_WIRE) + gremlin.sizes.sizeU32(self.sport);
        }
        return res;
    }
    pub fn encode(self: *const InetSockSetStateFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InetSockSetStateFtraceEvent, target: *gremlin.Writer) void {
        if (self.daddr != 0) {
            target.appendUint32(InetSockSetStateFtraceEventWire.DADDR_WIRE, self.daddr);
        }
        if (self.dport != 0) {
            target.appendUint32(InetSockSetStateFtraceEventWire.DPORT_WIRE, self.dport);
        }
        if (self.family != 0) {
            target.appendUint32(InetSockSetStateFtraceEventWire.FAMILY_WIRE, self.family);
        }
        if (self.newstate != 0) {
            target.appendInt32(InetSockSetStateFtraceEventWire.NEWSTATE_WIRE, self.newstate);
        }
        if (self.oldstate != 0) {
            target.appendInt32(InetSockSetStateFtraceEventWire.OLDSTATE_WIRE, self.oldstate);
        }
        if (self.protocol != 0) {
            target.appendUint32(InetSockSetStateFtraceEventWire.PROTOCOL_WIRE, self.protocol);
        }
        if (self.saddr != 0) {
            target.appendUint32(InetSockSetStateFtraceEventWire.SADDR_WIRE, self.saddr);
        }
        if (self.skaddr != 0) {
            target.appendUint64(InetSockSetStateFtraceEventWire.SKADDR_WIRE, self.skaddr);
        }
        if (self.sport != 0) {
            target.appendUint32(InetSockSetStateFtraceEventWire.SPORT_WIRE, self.sport);
        }
    }
};
pub const InetSockSetStateFtraceEventReader = struct {
    buf: gremlin.Reader,
    _daddr: u32 = 0,
    _dport: u32 = 0,
    _family: u32 = 0,
    _newstate: i32 = 0,
    _oldstate: i32 = 0,
    _protocol: u32 = 0,
    _saddr: u32 = 0,
    _skaddr: u64 = 0,
    _sport: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!InetSockSetStateFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = InetSockSetStateFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InetSockSetStateFtraceEventWire.DADDR_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._daddr = result.value;
                },
                InetSockSetStateFtraceEventWire.DPORT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._dport = result.value;
                },
                InetSockSetStateFtraceEventWire.FAMILY_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._family = result.value;
                },
                InetSockSetStateFtraceEventWire.NEWSTATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._newstate = result.value;
                },
                InetSockSetStateFtraceEventWire.OLDSTATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._oldstate = result.value;
                },
                InetSockSetStateFtraceEventWire.PROTOCOL_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._protocol = result.value;
                },
                InetSockSetStateFtraceEventWire.SADDR_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._saddr = result.value;
                },
                InetSockSetStateFtraceEventWire.SKADDR_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._skaddr = result.value;
                },
                InetSockSetStateFtraceEventWire.SPORT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._sport = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDaddr(self: *const InetSockSetStateFtraceEventReader) u32 {
        return self._daddr;
    }
    pub inline fn getDport(self: *const InetSockSetStateFtraceEventReader) u32 {
        return self._dport;
    }
    pub inline fn getFamily(self: *const InetSockSetStateFtraceEventReader) u32 {
        return self._family;
    }
    pub inline fn getNewstate(self: *const InetSockSetStateFtraceEventReader) i32 {
        return self._newstate;
    }
    pub inline fn getOldstate(self: *const InetSockSetStateFtraceEventReader) i32 {
        return self._oldstate;
    }
    pub inline fn getProtocol(self: *const InetSockSetStateFtraceEventReader) u32 {
        return self._protocol;
    }
    pub inline fn getSaddr(self: *const InetSockSetStateFtraceEventReader) u32 {
        return self._saddr;
    }
    pub inline fn getSkaddr(self: *const InetSockSetStateFtraceEventReader) u64 {
        return self._skaddr;
    }
    pub inline fn getSport(self: *const InetSockSetStateFtraceEventReader) u32 {
        return self._sport;
    }
};
