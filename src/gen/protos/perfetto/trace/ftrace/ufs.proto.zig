// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const UfshcdCommandFtraceEventWire = struct {
    const DEV_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const DOORBELL_WIRE: gremlin.ProtoWireNumber = 2;
    const INTR_WIRE: gremlin.ProtoWireNumber = 3;
    const LBA_WIRE: gremlin.ProtoWireNumber = 4;
    const OPCODE_WIRE: gremlin.ProtoWireNumber = 5;
    const STR_WIRE: gremlin.ProtoWireNumber = 6;
    const TAG_WIRE: gremlin.ProtoWireNumber = 7;
    const TRANSFER_LEN_WIRE: gremlin.ProtoWireNumber = 8;
    const GROUP_ID_WIRE: gremlin.ProtoWireNumber = 9;
    const STR_T_WIRE: gremlin.ProtoWireNumber = 10;
};
pub const UfshcdCommandFtraceEvent = struct {
    // fields
    dev_name: ?[]const u8 = null,
    doorbell: u32 = 0,
    intr: u32 = 0,
    lba: u64 = 0,
    opcode: u32 = 0,
    str: ?[]const u8 = null,
    tag: u32 = 0,
    transfer_len: i32 = 0,
    group_id: u32 = 0,
    str_t: u32 = 0,
    pub fn calcProtobufSize(self: *const UfshcdCommandFtraceEvent) usize {
        var res: usize = 0;
        if (self.dev_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.DEV_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.doorbell != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.DOORBELL_WIRE) + gremlin.sizes.sizeU32(self.doorbell);
        }
        if (self.intr != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.INTR_WIRE) + gremlin.sizes.sizeU32(self.intr);
        }
        if (self.lba != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.LBA_WIRE) + gremlin.sizes.sizeU64(self.lba);
        }
        if (self.opcode != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.OPCODE_WIRE) + gremlin.sizes.sizeU32(self.opcode);
        }
        if (self.str) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.STR_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.tag != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.TAG_WIRE) + gremlin.sizes.sizeU32(self.tag);
        }
        if (self.transfer_len != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.TRANSFER_LEN_WIRE) + gremlin.sizes.sizeI32(self.transfer_len);
        }
        if (self.group_id != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.GROUP_ID_WIRE) + gremlin.sizes.sizeU32(self.group_id);
        }
        if (self.str_t != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdCommandFtraceEventWire.STR_T_WIRE) + gremlin.sizes.sizeU32(self.str_t);
        }
        return res;
    }
    pub fn encode(self: *const UfshcdCommandFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const UfshcdCommandFtraceEvent, target: *gremlin.Writer) void {
        if (self.dev_name) |v| {
            if (v.len > 0) {
                target.appendBytes(UfshcdCommandFtraceEventWire.DEV_NAME_WIRE, v);
            }
        }
        if (self.doorbell != 0) {
            target.appendUint32(UfshcdCommandFtraceEventWire.DOORBELL_WIRE, self.doorbell);
        }
        if (self.intr != 0) {
            target.appendUint32(UfshcdCommandFtraceEventWire.INTR_WIRE, self.intr);
        }
        if (self.lba != 0) {
            target.appendUint64(UfshcdCommandFtraceEventWire.LBA_WIRE, self.lba);
        }
        if (self.opcode != 0) {
            target.appendUint32(UfshcdCommandFtraceEventWire.OPCODE_WIRE, self.opcode);
        }
        if (self.str) |v| {
            if (v.len > 0) {
                target.appendBytes(UfshcdCommandFtraceEventWire.STR_WIRE, v);
            }
        }
        if (self.tag != 0) {
            target.appendUint32(UfshcdCommandFtraceEventWire.TAG_WIRE, self.tag);
        }
        if (self.transfer_len != 0) {
            target.appendInt32(UfshcdCommandFtraceEventWire.TRANSFER_LEN_WIRE, self.transfer_len);
        }
        if (self.group_id != 0) {
            target.appendUint32(UfshcdCommandFtraceEventWire.GROUP_ID_WIRE, self.group_id);
        }
        if (self.str_t != 0) {
            target.appendUint32(UfshcdCommandFtraceEventWire.STR_T_WIRE, self.str_t);
        }
    }
};
pub const UfshcdCommandFtraceEventReader = struct {
    buf: gremlin.Reader,
    _dev_name: ?[]const u8 = null,
    _doorbell: u32 = 0,
    _intr: u32 = 0,
    _lba: u64 = 0,
    _opcode: u32 = 0,
    _str: ?[]const u8 = null,
    _tag: u32 = 0,
    _transfer_len: i32 = 0,
    _group_id: u32 = 0,
    _str_t: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!UfshcdCommandFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = UfshcdCommandFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                UfshcdCommandFtraceEventWire.DEV_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._dev_name = result.value;
                },
                UfshcdCommandFtraceEventWire.DOORBELL_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._doorbell = result.value;
                },
                UfshcdCommandFtraceEventWire.INTR_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._intr = result.value;
                },
                UfshcdCommandFtraceEventWire.LBA_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._lba = result.value;
                },
                UfshcdCommandFtraceEventWire.OPCODE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._opcode = result.value;
                },
                UfshcdCommandFtraceEventWire.STR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._str = result.value;
                },
                UfshcdCommandFtraceEventWire.TAG_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._tag = result.value;
                },
                UfshcdCommandFtraceEventWire.TRANSFER_LEN_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._transfer_len = result.value;
                },
                UfshcdCommandFtraceEventWire.GROUP_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._group_id = result.value;
                },
                UfshcdCommandFtraceEventWire.STR_T_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._str_t = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDevName(self: *const UfshcdCommandFtraceEventReader) []const u8 {
        return self._dev_name orelse &[_]u8{};
    }
    pub inline fn getDoorbell(self: *const UfshcdCommandFtraceEventReader) u32 {
        return self._doorbell;
    }
    pub inline fn getIntr(self: *const UfshcdCommandFtraceEventReader) u32 {
        return self._intr;
    }
    pub inline fn getLba(self: *const UfshcdCommandFtraceEventReader) u64 {
        return self._lba;
    }
    pub inline fn getOpcode(self: *const UfshcdCommandFtraceEventReader) u32 {
        return self._opcode;
    }
    pub inline fn getStr(self: *const UfshcdCommandFtraceEventReader) []const u8 {
        return self._str orelse &[_]u8{};
    }
    pub inline fn getTag(self: *const UfshcdCommandFtraceEventReader) u32 {
        return self._tag;
    }
    pub inline fn getTransferLen(self: *const UfshcdCommandFtraceEventReader) i32 {
        return self._transfer_len;
    }
    pub inline fn getGroupId(self: *const UfshcdCommandFtraceEventReader) u32 {
        return self._group_id;
    }
    pub inline fn getStrT(self: *const UfshcdCommandFtraceEventReader) u32 {
        return self._str_t;
    }
};
const UfshcdClkGatingFtraceEventWire = struct {
    const DEV_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const STATE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const UfshcdClkGatingFtraceEvent = struct {
    // fields
    dev_name: ?[]const u8 = null,
    state: i32 = 0,
    pub fn calcProtobufSize(self: *const UfshcdClkGatingFtraceEvent) usize {
        var res: usize = 0;
        if (self.dev_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(UfshcdClkGatingFtraceEventWire.DEV_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.state != 0) {
            res += gremlin.sizes.sizeWireNumber(UfshcdClkGatingFtraceEventWire.STATE_WIRE) + gremlin.sizes.sizeI32(self.state);
        }
        return res;
    }
    pub fn encode(self: *const UfshcdClkGatingFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const UfshcdClkGatingFtraceEvent, target: *gremlin.Writer) void {
        if (self.dev_name) |v| {
            if (v.len > 0) {
                target.appendBytes(UfshcdClkGatingFtraceEventWire.DEV_NAME_WIRE, v);
            }
        }
        if (self.state != 0) {
            target.appendInt32(UfshcdClkGatingFtraceEventWire.STATE_WIRE, self.state);
        }
    }
};
pub const UfshcdClkGatingFtraceEventReader = struct {
    buf: gremlin.Reader,
    _dev_name: ?[]const u8 = null,
    _state: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!UfshcdClkGatingFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = UfshcdClkGatingFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                UfshcdClkGatingFtraceEventWire.DEV_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._dev_name = result.value;
                },
                UfshcdClkGatingFtraceEventWire.STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._state = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDevName(self: *const UfshcdClkGatingFtraceEventReader) []const u8 {
        return self._dev_name orelse &[_]u8{};
    }
    pub inline fn getState(self: *const UfshcdClkGatingFtraceEventReader) i32 {
        return self._state;
    }
};
