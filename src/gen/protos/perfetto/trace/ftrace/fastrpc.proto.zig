// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const FastrpcDmaStatFtraceEventWire = struct {
    const CID_WIRE: gremlin.ProtoWireNumber = 1;
    const LEN_WIRE: gremlin.ProtoWireNumber = 2;
    const TOTAL_ALLOCATED_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const FastrpcDmaStatFtraceEvent = struct {
    // fields
    cid: i32 = 0,
    len: i64 = 0,
    total_allocated: u64 = 0,
    pub fn calcProtobufSize(self: *const FastrpcDmaStatFtraceEvent) usize {
        var res: usize = 0;
        if (self.cid != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaStatFtraceEventWire.CID_WIRE) + gremlin.sizes.sizeI32(self.cid);
        }
        if (self.len != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaStatFtraceEventWire.LEN_WIRE) + gremlin.sizes.sizeI64(self.len);
        }
        if (self.total_allocated != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaStatFtraceEventWire.TOTAL_ALLOCATED_WIRE) + gremlin.sizes.sizeU64(self.total_allocated);
        }
        return res;
    }
    pub fn encode(self: *const FastrpcDmaStatFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FastrpcDmaStatFtraceEvent, target: *gremlin.Writer) void {
        if (self.cid != 0) {
            target.appendInt32(FastrpcDmaStatFtraceEventWire.CID_WIRE, self.cid);
        }
        if (self.len != 0) {
            target.appendInt64(FastrpcDmaStatFtraceEventWire.LEN_WIRE, self.len);
        }
        if (self.total_allocated != 0) {
            target.appendUint64(FastrpcDmaStatFtraceEventWire.TOTAL_ALLOCATED_WIRE, self.total_allocated);
        }
    }
};
pub const FastrpcDmaStatFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cid: i32 = 0,
    _len: i64 = 0,
    _total_allocated: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!FastrpcDmaStatFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FastrpcDmaStatFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FastrpcDmaStatFtraceEventWire.CID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._cid = result.value;
                },
                FastrpcDmaStatFtraceEventWire.LEN_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._len = result.value;
                },
                FastrpcDmaStatFtraceEventWire.TOTAL_ALLOCATED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._total_allocated = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCid(self: *const FastrpcDmaStatFtraceEventReader) i32 {
        return self._cid;
    }
    pub inline fn getLen(self: *const FastrpcDmaStatFtraceEventReader) i64 {
        return self._len;
    }
    pub inline fn getTotalAllocated(self: *const FastrpcDmaStatFtraceEventReader) u64 {
        return self._total_allocated;
    }
};
const FastrpcDmaFreeFtraceEventWire = struct {
    const CID_WIRE: gremlin.ProtoWireNumber = 1;
    const PHYS_WIRE: gremlin.ProtoWireNumber = 2;
    const SIZE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const FastrpcDmaFreeFtraceEvent = struct {
    // fields
    cid: i32 = 0,
    phys: u64 = 0,
    size: u64 = 0,
    pub fn calcProtobufSize(self: *const FastrpcDmaFreeFtraceEvent) usize {
        var res: usize = 0;
        if (self.cid != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaFreeFtraceEventWire.CID_WIRE) + gremlin.sizes.sizeI32(self.cid);
        }
        if (self.phys != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaFreeFtraceEventWire.PHYS_WIRE) + gremlin.sizes.sizeU64(self.phys);
        }
        if (self.size != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaFreeFtraceEventWire.SIZE_WIRE) + gremlin.sizes.sizeU64(self.size);
        }
        return res;
    }
    pub fn encode(self: *const FastrpcDmaFreeFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FastrpcDmaFreeFtraceEvent, target: *gremlin.Writer) void {
        if (self.cid != 0) {
            target.appendInt32(FastrpcDmaFreeFtraceEventWire.CID_WIRE, self.cid);
        }
        if (self.phys != 0) {
            target.appendUint64(FastrpcDmaFreeFtraceEventWire.PHYS_WIRE, self.phys);
        }
        if (self.size != 0) {
            target.appendUint64(FastrpcDmaFreeFtraceEventWire.SIZE_WIRE, self.size);
        }
    }
};
pub const FastrpcDmaFreeFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cid: i32 = 0,
    _phys: u64 = 0,
    _size: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!FastrpcDmaFreeFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FastrpcDmaFreeFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FastrpcDmaFreeFtraceEventWire.CID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._cid = result.value;
                },
                FastrpcDmaFreeFtraceEventWire.PHYS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._phys = result.value;
                },
                FastrpcDmaFreeFtraceEventWire.SIZE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._size = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCid(self: *const FastrpcDmaFreeFtraceEventReader) i32 {
        return self._cid;
    }
    pub inline fn getPhys(self: *const FastrpcDmaFreeFtraceEventReader) u64 {
        return self._phys;
    }
    pub inline fn getSize(self: *const FastrpcDmaFreeFtraceEventReader) u64 {
        return self._size;
    }
};
const FastrpcDmaAllocFtraceEventWire = struct {
    const CID_WIRE: gremlin.ProtoWireNumber = 1;
    const PHYS_WIRE: gremlin.ProtoWireNumber = 2;
    const SIZE_WIRE: gremlin.ProtoWireNumber = 3;
    const ATTR_WIRE: gremlin.ProtoWireNumber = 4;
    const MFLAGS_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const FastrpcDmaAllocFtraceEvent = struct {
    // fields
    cid: i32 = 0,
    phys: u64 = 0,
    size: u64 = 0,
    attr: u64 = 0,
    mflags: i32 = 0,
    pub fn calcProtobufSize(self: *const FastrpcDmaAllocFtraceEvent) usize {
        var res: usize = 0;
        if (self.cid != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaAllocFtraceEventWire.CID_WIRE) + gremlin.sizes.sizeI32(self.cid);
        }
        if (self.phys != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaAllocFtraceEventWire.PHYS_WIRE) + gremlin.sizes.sizeU64(self.phys);
        }
        if (self.size != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaAllocFtraceEventWire.SIZE_WIRE) + gremlin.sizes.sizeU64(self.size);
        }
        if (self.attr != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaAllocFtraceEventWire.ATTR_WIRE) + gremlin.sizes.sizeU64(self.attr);
        }
        if (self.mflags != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaAllocFtraceEventWire.MFLAGS_WIRE) + gremlin.sizes.sizeI32(self.mflags);
        }
        return res;
    }
    pub fn encode(self: *const FastrpcDmaAllocFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FastrpcDmaAllocFtraceEvent, target: *gremlin.Writer) void {
        if (self.cid != 0) {
            target.appendInt32(FastrpcDmaAllocFtraceEventWire.CID_WIRE, self.cid);
        }
        if (self.phys != 0) {
            target.appendUint64(FastrpcDmaAllocFtraceEventWire.PHYS_WIRE, self.phys);
        }
        if (self.size != 0) {
            target.appendUint64(FastrpcDmaAllocFtraceEventWire.SIZE_WIRE, self.size);
        }
        if (self.attr != 0) {
            target.appendUint64(FastrpcDmaAllocFtraceEventWire.ATTR_WIRE, self.attr);
        }
        if (self.mflags != 0) {
            target.appendInt32(FastrpcDmaAllocFtraceEventWire.MFLAGS_WIRE, self.mflags);
        }
    }
};
pub const FastrpcDmaAllocFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cid: i32 = 0,
    _phys: u64 = 0,
    _size: u64 = 0,
    _attr: u64 = 0,
    _mflags: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!FastrpcDmaAllocFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FastrpcDmaAllocFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FastrpcDmaAllocFtraceEventWire.CID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._cid = result.value;
                },
                FastrpcDmaAllocFtraceEventWire.PHYS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._phys = result.value;
                },
                FastrpcDmaAllocFtraceEventWire.SIZE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._size = result.value;
                },
                FastrpcDmaAllocFtraceEventWire.ATTR_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._attr = result.value;
                },
                FastrpcDmaAllocFtraceEventWire.MFLAGS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._mflags = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCid(self: *const FastrpcDmaAllocFtraceEventReader) i32 {
        return self._cid;
    }
    pub inline fn getPhys(self: *const FastrpcDmaAllocFtraceEventReader) u64 {
        return self._phys;
    }
    pub inline fn getSize(self: *const FastrpcDmaAllocFtraceEventReader) u64 {
        return self._size;
    }
    pub inline fn getAttr(self: *const FastrpcDmaAllocFtraceEventReader) u64 {
        return self._attr;
    }
    pub inline fn getMflags(self: *const FastrpcDmaAllocFtraceEventReader) i32 {
        return self._mflags;
    }
};
const FastrpcDmaUnmapFtraceEventWire = struct {
    const CID_WIRE: gremlin.ProtoWireNumber = 1;
    const PHYS_WIRE: gremlin.ProtoWireNumber = 2;
    const SIZE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const FastrpcDmaUnmapFtraceEvent = struct {
    // fields
    cid: i32 = 0,
    phys: u64 = 0,
    size: u64 = 0,
    pub fn calcProtobufSize(self: *const FastrpcDmaUnmapFtraceEvent) usize {
        var res: usize = 0;
        if (self.cid != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaUnmapFtraceEventWire.CID_WIRE) + gremlin.sizes.sizeI32(self.cid);
        }
        if (self.phys != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaUnmapFtraceEventWire.PHYS_WIRE) + gremlin.sizes.sizeU64(self.phys);
        }
        if (self.size != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaUnmapFtraceEventWire.SIZE_WIRE) + gremlin.sizes.sizeU64(self.size);
        }
        return res;
    }
    pub fn encode(self: *const FastrpcDmaUnmapFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FastrpcDmaUnmapFtraceEvent, target: *gremlin.Writer) void {
        if (self.cid != 0) {
            target.appendInt32(FastrpcDmaUnmapFtraceEventWire.CID_WIRE, self.cid);
        }
        if (self.phys != 0) {
            target.appendUint64(FastrpcDmaUnmapFtraceEventWire.PHYS_WIRE, self.phys);
        }
        if (self.size != 0) {
            target.appendUint64(FastrpcDmaUnmapFtraceEventWire.SIZE_WIRE, self.size);
        }
    }
};
pub const FastrpcDmaUnmapFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cid: i32 = 0,
    _phys: u64 = 0,
    _size: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!FastrpcDmaUnmapFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FastrpcDmaUnmapFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FastrpcDmaUnmapFtraceEventWire.CID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._cid = result.value;
                },
                FastrpcDmaUnmapFtraceEventWire.PHYS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._phys = result.value;
                },
                FastrpcDmaUnmapFtraceEventWire.SIZE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._size = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCid(self: *const FastrpcDmaUnmapFtraceEventReader) i32 {
        return self._cid;
    }
    pub inline fn getPhys(self: *const FastrpcDmaUnmapFtraceEventReader) u64 {
        return self._phys;
    }
    pub inline fn getSize(self: *const FastrpcDmaUnmapFtraceEventReader) u64 {
        return self._size;
    }
};
const FastrpcDmaMapFtraceEventWire = struct {
    const CID_WIRE: gremlin.ProtoWireNumber = 1;
    const FD_WIRE: gremlin.ProtoWireNumber = 2;
    const PHYS_WIRE: gremlin.ProtoWireNumber = 3;
    const SIZE_WIRE: gremlin.ProtoWireNumber = 4;
    const LEN_WIRE: gremlin.ProtoWireNumber = 5;
    const ATTR_WIRE: gremlin.ProtoWireNumber = 6;
    const MFLAGS_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const FastrpcDmaMapFtraceEvent = struct {
    // fields
    cid: i32 = 0,
    fd: i32 = 0,
    phys: u64 = 0,
    size: u64 = 0,
    len: u64 = 0,
    attr: u32 = 0,
    mflags: i32 = 0,
    pub fn calcProtobufSize(self: *const FastrpcDmaMapFtraceEvent) usize {
        var res: usize = 0;
        if (self.cid != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaMapFtraceEventWire.CID_WIRE) + gremlin.sizes.sizeI32(self.cid);
        }
        if (self.fd != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaMapFtraceEventWire.FD_WIRE) + gremlin.sizes.sizeI32(self.fd);
        }
        if (self.phys != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaMapFtraceEventWire.PHYS_WIRE) + gremlin.sizes.sizeU64(self.phys);
        }
        if (self.size != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaMapFtraceEventWire.SIZE_WIRE) + gremlin.sizes.sizeU64(self.size);
        }
        if (self.len != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaMapFtraceEventWire.LEN_WIRE) + gremlin.sizes.sizeU64(self.len);
        }
        if (self.attr != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaMapFtraceEventWire.ATTR_WIRE) + gremlin.sizes.sizeU32(self.attr);
        }
        if (self.mflags != 0) {
            res += gremlin.sizes.sizeWireNumber(FastrpcDmaMapFtraceEventWire.MFLAGS_WIRE) + gremlin.sizes.sizeI32(self.mflags);
        }
        return res;
    }
    pub fn encode(self: *const FastrpcDmaMapFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FastrpcDmaMapFtraceEvent, target: *gremlin.Writer) void {
        if (self.cid != 0) {
            target.appendInt32(FastrpcDmaMapFtraceEventWire.CID_WIRE, self.cid);
        }
        if (self.fd != 0) {
            target.appendInt32(FastrpcDmaMapFtraceEventWire.FD_WIRE, self.fd);
        }
        if (self.phys != 0) {
            target.appendUint64(FastrpcDmaMapFtraceEventWire.PHYS_WIRE, self.phys);
        }
        if (self.size != 0) {
            target.appendUint64(FastrpcDmaMapFtraceEventWire.SIZE_WIRE, self.size);
        }
        if (self.len != 0) {
            target.appendUint64(FastrpcDmaMapFtraceEventWire.LEN_WIRE, self.len);
        }
        if (self.attr != 0) {
            target.appendUint32(FastrpcDmaMapFtraceEventWire.ATTR_WIRE, self.attr);
        }
        if (self.mflags != 0) {
            target.appendInt32(FastrpcDmaMapFtraceEventWire.MFLAGS_WIRE, self.mflags);
        }
    }
};
pub const FastrpcDmaMapFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cid: i32 = 0,
    _fd: i32 = 0,
    _phys: u64 = 0,
    _size: u64 = 0,
    _len: u64 = 0,
    _attr: u32 = 0,
    _mflags: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!FastrpcDmaMapFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FastrpcDmaMapFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FastrpcDmaMapFtraceEventWire.CID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._cid = result.value;
                },
                FastrpcDmaMapFtraceEventWire.FD_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._fd = result.value;
                },
                FastrpcDmaMapFtraceEventWire.PHYS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._phys = result.value;
                },
                FastrpcDmaMapFtraceEventWire.SIZE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._size = result.value;
                },
                FastrpcDmaMapFtraceEventWire.LEN_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._len = result.value;
                },
                FastrpcDmaMapFtraceEventWire.ATTR_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._attr = result.value;
                },
                FastrpcDmaMapFtraceEventWire.MFLAGS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._mflags = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCid(self: *const FastrpcDmaMapFtraceEventReader) i32 {
        return self._cid;
    }
    pub inline fn getFd(self: *const FastrpcDmaMapFtraceEventReader) i32 {
        return self._fd;
    }
    pub inline fn getPhys(self: *const FastrpcDmaMapFtraceEventReader) u64 {
        return self._phys;
    }
    pub inline fn getSize(self: *const FastrpcDmaMapFtraceEventReader) u64 {
        return self._size;
    }
    pub inline fn getLen(self: *const FastrpcDmaMapFtraceEventReader) u64 {
        return self._len;
    }
    pub inline fn getAttr(self: *const FastrpcDmaMapFtraceEventReader) u32 {
        return self._attr;
    }
    pub inline fn getMflags(self: *const FastrpcDmaMapFtraceEventReader) i32 {
        return self._mflags;
    }
};
