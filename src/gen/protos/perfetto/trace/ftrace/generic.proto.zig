// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const GenericFtraceEventWire = struct {
    const EVENT_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const FIELD_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const GenericFtraceEvent = struct {
    // nested structs
    const FieldWire = struct {
        const NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const STR_VALUE_WIRE: gremlin.ProtoWireNumber = 3;
        const INT_VALUE_WIRE: gremlin.ProtoWireNumber = 4;
        const UINT_VALUE_WIRE: gremlin.ProtoWireNumber = 5;
    };
    pub const Field = struct {
        // fields
        name: ?[]const u8 = null,
        str_value: ?[]const u8 = null,
        int_value: i64 = 0,
        uint_value: u64 = 0,
        pub fn calcProtobufSize(self: *const GenericFtraceEvent.Field) usize {
            var res: usize = 0;
            if (self.name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(GenericFtraceEvent.FieldWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.str_value) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(GenericFtraceEvent.FieldWire.STR_VALUE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.int_value != 0) {
                res += gremlin.sizes.sizeWireNumber(GenericFtraceEvent.FieldWire.INT_VALUE_WIRE) + gremlin.sizes.sizeI64(self.int_value);
            }
            if (self.uint_value != 0) {
                res += gremlin.sizes.sizeWireNumber(GenericFtraceEvent.FieldWire.UINT_VALUE_WIRE) + gremlin.sizes.sizeU64(self.uint_value);
            }
            return res;
        }
        pub fn encode(self: *const GenericFtraceEvent.Field, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GenericFtraceEvent.Field, target: *gremlin.Writer) void {
            if (self.name) |v| {
                if (v.len > 0) {
                    target.appendBytes(GenericFtraceEvent.FieldWire.NAME_WIRE, v);
                }
            }
            if (self.str_value) |v| {
                if (v.len > 0) {
                    target.appendBytes(GenericFtraceEvent.FieldWire.STR_VALUE_WIRE, v);
                }
            }
            if (self.int_value != 0) {
                target.appendInt64(GenericFtraceEvent.FieldWire.INT_VALUE_WIRE, self.int_value);
            }
            if (self.uint_value != 0) {
                target.appendUint64(GenericFtraceEvent.FieldWire.UINT_VALUE_WIRE, self.uint_value);
            }
        }
    };
    pub const FieldReader = struct {
        buf: gremlin.Reader,
        _name: ?[]const u8 = null,
        _str_value: ?[]const u8 = null,
        _int_value: i64 = 0,
        _uint_value: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!GenericFtraceEvent.FieldReader {
            const buf = gremlin.Reader.init(src);
            var res = GenericFtraceEvent.FieldReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GenericFtraceEvent.FieldWire.NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._name = result.value;
                    },
                    GenericFtraceEvent.FieldWire.STR_VALUE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._str_value = result.value;
                    },
                    GenericFtraceEvent.FieldWire.INT_VALUE_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._int_value = result.value;
                    },
                    GenericFtraceEvent.FieldWire.UINT_VALUE_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._uint_value = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getName(self: *const GenericFtraceEvent.FieldReader) []const u8 {
            return self._name orelse &[_]u8{};
        }
        pub inline fn getStrValue(self: *const GenericFtraceEvent.FieldReader) []const u8 {
            return self._str_value orelse &[_]u8{};
        }
        pub inline fn getIntValue(self: *const GenericFtraceEvent.FieldReader) i64 {
            return self._int_value;
        }
        pub inline fn getUintValue(self: *const GenericFtraceEvent.FieldReader) u64 {
            return self._uint_value;
        }
    };
    // fields
    event_name: ?[]const u8 = null,
    field: ?[]const ?GenericFtraceEvent.Field = null,
    pub fn calcProtobufSize(self: *const GenericFtraceEvent) usize {
        var res: usize = 0;
        if (self.event_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(GenericFtraceEventWire.EVENT_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.field) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(GenericFtraceEventWire.FIELD_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const GenericFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GenericFtraceEvent, target: *gremlin.Writer) void {
        if (self.event_name) |v| {
            if (v.len > 0) {
                target.appendBytes(GenericFtraceEventWire.EVENT_NAME_WIRE, v);
            }
        }
        if (self.field) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(GenericFtraceEventWire.FIELD_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(GenericFtraceEventWire.FIELD_WIRE, 0);
                }
            }
        }
    }
};
pub const GenericFtraceEventReader = struct {
    buf: gremlin.Reader,
    _event_name: ?[]const u8 = null,
    _field_offset: ?usize = null,
    _field_last_offset: ?usize = null,
    _field_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!GenericFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = GenericFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GenericFtraceEventWire.EVENT_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._event_name = result.value;
                },
                GenericFtraceEventWire.FIELD_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._field_offset == null) {
                        res._field_offset = offset - result.size;
                    }
                    res._field_last_offset = offset;
                    res._field_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getEventName(self: *const GenericFtraceEventReader) []const u8 {
        return self._event_name orelse &[_]u8{};
    }
    pub fn fieldCount(self: *const GenericFtraceEventReader) usize {
        return self._field_cnt;
    }
    pub fn fieldNext(self: *GenericFtraceEventReader) ?GenericFtraceEvent.FieldReader {
        if (self._field_offset == null) return null;
        const current_offset = self._field_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = GenericFtraceEvent.FieldReader.init(result.value) catch return null;
        if (self._field_last_offset != null and current_offset >= self._field_last_offset.?) {
            self._field_offset = null;
            return msg;
        }
        if (self._field_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._field_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == GenericFtraceEventWire.FIELD_WIRE) {
                self._field_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._field_offset = null;
        return msg;
    }
};
const KprobeEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const KprobeEvent = struct {
    // nested enums
    pub const KprobeType = enum(i32) {
        KPROBE_TYPE_UNKNOWN = 0,
        KPROBE_TYPE_BEGIN = 1,
        KPROBE_TYPE_END = 2,
        KPROBE_TYPE_INSTANT = 3,
    };
    // fields
    name: ?[]const u8 = null,
    type: KprobeEvent.KprobeType = @enumFromInt(0),
    pub fn calcProtobufSize(self: *const KprobeEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(KprobeEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (@intFromEnum(self.type) != 0) {
            res += gremlin.sizes.sizeWireNumber(KprobeEventWire.TYPE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.type));
        }
        return res;
    }
    pub fn encode(self: *const KprobeEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const KprobeEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(KprobeEventWire.NAME_WIRE, v);
            }
        }
        if (@intFromEnum(self.type) != 0) {
            target.appendInt32(KprobeEventWire.TYPE_WIRE, @intFromEnum(self.type));
        }
    }
};
pub const KprobeEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _type: KprobeEvent.KprobeType = @enumFromInt(0),
    pub fn init(src: []const u8) gremlin.Error!KprobeEventReader {
        const buf = gremlin.Reader.init(src);
        var res = KprobeEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                KprobeEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                KprobeEventWire.TYPE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._type = @enumFromInt(result.value);
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const KprobeEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getType(self: *const KprobeEventReader) KprobeEvent.KprobeType {
        return self._type;
    }
};
