// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const SysEnterFtraceEventWire = struct {
    const ID_WIRE: gremlin.ProtoWireNumber = 1;
    const ARGS_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const SysEnterFtraceEvent = struct {
    // fields
    id: i64 = 0,
    args: ?[]const u64 = null,
    pub fn calcProtobufSize(self: *const SysEnterFtraceEvent) usize {
        var res: usize = 0;
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(SysEnterFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI64(self.id);
        }
        if (self.args) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(SysEnterFtraceEventWire.ARGS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                res += gremlin.sizes.sizeWireNumber(SysEnterFtraceEventWire.ARGS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        return res;
    }
    pub fn encode(self: *const SysEnterFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SysEnterFtraceEvent, target: *gremlin.Writer) void {
        if (self.id != 0) {
            target.appendInt64(SysEnterFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.args) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendUint64(SysEnterFtraceEventWire.ARGS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeU64(v);
                }
                target.appendBytesTag(SysEnterFtraceEventWire.ARGS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendUint64WithoutTag(v);
                }
            }
        }
    }
};
pub const SysEnterFtraceEventReader = struct {
    buf: gremlin.Reader,
    _id: i64 = 0,
    _args_offset: ?usize = null,
    _args_last_offset: ?usize = null,
    _args_packed: bool = false,
    pub fn init(src: []const u8) gremlin.Error!SysEnterFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = SysEnterFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SysEnterFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                SysEnterFtraceEventWire.ARGS_WIRE => {
                    if (res._args_offset == null) {
                        res._args_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._args_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._args_offset = offset + length_result.size;
                        res._args_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._args_last_offset.?;
                    } else {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._args_last_offset = offset;
                    }
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getId(self: *const SysEnterFtraceEventReader) i64 {
        return self._id;
    }
    pub fn argsNext(self: *SysEnterFtraceEventReader) gremlin.Error!?u64 {
        if (self._args_offset == null) return null;
        const current_offset = self._args_offset.?;
        if (current_offset >= self._args_last_offset.?) {
            self._args_offset = null;
            return null;
        }
        if (self._args_packed) {
            const value_result = try self.buf.readUInt64(current_offset);
            self._args_offset = current_offset + value_result.size;
            if (self._args_offset.? >= self._args_last_offset.?) {
                self._args_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readUInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._args_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == SysEnterFtraceEventWire.ARGS_WIRE) {
                    self._args_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._args_offset = null;
            return value_result.value;
        }
    }
};
const SysExitFtraceEventWire = struct {
    const ID_WIRE: gremlin.ProtoWireNumber = 1;
    const RET_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const SysExitFtraceEvent = struct {
    // fields
    id: i64 = 0,
    ret: i64 = 0,
    pub fn calcProtobufSize(self: *const SysExitFtraceEvent) usize {
        var res: usize = 0;
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(SysExitFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI64(self.id);
        }
        if (self.ret != 0) {
            res += gremlin.sizes.sizeWireNumber(SysExitFtraceEventWire.RET_WIRE) + gremlin.sizes.sizeI64(self.ret);
        }
        return res;
    }
    pub fn encode(self: *const SysExitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SysExitFtraceEvent, target: *gremlin.Writer) void {
        if (self.id != 0) {
            target.appendInt64(SysExitFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.ret != 0) {
            target.appendInt64(SysExitFtraceEventWire.RET_WIRE, self.ret);
        }
    }
};
pub const SysExitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _id: i64 = 0,
    _ret: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!SysExitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = SysExitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SysExitFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                SysExitFtraceEventWire.RET_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._ret = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getId(self: *const SysExitFtraceEventReader) i64 {
        return self._id;
    }
    pub inline fn getRet(self: *const SysExitFtraceEventReader) i64 {
        return self._ret;
    }
};
