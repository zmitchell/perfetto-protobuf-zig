// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ConsoleFtraceEventWire = struct {
    const MSG_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ConsoleFtraceEvent = struct {
    // fields
    msg: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ConsoleFtraceEvent) usize {
        var res: usize = 0;
        if (self.msg) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ConsoleFtraceEventWire.MSG_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const ConsoleFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ConsoleFtraceEvent, target: *gremlin.Writer) void {
        if (self.msg) |v| {
            if (v.len > 0) {
                target.appendBytes(ConsoleFtraceEventWire.MSG_WIRE, v);
            }
        }
    }
};
pub const ConsoleFtraceEventReader = struct {
    buf: gremlin.Reader,
    _msg: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ConsoleFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ConsoleFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ConsoleFtraceEventWire.MSG_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._msg = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getMsg(self: *const ConsoleFtraceEventReader) []const u8 {
        return self._msg orelse &[_]u8{};
    }
};
