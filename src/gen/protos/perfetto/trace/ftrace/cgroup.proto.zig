// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const CgroupAttachTaskFtraceEventWire = struct {
    const DST_ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const DST_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const PID_WIRE: gremlin.ProtoWireNumber = 3;
    const COMM_WIRE: gremlin.ProtoWireNumber = 4;
    const CNAME_WIRE: gremlin.ProtoWireNumber = 5;
    const DST_LEVEL_WIRE: gremlin.ProtoWireNumber = 6;
    const DST_PATH_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const CgroupAttachTaskFtraceEvent = struct {
    // fields
    dst_root: i32 = 0,
    dst_id: i32 = 0,
    pid: i32 = 0,
    comm: ?[]const u8 = null,
    cname: ?[]const u8 = null,
    dst_level: i32 = 0,
    dst_path: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupAttachTaskFtraceEvent) usize {
        var res: usize = 0;
        if (self.dst_root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupAttachTaskFtraceEventWire.DST_ROOT_WIRE) + gremlin.sizes.sizeI32(self.dst_root);
        }
        if (self.dst_id != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupAttachTaskFtraceEventWire.DST_ID_WIRE) + gremlin.sizes.sizeI32(self.dst_id);
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupAttachTaskFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupAttachTaskFtraceEventWire.COMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupAttachTaskFtraceEventWire.CNAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.dst_level != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupAttachTaskFtraceEventWire.DST_LEVEL_WIRE) + gremlin.sizes.sizeI32(self.dst_level);
        }
        if (self.dst_path) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupAttachTaskFtraceEventWire.DST_PATH_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupAttachTaskFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupAttachTaskFtraceEvent, target: *gremlin.Writer) void {
        if (self.dst_root != 0) {
            target.appendInt32(CgroupAttachTaskFtraceEventWire.DST_ROOT_WIRE, self.dst_root);
        }
        if (self.dst_id != 0) {
            target.appendInt32(CgroupAttachTaskFtraceEventWire.DST_ID_WIRE, self.dst_id);
        }
        if (self.pid != 0) {
            target.appendInt32(CgroupAttachTaskFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupAttachTaskFtraceEventWire.COMM_WIRE, v);
            }
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupAttachTaskFtraceEventWire.CNAME_WIRE, v);
            }
        }
        if (self.dst_level != 0) {
            target.appendInt32(CgroupAttachTaskFtraceEventWire.DST_LEVEL_WIRE, self.dst_level);
        }
        if (self.dst_path) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupAttachTaskFtraceEventWire.DST_PATH_WIRE, v);
            }
        }
    }
};
pub const CgroupAttachTaskFtraceEventReader = struct {
    buf: gremlin.Reader,
    _dst_root: i32 = 0,
    _dst_id: i32 = 0,
    _pid: i32 = 0,
    _comm: ?[]const u8 = null,
    _cname: ?[]const u8 = null,
    _dst_level: i32 = 0,
    _dst_path: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupAttachTaskFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupAttachTaskFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupAttachTaskFtraceEventWire.DST_ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dst_root = result.value;
                },
                CgroupAttachTaskFtraceEventWire.DST_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dst_id = result.value;
                },
                CgroupAttachTaskFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                CgroupAttachTaskFtraceEventWire.COMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._comm = result.value;
                },
                CgroupAttachTaskFtraceEventWire.CNAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cname = result.value;
                },
                CgroupAttachTaskFtraceEventWire.DST_LEVEL_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dst_level = result.value;
                },
                CgroupAttachTaskFtraceEventWire.DST_PATH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._dst_path = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDstRoot(self: *const CgroupAttachTaskFtraceEventReader) i32 {
        return self._dst_root;
    }
    pub inline fn getDstId(self: *const CgroupAttachTaskFtraceEventReader) i32 {
        return self._dst_id;
    }
    pub inline fn getPid(self: *const CgroupAttachTaskFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getComm(self: *const CgroupAttachTaskFtraceEventReader) []const u8 {
        return self._comm orelse &[_]u8{};
    }
    pub inline fn getCname(self: *const CgroupAttachTaskFtraceEventReader) []const u8 {
        return self._cname orelse &[_]u8{};
    }
    pub inline fn getDstLevel(self: *const CgroupAttachTaskFtraceEventReader) i32 {
        return self._dst_level;
    }
    pub inline fn getDstPath(self: *const CgroupAttachTaskFtraceEventReader) []const u8 {
        return self._dst_path orelse &[_]u8{};
    }
};
const CgroupMkdirFtraceEventWire = struct {
    const ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const ID_WIRE: gremlin.ProtoWireNumber = 2;
    const CNAME_WIRE: gremlin.ProtoWireNumber = 3;
    const LEVEL_WIRE: gremlin.ProtoWireNumber = 4;
    const PATH_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const CgroupMkdirFtraceEvent = struct {
    // fields
    root: i32 = 0,
    id: i32 = 0,
    cname: ?[]const u8 = null,
    level: i32 = 0,
    path: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupMkdirFtraceEvent) usize {
        var res: usize = 0;
        if (self.root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupMkdirFtraceEventWire.ROOT_WIRE) + gremlin.sizes.sizeI32(self.root);
        }
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupMkdirFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI32(self.id);
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupMkdirFtraceEventWire.CNAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.level != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupMkdirFtraceEventWire.LEVEL_WIRE) + gremlin.sizes.sizeI32(self.level);
        }
        if (self.path) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupMkdirFtraceEventWire.PATH_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupMkdirFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupMkdirFtraceEvent, target: *gremlin.Writer) void {
        if (self.root != 0) {
            target.appendInt32(CgroupMkdirFtraceEventWire.ROOT_WIRE, self.root);
        }
        if (self.id != 0) {
            target.appendInt32(CgroupMkdirFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupMkdirFtraceEventWire.CNAME_WIRE, v);
            }
        }
        if (self.level != 0) {
            target.appendInt32(CgroupMkdirFtraceEventWire.LEVEL_WIRE, self.level);
        }
        if (self.path) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupMkdirFtraceEventWire.PATH_WIRE, v);
            }
        }
    }
};
pub const CgroupMkdirFtraceEventReader = struct {
    buf: gremlin.Reader,
    _root: i32 = 0,
    _id: i32 = 0,
    _cname: ?[]const u8 = null,
    _level: i32 = 0,
    _path: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupMkdirFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupMkdirFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupMkdirFtraceEventWire.ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._root = result.value;
                },
                CgroupMkdirFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                CgroupMkdirFtraceEventWire.CNAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cname = result.value;
                },
                CgroupMkdirFtraceEventWire.LEVEL_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._level = result.value;
                },
                CgroupMkdirFtraceEventWire.PATH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._path = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getRoot(self: *const CgroupMkdirFtraceEventReader) i32 {
        return self._root;
    }
    pub inline fn getId(self: *const CgroupMkdirFtraceEventReader) i32 {
        return self._id;
    }
    pub inline fn getCname(self: *const CgroupMkdirFtraceEventReader) []const u8 {
        return self._cname orelse &[_]u8{};
    }
    pub inline fn getLevel(self: *const CgroupMkdirFtraceEventReader) i32 {
        return self._level;
    }
    pub inline fn getPath(self: *const CgroupMkdirFtraceEventReader) []const u8 {
        return self._path orelse &[_]u8{};
    }
};
const CgroupRemountFtraceEventWire = struct {
    const ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const SS_MASK_WIRE: gremlin.ProtoWireNumber = 2;
    const NAME_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const CgroupRemountFtraceEvent = struct {
    // fields
    root: i32 = 0,
    ss_mask: u32 = 0,
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupRemountFtraceEvent) usize {
        var res: usize = 0;
        if (self.root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupRemountFtraceEventWire.ROOT_WIRE) + gremlin.sizes.sizeI32(self.root);
        }
        if (self.ss_mask != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupRemountFtraceEventWire.SS_MASK_WIRE) + gremlin.sizes.sizeU32(self.ss_mask);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupRemountFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupRemountFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupRemountFtraceEvent, target: *gremlin.Writer) void {
        if (self.root != 0) {
            target.appendInt32(CgroupRemountFtraceEventWire.ROOT_WIRE, self.root);
        }
        if (self.ss_mask != 0) {
            target.appendUint32(CgroupRemountFtraceEventWire.SS_MASK_WIRE, self.ss_mask);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupRemountFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const CgroupRemountFtraceEventReader = struct {
    buf: gremlin.Reader,
    _root: i32 = 0,
    _ss_mask: u32 = 0,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupRemountFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupRemountFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupRemountFtraceEventWire.ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._root = result.value;
                },
                CgroupRemountFtraceEventWire.SS_MASK_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._ss_mask = result.value;
                },
                CgroupRemountFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getRoot(self: *const CgroupRemountFtraceEventReader) i32 {
        return self._root;
    }
    pub inline fn getSsMask(self: *const CgroupRemountFtraceEventReader) u32 {
        return self._ss_mask;
    }
    pub inline fn getName(self: *const CgroupRemountFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const CgroupRmdirFtraceEventWire = struct {
    const ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const ID_WIRE: gremlin.ProtoWireNumber = 2;
    const CNAME_WIRE: gremlin.ProtoWireNumber = 3;
    const LEVEL_WIRE: gremlin.ProtoWireNumber = 4;
    const PATH_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const CgroupRmdirFtraceEvent = struct {
    // fields
    root: i32 = 0,
    id: i32 = 0,
    cname: ?[]const u8 = null,
    level: i32 = 0,
    path: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupRmdirFtraceEvent) usize {
        var res: usize = 0;
        if (self.root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupRmdirFtraceEventWire.ROOT_WIRE) + gremlin.sizes.sizeI32(self.root);
        }
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupRmdirFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI32(self.id);
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupRmdirFtraceEventWire.CNAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.level != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupRmdirFtraceEventWire.LEVEL_WIRE) + gremlin.sizes.sizeI32(self.level);
        }
        if (self.path) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupRmdirFtraceEventWire.PATH_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupRmdirFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupRmdirFtraceEvent, target: *gremlin.Writer) void {
        if (self.root != 0) {
            target.appendInt32(CgroupRmdirFtraceEventWire.ROOT_WIRE, self.root);
        }
        if (self.id != 0) {
            target.appendInt32(CgroupRmdirFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupRmdirFtraceEventWire.CNAME_WIRE, v);
            }
        }
        if (self.level != 0) {
            target.appendInt32(CgroupRmdirFtraceEventWire.LEVEL_WIRE, self.level);
        }
        if (self.path) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupRmdirFtraceEventWire.PATH_WIRE, v);
            }
        }
    }
};
pub const CgroupRmdirFtraceEventReader = struct {
    buf: gremlin.Reader,
    _root: i32 = 0,
    _id: i32 = 0,
    _cname: ?[]const u8 = null,
    _level: i32 = 0,
    _path: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupRmdirFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupRmdirFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupRmdirFtraceEventWire.ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._root = result.value;
                },
                CgroupRmdirFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                CgroupRmdirFtraceEventWire.CNAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cname = result.value;
                },
                CgroupRmdirFtraceEventWire.LEVEL_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._level = result.value;
                },
                CgroupRmdirFtraceEventWire.PATH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._path = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getRoot(self: *const CgroupRmdirFtraceEventReader) i32 {
        return self._root;
    }
    pub inline fn getId(self: *const CgroupRmdirFtraceEventReader) i32 {
        return self._id;
    }
    pub inline fn getCname(self: *const CgroupRmdirFtraceEventReader) []const u8 {
        return self._cname orelse &[_]u8{};
    }
    pub inline fn getLevel(self: *const CgroupRmdirFtraceEventReader) i32 {
        return self._level;
    }
    pub inline fn getPath(self: *const CgroupRmdirFtraceEventReader) []const u8 {
        return self._path orelse &[_]u8{};
    }
};
const CgroupTransferTasksFtraceEventWire = struct {
    const DST_ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const DST_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const PID_WIRE: gremlin.ProtoWireNumber = 3;
    const COMM_WIRE: gremlin.ProtoWireNumber = 4;
    const CNAME_WIRE: gremlin.ProtoWireNumber = 5;
    const DST_LEVEL_WIRE: gremlin.ProtoWireNumber = 6;
    const DST_PATH_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const CgroupTransferTasksFtraceEvent = struct {
    // fields
    dst_root: i32 = 0,
    dst_id: i32 = 0,
    pid: i32 = 0,
    comm: ?[]const u8 = null,
    cname: ?[]const u8 = null,
    dst_level: i32 = 0,
    dst_path: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupTransferTasksFtraceEvent) usize {
        var res: usize = 0;
        if (self.dst_root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupTransferTasksFtraceEventWire.DST_ROOT_WIRE) + gremlin.sizes.sizeI32(self.dst_root);
        }
        if (self.dst_id != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupTransferTasksFtraceEventWire.DST_ID_WIRE) + gremlin.sizes.sizeI32(self.dst_id);
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupTransferTasksFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupTransferTasksFtraceEventWire.COMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupTransferTasksFtraceEventWire.CNAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.dst_level != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupTransferTasksFtraceEventWire.DST_LEVEL_WIRE) + gremlin.sizes.sizeI32(self.dst_level);
        }
        if (self.dst_path) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupTransferTasksFtraceEventWire.DST_PATH_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupTransferTasksFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupTransferTasksFtraceEvent, target: *gremlin.Writer) void {
        if (self.dst_root != 0) {
            target.appendInt32(CgroupTransferTasksFtraceEventWire.DST_ROOT_WIRE, self.dst_root);
        }
        if (self.dst_id != 0) {
            target.appendInt32(CgroupTransferTasksFtraceEventWire.DST_ID_WIRE, self.dst_id);
        }
        if (self.pid != 0) {
            target.appendInt32(CgroupTransferTasksFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupTransferTasksFtraceEventWire.COMM_WIRE, v);
            }
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupTransferTasksFtraceEventWire.CNAME_WIRE, v);
            }
        }
        if (self.dst_level != 0) {
            target.appendInt32(CgroupTransferTasksFtraceEventWire.DST_LEVEL_WIRE, self.dst_level);
        }
        if (self.dst_path) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupTransferTasksFtraceEventWire.DST_PATH_WIRE, v);
            }
        }
    }
};
pub const CgroupTransferTasksFtraceEventReader = struct {
    buf: gremlin.Reader,
    _dst_root: i32 = 0,
    _dst_id: i32 = 0,
    _pid: i32 = 0,
    _comm: ?[]const u8 = null,
    _cname: ?[]const u8 = null,
    _dst_level: i32 = 0,
    _dst_path: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupTransferTasksFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupTransferTasksFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupTransferTasksFtraceEventWire.DST_ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dst_root = result.value;
                },
                CgroupTransferTasksFtraceEventWire.DST_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dst_id = result.value;
                },
                CgroupTransferTasksFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                CgroupTransferTasksFtraceEventWire.COMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._comm = result.value;
                },
                CgroupTransferTasksFtraceEventWire.CNAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cname = result.value;
                },
                CgroupTransferTasksFtraceEventWire.DST_LEVEL_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dst_level = result.value;
                },
                CgroupTransferTasksFtraceEventWire.DST_PATH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._dst_path = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDstRoot(self: *const CgroupTransferTasksFtraceEventReader) i32 {
        return self._dst_root;
    }
    pub inline fn getDstId(self: *const CgroupTransferTasksFtraceEventReader) i32 {
        return self._dst_id;
    }
    pub inline fn getPid(self: *const CgroupTransferTasksFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getComm(self: *const CgroupTransferTasksFtraceEventReader) []const u8 {
        return self._comm orelse &[_]u8{};
    }
    pub inline fn getCname(self: *const CgroupTransferTasksFtraceEventReader) []const u8 {
        return self._cname orelse &[_]u8{};
    }
    pub inline fn getDstLevel(self: *const CgroupTransferTasksFtraceEventReader) i32 {
        return self._dst_level;
    }
    pub inline fn getDstPath(self: *const CgroupTransferTasksFtraceEventReader) []const u8 {
        return self._dst_path orelse &[_]u8{};
    }
};
const CgroupDestroyRootFtraceEventWire = struct {
    const ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const SS_MASK_WIRE: gremlin.ProtoWireNumber = 2;
    const NAME_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const CgroupDestroyRootFtraceEvent = struct {
    // fields
    root: i32 = 0,
    ss_mask: u32 = 0,
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupDestroyRootFtraceEvent) usize {
        var res: usize = 0;
        if (self.root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupDestroyRootFtraceEventWire.ROOT_WIRE) + gremlin.sizes.sizeI32(self.root);
        }
        if (self.ss_mask != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupDestroyRootFtraceEventWire.SS_MASK_WIRE) + gremlin.sizes.sizeU32(self.ss_mask);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupDestroyRootFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupDestroyRootFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupDestroyRootFtraceEvent, target: *gremlin.Writer) void {
        if (self.root != 0) {
            target.appendInt32(CgroupDestroyRootFtraceEventWire.ROOT_WIRE, self.root);
        }
        if (self.ss_mask != 0) {
            target.appendUint32(CgroupDestroyRootFtraceEventWire.SS_MASK_WIRE, self.ss_mask);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupDestroyRootFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const CgroupDestroyRootFtraceEventReader = struct {
    buf: gremlin.Reader,
    _root: i32 = 0,
    _ss_mask: u32 = 0,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupDestroyRootFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupDestroyRootFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupDestroyRootFtraceEventWire.ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._root = result.value;
                },
                CgroupDestroyRootFtraceEventWire.SS_MASK_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._ss_mask = result.value;
                },
                CgroupDestroyRootFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getRoot(self: *const CgroupDestroyRootFtraceEventReader) i32 {
        return self._root;
    }
    pub inline fn getSsMask(self: *const CgroupDestroyRootFtraceEventReader) u32 {
        return self._ss_mask;
    }
    pub inline fn getName(self: *const CgroupDestroyRootFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const CgroupReleaseFtraceEventWire = struct {
    const ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const ID_WIRE: gremlin.ProtoWireNumber = 2;
    const CNAME_WIRE: gremlin.ProtoWireNumber = 3;
    const LEVEL_WIRE: gremlin.ProtoWireNumber = 4;
    const PATH_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const CgroupReleaseFtraceEvent = struct {
    // fields
    root: i32 = 0,
    id: i32 = 0,
    cname: ?[]const u8 = null,
    level: i32 = 0,
    path: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupReleaseFtraceEvent) usize {
        var res: usize = 0;
        if (self.root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupReleaseFtraceEventWire.ROOT_WIRE) + gremlin.sizes.sizeI32(self.root);
        }
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupReleaseFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI32(self.id);
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupReleaseFtraceEventWire.CNAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.level != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupReleaseFtraceEventWire.LEVEL_WIRE) + gremlin.sizes.sizeI32(self.level);
        }
        if (self.path) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupReleaseFtraceEventWire.PATH_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupReleaseFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupReleaseFtraceEvent, target: *gremlin.Writer) void {
        if (self.root != 0) {
            target.appendInt32(CgroupReleaseFtraceEventWire.ROOT_WIRE, self.root);
        }
        if (self.id != 0) {
            target.appendInt32(CgroupReleaseFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupReleaseFtraceEventWire.CNAME_WIRE, v);
            }
        }
        if (self.level != 0) {
            target.appendInt32(CgroupReleaseFtraceEventWire.LEVEL_WIRE, self.level);
        }
        if (self.path) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupReleaseFtraceEventWire.PATH_WIRE, v);
            }
        }
    }
};
pub const CgroupReleaseFtraceEventReader = struct {
    buf: gremlin.Reader,
    _root: i32 = 0,
    _id: i32 = 0,
    _cname: ?[]const u8 = null,
    _level: i32 = 0,
    _path: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupReleaseFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupReleaseFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupReleaseFtraceEventWire.ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._root = result.value;
                },
                CgroupReleaseFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                CgroupReleaseFtraceEventWire.CNAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cname = result.value;
                },
                CgroupReleaseFtraceEventWire.LEVEL_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._level = result.value;
                },
                CgroupReleaseFtraceEventWire.PATH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._path = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getRoot(self: *const CgroupReleaseFtraceEventReader) i32 {
        return self._root;
    }
    pub inline fn getId(self: *const CgroupReleaseFtraceEventReader) i32 {
        return self._id;
    }
    pub inline fn getCname(self: *const CgroupReleaseFtraceEventReader) []const u8 {
        return self._cname orelse &[_]u8{};
    }
    pub inline fn getLevel(self: *const CgroupReleaseFtraceEventReader) i32 {
        return self._level;
    }
    pub inline fn getPath(self: *const CgroupReleaseFtraceEventReader) []const u8 {
        return self._path orelse &[_]u8{};
    }
};
const CgroupRenameFtraceEventWire = struct {
    const ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const ID_WIRE: gremlin.ProtoWireNumber = 2;
    const CNAME_WIRE: gremlin.ProtoWireNumber = 3;
    const LEVEL_WIRE: gremlin.ProtoWireNumber = 4;
    const PATH_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const CgroupRenameFtraceEvent = struct {
    // fields
    root: i32 = 0,
    id: i32 = 0,
    cname: ?[]const u8 = null,
    level: i32 = 0,
    path: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupRenameFtraceEvent) usize {
        var res: usize = 0;
        if (self.root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupRenameFtraceEventWire.ROOT_WIRE) + gremlin.sizes.sizeI32(self.root);
        }
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupRenameFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI32(self.id);
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupRenameFtraceEventWire.CNAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.level != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupRenameFtraceEventWire.LEVEL_WIRE) + gremlin.sizes.sizeI32(self.level);
        }
        if (self.path) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupRenameFtraceEventWire.PATH_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupRenameFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupRenameFtraceEvent, target: *gremlin.Writer) void {
        if (self.root != 0) {
            target.appendInt32(CgroupRenameFtraceEventWire.ROOT_WIRE, self.root);
        }
        if (self.id != 0) {
            target.appendInt32(CgroupRenameFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.cname) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupRenameFtraceEventWire.CNAME_WIRE, v);
            }
        }
        if (self.level != 0) {
            target.appendInt32(CgroupRenameFtraceEventWire.LEVEL_WIRE, self.level);
        }
        if (self.path) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupRenameFtraceEventWire.PATH_WIRE, v);
            }
        }
    }
};
pub const CgroupRenameFtraceEventReader = struct {
    buf: gremlin.Reader,
    _root: i32 = 0,
    _id: i32 = 0,
    _cname: ?[]const u8 = null,
    _level: i32 = 0,
    _path: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupRenameFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupRenameFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupRenameFtraceEventWire.ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._root = result.value;
                },
                CgroupRenameFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                CgroupRenameFtraceEventWire.CNAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cname = result.value;
                },
                CgroupRenameFtraceEventWire.LEVEL_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._level = result.value;
                },
                CgroupRenameFtraceEventWire.PATH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._path = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getRoot(self: *const CgroupRenameFtraceEventReader) i32 {
        return self._root;
    }
    pub inline fn getId(self: *const CgroupRenameFtraceEventReader) i32 {
        return self._id;
    }
    pub inline fn getCname(self: *const CgroupRenameFtraceEventReader) []const u8 {
        return self._cname orelse &[_]u8{};
    }
    pub inline fn getLevel(self: *const CgroupRenameFtraceEventReader) i32 {
        return self._level;
    }
    pub inline fn getPath(self: *const CgroupRenameFtraceEventReader) []const u8 {
        return self._path orelse &[_]u8{};
    }
};
const CgroupSetupRootFtraceEventWire = struct {
    const ROOT_WIRE: gremlin.ProtoWireNumber = 1;
    const SS_MASK_WIRE: gremlin.ProtoWireNumber = 2;
    const NAME_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const CgroupSetupRootFtraceEvent = struct {
    // fields
    root: i32 = 0,
    ss_mask: u32 = 0,
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CgroupSetupRootFtraceEvent) usize {
        var res: usize = 0;
        if (self.root != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupSetupRootFtraceEventWire.ROOT_WIRE) + gremlin.sizes.sizeI32(self.root);
        }
        if (self.ss_mask != 0) {
            res += gremlin.sizes.sizeWireNumber(CgroupSetupRootFtraceEventWire.SS_MASK_WIRE) + gremlin.sizes.sizeU32(self.ss_mask);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CgroupSetupRootFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CgroupSetupRootFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CgroupSetupRootFtraceEvent, target: *gremlin.Writer) void {
        if (self.root != 0) {
            target.appendInt32(CgroupSetupRootFtraceEventWire.ROOT_WIRE, self.root);
        }
        if (self.ss_mask != 0) {
            target.appendUint32(CgroupSetupRootFtraceEventWire.SS_MASK_WIRE, self.ss_mask);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(CgroupSetupRootFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const CgroupSetupRootFtraceEventReader = struct {
    buf: gremlin.Reader,
    _root: i32 = 0,
    _ss_mask: u32 = 0,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CgroupSetupRootFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CgroupSetupRootFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CgroupSetupRootFtraceEventWire.ROOT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._root = result.value;
                },
                CgroupSetupRootFtraceEventWire.SS_MASK_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._ss_mask = result.value;
                },
                CgroupSetupRootFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getRoot(self: *const CgroupSetupRootFtraceEventReader) i32 {
        return self._root;
    }
    pub inline fn getSsMask(self: *const CgroupSetupRootFtraceEventReader) u32 {
        return self._ss_mask;
    }
    pub inline fn getName(self: *const CgroupSetupRootFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
