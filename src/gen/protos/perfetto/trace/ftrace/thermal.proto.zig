// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ThermalTemperatureFtraceEventWire = struct {
    const ID_WIRE: gremlin.ProtoWireNumber = 1;
    const TEMP_WIRE: gremlin.ProtoWireNumber = 2;
    const TEMP_PREV_WIRE: gremlin.ProtoWireNumber = 3;
    const THERMAL_ZONE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const ThermalTemperatureFtraceEvent = struct {
    // fields
    id: i32 = 0,
    temp: i32 = 0,
    temp_prev: i32 = 0,
    thermal_zone: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ThermalTemperatureFtraceEvent) usize {
        var res: usize = 0;
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(ThermalTemperatureFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI32(self.id);
        }
        if (self.temp != 0) {
            res += gremlin.sizes.sizeWireNumber(ThermalTemperatureFtraceEventWire.TEMP_WIRE) + gremlin.sizes.sizeI32(self.temp);
        }
        if (self.temp_prev != 0) {
            res += gremlin.sizes.sizeWireNumber(ThermalTemperatureFtraceEventWire.TEMP_PREV_WIRE) + gremlin.sizes.sizeI32(self.temp_prev);
        }
        if (self.thermal_zone) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ThermalTemperatureFtraceEventWire.THERMAL_ZONE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const ThermalTemperatureFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ThermalTemperatureFtraceEvent, target: *gremlin.Writer) void {
        if (self.id != 0) {
            target.appendInt32(ThermalTemperatureFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.temp != 0) {
            target.appendInt32(ThermalTemperatureFtraceEventWire.TEMP_WIRE, self.temp);
        }
        if (self.temp_prev != 0) {
            target.appendInt32(ThermalTemperatureFtraceEventWire.TEMP_PREV_WIRE, self.temp_prev);
        }
        if (self.thermal_zone) |v| {
            if (v.len > 0) {
                target.appendBytes(ThermalTemperatureFtraceEventWire.THERMAL_ZONE_WIRE, v);
            }
        }
    }
};
pub const ThermalTemperatureFtraceEventReader = struct {
    buf: gremlin.Reader,
    _id: i32 = 0,
    _temp: i32 = 0,
    _temp_prev: i32 = 0,
    _thermal_zone: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ThermalTemperatureFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ThermalTemperatureFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ThermalTemperatureFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                ThermalTemperatureFtraceEventWire.TEMP_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._temp = result.value;
                },
                ThermalTemperatureFtraceEventWire.TEMP_PREV_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._temp_prev = result.value;
                },
                ThermalTemperatureFtraceEventWire.THERMAL_ZONE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._thermal_zone = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getId(self: *const ThermalTemperatureFtraceEventReader) i32 {
        return self._id;
    }
    pub inline fn getTemp(self: *const ThermalTemperatureFtraceEventReader) i32 {
        return self._temp;
    }
    pub inline fn getTempPrev(self: *const ThermalTemperatureFtraceEventReader) i32 {
        return self._temp_prev;
    }
    pub inline fn getThermalZone(self: *const ThermalTemperatureFtraceEventReader) []const u8 {
        return self._thermal_zone orelse &[_]u8{};
    }
};
const CdevUpdateFtraceEventWire = struct {
    const TARGET_WIRE: gremlin.ProtoWireNumber = 1;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const CdevUpdateFtraceEvent = struct {
    // fields
    target: u64 = 0,
    type: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CdevUpdateFtraceEvent) usize {
        var res: usize = 0;
        if (self.target != 0) {
            res += gremlin.sizes.sizeWireNumber(CdevUpdateFtraceEventWire.TARGET_WIRE) + gremlin.sizes.sizeU64(self.target);
        }
        if (self.type) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CdevUpdateFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CdevUpdateFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CdevUpdateFtraceEvent, target: *gremlin.Writer) void {
        if (self.target != 0) {
            target.appendUint64(CdevUpdateFtraceEventWire.TARGET_WIRE, self.target);
        }
        if (self.type) |v| {
            if (v.len > 0) {
                target.appendBytes(CdevUpdateFtraceEventWire.TYPE_WIRE, v);
            }
        }
    }
};
pub const CdevUpdateFtraceEventReader = struct {
    buf: gremlin.Reader,
    _target: u64 = 0,
    _type: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CdevUpdateFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CdevUpdateFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CdevUpdateFtraceEventWire.TARGET_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._target = result.value;
                },
                CdevUpdateFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTarget(self: *const CdevUpdateFtraceEventReader) u64 {
        return self._target;
    }
    pub inline fn getType(self: *const CdevUpdateFtraceEventReader) []const u8 {
        return self._type orelse &[_]u8{};
    }
};
