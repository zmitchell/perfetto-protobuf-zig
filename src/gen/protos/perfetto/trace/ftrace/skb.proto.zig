// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const KfreeSkbFtraceEventWire = struct {
    const LOCATION_WIRE: gremlin.ProtoWireNumber = 1;
    const PROTOCOL_WIRE: gremlin.ProtoWireNumber = 2;
    const SKBADDR_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const KfreeSkbFtraceEvent = struct {
    // fields
    location: u64 = 0,
    protocol: u32 = 0,
    skbaddr: u64 = 0,
    pub fn calcProtobufSize(self: *const KfreeSkbFtraceEvent) usize {
        var res: usize = 0;
        if (self.location != 0) {
            res += gremlin.sizes.sizeWireNumber(KfreeSkbFtraceEventWire.LOCATION_WIRE) + gremlin.sizes.sizeU64(self.location);
        }
        if (self.protocol != 0) {
            res += gremlin.sizes.sizeWireNumber(KfreeSkbFtraceEventWire.PROTOCOL_WIRE) + gremlin.sizes.sizeU32(self.protocol);
        }
        if (self.skbaddr != 0) {
            res += gremlin.sizes.sizeWireNumber(KfreeSkbFtraceEventWire.SKBADDR_WIRE) + gremlin.sizes.sizeU64(self.skbaddr);
        }
        return res;
    }
    pub fn encode(self: *const KfreeSkbFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const KfreeSkbFtraceEvent, target: *gremlin.Writer) void {
        if (self.location != 0) {
            target.appendUint64(KfreeSkbFtraceEventWire.LOCATION_WIRE, self.location);
        }
        if (self.protocol != 0) {
            target.appendUint32(KfreeSkbFtraceEventWire.PROTOCOL_WIRE, self.protocol);
        }
        if (self.skbaddr != 0) {
            target.appendUint64(KfreeSkbFtraceEventWire.SKBADDR_WIRE, self.skbaddr);
        }
    }
};
pub const KfreeSkbFtraceEventReader = struct {
    buf: gremlin.Reader,
    _location: u64 = 0,
    _protocol: u32 = 0,
    _skbaddr: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!KfreeSkbFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = KfreeSkbFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                KfreeSkbFtraceEventWire.LOCATION_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._location = result.value;
                },
                KfreeSkbFtraceEventWire.PROTOCOL_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._protocol = result.value;
                },
                KfreeSkbFtraceEventWire.SKBADDR_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._skbaddr = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getLocation(self: *const KfreeSkbFtraceEventReader) u64 {
        return self._location;
    }
    pub inline fn getProtocol(self: *const KfreeSkbFtraceEventReader) u32 {
        return self._protocol;
    }
    pub inline fn getSkbaddr(self: *const KfreeSkbFtraceEventReader) u64 {
        return self._skbaddr;
    }
};
