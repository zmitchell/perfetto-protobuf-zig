// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const AndroidFsDatareadEndFtraceEventWire = struct {
    const BYTES_WIRE: gremlin.ProtoWireNumber = 1;
    const INO_WIRE: gremlin.ProtoWireNumber = 2;
    const OFFSET_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const AndroidFsDatareadEndFtraceEvent = struct {
    // fields
    bytes: i32 = 0,
    ino: u64 = 0,
    offset: i64 = 0,
    pub fn calcProtobufSize(self: *const AndroidFsDatareadEndFtraceEvent) usize {
        var res: usize = 0;
        if (self.bytes != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadEndFtraceEventWire.BYTES_WIRE) + gremlin.sizes.sizeI32(self.bytes);
        }
        if (self.ino != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadEndFtraceEventWire.INO_WIRE) + gremlin.sizes.sizeU64(self.ino);
        }
        if (self.offset != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadEndFtraceEventWire.OFFSET_WIRE) + gremlin.sizes.sizeI64(self.offset);
        }
        return res;
    }
    pub fn encode(self: *const AndroidFsDatareadEndFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidFsDatareadEndFtraceEvent, target: *gremlin.Writer) void {
        if (self.bytes != 0) {
            target.appendInt32(AndroidFsDatareadEndFtraceEventWire.BYTES_WIRE, self.bytes);
        }
        if (self.ino != 0) {
            target.appendUint64(AndroidFsDatareadEndFtraceEventWire.INO_WIRE, self.ino);
        }
        if (self.offset != 0) {
            target.appendInt64(AndroidFsDatareadEndFtraceEventWire.OFFSET_WIRE, self.offset);
        }
    }
};
pub const AndroidFsDatareadEndFtraceEventReader = struct {
    buf: gremlin.Reader,
    _bytes: i32 = 0,
    _ino: u64 = 0,
    _offset: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidFsDatareadEndFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidFsDatareadEndFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidFsDatareadEndFtraceEventWire.BYTES_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._bytes = result.value;
                },
                AndroidFsDatareadEndFtraceEventWire.INO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ino = result.value;
                },
                AndroidFsDatareadEndFtraceEventWire.OFFSET_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._offset = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBytes(self: *const AndroidFsDatareadEndFtraceEventReader) i32 {
        return self._bytes;
    }
    pub inline fn getIno(self: *const AndroidFsDatareadEndFtraceEventReader) u64 {
        return self._ino;
    }
    pub inline fn getOffset(self: *const AndroidFsDatareadEndFtraceEventReader) i64 {
        return self._offset;
    }
};
const AndroidFsDatareadStartFtraceEventWire = struct {
    const BYTES_WIRE: gremlin.ProtoWireNumber = 1;
    const CMDLINE_WIRE: gremlin.ProtoWireNumber = 2;
    const I_SIZE_WIRE: gremlin.ProtoWireNumber = 3;
    const INO_WIRE: gremlin.ProtoWireNumber = 4;
    const OFFSET_WIRE: gremlin.ProtoWireNumber = 5;
    const PATHBUF_WIRE: gremlin.ProtoWireNumber = 6;
    const PID_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const AndroidFsDatareadStartFtraceEvent = struct {
    // fields
    bytes: i32 = 0,
    cmdline: ?[]const u8 = null,
    i_size: i64 = 0,
    ino: u64 = 0,
    offset: i64 = 0,
    pathbuf: ?[]const u8 = null,
    pid: i32 = 0,
    pub fn calcProtobufSize(self: *const AndroidFsDatareadStartFtraceEvent) usize {
        var res: usize = 0;
        if (self.bytes != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadStartFtraceEventWire.BYTES_WIRE) + gremlin.sizes.sizeI32(self.bytes);
        }
        if (self.cmdline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadStartFtraceEventWire.CMDLINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.i_size != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadStartFtraceEventWire.I_SIZE_WIRE) + gremlin.sizes.sizeI64(self.i_size);
        }
        if (self.ino != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadStartFtraceEventWire.INO_WIRE) + gremlin.sizes.sizeU64(self.ino);
        }
        if (self.offset != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadStartFtraceEventWire.OFFSET_WIRE) + gremlin.sizes.sizeI64(self.offset);
        }
        if (self.pathbuf) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadStartFtraceEventWire.PATHBUF_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatareadStartFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        return res;
    }
    pub fn encode(self: *const AndroidFsDatareadStartFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidFsDatareadStartFtraceEvent, target: *gremlin.Writer) void {
        if (self.bytes != 0) {
            target.appendInt32(AndroidFsDatareadStartFtraceEventWire.BYTES_WIRE, self.bytes);
        }
        if (self.cmdline) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidFsDatareadStartFtraceEventWire.CMDLINE_WIRE, v);
            }
        }
        if (self.i_size != 0) {
            target.appendInt64(AndroidFsDatareadStartFtraceEventWire.I_SIZE_WIRE, self.i_size);
        }
        if (self.ino != 0) {
            target.appendUint64(AndroidFsDatareadStartFtraceEventWire.INO_WIRE, self.ino);
        }
        if (self.offset != 0) {
            target.appendInt64(AndroidFsDatareadStartFtraceEventWire.OFFSET_WIRE, self.offset);
        }
        if (self.pathbuf) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidFsDatareadStartFtraceEventWire.PATHBUF_WIRE, v);
            }
        }
        if (self.pid != 0) {
            target.appendInt32(AndroidFsDatareadStartFtraceEventWire.PID_WIRE, self.pid);
        }
    }
};
pub const AndroidFsDatareadStartFtraceEventReader = struct {
    buf: gremlin.Reader,
    _bytes: i32 = 0,
    _cmdline: ?[]const u8 = null,
    _i_size: i64 = 0,
    _ino: u64 = 0,
    _offset: i64 = 0,
    _pathbuf: ?[]const u8 = null,
    _pid: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidFsDatareadStartFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidFsDatareadStartFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidFsDatareadStartFtraceEventWire.BYTES_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._bytes = result.value;
                },
                AndroidFsDatareadStartFtraceEventWire.CMDLINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cmdline = result.value;
                },
                AndroidFsDatareadStartFtraceEventWire.I_SIZE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._i_size = result.value;
                },
                AndroidFsDatareadStartFtraceEventWire.INO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ino = result.value;
                },
                AndroidFsDatareadStartFtraceEventWire.OFFSET_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._offset = result.value;
                },
                AndroidFsDatareadStartFtraceEventWire.PATHBUF_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._pathbuf = result.value;
                },
                AndroidFsDatareadStartFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBytes(self: *const AndroidFsDatareadStartFtraceEventReader) i32 {
        return self._bytes;
    }
    pub inline fn getCmdline(self: *const AndroidFsDatareadStartFtraceEventReader) []const u8 {
        return self._cmdline orelse &[_]u8{};
    }
    pub inline fn getISize(self: *const AndroidFsDatareadStartFtraceEventReader) i64 {
        return self._i_size;
    }
    pub inline fn getIno(self: *const AndroidFsDatareadStartFtraceEventReader) u64 {
        return self._ino;
    }
    pub inline fn getOffset(self: *const AndroidFsDatareadStartFtraceEventReader) i64 {
        return self._offset;
    }
    pub inline fn getPathbuf(self: *const AndroidFsDatareadStartFtraceEventReader) []const u8 {
        return self._pathbuf orelse &[_]u8{};
    }
    pub inline fn getPid(self: *const AndroidFsDatareadStartFtraceEventReader) i32 {
        return self._pid;
    }
};
const AndroidFsDatawriteEndFtraceEventWire = struct {
    const BYTES_WIRE: gremlin.ProtoWireNumber = 1;
    const INO_WIRE: gremlin.ProtoWireNumber = 2;
    const OFFSET_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const AndroidFsDatawriteEndFtraceEvent = struct {
    // fields
    bytes: i32 = 0,
    ino: u64 = 0,
    offset: i64 = 0,
    pub fn calcProtobufSize(self: *const AndroidFsDatawriteEndFtraceEvent) usize {
        var res: usize = 0;
        if (self.bytes != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteEndFtraceEventWire.BYTES_WIRE) + gremlin.sizes.sizeI32(self.bytes);
        }
        if (self.ino != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteEndFtraceEventWire.INO_WIRE) + gremlin.sizes.sizeU64(self.ino);
        }
        if (self.offset != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteEndFtraceEventWire.OFFSET_WIRE) + gremlin.sizes.sizeI64(self.offset);
        }
        return res;
    }
    pub fn encode(self: *const AndroidFsDatawriteEndFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidFsDatawriteEndFtraceEvent, target: *gremlin.Writer) void {
        if (self.bytes != 0) {
            target.appendInt32(AndroidFsDatawriteEndFtraceEventWire.BYTES_WIRE, self.bytes);
        }
        if (self.ino != 0) {
            target.appendUint64(AndroidFsDatawriteEndFtraceEventWire.INO_WIRE, self.ino);
        }
        if (self.offset != 0) {
            target.appendInt64(AndroidFsDatawriteEndFtraceEventWire.OFFSET_WIRE, self.offset);
        }
    }
};
pub const AndroidFsDatawriteEndFtraceEventReader = struct {
    buf: gremlin.Reader,
    _bytes: i32 = 0,
    _ino: u64 = 0,
    _offset: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidFsDatawriteEndFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidFsDatawriteEndFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidFsDatawriteEndFtraceEventWire.BYTES_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._bytes = result.value;
                },
                AndroidFsDatawriteEndFtraceEventWire.INO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ino = result.value;
                },
                AndroidFsDatawriteEndFtraceEventWire.OFFSET_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._offset = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBytes(self: *const AndroidFsDatawriteEndFtraceEventReader) i32 {
        return self._bytes;
    }
    pub inline fn getIno(self: *const AndroidFsDatawriteEndFtraceEventReader) u64 {
        return self._ino;
    }
    pub inline fn getOffset(self: *const AndroidFsDatawriteEndFtraceEventReader) i64 {
        return self._offset;
    }
};
const AndroidFsDatawriteStartFtraceEventWire = struct {
    const BYTES_WIRE: gremlin.ProtoWireNumber = 1;
    const CMDLINE_WIRE: gremlin.ProtoWireNumber = 2;
    const I_SIZE_WIRE: gremlin.ProtoWireNumber = 3;
    const INO_WIRE: gremlin.ProtoWireNumber = 4;
    const OFFSET_WIRE: gremlin.ProtoWireNumber = 5;
    const PATHBUF_WIRE: gremlin.ProtoWireNumber = 6;
    const PID_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const AndroidFsDatawriteStartFtraceEvent = struct {
    // fields
    bytes: i32 = 0,
    cmdline: ?[]const u8 = null,
    i_size: i64 = 0,
    ino: u64 = 0,
    offset: i64 = 0,
    pathbuf: ?[]const u8 = null,
    pid: i32 = 0,
    pub fn calcProtobufSize(self: *const AndroidFsDatawriteStartFtraceEvent) usize {
        var res: usize = 0;
        if (self.bytes != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteStartFtraceEventWire.BYTES_WIRE) + gremlin.sizes.sizeI32(self.bytes);
        }
        if (self.cmdline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteStartFtraceEventWire.CMDLINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.i_size != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteStartFtraceEventWire.I_SIZE_WIRE) + gremlin.sizes.sizeI64(self.i_size);
        }
        if (self.ino != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteStartFtraceEventWire.INO_WIRE) + gremlin.sizes.sizeU64(self.ino);
        }
        if (self.offset != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteStartFtraceEventWire.OFFSET_WIRE) + gremlin.sizes.sizeI64(self.offset);
        }
        if (self.pathbuf) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteStartFtraceEventWire.PATHBUF_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsDatawriteStartFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        return res;
    }
    pub fn encode(self: *const AndroidFsDatawriteStartFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidFsDatawriteStartFtraceEvent, target: *gremlin.Writer) void {
        if (self.bytes != 0) {
            target.appendInt32(AndroidFsDatawriteStartFtraceEventWire.BYTES_WIRE, self.bytes);
        }
        if (self.cmdline) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidFsDatawriteStartFtraceEventWire.CMDLINE_WIRE, v);
            }
        }
        if (self.i_size != 0) {
            target.appendInt64(AndroidFsDatawriteStartFtraceEventWire.I_SIZE_WIRE, self.i_size);
        }
        if (self.ino != 0) {
            target.appendUint64(AndroidFsDatawriteStartFtraceEventWire.INO_WIRE, self.ino);
        }
        if (self.offset != 0) {
            target.appendInt64(AndroidFsDatawriteStartFtraceEventWire.OFFSET_WIRE, self.offset);
        }
        if (self.pathbuf) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidFsDatawriteStartFtraceEventWire.PATHBUF_WIRE, v);
            }
        }
        if (self.pid != 0) {
            target.appendInt32(AndroidFsDatawriteStartFtraceEventWire.PID_WIRE, self.pid);
        }
    }
};
pub const AndroidFsDatawriteStartFtraceEventReader = struct {
    buf: gremlin.Reader,
    _bytes: i32 = 0,
    _cmdline: ?[]const u8 = null,
    _i_size: i64 = 0,
    _ino: u64 = 0,
    _offset: i64 = 0,
    _pathbuf: ?[]const u8 = null,
    _pid: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidFsDatawriteStartFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidFsDatawriteStartFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidFsDatawriteStartFtraceEventWire.BYTES_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._bytes = result.value;
                },
                AndroidFsDatawriteStartFtraceEventWire.CMDLINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cmdline = result.value;
                },
                AndroidFsDatawriteStartFtraceEventWire.I_SIZE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._i_size = result.value;
                },
                AndroidFsDatawriteStartFtraceEventWire.INO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ino = result.value;
                },
                AndroidFsDatawriteStartFtraceEventWire.OFFSET_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._offset = result.value;
                },
                AndroidFsDatawriteStartFtraceEventWire.PATHBUF_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._pathbuf = result.value;
                },
                AndroidFsDatawriteStartFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBytes(self: *const AndroidFsDatawriteStartFtraceEventReader) i32 {
        return self._bytes;
    }
    pub inline fn getCmdline(self: *const AndroidFsDatawriteStartFtraceEventReader) []const u8 {
        return self._cmdline orelse &[_]u8{};
    }
    pub inline fn getISize(self: *const AndroidFsDatawriteStartFtraceEventReader) i64 {
        return self._i_size;
    }
    pub inline fn getIno(self: *const AndroidFsDatawriteStartFtraceEventReader) u64 {
        return self._ino;
    }
    pub inline fn getOffset(self: *const AndroidFsDatawriteStartFtraceEventReader) i64 {
        return self._offset;
    }
    pub inline fn getPathbuf(self: *const AndroidFsDatawriteStartFtraceEventReader) []const u8 {
        return self._pathbuf orelse &[_]u8{};
    }
    pub inline fn getPid(self: *const AndroidFsDatawriteStartFtraceEventReader) i32 {
        return self._pid;
    }
};
const AndroidFsFsyncEndFtraceEventWire = struct {
    const BYTES_WIRE: gremlin.ProtoWireNumber = 1;
    const INO_WIRE: gremlin.ProtoWireNumber = 2;
    const OFFSET_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const AndroidFsFsyncEndFtraceEvent = struct {
    // fields
    bytes: i32 = 0,
    ino: u64 = 0,
    offset: i64 = 0,
    pub fn calcProtobufSize(self: *const AndroidFsFsyncEndFtraceEvent) usize {
        var res: usize = 0;
        if (self.bytes != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsFsyncEndFtraceEventWire.BYTES_WIRE) + gremlin.sizes.sizeI32(self.bytes);
        }
        if (self.ino != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsFsyncEndFtraceEventWire.INO_WIRE) + gremlin.sizes.sizeU64(self.ino);
        }
        if (self.offset != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsFsyncEndFtraceEventWire.OFFSET_WIRE) + gremlin.sizes.sizeI64(self.offset);
        }
        return res;
    }
    pub fn encode(self: *const AndroidFsFsyncEndFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidFsFsyncEndFtraceEvent, target: *gremlin.Writer) void {
        if (self.bytes != 0) {
            target.appendInt32(AndroidFsFsyncEndFtraceEventWire.BYTES_WIRE, self.bytes);
        }
        if (self.ino != 0) {
            target.appendUint64(AndroidFsFsyncEndFtraceEventWire.INO_WIRE, self.ino);
        }
        if (self.offset != 0) {
            target.appendInt64(AndroidFsFsyncEndFtraceEventWire.OFFSET_WIRE, self.offset);
        }
    }
};
pub const AndroidFsFsyncEndFtraceEventReader = struct {
    buf: gremlin.Reader,
    _bytes: i32 = 0,
    _ino: u64 = 0,
    _offset: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidFsFsyncEndFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidFsFsyncEndFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidFsFsyncEndFtraceEventWire.BYTES_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._bytes = result.value;
                },
                AndroidFsFsyncEndFtraceEventWire.INO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ino = result.value;
                },
                AndroidFsFsyncEndFtraceEventWire.OFFSET_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._offset = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBytes(self: *const AndroidFsFsyncEndFtraceEventReader) i32 {
        return self._bytes;
    }
    pub inline fn getIno(self: *const AndroidFsFsyncEndFtraceEventReader) u64 {
        return self._ino;
    }
    pub inline fn getOffset(self: *const AndroidFsFsyncEndFtraceEventReader) i64 {
        return self._offset;
    }
};
const AndroidFsFsyncStartFtraceEventWire = struct {
    const CMDLINE_WIRE: gremlin.ProtoWireNumber = 1;
    const I_SIZE_WIRE: gremlin.ProtoWireNumber = 2;
    const INO_WIRE: gremlin.ProtoWireNumber = 3;
    const PATHBUF_WIRE: gremlin.ProtoWireNumber = 4;
    const PID_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const AndroidFsFsyncStartFtraceEvent = struct {
    // fields
    cmdline: ?[]const u8 = null,
    i_size: i64 = 0,
    ino: u64 = 0,
    pathbuf: ?[]const u8 = null,
    pid: i32 = 0,
    pub fn calcProtobufSize(self: *const AndroidFsFsyncStartFtraceEvent) usize {
        var res: usize = 0;
        if (self.cmdline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidFsFsyncStartFtraceEventWire.CMDLINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.i_size != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsFsyncStartFtraceEventWire.I_SIZE_WIRE) + gremlin.sizes.sizeI64(self.i_size);
        }
        if (self.ino != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsFsyncStartFtraceEventWire.INO_WIRE) + gremlin.sizes.sizeU64(self.ino);
        }
        if (self.pathbuf) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidFsFsyncStartFtraceEventWire.PATHBUF_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidFsFsyncStartFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        return res;
    }
    pub fn encode(self: *const AndroidFsFsyncStartFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidFsFsyncStartFtraceEvent, target: *gremlin.Writer) void {
        if (self.cmdline) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidFsFsyncStartFtraceEventWire.CMDLINE_WIRE, v);
            }
        }
        if (self.i_size != 0) {
            target.appendInt64(AndroidFsFsyncStartFtraceEventWire.I_SIZE_WIRE, self.i_size);
        }
        if (self.ino != 0) {
            target.appendUint64(AndroidFsFsyncStartFtraceEventWire.INO_WIRE, self.ino);
        }
        if (self.pathbuf) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidFsFsyncStartFtraceEventWire.PATHBUF_WIRE, v);
            }
        }
        if (self.pid != 0) {
            target.appendInt32(AndroidFsFsyncStartFtraceEventWire.PID_WIRE, self.pid);
        }
    }
};
pub const AndroidFsFsyncStartFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cmdline: ?[]const u8 = null,
    _i_size: i64 = 0,
    _ino: u64 = 0,
    _pathbuf: ?[]const u8 = null,
    _pid: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidFsFsyncStartFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidFsFsyncStartFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidFsFsyncStartFtraceEventWire.CMDLINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._cmdline = result.value;
                },
                AndroidFsFsyncStartFtraceEventWire.I_SIZE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._i_size = result.value;
                },
                AndroidFsFsyncStartFtraceEventWire.INO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ino = result.value;
                },
                AndroidFsFsyncStartFtraceEventWire.PATHBUF_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._pathbuf = result.value;
                },
                AndroidFsFsyncStartFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCmdline(self: *const AndroidFsFsyncStartFtraceEventReader) []const u8 {
        return self._cmdline orelse &[_]u8{};
    }
    pub inline fn getISize(self: *const AndroidFsFsyncStartFtraceEventReader) i64 {
        return self._i_size;
    }
    pub inline fn getIno(self: *const AndroidFsFsyncStartFtraceEventReader) u64 {
        return self._ino;
    }
    pub inline fn getPathbuf(self: *const AndroidFsFsyncStartFtraceEventReader) []const u8 {
        return self._pathbuf orelse &[_]u8{};
    }
    pub inline fn getPid(self: *const AndroidFsFsyncStartFtraceEventReader) i32 {
        return self._pid;
    }
};
