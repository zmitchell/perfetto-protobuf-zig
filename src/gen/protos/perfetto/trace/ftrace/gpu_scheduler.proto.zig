// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DrmSchedJobFtraceEventWire = struct {
    const ENTITY_WIRE: gremlin.ProtoWireNumber = 1;
    const FENCE_WIRE: gremlin.ProtoWireNumber = 2;
    const HW_JOB_COUNT_WIRE: gremlin.ProtoWireNumber = 3;
    const ID_WIRE: gremlin.ProtoWireNumber = 4;
    const JOB_COUNT_WIRE: gremlin.ProtoWireNumber = 5;
    const NAME_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const DrmSchedJobFtraceEvent = struct {
    // fields
    entity: u64 = 0,
    fence: u64 = 0,
    hw_job_count: i32 = 0,
    id: u64 = 0,
    job_count: u32 = 0,
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const DrmSchedJobFtraceEvent) usize {
        var res: usize = 0;
        if (self.entity != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobFtraceEventWire.ENTITY_WIRE) + gremlin.sizes.sizeU64(self.entity);
        }
        if (self.fence != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobFtraceEventWire.FENCE_WIRE) + gremlin.sizes.sizeU64(self.fence);
        }
        if (self.hw_job_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobFtraceEventWire.HW_JOB_COUNT_WIRE) + gremlin.sizes.sizeI32(self.hw_job_count);
        }
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeU64(self.id);
        }
        if (self.job_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobFtraceEventWire.JOB_COUNT_WIRE) + gremlin.sizes.sizeU32(self.job_count);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DrmSchedJobFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const DrmSchedJobFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmSchedJobFtraceEvent, target: *gremlin.Writer) void {
        if (self.entity != 0) {
            target.appendUint64(DrmSchedJobFtraceEventWire.ENTITY_WIRE, self.entity);
        }
        if (self.fence != 0) {
            target.appendUint64(DrmSchedJobFtraceEventWire.FENCE_WIRE, self.fence);
        }
        if (self.hw_job_count != 0) {
            target.appendInt32(DrmSchedJobFtraceEventWire.HW_JOB_COUNT_WIRE, self.hw_job_count);
        }
        if (self.id != 0) {
            target.appendUint64(DrmSchedJobFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.job_count != 0) {
            target.appendUint32(DrmSchedJobFtraceEventWire.JOB_COUNT_WIRE, self.job_count);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(DrmSchedJobFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const DrmSchedJobFtraceEventReader = struct {
    buf: gremlin.Reader,
    _entity: u64 = 0,
    _fence: u64 = 0,
    _hw_job_count: i32 = 0,
    _id: u64 = 0,
    _job_count: u32 = 0,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!DrmSchedJobFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmSchedJobFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmSchedJobFtraceEventWire.ENTITY_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._entity = result.value;
                },
                DrmSchedJobFtraceEventWire.FENCE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence = result.value;
                },
                DrmSchedJobFtraceEventWire.HW_JOB_COUNT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._hw_job_count = result.value;
                },
                DrmSchedJobFtraceEventWire.ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                DrmSchedJobFtraceEventWire.JOB_COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._job_count = result.value;
                },
                DrmSchedJobFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getEntity(self: *const DrmSchedJobFtraceEventReader) u64 {
        return self._entity;
    }
    pub inline fn getFence(self: *const DrmSchedJobFtraceEventReader) u64 {
        return self._fence;
    }
    pub inline fn getHwJobCount(self: *const DrmSchedJobFtraceEventReader) i32 {
        return self._hw_job_count;
    }
    pub inline fn getId(self: *const DrmSchedJobFtraceEventReader) u64 {
        return self._id;
    }
    pub inline fn getJobCount(self: *const DrmSchedJobFtraceEventReader) u32 {
        return self._job_count;
    }
    pub inline fn getName(self: *const DrmSchedJobFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const DrmRunJobFtraceEventWire = struct {
    const ENTITY_WIRE: gremlin.ProtoWireNumber = 1;
    const FENCE_WIRE: gremlin.ProtoWireNumber = 2;
    const HW_JOB_COUNT_WIRE: gremlin.ProtoWireNumber = 3;
    const ID_WIRE: gremlin.ProtoWireNumber = 4;
    const JOB_COUNT_WIRE: gremlin.ProtoWireNumber = 5;
    const NAME_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const DrmRunJobFtraceEvent = struct {
    // fields
    entity: u64 = 0,
    fence: u64 = 0,
    hw_job_count: i32 = 0,
    id: u64 = 0,
    job_count: u32 = 0,
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const DrmRunJobFtraceEvent) usize {
        var res: usize = 0;
        if (self.entity != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmRunJobFtraceEventWire.ENTITY_WIRE) + gremlin.sizes.sizeU64(self.entity);
        }
        if (self.fence != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmRunJobFtraceEventWire.FENCE_WIRE) + gremlin.sizes.sizeU64(self.fence);
        }
        if (self.hw_job_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmRunJobFtraceEventWire.HW_JOB_COUNT_WIRE) + gremlin.sizes.sizeI32(self.hw_job_count);
        }
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmRunJobFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeU64(self.id);
        }
        if (self.job_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmRunJobFtraceEventWire.JOB_COUNT_WIRE) + gremlin.sizes.sizeU32(self.job_count);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DrmRunJobFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const DrmRunJobFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmRunJobFtraceEvent, target: *gremlin.Writer) void {
        if (self.entity != 0) {
            target.appendUint64(DrmRunJobFtraceEventWire.ENTITY_WIRE, self.entity);
        }
        if (self.fence != 0) {
            target.appendUint64(DrmRunJobFtraceEventWire.FENCE_WIRE, self.fence);
        }
        if (self.hw_job_count != 0) {
            target.appendInt32(DrmRunJobFtraceEventWire.HW_JOB_COUNT_WIRE, self.hw_job_count);
        }
        if (self.id != 0) {
            target.appendUint64(DrmRunJobFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.job_count != 0) {
            target.appendUint32(DrmRunJobFtraceEventWire.JOB_COUNT_WIRE, self.job_count);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(DrmRunJobFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const DrmRunJobFtraceEventReader = struct {
    buf: gremlin.Reader,
    _entity: u64 = 0,
    _fence: u64 = 0,
    _hw_job_count: i32 = 0,
    _id: u64 = 0,
    _job_count: u32 = 0,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!DrmRunJobFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmRunJobFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmRunJobFtraceEventWire.ENTITY_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._entity = result.value;
                },
                DrmRunJobFtraceEventWire.FENCE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence = result.value;
                },
                DrmRunJobFtraceEventWire.HW_JOB_COUNT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._hw_job_count = result.value;
                },
                DrmRunJobFtraceEventWire.ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                DrmRunJobFtraceEventWire.JOB_COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._job_count = result.value;
                },
                DrmRunJobFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getEntity(self: *const DrmRunJobFtraceEventReader) u64 {
        return self._entity;
    }
    pub inline fn getFence(self: *const DrmRunJobFtraceEventReader) u64 {
        return self._fence;
    }
    pub inline fn getHwJobCount(self: *const DrmRunJobFtraceEventReader) i32 {
        return self._hw_job_count;
    }
    pub inline fn getId(self: *const DrmRunJobFtraceEventReader) u64 {
        return self._id;
    }
    pub inline fn getJobCount(self: *const DrmRunJobFtraceEventReader) u32 {
        return self._job_count;
    }
    pub inline fn getName(self: *const DrmRunJobFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const DrmSchedProcessJobFtraceEventWire = struct {
    const FENCE_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const DrmSchedProcessJobFtraceEvent = struct {
    // fields
    fence: u64 = 0,
    pub fn calcProtobufSize(self: *const DrmSchedProcessJobFtraceEvent) usize {
        var res: usize = 0;
        if (self.fence != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedProcessJobFtraceEventWire.FENCE_WIRE) + gremlin.sizes.sizeU64(self.fence);
        }
        return res;
    }
    pub fn encode(self: *const DrmSchedProcessJobFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmSchedProcessJobFtraceEvent, target: *gremlin.Writer) void {
        if (self.fence != 0) {
            target.appendUint64(DrmSchedProcessJobFtraceEventWire.FENCE_WIRE, self.fence);
        }
    }
};
pub const DrmSchedProcessJobFtraceEventReader = struct {
    buf: gremlin.Reader,
    _fence: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DrmSchedProcessJobFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmSchedProcessJobFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmSchedProcessJobFtraceEventWire.FENCE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFence(self: *const DrmSchedProcessJobFtraceEventReader) u64 {
        return self._fence;
    }
};
const DrmSchedJobAddDepFtraceEventWire = struct {
    const FENCE_CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const FENCE_SEQNO_WIRE: gremlin.ProtoWireNumber = 2;
    const CTX_WIRE: gremlin.ProtoWireNumber = 3;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DrmSchedJobAddDepFtraceEvent = struct {
    // fields
    fence_context: u64 = 0,
    fence_seqno: u64 = 0,
    ctx: u64 = 0,
    seqno: u64 = 0,
    pub fn calcProtobufSize(self: *const DrmSchedJobAddDepFtraceEvent) usize {
        var res: usize = 0;
        if (self.fence_context != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobAddDepFtraceEventWire.FENCE_CONTEXT_WIRE) + gremlin.sizes.sizeU64(self.fence_context);
        }
        if (self.fence_seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobAddDepFtraceEventWire.FENCE_SEQNO_WIRE) + gremlin.sizes.sizeU64(self.fence_seqno);
        }
        if (self.ctx != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobAddDepFtraceEventWire.CTX_WIRE) + gremlin.sizes.sizeU64(self.ctx);
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobAddDepFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU64(self.seqno);
        }
        return res;
    }
    pub fn encode(self: *const DrmSchedJobAddDepFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmSchedJobAddDepFtraceEvent, target: *gremlin.Writer) void {
        if (self.fence_context != 0) {
            target.appendUint64(DrmSchedJobAddDepFtraceEventWire.FENCE_CONTEXT_WIRE, self.fence_context);
        }
        if (self.fence_seqno != 0) {
            target.appendUint64(DrmSchedJobAddDepFtraceEventWire.FENCE_SEQNO_WIRE, self.fence_seqno);
        }
        if (self.ctx != 0) {
            target.appendUint64(DrmSchedJobAddDepFtraceEventWire.CTX_WIRE, self.ctx);
        }
        if (self.seqno != 0) {
            target.appendUint64(DrmSchedJobAddDepFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
    }
};
pub const DrmSchedJobAddDepFtraceEventReader = struct {
    buf: gremlin.Reader,
    _fence_context: u64 = 0,
    _fence_seqno: u64 = 0,
    _ctx: u64 = 0,
    _seqno: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DrmSchedJobAddDepFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmSchedJobAddDepFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmSchedJobAddDepFtraceEventWire.FENCE_CONTEXT_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_context = result.value;
                },
                DrmSchedJobAddDepFtraceEventWire.FENCE_SEQNO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_seqno = result.value;
                },
                DrmSchedJobAddDepFtraceEventWire.CTX_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ctx = result.value;
                },
                DrmSchedJobAddDepFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFenceContext(self: *const DrmSchedJobAddDepFtraceEventReader) u64 {
        return self._fence_context;
    }
    pub inline fn getFenceSeqno(self: *const DrmSchedJobAddDepFtraceEventReader) u64 {
        return self._fence_seqno;
    }
    pub inline fn getCtx(self: *const DrmSchedJobAddDepFtraceEventReader) u64 {
        return self._ctx;
    }
    pub inline fn getSeqno(self: *const DrmSchedJobAddDepFtraceEventReader) u64 {
        return self._seqno;
    }
};
const DrmSchedJobDoneFtraceEventWire = struct {
    const FENCE_CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const FENCE_SEQNO_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const DrmSchedJobDoneFtraceEvent = struct {
    // fields
    fence_context: u64 = 0,
    fence_seqno: u64 = 0,
    pub fn calcProtobufSize(self: *const DrmSchedJobDoneFtraceEvent) usize {
        var res: usize = 0;
        if (self.fence_context != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobDoneFtraceEventWire.FENCE_CONTEXT_WIRE) + gremlin.sizes.sizeU64(self.fence_context);
        }
        if (self.fence_seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobDoneFtraceEventWire.FENCE_SEQNO_WIRE) + gremlin.sizes.sizeU64(self.fence_seqno);
        }
        return res;
    }
    pub fn encode(self: *const DrmSchedJobDoneFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmSchedJobDoneFtraceEvent, target: *gremlin.Writer) void {
        if (self.fence_context != 0) {
            target.appendUint64(DrmSchedJobDoneFtraceEventWire.FENCE_CONTEXT_WIRE, self.fence_context);
        }
        if (self.fence_seqno != 0) {
            target.appendUint64(DrmSchedJobDoneFtraceEventWire.FENCE_SEQNO_WIRE, self.fence_seqno);
        }
    }
};
pub const DrmSchedJobDoneFtraceEventReader = struct {
    buf: gremlin.Reader,
    _fence_context: u64 = 0,
    _fence_seqno: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DrmSchedJobDoneFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmSchedJobDoneFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmSchedJobDoneFtraceEventWire.FENCE_CONTEXT_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_context = result.value;
                },
                DrmSchedJobDoneFtraceEventWire.FENCE_SEQNO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_seqno = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFenceContext(self: *const DrmSchedJobDoneFtraceEventReader) u64 {
        return self._fence_context;
    }
    pub inline fn getFenceSeqno(self: *const DrmSchedJobDoneFtraceEventReader) u64 {
        return self._fence_seqno;
    }
};
const DrmSchedJobQueueFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const JOB_COUNT_WIRE: gremlin.ProtoWireNumber = 2;
    const HW_JOB_COUNT_WIRE: gremlin.ProtoWireNumber = 3;
    const DEV_WIRE: gremlin.ProtoWireNumber = 4;
    const FENCE_CONTEXT_WIRE: gremlin.ProtoWireNumber = 5;
    const FENCE_SEQNO_WIRE: gremlin.ProtoWireNumber = 6;
    const CLIENT_ID_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const DrmSchedJobQueueFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    job_count: u32 = 0,
    hw_job_count: i32 = 0,
    dev: ?[]const u8 = null,
    fence_context: u64 = 0,
    fence_seqno: u64 = 0,
    client_id: u64 = 0,
    pub fn calcProtobufSize(self: *const DrmSchedJobQueueFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DrmSchedJobQueueFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.job_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobQueueFtraceEventWire.JOB_COUNT_WIRE) + gremlin.sizes.sizeU32(self.job_count);
        }
        if (self.hw_job_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobQueueFtraceEventWire.HW_JOB_COUNT_WIRE) + gremlin.sizes.sizeI32(self.hw_job_count);
        }
        if (self.dev) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DrmSchedJobQueueFtraceEventWire.DEV_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.fence_context != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobQueueFtraceEventWire.FENCE_CONTEXT_WIRE) + gremlin.sizes.sizeU64(self.fence_context);
        }
        if (self.fence_seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobQueueFtraceEventWire.FENCE_SEQNO_WIRE) + gremlin.sizes.sizeU64(self.fence_seqno);
        }
        if (self.client_id != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobQueueFtraceEventWire.CLIENT_ID_WIRE) + gremlin.sizes.sizeU64(self.client_id);
        }
        return res;
    }
    pub fn encode(self: *const DrmSchedJobQueueFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmSchedJobQueueFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(DrmSchedJobQueueFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.job_count != 0) {
            target.appendUint32(DrmSchedJobQueueFtraceEventWire.JOB_COUNT_WIRE, self.job_count);
        }
        if (self.hw_job_count != 0) {
            target.appendInt32(DrmSchedJobQueueFtraceEventWire.HW_JOB_COUNT_WIRE, self.hw_job_count);
        }
        if (self.dev) |v| {
            if (v.len > 0) {
                target.appendBytes(DrmSchedJobQueueFtraceEventWire.DEV_WIRE, v);
            }
        }
        if (self.fence_context != 0) {
            target.appendUint64(DrmSchedJobQueueFtraceEventWire.FENCE_CONTEXT_WIRE, self.fence_context);
        }
        if (self.fence_seqno != 0) {
            target.appendUint64(DrmSchedJobQueueFtraceEventWire.FENCE_SEQNO_WIRE, self.fence_seqno);
        }
        if (self.client_id != 0) {
            target.appendUint64(DrmSchedJobQueueFtraceEventWire.CLIENT_ID_WIRE, self.client_id);
        }
    }
};
pub const DrmSchedJobQueueFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _job_count: u32 = 0,
    _hw_job_count: i32 = 0,
    _dev: ?[]const u8 = null,
    _fence_context: u64 = 0,
    _fence_seqno: u64 = 0,
    _client_id: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DrmSchedJobQueueFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmSchedJobQueueFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmSchedJobQueueFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                DrmSchedJobQueueFtraceEventWire.JOB_COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._job_count = result.value;
                },
                DrmSchedJobQueueFtraceEventWire.HW_JOB_COUNT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._hw_job_count = result.value;
                },
                DrmSchedJobQueueFtraceEventWire.DEV_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._dev = result.value;
                },
                DrmSchedJobQueueFtraceEventWire.FENCE_CONTEXT_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_context = result.value;
                },
                DrmSchedJobQueueFtraceEventWire.FENCE_SEQNO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_seqno = result.value;
                },
                DrmSchedJobQueueFtraceEventWire.CLIENT_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._client_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const DrmSchedJobQueueFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getJobCount(self: *const DrmSchedJobQueueFtraceEventReader) u32 {
        return self._job_count;
    }
    pub inline fn getHwJobCount(self: *const DrmSchedJobQueueFtraceEventReader) i32 {
        return self._hw_job_count;
    }
    pub inline fn getDev(self: *const DrmSchedJobQueueFtraceEventReader) []const u8 {
        return self._dev orelse &[_]u8{};
    }
    pub inline fn getFenceContext(self: *const DrmSchedJobQueueFtraceEventReader) u64 {
        return self._fence_context;
    }
    pub inline fn getFenceSeqno(self: *const DrmSchedJobQueueFtraceEventReader) u64 {
        return self._fence_seqno;
    }
    pub inline fn getClientId(self: *const DrmSchedJobQueueFtraceEventReader) u64 {
        return self._client_id;
    }
};
const DrmSchedJobRunFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const JOB_COUNT_WIRE: gremlin.ProtoWireNumber = 2;
    const HW_JOB_COUNT_WIRE: gremlin.ProtoWireNumber = 3;
    const DEV_WIRE: gremlin.ProtoWireNumber = 4;
    const FENCE_CONTEXT_WIRE: gremlin.ProtoWireNumber = 5;
    const FENCE_SEQNO_WIRE: gremlin.ProtoWireNumber = 6;
    const CLIENT_ID_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const DrmSchedJobRunFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    job_count: u32 = 0,
    hw_job_count: i32 = 0,
    dev: ?[]const u8 = null,
    fence_context: u64 = 0,
    fence_seqno: u64 = 0,
    client_id: u64 = 0,
    pub fn calcProtobufSize(self: *const DrmSchedJobRunFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DrmSchedJobRunFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.job_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobRunFtraceEventWire.JOB_COUNT_WIRE) + gremlin.sizes.sizeU32(self.job_count);
        }
        if (self.hw_job_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobRunFtraceEventWire.HW_JOB_COUNT_WIRE) + gremlin.sizes.sizeI32(self.hw_job_count);
        }
        if (self.dev) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DrmSchedJobRunFtraceEventWire.DEV_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.fence_context != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobRunFtraceEventWire.FENCE_CONTEXT_WIRE) + gremlin.sizes.sizeU64(self.fence_context);
        }
        if (self.fence_seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobRunFtraceEventWire.FENCE_SEQNO_WIRE) + gremlin.sizes.sizeU64(self.fence_seqno);
        }
        if (self.client_id != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobRunFtraceEventWire.CLIENT_ID_WIRE) + gremlin.sizes.sizeU64(self.client_id);
        }
        return res;
    }
    pub fn encode(self: *const DrmSchedJobRunFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmSchedJobRunFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(DrmSchedJobRunFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.job_count != 0) {
            target.appendUint32(DrmSchedJobRunFtraceEventWire.JOB_COUNT_WIRE, self.job_count);
        }
        if (self.hw_job_count != 0) {
            target.appendInt32(DrmSchedJobRunFtraceEventWire.HW_JOB_COUNT_WIRE, self.hw_job_count);
        }
        if (self.dev) |v| {
            if (v.len > 0) {
                target.appendBytes(DrmSchedJobRunFtraceEventWire.DEV_WIRE, v);
            }
        }
        if (self.fence_context != 0) {
            target.appendUint64(DrmSchedJobRunFtraceEventWire.FENCE_CONTEXT_WIRE, self.fence_context);
        }
        if (self.fence_seqno != 0) {
            target.appendUint64(DrmSchedJobRunFtraceEventWire.FENCE_SEQNO_WIRE, self.fence_seqno);
        }
        if (self.client_id != 0) {
            target.appendUint64(DrmSchedJobRunFtraceEventWire.CLIENT_ID_WIRE, self.client_id);
        }
    }
};
pub const DrmSchedJobRunFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _job_count: u32 = 0,
    _hw_job_count: i32 = 0,
    _dev: ?[]const u8 = null,
    _fence_context: u64 = 0,
    _fence_seqno: u64 = 0,
    _client_id: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DrmSchedJobRunFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmSchedJobRunFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmSchedJobRunFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                DrmSchedJobRunFtraceEventWire.JOB_COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._job_count = result.value;
                },
                DrmSchedJobRunFtraceEventWire.HW_JOB_COUNT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._hw_job_count = result.value;
                },
                DrmSchedJobRunFtraceEventWire.DEV_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._dev = result.value;
                },
                DrmSchedJobRunFtraceEventWire.FENCE_CONTEXT_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_context = result.value;
                },
                DrmSchedJobRunFtraceEventWire.FENCE_SEQNO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_seqno = result.value;
                },
                DrmSchedJobRunFtraceEventWire.CLIENT_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._client_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const DrmSchedJobRunFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getJobCount(self: *const DrmSchedJobRunFtraceEventReader) u32 {
        return self._job_count;
    }
    pub inline fn getHwJobCount(self: *const DrmSchedJobRunFtraceEventReader) i32 {
        return self._hw_job_count;
    }
    pub inline fn getDev(self: *const DrmSchedJobRunFtraceEventReader) []const u8 {
        return self._dev orelse &[_]u8{};
    }
    pub inline fn getFenceContext(self: *const DrmSchedJobRunFtraceEventReader) u64 {
        return self._fence_context;
    }
    pub inline fn getFenceSeqno(self: *const DrmSchedJobRunFtraceEventReader) u64 {
        return self._fence_seqno;
    }
    pub inline fn getClientId(self: *const DrmSchedJobRunFtraceEventReader) u64 {
        return self._client_id;
    }
};
const DrmSchedJobUnschedulableFtraceEventWire = struct {
    const FENCE_CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const FENCE_SEQNO_WIRE: gremlin.ProtoWireNumber = 2;
    const CTX_WIRE: gremlin.ProtoWireNumber = 3;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DrmSchedJobUnschedulableFtraceEvent = struct {
    // fields
    fence_context: u64 = 0,
    fence_seqno: u64 = 0,
    ctx: u64 = 0,
    seqno: u64 = 0,
    pub fn calcProtobufSize(self: *const DrmSchedJobUnschedulableFtraceEvent) usize {
        var res: usize = 0;
        if (self.fence_context != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobUnschedulableFtraceEventWire.FENCE_CONTEXT_WIRE) + gremlin.sizes.sizeU64(self.fence_context);
        }
        if (self.fence_seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobUnschedulableFtraceEventWire.FENCE_SEQNO_WIRE) + gremlin.sizes.sizeU64(self.fence_seqno);
        }
        if (self.ctx != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobUnschedulableFtraceEventWire.CTX_WIRE) + gremlin.sizes.sizeU64(self.ctx);
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmSchedJobUnschedulableFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU64(self.seqno);
        }
        return res;
    }
    pub fn encode(self: *const DrmSchedJobUnschedulableFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmSchedJobUnschedulableFtraceEvent, target: *gremlin.Writer) void {
        if (self.fence_context != 0) {
            target.appendUint64(DrmSchedJobUnschedulableFtraceEventWire.FENCE_CONTEXT_WIRE, self.fence_context);
        }
        if (self.fence_seqno != 0) {
            target.appendUint64(DrmSchedJobUnschedulableFtraceEventWire.FENCE_SEQNO_WIRE, self.fence_seqno);
        }
        if (self.ctx != 0) {
            target.appendUint64(DrmSchedJobUnschedulableFtraceEventWire.CTX_WIRE, self.ctx);
        }
        if (self.seqno != 0) {
            target.appendUint64(DrmSchedJobUnschedulableFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
    }
};
pub const DrmSchedJobUnschedulableFtraceEventReader = struct {
    buf: gremlin.Reader,
    _fence_context: u64 = 0,
    _fence_seqno: u64 = 0,
    _ctx: u64 = 0,
    _seqno: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DrmSchedJobUnschedulableFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmSchedJobUnschedulableFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmSchedJobUnschedulableFtraceEventWire.FENCE_CONTEXT_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_context = result.value;
                },
                DrmSchedJobUnschedulableFtraceEventWire.FENCE_SEQNO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_seqno = result.value;
                },
                DrmSchedJobUnschedulableFtraceEventWire.CTX_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ctx = result.value;
                },
                DrmSchedJobUnschedulableFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFenceContext(self: *const DrmSchedJobUnschedulableFtraceEventReader) u64 {
        return self._fence_context;
    }
    pub inline fn getFenceSeqno(self: *const DrmSchedJobUnschedulableFtraceEventReader) u64 {
        return self._fence_seqno;
    }
    pub inline fn getCtx(self: *const DrmSchedJobUnschedulableFtraceEventReader) u64 {
        return self._ctx;
    }
    pub inline fn getSeqno(self: *const DrmSchedJobUnschedulableFtraceEventReader) u64 {
        return self._seqno;
    }
};
