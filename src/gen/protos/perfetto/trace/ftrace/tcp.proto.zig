// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TcpRetransmitSkbFtraceEventWire = struct {
    const DADDR_WIRE: gremlin.ProtoWireNumber = 1;
    const DPORT_WIRE: gremlin.ProtoWireNumber = 2;
    const SADDR_WIRE: gremlin.ProtoWireNumber = 3;
    const SKADDR_WIRE: gremlin.ProtoWireNumber = 4;
    const SKBADDR_WIRE: gremlin.ProtoWireNumber = 5;
    const SPORT_WIRE: gremlin.ProtoWireNumber = 6;
    const STATE_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const TcpRetransmitSkbFtraceEvent = struct {
    // fields
    daddr: u32 = 0,
    dport: u32 = 0,
    saddr: u32 = 0,
    skaddr: u64 = 0,
    skbaddr: u64 = 0,
    sport: u32 = 0,
    state: i32 = 0,
    pub fn calcProtobufSize(self: *const TcpRetransmitSkbFtraceEvent) usize {
        var res: usize = 0;
        if (self.daddr != 0) {
            res += gremlin.sizes.sizeWireNumber(TcpRetransmitSkbFtraceEventWire.DADDR_WIRE) + gremlin.sizes.sizeU32(self.daddr);
        }
        if (self.dport != 0) {
            res += gremlin.sizes.sizeWireNumber(TcpRetransmitSkbFtraceEventWire.DPORT_WIRE) + gremlin.sizes.sizeU32(self.dport);
        }
        if (self.saddr != 0) {
            res += gremlin.sizes.sizeWireNumber(TcpRetransmitSkbFtraceEventWire.SADDR_WIRE) + gremlin.sizes.sizeU32(self.saddr);
        }
        if (self.skaddr != 0) {
            res += gremlin.sizes.sizeWireNumber(TcpRetransmitSkbFtraceEventWire.SKADDR_WIRE) + gremlin.sizes.sizeU64(self.skaddr);
        }
        if (self.skbaddr != 0) {
            res += gremlin.sizes.sizeWireNumber(TcpRetransmitSkbFtraceEventWire.SKBADDR_WIRE) + gremlin.sizes.sizeU64(self.skbaddr);
        }
        if (self.sport != 0) {
            res += gremlin.sizes.sizeWireNumber(TcpRetransmitSkbFtraceEventWire.SPORT_WIRE) + gremlin.sizes.sizeU32(self.sport);
        }
        if (self.state != 0) {
            res += gremlin.sizes.sizeWireNumber(TcpRetransmitSkbFtraceEventWire.STATE_WIRE) + gremlin.sizes.sizeI32(self.state);
        }
        return res;
    }
    pub fn encode(self: *const TcpRetransmitSkbFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TcpRetransmitSkbFtraceEvent, target: *gremlin.Writer) void {
        if (self.daddr != 0) {
            target.appendUint32(TcpRetransmitSkbFtraceEventWire.DADDR_WIRE, self.daddr);
        }
        if (self.dport != 0) {
            target.appendUint32(TcpRetransmitSkbFtraceEventWire.DPORT_WIRE, self.dport);
        }
        if (self.saddr != 0) {
            target.appendUint32(TcpRetransmitSkbFtraceEventWire.SADDR_WIRE, self.saddr);
        }
        if (self.skaddr != 0) {
            target.appendUint64(TcpRetransmitSkbFtraceEventWire.SKADDR_WIRE, self.skaddr);
        }
        if (self.skbaddr != 0) {
            target.appendUint64(TcpRetransmitSkbFtraceEventWire.SKBADDR_WIRE, self.skbaddr);
        }
        if (self.sport != 0) {
            target.appendUint32(TcpRetransmitSkbFtraceEventWire.SPORT_WIRE, self.sport);
        }
        if (self.state != 0) {
            target.appendInt32(TcpRetransmitSkbFtraceEventWire.STATE_WIRE, self.state);
        }
    }
};
pub const TcpRetransmitSkbFtraceEventReader = struct {
    buf: gremlin.Reader,
    _daddr: u32 = 0,
    _dport: u32 = 0,
    _saddr: u32 = 0,
    _skaddr: u64 = 0,
    _skbaddr: u64 = 0,
    _sport: u32 = 0,
    _state: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!TcpRetransmitSkbFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = TcpRetransmitSkbFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TcpRetransmitSkbFtraceEventWire.DADDR_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._daddr = result.value;
                },
                TcpRetransmitSkbFtraceEventWire.DPORT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._dport = result.value;
                },
                TcpRetransmitSkbFtraceEventWire.SADDR_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._saddr = result.value;
                },
                TcpRetransmitSkbFtraceEventWire.SKADDR_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._skaddr = result.value;
                },
                TcpRetransmitSkbFtraceEventWire.SKBADDR_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._skbaddr = result.value;
                },
                TcpRetransmitSkbFtraceEventWire.SPORT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._sport = result.value;
                },
                TcpRetransmitSkbFtraceEventWire.STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._state = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDaddr(self: *const TcpRetransmitSkbFtraceEventReader) u32 {
        return self._daddr;
    }
    pub inline fn getDport(self: *const TcpRetransmitSkbFtraceEventReader) u32 {
        return self._dport;
    }
    pub inline fn getSaddr(self: *const TcpRetransmitSkbFtraceEventReader) u32 {
        return self._saddr;
    }
    pub inline fn getSkaddr(self: *const TcpRetransmitSkbFtraceEventReader) u64 {
        return self._skaddr;
    }
    pub inline fn getSkbaddr(self: *const TcpRetransmitSkbFtraceEventReader) u64 {
        return self._skbaddr;
    }
    pub inline fn getSport(self: *const TcpRetransmitSkbFtraceEventReader) u32 {
        return self._sport;
    }
    pub inline fn getState(self: *const TcpRetransmitSkbFtraceEventReader) i32 {
        return self._state;
    }
};
