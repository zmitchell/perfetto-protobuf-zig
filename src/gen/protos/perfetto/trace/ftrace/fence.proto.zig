// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const FenceInitFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const FenceInitFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const FenceInitFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(FenceInitFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FenceInitFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(FenceInitFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FenceInitFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const FenceInitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FenceInitFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(FenceInitFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(FenceInitFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(FenceInitFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(FenceInitFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const FenceInitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!FenceInitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FenceInitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FenceInitFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                FenceInitFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                FenceInitFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                FenceInitFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const FenceInitFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const FenceInitFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const FenceInitFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const FenceInitFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
const FenceDestroyFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const FenceDestroyFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const FenceDestroyFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(FenceDestroyFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FenceDestroyFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(FenceDestroyFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FenceDestroyFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const FenceDestroyFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FenceDestroyFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(FenceDestroyFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(FenceDestroyFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(FenceDestroyFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(FenceDestroyFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const FenceDestroyFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!FenceDestroyFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FenceDestroyFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FenceDestroyFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                FenceDestroyFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                FenceDestroyFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                FenceDestroyFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const FenceDestroyFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const FenceDestroyFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const FenceDestroyFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const FenceDestroyFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
const FenceEnableSignalFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const FenceEnableSignalFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const FenceEnableSignalFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(FenceEnableSignalFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FenceEnableSignalFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(FenceEnableSignalFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FenceEnableSignalFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const FenceEnableSignalFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FenceEnableSignalFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(FenceEnableSignalFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(FenceEnableSignalFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(FenceEnableSignalFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(FenceEnableSignalFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const FenceEnableSignalFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!FenceEnableSignalFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FenceEnableSignalFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FenceEnableSignalFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                FenceEnableSignalFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                FenceEnableSignalFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                FenceEnableSignalFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const FenceEnableSignalFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const FenceEnableSignalFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const FenceEnableSignalFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const FenceEnableSignalFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
const FenceSignaledFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const FenceSignaledFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const FenceSignaledFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(FenceSignaledFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FenceSignaledFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(FenceSignaledFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FenceSignaledFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const FenceSignaledFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FenceSignaledFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(FenceSignaledFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(FenceSignaledFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(FenceSignaledFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(FenceSignaledFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const FenceSignaledFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!FenceSignaledFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FenceSignaledFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FenceSignaledFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                FenceSignaledFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                FenceSignaledFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                FenceSignaledFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const FenceSignaledFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const FenceSignaledFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const FenceSignaledFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const FenceSignaledFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
