// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const ftrace_event_bundle = @import("ftrace_event_bundle.proto.zig");
// structs
const TestBundleWrapperWire = struct {
    const BEFORE_WIRE: gremlin.ProtoWireNumber = 1;
    const BUNDLE_WIRE: gremlin.ProtoWireNumber = 2;
    const AFTER_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const TestBundleWrapper = struct {
    // fields
    before: ?[]const u8 = null,
    bundle: ?[]const ?ftrace_event_bundle.FtraceEventBundle = null,
    after: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const TestBundleWrapper) usize {
        var res: usize = 0;
        if (self.before) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TestBundleWrapperWire.BEFORE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.bundle) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(TestBundleWrapperWire.BUNDLE_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.after) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TestBundleWrapperWire.AFTER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const TestBundleWrapper, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TestBundleWrapper, target: *gremlin.Writer) void {
        if (self.before) |v| {
            if (v.len > 0) {
                target.appendBytes(TestBundleWrapperWire.BEFORE_WIRE, v);
            }
        }
        if (self.bundle) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(TestBundleWrapperWire.BUNDLE_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(TestBundleWrapperWire.BUNDLE_WIRE, 0);
                }
            }
        }
        if (self.after) |v| {
            if (v.len > 0) {
                target.appendBytes(TestBundleWrapperWire.AFTER_WIRE, v);
            }
        }
    }
};
pub const TestBundleWrapperReader = struct {
    buf: gremlin.Reader,
    _before: ?[]const u8 = null,
    _bundle_offset: ?usize = null,
    _bundle_last_offset: ?usize = null,
    _bundle_cnt: usize = 0,
    _after: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!TestBundleWrapperReader {
        const buf = gremlin.Reader.init(src);
        var res = TestBundleWrapperReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TestBundleWrapperWire.BEFORE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._before = result.value;
                },
                TestBundleWrapperWire.BUNDLE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._bundle_offset == null) {
                        res._bundle_offset = offset - result.size;
                    }
                    res._bundle_last_offset = offset;
                    res._bundle_cnt += 1;
                },
                TestBundleWrapperWire.AFTER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._after = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBefore(self: *const TestBundleWrapperReader) []const u8 {
        return self._before orelse &[_]u8{};
    }
    pub fn bundleCount(self: *const TestBundleWrapperReader) usize {
        return self._bundle_cnt;
    }
    pub fn bundleNext(self: *TestBundleWrapperReader) ?ftrace_event_bundle.FtraceEventBundleReader {
        if (self._bundle_offset == null) return null;
        const current_offset = self._bundle_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ftrace_event_bundle.FtraceEventBundleReader.init(result.value) catch return null;
        if (self._bundle_last_offset != null and current_offset >= self._bundle_last_offset.?) {
            self._bundle_offset = null;
            return msg;
        }
        if (self._bundle_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._bundle_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == TestBundleWrapperWire.BUNDLE_WIRE) {
                self._bundle_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._bundle_offset = null;
        return msg;
    }
    pub inline fn getAfter(self: *const TestBundleWrapperReader) []const u8 {
        return self._after orelse &[_]u8{};
    }
};
