// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const OomScoreAdjUpdateFtraceEventWire = struct {
    const COMM_WIRE: gremlin.ProtoWireNumber = 1;
    const OOM_SCORE_ADJ_WIRE: gremlin.ProtoWireNumber = 2;
    const PID_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const OomScoreAdjUpdateFtraceEvent = struct {
    // fields
    comm: ?[]const u8 = null,
    oom_score_adj: i32 = 0,
    pid: i32 = 0,
    pub fn calcProtobufSize(self: *const OomScoreAdjUpdateFtraceEvent) usize {
        var res: usize = 0;
        if (self.comm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(OomScoreAdjUpdateFtraceEventWire.COMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.oom_score_adj != 0) {
            res += gremlin.sizes.sizeWireNumber(OomScoreAdjUpdateFtraceEventWire.OOM_SCORE_ADJ_WIRE) + gremlin.sizes.sizeI32(self.oom_score_adj);
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(OomScoreAdjUpdateFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        return res;
    }
    pub fn encode(self: *const OomScoreAdjUpdateFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const OomScoreAdjUpdateFtraceEvent, target: *gremlin.Writer) void {
        if (self.comm) |v| {
            if (v.len > 0) {
                target.appendBytes(OomScoreAdjUpdateFtraceEventWire.COMM_WIRE, v);
            }
        }
        if (self.oom_score_adj != 0) {
            target.appendInt32(OomScoreAdjUpdateFtraceEventWire.OOM_SCORE_ADJ_WIRE, self.oom_score_adj);
        }
        if (self.pid != 0) {
            target.appendInt32(OomScoreAdjUpdateFtraceEventWire.PID_WIRE, self.pid);
        }
    }
};
pub const OomScoreAdjUpdateFtraceEventReader = struct {
    buf: gremlin.Reader,
    _comm: ?[]const u8 = null,
    _oom_score_adj: i32 = 0,
    _pid: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!OomScoreAdjUpdateFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = OomScoreAdjUpdateFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                OomScoreAdjUpdateFtraceEventWire.COMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._comm = result.value;
                },
                OomScoreAdjUpdateFtraceEventWire.OOM_SCORE_ADJ_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._oom_score_adj = result.value;
                },
                OomScoreAdjUpdateFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getComm(self: *const OomScoreAdjUpdateFtraceEventReader) []const u8 {
        return self._comm orelse &[_]u8{};
    }
    pub inline fn getOomScoreAdj(self: *const OomScoreAdjUpdateFtraceEventReader) i32 {
        return self._oom_score_adj;
    }
    pub inline fn getPid(self: *const OomScoreAdjUpdateFtraceEventReader) i32 {
        return self._pid;
    }
};
const MarkVictimFtraceEventWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const MarkVictimFtraceEvent = struct {
    // fields
    pid: i32 = 0,
    pub fn calcProtobufSize(self: *const MarkVictimFtraceEvent) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(MarkVictimFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        return res;
    }
    pub fn encode(self: *const MarkVictimFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const MarkVictimFtraceEvent, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendInt32(MarkVictimFtraceEventWire.PID_WIRE, self.pid);
        }
    }
};
pub const MarkVictimFtraceEventReader = struct {
    buf: gremlin.Reader,
    _pid: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!MarkVictimFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = MarkVictimFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                MarkVictimFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const MarkVictimFtraceEventReader) i32 {
        return self._pid;
    }
};
