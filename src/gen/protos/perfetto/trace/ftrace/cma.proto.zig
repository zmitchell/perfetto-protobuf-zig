// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const CmaAllocStartFtraceEventWire = struct {
    const ALIGN_WIRE: gremlin.ProtoWireNumber = 1;
    const COUNT_WIRE: gremlin.ProtoWireNumber = 2;
    const NAME_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const CmaAllocStartFtraceEvent = struct {
    // fields
    align_: u32 = 0,
    count: u32 = 0,
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const CmaAllocStartFtraceEvent) usize {
        var res: usize = 0;
        if (self.align_ != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocStartFtraceEventWire.ALIGN_WIRE) + gremlin.sizes.sizeU32(self.align_);
        }
        if (self.count != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocStartFtraceEventWire.COUNT_WIRE) + gremlin.sizes.sizeU32(self.count);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CmaAllocStartFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const CmaAllocStartFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CmaAllocStartFtraceEvent, target: *gremlin.Writer) void {
        if (self.align_ != 0) {
            target.appendUint32(CmaAllocStartFtraceEventWire.ALIGN_WIRE, self.align_);
        }
        if (self.count != 0) {
            target.appendUint32(CmaAllocStartFtraceEventWire.COUNT_WIRE, self.count);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(CmaAllocStartFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const CmaAllocStartFtraceEventReader = struct {
    buf: gremlin.Reader,
    _align_: u32 = 0,
    _count: u32 = 0,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!CmaAllocStartFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CmaAllocStartFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CmaAllocStartFtraceEventWire.ALIGN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._align_ = result.value;
                },
                CmaAllocStartFtraceEventWire.COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._count = result.value;
                },
                CmaAllocStartFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getAlign(self: *const CmaAllocStartFtraceEventReader) u32 {
        return self._align_;
    }
    pub inline fn getCount(self: *const CmaAllocStartFtraceEventReader) u32 {
        return self._count;
    }
    pub inline fn getName(self: *const CmaAllocStartFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const CmaAllocInfoFtraceEventWire = struct {
    const ALIGN_WIRE: gremlin.ProtoWireNumber = 1;
    const COUNT_WIRE: gremlin.ProtoWireNumber = 2;
    const ERR_ISO_WIRE: gremlin.ProtoWireNumber = 3;
    const ERR_MIG_WIRE: gremlin.ProtoWireNumber = 4;
    const ERR_TEST_WIRE: gremlin.ProtoWireNumber = 5;
    const NAME_WIRE: gremlin.ProtoWireNumber = 6;
    const NR_MAPPED_WIRE: gremlin.ProtoWireNumber = 7;
    const NR_MIGRATED_WIRE: gremlin.ProtoWireNumber = 8;
    const NR_RECLAIMED_WIRE: gremlin.ProtoWireNumber = 9;
    const PFN_WIRE: gremlin.ProtoWireNumber = 10;
};
pub const CmaAllocInfoFtraceEvent = struct {
    // fields
    align_: u32 = 0,
    count: u32 = 0,
    err_iso: u32 = 0,
    err_mig: u32 = 0,
    err_test: u32 = 0,
    name: ?[]const u8 = null,
    nr_mapped: u64 = 0,
    nr_migrated: u64 = 0,
    nr_reclaimed: u64 = 0,
    pfn: u64 = 0,
    pub fn calcProtobufSize(self: *const CmaAllocInfoFtraceEvent) usize {
        var res: usize = 0;
        if (self.align_ != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.ALIGN_WIRE) + gremlin.sizes.sizeU32(self.align_);
        }
        if (self.count != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.COUNT_WIRE) + gremlin.sizes.sizeU32(self.count);
        }
        if (self.err_iso != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.ERR_ISO_WIRE) + gremlin.sizes.sizeU32(self.err_iso);
        }
        if (self.err_mig != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.ERR_MIG_WIRE) + gremlin.sizes.sizeU32(self.err_mig);
        }
        if (self.err_test != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.ERR_TEST_WIRE) + gremlin.sizes.sizeU32(self.err_test);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.nr_mapped != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.NR_MAPPED_WIRE) + gremlin.sizes.sizeU64(self.nr_mapped);
        }
        if (self.nr_migrated != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.NR_MIGRATED_WIRE) + gremlin.sizes.sizeU64(self.nr_migrated);
        }
        if (self.nr_reclaimed != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.NR_RECLAIMED_WIRE) + gremlin.sizes.sizeU64(self.nr_reclaimed);
        }
        if (self.pfn != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocInfoFtraceEventWire.PFN_WIRE) + gremlin.sizes.sizeU64(self.pfn);
        }
        return res;
    }
    pub fn encode(self: *const CmaAllocInfoFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CmaAllocInfoFtraceEvent, target: *gremlin.Writer) void {
        if (self.align_ != 0) {
            target.appendUint32(CmaAllocInfoFtraceEventWire.ALIGN_WIRE, self.align_);
        }
        if (self.count != 0) {
            target.appendUint32(CmaAllocInfoFtraceEventWire.COUNT_WIRE, self.count);
        }
        if (self.err_iso != 0) {
            target.appendUint32(CmaAllocInfoFtraceEventWire.ERR_ISO_WIRE, self.err_iso);
        }
        if (self.err_mig != 0) {
            target.appendUint32(CmaAllocInfoFtraceEventWire.ERR_MIG_WIRE, self.err_mig);
        }
        if (self.err_test != 0) {
            target.appendUint32(CmaAllocInfoFtraceEventWire.ERR_TEST_WIRE, self.err_test);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(CmaAllocInfoFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.nr_mapped != 0) {
            target.appendUint64(CmaAllocInfoFtraceEventWire.NR_MAPPED_WIRE, self.nr_mapped);
        }
        if (self.nr_migrated != 0) {
            target.appendUint64(CmaAllocInfoFtraceEventWire.NR_MIGRATED_WIRE, self.nr_migrated);
        }
        if (self.nr_reclaimed != 0) {
            target.appendUint64(CmaAllocInfoFtraceEventWire.NR_RECLAIMED_WIRE, self.nr_reclaimed);
        }
        if (self.pfn != 0) {
            target.appendUint64(CmaAllocInfoFtraceEventWire.PFN_WIRE, self.pfn);
        }
    }
};
pub const CmaAllocInfoFtraceEventReader = struct {
    buf: gremlin.Reader,
    _align_: u32 = 0,
    _count: u32 = 0,
    _err_iso: u32 = 0,
    _err_mig: u32 = 0,
    _err_test: u32 = 0,
    _name: ?[]const u8 = null,
    _nr_mapped: u64 = 0,
    _nr_migrated: u64 = 0,
    _nr_reclaimed: u64 = 0,
    _pfn: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!CmaAllocInfoFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CmaAllocInfoFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CmaAllocInfoFtraceEventWire.ALIGN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._align_ = result.value;
                },
                CmaAllocInfoFtraceEventWire.COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._count = result.value;
                },
                CmaAllocInfoFtraceEventWire.ERR_ISO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._err_iso = result.value;
                },
                CmaAllocInfoFtraceEventWire.ERR_MIG_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._err_mig = result.value;
                },
                CmaAllocInfoFtraceEventWire.ERR_TEST_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._err_test = result.value;
                },
                CmaAllocInfoFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                CmaAllocInfoFtraceEventWire.NR_MAPPED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._nr_mapped = result.value;
                },
                CmaAllocInfoFtraceEventWire.NR_MIGRATED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._nr_migrated = result.value;
                },
                CmaAllocInfoFtraceEventWire.NR_RECLAIMED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._nr_reclaimed = result.value;
                },
                CmaAllocInfoFtraceEventWire.PFN_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._pfn = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getAlign(self: *const CmaAllocInfoFtraceEventReader) u32 {
        return self._align_;
    }
    pub inline fn getCount(self: *const CmaAllocInfoFtraceEventReader) u32 {
        return self._count;
    }
    pub inline fn getErrIso(self: *const CmaAllocInfoFtraceEventReader) u32 {
        return self._err_iso;
    }
    pub inline fn getErrMig(self: *const CmaAllocInfoFtraceEventReader) u32 {
        return self._err_mig;
    }
    pub inline fn getErrTest(self: *const CmaAllocInfoFtraceEventReader) u32 {
        return self._err_test;
    }
    pub inline fn getName(self: *const CmaAllocInfoFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getNrMapped(self: *const CmaAllocInfoFtraceEventReader) u64 {
        return self._nr_mapped;
    }
    pub inline fn getNrMigrated(self: *const CmaAllocInfoFtraceEventReader) u64 {
        return self._nr_migrated;
    }
    pub inline fn getNrReclaimed(self: *const CmaAllocInfoFtraceEventReader) u64 {
        return self._nr_reclaimed;
    }
    pub inline fn getPfn(self: *const CmaAllocInfoFtraceEventReader) u64 {
        return self._pfn;
    }
};
const CmaAllocFinishFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const PFN_WIRE: gremlin.ProtoWireNumber = 2;
    const PAGE_WIRE: gremlin.ProtoWireNumber = 3;
    const COUNT_WIRE: gremlin.ProtoWireNumber = 4;
    const ALIGN_WIRE: gremlin.ProtoWireNumber = 5;
    const ERRORNO_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const CmaAllocFinishFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    pfn: u64 = 0,
    page: u64 = 0,
    count: u64 = 0,
    align_: u32 = 0,
    errorno: i32 = 0,
    pub fn calcProtobufSize(self: *const CmaAllocFinishFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(CmaAllocFinishFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.pfn != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocFinishFtraceEventWire.PFN_WIRE) + gremlin.sizes.sizeU64(self.pfn);
        }
        if (self.page != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocFinishFtraceEventWire.PAGE_WIRE) + gremlin.sizes.sizeU64(self.page);
        }
        if (self.count != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocFinishFtraceEventWire.COUNT_WIRE) + gremlin.sizes.sizeU64(self.count);
        }
        if (self.align_ != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocFinishFtraceEventWire.ALIGN_WIRE) + gremlin.sizes.sizeU32(self.align_);
        }
        if (self.errorno != 0) {
            res += gremlin.sizes.sizeWireNumber(CmaAllocFinishFtraceEventWire.ERRORNO_WIRE) + gremlin.sizes.sizeI32(self.errorno);
        }
        return res;
    }
    pub fn encode(self: *const CmaAllocFinishFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CmaAllocFinishFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(CmaAllocFinishFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.pfn != 0) {
            target.appendUint64(CmaAllocFinishFtraceEventWire.PFN_WIRE, self.pfn);
        }
        if (self.page != 0) {
            target.appendUint64(CmaAllocFinishFtraceEventWire.PAGE_WIRE, self.page);
        }
        if (self.count != 0) {
            target.appendUint64(CmaAllocFinishFtraceEventWire.COUNT_WIRE, self.count);
        }
        if (self.align_ != 0) {
            target.appendUint32(CmaAllocFinishFtraceEventWire.ALIGN_WIRE, self.align_);
        }
        if (self.errorno != 0) {
            target.appendInt32(CmaAllocFinishFtraceEventWire.ERRORNO_WIRE, self.errorno);
        }
    }
};
pub const CmaAllocFinishFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _pfn: u64 = 0,
    _page: u64 = 0,
    _count: u64 = 0,
    _align_: u32 = 0,
    _errorno: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!CmaAllocFinishFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CmaAllocFinishFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CmaAllocFinishFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                CmaAllocFinishFtraceEventWire.PFN_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._pfn = result.value;
                },
                CmaAllocFinishFtraceEventWire.PAGE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._page = result.value;
                },
                CmaAllocFinishFtraceEventWire.COUNT_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._count = result.value;
                },
                CmaAllocFinishFtraceEventWire.ALIGN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._align_ = result.value;
                },
                CmaAllocFinishFtraceEventWire.ERRORNO_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._errorno = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const CmaAllocFinishFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getPfn(self: *const CmaAllocFinishFtraceEventReader) u64 {
        return self._pfn;
    }
    pub inline fn getPage(self: *const CmaAllocFinishFtraceEventReader) u64 {
        return self._page;
    }
    pub inline fn getCount(self: *const CmaAllocFinishFtraceEventReader) u64 {
        return self._count;
    }
    pub inline fn getAlign(self: *const CmaAllocFinishFtraceEventReader) u32 {
        return self._align_;
    }
    pub inline fn getErrorno(self: *const CmaAllocFinishFtraceEventReader) i32 {
        return self._errorno;
    }
};
