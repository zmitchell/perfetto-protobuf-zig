// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const CpuhpExitFtraceEventWire = struct {
    const CPU_WIRE: gremlin.ProtoWireNumber = 1;
    const IDX_WIRE: gremlin.ProtoWireNumber = 2;
    const RET_WIRE: gremlin.ProtoWireNumber = 3;
    const STATE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const CpuhpExitFtraceEvent = struct {
    // fields
    cpu: u32 = 0,
    idx: i32 = 0,
    ret: i32 = 0,
    state: i32 = 0,
    pub fn calcProtobufSize(self: *const CpuhpExitFtraceEvent) usize {
        var res: usize = 0;
        if (self.cpu != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpExitFtraceEventWire.CPU_WIRE) + gremlin.sizes.sizeU32(self.cpu);
        }
        if (self.idx != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpExitFtraceEventWire.IDX_WIRE) + gremlin.sizes.sizeI32(self.idx);
        }
        if (self.ret != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpExitFtraceEventWire.RET_WIRE) + gremlin.sizes.sizeI32(self.ret);
        }
        if (self.state != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpExitFtraceEventWire.STATE_WIRE) + gremlin.sizes.sizeI32(self.state);
        }
        return res;
    }
    pub fn encode(self: *const CpuhpExitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CpuhpExitFtraceEvent, target: *gremlin.Writer) void {
        if (self.cpu != 0) {
            target.appendUint32(CpuhpExitFtraceEventWire.CPU_WIRE, self.cpu);
        }
        if (self.idx != 0) {
            target.appendInt32(CpuhpExitFtraceEventWire.IDX_WIRE, self.idx);
        }
        if (self.ret != 0) {
            target.appendInt32(CpuhpExitFtraceEventWire.RET_WIRE, self.ret);
        }
        if (self.state != 0) {
            target.appendInt32(CpuhpExitFtraceEventWire.STATE_WIRE, self.state);
        }
    }
};
pub const CpuhpExitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cpu: u32 = 0,
    _idx: i32 = 0,
    _ret: i32 = 0,
    _state: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!CpuhpExitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CpuhpExitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CpuhpExitFtraceEventWire.CPU_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cpu = result.value;
                },
                CpuhpExitFtraceEventWire.IDX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._idx = result.value;
                },
                CpuhpExitFtraceEventWire.RET_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._ret = result.value;
                },
                CpuhpExitFtraceEventWire.STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._state = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCpu(self: *const CpuhpExitFtraceEventReader) u32 {
        return self._cpu;
    }
    pub inline fn getIdx(self: *const CpuhpExitFtraceEventReader) i32 {
        return self._idx;
    }
    pub inline fn getRet(self: *const CpuhpExitFtraceEventReader) i32 {
        return self._ret;
    }
    pub inline fn getState(self: *const CpuhpExitFtraceEventReader) i32 {
        return self._state;
    }
};
const CpuhpMultiEnterFtraceEventWire = struct {
    const CPU_WIRE: gremlin.ProtoWireNumber = 1;
    const FUN_WIRE: gremlin.ProtoWireNumber = 2;
    const IDX_WIRE: gremlin.ProtoWireNumber = 3;
    const TARGET_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const CpuhpMultiEnterFtraceEvent = struct {
    // fields
    cpu: u32 = 0,
    fun: u64 = 0,
    idx: i32 = 0,
    target: i32 = 0,
    pub fn calcProtobufSize(self: *const CpuhpMultiEnterFtraceEvent) usize {
        var res: usize = 0;
        if (self.cpu != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpMultiEnterFtraceEventWire.CPU_WIRE) + gremlin.sizes.sizeU32(self.cpu);
        }
        if (self.fun != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpMultiEnterFtraceEventWire.FUN_WIRE) + gremlin.sizes.sizeU64(self.fun);
        }
        if (self.idx != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpMultiEnterFtraceEventWire.IDX_WIRE) + gremlin.sizes.sizeI32(self.idx);
        }
        if (self.target != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpMultiEnterFtraceEventWire.TARGET_WIRE) + gremlin.sizes.sizeI32(self.target);
        }
        return res;
    }
    pub fn encode(self: *const CpuhpMultiEnterFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CpuhpMultiEnterFtraceEvent, target: *gremlin.Writer) void {
        if (self.cpu != 0) {
            target.appendUint32(CpuhpMultiEnterFtraceEventWire.CPU_WIRE, self.cpu);
        }
        if (self.fun != 0) {
            target.appendUint64(CpuhpMultiEnterFtraceEventWire.FUN_WIRE, self.fun);
        }
        if (self.idx != 0) {
            target.appendInt32(CpuhpMultiEnterFtraceEventWire.IDX_WIRE, self.idx);
        }
        if (self.target != 0) {
            target.appendInt32(CpuhpMultiEnterFtraceEventWire.TARGET_WIRE, self.target);
        }
    }
};
pub const CpuhpMultiEnterFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cpu: u32 = 0,
    _fun: u64 = 0,
    _idx: i32 = 0,
    _target: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!CpuhpMultiEnterFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CpuhpMultiEnterFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CpuhpMultiEnterFtraceEventWire.CPU_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cpu = result.value;
                },
                CpuhpMultiEnterFtraceEventWire.FUN_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fun = result.value;
                },
                CpuhpMultiEnterFtraceEventWire.IDX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._idx = result.value;
                },
                CpuhpMultiEnterFtraceEventWire.TARGET_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._target = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCpu(self: *const CpuhpMultiEnterFtraceEventReader) u32 {
        return self._cpu;
    }
    pub inline fn getFun(self: *const CpuhpMultiEnterFtraceEventReader) u64 {
        return self._fun;
    }
    pub inline fn getIdx(self: *const CpuhpMultiEnterFtraceEventReader) i32 {
        return self._idx;
    }
    pub inline fn getTarget(self: *const CpuhpMultiEnterFtraceEventReader) i32 {
        return self._target;
    }
};
const CpuhpEnterFtraceEventWire = struct {
    const CPU_WIRE: gremlin.ProtoWireNumber = 1;
    const FUN_WIRE: gremlin.ProtoWireNumber = 2;
    const IDX_WIRE: gremlin.ProtoWireNumber = 3;
    const TARGET_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const CpuhpEnterFtraceEvent = struct {
    // fields
    cpu: u32 = 0,
    fun: u64 = 0,
    idx: i32 = 0,
    target: i32 = 0,
    pub fn calcProtobufSize(self: *const CpuhpEnterFtraceEvent) usize {
        var res: usize = 0;
        if (self.cpu != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpEnterFtraceEventWire.CPU_WIRE) + gremlin.sizes.sizeU32(self.cpu);
        }
        if (self.fun != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpEnterFtraceEventWire.FUN_WIRE) + gremlin.sizes.sizeU64(self.fun);
        }
        if (self.idx != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpEnterFtraceEventWire.IDX_WIRE) + gremlin.sizes.sizeI32(self.idx);
        }
        if (self.target != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpEnterFtraceEventWire.TARGET_WIRE) + gremlin.sizes.sizeI32(self.target);
        }
        return res;
    }
    pub fn encode(self: *const CpuhpEnterFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CpuhpEnterFtraceEvent, target: *gremlin.Writer) void {
        if (self.cpu != 0) {
            target.appendUint32(CpuhpEnterFtraceEventWire.CPU_WIRE, self.cpu);
        }
        if (self.fun != 0) {
            target.appendUint64(CpuhpEnterFtraceEventWire.FUN_WIRE, self.fun);
        }
        if (self.idx != 0) {
            target.appendInt32(CpuhpEnterFtraceEventWire.IDX_WIRE, self.idx);
        }
        if (self.target != 0) {
            target.appendInt32(CpuhpEnterFtraceEventWire.TARGET_WIRE, self.target);
        }
    }
};
pub const CpuhpEnterFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cpu: u32 = 0,
    _fun: u64 = 0,
    _idx: i32 = 0,
    _target: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!CpuhpEnterFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CpuhpEnterFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CpuhpEnterFtraceEventWire.CPU_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cpu = result.value;
                },
                CpuhpEnterFtraceEventWire.FUN_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fun = result.value;
                },
                CpuhpEnterFtraceEventWire.IDX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._idx = result.value;
                },
                CpuhpEnterFtraceEventWire.TARGET_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._target = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCpu(self: *const CpuhpEnterFtraceEventReader) u32 {
        return self._cpu;
    }
    pub inline fn getFun(self: *const CpuhpEnterFtraceEventReader) u64 {
        return self._fun;
    }
    pub inline fn getIdx(self: *const CpuhpEnterFtraceEventReader) i32 {
        return self._idx;
    }
    pub inline fn getTarget(self: *const CpuhpEnterFtraceEventReader) i32 {
        return self._target;
    }
};
const CpuhpLatencyFtraceEventWire = struct {
    const CPU_WIRE: gremlin.ProtoWireNumber = 1;
    const RET_WIRE: gremlin.ProtoWireNumber = 2;
    const STATE_WIRE: gremlin.ProtoWireNumber = 3;
    const TIME_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const CpuhpLatencyFtraceEvent = struct {
    // fields
    cpu: u32 = 0,
    ret: i32 = 0,
    state: u32 = 0,
    time: u64 = 0,
    pub fn calcProtobufSize(self: *const CpuhpLatencyFtraceEvent) usize {
        var res: usize = 0;
        if (self.cpu != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpLatencyFtraceEventWire.CPU_WIRE) + gremlin.sizes.sizeU32(self.cpu);
        }
        if (self.ret != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpLatencyFtraceEventWire.RET_WIRE) + gremlin.sizes.sizeI32(self.ret);
        }
        if (self.state != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpLatencyFtraceEventWire.STATE_WIRE) + gremlin.sizes.sizeU32(self.state);
        }
        if (self.time != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpLatencyFtraceEventWire.TIME_WIRE) + gremlin.sizes.sizeU64(self.time);
        }
        return res;
    }
    pub fn encode(self: *const CpuhpLatencyFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CpuhpLatencyFtraceEvent, target: *gremlin.Writer) void {
        if (self.cpu != 0) {
            target.appendUint32(CpuhpLatencyFtraceEventWire.CPU_WIRE, self.cpu);
        }
        if (self.ret != 0) {
            target.appendInt32(CpuhpLatencyFtraceEventWire.RET_WIRE, self.ret);
        }
        if (self.state != 0) {
            target.appendUint32(CpuhpLatencyFtraceEventWire.STATE_WIRE, self.state);
        }
        if (self.time != 0) {
            target.appendUint64(CpuhpLatencyFtraceEventWire.TIME_WIRE, self.time);
        }
    }
};
pub const CpuhpLatencyFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cpu: u32 = 0,
    _ret: i32 = 0,
    _state: u32 = 0,
    _time: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!CpuhpLatencyFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CpuhpLatencyFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CpuhpLatencyFtraceEventWire.CPU_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cpu = result.value;
                },
                CpuhpLatencyFtraceEventWire.RET_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._ret = result.value;
                },
                CpuhpLatencyFtraceEventWire.STATE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._state = result.value;
                },
                CpuhpLatencyFtraceEventWire.TIME_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._time = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCpu(self: *const CpuhpLatencyFtraceEventReader) u32 {
        return self._cpu;
    }
    pub inline fn getRet(self: *const CpuhpLatencyFtraceEventReader) i32 {
        return self._ret;
    }
    pub inline fn getState(self: *const CpuhpLatencyFtraceEventReader) u32 {
        return self._state;
    }
    pub inline fn getTime(self: *const CpuhpLatencyFtraceEventReader) u64 {
        return self._time;
    }
};
const CpuhpPauseFtraceEventWire = struct {
    const ACTIVE_CPUS_WIRE: gremlin.ProtoWireNumber = 1;
    const CPUS_WIRE: gremlin.ProtoWireNumber = 2;
    const PAUSE_WIRE: gremlin.ProtoWireNumber = 3;
    const TIME_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const CpuhpPauseFtraceEvent = struct {
    // fields
    active_cpus: u32 = 0,
    cpus: u32 = 0,
    pause: u32 = 0,
    time: u32 = 0,
    pub fn calcProtobufSize(self: *const CpuhpPauseFtraceEvent) usize {
        var res: usize = 0;
        if (self.active_cpus != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpPauseFtraceEventWire.ACTIVE_CPUS_WIRE) + gremlin.sizes.sizeU32(self.active_cpus);
        }
        if (self.cpus != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpPauseFtraceEventWire.CPUS_WIRE) + gremlin.sizes.sizeU32(self.cpus);
        }
        if (self.pause != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpPauseFtraceEventWire.PAUSE_WIRE) + gremlin.sizes.sizeU32(self.pause);
        }
        if (self.time != 0) {
            res += gremlin.sizes.sizeWireNumber(CpuhpPauseFtraceEventWire.TIME_WIRE) + gremlin.sizes.sizeU32(self.time);
        }
        return res;
    }
    pub fn encode(self: *const CpuhpPauseFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CpuhpPauseFtraceEvent, target: *gremlin.Writer) void {
        if (self.active_cpus != 0) {
            target.appendUint32(CpuhpPauseFtraceEventWire.ACTIVE_CPUS_WIRE, self.active_cpus);
        }
        if (self.cpus != 0) {
            target.appendUint32(CpuhpPauseFtraceEventWire.CPUS_WIRE, self.cpus);
        }
        if (self.pause != 0) {
            target.appendUint32(CpuhpPauseFtraceEventWire.PAUSE_WIRE, self.pause);
        }
        if (self.time != 0) {
            target.appendUint32(CpuhpPauseFtraceEventWire.TIME_WIRE, self.time);
        }
    }
};
pub const CpuhpPauseFtraceEventReader = struct {
    buf: gremlin.Reader,
    _active_cpus: u32 = 0,
    _cpus: u32 = 0,
    _pause: u32 = 0,
    _time: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!CpuhpPauseFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = CpuhpPauseFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CpuhpPauseFtraceEventWire.ACTIVE_CPUS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._active_cpus = result.value;
                },
                CpuhpPauseFtraceEventWire.CPUS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cpus = result.value;
                },
                CpuhpPauseFtraceEventWire.PAUSE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._pause = result.value;
                },
                CpuhpPauseFtraceEventWire.TIME_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._time = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getActiveCpus(self: *const CpuhpPauseFtraceEventReader) u32 {
        return self._active_cpus;
    }
    pub inline fn getCpus(self: *const CpuhpPauseFtraceEventReader) u32 {
        return self._cpus;
    }
    pub inline fn getPause(self: *const CpuhpPauseFtraceEventReader) u32 {
        return self._pause;
    }
    pub inline fn getTime(self: *const CpuhpPauseFtraceEventReader) u32 {
        return self._time;
    }
};
