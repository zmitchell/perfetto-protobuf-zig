// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PixelMmKswapdWakeFtraceEventWire = struct {
    const WHATEVER_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const PixelMmKswapdWakeFtraceEvent = struct {
    // fields
    whatever: i32 = 0,
    pub fn calcProtobufSize(self: *const PixelMmKswapdWakeFtraceEvent) usize {
        var res: usize = 0;
        if (self.whatever != 0) {
            res += gremlin.sizes.sizeWireNumber(PixelMmKswapdWakeFtraceEventWire.WHATEVER_WIRE) + gremlin.sizes.sizeI32(self.whatever);
        }
        return res;
    }
    pub fn encode(self: *const PixelMmKswapdWakeFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PixelMmKswapdWakeFtraceEvent, target: *gremlin.Writer) void {
        if (self.whatever != 0) {
            target.appendInt32(PixelMmKswapdWakeFtraceEventWire.WHATEVER_WIRE, self.whatever);
        }
    }
};
pub const PixelMmKswapdWakeFtraceEventReader = struct {
    buf: gremlin.Reader,
    _whatever: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!PixelMmKswapdWakeFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = PixelMmKswapdWakeFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PixelMmKswapdWakeFtraceEventWire.WHATEVER_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._whatever = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getWhatever(self: *const PixelMmKswapdWakeFtraceEventReader) i32 {
        return self._whatever;
    }
};
const PixelMmKswapdDoneFtraceEventWire = struct {
    const DELTA_NR_SCANNED_WIRE: gremlin.ProtoWireNumber = 1;
    const DELTA_NR_RECLAIMED_WIRE: gremlin.ProtoWireNumber = 2;
    const DELTA_NR_ALLOCATED_WIRE: gremlin.ProtoWireNumber = 3;
    const DURATION_NS_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const PixelMmKswapdDoneFtraceEvent = struct {
    // fields
    delta_nr_scanned: u64 = 0,
    delta_nr_reclaimed: u64 = 0,
    delta_nr_allocated: u64 = 0,
    duration_ns: u64 = 0,
    pub fn calcProtobufSize(self: *const PixelMmKswapdDoneFtraceEvent) usize {
        var res: usize = 0;
        if (self.delta_nr_scanned != 0) {
            res += gremlin.sizes.sizeWireNumber(PixelMmKswapdDoneFtraceEventWire.DELTA_NR_SCANNED_WIRE) + gremlin.sizes.sizeU64(self.delta_nr_scanned);
        }
        if (self.delta_nr_reclaimed != 0) {
            res += gremlin.sizes.sizeWireNumber(PixelMmKswapdDoneFtraceEventWire.DELTA_NR_RECLAIMED_WIRE) + gremlin.sizes.sizeU64(self.delta_nr_reclaimed);
        }
        if (self.delta_nr_allocated != 0) {
            res += gremlin.sizes.sizeWireNumber(PixelMmKswapdDoneFtraceEventWire.DELTA_NR_ALLOCATED_WIRE) + gremlin.sizes.sizeU64(self.delta_nr_allocated);
        }
        if (self.duration_ns != 0) {
            res += gremlin.sizes.sizeWireNumber(PixelMmKswapdDoneFtraceEventWire.DURATION_NS_WIRE) + gremlin.sizes.sizeU64(self.duration_ns);
        }
        return res;
    }
    pub fn encode(self: *const PixelMmKswapdDoneFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PixelMmKswapdDoneFtraceEvent, target: *gremlin.Writer) void {
        if (self.delta_nr_scanned != 0) {
            target.appendUint64(PixelMmKswapdDoneFtraceEventWire.DELTA_NR_SCANNED_WIRE, self.delta_nr_scanned);
        }
        if (self.delta_nr_reclaimed != 0) {
            target.appendUint64(PixelMmKswapdDoneFtraceEventWire.DELTA_NR_RECLAIMED_WIRE, self.delta_nr_reclaimed);
        }
        if (self.delta_nr_allocated != 0) {
            target.appendUint64(PixelMmKswapdDoneFtraceEventWire.DELTA_NR_ALLOCATED_WIRE, self.delta_nr_allocated);
        }
        if (self.duration_ns != 0) {
            target.appendUint64(PixelMmKswapdDoneFtraceEventWire.DURATION_NS_WIRE, self.duration_ns);
        }
    }
};
pub const PixelMmKswapdDoneFtraceEventReader = struct {
    buf: gremlin.Reader,
    _delta_nr_scanned: u64 = 0,
    _delta_nr_reclaimed: u64 = 0,
    _delta_nr_allocated: u64 = 0,
    _duration_ns: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!PixelMmKswapdDoneFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = PixelMmKswapdDoneFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PixelMmKswapdDoneFtraceEventWire.DELTA_NR_SCANNED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._delta_nr_scanned = result.value;
                },
                PixelMmKswapdDoneFtraceEventWire.DELTA_NR_RECLAIMED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._delta_nr_reclaimed = result.value;
                },
                PixelMmKswapdDoneFtraceEventWire.DELTA_NR_ALLOCATED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._delta_nr_allocated = result.value;
                },
                PixelMmKswapdDoneFtraceEventWire.DURATION_NS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._duration_ns = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDeltaNrScanned(self: *const PixelMmKswapdDoneFtraceEventReader) u64 {
        return self._delta_nr_scanned;
    }
    pub inline fn getDeltaNrReclaimed(self: *const PixelMmKswapdDoneFtraceEventReader) u64 {
        return self._delta_nr_reclaimed;
    }
    pub inline fn getDeltaNrAllocated(self: *const PixelMmKswapdDoneFtraceEventReader) u64 {
        return self._delta_nr_allocated;
    }
    pub inline fn getDurationNs(self: *const PixelMmKswapdDoneFtraceEventReader) u64 {
        return self._duration_ns;
    }
};
