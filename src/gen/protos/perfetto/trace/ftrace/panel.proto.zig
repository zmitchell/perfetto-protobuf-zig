// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DsiCmdFifoStatusFtraceEventWire = struct {
    const HEADER_WIRE: gremlin.ProtoWireNumber = 1;
    const PAYLOAD_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const DsiCmdFifoStatusFtraceEvent = struct {
    // fields
    header: u32 = 0,
    payload: u32 = 0,
    pub fn calcProtobufSize(self: *const DsiCmdFifoStatusFtraceEvent) usize {
        var res: usize = 0;
        if (self.header != 0) {
            res += gremlin.sizes.sizeWireNumber(DsiCmdFifoStatusFtraceEventWire.HEADER_WIRE) + gremlin.sizes.sizeU32(self.header);
        }
        if (self.payload != 0) {
            res += gremlin.sizes.sizeWireNumber(DsiCmdFifoStatusFtraceEventWire.PAYLOAD_WIRE) + gremlin.sizes.sizeU32(self.payload);
        }
        return res;
    }
    pub fn encode(self: *const DsiCmdFifoStatusFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DsiCmdFifoStatusFtraceEvent, target: *gremlin.Writer) void {
        if (self.header != 0) {
            target.appendUint32(DsiCmdFifoStatusFtraceEventWire.HEADER_WIRE, self.header);
        }
        if (self.payload != 0) {
            target.appendUint32(DsiCmdFifoStatusFtraceEventWire.PAYLOAD_WIRE, self.payload);
        }
    }
};
pub const DsiCmdFifoStatusFtraceEventReader = struct {
    buf: gremlin.Reader,
    _header: u32 = 0,
    _payload: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DsiCmdFifoStatusFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DsiCmdFifoStatusFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DsiCmdFifoStatusFtraceEventWire.HEADER_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._header = result.value;
                },
                DsiCmdFifoStatusFtraceEventWire.PAYLOAD_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._payload = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getHeader(self: *const DsiCmdFifoStatusFtraceEventReader) u32 {
        return self._header;
    }
    pub inline fn getPayload(self: *const DsiCmdFifoStatusFtraceEventReader) u32 {
        return self._payload;
    }
};
const DsiRxFtraceEventWire = struct {
    const CMD_WIRE: gremlin.ProtoWireNumber = 1;
    const RX_BUF_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const DsiRxFtraceEvent = struct {
    // fields
    cmd: u32 = 0,
    rx_buf: u32 = 0,
    pub fn calcProtobufSize(self: *const DsiRxFtraceEvent) usize {
        var res: usize = 0;
        if (self.cmd != 0) {
            res += gremlin.sizes.sizeWireNumber(DsiRxFtraceEventWire.CMD_WIRE) + gremlin.sizes.sizeU32(self.cmd);
        }
        if (self.rx_buf != 0) {
            res += gremlin.sizes.sizeWireNumber(DsiRxFtraceEventWire.RX_BUF_WIRE) + gremlin.sizes.sizeU32(self.rx_buf);
        }
        return res;
    }
    pub fn encode(self: *const DsiRxFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DsiRxFtraceEvent, target: *gremlin.Writer) void {
        if (self.cmd != 0) {
            target.appendUint32(DsiRxFtraceEventWire.CMD_WIRE, self.cmd);
        }
        if (self.rx_buf != 0) {
            target.appendUint32(DsiRxFtraceEventWire.RX_BUF_WIRE, self.rx_buf);
        }
    }
};
pub const DsiRxFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cmd: u32 = 0,
    _rx_buf: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DsiRxFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DsiRxFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DsiRxFtraceEventWire.CMD_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cmd = result.value;
                },
                DsiRxFtraceEventWire.RX_BUF_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._rx_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCmd(self: *const DsiRxFtraceEventReader) u32 {
        return self._cmd;
    }
    pub inline fn getRxBuf(self: *const DsiRxFtraceEventReader) u32 {
        return self._rx_buf;
    }
};
const DsiTxFtraceEventWire = struct {
    const LAST_WIRE: gremlin.ProtoWireNumber = 1;
    const TX_BUF_WIRE: gremlin.ProtoWireNumber = 2;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const DsiTxFtraceEvent = struct {
    // fields
    last: u32 = 0,
    tx_buf: u32 = 0,
    type: u32 = 0,
    pub fn calcProtobufSize(self: *const DsiTxFtraceEvent) usize {
        var res: usize = 0;
        if (self.last != 0) {
            res += gremlin.sizes.sizeWireNumber(DsiTxFtraceEventWire.LAST_WIRE) + gremlin.sizes.sizeU32(self.last);
        }
        if (self.tx_buf != 0) {
            res += gremlin.sizes.sizeWireNumber(DsiTxFtraceEventWire.TX_BUF_WIRE) + gremlin.sizes.sizeU32(self.tx_buf);
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(DsiTxFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        return res;
    }
    pub fn encode(self: *const DsiTxFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DsiTxFtraceEvent, target: *gremlin.Writer) void {
        if (self.last != 0) {
            target.appendUint32(DsiTxFtraceEventWire.LAST_WIRE, self.last);
        }
        if (self.tx_buf != 0) {
            target.appendUint32(DsiTxFtraceEventWire.TX_BUF_WIRE, self.tx_buf);
        }
        if (self.type != 0) {
            target.appendUint32(DsiTxFtraceEventWire.TYPE_WIRE, self.type);
        }
    }
};
pub const DsiTxFtraceEventReader = struct {
    buf: gremlin.Reader,
    _last: u32 = 0,
    _tx_buf: u32 = 0,
    _type: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DsiTxFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DsiTxFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DsiTxFtraceEventWire.LAST_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._last = result.value;
                },
                DsiTxFtraceEventWire.TX_BUF_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._tx_buf = result.value;
                },
                DsiTxFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getLast(self: *const DsiTxFtraceEventReader) u32 {
        return self._last;
    }
    pub inline fn getTxBuf(self: *const DsiTxFtraceEventReader) u32 {
        return self._tx_buf;
    }
    pub inline fn getType(self: *const DsiTxFtraceEventReader) u32 {
        return self._type;
    }
};
const PanelWriteGenericFtraceEventWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
    const TRACE_NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const TRACE_BEGIN_WIRE: gremlin.ProtoWireNumber = 3;
    const NAME_WIRE: gremlin.ProtoWireNumber = 4;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 5;
    const VALUE_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const PanelWriteGenericFtraceEvent = struct {
    // fields
    pid: i32 = 0,
    trace_name: ?[]const u8 = null,
    trace_begin: u32 = 0,
    name: ?[]const u8 = null,
    type: u32 = 0,
    value: i32 = 0,
    pub fn calcProtobufSize(self: *const PanelWriteGenericFtraceEvent) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(PanelWriteGenericFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.trace_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(PanelWriteGenericFtraceEventWire.TRACE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.trace_begin != 0) {
            res += gremlin.sizes.sizeWireNumber(PanelWriteGenericFtraceEventWire.TRACE_BEGIN_WIRE) + gremlin.sizes.sizeU32(self.trace_begin);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(PanelWriteGenericFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(PanelWriteGenericFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        if (self.value != 0) {
            res += gremlin.sizes.sizeWireNumber(PanelWriteGenericFtraceEventWire.VALUE_WIRE) + gremlin.sizes.sizeI32(self.value);
        }
        return res;
    }
    pub fn encode(self: *const PanelWriteGenericFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PanelWriteGenericFtraceEvent, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendInt32(PanelWriteGenericFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.trace_name) |v| {
            if (v.len > 0) {
                target.appendBytes(PanelWriteGenericFtraceEventWire.TRACE_NAME_WIRE, v);
            }
        }
        if (self.trace_begin != 0) {
            target.appendUint32(PanelWriteGenericFtraceEventWire.TRACE_BEGIN_WIRE, self.trace_begin);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(PanelWriteGenericFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.type != 0) {
            target.appendUint32(PanelWriteGenericFtraceEventWire.TYPE_WIRE, self.type);
        }
        if (self.value != 0) {
            target.appendInt32(PanelWriteGenericFtraceEventWire.VALUE_WIRE, self.value);
        }
    }
};
pub const PanelWriteGenericFtraceEventReader = struct {
    buf: gremlin.Reader,
    _pid: i32 = 0,
    _trace_name: ?[]const u8 = null,
    _trace_begin: u32 = 0,
    _name: ?[]const u8 = null,
    _type: u32 = 0,
    _value: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!PanelWriteGenericFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = PanelWriteGenericFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PanelWriteGenericFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                PanelWriteGenericFtraceEventWire.TRACE_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._trace_name = result.value;
                },
                PanelWriteGenericFtraceEventWire.TRACE_BEGIN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._trace_begin = result.value;
                },
                PanelWriteGenericFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                PanelWriteGenericFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                PanelWriteGenericFtraceEventWire.VALUE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._value = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const PanelWriteGenericFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getTraceName(self: *const PanelWriteGenericFtraceEventReader) []const u8 {
        return self._trace_name orelse &[_]u8{};
    }
    pub inline fn getTraceBegin(self: *const PanelWriteGenericFtraceEventReader) u32 {
        return self._trace_begin;
    }
    pub inline fn getName(self: *const PanelWriteGenericFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getType(self: *const PanelWriteGenericFtraceEventReader) u32 {
        return self._type;
    }
    pub inline fn getValue(self: *const PanelWriteGenericFtraceEventReader) i32 {
        return self._value;
    }
};
