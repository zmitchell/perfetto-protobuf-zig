// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const VirtioGpuCmdQueueFtraceEventWire = struct {
    const CTX_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const DEV_WIRE: gremlin.ProtoWireNumber = 2;
    const FENCE_ID_WIRE: gremlin.ProtoWireNumber = 3;
    const FLAGS_WIRE: gremlin.ProtoWireNumber = 4;
    const NAME_WIRE: gremlin.ProtoWireNumber = 5;
    const NUM_FREE_WIRE: gremlin.ProtoWireNumber = 6;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 7;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 8;
    const VQ_WIRE: gremlin.ProtoWireNumber = 9;
};
pub const VirtioGpuCmdQueueFtraceEvent = struct {
    // fields
    ctx_id: u32 = 0,
    dev: i32 = 0,
    fence_id: u64 = 0,
    flags: u32 = 0,
    name: ?[]const u8 = null,
    num_free: u32 = 0,
    seqno: u32 = 0,
    type: u32 = 0,
    vq: u32 = 0,
    pub fn calcProtobufSize(self: *const VirtioGpuCmdQueueFtraceEvent) usize {
        var res: usize = 0;
        if (self.ctx_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.CTX_ID_WIRE) + gremlin.sizes.sizeU32(self.ctx_id);
        }
        if (self.dev != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.DEV_WIRE) + gremlin.sizes.sizeI32(self.dev);
        }
        if (self.fence_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.FENCE_ID_WIRE) + gremlin.sizes.sizeU64(self.fence_id);
        }
        if (self.flags != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.FLAGS_WIRE) + gremlin.sizes.sizeU32(self.flags);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.num_free != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.NUM_FREE_WIRE) + gremlin.sizes.sizeU32(self.num_free);
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        if (self.vq != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdQueueFtraceEventWire.VQ_WIRE) + gremlin.sizes.sizeU32(self.vq);
        }
        return res;
    }
    pub fn encode(self: *const VirtioGpuCmdQueueFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const VirtioGpuCmdQueueFtraceEvent, target: *gremlin.Writer) void {
        if (self.ctx_id != 0) {
            target.appendUint32(VirtioGpuCmdQueueFtraceEventWire.CTX_ID_WIRE, self.ctx_id);
        }
        if (self.dev != 0) {
            target.appendInt32(VirtioGpuCmdQueueFtraceEventWire.DEV_WIRE, self.dev);
        }
        if (self.fence_id != 0) {
            target.appendUint64(VirtioGpuCmdQueueFtraceEventWire.FENCE_ID_WIRE, self.fence_id);
        }
        if (self.flags != 0) {
            target.appendUint32(VirtioGpuCmdQueueFtraceEventWire.FLAGS_WIRE, self.flags);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(VirtioGpuCmdQueueFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.num_free != 0) {
            target.appendUint32(VirtioGpuCmdQueueFtraceEventWire.NUM_FREE_WIRE, self.num_free);
        }
        if (self.seqno != 0) {
            target.appendUint32(VirtioGpuCmdQueueFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.type != 0) {
            target.appendUint32(VirtioGpuCmdQueueFtraceEventWire.TYPE_WIRE, self.type);
        }
        if (self.vq != 0) {
            target.appendUint32(VirtioGpuCmdQueueFtraceEventWire.VQ_WIRE, self.vq);
        }
    }
};
pub const VirtioGpuCmdQueueFtraceEventReader = struct {
    buf: gremlin.Reader,
    _ctx_id: u32 = 0,
    _dev: i32 = 0,
    _fence_id: u64 = 0,
    _flags: u32 = 0,
    _name: ?[]const u8 = null,
    _num_free: u32 = 0,
    _seqno: u32 = 0,
    _type: u32 = 0,
    _vq: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!VirtioGpuCmdQueueFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = VirtioGpuCmdQueueFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                VirtioGpuCmdQueueFtraceEventWire.CTX_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._ctx_id = result.value;
                },
                VirtioGpuCmdQueueFtraceEventWire.DEV_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dev = result.value;
                },
                VirtioGpuCmdQueueFtraceEventWire.FENCE_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_id = result.value;
                },
                VirtioGpuCmdQueueFtraceEventWire.FLAGS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._flags = result.value;
                },
                VirtioGpuCmdQueueFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                VirtioGpuCmdQueueFtraceEventWire.NUM_FREE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._num_free = result.value;
                },
                VirtioGpuCmdQueueFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                VirtioGpuCmdQueueFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                VirtioGpuCmdQueueFtraceEventWire.VQ_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._vq = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCtxId(self: *const VirtioGpuCmdQueueFtraceEventReader) u32 {
        return self._ctx_id;
    }
    pub inline fn getDev(self: *const VirtioGpuCmdQueueFtraceEventReader) i32 {
        return self._dev;
    }
    pub inline fn getFenceId(self: *const VirtioGpuCmdQueueFtraceEventReader) u64 {
        return self._fence_id;
    }
    pub inline fn getFlags(self: *const VirtioGpuCmdQueueFtraceEventReader) u32 {
        return self._flags;
    }
    pub inline fn getName(self: *const VirtioGpuCmdQueueFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getNumFree(self: *const VirtioGpuCmdQueueFtraceEventReader) u32 {
        return self._num_free;
    }
    pub inline fn getSeqno(self: *const VirtioGpuCmdQueueFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getType(self: *const VirtioGpuCmdQueueFtraceEventReader) u32 {
        return self._type;
    }
    pub inline fn getVq(self: *const VirtioGpuCmdQueueFtraceEventReader) u32 {
        return self._vq;
    }
};
const VirtioGpuCmdResponseFtraceEventWire = struct {
    const CTX_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const DEV_WIRE: gremlin.ProtoWireNumber = 2;
    const FENCE_ID_WIRE: gremlin.ProtoWireNumber = 3;
    const FLAGS_WIRE: gremlin.ProtoWireNumber = 4;
    const NAME_WIRE: gremlin.ProtoWireNumber = 5;
    const NUM_FREE_WIRE: gremlin.ProtoWireNumber = 6;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 7;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 8;
    const VQ_WIRE: gremlin.ProtoWireNumber = 9;
};
pub const VirtioGpuCmdResponseFtraceEvent = struct {
    // fields
    ctx_id: u32 = 0,
    dev: i32 = 0,
    fence_id: u64 = 0,
    flags: u32 = 0,
    name: ?[]const u8 = null,
    num_free: u32 = 0,
    seqno: u32 = 0,
    type: u32 = 0,
    vq: u32 = 0,
    pub fn calcProtobufSize(self: *const VirtioGpuCmdResponseFtraceEvent) usize {
        var res: usize = 0;
        if (self.ctx_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.CTX_ID_WIRE) + gremlin.sizes.sizeU32(self.ctx_id);
        }
        if (self.dev != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.DEV_WIRE) + gremlin.sizes.sizeI32(self.dev);
        }
        if (self.fence_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.FENCE_ID_WIRE) + gremlin.sizes.sizeU64(self.fence_id);
        }
        if (self.flags != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.FLAGS_WIRE) + gremlin.sizes.sizeU32(self.flags);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.num_free != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.NUM_FREE_WIRE) + gremlin.sizes.sizeU32(self.num_free);
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        if (self.vq != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioGpuCmdResponseFtraceEventWire.VQ_WIRE) + gremlin.sizes.sizeU32(self.vq);
        }
        return res;
    }
    pub fn encode(self: *const VirtioGpuCmdResponseFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const VirtioGpuCmdResponseFtraceEvent, target: *gremlin.Writer) void {
        if (self.ctx_id != 0) {
            target.appendUint32(VirtioGpuCmdResponseFtraceEventWire.CTX_ID_WIRE, self.ctx_id);
        }
        if (self.dev != 0) {
            target.appendInt32(VirtioGpuCmdResponseFtraceEventWire.DEV_WIRE, self.dev);
        }
        if (self.fence_id != 0) {
            target.appendUint64(VirtioGpuCmdResponseFtraceEventWire.FENCE_ID_WIRE, self.fence_id);
        }
        if (self.flags != 0) {
            target.appendUint32(VirtioGpuCmdResponseFtraceEventWire.FLAGS_WIRE, self.flags);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(VirtioGpuCmdResponseFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.num_free != 0) {
            target.appendUint32(VirtioGpuCmdResponseFtraceEventWire.NUM_FREE_WIRE, self.num_free);
        }
        if (self.seqno != 0) {
            target.appendUint32(VirtioGpuCmdResponseFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.type != 0) {
            target.appendUint32(VirtioGpuCmdResponseFtraceEventWire.TYPE_WIRE, self.type);
        }
        if (self.vq != 0) {
            target.appendUint32(VirtioGpuCmdResponseFtraceEventWire.VQ_WIRE, self.vq);
        }
    }
};
pub const VirtioGpuCmdResponseFtraceEventReader = struct {
    buf: gremlin.Reader,
    _ctx_id: u32 = 0,
    _dev: i32 = 0,
    _fence_id: u64 = 0,
    _flags: u32 = 0,
    _name: ?[]const u8 = null,
    _num_free: u32 = 0,
    _seqno: u32 = 0,
    _type: u32 = 0,
    _vq: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!VirtioGpuCmdResponseFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = VirtioGpuCmdResponseFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                VirtioGpuCmdResponseFtraceEventWire.CTX_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._ctx_id = result.value;
                },
                VirtioGpuCmdResponseFtraceEventWire.DEV_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._dev = result.value;
                },
                VirtioGpuCmdResponseFtraceEventWire.FENCE_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._fence_id = result.value;
                },
                VirtioGpuCmdResponseFtraceEventWire.FLAGS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._flags = result.value;
                },
                VirtioGpuCmdResponseFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                VirtioGpuCmdResponseFtraceEventWire.NUM_FREE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._num_free = result.value;
                },
                VirtioGpuCmdResponseFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                VirtioGpuCmdResponseFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                VirtioGpuCmdResponseFtraceEventWire.VQ_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._vq = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCtxId(self: *const VirtioGpuCmdResponseFtraceEventReader) u32 {
        return self._ctx_id;
    }
    pub inline fn getDev(self: *const VirtioGpuCmdResponseFtraceEventReader) i32 {
        return self._dev;
    }
    pub inline fn getFenceId(self: *const VirtioGpuCmdResponseFtraceEventReader) u64 {
        return self._fence_id;
    }
    pub inline fn getFlags(self: *const VirtioGpuCmdResponseFtraceEventReader) u32 {
        return self._flags;
    }
    pub inline fn getName(self: *const VirtioGpuCmdResponseFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getNumFree(self: *const VirtioGpuCmdResponseFtraceEventReader) u32 {
        return self._num_free;
    }
    pub inline fn getSeqno(self: *const VirtioGpuCmdResponseFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getType(self: *const VirtioGpuCmdResponseFtraceEventReader) u32 {
        return self._type;
    }
    pub inline fn getVq(self: *const VirtioGpuCmdResponseFtraceEventReader) u32 {
        return self._vq;
    }
};
