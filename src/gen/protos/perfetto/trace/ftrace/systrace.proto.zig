// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ZeroFtraceEventWire = struct {
    const FLAG_WIRE: gremlin.ProtoWireNumber = 1;
    const NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const PID_WIRE: gremlin.ProtoWireNumber = 3;
    const VALUE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const ZeroFtraceEvent = struct {
    // fields
    flag: i32 = 0,
    name: ?[]const u8 = null,
    pid: i32 = 0,
    value: i64 = 0,
    pub fn calcProtobufSize(self: *const ZeroFtraceEvent) usize {
        var res: usize = 0;
        if (self.flag != 0) {
            res += gremlin.sizes.sizeWireNumber(ZeroFtraceEventWire.FLAG_WIRE) + gremlin.sizes.sizeI32(self.flag);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ZeroFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(ZeroFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.value != 0) {
            res += gremlin.sizes.sizeWireNumber(ZeroFtraceEventWire.VALUE_WIRE) + gremlin.sizes.sizeI64(self.value);
        }
        return res;
    }
    pub fn encode(self: *const ZeroFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ZeroFtraceEvent, target: *gremlin.Writer) void {
        if (self.flag != 0) {
            target.appendInt32(ZeroFtraceEventWire.FLAG_WIRE, self.flag);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(ZeroFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.pid != 0) {
            target.appendInt32(ZeroFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.value != 0) {
            target.appendInt64(ZeroFtraceEventWire.VALUE_WIRE, self.value);
        }
    }
};
pub const ZeroFtraceEventReader = struct {
    buf: gremlin.Reader,
    _flag: i32 = 0,
    _name: ?[]const u8 = null,
    _pid: i32 = 0,
    _value: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ZeroFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ZeroFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ZeroFtraceEventWire.FLAG_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._flag = result.value;
                },
                ZeroFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                ZeroFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                ZeroFtraceEventWire.VALUE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._value = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFlag(self: *const ZeroFtraceEventReader) i32 {
        return self._flag;
    }
    pub inline fn getName(self: *const ZeroFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getPid(self: *const ZeroFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getValue(self: *const ZeroFtraceEventReader) i64 {
        return self._value;
    }
};
