// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DmaFenceInitFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DmaFenceInitFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const DmaFenceInitFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceInitFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceInitFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceInitFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceInitFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const DmaFenceInitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DmaFenceInitFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(DmaFenceInitFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceInitFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(DmaFenceInitFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceInitFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const DmaFenceInitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!DmaFenceInitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DmaFenceInitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DmaFenceInitFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                DmaFenceInitFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                DmaFenceInitFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                DmaFenceInitFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const DmaFenceInitFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const DmaFenceInitFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const DmaFenceInitFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const DmaFenceInitFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
const DmaFenceEmitFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DmaFenceEmitFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const DmaFenceEmitFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceEmitFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceEmitFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceEmitFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceEmitFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const DmaFenceEmitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DmaFenceEmitFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(DmaFenceEmitFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceEmitFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(DmaFenceEmitFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceEmitFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const DmaFenceEmitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!DmaFenceEmitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DmaFenceEmitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DmaFenceEmitFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                DmaFenceEmitFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                DmaFenceEmitFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                DmaFenceEmitFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const DmaFenceEmitFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const DmaFenceEmitFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const DmaFenceEmitFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const DmaFenceEmitFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
const DmaFenceSignaledFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DmaFenceSignaledFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const DmaFenceSignaledFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceSignaledFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceSignaledFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceSignaledFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceSignaledFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const DmaFenceSignaledFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DmaFenceSignaledFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(DmaFenceSignaledFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceSignaledFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(DmaFenceSignaledFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceSignaledFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const DmaFenceSignaledFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!DmaFenceSignaledFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DmaFenceSignaledFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DmaFenceSignaledFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                DmaFenceSignaledFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                DmaFenceSignaledFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                DmaFenceSignaledFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const DmaFenceSignaledFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const DmaFenceSignaledFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const DmaFenceSignaledFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const DmaFenceSignaledFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
const DmaFenceWaitStartFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DmaFenceWaitStartFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const DmaFenceWaitStartFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceWaitStartFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceWaitStartFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceWaitStartFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceWaitStartFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const DmaFenceWaitStartFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DmaFenceWaitStartFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(DmaFenceWaitStartFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceWaitStartFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(DmaFenceWaitStartFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceWaitStartFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const DmaFenceWaitStartFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!DmaFenceWaitStartFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DmaFenceWaitStartFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DmaFenceWaitStartFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                DmaFenceWaitStartFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                DmaFenceWaitStartFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                DmaFenceWaitStartFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const DmaFenceWaitStartFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const DmaFenceWaitStartFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const DmaFenceWaitStartFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const DmaFenceWaitStartFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
const DmaFenceWaitEndFtraceEventWire = struct {
    const CONTEXT_WIRE: gremlin.ProtoWireNumber = 1;
    const DRIVER_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQNO_WIRE: gremlin.ProtoWireNumber = 3;
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DmaFenceWaitEndFtraceEvent = struct {
    // fields
    context: u32 = 0,
    driver: ?[]const u8 = null,
    seqno: u32 = 0,
    timeline: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const DmaFenceWaitEndFtraceEvent) usize {
        var res: usize = 0;
        if (self.context != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceWaitEndFtraceEventWire.CONTEXT_WIRE) + gremlin.sizes.sizeU32(self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceWaitEndFtraceEventWire.DRIVER_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.seqno != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaFenceWaitEndFtraceEventWire.SEQNO_WIRE) + gremlin.sizes.sizeU32(self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DmaFenceWaitEndFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const DmaFenceWaitEndFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DmaFenceWaitEndFtraceEvent, target: *gremlin.Writer) void {
        if (self.context != 0) {
            target.appendUint32(DmaFenceWaitEndFtraceEventWire.CONTEXT_WIRE, self.context);
        }
        if (self.driver) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceWaitEndFtraceEventWire.DRIVER_WIRE, v);
            }
        }
        if (self.seqno != 0) {
            target.appendUint32(DmaFenceWaitEndFtraceEventWire.SEQNO_WIRE, self.seqno);
        }
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(DmaFenceWaitEndFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
    }
};
pub const DmaFenceWaitEndFtraceEventReader = struct {
    buf: gremlin.Reader,
    _context: u32 = 0,
    _driver: ?[]const u8 = null,
    _seqno: u32 = 0,
    _timeline: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!DmaFenceWaitEndFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DmaFenceWaitEndFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DmaFenceWaitEndFtraceEventWire.CONTEXT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._context = result.value;
                },
                DmaFenceWaitEndFtraceEventWire.DRIVER_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._driver = result.value;
                },
                DmaFenceWaitEndFtraceEventWire.SEQNO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seqno = result.value;
                },
                DmaFenceWaitEndFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getContext(self: *const DmaFenceWaitEndFtraceEventReader) u32 {
        return self._context;
    }
    pub inline fn getDriver(self: *const DmaFenceWaitEndFtraceEventReader) []const u8 {
        return self._driver orelse &[_]u8{};
    }
    pub inline fn getSeqno(self: *const DmaFenceWaitEndFtraceEventReader) u32 {
        return self._seqno;
    }
    pub inline fn getTimeline(self: *const DmaFenceWaitEndFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
};
