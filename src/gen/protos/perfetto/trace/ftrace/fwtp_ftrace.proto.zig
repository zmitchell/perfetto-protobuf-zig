// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const FwtpPerfettoCounterFtraceEventWire = struct {
    const TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 1;
    const TRACK_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const CATEGORY_WIRE: gremlin.ProtoWireNumber = 3;
    const NAME_WIRE: gremlin.ProtoWireNumber = 4;
    const VALUE_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const FwtpPerfettoCounterFtraceEvent = struct {
    // fields
    timestamp: u64 = 0,
    track_id: u32 = 0,
    category: ?[]const u8 = null,
    name: ?[]const u8 = null,
    value: u32 = 0,
    pub fn calcProtobufSize(self: *const FwtpPerfettoCounterFtraceEvent) usize {
        var res: usize = 0;
        if (self.timestamp != 0) {
            res += gremlin.sizes.sizeWireNumber(FwtpPerfettoCounterFtraceEventWire.TIMESTAMP_WIRE) + gremlin.sizes.sizeU64(self.timestamp);
        }
        if (self.track_id != 0) {
            res += gremlin.sizes.sizeWireNumber(FwtpPerfettoCounterFtraceEventWire.TRACK_ID_WIRE) + gremlin.sizes.sizeU32(self.track_id);
        }
        if (self.category) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FwtpPerfettoCounterFtraceEventWire.CATEGORY_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FwtpPerfettoCounterFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.value != 0) {
            res += gremlin.sizes.sizeWireNumber(FwtpPerfettoCounterFtraceEventWire.VALUE_WIRE) + gremlin.sizes.sizeU32(self.value);
        }
        return res;
    }
    pub fn encode(self: *const FwtpPerfettoCounterFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FwtpPerfettoCounterFtraceEvent, target: *gremlin.Writer) void {
        if (self.timestamp != 0) {
            target.appendUint64(FwtpPerfettoCounterFtraceEventWire.TIMESTAMP_WIRE, self.timestamp);
        }
        if (self.track_id != 0) {
            target.appendUint32(FwtpPerfettoCounterFtraceEventWire.TRACK_ID_WIRE, self.track_id);
        }
        if (self.category) |v| {
            if (v.len > 0) {
                target.appendBytes(FwtpPerfettoCounterFtraceEventWire.CATEGORY_WIRE, v);
            }
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(FwtpPerfettoCounterFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.value != 0) {
            target.appendUint32(FwtpPerfettoCounterFtraceEventWire.VALUE_WIRE, self.value);
        }
    }
};
pub const FwtpPerfettoCounterFtraceEventReader = struct {
    buf: gremlin.Reader,
    _timestamp: u64 = 0,
    _track_id: u32 = 0,
    _category: ?[]const u8 = null,
    _name: ?[]const u8 = null,
    _value: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!FwtpPerfettoCounterFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FwtpPerfettoCounterFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FwtpPerfettoCounterFtraceEventWire.TIMESTAMP_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._timestamp = result.value;
                },
                FwtpPerfettoCounterFtraceEventWire.TRACK_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._track_id = result.value;
                },
                FwtpPerfettoCounterFtraceEventWire.CATEGORY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._category = result.value;
                },
                FwtpPerfettoCounterFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                FwtpPerfettoCounterFtraceEventWire.VALUE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._value = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTimestamp(self: *const FwtpPerfettoCounterFtraceEventReader) u64 {
        return self._timestamp;
    }
    pub inline fn getTrackId(self: *const FwtpPerfettoCounterFtraceEventReader) u32 {
        return self._track_id;
    }
    pub inline fn getCategory(self: *const FwtpPerfettoCounterFtraceEventReader) []const u8 {
        return self._category orelse &[_]u8{};
    }
    pub inline fn getName(self: *const FwtpPerfettoCounterFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getValue(self: *const FwtpPerfettoCounterFtraceEventReader) u32 {
        return self._value;
    }
};
