// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const SyncPtFtraceEventWire = struct {
    const TIMELINE_WIRE: gremlin.ProtoWireNumber = 1;
    const VALUE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const SyncPtFtraceEvent = struct {
    // fields
    timeline: ?[]const u8 = null,
    value: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const SyncPtFtraceEvent) usize {
        var res: usize = 0;
        if (self.timeline) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SyncPtFtraceEventWire.TIMELINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.value) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SyncPtFtraceEventWire.VALUE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const SyncPtFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SyncPtFtraceEvent, target: *gremlin.Writer) void {
        if (self.timeline) |v| {
            if (v.len > 0) {
                target.appendBytes(SyncPtFtraceEventWire.TIMELINE_WIRE, v);
            }
        }
        if (self.value) |v| {
            if (v.len > 0) {
                target.appendBytes(SyncPtFtraceEventWire.VALUE_WIRE, v);
            }
        }
    }
};
pub const SyncPtFtraceEventReader = struct {
    buf: gremlin.Reader,
    _timeline: ?[]const u8 = null,
    _value: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!SyncPtFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = SyncPtFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SyncPtFtraceEventWire.TIMELINE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._timeline = result.value;
                },
                SyncPtFtraceEventWire.VALUE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._value = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTimeline(self: *const SyncPtFtraceEventReader) []const u8 {
        return self._timeline orelse &[_]u8{};
    }
    pub inline fn getValue(self: *const SyncPtFtraceEventReader) []const u8 {
        return self._value orelse &[_]u8{};
    }
};
const SyncTimelineFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const VALUE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const SyncTimelineFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    value: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const SyncTimelineFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SyncTimelineFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.value) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SyncTimelineFtraceEventWire.VALUE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const SyncTimelineFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SyncTimelineFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(SyncTimelineFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.value) |v| {
            if (v.len > 0) {
                target.appendBytes(SyncTimelineFtraceEventWire.VALUE_WIRE, v);
            }
        }
    }
};
pub const SyncTimelineFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _value: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!SyncTimelineFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = SyncTimelineFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SyncTimelineFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                SyncTimelineFtraceEventWire.VALUE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._value = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const SyncTimelineFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getValue(self: *const SyncTimelineFtraceEventReader) []const u8 {
        return self._value orelse &[_]u8{};
    }
};
const SyncWaitFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const STATUS_WIRE: gremlin.ProtoWireNumber = 2;
    const BEGIN_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const SyncWaitFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    status: i32 = 0,
    begin: u32 = 0,
    pub fn calcProtobufSize(self: *const SyncWaitFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SyncWaitFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.status != 0) {
            res += gremlin.sizes.sizeWireNumber(SyncWaitFtraceEventWire.STATUS_WIRE) + gremlin.sizes.sizeI32(self.status);
        }
        if (self.begin != 0) {
            res += gremlin.sizes.sizeWireNumber(SyncWaitFtraceEventWire.BEGIN_WIRE) + gremlin.sizes.sizeU32(self.begin);
        }
        return res;
    }
    pub fn encode(self: *const SyncWaitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SyncWaitFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(SyncWaitFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.status != 0) {
            target.appendInt32(SyncWaitFtraceEventWire.STATUS_WIRE, self.status);
        }
        if (self.begin != 0) {
            target.appendUint32(SyncWaitFtraceEventWire.BEGIN_WIRE, self.begin);
        }
    }
};
pub const SyncWaitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _status: i32 = 0,
    _begin: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!SyncWaitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = SyncWaitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SyncWaitFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                SyncWaitFtraceEventWire.STATUS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._status = result.value;
                },
                SyncWaitFtraceEventWire.BEGIN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._begin = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const SyncWaitFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getStatus(self: *const SyncWaitFtraceEventReader) i32 {
        return self._status;
    }
    pub inline fn getBegin(self: *const SyncWaitFtraceEventReader) u32 {
        return self._begin;
    }
};
