// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const LocalTimerEntryFtraceEventWire = struct {
    const VECTOR_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const LocalTimerEntryFtraceEvent = struct {
    // fields
    vector: i32 = 0,
    pub fn calcProtobufSize(self: *const LocalTimerEntryFtraceEvent) usize {
        var res: usize = 0;
        if (self.vector != 0) {
            res += gremlin.sizes.sizeWireNumber(LocalTimerEntryFtraceEventWire.VECTOR_WIRE) + gremlin.sizes.sizeI32(self.vector);
        }
        return res;
    }
    pub fn encode(self: *const LocalTimerEntryFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const LocalTimerEntryFtraceEvent, target: *gremlin.Writer) void {
        if (self.vector != 0) {
            target.appendInt32(LocalTimerEntryFtraceEventWire.VECTOR_WIRE, self.vector);
        }
    }
};
pub const LocalTimerEntryFtraceEventReader = struct {
    buf: gremlin.Reader,
    _vector: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!LocalTimerEntryFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = LocalTimerEntryFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                LocalTimerEntryFtraceEventWire.VECTOR_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._vector = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getVector(self: *const LocalTimerEntryFtraceEventReader) i32 {
        return self._vector;
    }
};
const LocalTimerExitFtraceEventWire = struct {
    const VECTOR_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const LocalTimerExitFtraceEvent = struct {
    // fields
    vector: i32 = 0,
    pub fn calcProtobufSize(self: *const LocalTimerExitFtraceEvent) usize {
        var res: usize = 0;
        if (self.vector != 0) {
            res += gremlin.sizes.sizeWireNumber(LocalTimerExitFtraceEventWire.VECTOR_WIRE) + gremlin.sizes.sizeI32(self.vector);
        }
        return res;
    }
    pub fn encode(self: *const LocalTimerExitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const LocalTimerExitFtraceEvent, target: *gremlin.Writer) void {
        if (self.vector != 0) {
            target.appendInt32(LocalTimerExitFtraceEventWire.VECTOR_WIRE, self.vector);
        }
    }
};
pub const LocalTimerExitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _vector: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!LocalTimerExitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = LocalTimerExitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                LocalTimerExitFtraceEventWire.VECTOR_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._vector = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getVector(self: *const LocalTimerExitFtraceEventReader) i32 {
        return self._vector;
    }
};
