// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const VirtioVideoCmdFtraceEventWire = struct {
    const STREAM_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const VirtioVideoCmdFtraceEvent = struct {
    // fields
    stream_id: u32 = 0,
    type: u32 = 0,
    pub fn calcProtobufSize(self: *const VirtioVideoCmdFtraceEvent) usize {
        var res: usize = 0;
        if (self.stream_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoCmdFtraceEventWire.STREAM_ID_WIRE) + gremlin.sizes.sizeU32(self.stream_id);
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoCmdFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        return res;
    }
    pub fn encode(self: *const VirtioVideoCmdFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const VirtioVideoCmdFtraceEvent, target: *gremlin.Writer) void {
        if (self.stream_id != 0) {
            target.appendUint32(VirtioVideoCmdFtraceEventWire.STREAM_ID_WIRE, self.stream_id);
        }
        if (self.type != 0) {
            target.appendUint32(VirtioVideoCmdFtraceEventWire.TYPE_WIRE, self.type);
        }
    }
};
pub const VirtioVideoCmdFtraceEventReader = struct {
    buf: gremlin.Reader,
    _stream_id: u32 = 0,
    _type: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!VirtioVideoCmdFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = VirtioVideoCmdFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                VirtioVideoCmdFtraceEventWire.STREAM_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._stream_id = result.value;
                },
                VirtioVideoCmdFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getStreamId(self: *const VirtioVideoCmdFtraceEventReader) u32 {
        return self._stream_id;
    }
    pub inline fn getType(self: *const VirtioVideoCmdFtraceEventReader) u32 {
        return self._type;
    }
};
const VirtioVideoCmdDoneFtraceEventWire = struct {
    const STREAM_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const VirtioVideoCmdDoneFtraceEvent = struct {
    // fields
    stream_id: u32 = 0,
    type: u32 = 0,
    pub fn calcProtobufSize(self: *const VirtioVideoCmdDoneFtraceEvent) usize {
        var res: usize = 0;
        if (self.stream_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoCmdDoneFtraceEventWire.STREAM_ID_WIRE) + gremlin.sizes.sizeU32(self.stream_id);
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoCmdDoneFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        return res;
    }
    pub fn encode(self: *const VirtioVideoCmdDoneFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const VirtioVideoCmdDoneFtraceEvent, target: *gremlin.Writer) void {
        if (self.stream_id != 0) {
            target.appendUint32(VirtioVideoCmdDoneFtraceEventWire.STREAM_ID_WIRE, self.stream_id);
        }
        if (self.type != 0) {
            target.appendUint32(VirtioVideoCmdDoneFtraceEventWire.TYPE_WIRE, self.type);
        }
    }
};
pub const VirtioVideoCmdDoneFtraceEventReader = struct {
    buf: gremlin.Reader,
    _stream_id: u32 = 0,
    _type: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!VirtioVideoCmdDoneFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = VirtioVideoCmdDoneFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                VirtioVideoCmdDoneFtraceEventWire.STREAM_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._stream_id = result.value;
                },
                VirtioVideoCmdDoneFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getStreamId(self: *const VirtioVideoCmdDoneFtraceEventReader) u32 {
        return self._stream_id;
    }
    pub inline fn getType(self: *const VirtioVideoCmdDoneFtraceEventReader) u32 {
        return self._type;
    }
};
const VirtioVideoResourceQueueFtraceEventWire = struct {
    const DATA_SIZE0_WIRE: gremlin.ProtoWireNumber = 1;
    const DATA_SIZE1_WIRE: gremlin.ProtoWireNumber = 2;
    const DATA_SIZE2_WIRE: gremlin.ProtoWireNumber = 3;
    const DATA_SIZE3_WIRE: gremlin.ProtoWireNumber = 4;
    const QUEUE_TYPE_WIRE: gremlin.ProtoWireNumber = 5;
    const RESOURCE_ID_WIRE: gremlin.ProtoWireNumber = 6;
    const STREAM_ID_WIRE: gremlin.ProtoWireNumber = 7;
    const TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const VirtioVideoResourceQueueFtraceEvent = struct {
    // fields
    data_size0: u32 = 0,
    data_size1: u32 = 0,
    data_size2: u32 = 0,
    data_size3: u32 = 0,
    queue_type: u32 = 0,
    resource_id: i32 = 0,
    stream_id: i32 = 0,
    timestamp: u64 = 0,
    pub fn calcProtobufSize(self: *const VirtioVideoResourceQueueFtraceEvent) usize {
        var res: usize = 0;
        if (self.data_size0 != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE0_WIRE) + gremlin.sizes.sizeU32(self.data_size0);
        }
        if (self.data_size1 != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE1_WIRE) + gremlin.sizes.sizeU32(self.data_size1);
        }
        if (self.data_size2 != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE2_WIRE) + gremlin.sizes.sizeU32(self.data_size2);
        }
        if (self.data_size3 != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE3_WIRE) + gremlin.sizes.sizeU32(self.data_size3);
        }
        if (self.queue_type != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueFtraceEventWire.QUEUE_TYPE_WIRE) + gremlin.sizes.sizeU32(self.queue_type);
        }
        if (self.resource_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueFtraceEventWire.RESOURCE_ID_WIRE) + gremlin.sizes.sizeI32(self.resource_id);
        }
        if (self.stream_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueFtraceEventWire.STREAM_ID_WIRE) + gremlin.sizes.sizeI32(self.stream_id);
        }
        if (self.timestamp != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueFtraceEventWire.TIMESTAMP_WIRE) + gremlin.sizes.sizeU64(self.timestamp);
        }
        return res;
    }
    pub fn encode(self: *const VirtioVideoResourceQueueFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const VirtioVideoResourceQueueFtraceEvent, target: *gremlin.Writer) void {
        if (self.data_size0 != 0) {
            target.appendUint32(VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE0_WIRE, self.data_size0);
        }
        if (self.data_size1 != 0) {
            target.appendUint32(VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE1_WIRE, self.data_size1);
        }
        if (self.data_size2 != 0) {
            target.appendUint32(VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE2_WIRE, self.data_size2);
        }
        if (self.data_size3 != 0) {
            target.appendUint32(VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE3_WIRE, self.data_size3);
        }
        if (self.queue_type != 0) {
            target.appendUint32(VirtioVideoResourceQueueFtraceEventWire.QUEUE_TYPE_WIRE, self.queue_type);
        }
        if (self.resource_id != 0) {
            target.appendInt32(VirtioVideoResourceQueueFtraceEventWire.RESOURCE_ID_WIRE, self.resource_id);
        }
        if (self.stream_id != 0) {
            target.appendInt32(VirtioVideoResourceQueueFtraceEventWire.STREAM_ID_WIRE, self.stream_id);
        }
        if (self.timestamp != 0) {
            target.appendUint64(VirtioVideoResourceQueueFtraceEventWire.TIMESTAMP_WIRE, self.timestamp);
        }
    }
};
pub const VirtioVideoResourceQueueFtraceEventReader = struct {
    buf: gremlin.Reader,
    _data_size0: u32 = 0,
    _data_size1: u32 = 0,
    _data_size2: u32 = 0,
    _data_size3: u32 = 0,
    _queue_type: u32 = 0,
    _resource_id: i32 = 0,
    _stream_id: i32 = 0,
    _timestamp: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!VirtioVideoResourceQueueFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = VirtioVideoResourceQueueFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE0_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._data_size0 = result.value;
                },
                VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE1_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._data_size1 = result.value;
                },
                VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE2_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._data_size2 = result.value;
                },
                VirtioVideoResourceQueueFtraceEventWire.DATA_SIZE3_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._data_size3 = result.value;
                },
                VirtioVideoResourceQueueFtraceEventWire.QUEUE_TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._queue_type = result.value;
                },
                VirtioVideoResourceQueueFtraceEventWire.RESOURCE_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._resource_id = result.value;
                },
                VirtioVideoResourceQueueFtraceEventWire.STREAM_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._stream_id = result.value;
                },
                VirtioVideoResourceQueueFtraceEventWire.TIMESTAMP_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._timestamp = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDataSize0(self: *const VirtioVideoResourceQueueFtraceEventReader) u32 {
        return self._data_size0;
    }
    pub inline fn getDataSize1(self: *const VirtioVideoResourceQueueFtraceEventReader) u32 {
        return self._data_size1;
    }
    pub inline fn getDataSize2(self: *const VirtioVideoResourceQueueFtraceEventReader) u32 {
        return self._data_size2;
    }
    pub inline fn getDataSize3(self: *const VirtioVideoResourceQueueFtraceEventReader) u32 {
        return self._data_size3;
    }
    pub inline fn getQueueType(self: *const VirtioVideoResourceQueueFtraceEventReader) u32 {
        return self._queue_type;
    }
    pub inline fn getResourceId(self: *const VirtioVideoResourceQueueFtraceEventReader) i32 {
        return self._resource_id;
    }
    pub inline fn getStreamId(self: *const VirtioVideoResourceQueueFtraceEventReader) i32 {
        return self._stream_id;
    }
    pub inline fn getTimestamp(self: *const VirtioVideoResourceQueueFtraceEventReader) u64 {
        return self._timestamp;
    }
};
const VirtioVideoResourceQueueDoneFtraceEventWire = struct {
    const DATA_SIZE0_WIRE: gremlin.ProtoWireNumber = 1;
    const DATA_SIZE1_WIRE: gremlin.ProtoWireNumber = 2;
    const DATA_SIZE2_WIRE: gremlin.ProtoWireNumber = 3;
    const DATA_SIZE3_WIRE: gremlin.ProtoWireNumber = 4;
    const QUEUE_TYPE_WIRE: gremlin.ProtoWireNumber = 5;
    const RESOURCE_ID_WIRE: gremlin.ProtoWireNumber = 6;
    const STREAM_ID_WIRE: gremlin.ProtoWireNumber = 7;
    const TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 8;
};
pub const VirtioVideoResourceQueueDoneFtraceEvent = struct {
    // fields
    data_size0: u32 = 0,
    data_size1: u32 = 0,
    data_size2: u32 = 0,
    data_size3: u32 = 0,
    queue_type: u32 = 0,
    resource_id: i32 = 0,
    stream_id: i32 = 0,
    timestamp: u64 = 0,
    pub fn calcProtobufSize(self: *const VirtioVideoResourceQueueDoneFtraceEvent) usize {
        var res: usize = 0;
        if (self.data_size0 != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE0_WIRE) + gremlin.sizes.sizeU32(self.data_size0);
        }
        if (self.data_size1 != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE1_WIRE) + gremlin.sizes.sizeU32(self.data_size1);
        }
        if (self.data_size2 != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE2_WIRE) + gremlin.sizes.sizeU32(self.data_size2);
        }
        if (self.data_size3 != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE3_WIRE) + gremlin.sizes.sizeU32(self.data_size3);
        }
        if (self.queue_type != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueDoneFtraceEventWire.QUEUE_TYPE_WIRE) + gremlin.sizes.sizeU32(self.queue_type);
        }
        if (self.resource_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueDoneFtraceEventWire.RESOURCE_ID_WIRE) + gremlin.sizes.sizeI32(self.resource_id);
        }
        if (self.stream_id != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueDoneFtraceEventWire.STREAM_ID_WIRE) + gremlin.sizes.sizeI32(self.stream_id);
        }
        if (self.timestamp != 0) {
            res += gremlin.sizes.sizeWireNumber(VirtioVideoResourceQueueDoneFtraceEventWire.TIMESTAMP_WIRE) + gremlin.sizes.sizeU64(self.timestamp);
        }
        return res;
    }
    pub fn encode(self: *const VirtioVideoResourceQueueDoneFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const VirtioVideoResourceQueueDoneFtraceEvent, target: *gremlin.Writer) void {
        if (self.data_size0 != 0) {
            target.appendUint32(VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE0_WIRE, self.data_size0);
        }
        if (self.data_size1 != 0) {
            target.appendUint32(VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE1_WIRE, self.data_size1);
        }
        if (self.data_size2 != 0) {
            target.appendUint32(VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE2_WIRE, self.data_size2);
        }
        if (self.data_size3 != 0) {
            target.appendUint32(VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE3_WIRE, self.data_size3);
        }
        if (self.queue_type != 0) {
            target.appendUint32(VirtioVideoResourceQueueDoneFtraceEventWire.QUEUE_TYPE_WIRE, self.queue_type);
        }
        if (self.resource_id != 0) {
            target.appendInt32(VirtioVideoResourceQueueDoneFtraceEventWire.RESOURCE_ID_WIRE, self.resource_id);
        }
        if (self.stream_id != 0) {
            target.appendInt32(VirtioVideoResourceQueueDoneFtraceEventWire.STREAM_ID_WIRE, self.stream_id);
        }
        if (self.timestamp != 0) {
            target.appendUint64(VirtioVideoResourceQueueDoneFtraceEventWire.TIMESTAMP_WIRE, self.timestamp);
        }
    }
};
pub const VirtioVideoResourceQueueDoneFtraceEventReader = struct {
    buf: gremlin.Reader,
    _data_size0: u32 = 0,
    _data_size1: u32 = 0,
    _data_size2: u32 = 0,
    _data_size3: u32 = 0,
    _queue_type: u32 = 0,
    _resource_id: i32 = 0,
    _stream_id: i32 = 0,
    _timestamp: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!VirtioVideoResourceQueueDoneFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = VirtioVideoResourceQueueDoneFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE0_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._data_size0 = result.value;
                },
                VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE1_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._data_size1 = result.value;
                },
                VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE2_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._data_size2 = result.value;
                },
                VirtioVideoResourceQueueDoneFtraceEventWire.DATA_SIZE3_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._data_size3 = result.value;
                },
                VirtioVideoResourceQueueDoneFtraceEventWire.QUEUE_TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._queue_type = result.value;
                },
                VirtioVideoResourceQueueDoneFtraceEventWire.RESOURCE_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._resource_id = result.value;
                },
                VirtioVideoResourceQueueDoneFtraceEventWire.STREAM_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._stream_id = result.value;
                },
                VirtioVideoResourceQueueDoneFtraceEventWire.TIMESTAMP_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._timestamp = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDataSize0(self: *const VirtioVideoResourceQueueDoneFtraceEventReader) u32 {
        return self._data_size0;
    }
    pub inline fn getDataSize1(self: *const VirtioVideoResourceQueueDoneFtraceEventReader) u32 {
        return self._data_size1;
    }
    pub inline fn getDataSize2(self: *const VirtioVideoResourceQueueDoneFtraceEventReader) u32 {
        return self._data_size2;
    }
    pub inline fn getDataSize3(self: *const VirtioVideoResourceQueueDoneFtraceEventReader) u32 {
        return self._data_size3;
    }
    pub inline fn getQueueType(self: *const VirtioVideoResourceQueueDoneFtraceEventReader) u32 {
        return self._queue_type;
    }
    pub inline fn getResourceId(self: *const VirtioVideoResourceQueueDoneFtraceEventReader) i32 {
        return self._resource_id;
    }
    pub inline fn getStreamId(self: *const VirtioVideoResourceQueueDoneFtraceEventReader) i32 {
        return self._stream_id;
    }
    pub inline fn getTimestamp(self: *const VirtioVideoResourceQueueDoneFtraceEventReader) u64 {
        return self._timestamp;
    }
};
