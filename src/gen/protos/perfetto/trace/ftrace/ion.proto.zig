// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const IonStatFtraceEventWire = struct {
    const BUFFER_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const LEN_WIRE: gremlin.ProtoWireNumber = 2;
    const TOTAL_ALLOCATED_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const IonStatFtraceEvent = struct {
    // fields
    buffer_id: u32 = 0,
    len: i64 = 0,
    total_allocated: u64 = 0,
    pub fn calcProtobufSize(self: *const IonStatFtraceEvent) usize {
        var res: usize = 0;
        if (self.buffer_id != 0) {
            res += gremlin.sizes.sizeWireNumber(IonStatFtraceEventWire.BUFFER_ID_WIRE) + gremlin.sizes.sizeU32(self.buffer_id);
        }
        if (self.len != 0) {
            res += gremlin.sizes.sizeWireNumber(IonStatFtraceEventWire.LEN_WIRE) + gremlin.sizes.sizeI64(self.len);
        }
        if (self.total_allocated != 0) {
            res += gremlin.sizes.sizeWireNumber(IonStatFtraceEventWire.TOTAL_ALLOCATED_WIRE) + gremlin.sizes.sizeU64(self.total_allocated);
        }
        return res;
    }
    pub fn encode(self: *const IonStatFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const IonStatFtraceEvent, target: *gremlin.Writer) void {
        if (self.buffer_id != 0) {
            target.appendUint32(IonStatFtraceEventWire.BUFFER_ID_WIRE, self.buffer_id);
        }
        if (self.len != 0) {
            target.appendInt64(IonStatFtraceEventWire.LEN_WIRE, self.len);
        }
        if (self.total_allocated != 0) {
            target.appendUint64(IonStatFtraceEventWire.TOTAL_ALLOCATED_WIRE, self.total_allocated);
        }
    }
};
pub const IonStatFtraceEventReader = struct {
    buf: gremlin.Reader,
    _buffer_id: u32 = 0,
    _len: i64 = 0,
    _total_allocated: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!IonStatFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = IonStatFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                IonStatFtraceEventWire.BUFFER_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._buffer_id = result.value;
                },
                IonStatFtraceEventWire.LEN_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._len = result.value;
                },
                IonStatFtraceEventWire.TOTAL_ALLOCATED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._total_allocated = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBufferId(self: *const IonStatFtraceEventReader) u32 {
        return self._buffer_id;
    }
    pub inline fn getLen(self: *const IonStatFtraceEventReader) i64 {
        return self._len;
    }
    pub inline fn getTotalAllocated(self: *const IonStatFtraceEventReader) u64 {
        return self._total_allocated;
    }
};
