// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ParamSetValueCpmFtraceEventWire = struct {
    const BODY_WIRE: gremlin.ProtoWireNumber = 1;
    const VALUE_WIRE: gremlin.ProtoWireNumber = 2;
    const TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ParamSetValueCpmFtraceEvent = struct {
    // fields
    body: ?[]const u8 = null,
    value: u32 = 0,
    timestamp: i64 = 0,
    pub fn calcProtobufSize(self: *const ParamSetValueCpmFtraceEvent) usize {
        var res: usize = 0;
        if (self.body) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ParamSetValueCpmFtraceEventWire.BODY_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.value != 0) {
            res += gremlin.sizes.sizeWireNumber(ParamSetValueCpmFtraceEventWire.VALUE_WIRE) + gremlin.sizes.sizeU32(self.value);
        }
        if (self.timestamp != 0) {
            res += gremlin.sizes.sizeWireNumber(ParamSetValueCpmFtraceEventWire.TIMESTAMP_WIRE) + gremlin.sizes.sizeI64(self.timestamp);
        }
        return res;
    }
    pub fn encode(self: *const ParamSetValueCpmFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ParamSetValueCpmFtraceEvent, target: *gremlin.Writer) void {
        if (self.body) |v| {
            if (v.len > 0) {
                target.appendBytes(ParamSetValueCpmFtraceEventWire.BODY_WIRE, v);
            }
        }
        if (self.value != 0) {
            target.appendUint32(ParamSetValueCpmFtraceEventWire.VALUE_WIRE, self.value);
        }
        if (self.timestamp != 0) {
            target.appendInt64(ParamSetValueCpmFtraceEventWire.TIMESTAMP_WIRE, self.timestamp);
        }
    }
};
pub const ParamSetValueCpmFtraceEventReader = struct {
    buf: gremlin.Reader,
    _body: ?[]const u8 = null,
    _value: u32 = 0,
    _timestamp: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ParamSetValueCpmFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ParamSetValueCpmFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ParamSetValueCpmFtraceEventWire.BODY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._body = result.value;
                },
                ParamSetValueCpmFtraceEventWire.VALUE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._value = result.value;
                },
                ParamSetValueCpmFtraceEventWire.TIMESTAMP_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._timestamp = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBody(self: *const ParamSetValueCpmFtraceEventReader) []const u8 {
        return self._body orelse &[_]u8{};
    }
    pub inline fn getValue(self: *const ParamSetValueCpmFtraceEventReader) u32 {
        return self._value;
    }
    pub inline fn getTimestamp(self: *const ParamSetValueCpmFtraceEventReader) i64 {
        return self._timestamp;
    }
};
