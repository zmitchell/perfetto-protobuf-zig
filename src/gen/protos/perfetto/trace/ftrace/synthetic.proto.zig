// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const RssStatThrottledFtraceEventWire = struct {
    const CURR_WIRE: gremlin.ProtoWireNumber = 1;
    const MEMBER_WIRE: gremlin.ProtoWireNumber = 2;
    const MM_ID_WIRE: gremlin.ProtoWireNumber = 3;
    const SIZE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const RssStatThrottledFtraceEvent = struct {
    // fields
    curr: u32 = 0,
    member: i32 = 0,
    mm_id: u32 = 0,
    size: i64 = 0,
    pub fn calcProtobufSize(self: *const RssStatThrottledFtraceEvent) usize {
        var res: usize = 0;
        if (self.curr != 0) {
            res += gremlin.sizes.sizeWireNumber(RssStatThrottledFtraceEventWire.CURR_WIRE) + gremlin.sizes.sizeU32(self.curr);
        }
        if (self.member != 0) {
            res += gremlin.sizes.sizeWireNumber(RssStatThrottledFtraceEventWire.MEMBER_WIRE) + gremlin.sizes.sizeI32(self.member);
        }
        if (self.mm_id != 0) {
            res += gremlin.sizes.sizeWireNumber(RssStatThrottledFtraceEventWire.MM_ID_WIRE) + gremlin.sizes.sizeU32(self.mm_id);
        }
        if (self.size != 0) {
            res += gremlin.sizes.sizeWireNumber(RssStatThrottledFtraceEventWire.SIZE_WIRE) + gremlin.sizes.sizeI64(self.size);
        }
        return res;
    }
    pub fn encode(self: *const RssStatThrottledFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RssStatThrottledFtraceEvent, target: *gremlin.Writer) void {
        if (self.curr != 0) {
            target.appendUint32(RssStatThrottledFtraceEventWire.CURR_WIRE, self.curr);
        }
        if (self.member != 0) {
            target.appendInt32(RssStatThrottledFtraceEventWire.MEMBER_WIRE, self.member);
        }
        if (self.mm_id != 0) {
            target.appendUint32(RssStatThrottledFtraceEventWire.MM_ID_WIRE, self.mm_id);
        }
        if (self.size != 0) {
            target.appendInt64(RssStatThrottledFtraceEventWire.SIZE_WIRE, self.size);
        }
    }
};
pub const RssStatThrottledFtraceEventReader = struct {
    buf: gremlin.Reader,
    _curr: u32 = 0,
    _member: i32 = 0,
    _mm_id: u32 = 0,
    _size: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!RssStatThrottledFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RssStatThrottledFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RssStatThrottledFtraceEventWire.CURR_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._curr = result.value;
                },
                RssStatThrottledFtraceEventWire.MEMBER_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._member = result.value;
                },
                RssStatThrottledFtraceEventWire.MM_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._mm_id = result.value;
                },
                RssStatThrottledFtraceEventWire.SIZE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._size = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCurr(self: *const RssStatThrottledFtraceEventReader) u32 {
        return self._curr;
    }
    pub inline fn getMember(self: *const RssStatThrottledFtraceEventReader) i32 {
        return self._member;
    }
    pub inline fn getMmId(self: *const RssStatThrottledFtraceEventReader) u32 {
        return self._mm_id;
    }
    pub inline fn getSize(self: *const RssStatThrottledFtraceEventReader) i64 {
        return self._size;
    }
};
const SuspendResumeMinimalFtraceEventWire = struct {
    const START_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const SuspendResumeMinimalFtraceEvent = struct {
    // fields
    start: u32 = 0,
    pub fn calcProtobufSize(self: *const SuspendResumeMinimalFtraceEvent) usize {
        var res: usize = 0;
        if (self.start != 0) {
            res += gremlin.sizes.sizeWireNumber(SuspendResumeMinimalFtraceEventWire.START_WIRE) + gremlin.sizes.sizeU32(self.start);
        }
        return res;
    }
    pub fn encode(self: *const SuspendResumeMinimalFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SuspendResumeMinimalFtraceEvent, target: *gremlin.Writer) void {
        if (self.start != 0) {
            target.appendUint32(SuspendResumeMinimalFtraceEventWire.START_WIRE, self.start);
        }
    }
};
pub const SuspendResumeMinimalFtraceEventReader = struct {
    buf: gremlin.Reader,
    _start: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!SuspendResumeMinimalFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = SuspendResumeMinimalFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SuspendResumeMinimalFtraceEventWire.START_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._start = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getStart(self: *const SuspendResumeMinimalFtraceEventReader) u32 {
        return self._start;
    }
};
