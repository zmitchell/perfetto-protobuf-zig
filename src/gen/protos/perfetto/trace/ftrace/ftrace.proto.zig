// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PrintFtraceEventWire = struct {
    const IP_WIRE: gremlin.ProtoWireNumber = 1;
    const BUF_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const PrintFtraceEvent = struct {
    // fields
    ip: u64 = 0,
    buf: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const PrintFtraceEvent) usize {
        var res: usize = 0;
        if (self.ip != 0) {
            res += gremlin.sizes.sizeWireNumber(PrintFtraceEventWire.IP_WIRE) + gremlin.sizes.sizeU64(self.ip);
        }
        if (self.buf) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(PrintFtraceEventWire.BUF_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const PrintFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PrintFtraceEvent, target: *gremlin.Writer) void {
        if (self.ip != 0) {
            target.appendUint64(PrintFtraceEventWire.IP_WIRE, self.ip);
        }
        if (self.buf) |v| {
            if (v.len > 0) {
                target.appendBytes(PrintFtraceEventWire.BUF_WIRE, v);
            }
        }
    }
};
pub const PrintFtraceEventReader = struct {
    buf: gremlin.Reader,
    _ip: u64 = 0,
    _buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!PrintFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = PrintFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PrintFtraceEventWire.IP_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._ip = result.value;
                },
                PrintFtraceEventWire.BUF_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIp(self: *const PrintFtraceEventReader) u64 {
        return self._ip;
    }
    pub inline fn getBuf(self: *const PrintFtraceEventReader) []const u8 {
        return self._buf orelse &[_]u8{};
    }
};
const FuncgraphEntryFtraceEventWire = struct {
    const DEPTH_WIRE: gremlin.ProtoWireNumber = 1;
    const FUNC_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const FuncgraphEntryFtraceEvent = struct {
    // fields
    depth: i32 = 0,
    func: u64 = 0,
    pub fn calcProtobufSize(self: *const FuncgraphEntryFtraceEvent) usize {
        var res: usize = 0;
        if (self.depth != 0) {
            res += gremlin.sizes.sizeWireNumber(FuncgraphEntryFtraceEventWire.DEPTH_WIRE) + gremlin.sizes.sizeI32(self.depth);
        }
        if (self.func != 0) {
            res += gremlin.sizes.sizeWireNumber(FuncgraphEntryFtraceEventWire.FUNC_WIRE) + gremlin.sizes.sizeU64(self.func);
        }
        return res;
    }
    pub fn encode(self: *const FuncgraphEntryFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FuncgraphEntryFtraceEvent, target: *gremlin.Writer) void {
        if (self.depth != 0) {
            target.appendInt32(FuncgraphEntryFtraceEventWire.DEPTH_WIRE, self.depth);
        }
        if (self.func != 0) {
            target.appendUint64(FuncgraphEntryFtraceEventWire.FUNC_WIRE, self.func);
        }
    }
};
pub const FuncgraphEntryFtraceEventReader = struct {
    buf: gremlin.Reader,
    _depth: i32 = 0,
    _func: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!FuncgraphEntryFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FuncgraphEntryFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FuncgraphEntryFtraceEventWire.DEPTH_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._depth = result.value;
                },
                FuncgraphEntryFtraceEventWire.FUNC_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._func = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDepth(self: *const FuncgraphEntryFtraceEventReader) i32 {
        return self._depth;
    }
    pub inline fn getFunc(self: *const FuncgraphEntryFtraceEventReader) u64 {
        return self._func;
    }
};
const FuncgraphExitFtraceEventWire = struct {
    const CALLTIME_WIRE: gremlin.ProtoWireNumber = 1;
    const DEPTH_WIRE: gremlin.ProtoWireNumber = 2;
    const FUNC_WIRE: gremlin.ProtoWireNumber = 3;
    const OVERRUN_WIRE: gremlin.ProtoWireNumber = 4;
    const RETTIME_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const FuncgraphExitFtraceEvent = struct {
    // fields
    calltime: u64 = 0,
    depth: i32 = 0,
    func: u64 = 0,
    overrun: u64 = 0,
    rettime: u64 = 0,
    pub fn calcProtobufSize(self: *const FuncgraphExitFtraceEvent) usize {
        var res: usize = 0;
        if (self.calltime != 0) {
            res += gremlin.sizes.sizeWireNumber(FuncgraphExitFtraceEventWire.CALLTIME_WIRE) + gremlin.sizes.sizeU64(self.calltime);
        }
        if (self.depth != 0) {
            res += gremlin.sizes.sizeWireNumber(FuncgraphExitFtraceEventWire.DEPTH_WIRE) + gremlin.sizes.sizeI32(self.depth);
        }
        if (self.func != 0) {
            res += gremlin.sizes.sizeWireNumber(FuncgraphExitFtraceEventWire.FUNC_WIRE) + gremlin.sizes.sizeU64(self.func);
        }
        if (self.overrun != 0) {
            res += gremlin.sizes.sizeWireNumber(FuncgraphExitFtraceEventWire.OVERRUN_WIRE) + gremlin.sizes.sizeU64(self.overrun);
        }
        if (self.rettime != 0) {
            res += gremlin.sizes.sizeWireNumber(FuncgraphExitFtraceEventWire.RETTIME_WIRE) + gremlin.sizes.sizeU64(self.rettime);
        }
        return res;
    }
    pub fn encode(self: *const FuncgraphExitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FuncgraphExitFtraceEvent, target: *gremlin.Writer) void {
        if (self.calltime != 0) {
            target.appendUint64(FuncgraphExitFtraceEventWire.CALLTIME_WIRE, self.calltime);
        }
        if (self.depth != 0) {
            target.appendInt32(FuncgraphExitFtraceEventWire.DEPTH_WIRE, self.depth);
        }
        if (self.func != 0) {
            target.appendUint64(FuncgraphExitFtraceEventWire.FUNC_WIRE, self.func);
        }
        if (self.overrun != 0) {
            target.appendUint64(FuncgraphExitFtraceEventWire.OVERRUN_WIRE, self.overrun);
        }
        if (self.rettime != 0) {
            target.appendUint64(FuncgraphExitFtraceEventWire.RETTIME_WIRE, self.rettime);
        }
    }
};
pub const FuncgraphExitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _calltime: u64 = 0,
    _depth: i32 = 0,
    _func: u64 = 0,
    _overrun: u64 = 0,
    _rettime: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!FuncgraphExitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FuncgraphExitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FuncgraphExitFtraceEventWire.CALLTIME_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._calltime = result.value;
                },
                FuncgraphExitFtraceEventWire.DEPTH_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._depth = result.value;
                },
                FuncgraphExitFtraceEventWire.FUNC_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._func = result.value;
                },
                FuncgraphExitFtraceEventWire.OVERRUN_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._overrun = result.value;
                },
                FuncgraphExitFtraceEventWire.RETTIME_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._rettime = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCalltime(self: *const FuncgraphExitFtraceEventReader) u64 {
        return self._calltime;
    }
    pub inline fn getDepth(self: *const FuncgraphExitFtraceEventReader) i32 {
        return self._depth;
    }
    pub inline fn getFunc(self: *const FuncgraphExitFtraceEventReader) u64 {
        return self._func;
    }
    pub inline fn getOverrun(self: *const FuncgraphExitFtraceEventReader) u64 {
        return self._overrun;
    }
    pub inline fn getRettime(self: *const FuncgraphExitFtraceEventReader) u64 {
        return self._rettime;
    }
};
