// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DmaHeapStatFtraceEventWire = struct {
    const INODE_WIRE: gremlin.ProtoWireNumber = 1;
    const LEN_WIRE: gremlin.ProtoWireNumber = 2;
    const TOTAL_ALLOCATED_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const DmaHeapStatFtraceEvent = struct {
    // fields
    inode: u64 = 0,
    len: i64 = 0,
    total_allocated: u64 = 0,
    pub fn calcProtobufSize(self: *const DmaHeapStatFtraceEvent) usize {
        var res: usize = 0;
        if (self.inode != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaHeapStatFtraceEventWire.INODE_WIRE) + gremlin.sizes.sizeU64(self.inode);
        }
        if (self.len != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaHeapStatFtraceEventWire.LEN_WIRE) + gremlin.sizes.sizeI64(self.len);
        }
        if (self.total_allocated != 0) {
            res += gremlin.sizes.sizeWireNumber(DmaHeapStatFtraceEventWire.TOTAL_ALLOCATED_WIRE) + gremlin.sizes.sizeU64(self.total_allocated);
        }
        return res;
    }
    pub fn encode(self: *const DmaHeapStatFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DmaHeapStatFtraceEvent, target: *gremlin.Writer) void {
        if (self.inode != 0) {
            target.appendUint64(DmaHeapStatFtraceEventWire.INODE_WIRE, self.inode);
        }
        if (self.len != 0) {
            target.appendInt64(DmaHeapStatFtraceEventWire.LEN_WIRE, self.len);
        }
        if (self.total_allocated != 0) {
            target.appendUint64(DmaHeapStatFtraceEventWire.TOTAL_ALLOCATED_WIRE, self.total_allocated);
        }
    }
};
pub const DmaHeapStatFtraceEventReader = struct {
    buf: gremlin.Reader,
    _inode: u64 = 0,
    _len: i64 = 0,
    _total_allocated: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DmaHeapStatFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DmaHeapStatFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DmaHeapStatFtraceEventWire.INODE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._inode = result.value;
                },
                DmaHeapStatFtraceEventWire.LEN_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._len = result.value;
                },
                DmaHeapStatFtraceEventWire.TOTAL_ALLOCATED_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._total_allocated = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getInode(self: *const DmaHeapStatFtraceEventReader) u64 {
        return self._inode;
    }
    pub inline fn getLen(self: *const DmaHeapStatFtraceEventReader) i64 {
        return self._len;
    }
    pub inline fn getTotalAllocated(self: *const DmaHeapStatFtraceEventReader) u64 {
        return self._total_allocated;
    }
};
