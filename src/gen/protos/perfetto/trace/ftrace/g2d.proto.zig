// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const G2dTracingMarkWriteFtraceEventWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
    const NAME_WIRE: gremlin.ProtoWireNumber = 4;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 5;
    const VALUE_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const G2dTracingMarkWriteFtraceEvent = struct {
    // fields
    pid: i32 = 0,
    name: ?[]const u8 = null,
    type: u32 = 0,
    value: i32 = 0,
    pub fn calcProtobufSize(self: *const G2dTracingMarkWriteFtraceEvent) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(G2dTracingMarkWriteFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(G2dTracingMarkWriteFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(G2dTracingMarkWriteFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        if (self.value != 0) {
            res += gremlin.sizes.sizeWireNumber(G2dTracingMarkWriteFtraceEventWire.VALUE_WIRE) + gremlin.sizes.sizeI32(self.value);
        }
        return res;
    }
    pub fn encode(self: *const G2dTracingMarkWriteFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const G2dTracingMarkWriteFtraceEvent, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendInt32(G2dTracingMarkWriteFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(G2dTracingMarkWriteFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.type != 0) {
            target.appendUint32(G2dTracingMarkWriteFtraceEventWire.TYPE_WIRE, self.type);
        }
        if (self.value != 0) {
            target.appendInt32(G2dTracingMarkWriteFtraceEventWire.VALUE_WIRE, self.value);
        }
    }
};
pub const G2dTracingMarkWriteFtraceEventReader = struct {
    buf: gremlin.Reader,
    _pid: i32 = 0,
    _name: ?[]const u8 = null,
    _type: u32 = 0,
    _value: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!G2dTracingMarkWriteFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = G2dTracingMarkWriteFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                G2dTracingMarkWriteFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                G2dTracingMarkWriteFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                G2dTracingMarkWriteFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                G2dTracingMarkWriteFtraceEventWire.VALUE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._value = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const G2dTracingMarkWriteFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getName(self: *const G2dTracingMarkWriteFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getType(self: *const G2dTracingMarkWriteFtraceEventReader) u32 {
        return self._type;
    }
    pub inline fn getValue(self: *const G2dTracingMarkWriteFtraceEventReader) i32 {
        return self._value;
    }
};
