// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DcvshFreqFtraceEventWire = struct {
    const CPU_WIRE: gremlin.ProtoWireNumber = 1;
    const FREQ_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const DcvshFreqFtraceEvent = struct {
    // fields
    cpu: u64 = 0,
    freq: u64 = 0,
    pub fn calcProtobufSize(self: *const DcvshFreqFtraceEvent) usize {
        var res: usize = 0;
        if (self.cpu != 0) {
            res += gremlin.sizes.sizeWireNumber(DcvshFreqFtraceEventWire.CPU_WIRE) + gremlin.sizes.sizeU64(self.cpu);
        }
        if (self.freq != 0) {
            res += gremlin.sizes.sizeWireNumber(DcvshFreqFtraceEventWire.FREQ_WIRE) + gremlin.sizes.sizeU64(self.freq);
        }
        return res;
    }
    pub fn encode(self: *const DcvshFreqFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DcvshFreqFtraceEvent, target: *gremlin.Writer) void {
        if (self.cpu != 0) {
            target.appendUint64(DcvshFreqFtraceEventWire.CPU_WIRE, self.cpu);
        }
        if (self.freq != 0) {
            target.appendUint64(DcvshFreqFtraceEventWire.FREQ_WIRE, self.freq);
        }
    }
};
pub const DcvshFreqFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cpu: u64 = 0,
    _freq: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DcvshFreqFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DcvshFreqFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DcvshFreqFtraceEventWire.CPU_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._cpu = result.value;
                },
                DcvshFreqFtraceEventWire.FREQ_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._freq = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCpu(self: *const DcvshFreqFtraceEventReader) u64 {
        return self._cpu;
    }
    pub inline fn getFreq(self: *const DcvshFreqFtraceEventReader) u64 {
        return self._freq;
    }
};
