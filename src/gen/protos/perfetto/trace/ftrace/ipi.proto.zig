// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const IpiEntryFtraceEventWire = struct {
    const REASON_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const IpiEntryFtraceEvent = struct {
    // fields
    reason: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const IpiEntryFtraceEvent) usize {
        var res: usize = 0;
        if (self.reason) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(IpiEntryFtraceEventWire.REASON_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const IpiEntryFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const IpiEntryFtraceEvent, target: *gremlin.Writer) void {
        if (self.reason) |v| {
            if (v.len > 0) {
                target.appendBytes(IpiEntryFtraceEventWire.REASON_WIRE, v);
            }
        }
    }
};
pub const IpiEntryFtraceEventReader = struct {
    buf: gremlin.Reader,
    _reason: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!IpiEntryFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = IpiEntryFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                IpiEntryFtraceEventWire.REASON_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._reason = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getReason(self: *const IpiEntryFtraceEventReader) []const u8 {
        return self._reason orelse &[_]u8{};
    }
};
const IpiExitFtraceEventWire = struct {
    const REASON_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const IpiExitFtraceEvent = struct {
    // fields
    reason: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const IpiExitFtraceEvent) usize {
        var res: usize = 0;
        if (self.reason) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(IpiExitFtraceEventWire.REASON_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const IpiExitFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const IpiExitFtraceEvent, target: *gremlin.Writer) void {
        if (self.reason) |v| {
            if (v.len > 0) {
                target.appendBytes(IpiExitFtraceEventWire.REASON_WIRE, v);
            }
        }
    }
};
pub const IpiExitFtraceEventReader = struct {
    buf: gremlin.Reader,
    _reason: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!IpiExitFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = IpiExitFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                IpiExitFtraceEventWire.REASON_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._reason = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getReason(self: *const IpiExitFtraceEventReader) []const u8 {
        return self._reason orelse &[_]u8{};
    }
};
const IpiRaiseFtraceEventWire = struct {
    const TARGET_CPUS_WIRE: gremlin.ProtoWireNumber = 1;
    const REASON_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const IpiRaiseFtraceEvent = struct {
    // fields
    target_cpus: u32 = 0,
    reason: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const IpiRaiseFtraceEvent) usize {
        var res: usize = 0;
        if (self.target_cpus != 0) {
            res += gremlin.sizes.sizeWireNumber(IpiRaiseFtraceEventWire.TARGET_CPUS_WIRE) + gremlin.sizes.sizeU32(self.target_cpus);
        }
        if (self.reason) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(IpiRaiseFtraceEventWire.REASON_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const IpiRaiseFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const IpiRaiseFtraceEvent, target: *gremlin.Writer) void {
        if (self.target_cpus != 0) {
            target.appendUint32(IpiRaiseFtraceEventWire.TARGET_CPUS_WIRE, self.target_cpus);
        }
        if (self.reason) |v| {
            if (v.len > 0) {
                target.appendBytes(IpiRaiseFtraceEventWire.REASON_WIRE, v);
            }
        }
    }
};
pub const IpiRaiseFtraceEventReader = struct {
    buf: gremlin.Reader,
    _target_cpus: u32 = 0,
    _reason: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!IpiRaiseFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = IpiRaiseFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                IpiRaiseFtraceEventWire.TARGET_CPUS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._target_cpus = result.value;
                },
                IpiRaiseFtraceEventWire.REASON_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._reason = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTargetCpus(self: *const IpiRaiseFtraceEventReader) u32 {
        return self._target_cpus;
    }
    pub inline fn getReason(self: *const IpiRaiseFtraceEventReader) []const u8 {
        return self._reason orelse &[_]u8{};
    }
};
