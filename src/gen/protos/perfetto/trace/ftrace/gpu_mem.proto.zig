// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const GpuMemTotalFtraceEventWire = struct {
    const GPU_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const PID_WIRE: gremlin.ProtoWireNumber = 2;
    const SIZE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const GpuMemTotalFtraceEvent = struct {
    // fields
    gpu_id: u32 = 0,
    pid: u32 = 0,
    size: u64 = 0,
    pub fn calcProtobufSize(self: *const GpuMemTotalFtraceEvent) usize {
        var res: usize = 0;
        if (self.gpu_id != 0) {
            res += gremlin.sizes.sizeWireNumber(GpuMemTotalFtraceEventWire.GPU_ID_WIRE) + gremlin.sizes.sizeU32(self.gpu_id);
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(GpuMemTotalFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeU32(self.pid);
        }
        if (self.size != 0) {
            res += gremlin.sizes.sizeWireNumber(GpuMemTotalFtraceEventWire.SIZE_WIRE) + gremlin.sizes.sizeU64(self.size);
        }
        return res;
    }
    pub fn encode(self: *const GpuMemTotalFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GpuMemTotalFtraceEvent, target: *gremlin.Writer) void {
        if (self.gpu_id != 0) {
            target.appendUint32(GpuMemTotalFtraceEventWire.GPU_ID_WIRE, self.gpu_id);
        }
        if (self.pid != 0) {
            target.appendUint32(GpuMemTotalFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.size != 0) {
            target.appendUint64(GpuMemTotalFtraceEventWire.SIZE_WIRE, self.size);
        }
    }
};
pub const GpuMemTotalFtraceEventReader = struct {
    buf: gremlin.Reader,
    _gpu_id: u32 = 0,
    _pid: u32 = 0,
    _size: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!GpuMemTotalFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = GpuMemTotalFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GpuMemTotalFtraceEventWire.GPU_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._gpu_id = result.value;
                },
                GpuMemTotalFtraceEventWire.PID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                GpuMemTotalFtraceEventWire.SIZE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._size = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getGpuId(self: *const GpuMemTotalFtraceEventReader) u32 {
        return self._gpu_id;
    }
    pub inline fn getPid(self: *const GpuMemTotalFtraceEventReader) u32 {
        return self._pid;
    }
    pub inline fn getSize(self: *const GpuMemTotalFtraceEventReader) u64 {
        return self._size;
    }
};
