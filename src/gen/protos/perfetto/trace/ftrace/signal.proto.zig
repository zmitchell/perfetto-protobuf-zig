// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const SignalDeliverFtraceEventWire = struct {
    const CODE_WIRE: gremlin.ProtoWireNumber = 1;
    const SA_FLAGS_WIRE: gremlin.ProtoWireNumber = 2;
    const SIG_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const SignalDeliverFtraceEvent = struct {
    // fields
    code: i32 = 0,
    sa_flags: u64 = 0,
    sig: i32 = 0,
    pub fn calcProtobufSize(self: *const SignalDeliverFtraceEvent) usize {
        var res: usize = 0;
        if (self.code != 0) {
            res += gremlin.sizes.sizeWireNumber(SignalDeliverFtraceEventWire.CODE_WIRE) + gremlin.sizes.sizeI32(self.code);
        }
        if (self.sa_flags != 0) {
            res += gremlin.sizes.sizeWireNumber(SignalDeliverFtraceEventWire.SA_FLAGS_WIRE) + gremlin.sizes.sizeU64(self.sa_flags);
        }
        if (self.sig != 0) {
            res += gremlin.sizes.sizeWireNumber(SignalDeliverFtraceEventWire.SIG_WIRE) + gremlin.sizes.sizeI32(self.sig);
        }
        return res;
    }
    pub fn encode(self: *const SignalDeliverFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SignalDeliverFtraceEvent, target: *gremlin.Writer) void {
        if (self.code != 0) {
            target.appendInt32(SignalDeliverFtraceEventWire.CODE_WIRE, self.code);
        }
        if (self.sa_flags != 0) {
            target.appendUint64(SignalDeliverFtraceEventWire.SA_FLAGS_WIRE, self.sa_flags);
        }
        if (self.sig != 0) {
            target.appendInt32(SignalDeliverFtraceEventWire.SIG_WIRE, self.sig);
        }
    }
};
pub const SignalDeliverFtraceEventReader = struct {
    buf: gremlin.Reader,
    _code: i32 = 0,
    _sa_flags: u64 = 0,
    _sig: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!SignalDeliverFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = SignalDeliverFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SignalDeliverFtraceEventWire.CODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._code = result.value;
                },
                SignalDeliverFtraceEventWire.SA_FLAGS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._sa_flags = result.value;
                },
                SignalDeliverFtraceEventWire.SIG_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._sig = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCode(self: *const SignalDeliverFtraceEventReader) i32 {
        return self._code;
    }
    pub inline fn getSaFlags(self: *const SignalDeliverFtraceEventReader) u64 {
        return self._sa_flags;
    }
    pub inline fn getSig(self: *const SignalDeliverFtraceEventReader) i32 {
        return self._sig;
    }
};
const SignalGenerateFtraceEventWire = struct {
    const CODE_WIRE: gremlin.ProtoWireNumber = 1;
    const COMM_WIRE: gremlin.ProtoWireNumber = 2;
    const GROUP_WIRE: gremlin.ProtoWireNumber = 3;
    const PID_WIRE: gremlin.ProtoWireNumber = 4;
    const RESULT_WIRE: gremlin.ProtoWireNumber = 5;
    const SIG_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const SignalGenerateFtraceEvent = struct {
    // fields
    code: i32 = 0,
    comm: ?[]const u8 = null,
    group: i32 = 0,
    pid: i32 = 0,
    result: i32 = 0,
    sig: i32 = 0,
    pub fn calcProtobufSize(self: *const SignalGenerateFtraceEvent) usize {
        var res: usize = 0;
        if (self.code != 0) {
            res += gremlin.sizes.sizeWireNumber(SignalGenerateFtraceEventWire.CODE_WIRE) + gremlin.sizes.sizeI32(self.code);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SignalGenerateFtraceEventWire.COMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.group != 0) {
            res += gremlin.sizes.sizeWireNumber(SignalGenerateFtraceEventWire.GROUP_WIRE) + gremlin.sizes.sizeI32(self.group);
        }
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(SignalGenerateFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.result != 0) {
            res += gremlin.sizes.sizeWireNumber(SignalGenerateFtraceEventWire.RESULT_WIRE) + gremlin.sizes.sizeI32(self.result);
        }
        if (self.sig != 0) {
            res += gremlin.sizes.sizeWireNumber(SignalGenerateFtraceEventWire.SIG_WIRE) + gremlin.sizes.sizeI32(self.sig);
        }
        return res;
    }
    pub fn encode(self: *const SignalGenerateFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SignalGenerateFtraceEvent, target: *gremlin.Writer) void {
        if (self.code != 0) {
            target.appendInt32(SignalGenerateFtraceEventWire.CODE_WIRE, self.code);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                target.appendBytes(SignalGenerateFtraceEventWire.COMM_WIRE, v);
            }
        }
        if (self.group != 0) {
            target.appendInt32(SignalGenerateFtraceEventWire.GROUP_WIRE, self.group);
        }
        if (self.pid != 0) {
            target.appendInt32(SignalGenerateFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.result != 0) {
            target.appendInt32(SignalGenerateFtraceEventWire.RESULT_WIRE, self.result);
        }
        if (self.sig != 0) {
            target.appendInt32(SignalGenerateFtraceEventWire.SIG_WIRE, self.sig);
        }
    }
};
pub const SignalGenerateFtraceEventReader = struct {
    buf: gremlin.Reader,
    _code: i32 = 0,
    _comm: ?[]const u8 = null,
    _group: i32 = 0,
    _pid: i32 = 0,
    _result: i32 = 0,
    _sig: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!SignalGenerateFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = SignalGenerateFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SignalGenerateFtraceEventWire.CODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._code = result.value;
                },
                SignalGenerateFtraceEventWire.COMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._comm = result.value;
                },
                SignalGenerateFtraceEventWire.GROUP_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._group = result.value;
                },
                SignalGenerateFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                SignalGenerateFtraceEventWire.RESULT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._result = result.value;
                },
                SignalGenerateFtraceEventWire.SIG_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._sig = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCode(self: *const SignalGenerateFtraceEventReader) i32 {
        return self._code;
    }
    pub inline fn getComm(self: *const SignalGenerateFtraceEventReader) []const u8 {
        return self._comm orelse &[_]u8{};
    }
    pub inline fn getGroup(self: *const SignalGenerateFtraceEventReader) i32 {
        return self._group;
    }
    pub inline fn getPid(self: *const SignalGenerateFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getResult(self: *const SignalGenerateFtraceEventReader) i32 {
        return self._result;
    }
    pub inline fn getSig(self: *const SignalGenerateFtraceEventReader) i32 {
        return self._sig;
    }
};
