// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DpuTracingMarkWriteFtraceEventWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
    const TRACE_NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const TRACE_BEGIN_WIRE: gremlin.ProtoWireNumber = 3;
    const NAME_WIRE: gremlin.ProtoWireNumber = 4;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 5;
    const VALUE_WIRE: gremlin.ProtoWireNumber = 6;
};
pub const DpuTracingMarkWriteFtraceEvent = struct {
    // fields
    pid: i32 = 0,
    trace_name: ?[]const u8 = null,
    trace_begin: u32 = 0,
    name: ?[]const u8 = null,
    type: u32 = 0,
    value: i32 = 0,
    pub fn calcProtobufSize(self: *const DpuTracingMarkWriteFtraceEvent) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuTracingMarkWriteFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.trace_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DpuTracingMarkWriteFtraceEventWire.TRACE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.trace_begin != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuTracingMarkWriteFtraceEventWire.TRACE_BEGIN_WIRE) + gremlin.sizes.sizeU32(self.trace_begin);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DpuTracingMarkWriteFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuTracingMarkWriteFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        if (self.value != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuTracingMarkWriteFtraceEventWire.VALUE_WIRE) + gremlin.sizes.sizeI32(self.value);
        }
        return res;
    }
    pub fn encode(self: *const DpuTracingMarkWriteFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DpuTracingMarkWriteFtraceEvent, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendInt32(DpuTracingMarkWriteFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.trace_name) |v| {
            if (v.len > 0) {
                target.appendBytes(DpuTracingMarkWriteFtraceEventWire.TRACE_NAME_WIRE, v);
            }
        }
        if (self.trace_begin != 0) {
            target.appendUint32(DpuTracingMarkWriteFtraceEventWire.TRACE_BEGIN_WIRE, self.trace_begin);
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(DpuTracingMarkWriteFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.type != 0) {
            target.appendUint32(DpuTracingMarkWriteFtraceEventWire.TYPE_WIRE, self.type);
        }
        if (self.value != 0) {
            target.appendInt32(DpuTracingMarkWriteFtraceEventWire.VALUE_WIRE, self.value);
        }
    }
};
pub const DpuTracingMarkWriteFtraceEventReader = struct {
    buf: gremlin.Reader,
    _pid: i32 = 0,
    _trace_name: ?[]const u8 = null,
    _trace_begin: u32 = 0,
    _name: ?[]const u8 = null,
    _type: u32 = 0,
    _value: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DpuTracingMarkWriteFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DpuTracingMarkWriteFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DpuTracingMarkWriteFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                DpuTracingMarkWriteFtraceEventWire.TRACE_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._trace_name = result.value;
                },
                DpuTracingMarkWriteFtraceEventWire.TRACE_BEGIN_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._trace_begin = result.value;
                },
                DpuTracingMarkWriteFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                DpuTracingMarkWriteFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                DpuTracingMarkWriteFtraceEventWire.VALUE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._value = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const DpuTracingMarkWriteFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getTraceName(self: *const DpuTracingMarkWriteFtraceEventReader) []const u8 {
        return self._trace_name orelse &[_]u8{};
    }
    pub inline fn getTraceBegin(self: *const DpuTracingMarkWriteFtraceEventReader) u32 {
        return self._trace_begin;
    }
    pub inline fn getName(self: *const DpuTracingMarkWriteFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getType(self: *const DpuTracingMarkWriteFtraceEventReader) u32 {
        return self._type;
    }
    pub inline fn getValue(self: *const DpuTracingMarkWriteFtraceEventReader) i32 {
        return self._value;
    }
};
const DpuDsiCmdFifoStatusFtraceEventWire = struct {
    const HEADER_WIRE: gremlin.ProtoWireNumber = 1;
    const PAYLOAD_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const DpuDsiCmdFifoStatusFtraceEvent = struct {
    // fields
    header: u32 = 0,
    payload: u32 = 0,
    pub fn calcProtobufSize(self: *const DpuDsiCmdFifoStatusFtraceEvent) usize {
        var res: usize = 0;
        if (self.header != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDsiCmdFifoStatusFtraceEventWire.HEADER_WIRE) + gremlin.sizes.sizeU32(self.header);
        }
        if (self.payload != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDsiCmdFifoStatusFtraceEventWire.PAYLOAD_WIRE) + gremlin.sizes.sizeU32(self.payload);
        }
        return res;
    }
    pub fn encode(self: *const DpuDsiCmdFifoStatusFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DpuDsiCmdFifoStatusFtraceEvent, target: *gremlin.Writer) void {
        if (self.header != 0) {
            target.appendUint32(DpuDsiCmdFifoStatusFtraceEventWire.HEADER_WIRE, self.header);
        }
        if (self.payload != 0) {
            target.appendUint32(DpuDsiCmdFifoStatusFtraceEventWire.PAYLOAD_WIRE, self.payload);
        }
    }
};
pub const DpuDsiCmdFifoStatusFtraceEventReader = struct {
    buf: gremlin.Reader,
    _header: u32 = 0,
    _payload: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DpuDsiCmdFifoStatusFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DpuDsiCmdFifoStatusFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DpuDsiCmdFifoStatusFtraceEventWire.HEADER_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._header = result.value;
                },
                DpuDsiCmdFifoStatusFtraceEventWire.PAYLOAD_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._payload = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getHeader(self: *const DpuDsiCmdFifoStatusFtraceEventReader) u32 {
        return self._header;
    }
    pub inline fn getPayload(self: *const DpuDsiCmdFifoStatusFtraceEventReader) u32 {
        return self._payload;
    }
};
const DpuDsiRxFtraceEventWire = struct {
    const CMD_WIRE: gremlin.ProtoWireNumber = 1;
    const RX_BUF_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const DpuDsiRxFtraceEvent = struct {
    // fields
    cmd: u32 = 0,
    rx_buf: u32 = 0,
    pub fn calcProtobufSize(self: *const DpuDsiRxFtraceEvent) usize {
        var res: usize = 0;
        if (self.cmd != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDsiRxFtraceEventWire.CMD_WIRE) + gremlin.sizes.sizeU32(self.cmd);
        }
        if (self.rx_buf != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDsiRxFtraceEventWire.RX_BUF_WIRE) + gremlin.sizes.sizeU32(self.rx_buf);
        }
        return res;
    }
    pub fn encode(self: *const DpuDsiRxFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DpuDsiRxFtraceEvent, target: *gremlin.Writer) void {
        if (self.cmd != 0) {
            target.appendUint32(DpuDsiRxFtraceEventWire.CMD_WIRE, self.cmd);
        }
        if (self.rx_buf != 0) {
            target.appendUint32(DpuDsiRxFtraceEventWire.RX_BUF_WIRE, self.rx_buf);
        }
    }
};
pub const DpuDsiRxFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cmd: u32 = 0,
    _rx_buf: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DpuDsiRxFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DpuDsiRxFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DpuDsiRxFtraceEventWire.CMD_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cmd = result.value;
                },
                DpuDsiRxFtraceEventWire.RX_BUF_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._rx_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCmd(self: *const DpuDsiRxFtraceEventReader) u32 {
        return self._cmd;
    }
    pub inline fn getRxBuf(self: *const DpuDsiRxFtraceEventReader) u32 {
        return self._rx_buf;
    }
};
const DpuDsiTxFtraceEventWire = struct {
    const TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const TX_BUF_WIRE: gremlin.ProtoWireNumber = 2;
    const LAST_WIRE: gremlin.ProtoWireNumber = 3;
    const DELAY_MS_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DpuDsiTxFtraceEvent = struct {
    // fields
    type: u32 = 0,
    tx_buf: u32 = 0,
    last: u32 = 0,
    delay_ms: u32 = 0,
    pub fn calcProtobufSize(self: *const DpuDsiTxFtraceEvent) usize {
        var res: usize = 0;
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDsiTxFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        if (self.tx_buf != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDsiTxFtraceEventWire.TX_BUF_WIRE) + gremlin.sizes.sizeU32(self.tx_buf);
        }
        if (self.last != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDsiTxFtraceEventWire.LAST_WIRE) + gremlin.sizes.sizeU32(self.last);
        }
        if (self.delay_ms != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDsiTxFtraceEventWire.DELAY_MS_WIRE) + gremlin.sizes.sizeU32(self.delay_ms);
        }
        return res;
    }
    pub fn encode(self: *const DpuDsiTxFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DpuDsiTxFtraceEvent, target: *gremlin.Writer) void {
        if (self.type != 0) {
            target.appendUint32(DpuDsiTxFtraceEventWire.TYPE_WIRE, self.type);
        }
        if (self.tx_buf != 0) {
            target.appendUint32(DpuDsiTxFtraceEventWire.TX_BUF_WIRE, self.tx_buf);
        }
        if (self.last != 0) {
            target.appendUint32(DpuDsiTxFtraceEventWire.LAST_WIRE, self.last);
        }
        if (self.delay_ms != 0) {
            target.appendUint32(DpuDsiTxFtraceEventWire.DELAY_MS_WIRE, self.delay_ms);
        }
    }
};
pub const DpuDsiTxFtraceEventReader = struct {
    buf: gremlin.Reader,
    _type: u32 = 0,
    _tx_buf: u32 = 0,
    _last: u32 = 0,
    _delay_ms: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DpuDsiTxFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DpuDsiTxFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DpuDsiTxFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                DpuDsiTxFtraceEventWire.TX_BUF_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._tx_buf = result.value;
                },
                DpuDsiTxFtraceEventWire.LAST_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._last = result.value;
                },
                DpuDsiTxFtraceEventWire.DELAY_MS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._delay_ms = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getType(self: *const DpuDsiTxFtraceEventReader) u32 {
        return self._type;
    }
    pub inline fn getTxBuf(self: *const DpuDsiTxFtraceEventReader) u32 {
        return self._tx_buf;
    }
    pub inline fn getLast(self: *const DpuDsiTxFtraceEventReader) u32 {
        return self._last;
    }
    pub inline fn getDelayMs(self: *const DpuDsiTxFtraceEventReader) u32 {
        return self._delay_ms;
    }
};
const DpuDispDpuUnderrunFtraceEventWire = struct {
    const ID_WIRE: gremlin.ProtoWireNumber = 1;
    const FRAMES_PENDING_WIRE: gremlin.ProtoWireNumber = 2;
    const VSYNC_COUNT_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const DpuDispDpuUnderrunFtraceEvent = struct {
    // fields
    id: i32 = 0,
    frames_pending: i32 = 0,
    vsync_count: i32 = 0,
    pub fn calcProtobufSize(self: *const DpuDispDpuUnderrunFtraceEvent) usize {
        var res: usize = 0;
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDispDpuUnderrunFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI32(self.id);
        }
        if (self.frames_pending != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDispDpuUnderrunFtraceEventWire.FRAMES_PENDING_WIRE) + gremlin.sizes.sizeI32(self.frames_pending);
        }
        if (self.vsync_count != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDispDpuUnderrunFtraceEventWire.VSYNC_COUNT_WIRE) + gremlin.sizes.sizeI32(self.vsync_count);
        }
        return res;
    }
    pub fn encode(self: *const DpuDispDpuUnderrunFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DpuDispDpuUnderrunFtraceEvent, target: *gremlin.Writer) void {
        if (self.id != 0) {
            target.appendInt32(DpuDispDpuUnderrunFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.frames_pending != 0) {
            target.appendInt32(DpuDispDpuUnderrunFtraceEventWire.FRAMES_PENDING_WIRE, self.frames_pending);
        }
        if (self.vsync_count != 0) {
            target.appendInt32(DpuDispDpuUnderrunFtraceEventWire.VSYNC_COUNT_WIRE, self.vsync_count);
        }
    }
};
pub const DpuDispDpuUnderrunFtraceEventReader = struct {
    buf: gremlin.Reader,
    _id: i32 = 0,
    _frames_pending: i32 = 0,
    _vsync_count: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DpuDispDpuUnderrunFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DpuDispDpuUnderrunFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DpuDispDpuUnderrunFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                DpuDispDpuUnderrunFtraceEventWire.FRAMES_PENDING_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._frames_pending = result.value;
                },
                DpuDispDpuUnderrunFtraceEventWire.VSYNC_COUNT_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._vsync_count = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getId(self: *const DpuDispDpuUnderrunFtraceEventReader) i32 {
        return self._id;
    }
    pub inline fn getFramesPending(self: *const DpuDispDpuUnderrunFtraceEventReader) i32 {
        return self._frames_pending;
    }
    pub inline fn getVsyncCount(self: *const DpuDispDpuUnderrunFtraceEventReader) i32 {
        return self._vsync_count;
    }
};
const DpuDispVblankIrqEnableFtraceEventWire = struct {
    const ID_WIRE: gremlin.ProtoWireNumber = 1;
    const OUTPUT_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const ENABLE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const DpuDispVblankIrqEnableFtraceEvent = struct {
    // fields
    id: i32 = 0,
    output_id: i32 = 0,
    enable: i32 = 0,
    pub fn calcProtobufSize(self: *const DpuDispVblankIrqEnableFtraceEvent) usize {
        var res: usize = 0;
        if (self.id != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDispVblankIrqEnableFtraceEventWire.ID_WIRE) + gremlin.sizes.sizeI32(self.id);
        }
        if (self.output_id != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDispVblankIrqEnableFtraceEventWire.OUTPUT_ID_WIRE) + gremlin.sizes.sizeI32(self.output_id);
        }
        if (self.enable != 0) {
            res += gremlin.sizes.sizeWireNumber(DpuDispVblankIrqEnableFtraceEventWire.ENABLE_WIRE) + gremlin.sizes.sizeI32(self.enable);
        }
        return res;
    }
    pub fn encode(self: *const DpuDispVblankIrqEnableFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DpuDispVblankIrqEnableFtraceEvent, target: *gremlin.Writer) void {
        if (self.id != 0) {
            target.appendInt32(DpuDispVblankIrqEnableFtraceEventWire.ID_WIRE, self.id);
        }
        if (self.output_id != 0) {
            target.appendInt32(DpuDispVblankIrqEnableFtraceEventWire.OUTPUT_ID_WIRE, self.output_id);
        }
        if (self.enable != 0) {
            target.appendInt32(DpuDispVblankIrqEnableFtraceEventWire.ENABLE_WIRE, self.enable);
        }
    }
};
pub const DpuDispVblankIrqEnableFtraceEventReader = struct {
    buf: gremlin.Reader,
    _id: i32 = 0,
    _output_id: i32 = 0,
    _enable: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DpuDispVblankIrqEnableFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DpuDispVblankIrqEnableFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DpuDispVblankIrqEnableFtraceEventWire.ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._id = result.value;
                },
                DpuDispVblankIrqEnableFtraceEventWire.OUTPUT_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._output_id = result.value;
                },
                DpuDispVblankIrqEnableFtraceEventWire.ENABLE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._enable = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getId(self: *const DpuDispVblankIrqEnableFtraceEventReader) i32 {
        return self._id;
    }
    pub inline fn getOutputId(self: *const DpuDispVblankIrqEnableFtraceEventReader) i32 {
        return self._output_id;
    }
    pub inline fn getEnable(self: *const DpuDispVblankIrqEnableFtraceEventReader) i32 {
        return self._enable;
    }
};
