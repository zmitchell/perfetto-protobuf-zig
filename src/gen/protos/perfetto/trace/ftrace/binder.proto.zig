// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const BinderTransactionFtraceEventWire = struct {
    const DEBUG_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const TARGET_NODE_WIRE: gremlin.ProtoWireNumber = 2;
    const TO_PROC_WIRE: gremlin.ProtoWireNumber = 3;
    const TO_THREAD_WIRE: gremlin.ProtoWireNumber = 4;
    const REPLY_WIRE: gremlin.ProtoWireNumber = 5;
    const CODE_WIRE: gremlin.ProtoWireNumber = 6;
    const FLAGS_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const BinderTransactionFtraceEvent = struct {
    // fields
    debug_id: i32 = 0,
    target_node: i32 = 0,
    to_proc: i32 = 0,
    to_thread: i32 = 0,
    reply: i32 = 0,
    code: u32 = 0,
    flags: u32 = 0,
    pub fn calcProtobufSize(self: *const BinderTransactionFtraceEvent) usize {
        var res: usize = 0;
        if (self.debug_id != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionFtraceEventWire.DEBUG_ID_WIRE) + gremlin.sizes.sizeI32(self.debug_id);
        }
        if (self.target_node != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionFtraceEventWire.TARGET_NODE_WIRE) + gremlin.sizes.sizeI32(self.target_node);
        }
        if (self.to_proc != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionFtraceEventWire.TO_PROC_WIRE) + gremlin.sizes.sizeI32(self.to_proc);
        }
        if (self.to_thread != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionFtraceEventWire.TO_THREAD_WIRE) + gremlin.sizes.sizeI32(self.to_thread);
        }
        if (self.reply != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionFtraceEventWire.REPLY_WIRE) + gremlin.sizes.sizeI32(self.reply);
        }
        if (self.code != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionFtraceEventWire.CODE_WIRE) + gremlin.sizes.sizeU32(self.code);
        }
        if (self.flags != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionFtraceEventWire.FLAGS_WIRE) + gremlin.sizes.sizeU32(self.flags);
        }
        return res;
    }
    pub fn encode(self: *const BinderTransactionFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderTransactionFtraceEvent, target: *gremlin.Writer) void {
        if (self.debug_id != 0) {
            target.appendInt32(BinderTransactionFtraceEventWire.DEBUG_ID_WIRE, self.debug_id);
        }
        if (self.target_node != 0) {
            target.appendInt32(BinderTransactionFtraceEventWire.TARGET_NODE_WIRE, self.target_node);
        }
        if (self.to_proc != 0) {
            target.appendInt32(BinderTransactionFtraceEventWire.TO_PROC_WIRE, self.to_proc);
        }
        if (self.to_thread != 0) {
            target.appendInt32(BinderTransactionFtraceEventWire.TO_THREAD_WIRE, self.to_thread);
        }
        if (self.reply != 0) {
            target.appendInt32(BinderTransactionFtraceEventWire.REPLY_WIRE, self.reply);
        }
        if (self.code != 0) {
            target.appendUint32(BinderTransactionFtraceEventWire.CODE_WIRE, self.code);
        }
        if (self.flags != 0) {
            target.appendUint32(BinderTransactionFtraceEventWire.FLAGS_WIRE, self.flags);
        }
    }
};
pub const BinderTransactionFtraceEventReader = struct {
    buf: gremlin.Reader,
    _debug_id: i32 = 0,
    _target_node: i32 = 0,
    _to_proc: i32 = 0,
    _to_thread: i32 = 0,
    _reply: i32 = 0,
    _code: u32 = 0,
    _flags: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!BinderTransactionFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderTransactionFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderTransactionFtraceEventWire.DEBUG_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._debug_id = result.value;
                },
                BinderTransactionFtraceEventWire.TARGET_NODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._target_node = result.value;
                },
                BinderTransactionFtraceEventWire.TO_PROC_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._to_proc = result.value;
                },
                BinderTransactionFtraceEventWire.TO_THREAD_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._to_thread = result.value;
                },
                BinderTransactionFtraceEventWire.REPLY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._reply = result.value;
                },
                BinderTransactionFtraceEventWire.CODE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._code = result.value;
                },
                BinderTransactionFtraceEventWire.FLAGS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._flags = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDebugId(self: *const BinderTransactionFtraceEventReader) i32 {
        return self._debug_id;
    }
    pub inline fn getTargetNode(self: *const BinderTransactionFtraceEventReader) i32 {
        return self._target_node;
    }
    pub inline fn getToProc(self: *const BinderTransactionFtraceEventReader) i32 {
        return self._to_proc;
    }
    pub inline fn getToThread(self: *const BinderTransactionFtraceEventReader) i32 {
        return self._to_thread;
    }
    pub inline fn getReply(self: *const BinderTransactionFtraceEventReader) i32 {
        return self._reply;
    }
    pub inline fn getCode(self: *const BinderTransactionFtraceEventReader) u32 {
        return self._code;
    }
    pub inline fn getFlags(self: *const BinderTransactionFtraceEventReader) u32 {
        return self._flags;
    }
};
const BinderTransactionReceivedFtraceEventWire = struct {
    const DEBUG_ID_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const BinderTransactionReceivedFtraceEvent = struct {
    // fields
    debug_id: i32 = 0,
    pub fn calcProtobufSize(self: *const BinderTransactionReceivedFtraceEvent) usize {
        var res: usize = 0;
        if (self.debug_id != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionReceivedFtraceEventWire.DEBUG_ID_WIRE) + gremlin.sizes.sizeI32(self.debug_id);
        }
        return res;
    }
    pub fn encode(self: *const BinderTransactionReceivedFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderTransactionReceivedFtraceEvent, target: *gremlin.Writer) void {
        if (self.debug_id != 0) {
            target.appendInt32(BinderTransactionReceivedFtraceEventWire.DEBUG_ID_WIRE, self.debug_id);
        }
    }
};
pub const BinderTransactionReceivedFtraceEventReader = struct {
    buf: gremlin.Reader,
    _debug_id: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!BinderTransactionReceivedFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderTransactionReceivedFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderTransactionReceivedFtraceEventWire.DEBUG_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._debug_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDebugId(self: *const BinderTransactionReceivedFtraceEventReader) i32 {
        return self._debug_id;
    }
};
const BinderSetPriorityFtraceEventWire = struct {
    const PROC_WIRE: gremlin.ProtoWireNumber = 1;
    const THREAD_WIRE: gremlin.ProtoWireNumber = 2;
    const OLD_PRIO_WIRE: gremlin.ProtoWireNumber = 3;
    const NEW_PRIO_WIRE: gremlin.ProtoWireNumber = 4;
    const DESIRED_PRIO_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const BinderSetPriorityFtraceEvent = struct {
    // fields
    proc: i32 = 0,
    thread: i32 = 0,
    old_prio: u32 = 0,
    new_prio: u32 = 0,
    desired_prio: u32 = 0,
    pub fn calcProtobufSize(self: *const BinderSetPriorityFtraceEvent) usize {
        var res: usize = 0;
        if (self.proc != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderSetPriorityFtraceEventWire.PROC_WIRE) + gremlin.sizes.sizeI32(self.proc);
        }
        if (self.thread != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderSetPriorityFtraceEventWire.THREAD_WIRE) + gremlin.sizes.sizeI32(self.thread);
        }
        if (self.old_prio != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderSetPriorityFtraceEventWire.OLD_PRIO_WIRE) + gremlin.sizes.sizeU32(self.old_prio);
        }
        if (self.new_prio != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderSetPriorityFtraceEventWire.NEW_PRIO_WIRE) + gremlin.sizes.sizeU32(self.new_prio);
        }
        if (self.desired_prio != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderSetPriorityFtraceEventWire.DESIRED_PRIO_WIRE) + gremlin.sizes.sizeU32(self.desired_prio);
        }
        return res;
    }
    pub fn encode(self: *const BinderSetPriorityFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderSetPriorityFtraceEvent, target: *gremlin.Writer) void {
        if (self.proc != 0) {
            target.appendInt32(BinderSetPriorityFtraceEventWire.PROC_WIRE, self.proc);
        }
        if (self.thread != 0) {
            target.appendInt32(BinderSetPriorityFtraceEventWire.THREAD_WIRE, self.thread);
        }
        if (self.old_prio != 0) {
            target.appendUint32(BinderSetPriorityFtraceEventWire.OLD_PRIO_WIRE, self.old_prio);
        }
        if (self.new_prio != 0) {
            target.appendUint32(BinderSetPriorityFtraceEventWire.NEW_PRIO_WIRE, self.new_prio);
        }
        if (self.desired_prio != 0) {
            target.appendUint32(BinderSetPriorityFtraceEventWire.DESIRED_PRIO_WIRE, self.desired_prio);
        }
    }
};
pub const BinderSetPriorityFtraceEventReader = struct {
    buf: gremlin.Reader,
    _proc: i32 = 0,
    _thread: i32 = 0,
    _old_prio: u32 = 0,
    _new_prio: u32 = 0,
    _desired_prio: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!BinderSetPriorityFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderSetPriorityFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderSetPriorityFtraceEventWire.PROC_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._proc = result.value;
                },
                BinderSetPriorityFtraceEventWire.THREAD_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._thread = result.value;
                },
                BinderSetPriorityFtraceEventWire.OLD_PRIO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._old_prio = result.value;
                },
                BinderSetPriorityFtraceEventWire.NEW_PRIO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._new_prio = result.value;
                },
                BinderSetPriorityFtraceEventWire.DESIRED_PRIO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._desired_prio = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getProc(self: *const BinderSetPriorityFtraceEventReader) i32 {
        return self._proc;
    }
    pub inline fn getThread(self: *const BinderSetPriorityFtraceEventReader) i32 {
        return self._thread;
    }
    pub inline fn getOldPrio(self: *const BinderSetPriorityFtraceEventReader) u32 {
        return self._old_prio;
    }
    pub inline fn getNewPrio(self: *const BinderSetPriorityFtraceEventReader) u32 {
        return self._new_prio;
    }
    pub inline fn getDesiredPrio(self: *const BinderSetPriorityFtraceEventReader) u32 {
        return self._desired_prio;
    }
};
const BinderLockFtraceEventWire = struct {
    const TAG_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const BinderLockFtraceEvent = struct {
    // fields
    tag: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const BinderLockFtraceEvent) usize {
        var res: usize = 0;
        if (self.tag) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(BinderLockFtraceEventWire.TAG_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const BinderLockFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderLockFtraceEvent, target: *gremlin.Writer) void {
        if (self.tag) |v| {
            if (v.len > 0) {
                target.appendBytes(BinderLockFtraceEventWire.TAG_WIRE, v);
            }
        }
    }
};
pub const BinderLockFtraceEventReader = struct {
    buf: gremlin.Reader,
    _tag: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!BinderLockFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderLockFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderLockFtraceEventWire.TAG_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._tag = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTag(self: *const BinderLockFtraceEventReader) []const u8 {
        return self._tag orelse &[_]u8{};
    }
};
const BinderLockedFtraceEventWire = struct {
    const TAG_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const BinderLockedFtraceEvent = struct {
    // fields
    tag: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const BinderLockedFtraceEvent) usize {
        var res: usize = 0;
        if (self.tag) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(BinderLockedFtraceEventWire.TAG_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const BinderLockedFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderLockedFtraceEvent, target: *gremlin.Writer) void {
        if (self.tag) |v| {
            if (v.len > 0) {
                target.appendBytes(BinderLockedFtraceEventWire.TAG_WIRE, v);
            }
        }
    }
};
pub const BinderLockedFtraceEventReader = struct {
    buf: gremlin.Reader,
    _tag: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!BinderLockedFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderLockedFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderLockedFtraceEventWire.TAG_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._tag = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTag(self: *const BinderLockedFtraceEventReader) []const u8 {
        return self._tag orelse &[_]u8{};
    }
};
const BinderUnlockFtraceEventWire = struct {
    const TAG_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const BinderUnlockFtraceEvent = struct {
    // fields
    tag: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const BinderUnlockFtraceEvent) usize {
        var res: usize = 0;
        if (self.tag) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(BinderUnlockFtraceEventWire.TAG_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const BinderUnlockFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderUnlockFtraceEvent, target: *gremlin.Writer) void {
        if (self.tag) |v| {
            if (v.len > 0) {
                target.appendBytes(BinderUnlockFtraceEventWire.TAG_WIRE, v);
            }
        }
    }
};
pub const BinderUnlockFtraceEventReader = struct {
    buf: gremlin.Reader,
    _tag: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!BinderUnlockFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderUnlockFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderUnlockFtraceEventWire.TAG_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._tag = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTag(self: *const BinderUnlockFtraceEventReader) []const u8 {
        return self._tag orelse &[_]u8{};
    }
};
const BinderTransactionAllocBufFtraceEventWire = struct {
    const DATA_SIZE_WIRE: gremlin.ProtoWireNumber = 1;
    const DEBUG_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const OFFSETS_SIZE_WIRE: gremlin.ProtoWireNumber = 3;
    const EXTRA_BUFFERS_SIZE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const BinderTransactionAllocBufFtraceEvent = struct {
    // fields
    data_size: u64 = 0,
    debug_id: i32 = 0,
    offsets_size: u64 = 0,
    extra_buffers_size: u64 = 0,
    pub fn calcProtobufSize(self: *const BinderTransactionAllocBufFtraceEvent) usize {
        var res: usize = 0;
        if (self.data_size != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionAllocBufFtraceEventWire.DATA_SIZE_WIRE) + gremlin.sizes.sizeU64(self.data_size);
        }
        if (self.debug_id != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionAllocBufFtraceEventWire.DEBUG_ID_WIRE) + gremlin.sizes.sizeI32(self.debug_id);
        }
        if (self.offsets_size != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionAllocBufFtraceEventWire.OFFSETS_SIZE_WIRE) + gremlin.sizes.sizeU64(self.offsets_size);
        }
        if (self.extra_buffers_size != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderTransactionAllocBufFtraceEventWire.EXTRA_BUFFERS_SIZE_WIRE) + gremlin.sizes.sizeU64(self.extra_buffers_size);
        }
        return res;
    }
    pub fn encode(self: *const BinderTransactionAllocBufFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderTransactionAllocBufFtraceEvent, target: *gremlin.Writer) void {
        if (self.data_size != 0) {
            target.appendUint64(BinderTransactionAllocBufFtraceEventWire.DATA_SIZE_WIRE, self.data_size);
        }
        if (self.debug_id != 0) {
            target.appendInt32(BinderTransactionAllocBufFtraceEventWire.DEBUG_ID_WIRE, self.debug_id);
        }
        if (self.offsets_size != 0) {
            target.appendUint64(BinderTransactionAllocBufFtraceEventWire.OFFSETS_SIZE_WIRE, self.offsets_size);
        }
        if (self.extra_buffers_size != 0) {
            target.appendUint64(BinderTransactionAllocBufFtraceEventWire.EXTRA_BUFFERS_SIZE_WIRE, self.extra_buffers_size);
        }
    }
};
pub const BinderTransactionAllocBufFtraceEventReader = struct {
    buf: gremlin.Reader,
    _data_size: u64 = 0,
    _debug_id: i32 = 0,
    _offsets_size: u64 = 0,
    _extra_buffers_size: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!BinderTransactionAllocBufFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderTransactionAllocBufFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderTransactionAllocBufFtraceEventWire.DATA_SIZE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._data_size = result.value;
                },
                BinderTransactionAllocBufFtraceEventWire.DEBUG_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._debug_id = result.value;
                },
                BinderTransactionAllocBufFtraceEventWire.OFFSETS_SIZE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._offsets_size = result.value;
                },
                BinderTransactionAllocBufFtraceEventWire.EXTRA_BUFFERS_SIZE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._extra_buffers_size = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDataSize(self: *const BinderTransactionAllocBufFtraceEventReader) u64 {
        return self._data_size;
    }
    pub inline fn getDebugId(self: *const BinderTransactionAllocBufFtraceEventReader) i32 {
        return self._debug_id;
    }
    pub inline fn getOffsetsSize(self: *const BinderTransactionAllocBufFtraceEventReader) u64 {
        return self._offsets_size;
    }
    pub inline fn getExtraBuffersSize(self: *const BinderTransactionAllocBufFtraceEventReader) u64 {
        return self._extra_buffers_size;
    }
};
const BinderCommandFtraceEventWire = struct {
    const CMD_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const BinderCommandFtraceEvent = struct {
    // fields
    cmd: u32 = 0,
    pub fn calcProtobufSize(self: *const BinderCommandFtraceEvent) usize {
        var res: usize = 0;
        if (self.cmd != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderCommandFtraceEventWire.CMD_WIRE) + gremlin.sizes.sizeU32(self.cmd);
        }
        return res;
    }
    pub fn encode(self: *const BinderCommandFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderCommandFtraceEvent, target: *gremlin.Writer) void {
        if (self.cmd != 0) {
            target.appendUint32(BinderCommandFtraceEventWire.CMD_WIRE, self.cmd);
        }
    }
};
pub const BinderCommandFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cmd: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!BinderCommandFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderCommandFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderCommandFtraceEventWire.CMD_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cmd = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCmd(self: *const BinderCommandFtraceEventReader) u32 {
        return self._cmd;
    }
};
const BinderReturnFtraceEventWire = struct {
    const CMD_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const BinderReturnFtraceEvent = struct {
    // fields
    cmd: u32 = 0,
    pub fn calcProtobufSize(self: *const BinderReturnFtraceEvent) usize {
        var res: usize = 0;
        if (self.cmd != 0) {
            res += gremlin.sizes.sizeWireNumber(BinderReturnFtraceEventWire.CMD_WIRE) + gremlin.sizes.sizeU32(self.cmd);
        }
        return res;
    }
    pub fn encode(self: *const BinderReturnFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BinderReturnFtraceEvent, target: *gremlin.Writer) void {
        if (self.cmd != 0) {
            target.appendUint32(BinderReturnFtraceEventWire.CMD_WIRE, self.cmd);
        }
    }
};
pub const BinderReturnFtraceEventReader = struct {
    buf: gremlin.Reader,
    _cmd: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!BinderReturnFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = BinderReturnFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BinderReturnFtraceEventWire.CMD_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._cmd = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCmd(self: *const BinderReturnFtraceEventReader) u32 {
        return self._cmd;
    }
};
