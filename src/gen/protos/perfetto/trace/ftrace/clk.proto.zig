// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ClkEnableFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ClkEnableFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ClkEnableFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ClkEnableFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const ClkEnableFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ClkEnableFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(ClkEnableFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const ClkEnableFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ClkEnableFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ClkEnableFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ClkEnableFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const ClkEnableFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const ClkDisableFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ClkDisableFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ClkDisableFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ClkDisableFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const ClkDisableFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ClkDisableFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(ClkDisableFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const ClkDisableFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ClkDisableFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ClkDisableFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ClkDisableFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const ClkDisableFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const ClkSetRateFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const RATE_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const ClkSetRateFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    rate: u64 = 0,
    pub fn calcProtobufSize(self: *const ClkSetRateFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ClkSetRateFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.rate != 0) {
            res += gremlin.sizes.sizeWireNumber(ClkSetRateFtraceEventWire.RATE_WIRE) + gremlin.sizes.sizeU64(self.rate);
        }
        return res;
    }
    pub fn encode(self: *const ClkSetRateFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ClkSetRateFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(ClkSetRateFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.rate != 0) {
            target.appendUint64(ClkSetRateFtraceEventWire.RATE_WIRE, self.rate);
        }
    }
};
pub const ClkSetRateFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _rate: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ClkSetRateFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ClkSetRateFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ClkSetRateFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                ClkSetRateFtraceEventWire.RATE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._rate = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const ClkSetRateFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getRate(self: *const ClkSetRateFtraceEventReader) u64 {
        return self._rate;
    }
};
