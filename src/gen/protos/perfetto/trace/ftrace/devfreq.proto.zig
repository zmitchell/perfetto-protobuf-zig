// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DevfreqFrequencyFtraceEventWire = struct {
    const DEV_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const FREQ_WIRE: gremlin.ProtoWireNumber = 2;
    const PREV_FREQ_WIRE: gremlin.ProtoWireNumber = 3;
    const BUSY_TIME_WIRE: gremlin.ProtoWireNumber = 4;
    const TOTAL_TIME_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const DevfreqFrequencyFtraceEvent = struct {
    // fields
    dev_name: ?[]const u8 = null,
    freq: u64 = 0,
    prev_freq: u64 = 0,
    busy_time: u64 = 0,
    total_time: u64 = 0,
    pub fn calcProtobufSize(self: *const DevfreqFrequencyFtraceEvent) usize {
        var res: usize = 0;
        if (self.dev_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DevfreqFrequencyFtraceEventWire.DEV_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.freq != 0) {
            res += gremlin.sizes.sizeWireNumber(DevfreqFrequencyFtraceEventWire.FREQ_WIRE) + gremlin.sizes.sizeU64(self.freq);
        }
        if (self.prev_freq != 0) {
            res += gremlin.sizes.sizeWireNumber(DevfreqFrequencyFtraceEventWire.PREV_FREQ_WIRE) + gremlin.sizes.sizeU64(self.prev_freq);
        }
        if (self.busy_time != 0) {
            res += gremlin.sizes.sizeWireNumber(DevfreqFrequencyFtraceEventWire.BUSY_TIME_WIRE) + gremlin.sizes.sizeU64(self.busy_time);
        }
        if (self.total_time != 0) {
            res += gremlin.sizes.sizeWireNumber(DevfreqFrequencyFtraceEventWire.TOTAL_TIME_WIRE) + gremlin.sizes.sizeU64(self.total_time);
        }
        return res;
    }
    pub fn encode(self: *const DevfreqFrequencyFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DevfreqFrequencyFtraceEvent, target: *gremlin.Writer) void {
        if (self.dev_name) |v| {
            if (v.len > 0) {
                target.appendBytes(DevfreqFrequencyFtraceEventWire.DEV_NAME_WIRE, v);
            }
        }
        if (self.freq != 0) {
            target.appendUint64(DevfreqFrequencyFtraceEventWire.FREQ_WIRE, self.freq);
        }
        if (self.prev_freq != 0) {
            target.appendUint64(DevfreqFrequencyFtraceEventWire.PREV_FREQ_WIRE, self.prev_freq);
        }
        if (self.busy_time != 0) {
            target.appendUint64(DevfreqFrequencyFtraceEventWire.BUSY_TIME_WIRE, self.busy_time);
        }
        if (self.total_time != 0) {
            target.appendUint64(DevfreqFrequencyFtraceEventWire.TOTAL_TIME_WIRE, self.total_time);
        }
    }
};
pub const DevfreqFrequencyFtraceEventReader = struct {
    buf: gremlin.Reader,
    _dev_name: ?[]const u8 = null,
    _freq: u64 = 0,
    _prev_freq: u64 = 0,
    _busy_time: u64 = 0,
    _total_time: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DevfreqFrequencyFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DevfreqFrequencyFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DevfreqFrequencyFtraceEventWire.DEV_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._dev_name = result.value;
                },
                DevfreqFrequencyFtraceEventWire.FREQ_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._freq = result.value;
                },
                DevfreqFrequencyFtraceEventWire.PREV_FREQ_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._prev_freq = result.value;
                },
                DevfreqFrequencyFtraceEventWire.BUSY_TIME_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._busy_time = result.value;
                },
                DevfreqFrequencyFtraceEventWire.TOTAL_TIME_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._total_time = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDevName(self: *const DevfreqFrequencyFtraceEventReader) []const u8 {
        return self._dev_name orelse &[_]u8{};
    }
    pub inline fn getFreq(self: *const DevfreqFrequencyFtraceEventReader) u64 {
        return self._freq;
    }
    pub inline fn getPrevFreq(self: *const DevfreqFrequencyFtraceEventReader) u64 {
        return self._prev_freq;
    }
    pub inline fn getBusyTime(self: *const DevfreqFrequencyFtraceEventReader) u64 {
        return self._busy_time;
    }
    pub inline fn getTotalTime(self: *const DevfreqFrequencyFtraceEventReader) u64 {
        return self._total_time;
    }
};
