// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TaskNewtaskFtraceEventWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
    const COMM_WIRE: gremlin.ProtoWireNumber = 2;
    const CLONE_FLAGS_WIRE: gremlin.ProtoWireNumber = 3;
    const OOM_SCORE_ADJ_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const TaskNewtaskFtraceEvent = struct {
    // fields
    pid: i32 = 0,
    comm: ?[]const u8 = null,
    clone_flags: u64 = 0,
    oom_score_adj: i32 = 0,
    pub fn calcProtobufSize(self: *const TaskNewtaskFtraceEvent) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(TaskNewtaskFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TaskNewtaskFtraceEventWire.COMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.clone_flags != 0) {
            res += gremlin.sizes.sizeWireNumber(TaskNewtaskFtraceEventWire.CLONE_FLAGS_WIRE) + gremlin.sizes.sizeU64(self.clone_flags);
        }
        if (self.oom_score_adj != 0) {
            res += gremlin.sizes.sizeWireNumber(TaskNewtaskFtraceEventWire.OOM_SCORE_ADJ_WIRE) + gremlin.sizes.sizeI32(self.oom_score_adj);
        }
        return res;
    }
    pub fn encode(self: *const TaskNewtaskFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TaskNewtaskFtraceEvent, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendInt32(TaskNewtaskFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                target.appendBytes(TaskNewtaskFtraceEventWire.COMM_WIRE, v);
            }
        }
        if (self.clone_flags != 0) {
            target.appendUint64(TaskNewtaskFtraceEventWire.CLONE_FLAGS_WIRE, self.clone_flags);
        }
        if (self.oom_score_adj != 0) {
            target.appendInt32(TaskNewtaskFtraceEventWire.OOM_SCORE_ADJ_WIRE, self.oom_score_adj);
        }
    }
};
pub const TaskNewtaskFtraceEventReader = struct {
    buf: gremlin.Reader,
    _pid: i32 = 0,
    _comm: ?[]const u8 = null,
    _clone_flags: u64 = 0,
    _oom_score_adj: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!TaskNewtaskFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = TaskNewtaskFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TaskNewtaskFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                TaskNewtaskFtraceEventWire.COMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._comm = result.value;
                },
                TaskNewtaskFtraceEventWire.CLONE_FLAGS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._clone_flags = result.value;
                },
                TaskNewtaskFtraceEventWire.OOM_SCORE_ADJ_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._oom_score_adj = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const TaskNewtaskFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getComm(self: *const TaskNewtaskFtraceEventReader) []const u8 {
        return self._comm orelse &[_]u8{};
    }
    pub inline fn getCloneFlags(self: *const TaskNewtaskFtraceEventReader) u64 {
        return self._clone_flags;
    }
    pub inline fn getOomScoreAdj(self: *const TaskNewtaskFtraceEventReader) i32 {
        return self._oom_score_adj;
    }
};
const TaskRenameFtraceEventWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
    const OLDCOMM_WIRE: gremlin.ProtoWireNumber = 2;
    const NEWCOMM_WIRE: gremlin.ProtoWireNumber = 3;
    const OOM_SCORE_ADJ_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const TaskRenameFtraceEvent = struct {
    // fields
    pid: i32 = 0,
    oldcomm: ?[]const u8 = null,
    newcomm: ?[]const u8 = null,
    oom_score_adj: i32 = 0,
    pub fn calcProtobufSize(self: *const TaskRenameFtraceEvent) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(TaskRenameFtraceEventWire.PID_WIRE) + gremlin.sizes.sizeI32(self.pid);
        }
        if (self.oldcomm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TaskRenameFtraceEventWire.OLDCOMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.newcomm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TaskRenameFtraceEventWire.NEWCOMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.oom_score_adj != 0) {
            res += gremlin.sizes.sizeWireNumber(TaskRenameFtraceEventWire.OOM_SCORE_ADJ_WIRE) + gremlin.sizes.sizeI32(self.oom_score_adj);
        }
        return res;
    }
    pub fn encode(self: *const TaskRenameFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TaskRenameFtraceEvent, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendInt32(TaskRenameFtraceEventWire.PID_WIRE, self.pid);
        }
        if (self.oldcomm) |v| {
            if (v.len > 0) {
                target.appendBytes(TaskRenameFtraceEventWire.OLDCOMM_WIRE, v);
            }
        }
        if (self.newcomm) |v| {
            if (v.len > 0) {
                target.appendBytes(TaskRenameFtraceEventWire.NEWCOMM_WIRE, v);
            }
        }
        if (self.oom_score_adj != 0) {
            target.appendInt32(TaskRenameFtraceEventWire.OOM_SCORE_ADJ_WIRE, self.oom_score_adj);
        }
    }
};
pub const TaskRenameFtraceEventReader = struct {
    buf: gremlin.Reader,
    _pid: i32 = 0,
    _oldcomm: ?[]const u8 = null,
    _newcomm: ?[]const u8 = null,
    _oom_score_adj: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!TaskRenameFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = TaskRenameFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TaskRenameFtraceEventWire.PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                TaskRenameFtraceEventWire.OLDCOMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._oldcomm = result.value;
                },
                TaskRenameFtraceEventWire.NEWCOMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._newcomm = result.value;
                },
                TaskRenameFtraceEventWire.OOM_SCORE_ADJ_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._oom_score_adj = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const TaskRenameFtraceEventReader) i32 {
        return self._pid;
    }
    pub inline fn getOldcomm(self: *const TaskRenameFtraceEventReader) []const u8 {
        return self._oldcomm orelse &[_]u8{};
    }
    pub inline fn getNewcomm(self: *const TaskRenameFtraceEventReader) []const u8 {
        return self._newcomm orelse &[_]u8{};
    }
    pub inline fn getOomScoreAdj(self: *const TaskRenameFtraceEventReader) i32 {
        return self._oom_score_adj;
    }
};
