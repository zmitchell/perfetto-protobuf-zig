// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DrmVblankEventFtraceEventWire = struct {
    const CRTC_WIRE: gremlin.ProtoWireNumber = 1;
    const HIGH_PREC_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQ_WIRE: gremlin.ProtoWireNumber = 3;
    const TIME_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const DrmVblankEventFtraceEvent = struct {
    // fields
    crtc: i32 = 0,
    high_prec: u32 = 0,
    seq: u32 = 0,
    time: i64 = 0,
    pub fn calcProtobufSize(self: *const DrmVblankEventFtraceEvent) usize {
        var res: usize = 0;
        if (self.crtc != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmVblankEventFtraceEventWire.CRTC_WIRE) + gremlin.sizes.sizeI32(self.crtc);
        }
        if (self.high_prec != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmVblankEventFtraceEventWire.HIGH_PREC_WIRE) + gremlin.sizes.sizeU32(self.high_prec);
        }
        if (self.seq != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmVblankEventFtraceEventWire.SEQ_WIRE) + gremlin.sizes.sizeU32(self.seq);
        }
        if (self.time != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmVblankEventFtraceEventWire.TIME_WIRE) + gremlin.sizes.sizeI64(self.time);
        }
        return res;
    }
    pub fn encode(self: *const DrmVblankEventFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmVblankEventFtraceEvent, target: *gremlin.Writer) void {
        if (self.crtc != 0) {
            target.appendInt32(DrmVblankEventFtraceEventWire.CRTC_WIRE, self.crtc);
        }
        if (self.high_prec != 0) {
            target.appendUint32(DrmVblankEventFtraceEventWire.HIGH_PREC_WIRE, self.high_prec);
        }
        if (self.seq != 0) {
            target.appendUint32(DrmVblankEventFtraceEventWire.SEQ_WIRE, self.seq);
        }
        if (self.time != 0) {
            target.appendInt64(DrmVblankEventFtraceEventWire.TIME_WIRE, self.time);
        }
    }
};
pub const DrmVblankEventFtraceEventReader = struct {
    buf: gremlin.Reader,
    _crtc: i32 = 0,
    _high_prec: u32 = 0,
    _seq: u32 = 0,
    _time: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!DrmVblankEventFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmVblankEventFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmVblankEventFtraceEventWire.CRTC_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._crtc = result.value;
                },
                DrmVblankEventFtraceEventWire.HIGH_PREC_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._high_prec = result.value;
                },
                DrmVblankEventFtraceEventWire.SEQ_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seq = result.value;
                },
                DrmVblankEventFtraceEventWire.TIME_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._time = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCrtc(self: *const DrmVblankEventFtraceEventReader) i32 {
        return self._crtc;
    }
    pub inline fn getHighPrec(self: *const DrmVblankEventFtraceEventReader) u32 {
        return self._high_prec;
    }
    pub inline fn getSeq(self: *const DrmVblankEventFtraceEventReader) u32 {
        return self._seq;
    }
    pub inline fn getTime(self: *const DrmVblankEventFtraceEventReader) i64 {
        return self._time;
    }
};
const DrmVblankEventDeliveredFtraceEventWire = struct {
    const CRTC_WIRE: gremlin.ProtoWireNumber = 1;
    const FILE_WIRE: gremlin.ProtoWireNumber = 2;
    const SEQ_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const DrmVblankEventDeliveredFtraceEvent = struct {
    // fields
    crtc: i32 = 0,
    file: u64 = 0,
    seq: u32 = 0,
    pub fn calcProtobufSize(self: *const DrmVblankEventDeliveredFtraceEvent) usize {
        var res: usize = 0;
        if (self.crtc != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmVblankEventDeliveredFtraceEventWire.CRTC_WIRE) + gremlin.sizes.sizeI32(self.crtc);
        }
        if (self.file != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmVblankEventDeliveredFtraceEventWire.FILE_WIRE) + gremlin.sizes.sizeU64(self.file);
        }
        if (self.seq != 0) {
            res += gremlin.sizes.sizeWireNumber(DrmVblankEventDeliveredFtraceEventWire.SEQ_WIRE) + gremlin.sizes.sizeU32(self.seq);
        }
        return res;
    }
    pub fn encode(self: *const DrmVblankEventDeliveredFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DrmVblankEventDeliveredFtraceEvent, target: *gremlin.Writer) void {
        if (self.crtc != 0) {
            target.appendInt32(DrmVblankEventDeliveredFtraceEventWire.CRTC_WIRE, self.crtc);
        }
        if (self.file != 0) {
            target.appendUint64(DrmVblankEventDeliveredFtraceEventWire.FILE_WIRE, self.file);
        }
        if (self.seq != 0) {
            target.appendUint32(DrmVblankEventDeliveredFtraceEventWire.SEQ_WIRE, self.seq);
        }
    }
};
pub const DrmVblankEventDeliveredFtraceEventReader = struct {
    buf: gremlin.Reader,
    _crtc: i32 = 0,
    _file: u64 = 0,
    _seq: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DrmVblankEventDeliveredFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DrmVblankEventDeliveredFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DrmVblankEventDeliveredFtraceEventWire.CRTC_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._crtc = result.value;
                },
                DrmVblankEventDeliveredFtraceEventWire.FILE_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._file = result.value;
                },
                DrmVblankEventDeliveredFtraceEventWire.SEQ_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._seq = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCrtc(self: *const DrmVblankEventDeliveredFtraceEventReader) i32 {
        return self._crtc;
    }
    pub inline fn getFile(self: *const DrmVblankEventDeliveredFtraceEventReader) u64 {
        return self._file;
    }
    pub inline fn getSeq(self: *const DrmVblankEventDeliveredFtraceEventReader) u32 {
        return self._seq;
    }
};
