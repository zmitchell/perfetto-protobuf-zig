// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const RpmStatusFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const STATUS_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const RpmStatusFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    status: i32 = 0,
    pub fn calcProtobufSize(self: *const RpmStatusFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RpmStatusFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.status != 0) {
            res += gremlin.sizes.sizeWireNumber(RpmStatusFtraceEventWire.STATUS_WIRE) + gremlin.sizes.sizeI32(self.status);
        }
        return res;
    }
    pub fn encode(self: *const RpmStatusFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RpmStatusFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(RpmStatusFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.status != 0) {
            target.appendInt32(RpmStatusFtraceEventWire.STATUS_WIRE, self.status);
        }
    }
};
pub const RpmStatusFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _status: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!RpmStatusFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RpmStatusFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RpmStatusFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                RpmStatusFtraceEventWire.STATUS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._status = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const RpmStatusFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getStatus(self: *const RpmStatusFtraceEventReader) i32 {
        return self._status;
    }
};
