// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const MmEventRecordFtraceEventWire = struct {
    const AVG_LAT_WIRE: gremlin.ProtoWireNumber = 1;
    const COUNT_WIRE: gremlin.ProtoWireNumber = 2;
    const MAX_LAT_WIRE: gremlin.ProtoWireNumber = 3;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const MmEventRecordFtraceEvent = struct {
    // fields
    avg_lat: u32 = 0,
    count: u32 = 0,
    max_lat: u32 = 0,
    type: u32 = 0,
    pub fn calcProtobufSize(self: *const MmEventRecordFtraceEvent) usize {
        var res: usize = 0;
        if (self.avg_lat != 0) {
            res += gremlin.sizes.sizeWireNumber(MmEventRecordFtraceEventWire.AVG_LAT_WIRE) + gremlin.sizes.sizeU32(self.avg_lat);
        }
        if (self.count != 0) {
            res += gremlin.sizes.sizeWireNumber(MmEventRecordFtraceEventWire.COUNT_WIRE) + gremlin.sizes.sizeU32(self.count);
        }
        if (self.max_lat != 0) {
            res += gremlin.sizes.sizeWireNumber(MmEventRecordFtraceEventWire.MAX_LAT_WIRE) + gremlin.sizes.sizeU32(self.max_lat);
        }
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(MmEventRecordFtraceEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        return res;
    }
    pub fn encode(self: *const MmEventRecordFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const MmEventRecordFtraceEvent, target: *gremlin.Writer) void {
        if (self.avg_lat != 0) {
            target.appendUint32(MmEventRecordFtraceEventWire.AVG_LAT_WIRE, self.avg_lat);
        }
        if (self.count != 0) {
            target.appendUint32(MmEventRecordFtraceEventWire.COUNT_WIRE, self.count);
        }
        if (self.max_lat != 0) {
            target.appendUint32(MmEventRecordFtraceEventWire.MAX_LAT_WIRE, self.max_lat);
        }
        if (self.type != 0) {
            target.appendUint32(MmEventRecordFtraceEventWire.TYPE_WIRE, self.type);
        }
    }
};
pub const MmEventRecordFtraceEventReader = struct {
    buf: gremlin.Reader,
    _avg_lat: u32 = 0,
    _count: u32 = 0,
    _max_lat: u32 = 0,
    _type: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!MmEventRecordFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = MmEventRecordFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                MmEventRecordFtraceEventWire.AVG_LAT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._avg_lat = result.value;
                },
                MmEventRecordFtraceEventWire.COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._count = result.value;
                },
                MmEventRecordFtraceEventWire.MAX_LAT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._max_lat = result.value;
                },
                MmEventRecordFtraceEventWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getAvgLat(self: *const MmEventRecordFtraceEventReader) u32 {
        return self._avg_lat;
    }
    pub inline fn getCount(self: *const MmEventRecordFtraceEventReader) u32 {
        return self._count;
    }
    pub inline fn getMaxLat(self: *const MmEventRecordFtraceEventReader) u32 {
        return self._max_lat;
    }
    pub inline fn getType(self: *const MmEventRecordFtraceEventReader) u32 {
        return self._type;
    }
};
