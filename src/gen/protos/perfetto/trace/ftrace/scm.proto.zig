// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ScmCallStartFtraceEventWire = struct {
    const ARGINFO_WIRE: gremlin.ProtoWireNumber = 1;
    const X0_WIRE: gremlin.ProtoWireNumber = 2;
    const X5_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ScmCallStartFtraceEvent = struct {
    // fields
    arginfo: u32 = 0,
    x0: u64 = 0,
    x5: u64 = 0,
    pub fn calcProtobufSize(self: *const ScmCallStartFtraceEvent) usize {
        var res: usize = 0;
        if (self.arginfo != 0) {
            res += gremlin.sizes.sizeWireNumber(ScmCallStartFtraceEventWire.ARGINFO_WIRE) + gremlin.sizes.sizeU32(self.arginfo);
        }
        if (self.x0 != 0) {
            res += gremlin.sizes.sizeWireNumber(ScmCallStartFtraceEventWire.X0_WIRE) + gremlin.sizes.sizeU64(self.x0);
        }
        if (self.x5 != 0) {
            res += gremlin.sizes.sizeWireNumber(ScmCallStartFtraceEventWire.X5_WIRE) + gremlin.sizes.sizeU64(self.x5);
        }
        return res;
    }
    pub fn encode(self: *const ScmCallStartFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ScmCallStartFtraceEvent, target: *gremlin.Writer) void {
        if (self.arginfo != 0) {
            target.appendUint32(ScmCallStartFtraceEventWire.ARGINFO_WIRE, self.arginfo);
        }
        if (self.x0 != 0) {
            target.appendUint64(ScmCallStartFtraceEventWire.X0_WIRE, self.x0);
        }
        if (self.x5 != 0) {
            target.appendUint64(ScmCallStartFtraceEventWire.X5_WIRE, self.x5);
        }
    }
};
pub const ScmCallStartFtraceEventReader = struct {
    buf: gremlin.Reader,
    _arginfo: u32 = 0,
    _x0: u64 = 0,
    _x5: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!ScmCallStartFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = ScmCallStartFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ScmCallStartFtraceEventWire.ARGINFO_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._arginfo = result.value;
                },
                ScmCallStartFtraceEventWire.X0_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._x0 = result.value;
                },
                ScmCallStartFtraceEventWire.X5_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._x5 = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getArginfo(self: *const ScmCallStartFtraceEventReader) u32 {
        return self._arginfo;
    }
    pub inline fn getX0(self: *const ScmCallStartFtraceEventReader) u64 {
        return self._x0;
    }
    pub inline fn getX5(self: *const ScmCallStartFtraceEventReader) u64 {
        return self._x5;
    }
};
pub const ScmCallEndFtraceEvent = struct {
    pub fn calcProtobufSize(_: *const ScmCallEndFtraceEvent) usize {
        return 0;
    }
    pub fn encode(self: *const ScmCallEndFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const ScmCallEndFtraceEvent, _: *gremlin.Writer) void {}
};
pub const ScmCallEndFtraceEventReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!ScmCallEndFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        return ScmCallEndFtraceEventReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
