// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const RegulatorDisableFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const RegulatorDisableFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RegulatorDisableFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RegulatorDisableFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const RegulatorDisableFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegulatorDisableFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(RegulatorDisableFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const RegulatorDisableFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RegulatorDisableFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RegulatorDisableFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegulatorDisableFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const RegulatorDisableFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const RegulatorDisableCompleteFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const RegulatorDisableCompleteFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RegulatorDisableCompleteFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RegulatorDisableCompleteFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const RegulatorDisableCompleteFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegulatorDisableCompleteFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(RegulatorDisableCompleteFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const RegulatorDisableCompleteFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RegulatorDisableCompleteFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RegulatorDisableCompleteFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegulatorDisableCompleteFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const RegulatorDisableCompleteFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const RegulatorEnableFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const RegulatorEnableFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RegulatorEnableFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RegulatorEnableFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const RegulatorEnableFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegulatorEnableFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(RegulatorEnableFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const RegulatorEnableFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RegulatorEnableFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RegulatorEnableFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegulatorEnableFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const RegulatorEnableFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const RegulatorEnableCompleteFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const RegulatorEnableCompleteFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RegulatorEnableCompleteFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RegulatorEnableCompleteFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const RegulatorEnableCompleteFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegulatorEnableCompleteFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(RegulatorEnableCompleteFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const RegulatorEnableCompleteFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RegulatorEnableCompleteFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RegulatorEnableCompleteFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegulatorEnableCompleteFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const RegulatorEnableCompleteFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const RegulatorEnableDelayFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const RegulatorEnableDelayFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RegulatorEnableDelayFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RegulatorEnableDelayFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const RegulatorEnableDelayFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegulatorEnableDelayFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(RegulatorEnableDelayFtraceEventWire.NAME_WIRE, v);
            }
        }
    }
};
pub const RegulatorEnableDelayFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RegulatorEnableDelayFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RegulatorEnableDelayFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegulatorEnableDelayFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const RegulatorEnableDelayFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const RegulatorSetVoltageFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const MIN_WIRE: gremlin.ProtoWireNumber = 2;
    const MAX_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const RegulatorSetVoltageFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    min: i32 = 0,
    max: i32 = 0,
    pub fn calcProtobufSize(self: *const RegulatorSetVoltageFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RegulatorSetVoltageFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.min != 0) {
            res += gremlin.sizes.sizeWireNumber(RegulatorSetVoltageFtraceEventWire.MIN_WIRE) + gremlin.sizes.sizeI32(self.min);
        }
        if (self.max != 0) {
            res += gremlin.sizes.sizeWireNumber(RegulatorSetVoltageFtraceEventWire.MAX_WIRE) + gremlin.sizes.sizeI32(self.max);
        }
        return res;
    }
    pub fn encode(self: *const RegulatorSetVoltageFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegulatorSetVoltageFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(RegulatorSetVoltageFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.min != 0) {
            target.appendInt32(RegulatorSetVoltageFtraceEventWire.MIN_WIRE, self.min);
        }
        if (self.max != 0) {
            target.appendInt32(RegulatorSetVoltageFtraceEventWire.MAX_WIRE, self.max);
        }
    }
};
pub const RegulatorSetVoltageFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _min: i32 = 0,
    _max: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!RegulatorSetVoltageFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RegulatorSetVoltageFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegulatorSetVoltageFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                RegulatorSetVoltageFtraceEventWire.MIN_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._min = result.value;
                },
                RegulatorSetVoltageFtraceEventWire.MAX_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._max = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const RegulatorSetVoltageFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getMin(self: *const RegulatorSetVoltageFtraceEventReader) i32 {
        return self._min;
    }
    pub inline fn getMax(self: *const RegulatorSetVoltageFtraceEventReader) i32 {
        return self._max;
    }
};
const RegulatorSetVoltageCompleteFtraceEventWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const VAL_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const RegulatorSetVoltageCompleteFtraceEvent = struct {
    // fields
    name: ?[]const u8 = null,
    val: u32 = 0,
    pub fn calcProtobufSize(self: *const RegulatorSetVoltageCompleteFtraceEvent) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RegulatorSetVoltageCompleteFtraceEventWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.val != 0) {
            res += gremlin.sizes.sizeWireNumber(RegulatorSetVoltageCompleteFtraceEventWire.VAL_WIRE) + gremlin.sizes.sizeU32(self.val);
        }
        return res;
    }
    pub fn encode(self: *const RegulatorSetVoltageCompleteFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegulatorSetVoltageCompleteFtraceEvent, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(RegulatorSetVoltageCompleteFtraceEventWire.NAME_WIRE, v);
            }
        }
        if (self.val != 0) {
            target.appendUint32(RegulatorSetVoltageCompleteFtraceEventWire.VAL_WIRE, self.val);
        }
    }
};
pub const RegulatorSetVoltageCompleteFtraceEventReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _val: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!RegulatorSetVoltageCompleteFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RegulatorSetVoltageCompleteFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegulatorSetVoltageCompleteFtraceEventWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                RegulatorSetVoltageCompleteFtraceEventWire.VAL_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._val = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const RegulatorSetVoltageCompleteFtraceEventReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getVal(self: *const RegulatorSetVoltageCompleteFtraceEventReader) u32 {
        return self._val;
    }
};
