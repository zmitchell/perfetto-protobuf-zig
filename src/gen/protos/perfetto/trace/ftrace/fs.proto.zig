// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const DoSysOpenFtraceEventWire = struct {
    const FILENAME_WIRE: gremlin.ProtoWireNumber = 1;
    const FLAGS_WIRE: gremlin.ProtoWireNumber = 2;
    const MODE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const DoSysOpenFtraceEvent = struct {
    // fields
    filename: ?[]const u8 = null,
    flags: i32 = 0,
    mode: i32 = 0,
    pub fn calcProtobufSize(self: *const DoSysOpenFtraceEvent) usize {
        var res: usize = 0;
        if (self.filename) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DoSysOpenFtraceEventWire.FILENAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.flags != 0) {
            res += gremlin.sizes.sizeWireNumber(DoSysOpenFtraceEventWire.FLAGS_WIRE) + gremlin.sizes.sizeI32(self.flags);
        }
        if (self.mode != 0) {
            res += gremlin.sizes.sizeWireNumber(DoSysOpenFtraceEventWire.MODE_WIRE) + gremlin.sizes.sizeI32(self.mode);
        }
        return res;
    }
    pub fn encode(self: *const DoSysOpenFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DoSysOpenFtraceEvent, target: *gremlin.Writer) void {
        if (self.filename) |v| {
            if (v.len > 0) {
                target.appendBytes(DoSysOpenFtraceEventWire.FILENAME_WIRE, v);
            }
        }
        if (self.flags != 0) {
            target.appendInt32(DoSysOpenFtraceEventWire.FLAGS_WIRE, self.flags);
        }
        if (self.mode != 0) {
            target.appendInt32(DoSysOpenFtraceEventWire.MODE_WIRE, self.mode);
        }
    }
};
pub const DoSysOpenFtraceEventReader = struct {
    buf: gremlin.Reader,
    _filename: ?[]const u8 = null,
    _flags: i32 = 0,
    _mode: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!DoSysOpenFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = DoSysOpenFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DoSysOpenFtraceEventWire.FILENAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._filename = result.value;
                },
                DoSysOpenFtraceEventWire.FLAGS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._flags = result.value;
                },
                DoSysOpenFtraceEventWire.MODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._mode = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFilename(self: *const DoSysOpenFtraceEventReader) []const u8 {
        return self._filename orelse &[_]u8{};
    }
    pub inline fn getFlags(self: *const DoSysOpenFtraceEventReader) i32 {
        return self._flags;
    }
    pub inline fn getMode(self: *const DoSysOpenFtraceEventReader) i32 {
        return self._mode;
    }
};
const OpenExecFtraceEventWire = struct {
    const FILENAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const OpenExecFtraceEvent = struct {
    // fields
    filename: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const OpenExecFtraceEvent) usize {
        var res: usize = 0;
        if (self.filename) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(OpenExecFtraceEventWire.FILENAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const OpenExecFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const OpenExecFtraceEvent, target: *gremlin.Writer) void {
        if (self.filename) |v| {
            if (v.len > 0) {
                target.appendBytes(OpenExecFtraceEventWire.FILENAME_WIRE, v);
            }
        }
    }
};
pub const OpenExecFtraceEventReader = struct {
    buf: gremlin.Reader,
    _filename: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!OpenExecFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = OpenExecFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                OpenExecFtraceEventWire.FILENAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._filename = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFilename(self: *const OpenExecFtraceEventReader) []const u8 {
        return self._filename orelse &[_]u8{};
    }
};
