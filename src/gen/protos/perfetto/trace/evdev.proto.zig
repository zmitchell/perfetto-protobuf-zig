// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const EvdevEventWire = struct {
    const DEVICE_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const INPUT_EVENT_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const EvdevEvent = struct {
    // nested structs
    const InputEventWire = struct {
        const KERNEL_TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 1;
        const TYPE_WIRE: gremlin.ProtoWireNumber = 2;
        const CODE_WIRE: gremlin.ProtoWireNumber = 3;
        const VALUE_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const InputEvent = struct {
        // fields
        kernel_timestamp: u64 = 0,
        type: u32 = 0,
        code: u32 = 0,
        value: i32 = 0,
        pub fn calcProtobufSize(self: *const EvdevEvent.InputEvent) usize {
            var res: usize = 0;
            if (self.kernel_timestamp != 0) {
                res += gremlin.sizes.sizeWireNumber(EvdevEvent.InputEventWire.KERNEL_TIMESTAMP_WIRE) + gremlin.sizes.sizeU64(self.kernel_timestamp);
            }
            if (self.type != 0) {
                res += gremlin.sizes.sizeWireNumber(EvdevEvent.InputEventWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
            }
            if (self.code != 0) {
                res += gremlin.sizes.sizeWireNumber(EvdevEvent.InputEventWire.CODE_WIRE) + gremlin.sizes.sizeU32(self.code);
            }
            if (self.value != 0) {
                res += gremlin.sizes.sizeWireNumber(EvdevEvent.InputEventWire.VALUE_WIRE) + gremlin.sizes.sizeSI32(self.value);
            }
            return res;
        }
        pub fn encode(self: *const EvdevEvent.InputEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const EvdevEvent.InputEvent, target: *gremlin.Writer) void {
            if (self.kernel_timestamp != 0) {
                target.appendUint64(EvdevEvent.InputEventWire.KERNEL_TIMESTAMP_WIRE, self.kernel_timestamp);
            }
            if (self.type != 0) {
                target.appendUint32(EvdevEvent.InputEventWire.TYPE_WIRE, self.type);
            }
            if (self.code != 0) {
                target.appendUint32(EvdevEvent.InputEventWire.CODE_WIRE, self.code);
            }
            if (self.value != 0) {
                target.appendSint32(EvdevEvent.InputEventWire.VALUE_WIRE, self.value);
            }
        }
    };
    pub const InputEventReader = struct {
        buf: gremlin.Reader,
        _kernel_timestamp: u64 = 0,
        _type: u32 = 0,
        _code: u32 = 0,
        _value: i32 = 0,
        pub fn init(src: []const u8) gremlin.Error!EvdevEvent.InputEventReader {
            const buf = gremlin.Reader.init(src);
            var res = EvdevEvent.InputEventReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    EvdevEvent.InputEventWire.KERNEL_TIMESTAMP_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._kernel_timestamp = result.value;
                    },
                    EvdevEvent.InputEventWire.TYPE_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._type = result.value;
                    },
                    EvdevEvent.InputEventWire.CODE_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._code = result.value;
                    },
                    EvdevEvent.InputEventWire.VALUE_WIRE => {
                        const result = try buf.readSInt32(offset);
                        offset += result.size;
                        res._value = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getKernelTimestamp(self: *const EvdevEvent.InputEventReader) u64 {
            return self._kernel_timestamp;
        }
        pub inline fn getType(self: *const EvdevEvent.InputEventReader) u32 {
            return self._type;
        }
        pub inline fn getCode(self: *const EvdevEvent.InputEventReader) u32 {
            return self._code;
        }
        pub inline fn getValue(self: *const EvdevEvent.InputEventReader) i32 {
            return self._value;
        }
    };
    // fields
    device_id: u32 = 0,
    input_event: ?EvdevEvent.InputEvent = null,
    pub fn calcProtobufSize(self: *const EvdevEvent) usize {
        var res: usize = 0;
        if (self.device_id != 0) {
            res += gremlin.sizes.sizeWireNumber(EvdevEventWire.DEVICE_ID_WIRE) + gremlin.sizes.sizeU32(self.device_id);
        }
        if (self.input_event) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(EvdevEventWire.INPUT_EVENT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const EvdevEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const EvdevEvent, target: *gremlin.Writer) void {
        if (self.device_id != 0) {
            target.appendUint32(EvdevEventWire.DEVICE_ID_WIRE, self.device_id);
        }
        if (self.input_event) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(EvdevEventWire.INPUT_EVENT_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const EvdevEventReader = struct {
    buf: gremlin.Reader,
    _device_id: u32 = 0,
    _input_event_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!EvdevEventReader {
        const buf = gremlin.Reader.init(src);
        var res = EvdevEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                EvdevEventWire.DEVICE_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._device_id = result.value;
                },
                EvdevEventWire.INPUT_EVENT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._input_event_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDeviceId(self: *const EvdevEventReader) u32 {
        return self._device_id;
    }
    pub fn getInputEvent(self: *const EvdevEventReader) gremlin.Error!EvdevEvent.InputEventReader {
        if (self._input_event_buf) |buf| {
            return try EvdevEvent.InputEventReader.init(buf);
        }
        return try EvdevEvent.InputEventReader.init(&[_]u8{});
    }
};
