// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TracingServiceEventWire = struct {
    const TRACING_STARTED_WIRE: gremlin.ProtoWireNumber = 2;
    const ALL_DATA_SOURCES_STARTED_WIRE: gremlin.ProtoWireNumber = 1;
    const FLUSH_STARTED_WIRE: gremlin.ProtoWireNumber = 9;
    const ALL_DATA_SOURCES_FLUSHED_WIRE: gremlin.ProtoWireNumber = 3;
    const READ_TRACING_BUFFERS_COMPLETED_WIRE: gremlin.ProtoWireNumber = 4;
    const TRACING_DISABLED_WIRE: gremlin.ProtoWireNumber = 5;
    const SEIZED_FOR_BUGREPORT_WIRE: gremlin.ProtoWireNumber = 6;
    const SLOW_STARTING_DATA_SOURCES_WIRE: gremlin.ProtoWireNumber = 7;
    const LAST_FLUSH_SLOW_DATA_SOURCES_WIRE: gremlin.ProtoWireNumber = 8;
    const CLONE_STARTED_WIRE: gremlin.ProtoWireNumber = 10;
    const BUFFER_CLONED_WIRE: gremlin.ProtoWireNumber = 11;
};
pub const TracingServiceEvent = struct {
    // nested structs
    const DataSourcesWire = struct {
        const DATA_SOURCE_WIRE: gremlin.ProtoWireNumber = 1;
    };
    pub const DataSources = struct {
        // nested structs
        const DataSourceWire = struct {
            const PRODUCER_NAME_WIRE: gremlin.ProtoWireNumber = 1;
            const DATA_SOURCE_NAME_WIRE: gremlin.ProtoWireNumber = 2;
        };
        pub const DataSource = struct {
            // fields
            producer_name: ?[]const u8 = null,
            data_source_name: ?[]const u8 = null,
            pub fn calcProtobufSize(self: *const TracingServiceEvent.DataSources.DataSource) usize {
                var res: usize = 0;
                if (self.producer_name) |v| {
                    if (v.len > 0) {
                        res += gremlin.sizes.sizeWireNumber(TracingServiceEvent.DataSources.DataSourceWire.PRODUCER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                    }
                }
                if (self.data_source_name) |v| {
                    if (v.len > 0) {
                        res += gremlin.sizes.sizeWireNumber(TracingServiceEvent.DataSources.DataSourceWire.DATA_SOURCE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                    }
                }
                return res;
            }
            pub fn encode(self: *const TracingServiceEvent.DataSources.DataSource, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
                const size = self.calcProtobufSize();
                if (size == 0) {
                    return &[_]u8{};
                }
                const buf = try allocator.alloc(u8, self.calcProtobufSize());
                var writer = gremlin.Writer.init(buf);
                self.encodeTo(&writer);
                return buf;
            }
            pub fn encodeTo(self: *const TracingServiceEvent.DataSources.DataSource, target: *gremlin.Writer) void {
                if (self.producer_name) |v| {
                    if (v.len > 0) {
                        target.appendBytes(TracingServiceEvent.DataSources.DataSourceWire.PRODUCER_NAME_WIRE, v);
                    }
                }
                if (self.data_source_name) |v| {
                    if (v.len > 0) {
                        target.appendBytes(TracingServiceEvent.DataSources.DataSourceWire.DATA_SOURCE_NAME_WIRE, v);
                    }
                }
            }
        };
        pub const DataSourceReader = struct {
            buf: gremlin.Reader,
            _producer_name: ?[]const u8 = null,
            _data_source_name: ?[]const u8 = null,
            pub fn init(src: []const u8) gremlin.Error!TracingServiceEvent.DataSources.DataSourceReader {
                const buf = gremlin.Reader.init(src);
                var res = TracingServiceEvent.DataSources.DataSourceReader{ .buf = buf };
                if (buf.buf.len == 0) {
                    return res;
                }
                var offset: usize = 0;
                while (buf.hasNext(offset, 0)) {
                    const tag = try buf.readTagAt(offset);
                    offset += tag.size;
                    switch (tag.number) {
                        TracingServiceEvent.DataSources.DataSourceWire.PRODUCER_NAME_WIRE => {
                            const result = try buf.readBytes(offset);
                            offset += result.size;
                            res._producer_name = result.value;
                        },
                        TracingServiceEvent.DataSources.DataSourceWire.DATA_SOURCE_NAME_WIRE => {
                            const result = try buf.readBytes(offset);
                            offset += result.size;
                            res._data_source_name = result.value;
                        },
                        else => {
                            offset = try buf.skipData(offset, tag.wire);
                        },
                    }
                }
                return res;
            }
            pub fn sourceBytes(self: *const @This()) []const u8 {
                return self.buf.buf;
            }
            pub inline fn getProducerName(self: *const TracingServiceEvent.DataSources.DataSourceReader) []const u8 {
                return self._producer_name orelse &[_]u8{};
            }
            pub inline fn getDataSourceName(self: *const TracingServiceEvent.DataSources.DataSourceReader) []const u8 {
                return self._data_source_name orelse &[_]u8{};
            }
        };
        // fields
        data_source: ?[]const ?TracingServiceEvent.DataSources.DataSource = null,
        pub fn calcProtobufSize(self: *const TracingServiceEvent.DataSources) usize {
            var res: usize = 0;
            if (self.data_source) |arr| {
                for (arr) |maybe_v| {
                    res += gremlin.sizes.sizeWireNumber(TracingServiceEvent.DataSourcesWire.DATA_SOURCE_WIRE);
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        res += gremlin.sizes.sizeUsize(size) + size;
                    } else {
                        res += gremlin.sizes.sizeUsize(0);
                    }
                }
            }
            return res;
        }
        pub fn encode(self: *const TracingServiceEvent.DataSources, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const TracingServiceEvent.DataSources, target: *gremlin.Writer) void {
            if (self.data_source) |arr| {
                for (arr) |maybe_v| {
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        target.appendBytesTag(TracingServiceEvent.DataSourcesWire.DATA_SOURCE_WIRE, size);
                        v.encodeTo(target);
                    } else {
                        target.appendBytesTag(TracingServiceEvent.DataSourcesWire.DATA_SOURCE_WIRE, 0);
                    }
                }
            }
        }
    };
    pub const DataSourcesReader = struct {
        buf: gremlin.Reader,
        _data_source_offset: ?usize = null,
        _data_source_last_offset: ?usize = null,
        _data_source_cnt: usize = 0,
        pub fn init(src: []const u8) gremlin.Error!TracingServiceEvent.DataSourcesReader {
            const buf = gremlin.Reader.init(src);
            var res = TracingServiceEvent.DataSourcesReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    TracingServiceEvent.DataSourcesWire.DATA_SOURCE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        if (res._data_source_offset == null) {
                            res._data_source_offset = offset - result.size;
                        }
                        res._data_source_last_offset = offset;
                        res._data_source_cnt += 1;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub fn dataSourceCount(self: *const TracingServiceEvent.DataSourcesReader) usize {
            return self._data_source_cnt;
        }
        pub fn dataSourceNext(self: *TracingServiceEvent.DataSourcesReader) ?TracingServiceEvent.DataSources.DataSourceReader {
            if (self._data_source_offset == null) return null;
            const current_offset = self._data_source_offset.?;
            const result = self.buf.readBytes(current_offset) catch return null;
            const msg = TracingServiceEvent.DataSources.DataSourceReader.init(result.value) catch return null;
            if (self._data_source_last_offset != null and current_offset >= self._data_source_last_offset.?) {
                self._data_source_offset = null;
                return msg;
            }
            if (self._data_source_last_offset == null) unreachable;
            var next_offset = current_offset + result.size;
            const max_offset = self._data_source_last_offset.?;
            while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
                const tag = self.buf.readTagAt(next_offset) catch break;
                next_offset += tag.size;
                if (tag.number == TracingServiceEvent.DataSourcesWire.DATA_SOURCE_WIRE) {
                    self._data_source_offset = next_offset;
                    return msg;
                } else {
                    next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
                }
            }
            self._data_source_offset = null;
            return msg;
        }
    };
    // fields
    tracing_started: bool = false,
    all_data_sources_started: bool = false,
    flush_started: bool = false,
    all_data_sources_flushed: bool = false,
    read_tracing_buffers_completed: bool = false,
    tracing_disabled: bool = false,
    seized_for_bugreport: bool = false,
    slow_starting_data_sources: ?TracingServiceEvent.DataSources = null,
    last_flush_slow_data_sources: ?TracingServiceEvent.DataSources = null,
    clone_started: bool = false,
    buffer_cloned: u32 = 0,
    pub fn calcProtobufSize(self: *const TracingServiceEvent) usize {
        var res: usize = 0;
        if (self.tracing_started != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.TRACING_STARTED_WIRE) + gremlin.sizes.sizeBool(self.tracing_started);
        }
        if (self.all_data_sources_started != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.ALL_DATA_SOURCES_STARTED_WIRE) + gremlin.sizes.sizeBool(self.all_data_sources_started);
        }
        if (self.flush_started != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.FLUSH_STARTED_WIRE) + gremlin.sizes.sizeBool(self.flush_started);
        }
        if (self.all_data_sources_flushed != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.ALL_DATA_SOURCES_FLUSHED_WIRE) + gremlin.sizes.sizeBool(self.all_data_sources_flushed);
        }
        if (self.read_tracing_buffers_completed != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.READ_TRACING_BUFFERS_COMPLETED_WIRE) + gremlin.sizes.sizeBool(self.read_tracing_buffers_completed);
        }
        if (self.tracing_disabled != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.TRACING_DISABLED_WIRE) + gremlin.sizes.sizeBool(self.tracing_disabled);
        }
        if (self.seized_for_bugreport != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.SEIZED_FOR_BUGREPORT_WIRE) + gremlin.sizes.sizeBool(self.seized_for_bugreport);
        }
        if (self.slow_starting_data_sources) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.SLOW_STARTING_DATA_SOURCES_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.last_flush_slow_data_sources) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.LAST_FLUSH_SLOW_DATA_SOURCES_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.clone_started != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.CLONE_STARTED_WIRE) + gremlin.sizes.sizeBool(self.clone_started);
        }
        if (self.buffer_cloned != 0) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceEventWire.BUFFER_CLONED_WIRE) + gremlin.sizes.sizeU32(self.buffer_cloned);
        }
        return res;
    }
    pub fn encode(self: *const TracingServiceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TracingServiceEvent, target: *gremlin.Writer) void {
        if (self.tracing_started != false) {
            target.appendBool(TracingServiceEventWire.TRACING_STARTED_WIRE, self.tracing_started);
        }
        if (self.all_data_sources_started != false) {
            target.appendBool(TracingServiceEventWire.ALL_DATA_SOURCES_STARTED_WIRE, self.all_data_sources_started);
        }
        if (self.flush_started != false) {
            target.appendBool(TracingServiceEventWire.FLUSH_STARTED_WIRE, self.flush_started);
        }
        if (self.all_data_sources_flushed != false) {
            target.appendBool(TracingServiceEventWire.ALL_DATA_SOURCES_FLUSHED_WIRE, self.all_data_sources_flushed);
        }
        if (self.read_tracing_buffers_completed != false) {
            target.appendBool(TracingServiceEventWire.READ_TRACING_BUFFERS_COMPLETED_WIRE, self.read_tracing_buffers_completed);
        }
        if (self.tracing_disabled != false) {
            target.appendBool(TracingServiceEventWire.TRACING_DISABLED_WIRE, self.tracing_disabled);
        }
        if (self.seized_for_bugreport != false) {
            target.appendBool(TracingServiceEventWire.SEIZED_FOR_BUGREPORT_WIRE, self.seized_for_bugreport);
        }
        if (self.slow_starting_data_sources) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(TracingServiceEventWire.SLOW_STARTING_DATA_SOURCES_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.last_flush_slow_data_sources) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(TracingServiceEventWire.LAST_FLUSH_SLOW_DATA_SOURCES_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.clone_started != false) {
            target.appendBool(TracingServiceEventWire.CLONE_STARTED_WIRE, self.clone_started);
        }
        if (self.buffer_cloned != 0) {
            target.appendUint32(TracingServiceEventWire.BUFFER_CLONED_WIRE, self.buffer_cloned);
        }
    }
};
pub const TracingServiceEventReader = struct {
    buf: gremlin.Reader,
    _tracing_started: bool = false,
    _all_data_sources_started: bool = false,
    _flush_started: bool = false,
    _all_data_sources_flushed: bool = false,
    _read_tracing_buffers_completed: bool = false,
    _tracing_disabled: bool = false,
    _seized_for_bugreport: bool = false,
    _slow_starting_data_sources_buf: ?[]const u8 = null,
    _last_flush_slow_data_sources_buf: ?[]const u8 = null,
    _clone_started: bool = false,
    _buffer_cloned: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!TracingServiceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = TracingServiceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TracingServiceEventWire.TRACING_STARTED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._tracing_started = result.value;
                },
                TracingServiceEventWire.ALL_DATA_SOURCES_STARTED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._all_data_sources_started = result.value;
                },
                TracingServiceEventWire.FLUSH_STARTED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._flush_started = result.value;
                },
                TracingServiceEventWire.ALL_DATA_SOURCES_FLUSHED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._all_data_sources_flushed = result.value;
                },
                TracingServiceEventWire.READ_TRACING_BUFFERS_COMPLETED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._read_tracing_buffers_completed = result.value;
                },
                TracingServiceEventWire.TRACING_DISABLED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._tracing_disabled = result.value;
                },
                TracingServiceEventWire.SEIZED_FOR_BUGREPORT_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._seized_for_bugreport = result.value;
                },
                TracingServiceEventWire.SLOW_STARTING_DATA_SOURCES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._slow_starting_data_sources_buf = result.value;
                },
                TracingServiceEventWire.LAST_FLUSH_SLOW_DATA_SOURCES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._last_flush_slow_data_sources_buf = result.value;
                },
                TracingServiceEventWire.CLONE_STARTED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._clone_started = result.value;
                },
                TracingServiceEventWire.BUFFER_CLONED_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._buffer_cloned = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTracingStarted(self: *const TracingServiceEventReader) bool {
        return self._tracing_started;
    }
    pub inline fn getAllDataSourcesStarted(self: *const TracingServiceEventReader) bool {
        return self._all_data_sources_started;
    }
    pub inline fn getFlushStarted(self: *const TracingServiceEventReader) bool {
        return self._flush_started;
    }
    pub inline fn getAllDataSourcesFlushed(self: *const TracingServiceEventReader) bool {
        return self._all_data_sources_flushed;
    }
    pub inline fn getReadTracingBuffersCompleted(self: *const TracingServiceEventReader) bool {
        return self._read_tracing_buffers_completed;
    }
    pub inline fn getTracingDisabled(self: *const TracingServiceEventReader) bool {
        return self._tracing_disabled;
    }
    pub inline fn getSeizedForBugreport(self: *const TracingServiceEventReader) bool {
        return self._seized_for_bugreport;
    }
    pub fn getSlowStartingDataSources(self: *const TracingServiceEventReader) gremlin.Error!TracingServiceEvent.DataSourcesReader {
        if (self._slow_starting_data_sources_buf) |buf| {
            return try TracingServiceEvent.DataSourcesReader.init(buf);
        }
        return try TracingServiceEvent.DataSourcesReader.init(&[_]u8{});
    }
    pub fn getLastFlushSlowDataSources(self: *const TracingServiceEventReader) gremlin.Error!TracingServiceEvent.DataSourcesReader {
        if (self._last_flush_slow_data_sources_buf) |buf| {
            return try TracingServiceEvent.DataSourcesReader.init(buf);
        }
        return try TracingServiceEvent.DataSourcesReader.init(&[_]u8{});
    }
    pub inline fn getCloneStarted(self: *const TracingServiceEventReader) bool {
        return self._clone_started;
    }
    pub inline fn getBufferCloned(self: *const TracingServiceEventReader) u32 {
        return self._buffer_cloned;
    }
};
