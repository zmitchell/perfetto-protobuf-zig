// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PerfettoMetatraceWire = struct {
    const EVENT_DURATION_NS_WIRE: gremlin.ProtoWireNumber = 3;
    const COUNTER_VALUE_WIRE: gremlin.ProtoWireNumber = 4;
    const THREAD_ID_WIRE: gremlin.ProtoWireNumber = 5;
    const HAS_OVERRUNS_WIRE: gremlin.ProtoWireNumber = 6;
    const ARGS_WIRE: gremlin.ProtoWireNumber = 7;
    const INTERNED_STRINGS_WIRE: gremlin.ProtoWireNumber = 10;
    const EVENT_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const COUNTER_ID_WIRE: gremlin.ProtoWireNumber = 2;
    const EVENT_NAME_WIRE: gremlin.ProtoWireNumber = 8;
    const EVENT_NAME_IID_WIRE: gremlin.ProtoWireNumber = 11;
    const COUNTER_NAME_WIRE: gremlin.ProtoWireNumber = 9;
};
pub const PerfettoMetatrace = struct {
    // nested structs
    const ArgWire = struct {
        const KEY_WIRE: gremlin.ProtoWireNumber = 1;
        const KEY_IID_WIRE: gremlin.ProtoWireNumber = 3;
        const VALUE_WIRE: gremlin.ProtoWireNumber = 2;
        const VALUE_IID_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const Arg = struct {
        // fields
        key: ?[]const u8 = null,
        key_iid: u64 = 0,
        value: ?[]const u8 = null,
        value_iid: u64 = 0,
        pub fn calcProtobufSize(self: *const PerfettoMetatrace.Arg) usize {
            var res: usize = 0;
            if (self.key) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(PerfettoMetatrace.ArgWire.KEY_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.key_iid != 0) {
                res += gremlin.sizes.sizeWireNumber(PerfettoMetatrace.ArgWire.KEY_IID_WIRE) + gremlin.sizes.sizeU64(self.key_iid);
            }
            if (self.value) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(PerfettoMetatrace.ArgWire.VALUE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.value_iid != 0) {
                res += gremlin.sizes.sizeWireNumber(PerfettoMetatrace.ArgWire.VALUE_IID_WIRE) + gremlin.sizes.sizeU64(self.value_iid);
            }
            return res;
        }
        pub fn encode(self: *const PerfettoMetatrace.Arg, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const PerfettoMetatrace.Arg, target: *gremlin.Writer) void {
            if (self.key) |v| {
                if (v.len > 0) {
                    target.appendBytes(PerfettoMetatrace.ArgWire.KEY_WIRE, v);
                }
            }
            if (self.key_iid != 0) {
                target.appendUint64(PerfettoMetatrace.ArgWire.KEY_IID_WIRE, self.key_iid);
            }
            if (self.value) |v| {
                if (v.len > 0) {
                    target.appendBytes(PerfettoMetatrace.ArgWire.VALUE_WIRE, v);
                }
            }
            if (self.value_iid != 0) {
                target.appendUint64(PerfettoMetatrace.ArgWire.VALUE_IID_WIRE, self.value_iid);
            }
        }
    };
    pub const ArgReader = struct {
        buf: gremlin.Reader,
        _key: ?[]const u8 = null,
        _key_iid: u64 = 0,
        _value: ?[]const u8 = null,
        _value_iid: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!PerfettoMetatrace.ArgReader {
            const buf = gremlin.Reader.init(src);
            var res = PerfettoMetatrace.ArgReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    PerfettoMetatrace.ArgWire.KEY_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._key = result.value;
                    },
                    PerfettoMetatrace.ArgWire.KEY_IID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._key_iid = result.value;
                    },
                    PerfettoMetatrace.ArgWire.VALUE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._value = result.value;
                    },
                    PerfettoMetatrace.ArgWire.VALUE_IID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._value_iid = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getKey(self: *const PerfettoMetatrace.ArgReader) []const u8 {
            return self._key orelse &[_]u8{};
        }
        pub inline fn getKeyIid(self: *const PerfettoMetatrace.ArgReader) u64 {
            return self._key_iid;
        }
        pub inline fn getValue(self: *const PerfettoMetatrace.ArgReader) []const u8 {
            return self._value orelse &[_]u8{};
        }
        pub inline fn getValueIid(self: *const PerfettoMetatrace.ArgReader) u64 {
            return self._value_iid;
        }
    };
    const InternedStringWire = struct {
        const IID_WIRE: gremlin.ProtoWireNumber = 1;
        const VALUE_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const InternedString = struct {
        // fields
        iid: u64 = 0,
        value: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const PerfettoMetatrace.InternedString) usize {
            var res: usize = 0;
            if (self.iid != 0) {
                res += gremlin.sizes.sizeWireNumber(PerfettoMetatrace.InternedStringWire.IID_WIRE) + gremlin.sizes.sizeU64(self.iid);
            }
            if (self.value) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(PerfettoMetatrace.InternedStringWire.VALUE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const PerfettoMetatrace.InternedString, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const PerfettoMetatrace.InternedString, target: *gremlin.Writer) void {
            if (self.iid != 0) {
                target.appendUint64(PerfettoMetatrace.InternedStringWire.IID_WIRE, self.iid);
            }
            if (self.value) |v| {
                if (v.len > 0) {
                    target.appendBytes(PerfettoMetatrace.InternedStringWire.VALUE_WIRE, v);
                }
            }
        }
    };
    pub const InternedStringReader = struct {
        buf: gremlin.Reader,
        _iid: u64 = 0,
        _value: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!PerfettoMetatrace.InternedStringReader {
            const buf = gremlin.Reader.init(src);
            var res = PerfettoMetatrace.InternedStringReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    PerfettoMetatrace.InternedStringWire.IID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._iid = result.value;
                    },
                    PerfettoMetatrace.InternedStringWire.VALUE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._value = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getIid(self: *const PerfettoMetatrace.InternedStringReader) u64 {
            return self._iid;
        }
        pub inline fn getValue(self: *const PerfettoMetatrace.InternedStringReader) []const u8 {
            return self._value orelse &[_]u8{};
        }
    };
    // fields
    event_duration_ns: u64 = 0,
    counter_value: i32 = 0,
    thread_id: u32 = 0,
    has_overruns: bool = false,
    args: ?[]const ?PerfettoMetatrace.Arg = null,
    interned_strings: ?[]const ?PerfettoMetatrace.InternedString = null,
    event_id: u32 = 0,
    counter_id: u32 = 0,
    event_name: ?[]const u8 = null,
    event_name_iid: u64 = 0,
    counter_name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const PerfettoMetatrace) usize {
        var res: usize = 0;
        if (self.event_duration_ns != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.EVENT_DURATION_NS_WIRE) + gremlin.sizes.sizeU64(self.event_duration_ns);
        }
        if (self.counter_value != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.COUNTER_VALUE_WIRE) + gremlin.sizes.sizeI32(self.counter_value);
        }
        if (self.thread_id != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.THREAD_ID_WIRE) + gremlin.sizes.sizeU32(self.thread_id);
        }
        if (self.has_overruns != false) {
            res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.HAS_OVERRUNS_WIRE) + gremlin.sizes.sizeBool(self.has_overruns);
        }
        if (self.args) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.ARGS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.interned_strings) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.INTERNED_STRINGS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.event_id != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.EVENT_ID_WIRE) + gremlin.sizes.sizeU32(self.event_id);
        }
        if (self.counter_id != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.COUNTER_ID_WIRE) + gremlin.sizes.sizeU32(self.counter_id);
        }
        if (self.event_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.EVENT_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.event_name_iid != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.EVENT_NAME_IID_WIRE) + gremlin.sizes.sizeU64(self.event_name_iid);
        }
        if (self.counter_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(PerfettoMetatraceWire.COUNTER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const PerfettoMetatrace, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PerfettoMetatrace, target: *gremlin.Writer) void {
        if (self.event_duration_ns != 0) {
            target.appendUint64(PerfettoMetatraceWire.EVENT_DURATION_NS_WIRE, self.event_duration_ns);
        }
        if (self.counter_value != 0) {
            target.appendInt32(PerfettoMetatraceWire.COUNTER_VALUE_WIRE, self.counter_value);
        }
        if (self.thread_id != 0) {
            target.appendUint32(PerfettoMetatraceWire.THREAD_ID_WIRE, self.thread_id);
        }
        if (self.has_overruns != false) {
            target.appendBool(PerfettoMetatraceWire.HAS_OVERRUNS_WIRE, self.has_overruns);
        }
        if (self.args) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(PerfettoMetatraceWire.ARGS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(PerfettoMetatraceWire.ARGS_WIRE, 0);
                }
            }
        }
        if (self.interned_strings) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(PerfettoMetatraceWire.INTERNED_STRINGS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(PerfettoMetatraceWire.INTERNED_STRINGS_WIRE, 0);
                }
            }
        }
        if (self.event_id != 0) {
            target.appendUint32(PerfettoMetatraceWire.EVENT_ID_WIRE, self.event_id);
        }
        if (self.counter_id != 0) {
            target.appendUint32(PerfettoMetatraceWire.COUNTER_ID_WIRE, self.counter_id);
        }
        if (self.event_name) |v| {
            if (v.len > 0) {
                target.appendBytes(PerfettoMetatraceWire.EVENT_NAME_WIRE, v);
            }
        }
        if (self.event_name_iid != 0) {
            target.appendUint64(PerfettoMetatraceWire.EVENT_NAME_IID_WIRE, self.event_name_iid);
        }
        if (self.counter_name) |v| {
            if (v.len > 0) {
                target.appendBytes(PerfettoMetatraceWire.COUNTER_NAME_WIRE, v);
            }
        }
    }
};
pub const PerfettoMetatraceReader = struct {
    buf: gremlin.Reader,
    _event_duration_ns: u64 = 0,
    _counter_value: i32 = 0,
    _thread_id: u32 = 0,
    _has_overruns: bool = false,
    _args_offset: ?usize = null,
    _args_last_offset: ?usize = null,
    _args_cnt: usize = 0,
    _interned_strings_offset: ?usize = null,
    _interned_strings_last_offset: ?usize = null,
    _interned_strings_cnt: usize = 0,
    _event_id: u32 = 0,
    _counter_id: u32 = 0,
    _event_name: ?[]const u8 = null,
    _event_name_iid: u64 = 0,
    _counter_name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!PerfettoMetatraceReader {
        const buf = gremlin.Reader.init(src);
        var res = PerfettoMetatraceReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PerfettoMetatraceWire.EVENT_DURATION_NS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._event_duration_ns = result.value;
                },
                PerfettoMetatraceWire.COUNTER_VALUE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._counter_value = result.value;
                },
                PerfettoMetatraceWire.THREAD_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._thread_id = result.value;
                },
                PerfettoMetatraceWire.HAS_OVERRUNS_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._has_overruns = result.value;
                },
                PerfettoMetatraceWire.ARGS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._args_offset == null) {
                        res._args_offset = offset - result.size;
                    }
                    res._args_last_offset = offset;
                    res._args_cnt += 1;
                },
                PerfettoMetatraceWire.INTERNED_STRINGS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._interned_strings_offset == null) {
                        res._interned_strings_offset = offset - result.size;
                    }
                    res._interned_strings_last_offset = offset;
                    res._interned_strings_cnt += 1;
                },
                PerfettoMetatraceWire.EVENT_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._event_id = result.value;
                },
                PerfettoMetatraceWire.COUNTER_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._counter_id = result.value;
                },
                PerfettoMetatraceWire.EVENT_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._event_name = result.value;
                },
                PerfettoMetatraceWire.EVENT_NAME_IID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._event_name_iid = result.value;
                },
                PerfettoMetatraceWire.COUNTER_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._counter_name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getEventDurationNs(self: *const PerfettoMetatraceReader) u64 {
        return self._event_duration_ns;
    }
    pub inline fn getCounterValue(self: *const PerfettoMetatraceReader) i32 {
        return self._counter_value;
    }
    pub inline fn getThreadId(self: *const PerfettoMetatraceReader) u32 {
        return self._thread_id;
    }
    pub inline fn getHasOverruns(self: *const PerfettoMetatraceReader) bool {
        return self._has_overruns;
    }
    pub fn argsCount(self: *const PerfettoMetatraceReader) usize {
        return self._args_cnt;
    }
    pub fn argsNext(self: *PerfettoMetatraceReader) ?PerfettoMetatrace.ArgReader {
        if (self._args_offset == null) return null;
        const current_offset = self._args_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = PerfettoMetatrace.ArgReader.init(result.value) catch return null;
        if (self._args_last_offset != null and current_offset >= self._args_last_offset.?) {
            self._args_offset = null;
            return msg;
        }
        if (self._args_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._args_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == PerfettoMetatraceWire.ARGS_WIRE) {
                self._args_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._args_offset = null;
        return msg;
    }
    pub fn internedStringsCount(self: *const PerfettoMetatraceReader) usize {
        return self._interned_strings_cnt;
    }
    pub fn internedStringsNext(self: *PerfettoMetatraceReader) ?PerfettoMetatrace.InternedStringReader {
        if (self._interned_strings_offset == null) return null;
        const current_offset = self._interned_strings_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = PerfettoMetatrace.InternedStringReader.init(result.value) catch return null;
        if (self._interned_strings_last_offset != null and current_offset >= self._interned_strings_last_offset.?) {
            self._interned_strings_offset = null;
            return msg;
        }
        if (self._interned_strings_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._interned_strings_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == PerfettoMetatraceWire.INTERNED_STRINGS_WIRE) {
                self._interned_strings_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._interned_strings_offset = null;
        return msg;
    }
    pub inline fn getEventId(self: *const PerfettoMetatraceReader) u32 {
        return self._event_id;
    }
    pub inline fn getCounterId(self: *const PerfettoMetatraceReader) u32 {
        return self._counter_id;
    }
    pub inline fn getEventName(self: *const PerfettoMetatraceReader) []const u8 {
        return self._event_name orelse &[_]u8{};
    }
    pub inline fn getEventNameIid(self: *const PerfettoMetatraceReader) u64 {
        return self._event_name_iid;
    }
    pub inline fn getCounterName(self: *const PerfettoMetatraceReader) []const u8 {
        return self._counter_name orelse &[_]u8{};
    }
};
