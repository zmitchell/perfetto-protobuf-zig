// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const SmapsEntryWire = struct {
    const PATH_WIRE: gremlin.ProtoWireNumber = 1;
    const SIZE_KB_WIRE: gremlin.ProtoWireNumber = 2;
    const PRIVATE_DIRTY_KB_WIRE: gremlin.ProtoWireNumber = 3;
    const SWAP_KB_WIRE: gremlin.ProtoWireNumber = 4;
    const FILE_NAME_WIRE: gremlin.ProtoWireNumber = 5;
    const START_ADDRESS_WIRE: gremlin.ProtoWireNumber = 6;
    const MODULE_TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 7;
    const MODULE_DEBUGID_WIRE: gremlin.ProtoWireNumber = 8;
    const MODULE_DEBUG_PATH_WIRE: gremlin.ProtoWireNumber = 9;
    const PROTECTION_FLAGS_WIRE: gremlin.ProtoWireNumber = 10;
    const PRIVATE_CLEAN_RESIDENT_KB_WIRE: gremlin.ProtoWireNumber = 11;
    const SHARED_DIRTY_RESIDENT_KB_WIRE: gremlin.ProtoWireNumber = 12;
    const SHARED_CLEAN_RESIDENT_KB_WIRE: gremlin.ProtoWireNumber = 13;
    const LOCKED_KB_WIRE: gremlin.ProtoWireNumber = 14;
    const PROPORTIONAL_RESIDENT_KB_WIRE: gremlin.ProtoWireNumber = 15;
};
pub const SmapsEntry = struct {
    // fields
    path: ?[]const u8 = null,
    size_kb: u64 = 0,
    private_dirty_kb: u64 = 0,
    swap_kb: u64 = 0,
    file_name: ?[]const u8 = null,
    start_address: u64 = 0,
    module_timestamp: u64 = 0,
    module_debugid: ?[]const u8 = null,
    module_debug_path: ?[]const u8 = null,
    protection_flags: u32 = 0,
    private_clean_resident_kb: u64 = 0,
    shared_dirty_resident_kb: u64 = 0,
    shared_clean_resident_kb: u64 = 0,
    locked_kb: u64 = 0,
    proportional_resident_kb: u64 = 0,
    pub fn calcProtobufSize(self: *const SmapsEntry) usize {
        var res: usize = 0;
        if (self.path) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.PATH_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.size_kb != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.SIZE_KB_WIRE) + gremlin.sizes.sizeU64(self.size_kb);
        }
        if (self.private_dirty_kb != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.PRIVATE_DIRTY_KB_WIRE) + gremlin.sizes.sizeU64(self.private_dirty_kb);
        }
        if (self.swap_kb != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.SWAP_KB_WIRE) + gremlin.sizes.sizeU64(self.swap_kb);
        }
        if (self.file_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.FILE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.start_address != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.START_ADDRESS_WIRE) + gremlin.sizes.sizeU64(self.start_address);
        }
        if (self.module_timestamp != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.MODULE_TIMESTAMP_WIRE) + gremlin.sizes.sizeU64(self.module_timestamp);
        }
        if (self.module_debugid) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.MODULE_DEBUGID_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.module_debug_path) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.MODULE_DEBUG_PATH_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.protection_flags != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.PROTECTION_FLAGS_WIRE) + gremlin.sizes.sizeU32(self.protection_flags);
        }
        if (self.private_clean_resident_kb != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.PRIVATE_CLEAN_RESIDENT_KB_WIRE) + gremlin.sizes.sizeU64(self.private_clean_resident_kb);
        }
        if (self.shared_dirty_resident_kb != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.SHARED_DIRTY_RESIDENT_KB_WIRE) + gremlin.sizes.sizeU64(self.shared_dirty_resident_kb);
        }
        if (self.shared_clean_resident_kb != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.SHARED_CLEAN_RESIDENT_KB_WIRE) + gremlin.sizes.sizeU64(self.shared_clean_resident_kb);
        }
        if (self.locked_kb != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.LOCKED_KB_WIRE) + gremlin.sizes.sizeU64(self.locked_kb);
        }
        if (self.proportional_resident_kb != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsEntryWire.PROPORTIONAL_RESIDENT_KB_WIRE) + gremlin.sizes.sizeU64(self.proportional_resident_kb);
        }
        return res;
    }
    pub fn encode(self: *const SmapsEntry, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SmapsEntry, target: *gremlin.Writer) void {
        if (self.path) |v| {
            if (v.len > 0) {
                target.appendBytes(SmapsEntryWire.PATH_WIRE, v);
            }
        }
        if (self.size_kb != 0) {
            target.appendUint64(SmapsEntryWire.SIZE_KB_WIRE, self.size_kb);
        }
        if (self.private_dirty_kb != 0) {
            target.appendUint64(SmapsEntryWire.PRIVATE_DIRTY_KB_WIRE, self.private_dirty_kb);
        }
        if (self.swap_kb != 0) {
            target.appendUint64(SmapsEntryWire.SWAP_KB_WIRE, self.swap_kb);
        }
        if (self.file_name) |v| {
            if (v.len > 0) {
                target.appendBytes(SmapsEntryWire.FILE_NAME_WIRE, v);
            }
        }
        if (self.start_address != 0) {
            target.appendUint64(SmapsEntryWire.START_ADDRESS_WIRE, self.start_address);
        }
        if (self.module_timestamp != 0) {
            target.appendUint64(SmapsEntryWire.MODULE_TIMESTAMP_WIRE, self.module_timestamp);
        }
        if (self.module_debugid) |v| {
            if (v.len > 0) {
                target.appendBytes(SmapsEntryWire.MODULE_DEBUGID_WIRE, v);
            }
        }
        if (self.module_debug_path) |v| {
            if (v.len > 0) {
                target.appendBytes(SmapsEntryWire.MODULE_DEBUG_PATH_WIRE, v);
            }
        }
        if (self.protection_flags != 0) {
            target.appendUint32(SmapsEntryWire.PROTECTION_FLAGS_WIRE, self.protection_flags);
        }
        if (self.private_clean_resident_kb != 0) {
            target.appendUint64(SmapsEntryWire.PRIVATE_CLEAN_RESIDENT_KB_WIRE, self.private_clean_resident_kb);
        }
        if (self.shared_dirty_resident_kb != 0) {
            target.appendUint64(SmapsEntryWire.SHARED_DIRTY_RESIDENT_KB_WIRE, self.shared_dirty_resident_kb);
        }
        if (self.shared_clean_resident_kb != 0) {
            target.appendUint64(SmapsEntryWire.SHARED_CLEAN_RESIDENT_KB_WIRE, self.shared_clean_resident_kb);
        }
        if (self.locked_kb != 0) {
            target.appendUint64(SmapsEntryWire.LOCKED_KB_WIRE, self.locked_kb);
        }
        if (self.proportional_resident_kb != 0) {
            target.appendUint64(SmapsEntryWire.PROPORTIONAL_RESIDENT_KB_WIRE, self.proportional_resident_kb);
        }
    }
};
pub const SmapsEntryReader = struct {
    buf: gremlin.Reader,
    _path: ?[]const u8 = null,
    _size_kb: u64 = 0,
    _private_dirty_kb: u64 = 0,
    _swap_kb: u64 = 0,
    _file_name: ?[]const u8 = null,
    _start_address: u64 = 0,
    _module_timestamp: u64 = 0,
    _module_debugid: ?[]const u8 = null,
    _module_debug_path: ?[]const u8 = null,
    _protection_flags: u32 = 0,
    _private_clean_resident_kb: u64 = 0,
    _shared_dirty_resident_kb: u64 = 0,
    _shared_clean_resident_kb: u64 = 0,
    _locked_kb: u64 = 0,
    _proportional_resident_kb: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!SmapsEntryReader {
        const buf = gremlin.Reader.init(src);
        var res = SmapsEntryReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SmapsEntryWire.PATH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._path = result.value;
                },
                SmapsEntryWire.SIZE_KB_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._size_kb = result.value;
                },
                SmapsEntryWire.PRIVATE_DIRTY_KB_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._private_dirty_kb = result.value;
                },
                SmapsEntryWire.SWAP_KB_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._swap_kb = result.value;
                },
                SmapsEntryWire.FILE_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._file_name = result.value;
                },
                SmapsEntryWire.START_ADDRESS_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._start_address = result.value;
                },
                SmapsEntryWire.MODULE_TIMESTAMP_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._module_timestamp = result.value;
                },
                SmapsEntryWire.MODULE_DEBUGID_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._module_debugid = result.value;
                },
                SmapsEntryWire.MODULE_DEBUG_PATH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._module_debug_path = result.value;
                },
                SmapsEntryWire.PROTECTION_FLAGS_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._protection_flags = result.value;
                },
                SmapsEntryWire.PRIVATE_CLEAN_RESIDENT_KB_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._private_clean_resident_kb = result.value;
                },
                SmapsEntryWire.SHARED_DIRTY_RESIDENT_KB_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._shared_dirty_resident_kb = result.value;
                },
                SmapsEntryWire.SHARED_CLEAN_RESIDENT_KB_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._shared_clean_resident_kb = result.value;
                },
                SmapsEntryWire.LOCKED_KB_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._locked_kb = result.value;
                },
                SmapsEntryWire.PROPORTIONAL_RESIDENT_KB_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._proportional_resident_kb = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPath(self: *const SmapsEntryReader) []const u8 {
        return self._path orelse &[_]u8{};
    }
    pub inline fn getSizeKb(self: *const SmapsEntryReader) u64 {
        return self._size_kb;
    }
    pub inline fn getPrivateDirtyKb(self: *const SmapsEntryReader) u64 {
        return self._private_dirty_kb;
    }
    pub inline fn getSwapKb(self: *const SmapsEntryReader) u64 {
        return self._swap_kb;
    }
    pub inline fn getFileName(self: *const SmapsEntryReader) []const u8 {
        return self._file_name orelse &[_]u8{};
    }
    pub inline fn getStartAddress(self: *const SmapsEntryReader) u64 {
        return self._start_address;
    }
    pub inline fn getModuleTimestamp(self: *const SmapsEntryReader) u64 {
        return self._module_timestamp;
    }
    pub inline fn getModuleDebugid(self: *const SmapsEntryReader) []const u8 {
        return self._module_debugid orelse &[_]u8{};
    }
    pub inline fn getModuleDebugPath(self: *const SmapsEntryReader) []const u8 {
        return self._module_debug_path orelse &[_]u8{};
    }
    pub inline fn getProtectionFlags(self: *const SmapsEntryReader) u32 {
        return self._protection_flags;
    }
    pub inline fn getPrivateCleanResidentKb(self: *const SmapsEntryReader) u64 {
        return self._private_clean_resident_kb;
    }
    pub inline fn getSharedDirtyResidentKb(self: *const SmapsEntryReader) u64 {
        return self._shared_dirty_resident_kb;
    }
    pub inline fn getSharedCleanResidentKb(self: *const SmapsEntryReader) u64 {
        return self._shared_clean_resident_kb;
    }
    pub inline fn getLockedKb(self: *const SmapsEntryReader) u64 {
        return self._locked_kb;
    }
    pub inline fn getProportionalResidentKb(self: *const SmapsEntryReader) u64 {
        return self._proportional_resident_kb;
    }
};
const SmapsPacketWire = struct {
    const PID_WIRE: gremlin.ProtoWireNumber = 1;
    const ENTRIES_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const SmapsPacket = struct {
    // fields
    pid: u32 = 0,
    entries: ?[]const ?SmapsEntry = null,
    pub fn calcProtobufSize(self: *const SmapsPacket) usize {
        var res: usize = 0;
        if (self.pid != 0) {
            res += gremlin.sizes.sizeWireNumber(SmapsPacketWire.PID_WIRE) + gremlin.sizes.sizeU32(self.pid);
        }
        if (self.entries) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(SmapsPacketWire.ENTRIES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const SmapsPacket, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SmapsPacket, target: *gremlin.Writer) void {
        if (self.pid != 0) {
            target.appendUint32(SmapsPacketWire.PID_WIRE, self.pid);
        }
        if (self.entries) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(SmapsPacketWire.ENTRIES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(SmapsPacketWire.ENTRIES_WIRE, 0);
                }
            }
        }
    }
};
pub const SmapsPacketReader = struct {
    buf: gremlin.Reader,
    _pid: u32 = 0,
    _entries_offset: ?usize = null,
    _entries_last_offset: ?usize = null,
    _entries_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!SmapsPacketReader {
        const buf = gremlin.Reader.init(src);
        var res = SmapsPacketReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SmapsPacketWire.PID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._pid = result.value;
                },
                SmapsPacketWire.ENTRIES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._entries_offset == null) {
                        res._entries_offset = offset - result.size;
                    }
                    res._entries_last_offset = offset;
                    res._entries_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPid(self: *const SmapsPacketReader) u32 {
        return self._pid;
    }
    pub fn entriesCount(self: *const SmapsPacketReader) usize {
        return self._entries_cnt;
    }
    pub fn entriesNext(self: *SmapsPacketReader) ?SmapsEntryReader {
        if (self._entries_offset == null) return null;
        const current_offset = self._entries_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = SmapsEntryReader.init(result.value) catch return null;
        if (self._entries_last_offset != null and current_offset >= self._entries_last_offset.?) {
            self._entries_offset = null;
            return msg;
        }
        if (self._entries_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._entries_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == SmapsPacketWire.ENTRIES_WIRE) {
                self._entries_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._entries_offset = null;
        return msg;
    }
};
