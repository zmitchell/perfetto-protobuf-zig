// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ObfuscatedMemberWire = struct {
    const OBFUSCATED_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const DEOBFUSCATED_NAME_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const ObfuscatedMember = struct {
    // fields
    obfuscated_name: ?[]const u8 = null,
    deobfuscated_name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ObfuscatedMember) usize {
        var res: usize = 0;
        if (self.obfuscated_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ObfuscatedMemberWire.OBFUSCATED_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.deobfuscated_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ObfuscatedMemberWire.DEOBFUSCATED_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const ObfuscatedMember, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ObfuscatedMember, target: *gremlin.Writer) void {
        if (self.obfuscated_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ObfuscatedMemberWire.OBFUSCATED_NAME_WIRE, v);
            }
        }
        if (self.deobfuscated_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ObfuscatedMemberWire.DEOBFUSCATED_NAME_WIRE, v);
            }
        }
    }
};
pub const ObfuscatedMemberReader = struct {
    buf: gremlin.Reader,
    _obfuscated_name: ?[]const u8 = null,
    _deobfuscated_name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ObfuscatedMemberReader {
        const buf = gremlin.Reader.init(src);
        var res = ObfuscatedMemberReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ObfuscatedMemberWire.OBFUSCATED_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._obfuscated_name = result.value;
                },
                ObfuscatedMemberWire.DEOBFUSCATED_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._deobfuscated_name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getObfuscatedName(self: *const ObfuscatedMemberReader) []const u8 {
        return self._obfuscated_name orelse &[_]u8{};
    }
    pub inline fn getDeobfuscatedName(self: *const ObfuscatedMemberReader) []const u8 {
        return self._deobfuscated_name orelse &[_]u8{};
    }
};
const ObfuscatedClassWire = struct {
    const OBFUSCATED_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const DEOBFUSCATED_NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const OBFUSCATED_MEMBERS_WIRE: gremlin.ProtoWireNumber = 3;
    const OBFUSCATED_METHODS_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const ObfuscatedClass = struct {
    // fields
    obfuscated_name: ?[]const u8 = null,
    deobfuscated_name: ?[]const u8 = null,
    obfuscated_members: ?[]const ?ObfuscatedMember = null,
    obfuscated_methods: ?[]const ?ObfuscatedMember = null,
    pub fn calcProtobufSize(self: *const ObfuscatedClass) usize {
        var res: usize = 0;
        if (self.obfuscated_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ObfuscatedClassWire.OBFUSCATED_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.deobfuscated_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ObfuscatedClassWire.DEOBFUSCATED_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.obfuscated_members) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ObfuscatedClassWire.OBFUSCATED_MEMBERS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.obfuscated_methods) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ObfuscatedClassWire.OBFUSCATED_METHODS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ObfuscatedClass, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ObfuscatedClass, target: *gremlin.Writer) void {
        if (self.obfuscated_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ObfuscatedClassWire.OBFUSCATED_NAME_WIRE, v);
            }
        }
        if (self.deobfuscated_name) |v| {
            if (v.len > 0) {
                target.appendBytes(ObfuscatedClassWire.DEOBFUSCATED_NAME_WIRE, v);
            }
        }
        if (self.obfuscated_members) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ObfuscatedClassWire.OBFUSCATED_MEMBERS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ObfuscatedClassWire.OBFUSCATED_MEMBERS_WIRE, 0);
                }
            }
        }
        if (self.obfuscated_methods) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ObfuscatedClassWire.OBFUSCATED_METHODS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ObfuscatedClassWire.OBFUSCATED_METHODS_WIRE, 0);
                }
            }
        }
    }
};
pub const ObfuscatedClassReader = struct {
    buf: gremlin.Reader,
    _obfuscated_name: ?[]const u8 = null,
    _deobfuscated_name: ?[]const u8 = null,
    _obfuscated_members_offset: ?usize = null,
    _obfuscated_members_last_offset: ?usize = null,
    _obfuscated_members_cnt: usize = 0,
    _obfuscated_methods_offset: ?usize = null,
    _obfuscated_methods_last_offset: ?usize = null,
    _obfuscated_methods_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ObfuscatedClassReader {
        const buf = gremlin.Reader.init(src);
        var res = ObfuscatedClassReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ObfuscatedClassWire.OBFUSCATED_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._obfuscated_name = result.value;
                },
                ObfuscatedClassWire.DEOBFUSCATED_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._deobfuscated_name = result.value;
                },
                ObfuscatedClassWire.OBFUSCATED_MEMBERS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._obfuscated_members_offset == null) {
                        res._obfuscated_members_offset = offset - result.size;
                    }
                    res._obfuscated_members_last_offset = offset;
                    res._obfuscated_members_cnt += 1;
                },
                ObfuscatedClassWire.OBFUSCATED_METHODS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._obfuscated_methods_offset == null) {
                        res._obfuscated_methods_offset = offset - result.size;
                    }
                    res._obfuscated_methods_last_offset = offset;
                    res._obfuscated_methods_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getObfuscatedName(self: *const ObfuscatedClassReader) []const u8 {
        return self._obfuscated_name orelse &[_]u8{};
    }
    pub inline fn getDeobfuscatedName(self: *const ObfuscatedClassReader) []const u8 {
        return self._deobfuscated_name orelse &[_]u8{};
    }
    pub fn obfuscatedMembersCount(self: *const ObfuscatedClassReader) usize {
        return self._obfuscated_members_cnt;
    }
    pub fn obfuscatedMembersNext(self: *ObfuscatedClassReader) ?ObfuscatedMemberReader {
        if (self._obfuscated_members_offset == null) return null;
        const current_offset = self._obfuscated_members_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ObfuscatedMemberReader.init(result.value) catch return null;
        if (self._obfuscated_members_last_offset != null and current_offset >= self._obfuscated_members_last_offset.?) {
            self._obfuscated_members_offset = null;
            return msg;
        }
        if (self._obfuscated_members_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._obfuscated_members_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ObfuscatedClassWire.OBFUSCATED_MEMBERS_WIRE) {
                self._obfuscated_members_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._obfuscated_members_offset = null;
        return msg;
    }
    pub fn obfuscatedMethodsCount(self: *const ObfuscatedClassReader) usize {
        return self._obfuscated_methods_cnt;
    }
    pub fn obfuscatedMethodsNext(self: *ObfuscatedClassReader) ?ObfuscatedMemberReader {
        if (self._obfuscated_methods_offset == null) return null;
        const current_offset = self._obfuscated_methods_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ObfuscatedMemberReader.init(result.value) catch return null;
        if (self._obfuscated_methods_last_offset != null and current_offset >= self._obfuscated_methods_last_offset.?) {
            self._obfuscated_methods_offset = null;
            return msg;
        }
        if (self._obfuscated_methods_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._obfuscated_methods_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ObfuscatedClassWire.OBFUSCATED_METHODS_WIRE) {
                self._obfuscated_methods_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._obfuscated_methods_offset = null;
        return msg;
    }
};
const DeobfuscationMappingWire = struct {
    const PACKAGE_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const VERSION_CODE_WIRE: gremlin.ProtoWireNumber = 2;
    const OBFUSCATED_CLASSES_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const DeobfuscationMapping = struct {
    // fields
    package_name: ?[]const u8 = null,
    version_code: i64 = 0,
    obfuscated_classes: ?[]const ?ObfuscatedClass = null,
    pub fn calcProtobufSize(self: *const DeobfuscationMapping) usize {
        var res: usize = 0;
        if (self.package_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(DeobfuscationMappingWire.PACKAGE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.version_code != 0) {
            res += gremlin.sizes.sizeWireNumber(DeobfuscationMappingWire.VERSION_CODE_WIRE) + gremlin.sizes.sizeI64(self.version_code);
        }
        if (self.obfuscated_classes) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(DeobfuscationMappingWire.OBFUSCATED_CLASSES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const DeobfuscationMapping, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const DeobfuscationMapping, target: *gremlin.Writer) void {
        if (self.package_name) |v| {
            if (v.len > 0) {
                target.appendBytes(DeobfuscationMappingWire.PACKAGE_NAME_WIRE, v);
            }
        }
        if (self.version_code != 0) {
            target.appendInt64(DeobfuscationMappingWire.VERSION_CODE_WIRE, self.version_code);
        }
        if (self.obfuscated_classes) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(DeobfuscationMappingWire.OBFUSCATED_CLASSES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(DeobfuscationMappingWire.OBFUSCATED_CLASSES_WIRE, 0);
                }
            }
        }
    }
};
pub const DeobfuscationMappingReader = struct {
    buf: gremlin.Reader,
    _package_name: ?[]const u8 = null,
    _version_code: i64 = 0,
    _obfuscated_classes_offset: ?usize = null,
    _obfuscated_classes_last_offset: ?usize = null,
    _obfuscated_classes_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!DeobfuscationMappingReader {
        const buf = gremlin.Reader.init(src);
        var res = DeobfuscationMappingReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                DeobfuscationMappingWire.PACKAGE_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._package_name = result.value;
                },
                DeobfuscationMappingWire.VERSION_CODE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._version_code = result.value;
                },
                DeobfuscationMappingWire.OBFUSCATED_CLASSES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._obfuscated_classes_offset == null) {
                        res._obfuscated_classes_offset = offset - result.size;
                    }
                    res._obfuscated_classes_last_offset = offset;
                    res._obfuscated_classes_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPackageName(self: *const DeobfuscationMappingReader) []const u8 {
        return self._package_name orelse &[_]u8{};
    }
    pub inline fn getVersionCode(self: *const DeobfuscationMappingReader) i64 {
        return self._version_code;
    }
    pub fn obfuscatedClassesCount(self: *const DeobfuscationMappingReader) usize {
        return self._obfuscated_classes_cnt;
    }
    pub fn obfuscatedClassesNext(self: *DeobfuscationMappingReader) ?ObfuscatedClassReader {
        if (self._obfuscated_classes_offset == null) return null;
        const current_offset = self._obfuscated_classes_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ObfuscatedClassReader.init(result.value) catch return null;
        if (self._obfuscated_classes_last_offset != null and current_offset >= self._obfuscated_classes_last_offset.?) {
            self._obfuscated_classes_offset = null;
            return msg;
        }
        if (self._obfuscated_classes_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._obfuscated_classes_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == DeobfuscationMappingWire.OBFUSCATED_CLASSES_WIRE) {
                self._obfuscated_classes_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._obfuscated_classes_offset = null;
        return msg;
    }
};
