// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const GenericKernelTaskStateEventWire = struct {
    const CPU_WIRE: gremlin.ProtoWireNumber = 1;
    const COMM_WIRE: gremlin.ProtoWireNumber = 2;
    const TID_WIRE: gremlin.ProtoWireNumber = 3;
    const STATE_WIRE: gremlin.ProtoWireNumber = 4;
    const PRIO_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const GenericKernelTaskStateEvent = struct {
    // nested enums
    pub const TaskStateEnum = enum(i32) {
        TASK_STATE_UNKNOWN = 0,
        TASK_STATE_CREATED = 1,
        TASK_STATE_RUNNABLE = 2,
        TASK_STATE_RUNNING = 3,
        TASK_STATE_INTERRUPTIBLE_SLEEP = 4,
        TASK_STATE_UNINTERRUPTIBLE_SLEEP = 5,
        TASK_STATE_STOPPED = 6,
        TASK_STATE_DEAD = 7,
        TASK_STATE_DESTROYED = 8,
    };
    // fields
    cpu: i32 = 0,
    comm: ?[]const u8 = null,
    tid: i64 = 0,
    state: GenericKernelTaskStateEvent.TaskStateEnum = @enumFromInt(0),
    prio: i32 = 0,
    pub fn calcProtobufSize(self: *const GenericKernelTaskStateEvent) usize {
        var res: usize = 0;
        if (self.cpu != 0) {
            res += gremlin.sizes.sizeWireNumber(GenericKernelTaskStateEventWire.CPU_WIRE) + gremlin.sizes.sizeI32(self.cpu);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(GenericKernelTaskStateEventWire.COMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.tid != 0) {
            res += gremlin.sizes.sizeWireNumber(GenericKernelTaskStateEventWire.TID_WIRE) + gremlin.sizes.sizeI64(self.tid);
        }
        if (@intFromEnum(self.state) != 0) {
            res += gremlin.sizes.sizeWireNumber(GenericKernelTaskStateEventWire.STATE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.state));
        }
        if (self.prio != 0) {
            res += gremlin.sizes.sizeWireNumber(GenericKernelTaskStateEventWire.PRIO_WIRE) + gremlin.sizes.sizeI32(self.prio);
        }
        return res;
    }
    pub fn encode(self: *const GenericKernelTaskStateEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GenericKernelTaskStateEvent, target: *gremlin.Writer) void {
        if (self.cpu != 0) {
            target.appendInt32(GenericKernelTaskStateEventWire.CPU_WIRE, self.cpu);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                target.appendBytes(GenericKernelTaskStateEventWire.COMM_WIRE, v);
            }
        }
        if (self.tid != 0) {
            target.appendInt64(GenericKernelTaskStateEventWire.TID_WIRE, self.tid);
        }
        if (@intFromEnum(self.state) != 0) {
            target.appendInt32(GenericKernelTaskStateEventWire.STATE_WIRE, @intFromEnum(self.state));
        }
        if (self.prio != 0) {
            target.appendInt32(GenericKernelTaskStateEventWire.PRIO_WIRE, self.prio);
        }
    }
};
pub const GenericKernelTaskStateEventReader = struct {
    buf: gremlin.Reader,
    _cpu: i32 = 0,
    _comm: ?[]const u8 = null,
    _tid: i64 = 0,
    _state: GenericKernelTaskStateEvent.TaskStateEnum = @enumFromInt(0),
    _prio: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!GenericKernelTaskStateEventReader {
        const buf = gremlin.Reader.init(src);
        var res = GenericKernelTaskStateEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GenericKernelTaskStateEventWire.CPU_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._cpu = result.value;
                },
                GenericKernelTaskStateEventWire.COMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._comm = result.value;
                },
                GenericKernelTaskStateEventWire.TID_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._tid = result.value;
                },
                GenericKernelTaskStateEventWire.STATE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._state = @enumFromInt(result.value);
                },
                GenericKernelTaskStateEventWire.PRIO_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._prio = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCpu(self: *const GenericKernelTaskStateEventReader) i32 {
        return self._cpu;
    }
    pub inline fn getComm(self: *const GenericKernelTaskStateEventReader) []const u8 {
        return self._comm orelse &[_]u8{};
    }
    pub inline fn getTid(self: *const GenericKernelTaskStateEventReader) i64 {
        return self._tid;
    }
    pub inline fn getState(self: *const GenericKernelTaskStateEventReader) GenericKernelTaskStateEvent.TaskStateEnum {
        return self._state;
    }
    pub inline fn getPrio(self: *const GenericKernelTaskStateEventReader) i32 {
        return self._prio;
    }
};
const GenericKernelTaskRenameEventWire = struct {
    const TID_WIRE: gremlin.ProtoWireNumber = 1;
    const COMM_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const GenericKernelTaskRenameEvent = struct {
    // fields
    tid: i64 = 0,
    comm: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const GenericKernelTaskRenameEvent) usize {
        var res: usize = 0;
        if (self.tid != 0) {
            res += gremlin.sizes.sizeWireNumber(GenericKernelTaskRenameEventWire.TID_WIRE) + gremlin.sizes.sizeI64(self.tid);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(GenericKernelTaskRenameEventWire.COMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const GenericKernelTaskRenameEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GenericKernelTaskRenameEvent, target: *gremlin.Writer) void {
        if (self.tid != 0) {
            target.appendInt64(GenericKernelTaskRenameEventWire.TID_WIRE, self.tid);
        }
        if (self.comm) |v| {
            if (v.len > 0) {
                target.appendBytes(GenericKernelTaskRenameEventWire.COMM_WIRE, v);
            }
        }
    }
};
pub const GenericKernelTaskRenameEventReader = struct {
    buf: gremlin.Reader,
    _tid: i64 = 0,
    _comm: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!GenericKernelTaskRenameEventReader {
        const buf = gremlin.Reader.init(src);
        var res = GenericKernelTaskRenameEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GenericKernelTaskRenameEventWire.TID_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._tid = result.value;
                },
                GenericKernelTaskRenameEventWire.COMM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._comm = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTid(self: *const GenericKernelTaskRenameEventReader) i64 {
        return self._tid;
    }
    pub inline fn getComm(self: *const GenericKernelTaskRenameEventReader) []const u8 {
        return self._comm orelse &[_]u8{};
    }
};
const GenericKernelProcessTreeWire = struct {
    const PROCESSES_WIRE: gremlin.ProtoWireNumber = 1;
    const THREADS_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const GenericKernelProcessTree = struct {
    // nested structs
    const ThreadWire = struct {
        const TID_WIRE: gremlin.ProtoWireNumber = 1;
        const PID_WIRE: gremlin.ProtoWireNumber = 2;
        const COMM_WIRE: gremlin.ProtoWireNumber = 3;
        const IS_MAIN_THREAD_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const Thread = struct {
        // fields
        tid: i64 = 0,
        pid: i64 = 0,
        comm: ?[]const u8 = null,
        is_main_thread: bool = false,
        pub fn calcProtobufSize(self: *const GenericKernelProcessTree.Thread) usize {
            var res: usize = 0;
            if (self.tid != 0) {
                res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTree.ThreadWire.TID_WIRE) + gremlin.sizes.sizeI64(self.tid);
            }
            if (self.pid != 0) {
                res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTree.ThreadWire.PID_WIRE) + gremlin.sizes.sizeI64(self.pid);
            }
            if (self.comm) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTree.ThreadWire.COMM_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.is_main_thread != false) {
                res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTree.ThreadWire.IS_MAIN_THREAD_WIRE) + gremlin.sizes.sizeBool(self.is_main_thread);
            }
            return res;
        }
        pub fn encode(self: *const GenericKernelProcessTree.Thread, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GenericKernelProcessTree.Thread, target: *gremlin.Writer) void {
            if (self.tid != 0) {
                target.appendInt64(GenericKernelProcessTree.ThreadWire.TID_WIRE, self.tid);
            }
            if (self.pid != 0) {
                target.appendInt64(GenericKernelProcessTree.ThreadWire.PID_WIRE, self.pid);
            }
            if (self.comm) |v| {
                if (v.len > 0) {
                    target.appendBytes(GenericKernelProcessTree.ThreadWire.COMM_WIRE, v);
                }
            }
            if (self.is_main_thread != false) {
                target.appendBool(GenericKernelProcessTree.ThreadWire.IS_MAIN_THREAD_WIRE, self.is_main_thread);
            }
        }
    };
    pub const ThreadReader = struct {
        buf: gremlin.Reader,
        _tid: i64 = 0,
        _pid: i64 = 0,
        _comm: ?[]const u8 = null,
        _is_main_thread: bool = false,
        pub fn init(src: []const u8) gremlin.Error!GenericKernelProcessTree.ThreadReader {
            const buf = gremlin.Reader.init(src);
            var res = GenericKernelProcessTree.ThreadReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GenericKernelProcessTree.ThreadWire.TID_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._tid = result.value;
                    },
                    GenericKernelProcessTree.ThreadWire.PID_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._pid = result.value;
                    },
                    GenericKernelProcessTree.ThreadWire.COMM_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._comm = result.value;
                    },
                    GenericKernelProcessTree.ThreadWire.IS_MAIN_THREAD_WIRE => {
                        const result = try buf.readBool(offset);
                        offset += result.size;
                        res._is_main_thread = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getTid(self: *const GenericKernelProcessTree.ThreadReader) i64 {
            return self._tid;
        }
        pub inline fn getPid(self: *const GenericKernelProcessTree.ThreadReader) i64 {
            return self._pid;
        }
        pub inline fn getComm(self: *const GenericKernelProcessTree.ThreadReader) []const u8 {
            return self._comm orelse &[_]u8{};
        }
        pub inline fn getIsMainThread(self: *const GenericKernelProcessTree.ThreadReader) bool {
            return self._is_main_thread;
        }
    };
    const ProcessWire = struct {
        const PID_WIRE: gremlin.ProtoWireNumber = 1;
        const PPID_WIRE: gremlin.ProtoWireNumber = 2;
        const CMDLINE_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const Process = struct {
        // fields
        pid: i64 = 0,
        ppid: i64 = 0,
        cmdline: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const GenericKernelProcessTree.Process) usize {
            var res: usize = 0;
            if (self.pid != 0) {
                res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTree.ProcessWire.PID_WIRE) + gremlin.sizes.sizeI64(self.pid);
            }
            if (self.ppid != 0) {
                res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTree.ProcessWire.PPID_WIRE) + gremlin.sizes.sizeI64(self.ppid);
            }
            if (self.cmdline) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTree.ProcessWire.CMDLINE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const GenericKernelProcessTree.Process, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GenericKernelProcessTree.Process, target: *gremlin.Writer) void {
            if (self.pid != 0) {
                target.appendInt64(GenericKernelProcessTree.ProcessWire.PID_WIRE, self.pid);
            }
            if (self.ppid != 0) {
                target.appendInt64(GenericKernelProcessTree.ProcessWire.PPID_WIRE, self.ppid);
            }
            if (self.cmdline) |v| {
                if (v.len > 0) {
                    target.appendBytes(GenericKernelProcessTree.ProcessWire.CMDLINE_WIRE, v);
                }
            }
        }
    };
    pub const ProcessReader = struct {
        buf: gremlin.Reader,
        _pid: i64 = 0,
        _ppid: i64 = 0,
        _cmdline: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!GenericKernelProcessTree.ProcessReader {
            const buf = gremlin.Reader.init(src);
            var res = GenericKernelProcessTree.ProcessReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GenericKernelProcessTree.ProcessWire.PID_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._pid = result.value;
                    },
                    GenericKernelProcessTree.ProcessWire.PPID_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._ppid = result.value;
                    },
                    GenericKernelProcessTree.ProcessWire.CMDLINE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._cmdline = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getPid(self: *const GenericKernelProcessTree.ProcessReader) i64 {
            return self._pid;
        }
        pub inline fn getPpid(self: *const GenericKernelProcessTree.ProcessReader) i64 {
            return self._ppid;
        }
        pub inline fn getCmdline(self: *const GenericKernelProcessTree.ProcessReader) []const u8 {
            return self._cmdline orelse &[_]u8{};
        }
    };
    // fields
    processes: ?[]const ?GenericKernelProcessTree.Process = null,
    threads: ?[]const ?GenericKernelProcessTree.Thread = null,
    pub fn calcProtobufSize(self: *const GenericKernelProcessTree) usize {
        var res: usize = 0;
        if (self.processes) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTreeWire.PROCESSES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.threads) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(GenericKernelProcessTreeWire.THREADS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const GenericKernelProcessTree, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GenericKernelProcessTree, target: *gremlin.Writer) void {
        if (self.processes) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(GenericKernelProcessTreeWire.PROCESSES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(GenericKernelProcessTreeWire.PROCESSES_WIRE, 0);
                }
            }
        }
        if (self.threads) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(GenericKernelProcessTreeWire.THREADS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(GenericKernelProcessTreeWire.THREADS_WIRE, 0);
                }
            }
        }
    }
};
pub const GenericKernelProcessTreeReader = struct {
    buf: gremlin.Reader,
    _processes_offset: ?usize = null,
    _processes_last_offset: ?usize = null,
    _processes_cnt: usize = 0,
    _threads_offset: ?usize = null,
    _threads_last_offset: ?usize = null,
    _threads_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!GenericKernelProcessTreeReader {
        const buf = gremlin.Reader.init(src);
        var res = GenericKernelProcessTreeReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GenericKernelProcessTreeWire.PROCESSES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._processes_offset == null) {
                        res._processes_offset = offset - result.size;
                    }
                    res._processes_last_offset = offset;
                    res._processes_cnt += 1;
                },
                GenericKernelProcessTreeWire.THREADS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._threads_offset == null) {
                        res._threads_offset = offset - result.size;
                    }
                    res._threads_last_offset = offset;
                    res._threads_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn processesCount(self: *const GenericKernelProcessTreeReader) usize {
        return self._processes_cnt;
    }
    pub fn processesNext(self: *GenericKernelProcessTreeReader) ?GenericKernelProcessTree.ProcessReader {
        if (self._processes_offset == null) return null;
        const current_offset = self._processes_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = GenericKernelProcessTree.ProcessReader.init(result.value) catch return null;
        if (self._processes_last_offset != null and current_offset >= self._processes_last_offset.?) {
            self._processes_offset = null;
            return msg;
        }
        if (self._processes_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._processes_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == GenericKernelProcessTreeWire.PROCESSES_WIRE) {
                self._processes_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._processes_offset = null;
        return msg;
    }
    pub fn threadsCount(self: *const GenericKernelProcessTreeReader) usize {
        return self._threads_cnt;
    }
    pub fn threadsNext(self: *GenericKernelProcessTreeReader) ?GenericKernelProcessTree.ThreadReader {
        if (self._threads_offset == null) return null;
        const current_offset = self._threads_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = GenericKernelProcessTree.ThreadReader.init(result.value) catch return null;
        if (self._threads_last_offset != null and current_offset >= self._threads_last_offset.?) {
            self._threads_offset = null;
            return msg;
        }
        if (self._threads_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._threads_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == GenericKernelProcessTreeWire.THREADS_WIRE) {
                self._threads_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._threads_offset = null;
        return msg;
    }
};
