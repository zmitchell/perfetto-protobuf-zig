// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const GenericKernelCpuFrequencyEventWire = struct {
    const CPU_WIRE: gremlin.ProtoWireNumber = 1;
    const FREQ_HZ_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const GenericKernelCpuFrequencyEvent = struct {
    // fields
    cpu: i32 = 0,
    freq_hz: i64 = 0,
    pub fn calcProtobufSize(self: *const GenericKernelCpuFrequencyEvent) usize {
        var res: usize = 0;
        if (self.cpu != 0) {
            res += gremlin.sizes.sizeWireNumber(GenericKernelCpuFrequencyEventWire.CPU_WIRE) + gremlin.sizes.sizeI32(self.cpu);
        }
        if (self.freq_hz != 0) {
            res += gremlin.sizes.sizeWireNumber(GenericKernelCpuFrequencyEventWire.FREQ_HZ_WIRE) + gremlin.sizes.sizeI64(self.freq_hz);
        }
        return res;
    }
    pub fn encode(self: *const GenericKernelCpuFrequencyEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GenericKernelCpuFrequencyEvent, target: *gremlin.Writer) void {
        if (self.cpu != 0) {
            target.appendInt32(GenericKernelCpuFrequencyEventWire.CPU_WIRE, self.cpu);
        }
        if (self.freq_hz != 0) {
            target.appendInt64(GenericKernelCpuFrequencyEventWire.FREQ_HZ_WIRE, self.freq_hz);
        }
    }
};
pub const GenericKernelCpuFrequencyEventReader = struct {
    buf: gremlin.Reader,
    _cpu: i32 = 0,
    _freq_hz: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!GenericKernelCpuFrequencyEventReader {
        const buf = gremlin.Reader.init(src);
        var res = GenericKernelCpuFrequencyEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GenericKernelCpuFrequencyEventWire.CPU_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._cpu = result.value;
                },
                GenericKernelCpuFrequencyEventWire.FREQ_HZ_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._freq_hz = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCpu(self: *const GenericKernelCpuFrequencyEventReader) i32 {
        return self._cpu;
    }
    pub inline fn getFreqHz(self: *const GenericKernelCpuFrequencyEventReader) i64 {
        return self._freq_hz;
    }
};
