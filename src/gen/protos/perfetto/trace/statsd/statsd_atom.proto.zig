// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
pub const Atom = struct {
    pub fn calcProtobufSize(_: *const Atom) usize {
        return 0;
    }
    pub fn encode(self: *const Atom, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const Atom, _: *gremlin.Writer) void {}
};
pub const AtomReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!AtomReader {
        const buf = gremlin.Reader.init(src);
        return AtomReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const StatsdAtomWire = struct {
    const ATOM_WIRE: gremlin.ProtoWireNumber = 1;
    const TIMESTAMP_NANOS_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const StatsdAtom = struct {
    // fields
    atom: ?[]const ?Atom = null,
    timestamp_nanos: ?[]const i64 = null,
    pub fn calcProtobufSize(self: *const StatsdAtom) usize {
        var res: usize = 0;
        if (self.atom) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(StatsdAtomWire.ATOM_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.timestamp_nanos) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(StatsdAtomWire.TIMESTAMP_NANOS_WIRE) + gremlin.sizes.sizeI64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI64(v);
                }
                res += gremlin.sizes.sizeWireNumber(StatsdAtomWire.TIMESTAMP_NANOS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        return res;
    }
    pub fn encode(self: *const StatsdAtom, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const StatsdAtom, target: *gremlin.Writer) void {
        if (self.atom) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(StatsdAtomWire.ATOM_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(StatsdAtomWire.ATOM_WIRE, 0);
                }
            }
        }
        if (self.timestamp_nanos) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendInt64(StatsdAtomWire.TIMESTAMP_NANOS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI64(v);
                }
                target.appendBytesTag(StatsdAtomWire.TIMESTAMP_NANOS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendInt64WithoutTag(v);
                }
            }
        }
    }
};
pub const StatsdAtomReader = struct {
    buf: gremlin.Reader,
    _atom_offset: ?usize = null,
    _atom_last_offset: ?usize = null,
    _atom_cnt: usize = 0,
    _timestamp_nanos_offset: ?usize = null,
    _timestamp_nanos_last_offset: ?usize = null,
    _timestamp_nanos_packed: bool = false,
    pub fn init(src: []const u8) gremlin.Error!StatsdAtomReader {
        const buf = gremlin.Reader.init(src);
        var res = StatsdAtomReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                StatsdAtomWire.ATOM_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._atom_offset == null) {
                        res._atom_offset = offset - result.size;
                    }
                    res._atom_last_offset = offset;
                    res._atom_cnt += 1;
                },
                StatsdAtomWire.TIMESTAMP_NANOS_WIRE => {
                    if (res._timestamp_nanos_offset == null) {
                        res._timestamp_nanos_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._timestamp_nanos_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._timestamp_nanos_offset = offset + length_result.size;
                        res._timestamp_nanos_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._timestamp_nanos_last_offset.?;
                    } else {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._timestamp_nanos_last_offset = offset;
                    }
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn atomCount(self: *const StatsdAtomReader) usize {
        return self._atom_cnt;
    }
    pub fn atomNext(self: *StatsdAtomReader) ?AtomReader {
        if (self._atom_offset == null) return null;
        const current_offset = self._atom_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = AtomReader.init(result.value) catch return null;
        if (self._atom_last_offset != null and current_offset >= self._atom_last_offset.?) {
            self._atom_offset = null;
            return msg;
        }
        if (self._atom_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._atom_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == StatsdAtomWire.ATOM_WIRE) {
                self._atom_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._atom_offset = null;
        return msg;
    }
    pub fn timestampNanosNext(self: *StatsdAtomReader) gremlin.Error!?i64 {
        if (self._timestamp_nanos_offset == null) return null;
        const current_offset = self._timestamp_nanos_offset.?;
        if (current_offset >= self._timestamp_nanos_last_offset.?) {
            self._timestamp_nanos_offset = null;
            return null;
        }
        if (self._timestamp_nanos_packed) {
            const value_result = try self.buf.readInt64(current_offset);
            self._timestamp_nanos_offset = current_offset + value_result.size;
            if (self._timestamp_nanos_offset.? >= self._timestamp_nanos_last_offset.?) {
                self._timestamp_nanos_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._timestamp_nanos_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == StatsdAtomWire.TIMESTAMP_NANOS_WIRE) {
                    self._timestamp_nanos_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._timestamp_nanos_offset = null;
            return value_result.value;
        }
    }
};
