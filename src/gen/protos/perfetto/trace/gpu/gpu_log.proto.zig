// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const GpuLogWire = struct {
    const SEVERITY_WIRE: gremlin.ProtoWireNumber = 1;
    const TAG_WIRE: gremlin.ProtoWireNumber = 2;
    const LOG_MESSAGE_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const GpuLog = struct {
    // nested enums
    pub const Severity = enum(i32) {
        LOG_SEVERITY_UNSPECIFIED = 0,
        LOG_SEVERITY_VERBOSE = 1,
        LOG_SEVERITY_DEBUG = 2,
        LOG_SEVERITY_INFO = 3,
        LOG_SEVERITY_WARNING = 4,
        LOG_SEVERITY_ERROR = 5,
    };
    // fields
    severity: GpuLog.Severity = @enumFromInt(0),
    tag: ?[]const u8 = null,
    log_message: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const GpuLog) usize {
        var res: usize = 0;
        if (@intFromEnum(self.severity) != 0) {
            res += gremlin.sizes.sizeWireNumber(GpuLogWire.SEVERITY_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.severity));
        }
        if (self.tag) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(GpuLogWire.TAG_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.log_message) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(GpuLogWire.LOG_MESSAGE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const GpuLog, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GpuLog, target: *gremlin.Writer) void {
        if (@intFromEnum(self.severity) != 0) {
            target.appendInt32(GpuLogWire.SEVERITY_WIRE, @intFromEnum(self.severity));
        }
        if (self.tag) |v| {
            if (v.len > 0) {
                target.appendBytes(GpuLogWire.TAG_WIRE, v);
            }
        }
        if (self.log_message) |v| {
            if (v.len > 0) {
                target.appendBytes(GpuLogWire.LOG_MESSAGE_WIRE, v);
            }
        }
    }
};
pub const GpuLogReader = struct {
    buf: gremlin.Reader,
    _severity: GpuLog.Severity = @enumFromInt(0),
    _tag: ?[]const u8 = null,
    _log_message: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!GpuLogReader {
        const buf = gremlin.Reader.init(src);
        var res = GpuLogReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GpuLogWire.SEVERITY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._severity = @enumFromInt(result.value);
                },
                GpuLogWire.TAG_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._tag = result.value;
                },
                GpuLogWire.LOG_MESSAGE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._log_message = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getSeverity(self: *const GpuLogReader) GpuLog.Severity {
        return self._severity;
    }
    pub inline fn getTag(self: *const GpuLogReader) []const u8 {
        return self._tag orelse &[_]u8{};
    }
    pub inline fn getLogMessage(self: *const GpuLogReader) []const u8 {
        return self._log_message orelse &[_]u8{};
    }
};
