// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const gpu_counter_descriptor = @import("src/gen/protos/perfetto/common/gpu_counter_descriptor.proto.zig");
// structs
const GpuCounterEventWire = struct {
    const COUNTER_DESCRIPTOR_WIRE: gremlin.ProtoWireNumber = 1;
    const COUNTERS_WIRE: gremlin.ProtoWireNumber = 2;
    const GPU_ID_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const GpuCounterEvent = struct {
    // nested structs
    const GpuCounterWire = struct {
        const COUNTER_ID_WIRE: gremlin.ProtoWireNumber = 1;
        const INT_VALUE_WIRE: gremlin.ProtoWireNumber = 2;
        const DOUBLE_VALUE_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const GpuCounter = struct {
        // fields
        counter_id: u32 = 0,
        int_value: i64 = 0,
        double_value: f64 = 0.0,
        pub fn calcProtobufSize(self: *const GpuCounterEvent.GpuCounter) usize {
            var res: usize = 0;
            if (self.counter_id != 0) {
                res += gremlin.sizes.sizeWireNumber(GpuCounterEvent.GpuCounterWire.COUNTER_ID_WIRE) + gremlin.sizes.sizeU32(self.counter_id);
            }
            if (self.int_value != 0) {
                res += gremlin.sizes.sizeWireNumber(GpuCounterEvent.GpuCounterWire.INT_VALUE_WIRE) + gremlin.sizes.sizeI64(self.int_value);
            }
            if (self.double_value != 0.0) {
                res += gremlin.sizes.sizeWireNumber(GpuCounterEvent.GpuCounterWire.DOUBLE_VALUE_WIRE) + gremlin.sizes.sizeDouble(self.double_value);
            }
            return res;
        }
        pub fn encode(self: *const GpuCounterEvent.GpuCounter, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GpuCounterEvent.GpuCounter, target: *gremlin.Writer) void {
            if (self.counter_id != 0) {
                target.appendUint32(GpuCounterEvent.GpuCounterWire.COUNTER_ID_WIRE, self.counter_id);
            }
            if (self.int_value != 0) {
                target.appendInt64(GpuCounterEvent.GpuCounterWire.INT_VALUE_WIRE, self.int_value);
            }
            if (self.double_value != 0.0) {
                target.appendFloat64(GpuCounterEvent.GpuCounterWire.DOUBLE_VALUE_WIRE, self.double_value);
            }
        }
    };
    pub const GpuCounterReader = struct {
        buf: gremlin.Reader,
        _counter_id: u32 = 0,
        _int_value: i64 = 0,
        _double_value: f64 = 0.0,
        pub fn init(src: []const u8) gremlin.Error!GpuCounterEvent.GpuCounterReader {
            const buf = gremlin.Reader.init(src);
            var res = GpuCounterEvent.GpuCounterReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GpuCounterEvent.GpuCounterWire.COUNTER_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._counter_id = result.value;
                    },
                    GpuCounterEvent.GpuCounterWire.INT_VALUE_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._int_value = result.value;
                    },
                    GpuCounterEvent.GpuCounterWire.DOUBLE_VALUE_WIRE => {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._double_value = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getCounterId(self: *const GpuCounterEvent.GpuCounterReader) u32 {
            return self._counter_id;
        }
        pub inline fn getIntValue(self: *const GpuCounterEvent.GpuCounterReader) i64 {
            return self._int_value;
        }
        pub inline fn getDoubleValue(self: *const GpuCounterEvent.GpuCounterReader) f64 {
            return self._double_value;
        }
    };
    // fields
    counter_descriptor: ?gpu_counter_descriptor.GpuCounterDescriptor = null,
    counters: ?[]const ?GpuCounterEvent.GpuCounter = null,
    gpu_id: i32 = 0,
    pub fn calcProtobufSize(self: *const GpuCounterEvent) usize {
        var res: usize = 0;
        if (self.counter_descriptor) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GpuCounterEventWire.COUNTER_DESCRIPTOR_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.counters) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(GpuCounterEventWire.COUNTERS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.gpu_id != 0) {
            res += gremlin.sizes.sizeWireNumber(GpuCounterEventWire.GPU_ID_WIRE) + gremlin.sizes.sizeI32(self.gpu_id);
        }
        return res;
    }
    pub fn encode(self: *const GpuCounterEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GpuCounterEvent, target: *gremlin.Writer) void {
        if (self.counter_descriptor) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GpuCounterEventWire.COUNTER_DESCRIPTOR_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.counters) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(GpuCounterEventWire.COUNTERS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(GpuCounterEventWire.COUNTERS_WIRE, 0);
                }
            }
        }
        if (self.gpu_id != 0) {
            target.appendInt32(GpuCounterEventWire.GPU_ID_WIRE, self.gpu_id);
        }
    }
};
pub const GpuCounterEventReader = struct {
    buf: gremlin.Reader,
    _counter_descriptor_buf: ?[]const u8 = null,
    _counters_offset: ?usize = null,
    _counters_last_offset: ?usize = null,
    _counters_cnt: usize = 0,
    _gpu_id: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!GpuCounterEventReader {
        const buf = gremlin.Reader.init(src);
        var res = GpuCounterEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GpuCounterEventWire.COUNTER_DESCRIPTOR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._counter_descriptor_buf = result.value;
                },
                GpuCounterEventWire.COUNTERS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._counters_offset == null) {
                        res._counters_offset = offset - result.size;
                    }
                    res._counters_last_offset = offset;
                    res._counters_cnt += 1;
                },
                GpuCounterEventWire.GPU_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._gpu_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getCounterDescriptor(self: *const GpuCounterEventReader) gremlin.Error!gpu_counter_descriptor.GpuCounterDescriptorReader {
        if (self._counter_descriptor_buf) |buf| {
            return try gpu_counter_descriptor.GpuCounterDescriptorReader.init(buf);
        }
        return try gpu_counter_descriptor.GpuCounterDescriptorReader.init(&[_]u8{});
    }
    pub fn countersCount(self: *const GpuCounterEventReader) usize {
        return self._counters_cnt;
    }
    pub fn countersNext(self: *GpuCounterEventReader) ?GpuCounterEvent.GpuCounterReader {
        if (self._counters_offset == null) return null;
        const current_offset = self._counters_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = GpuCounterEvent.GpuCounterReader.init(result.value) catch return null;
        if (self._counters_last_offset != null and current_offset >= self._counters_last_offset.?) {
            self._counters_offset = null;
            return msg;
        }
        if (self._counters_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._counters_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == GpuCounterEventWire.COUNTERS_WIRE) {
                self._counters_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._counters_offset = null;
        return msg;
    }
    pub inline fn getGpuId(self: *const GpuCounterEventReader) i32 {
        return self._gpu_id;
    }
};
