// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const VulkanApiEventWire = struct {
    const VK_DEBUG_UTILS_OBJECT_NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const VK_QUEUE_SUBMIT_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const VulkanApiEvent = struct {
    // nested structs
    const VkDebugUtilsObjectNameWire = struct {
        const PID_WIRE: gremlin.ProtoWireNumber = 1;
        const VK_DEVICE_WIRE: gremlin.ProtoWireNumber = 2;
        const OBJECT_TYPE_WIRE: gremlin.ProtoWireNumber = 3;
        const OBJECT_WIRE: gremlin.ProtoWireNumber = 4;
        const OBJECT_NAME_WIRE: gremlin.ProtoWireNumber = 5;
    };
    pub const VkDebugUtilsObjectName = struct {
        // fields
        pid: u32 = 0,
        vk_device: u64 = 0,
        object_type: i32 = 0,
        object: u64 = 0,
        object_name: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const VulkanApiEvent.VkDebugUtilsObjectName) usize {
            var res: usize = 0;
            if (self.pid != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkDebugUtilsObjectNameWire.PID_WIRE) + gremlin.sizes.sizeU32(self.pid);
            }
            if (self.vk_device != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkDebugUtilsObjectNameWire.VK_DEVICE_WIRE) + gremlin.sizes.sizeU64(self.vk_device);
            }
            if (self.object_type != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_TYPE_WIRE) + gremlin.sizes.sizeI32(self.object_type);
            }
            if (self.object != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_WIRE) + gremlin.sizes.sizeU64(self.object);
            }
            if (self.object_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const VulkanApiEvent.VkDebugUtilsObjectName, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const VulkanApiEvent.VkDebugUtilsObjectName, target: *gremlin.Writer) void {
            if (self.pid != 0) {
                target.appendUint32(VulkanApiEvent.VkDebugUtilsObjectNameWire.PID_WIRE, self.pid);
            }
            if (self.vk_device != 0) {
                target.appendUint64(VulkanApiEvent.VkDebugUtilsObjectNameWire.VK_DEVICE_WIRE, self.vk_device);
            }
            if (self.object_type != 0) {
                target.appendInt32(VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_TYPE_WIRE, self.object_type);
            }
            if (self.object != 0) {
                target.appendUint64(VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_WIRE, self.object);
            }
            if (self.object_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_NAME_WIRE, v);
                }
            }
        }
    };
    pub const VkDebugUtilsObjectNameReader = struct {
        buf: gremlin.Reader,
        _pid: u32 = 0,
        _vk_device: u64 = 0,
        _object_type: i32 = 0,
        _object: u64 = 0,
        _object_name: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!VulkanApiEvent.VkDebugUtilsObjectNameReader {
            const buf = gremlin.Reader.init(src);
            var res = VulkanApiEvent.VkDebugUtilsObjectNameReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    VulkanApiEvent.VkDebugUtilsObjectNameWire.PID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._pid = result.value;
                    },
                    VulkanApiEvent.VkDebugUtilsObjectNameWire.VK_DEVICE_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._vk_device = result.value;
                    },
                    VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_TYPE_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._object_type = result.value;
                    },
                    VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._object = result.value;
                    },
                    VulkanApiEvent.VkDebugUtilsObjectNameWire.OBJECT_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._object_name = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getPid(self: *const VulkanApiEvent.VkDebugUtilsObjectNameReader) u32 {
            return self._pid;
        }
        pub inline fn getVkDevice(self: *const VulkanApiEvent.VkDebugUtilsObjectNameReader) u64 {
            return self._vk_device;
        }
        pub inline fn getObjectType(self: *const VulkanApiEvent.VkDebugUtilsObjectNameReader) i32 {
            return self._object_type;
        }
        pub inline fn getObject(self: *const VulkanApiEvent.VkDebugUtilsObjectNameReader) u64 {
            return self._object;
        }
        pub inline fn getObjectName(self: *const VulkanApiEvent.VkDebugUtilsObjectNameReader) []const u8 {
            return self._object_name orelse &[_]u8{};
        }
    };
    const VkQueueSubmitWire = struct {
        const DURATION_NS_WIRE: gremlin.ProtoWireNumber = 1;
        const PID_WIRE: gremlin.ProtoWireNumber = 2;
        const TID_WIRE: gremlin.ProtoWireNumber = 3;
        const VK_QUEUE_WIRE: gremlin.ProtoWireNumber = 4;
        const VK_COMMAND_BUFFERS_WIRE: gremlin.ProtoWireNumber = 5;
        const SUBMISSION_ID_WIRE: gremlin.ProtoWireNumber = 6;
    };
    pub const VkQueueSubmit = struct {
        // fields
        duration_ns: u64 = 0,
        pid: u32 = 0,
        tid: u32 = 0,
        vk_queue: u64 = 0,
        vk_command_buffers: ?[]const u64 = null,
        submission_id: u32 = 0,
        pub fn calcProtobufSize(self: *const VulkanApiEvent.VkQueueSubmit) usize {
            var res: usize = 0;
            if (self.duration_ns != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkQueueSubmitWire.DURATION_NS_WIRE) + gremlin.sizes.sizeU64(self.duration_ns);
            }
            if (self.pid != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkQueueSubmitWire.PID_WIRE) + gremlin.sizes.sizeU32(self.pid);
            }
            if (self.tid != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkQueueSubmitWire.TID_WIRE) + gremlin.sizes.sizeU32(self.tid);
            }
            if (self.vk_queue != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkQueueSubmitWire.VK_QUEUE_WIRE) + gremlin.sizes.sizeU64(self.vk_queue);
            }
            if (self.vk_command_buffers) |arr| {
                if (arr.len == 0) {} else if (arr.len == 1) {
                    res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkQueueSubmitWire.VK_COMMAND_BUFFERS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
                } else {
                    var packed_size: usize = 0;
                    for (arr) |v| {
                        packed_size += gremlin.sizes.sizeU64(v);
                    }
                    res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkQueueSubmitWire.VK_COMMAND_BUFFERS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
                }
            }
            if (self.submission_id != 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEvent.VkQueueSubmitWire.SUBMISSION_ID_WIRE) + gremlin.sizes.sizeU32(self.submission_id);
            }
            return res;
        }
        pub fn encode(self: *const VulkanApiEvent.VkQueueSubmit, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const VulkanApiEvent.VkQueueSubmit, target: *gremlin.Writer) void {
            if (self.duration_ns != 0) {
                target.appendUint64(VulkanApiEvent.VkQueueSubmitWire.DURATION_NS_WIRE, self.duration_ns);
            }
            if (self.pid != 0) {
                target.appendUint32(VulkanApiEvent.VkQueueSubmitWire.PID_WIRE, self.pid);
            }
            if (self.tid != 0) {
                target.appendUint32(VulkanApiEvent.VkQueueSubmitWire.TID_WIRE, self.tid);
            }
            if (self.vk_queue != 0) {
                target.appendUint64(VulkanApiEvent.VkQueueSubmitWire.VK_QUEUE_WIRE, self.vk_queue);
            }
            if (self.vk_command_buffers) |arr| {
                if (arr.len == 0) {} else if (arr.len == 1) {
                    target.appendUint64(VulkanApiEvent.VkQueueSubmitWire.VK_COMMAND_BUFFERS_WIRE, arr[0]);
                } else {
                    var packed_size: usize = 0;
                    for (arr) |v| {
                        packed_size += gremlin.sizes.sizeU64(v);
                    }
                    target.appendBytesTag(VulkanApiEvent.VkQueueSubmitWire.VK_COMMAND_BUFFERS_WIRE, packed_size);
                    for (arr) |v| {
                        target.appendUint64WithoutTag(v);
                    }
                }
            }
            if (self.submission_id != 0) {
                target.appendUint32(VulkanApiEvent.VkQueueSubmitWire.SUBMISSION_ID_WIRE, self.submission_id);
            }
        }
    };
    pub const VkQueueSubmitReader = struct {
        buf: gremlin.Reader,
        _duration_ns: u64 = 0,
        _pid: u32 = 0,
        _tid: u32 = 0,
        _vk_queue: u64 = 0,
        _vk_command_buffers_offset: ?usize = null,
        _vk_command_buffers_last_offset: ?usize = null,
        _vk_command_buffers_packed: bool = false,
        _submission_id: u32 = 0,
        pub fn init(src: []const u8) gremlin.Error!VulkanApiEvent.VkQueueSubmitReader {
            const buf = gremlin.Reader.init(src);
            var res = VulkanApiEvent.VkQueueSubmitReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    VulkanApiEvent.VkQueueSubmitWire.DURATION_NS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._duration_ns = result.value;
                    },
                    VulkanApiEvent.VkQueueSubmitWire.PID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._pid = result.value;
                    },
                    VulkanApiEvent.VkQueueSubmitWire.TID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._tid = result.value;
                    },
                    VulkanApiEvent.VkQueueSubmitWire.VK_QUEUE_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._vk_queue = result.value;
                    },
                    VulkanApiEvent.VkQueueSubmitWire.VK_COMMAND_BUFFERS_WIRE => {
                        if (res._vk_command_buffers_offset == null) {
                            res._vk_command_buffers_offset = offset;
                        }
                        if (tag.wire == gremlin.ProtoWireType.bytes) {
                            res._vk_command_buffers_packed = true;
                            const length_result = try buf.readVarInt(offset);
                            res._vk_command_buffers_offset = offset + length_result.size;
                            res._vk_command_buffers_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                            offset = res._vk_command_buffers_last_offset.?;
                        } else {
                            const result = try buf.readUInt64(offset);
                            offset += result.size;
                            res._vk_command_buffers_last_offset = offset;
                        }
                    },
                    VulkanApiEvent.VkQueueSubmitWire.SUBMISSION_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._submission_id = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getDurationNs(self: *const VulkanApiEvent.VkQueueSubmitReader) u64 {
            return self._duration_ns;
        }
        pub inline fn getPid(self: *const VulkanApiEvent.VkQueueSubmitReader) u32 {
            return self._pid;
        }
        pub inline fn getTid(self: *const VulkanApiEvent.VkQueueSubmitReader) u32 {
            return self._tid;
        }
        pub inline fn getVkQueue(self: *const VulkanApiEvent.VkQueueSubmitReader) u64 {
            return self._vk_queue;
        }
        pub fn vkCommandBuffersNext(self: *VulkanApiEvent.VkQueueSubmitReader) gremlin.Error!?u64 {
            if (self._vk_command_buffers_offset == null) return null;
            const current_offset = self._vk_command_buffers_offset.?;
            if (current_offset >= self._vk_command_buffers_last_offset.?) {
                self._vk_command_buffers_offset = null;
                return null;
            }
            if (self._vk_command_buffers_packed) {
                const value_result = try self.buf.readUInt64(current_offset);
                self._vk_command_buffers_offset = current_offset + value_result.size;
                if (self._vk_command_buffers_offset.? >= self._vk_command_buffers_last_offset.?) {
                    self._vk_command_buffers_offset = null;
                }
                return value_result.value;
            } else {
                const value_result = try self.buf.readUInt64(current_offset);
                var next_offset = current_offset + value_result.size;
                const max_offset = self._vk_command_buffers_last_offset.?;
                // Search for the next occurrence of this field
                while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                    const next_tag = try self.buf.readTagAt(next_offset);
                    next_offset += next_tag.size;
                    if (next_tag.number == VulkanApiEvent.VkQueueSubmitWire.VK_COMMAND_BUFFERS_WIRE) {
                        self._vk_command_buffers_offset = next_offset;
                        return value_result.value;
                    } else {
                        next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                    }
                }
                self._vk_command_buffers_offset = null;
                return value_result.value;
            }
        }
        pub inline fn getSubmissionId(self: *const VulkanApiEvent.VkQueueSubmitReader) u32 {
            return self._submission_id;
        }
    };
    // fields
    vk_debug_utils_object_name: ?VulkanApiEvent.VkDebugUtilsObjectName = null,
    vk_queue_submit: ?VulkanApiEvent.VkQueueSubmit = null,
    pub fn calcProtobufSize(self: *const VulkanApiEvent) usize {
        var res: usize = 0;
        if (self.vk_debug_utils_object_name) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEventWire.VK_DEBUG_UTILS_OBJECT_NAME_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.vk_queue_submit) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(VulkanApiEventWire.VK_QUEUE_SUBMIT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const VulkanApiEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const VulkanApiEvent, target: *gremlin.Writer) void {
        if (self.vk_debug_utils_object_name) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(VulkanApiEventWire.VK_DEBUG_UTILS_OBJECT_NAME_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.vk_queue_submit) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(VulkanApiEventWire.VK_QUEUE_SUBMIT_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const VulkanApiEventReader = struct {
    buf: gremlin.Reader,
    _vk_debug_utils_object_name_buf: ?[]const u8 = null,
    _vk_queue_submit_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!VulkanApiEventReader {
        const buf = gremlin.Reader.init(src);
        var res = VulkanApiEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                VulkanApiEventWire.VK_DEBUG_UTILS_OBJECT_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._vk_debug_utils_object_name_buf = result.value;
                },
                VulkanApiEventWire.VK_QUEUE_SUBMIT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._vk_queue_submit_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getVkDebugUtilsObjectName(self: *const VulkanApiEventReader) gremlin.Error!VulkanApiEvent.VkDebugUtilsObjectNameReader {
        if (self._vk_debug_utils_object_name_buf) |buf| {
            return try VulkanApiEvent.VkDebugUtilsObjectNameReader.init(buf);
        }
        return try VulkanApiEvent.VkDebugUtilsObjectNameReader.init(&[_]u8{});
    }
    pub fn getVkQueueSubmit(self: *const VulkanApiEventReader) gremlin.Error!VulkanApiEvent.VkQueueSubmitReader {
        if (self._vk_queue_submit_buf) |buf| {
            return try VulkanApiEvent.VkQueueSubmitReader.init(buf);
        }
        return try VulkanApiEvent.VkQueueSubmitReader.init(&[_]u8{});
    }
};
