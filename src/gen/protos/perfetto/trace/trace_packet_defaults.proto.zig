// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const track_event = @import("src/gen/protos/perfetto/trace/track_event/track_event.proto.zig");
const profile_packet = @import("src/gen/protos/perfetto/trace/profiling/profile_packet.proto.zig");
const v8 = @import("src/gen/protos/perfetto/trace/chrome/v8.proto.zig");
// structs
const TracePacketDefaultsWire = struct {
    const TIMESTAMP_CLOCK_ID_WIRE: gremlin.ProtoWireNumber = 58;
    const TRACK_EVENT_DEFAULTS_WIRE: gremlin.ProtoWireNumber = 11;
    const PERF_SAMPLE_DEFAULTS_WIRE: gremlin.ProtoWireNumber = 12;
    const V8_CODE_DEFAULTS_WIRE: gremlin.ProtoWireNumber = 99;
};
pub const TracePacketDefaults = struct {
    // fields
    timestamp_clock_id: u32 = 0,
    track_event_defaults: ?track_event.TrackEventDefaults = null,
    perf_sample_defaults: ?profile_packet.PerfSampleDefaults = null,
    v8_code_defaults: ?v8.V8CodeDefaults = null,
    pub fn calcProtobufSize(self: *const TracePacketDefaults) usize {
        var res: usize = 0;
        if (self.timestamp_clock_id != 0) {
            res += gremlin.sizes.sizeWireNumber(TracePacketDefaultsWire.TIMESTAMP_CLOCK_ID_WIRE) + gremlin.sizes.sizeU32(self.timestamp_clock_id);
        }
        if (self.track_event_defaults) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(TracePacketDefaultsWire.TRACK_EVENT_DEFAULTS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.perf_sample_defaults) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(TracePacketDefaultsWire.PERF_SAMPLE_DEFAULTS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.v8_code_defaults) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(TracePacketDefaultsWire.V8_CODE_DEFAULTS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const TracePacketDefaults, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TracePacketDefaults, target: *gremlin.Writer) void {
        if (self.timestamp_clock_id != 0) {
            target.appendUint32(TracePacketDefaultsWire.TIMESTAMP_CLOCK_ID_WIRE, self.timestamp_clock_id);
        }
        if (self.track_event_defaults) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(TracePacketDefaultsWire.TRACK_EVENT_DEFAULTS_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.perf_sample_defaults) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(TracePacketDefaultsWire.PERF_SAMPLE_DEFAULTS_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.v8_code_defaults) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(TracePacketDefaultsWire.V8_CODE_DEFAULTS_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const TracePacketDefaultsReader = struct {
    buf: gremlin.Reader,
    _timestamp_clock_id: u32 = 0,
    _track_event_defaults_buf: ?[]const u8 = null,
    _perf_sample_defaults_buf: ?[]const u8 = null,
    _v8_code_defaults_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!TracePacketDefaultsReader {
        const buf = gremlin.Reader.init(src);
        var res = TracePacketDefaultsReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TracePacketDefaultsWire.TIMESTAMP_CLOCK_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._timestamp_clock_id = result.value;
                },
                TracePacketDefaultsWire.TRACK_EVENT_DEFAULTS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._track_event_defaults_buf = result.value;
                },
                TracePacketDefaultsWire.PERF_SAMPLE_DEFAULTS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._perf_sample_defaults_buf = result.value;
                },
                TracePacketDefaultsWire.V8_CODE_DEFAULTS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._v8_code_defaults_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTimestampClockId(self: *const TracePacketDefaultsReader) u32 {
        return self._timestamp_clock_id;
    }
    pub fn getTrackEventDefaults(self: *const TracePacketDefaultsReader) gremlin.Error!track_event.TrackEventDefaultsReader {
        if (self._track_event_defaults_buf) |buf| {
            return try track_event.TrackEventDefaultsReader.init(buf);
        }
        return try track_event.TrackEventDefaultsReader.init(&[_]u8{});
    }
    pub fn getPerfSampleDefaults(self: *const TracePacketDefaultsReader) gremlin.Error!profile_packet.PerfSampleDefaultsReader {
        if (self._perf_sample_defaults_buf) |buf| {
            return try profile_packet.PerfSampleDefaultsReader.init(buf);
        }
        return try profile_packet.PerfSampleDefaultsReader.init(&[_]u8{});
    }
    pub fn getV8CodeDefaults(self: *const TracePacketDefaultsReader) gremlin.Error!v8.V8CodeDefaultsReader {
        if (self._v8_code_defaults_buf) |buf| {
            return try v8.V8CodeDefaultsReader.init(buf);
        }
        return try v8.V8CodeDefaultsReader.init(&[_]u8{});
    }
};
