// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ObservableEventsWire = struct {
    const INSTANCE_STATE_CHANGES_WIRE: gremlin.ProtoWireNumber = 1;
    const ALL_DATA_SOURCES_STARTED_WIRE: gremlin.ProtoWireNumber = 2;
    const CLONE_TRIGGER_HIT_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ObservableEvents = struct {
    // nested enums
    pub const Type = enum(i32) {
        TYPE_UNSPECIFIED = 0,
        TYPE_DATA_SOURCES_INSTANCES = 1,
        TYPE_ALL_DATA_SOURCES_STARTED = 2,
        TYPE_CLONE_TRIGGER_HIT = 4,
    };
    pub const DataSourceInstanceState = enum(i32) {
        DATA_SOURCE_INSTANCE_STATE_STOPPED = 1,
        DATA_SOURCE_INSTANCE_STATE_STARTED = 2,
        _PROTOBUF_UNKNOWN = 0,
    };
    // nested structs
    const DataSourceInstanceStateChangeWire = struct {
        const PRODUCER_NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const DATA_SOURCE_NAME_WIRE: gremlin.ProtoWireNumber = 2;
        const STATE_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const DataSourceInstanceStateChange = struct {
        // fields
        producer_name: ?[]const u8 = null,
        data_source_name: ?[]const u8 = null,
        state: ObservableEvents.DataSourceInstanceState = @enumFromInt(0),
        pub fn calcProtobufSize(self: *const ObservableEvents.DataSourceInstanceStateChange) usize {
            var res: usize = 0;
            if (self.producer_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ObservableEvents.DataSourceInstanceStateChangeWire.PRODUCER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.data_source_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ObservableEvents.DataSourceInstanceStateChangeWire.DATA_SOURCE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (@intFromEnum(self.state) != 0) {
                res += gremlin.sizes.sizeWireNumber(ObservableEvents.DataSourceInstanceStateChangeWire.STATE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.state));
            }
            return res;
        }
        pub fn encode(self: *const ObservableEvents.DataSourceInstanceStateChange, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ObservableEvents.DataSourceInstanceStateChange, target: *gremlin.Writer) void {
            if (self.producer_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(ObservableEvents.DataSourceInstanceStateChangeWire.PRODUCER_NAME_WIRE, v);
                }
            }
            if (self.data_source_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(ObservableEvents.DataSourceInstanceStateChangeWire.DATA_SOURCE_NAME_WIRE, v);
                }
            }
            if (@intFromEnum(self.state) != 0) {
                target.appendInt32(ObservableEvents.DataSourceInstanceStateChangeWire.STATE_WIRE, @intFromEnum(self.state));
            }
        }
    };
    pub const DataSourceInstanceStateChangeReader = struct {
        buf: gremlin.Reader,
        _producer_name: ?[]const u8 = null,
        _data_source_name: ?[]const u8 = null,
        _state: ObservableEvents.DataSourceInstanceState = @enumFromInt(0),
        pub fn init(src: []const u8) gremlin.Error!ObservableEvents.DataSourceInstanceStateChangeReader {
            const buf = gremlin.Reader.init(src);
            var res = ObservableEvents.DataSourceInstanceStateChangeReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ObservableEvents.DataSourceInstanceStateChangeWire.PRODUCER_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._producer_name = result.value;
                    },
                    ObservableEvents.DataSourceInstanceStateChangeWire.DATA_SOURCE_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._data_source_name = result.value;
                    },
                    ObservableEvents.DataSourceInstanceStateChangeWire.STATE_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._state = @enumFromInt(result.value);
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getProducerName(self: *const ObservableEvents.DataSourceInstanceStateChangeReader) []const u8 {
            return self._producer_name orelse &[_]u8{};
        }
        pub inline fn getDataSourceName(self: *const ObservableEvents.DataSourceInstanceStateChangeReader) []const u8 {
            return self._data_source_name orelse &[_]u8{};
        }
        pub inline fn getState(self: *const ObservableEvents.DataSourceInstanceStateChangeReader) ObservableEvents.DataSourceInstanceState {
            return self._state;
        }
    };
    const CloneTriggerHitWire = struct {
        const TRACING_SESSION_ID_WIRE: gremlin.ProtoWireNumber = 1;
        const TRIGGER_NAME_WIRE: gremlin.ProtoWireNumber = 2;
        const PRODUCER_NAME_WIRE: gremlin.ProtoWireNumber = 3;
        const PRODUCER_UID_WIRE: gremlin.ProtoWireNumber = 4;
        const BOOT_TIME_NS_WIRE: gremlin.ProtoWireNumber = 5;
        const TRIGGER_DELAY_MS_WIRE: gremlin.ProtoWireNumber = 6;
    };
    pub const CloneTriggerHit = struct {
        // fields
        tracing_session_id: i64 = 0,
        trigger_name: ?[]const u8 = null,
        producer_name: ?[]const u8 = null,
        producer_uid: u32 = 0,
        boot_time_ns: u64 = 0,
        trigger_delay_ms: u64 = 0,
        pub fn calcProtobufSize(self: *const ObservableEvents.CloneTriggerHit) usize {
            var res: usize = 0;
            if (self.tracing_session_id != 0) {
                res += gremlin.sizes.sizeWireNumber(ObservableEvents.CloneTriggerHitWire.TRACING_SESSION_ID_WIRE) + gremlin.sizes.sizeI64(self.tracing_session_id);
            }
            if (self.trigger_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ObservableEvents.CloneTriggerHitWire.TRIGGER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.producer_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ObservableEvents.CloneTriggerHitWire.PRODUCER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.producer_uid != 0) {
                res += gremlin.sizes.sizeWireNumber(ObservableEvents.CloneTriggerHitWire.PRODUCER_UID_WIRE) + gremlin.sizes.sizeU32(self.producer_uid);
            }
            if (self.boot_time_ns != 0) {
                res += gremlin.sizes.sizeWireNumber(ObservableEvents.CloneTriggerHitWire.BOOT_TIME_NS_WIRE) + gremlin.sizes.sizeU64(self.boot_time_ns);
            }
            if (self.trigger_delay_ms != 0) {
                res += gremlin.sizes.sizeWireNumber(ObservableEvents.CloneTriggerHitWire.TRIGGER_DELAY_MS_WIRE) + gremlin.sizes.sizeU64(self.trigger_delay_ms);
            }
            return res;
        }
        pub fn encode(self: *const ObservableEvents.CloneTriggerHit, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ObservableEvents.CloneTriggerHit, target: *gremlin.Writer) void {
            if (self.tracing_session_id != 0) {
                target.appendInt64(ObservableEvents.CloneTriggerHitWire.TRACING_SESSION_ID_WIRE, self.tracing_session_id);
            }
            if (self.trigger_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(ObservableEvents.CloneTriggerHitWire.TRIGGER_NAME_WIRE, v);
                }
            }
            if (self.producer_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(ObservableEvents.CloneTriggerHitWire.PRODUCER_NAME_WIRE, v);
                }
            }
            if (self.producer_uid != 0) {
                target.appendUint32(ObservableEvents.CloneTriggerHitWire.PRODUCER_UID_WIRE, self.producer_uid);
            }
            if (self.boot_time_ns != 0) {
                target.appendUint64(ObservableEvents.CloneTriggerHitWire.BOOT_TIME_NS_WIRE, self.boot_time_ns);
            }
            if (self.trigger_delay_ms != 0) {
                target.appendUint64(ObservableEvents.CloneTriggerHitWire.TRIGGER_DELAY_MS_WIRE, self.trigger_delay_ms);
            }
        }
    };
    pub const CloneTriggerHitReader = struct {
        buf: gremlin.Reader,
        _tracing_session_id: i64 = 0,
        _trigger_name: ?[]const u8 = null,
        _producer_name: ?[]const u8 = null,
        _producer_uid: u32 = 0,
        _boot_time_ns: u64 = 0,
        _trigger_delay_ms: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!ObservableEvents.CloneTriggerHitReader {
            const buf = gremlin.Reader.init(src);
            var res = ObservableEvents.CloneTriggerHitReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ObservableEvents.CloneTriggerHitWire.TRACING_SESSION_ID_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._tracing_session_id = result.value;
                    },
                    ObservableEvents.CloneTriggerHitWire.TRIGGER_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._trigger_name = result.value;
                    },
                    ObservableEvents.CloneTriggerHitWire.PRODUCER_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._producer_name = result.value;
                    },
                    ObservableEvents.CloneTriggerHitWire.PRODUCER_UID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._producer_uid = result.value;
                    },
                    ObservableEvents.CloneTriggerHitWire.BOOT_TIME_NS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._boot_time_ns = result.value;
                    },
                    ObservableEvents.CloneTriggerHitWire.TRIGGER_DELAY_MS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._trigger_delay_ms = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getTracingSessionId(self: *const ObservableEvents.CloneTriggerHitReader) i64 {
            return self._tracing_session_id;
        }
        pub inline fn getTriggerName(self: *const ObservableEvents.CloneTriggerHitReader) []const u8 {
            return self._trigger_name orelse &[_]u8{};
        }
        pub inline fn getProducerName(self: *const ObservableEvents.CloneTriggerHitReader) []const u8 {
            return self._producer_name orelse &[_]u8{};
        }
        pub inline fn getProducerUid(self: *const ObservableEvents.CloneTriggerHitReader) u32 {
            return self._producer_uid;
        }
        pub inline fn getBootTimeNs(self: *const ObservableEvents.CloneTriggerHitReader) u64 {
            return self._boot_time_ns;
        }
        pub inline fn getTriggerDelayMs(self: *const ObservableEvents.CloneTriggerHitReader) u64 {
            return self._trigger_delay_ms;
        }
    };
    // fields
    instance_state_changes: ?[]const ?ObservableEvents.DataSourceInstanceStateChange = null,
    all_data_sources_started: bool = false,
    clone_trigger_hit: ?ObservableEvents.CloneTriggerHit = null,
    pub fn calcProtobufSize(self: *const ObservableEvents) usize {
        var res: usize = 0;
        if (self.instance_state_changes) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ObservableEventsWire.INSTANCE_STATE_CHANGES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.all_data_sources_started != false) {
            res += gremlin.sizes.sizeWireNumber(ObservableEventsWire.ALL_DATA_SOURCES_STARTED_WIRE) + gremlin.sizes.sizeBool(self.all_data_sources_started);
        }
        if (self.clone_trigger_hit) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(ObservableEventsWire.CLONE_TRIGGER_HIT_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const ObservableEvents, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ObservableEvents, target: *gremlin.Writer) void {
        if (self.instance_state_changes) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ObservableEventsWire.INSTANCE_STATE_CHANGES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ObservableEventsWire.INSTANCE_STATE_CHANGES_WIRE, 0);
                }
            }
        }
        if (self.all_data_sources_started != false) {
            target.appendBool(ObservableEventsWire.ALL_DATA_SOURCES_STARTED_WIRE, self.all_data_sources_started);
        }
        if (self.clone_trigger_hit) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(ObservableEventsWire.CLONE_TRIGGER_HIT_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const ObservableEventsReader = struct {
    buf: gremlin.Reader,
    _instance_state_changes_offset: ?usize = null,
    _instance_state_changes_last_offset: ?usize = null,
    _instance_state_changes_cnt: usize = 0,
    _all_data_sources_started: bool = false,
    _clone_trigger_hit_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ObservableEventsReader {
        const buf = gremlin.Reader.init(src);
        var res = ObservableEventsReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ObservableEventsWire.INSTANCE_STATE_CHANGES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._instance_state_changes_offset == null) {
                        res._instance_state_changes_offset = offset - result.size;
                    }
                    res._instance_state_changes_last_offset = offset;
                    res._instance_state_changes_cnt += 1;
                },
                ObservableEventsWire.ALL_DATA_SOURCES_STARTED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._all_data_sources_started = result.value;
                },
                ObservableEventsWire.CLONE_TRIGGER_HIT_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._clone_trigger_hit_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn instanceStateChangesCount(self: *const ObservableEventsReader) usize {
        return self._instance_state_changes_cnt;
    }
    pub fn instanceStateChangesNext(self: *ObservableEventsReader) ?ObservableEvents.DataSourceInstanceStateChangeReader {
        if (self._instance_state_changes_offset == null) return null;
        const current_offset = self._instance_state_changes_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ObservableEvents.DataSourceInstanceStateChangeReader.init(result.value) catch return null;
        if (self._instance_state_changes_last_offset != null and current_offset >= self._instance_state_changes_last_offset.?) {
            self._instance_state_changes_offset = null;
            return msg;
        }
        if (self._instance_state_changes_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._instance_state_changes_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ObservableEventsWire.INSTANCE_STATE_CHANGES_WIRE) {
                self._instance_state_changes_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._instance_state_changes_offset = null;
        return msg;
    }
    pub inline fn getAllDataSourcesStarted(self: *const ObservableEventsReader) bool {
        return self._all_data_sources_started;
    }
    pub fn getCloneTriggerHit(self: *const ObservableEventsReader) gremlin.Error!ObservableEvents.CloneTriggerHitReader {
        if (self._clone_trigger_hit_buf) |buf| {
            return try ObservableEvents.CloneTriggerHitReader.init(buf);
        }
        return try ObservableEvents.CloneTriggerHitReader.init(&[_]u8{});
    }
};
