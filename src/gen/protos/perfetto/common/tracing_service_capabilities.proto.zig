// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const observable_events = @import("observable_events.proto.zig");
// structs
const TracingServiceCapabilitiesWire = struct {
    const HAS_QUERY_CAPABILITIES_WIRE: gremlin.ProtoWireNumber = 1;
    const OBSERVABLE_EVENTS_WIRE: gremlin.ProtoWireNumber = 2;
    const HAS_TRACE_CONFIG_OUTPUT_PATH_WIRE: gremlin.ProtoWireNumber = 3;
    const HAS_CLONE_SESSION_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const TracingServiceCapabilities = struct {
    // fields
    has_query_capabilities: bool = false,
    observable_events: ?[]const observable_events.ObservableEvents.Type = null,
    has_trace_config_output_path: bool = false,
    has_clone_session: bool = false,
    pub fn calcProtobufSize(self: *const TracingServiceCapabilities) usize {
        var res: usize = 0;
        if (self.has_query_capabilities != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceCapabilitiesWire.HAS_QUERY_CAPABILITIES_WIRE) + gremlin.sizes.sizeBool(self.has_query_capabilities);
        }
        if (self.observable_events) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(TracingServiceCapabilitiesWire.OBSERVABLE_EVENTS_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(arr[0]));
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI32(@intFromEnum(v));
                }
                res += gremlin.sizes.sizeWireNumber(TracingServiceCapabilitiesWire.OBSERVABLE_EVENTS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.has_trace_config_output_path != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceCapabilitiesWire.HAS_TRACE_CONFIG_OUTPUT_PATH_WIRE) + gremlin.sizes.sizeBool(self.has_trace_config_output_path);
        }
        if (self.has_clone_session != false) {
            res += gremlin.sizes.sizeWireNumber(TracingServiceCapabilitiesWire.HAS_CLONE_SESSION_WIRE) + gremlin.sizes.sizeBool(self.has_clone_session);
        }
        return res;
    }
    pub fn encode(self: *const TracingServiceCapabilities, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TracingServiceCapabilities, target: *gremlin.Writer) void {
        if (self.has_query_capabilities != false) {
            target.appendBool(TracingServiceCapabilitiesWire.HAS_QUERY_CAPABILITIES_WIRE, self.has_query_capabilities);
        }
        if (self.observable_events) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendInt32(TracingServiceCapabilitiesWire.OBSERVABLE_EVENTS_WIRE, @intFromEnum(arr[0]));
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI32(@intFromEnum(v));
                }
                target.appendBytesTag(TracingServiceCapabilitiesWire.OBSERVABLE_EVENTS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendInt32WithoutTag(@intFromEnum(v));
                }
            }
        }
        if (self.has_trace_config_output_path != false) {
            target.appendBool(TracingServiceCapabilitiesWire.HAS_TRACE_CONFIG_OUTPUT_PATH_WIRE, self.has_trace_config_output_path);
        }
        if (self.has_clone_session != false) {
            target.appendBool(TracingServiceCapabilitiesWire.HAS_CLONE_SESSION_WIRE, self.has_clone_session);
        }
    }
};
pub const TracingServiceCapabilitiesReader = struct {
    buf: gremlin.Reader,
    _has_query_capabilities: bool = false,
    _observable_events_offset: ?usize = null,
    _observable_events_last_offset: ?usize = null,
    _observable_events_packed: bool = false,
    _has_trace_config_output_path: bool = false,
    _has_clone_session: bool = false,
    pub fn init(src: []const u8) gremlin.Error!TracingServiceCapabilitiesReader {
        const buf = gremlin.Reader.init(src);
        var res = TracingServiceCapabilitiesReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TracingServiceCapabilitiesWire.HAS_QUERY_CAPABILITIES_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._has_query_capabilities = result.value;
                },
                TracingServiceCapabilitiesWire.OBSERVABLE_EVENTS_WIRE => {
                    if (res._observable_events_offset == null) {
                        res._observable_events_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._observable_events_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._observable_events_offset = offset + length_result.size;
                        res._observable_events_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._observable_events_last_offset.?;
                    } else {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._observable_events_last_offset = offset;
                    }
                },
                TracingServiceCapabilitiesWire.HAS_TRACE_CONFIG_OUTPUT_PATH_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._has_trace_config_output_path = result.value;
                },
                TracingServiceCapabilitiesWire.HAS_CLONE_SESSION_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._has_clone_session = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getHasQueryCapabilities(self: *const TracingServiceCapabilitiesReader) bool {
        return self._has_query_capabilities;
    }
    pub fn observableEventsNext(self: *TracingServiceCapabilitiesReader) gremlin.Error!?observable_events.ObservableEvents.Type {
        if (self._observable_events_offset == null) return null;
        const current_offset = self._observable_events_offset.?;
        if (current_offset >= self._observable_events_last_offset.?) {
            self._observable_events_offset = null;
            return null;
        }
        if (self._observable_events_packed) {
            const value_result = try self.buf.readInt32(current_offset);
            self._observable_events_offset = current_offset + value_result.size;
            if (self._observable_events_offset.? >= self._observable_events_last_offset.?) {
                self._observable_events_offset = null;
            }
            return @enumFromInt(value_result.value);
        } else {
            const value_result = try self.buf.readInt32(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._observable_events_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == TracingServiceCapabilitiesWire.OBSERVABLE_EVENTS_WIRE) {
                    self._observable_events_offset = next_offset;
                    return @enumFromInt(value_result.value);
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._observable_events_offset = null;
            return @enumFromInt(value_result.value);
        }
    }
    pub inline fn getHasTraceConfigOutputPath(self: *const TracingServiceCapabilitiesReader) bool {
        return self._has_trace_config_output_path;
    }
    pub inline fn getHasCloneSession(self: *const TracingServiceCapabilitiesReader) bool {
        return self._has_clone_session;
    }
};
