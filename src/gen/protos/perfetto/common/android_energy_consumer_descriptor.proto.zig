// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const AndroidEnergyConsumerWire = struct {
    const ENERGY_CONSUMER_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const ORDINAL_WIRE: gremlin.ProtoWireNumber = 2;
    const TYPE_WIRE: gremlin.ProtoWireNumber = 3;
    const NAME_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const AndroidEnergyConsumer = struct {
    // fields
    energy_consumer_id: i32 = 0,
    ordinal: i32 = 0,
    type: ?[]const u8 = null,
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const AndroidEnergyConsumer) usize {
        var res: usize = 0;
        if (self.energy_consumer_id != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidEnergyConsumerWire.ENERGY_CONSUMER_ID_WIRE) + gremlin.sizes.sizeI32(self.energy_consumer_id);
        }
        if (self.ordinal != 0) {
            res += gremlin.sizes.sizeWireNumber(AndroidEnergyConsumerWire.ORDINAL_WIRE) + gremlin.sizes.sizeI32(self.ordinal);
        }
        if (self.type) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidEnergyConsumerWire.TYPE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidEnergyConsumerWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const AndroidEnergyConsumer, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidEnergyConsumer, target: *gremlin.Writer) void {
        if (self.energy_consumer_id != 0) {
            target.appendInt32(AndroidEnergyConsumerWire.ENERGY_CONSUMER_ID_WIRE, self.energy_consumer_id);
        }
        if (self.ordinal != 0) {
            target.appendInt32(AndroidEnergyConsumerWire.ORDINAL_WIRE, self.ordinal);
        }
        if (self.type) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidEnergyConsumerWire.TYPE_WIRE, v);
            }
        }
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(AndroidEnergyConsumerWire.NAME_WIRE, v);
            }
        }
    }
};
pub const AndroidEnergyConsumerReader = struct {
    buf: gremlin.Reader,
    _energy_consumer_id: i32 = 0,
    _ordinal: i32 = 0,
    _type: ?[]const u8 = null,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!AndroidEnergyConsumerReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidEnergyConsumerReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidEnergyConsumerWire.ENERGY_CONSUMER_ID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._energy_consumer_id = result.value;
                },
                AndroidEnergyConsumerWire.ORDINAL_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._ordinal = result.value;
                },
                AndroidEnergyConsumerWire.TYPE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                AndroidEnergyConsumerWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getEnergyConsumerId(self: *const AndroidEnergyConsumerReader) i32 {
        return self._energy_consumer_id;
    }
    pub inline fn getOrdinal(self: *const AndroidEnergyConsumerReader) i32 {
        return self._ordinal;
    }
    pub inline fn getType(self: *const AndroidEnergyConsumerReader) []const u8 {
        return self._type orelse &[_]u8{};
    }
    pub inline fn getName(self: *const AndroidEnergyConsumerReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const AndroidEnergyConsumerDescriptorWire = struct {
    const ENERGY_CONSUMERS_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const AndroidEnergyConsumerDescriptor = struct {
    // fields
    energy_consumers: ?[]const ?AndroidEnergyConsumer = null,
    pub fn calcProtobufSize(self: *const AndroidEnergyConsumerDescriptor) usize {
        var res: usize = 0;
        if (self.energy_consumers) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(AndroidEnergyConsumerDescriptorWire.ENERGY_CONSUMERS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const AndroidEnergyConsumerDescriptor, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidEnergyConsumerDescriptor, target: *gremlin.Writer) void {
        if (self.energy_consumers) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(AndroidEnergyConsumerDescriptorWire.ENERGY_CONSUMERS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(AndroidEnergyConsumerDescriptorWire.ENERGY_CONSUMERS_WIRE, 0);
                }
            }
        }
    }
};
pub const AndroidEnergyConsumerDescriptorReader = struct {
    buf: gremlin.Reader,
    _energy_consumers_offset: ?usize = null,
    _energy_consumers_last_offset: ?usize = null,
    _energy_consumers_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidEnergyConsumerDescriptorReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidEnergyConsumerDescriptorReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidEnergyConsumerDescriptorWire.ENERGY_CONSUMERS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._energy_consumers_offset == null) {
                        res._energy_consumers_offset = offset - result.size;
                    }
                    res._energy_consumers_last_offset = offset;
                    res._energy_consumers_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn energyConsumersCount(self: *const AndroidEnergyConsumerDescriptorReader) usize {
        return self._energy_consumers_cnt;
    }
    pub fn energyConsumersNext(self: *AndroidEnergyConsumerDescriptorReader) ?AndroidEnergyConsumerReader {
        if (self._energy_consumers_offset == null) return null;
        const current_offset = self._energy_consumers_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = AndroidEnergyConsumerReader.init(result.value) catch return null;
        if (self._energy_consumers_last_offset != null and current_offset >= self._energy_consumers_last_offset.?) {
            self._energy_consumers_offset = null;
            return msg;
        }
        if (self._energy_consumers_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._energy_consumers_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == AndroidEnergyConsumerDescriptorWire.ENERGY_CONSUMERS_WIRE) {
                self._energy_consumers_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._energy_consumers_offset = null;
        return msg;
    }
};
