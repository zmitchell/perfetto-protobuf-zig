// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const CommitDataRequestWire = struct {
    const CHUNKS_TO_MOVE_WIRE: gremlin.ProtoWireNumber = 1;
    const CHUNKS_TO_PATCH_WIRE: gremlin.ProtoWireNumber = 2;
    const FLUSH_REQUEST_ID_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const CommitDataRequest = struct {
    // nested structs
    const ChunksToMoveWire = struct {
        const PAGE_WIRE: gremlin.ProtoWireNumber = 1;
        const CHUNK_WIRE: gremlin.ProtoWireNumber = 2;
        const TARGET_BUFFER_WIRE: gremlin.ProtoWireNumber = 3;
        const DATA_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const ChunksToMove = struct {
        // fields
        page: u32 = 0,
        chunk: u32 = 0,
        target_buffer: u32 = 0,
        data: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const CommitDataRequest.ChunksToMove) usize {
            var res: usize = 0;
            if (self.page != 0) {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunksToMoveWire.PAGE_WIRE) + gremlin.sizes.sizeU32(self.page);
            }
            if (self.chunk != 0) {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunksToMoveWire.CHUNK_WIRE) + gremlin.sizes.sizeU32(self.chunk);
            }
            if (self.target_buffer != 0) {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunksToMoveWire.TARGET_BUFFER_WIRE) + gremlin.sizes.sizeU32(self.target_buffer);
            }
            if (self.data) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunksToMoveWire.DATA_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const CommitDataRequest.ChunksToMove, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const CommitDataRequest.ChunksToMove, target: *gremlin.Writer) void {
            if (self.page != 0) {
                target.appendUint32(CommitDataRequest.ChunksToMoveWire.PAGE_WIRE, self.page);
            }
            if (self.chunk != 0) {
                target.appendUint32(CommitDataRequest.ChunksToMoveWire.CHUNK_WIRE, self.chunk);
            }
            if (self.target_buffer != 0) {
                target.appendUint32(CommitDataRequest.ChunksToMoveWire.TARGET_BUFFER_WIRE, self.target_buffer);
            }
            if (self.data) |v| {
                if (v.len > 0) {
                    target.appendBytes(CommitDataRequest.ChunksToMoveWire.DATA_WIRE, v);
                }
            }
        }
    };
    pub const ChunksToMoveReader = struct {
        buf: gremlin.Reader,
        _page: u32 = 0,
        _chunk: u32 = 0,
        _target_buffer: u32 = 0,
        _data: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!CommitDataRequest.ChunksToMoveReader {
            const buf = gremlin.Reader.init(src);
            var res = CommitDataRequest.ChunksToMoveReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    CommitDataRequest.ChunksToMoveWire.PAGE_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._page = result.value;
                    },
                    CommitDataRequest.ChunksToMoveWire.CHUNK_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._chunk = result.value;
                    },
                    CommitDataRequest.ChunksToMoveWire.TARGET_BUFFER_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._target_buffer = result.value;
                    },
                    CommitDataRequest.ChunksToMoveWire.DATA_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._data = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getPage(self: *const CommitDataRequest.ChunksToMoveReader) u32 {
            return self._page;
        }
        pub inline fn getChunk(self: *const CommitDataRequest.ChunksToMoveReader) u32 {
            return self._chunk;
        }
        pub inline fn getTargetBuffer(self: *const CommitDataRequest.ChunksToMoveReader) u32 {
            return self._target_buffer;
        }
        pub inline fn getData(self: *const CommitDataRequest.ChunksToMoveReader) []const u8 {
            return self._data orelse &[_]u8{};
        }
    };
    const ChunkToPatchWire = struct {
        const TARGET_BUFFER_WIRE: gremlin.ProtoWireNumber = 1;
        const WRITER_ID_WIRE: gremlin.ProtoWireNumber = 2;
        const CHUNK_ID_WIRE: gremlin.ProtoWireNumber = 3;
        const PATCHES_WIRE: gremlin.ProtoWireNumber = 4;
        const HAS_MORE_PATCHES_WIRE: gremlin.ProtoWireNumber = 5;
    };
    pub const ChunkToPatch = struct {
        // nested structs
        const PatchWire = struct {
            const OFFSET_WIRE: gremlin.ProtoWireNumber = 1;
            const DATA_WIRE: gremlin.ProtoWireNumber = 2;
        };
        pub const Patch = struct {
            // fields
            offset: u32 = 0,
            data: ?[]const u8 = null,
            pub fn calcProtobufSize(self: *const CommitDataRequest.ChunkToPatch.Patch) usize {
                var res: usize = 0;
                if (self.offset != 0) {
                    res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunkToPatch.PatchWire.OFFSET_WIRE) + gremlin.sizes.sizeU32(self.offset);
                }
                if (self.data) |v| {
                    if (v.len > 0) {
                        res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunkToPatch.PatchWire.DATA_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                    }
                }
                return res;
            }
            pub fn encode(self: *const CommitDataRequest.ChunkToPatch.Patch, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
                const size = self.calcProtobufSize();
                if (size == 0) {
                    return &[_]u8{};
                }
                const buf = try allocator.alloc(u8, self.calcProtobufSize());
                var writer = gremlin.Writer.init(buf);
                self.encodeTo(&writer);
                return buf;
            }
            pub fn encodeTo(self: *const CommitDataRequest.ChunkToPatch.Patch, target: *gremlin.Writer) void {
                if (self.offset != 0) {
                    target.appendUint32(CommitDataRequest.ChunkToPatch.PatchWire.OFFSET_WIRE, self.offset);
                }
                if (self.data) |v| {
                    if (v.len > 0) {
                        target.appendBytes(CommitDataRequest.ChunkToPatch.PatchWire.DATA_WIRE, v);
                    }
                }
            }
        };
        pub const PatchReader = struct {
            buf: gremlin.Reader,
            _offset: u32 = 0,
            _data: ?[]const u8 = null,
            pub fn init(src: []const u8) gremlin.Error!CommitDataRequest.ChunkToPatch.PatchReader {
                const buf = gremlin.Reader.init(src);
                var res = CommitDataRequest.ChunkToPatch.PatchReader{ .buf = buf };
                if (buf.buf.len == 0) {
                    return res;
                }
                var offset: usize = 0;
                while (buf.hasNext(offset, 0)) {
                    const tag = try buf.readTagAt(offset);
                    offset += tag.size;
                    switch (tag.number) {
                        CommitDataRequest.ChunkToPatch.PatchWire.OFFSET_WIRE => {
                            const result = try buf.readUInt32(offset);
                            offset += result.size;
                            res._offset = result.value;
                        },
                        CommitDataRequest.ChunkToPatch.PatchWire.DATA_WIRE => {
                            const result = try buf.readBytes(offset);
                            offset += result.size;
                            res._data = result.value;
                        },
                        else => {
                            offset = try buf.skipData(offset, tag.wire);
                        },
                    }
                }
                return res;
            }
            pub fn sourceBytes(self: *const @This()) []const u8 {
                return self.buf.buf;
            }
            pub inline fn getOffset(self: *const CommitDataRequest.ChunkToPatch.PatchReader) u32 {
                return self._offset;
            }
            pub inline fn getData(self: *const CommitDataRequest.ChunkToPatch.PatchReader) []const u8 {
                return self._data orelse &[_]u8{};
            }
        };
        // fields
        target_buffer: u32 = 0,
        writer_id: u32 = 0,
        chunk_id: u32 = 0,
        patches: ?[]const ?CommitDataRequest.ChunkToPatch.Patch = null,
        has_more_patches: bool = false,
        pub fn calcProtobufSize(self: *const CommitDataRequest.ChunkToPatch) usize {
            var res: usize = 0;
            if (self.target_buffer != 0) {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunkToPatchWire.TARGET_BUFFER_WIRE) + gremlin.sizes.sizeU32(self.target_buffer);
            }
            if (self.writer_id != 0) {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunkToPatchWire.WRITER_ID_WIRE) + gremlin.sizes.sizeU32(self.writer_id);
            }
            if (self.chunk_id != 0) {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunkToPatchWire.CHUNK_ID_WIRE) + gremlin.sizes.sizeU32(self.chunk_id);
            }
            if (self.patches) |arr| {
                for (arr) |maybe_v| {
                    res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunkToPatchWire.PATCHES_WIRE);
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        res += gremlin.sizes.sizeUsize(size) + size;
                    } else {
                        res += gremlin.sizes.sizeUsize(0);
                    }
                }
            }
            if (self.has_more_patches != false) {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequest.ChunkToPatchWire.HAS_MORE_PATCHES_WIRE) + gremlin.sizes.sizeBool(self.has_more_patches);
            }
            return res;
        }
        pub fn encode(self: *const CommitDataRequest.ChunkToPatch, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const CommitDataRequest.ChunkToPatch, target: *gremlin.Writer) void {
            if (self.target_buffer != 0) {
                target.appendUint32(CommitDataRequest.ChunkToPatchWire.TARGET_BUFFER_WIRE, self.target_buffer);
            }
            if (self.writer_id != 0) {
                target.appendUint32(CommitDataRequest.ChunkToPatchWire.WRITER_ID_WIRE, self.writer_id);
            }
            if (self.chunk_id != 0) {
                target.appendUint32(CommitDataRequest.ChunkToPatchWire.CHUNK_ID_WIRE, self.chunk_id);
            }
            if (self.patches) |arr| {
                for (arr) |maybe_v| {
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        target.appendBytesTag(CommitDataRequest.ChunkToPatchWire.PATCHES_WIRE, size);
                        v.encodeTo(target);
                    } else {
                        target.appendBytesTag(CommitDataRequest.ChunkToPatchWire.PATCHES_WIRE, 0);
                    }
                }
            }
            if (self.has_more_patches != false) {
                target.appendBool(CommitDataRequest.ChunkToPatchWire.HAS_MORE_PATCHES_WIRE, self.has_more_patches);
            }
        }
    };
    pub const ChunkToPatchReader = struct {
        buf: gremlin.Reader,
        _target_buffer: u32 = 0,
        _writer_id: u32 = 0,
        _chunk_id: u32 = 0,
        _patches_offset: ?usize = null,
        _patches_last_offset: ?usize = null,
        _patches_cnt: usize = 0,
        _has_more_patches: bool = false,
        pub fn init(src: []const u8) gremlin.Error!CommitDataRequest.ChunkToPatchReader {
            const buf = gremlin.Reader.init(src);
            var res = CommitDataRequest.ChunkToPatchReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    CommitDataRequest.ChunkToPatchWire.TARGET_BUFFER_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._target_buffer = result.value;
                    },
                    CommitDataRequest.ChunkToPatchWire.WRITER_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._writer_id = result.value;
                    },
                    CommitDataRequest.ChunkToPatchWire.CHUNK_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._chunk_id = result.value;
                    },
                    CommitDataRequest.ChunkToPatchWire.PATCHES_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        if (res._patches_offset == null) {
                            res._patches_offset = offset - result.size;
                        }
                        res._patches_last_offset = offset;
                        res._patches_cnt += 1;
                    },
                    CommitDataRequest.ChunkToPatchWire.HAS_MORE_PATCHES_WIRE => {
                        const result = try buf.readBool(offset);
                        offset += result.size;
                        res._has_more_patches = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getTargetBuffer(self: *const CommitDataRequest.ChunkToPatchReader) u32 {
            return self._target_buffer;
        }
        pub inline fn getWriterId(self: *const CommitDataRequest.ChunkToPatchReader) u32 {
            return self._writer_id;
        }
        pub inline fn getChunkId(self: *const CommitDataRequest.ChunkToPatchReader) u32 {
            return self._chunk_id;
        }
        pub fn patchesCount(self: *const CommitDataRequest.ChunkToPatchReader) usize {
            return self._patches_cnt;
        }
        pub fn patchesNext(self: *CommitDataRequest.ChunkToPatchReader) ?CommitDataRequest.ChunkToPatch.PatchReader {
            if (self._patches_offset == null) return null;
            const current_offset = self._patches_offset.?;
            const result = self.buf.readBytes(current_offset) catch return null;
            const msg = CommitDataRequest.ChunkToPatch.PatchReader.init(result.value) catch return null;
            if (self._patches_last_offset != null and current_offset >= self._patches_last_offset.?) {
                self._patches_offset = null;
                return msg;
            }
            if (self._patches_last_offset == null) unreachable;
            var next_offset = current_offset + result.size;
            const max_offset = self._patches_last_offset.?;
            while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
                const tag = self.buf.readTagAt(next_offset) catch break;
                next_offset += tag.size;
                if (tag.number == CommitDataRequest.ChunkToPatchWire.PATCHES_WIRE) {
                    self._patches_offset = next_offset;
                    return msg;
                } else {
                    next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
                }
            }
            self._patches_offset = null;
            return msg;
        }
        pub inline fn getHasMorePatches(self: *const CommitDataRequest.ChunkToPatchReader) bool {
            return self._has_more_patches;
        }
    };
    // fields
    chunks_to_move: ?[]const ?CommitDataRequest.ChunksToMove = null,
    chunks_to_patch: ?[]const ?CommitDataRequest.ChunkToPatch = null,
    flush_request_id: u64 = 0,
    pub fn calcProtobufSize(self: *const CommitDataRequest) usize {
        var res: usize = 0;
        if (self.chunks_to_move) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequestWire.CHUNKS_TO_MOVE_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.chunks_to_patch) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(CommitDataRequestWire.CHUNKS_TO_PATCH_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.flush_request_id != 0) {
            res += gremlin.sizes.sizeWireNumber(CommitDataRequestWire.FLUSH_REQUEST_ID_WIRE) + gremlin.sizes.sizeU64(self.flush_request_id);
        }
        return res;
    }
    pub fn encode(self: *const CommitDataRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const CommitDataRequest, target: *gremlin.Writer) void {
        if (self.chunks_to_move) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(CommitDataRequestWire.CHUNKS_TO_MOVE_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(CommitDataRequestWire.CHUNKS_TO_MOVE_WIRE, 0);
                }
            }
        }
        if (self.chunks_to_patch) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(CommitDataRequestWire.CHUNKS_TO_PATCH_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(CommitDataRequestWire.CHUNKS_TO_PATCH_WIRE, 0);
                }
            }
        }
        if (self.flush_request_id != 0) {
            target.appendUint64(CommitDataRequestWire.FLUSH_REQUEST_ID_WIRE, self.flush_request_id);
        }
    }
};
pub const CommitDataRequestReader = struct {
    buf: gremlin.Reader,
    _chunks_to_move_offset: ?usize = null,
    _chunks_to_move_last_offset: ?usize = null,
    _chunks_to_move_cnt: usize = 0,
    _chunks_to_patch_offset: ?usize = null,
    _chunks_to_patch_last_offset: ?usize = null,
    _chunks_to_patch_cnt: usize = 0,
    _flush_request_id: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!CommitDataRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = CommitDataRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                CommitDataRequestWire.CHUNKS_TO_MOVE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._chunks_to_move_offset == null) {
                        res._chunks_to_move_offset = offset - result.size;
                    }
                    res._chunks_to_move_last_offset = offset;
                    res._chunks_to_move_cnt += 1;
                },
                CommitDataRequestWire.CHUNKS_TO_PATCH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._chunks_to_patch_offset == null) {
                        res._chunks_to_patch_offset = offset - result.size;
                    }
                    res._chunks_to_patch_last_offset = offset;
                    res._chunks_to_patch_cnt += 1;
                },
                CommitDataRequestWire.FLUSH_REQUEST_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._flush_request_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn chunksToMoveCount(self: *const CommitDataRequestReader) usize {
        return self._chunks_to_move_cnt;
    }
    pub fn chunksToMoveNext(self: *CommitDataRequestReader) ?CommitDataRequest.ChunksToMoveReader {
        if (self._chunks_to_move_offset == null) return null;
        const current_offset = self._chunks_to_move_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = CommitDataRequest.ChunksToMoveReader.init(result.value) catch return null;
        if (self._chunks_to_move_last_offset != null and current_offset >= self._chunks_to_move_last_offset.?) {
            self._chunks_to_move_offset = null;
            return msg;
        }
        if (self._chunks_to_move_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._chunks_to_move_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == CommitDataRequestWire.CHUNKS_TO_MOVE_WIRE) {
                self._chunks_to_move_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._chunks_to_move_offset = null;
        return msg;
    }
    pub fn chunksToPatchCount(self: *const CommitDataRequestReader) usize {
        return self._chunks_to_patch_cnt;
    }
    pub fn chunksToPatchNext(self: *CommitDataRequestReader) ?CommitDataRequest.ChunkToPatchReader {
        if (self._chunks_to_patch_offset == null) return null;
        const current_offset = self._chunks_to_patch_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = CommitDataRequest.ChunkToPatchReader.init(result.value) catch return null;
        if (self._chunks_to_patch_last_offset != null and current_offset >= self._chunks_to_patch_last_offset.?) {
            self._chunks_to_patch_offset = null;
            return msg;
        }
        if (self._chunks_to_patch_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._chunks_to_patch_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == CommitDataRequestWire.CHUNKS_TO_PATCH_WIRE) {
                self._chunks_to_patch_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._chunks_to_patch_offset = null;
        return msg;
    }
    pub inline fn getFlushRequestId(self: *const CommitDataRequestReader) u64 {
        return self._flush_request_id;
    }
};
