// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const data_source_descriptor = @import("src/gen/protos/perfetto/common/data_source_descriptor.proto.zig");
const data_source_config = @import("src/gen/protos/perfetto/config/data_source_config.proto.zig");
// structs
const InitializeConnectionRequestWire = struct {
    const SHARED_MEMORY_PAGE_SIZE_HINT_BYTES_WIRE: gremlin.ProtoWireNumber = 1;
    const SHARED_MEMORY_SIZE_HINT_BYTES_WIRE: gremlin.ProtoWireNumber = 2;
    const PRODUCER_NAME_WIRE: gremlin.ProtoWireNumber = 3;
    const SMB_SCRAPING_MODE_WIRE: gremlin.ProtoWireNumber = 4;
    const PRODUCER_PROVIDED_SHMEM_WIRE: gremlin.ProtoWireNumber = 6;
    const SDK_VERSION_WIRE: gremlin.ProtoWireNumber = 8;
    const SHM_KEY_WINDOWS_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const InitializeConnectionRequest = struct {
    // nested enums
    pub const ProducerSMBScrapingMode = enum(i32) {
        SMB_SCRAPING_UNSPECIFIED = 0,
        SMB_SCRAPING_ENABLED = 1,
        SMB_SCRAPING_DISABLED = 2,
    };
    // fields
    shared_memory_page_size_hint_bytes: u32 = 0,
    shared_memory_size_hint_bytes: u32 = 0,
    producer_name: ?[]const u8 = null,
    smb_scraping_mode: InitializeConnectionRequest.ProducerSMBScrapingMode = @enumFromInt(0),
    producer_provided_shmem: bool = false,
    sdk_version: ?[]const u8 = null,
    shm_key_windows: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const InitializeConnectionRequest) usize {
        var res: usize = 0;
        if (self.shared_memory_page_size_hint_bytes != 0) {
            res += gremlin.sizes.sizeWireNumber(InitializeConnectionRequestWire.SHARED_MEMORY_PAGE_SIZE_HINT_BYTES_WIRE) + gremlin.sizes.sizeU32(self.shared_memory_page_size_hint_bytes);
        }
        if (self.shared_memory_size_hint_bytes != 0) {
            res += gremlin.sizes.sizeWireNumber(InitializeConnectionRequestWire.SHARED_MEMORY_SIZE_HINT_BYTES_WIRE) + gremlin.sizes.sizeU32(self.shared_memory_size_hint_bytes);
        }
        if (self.producer_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InitializeConnectionRequestWire.PRODUCER_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (@intFromEnum(self.smb_scraping_mode) != 0) {
            res += gremlin.sizes.sizeWireNumber(InitializeConnectionRequestWire.SMB_SCRAPING_MODE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.smb_scraping_mode));
        }
        if (self.producer_provided_shmem != false) {
            res += gremlin.sizes.sizeWireNumber(InitializeConnectionRequestWire.PRODUCER_PROVIDED_SHMEM_WIRE) + gremlin.sizes.sizeBool(self.producer_provided_shmem);
        }
        if (self.sdk_version) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InitializeConnectionRequestWire.SDK_VERSION_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.shm_key_windows) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(InitializeConnectionRequestWire.SHM_KEY_WINDOWS_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const InitializeConnectionRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InitializeConnectionRequest, target: *gremlin.Writer) void {
        if (self.shared_memory_page_size_hint_bytes != 0) {
            target.appendUint32(InitializeConnectionRequestWire.SHARED_MEMORY_PAGE_SIZE_HINT_BYTES_WIRE, self.shared_memory_page_size_hint_bytes);
        }
        if (self.shared_memory_size_hint_bytes != 0) {
            target.appendUint32(InitializeConnectionRequestWire.SHARED_MEMORY_SIZE_HINT_BYTES_WIRE, self.shared_memory_size_hint_bytes);
        }
        if (self.producer_name) |v| {
            if (v.len > 0) {
                target.appendBytes(InitializeConnectionRequestWire.PRODUCER_NAME_WIRE, v);
            }
        }
        if (@intFromEnum(self.smb_scraping_mode) != 0) {
            target.appendInt32(InitializeConnectionRequestWire.SMB_SCRAPING_MODE_WIRE, @intFromEnum(self.smb_scraping_mode));
        }
        if (self.producer_provided_shmem != false) {
            target.appendBool(InitializeConnectionRequestWire.PRODUCER_PROVIDED_SHMEM_WIRE, self.producer_provided_shmem);
        }
        if (self.sdk_version) |v| {
            if (v.len > 0) {
                target.appendBytes(InitializeConnectionRequestWire.SDK_VERSION_WIRE, v);
            }
        }
        if (self.shm_key_windows) |v| {
            if (v.len > 0) {
                target.appendBytes(InitializeConnectionRequestWire.SHM_KEY_WINDOWS_WIRE, v);
            }
        }
    }
};
pub const InitializeConnectionRequestReader = struct {
    buf: gremlin.Reader,
    _shared_memory_page_size_hint_bytes: u32 = 0,
    _shared_memory_size_hint_bytes: u32 = 0,
    _producer_name: ?[]const u8 = null,
    _smb_scraping_mode: InitializeConnectionRequest.ProducerSMBScrapingMode = @enumFromInt(0),
    _producer_provided_shmem: bool = false,
    _sdk_version: ?[]const u8 = null,
    _shm_key_windows: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!InitializeConnectionRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = InitializeConnectionRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InitializeConnectionRequestWire.SHARED_MEMORY_PAGE_SIZE_HINT_BYTES_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._shared_memory_page_size_hint_bytes = result.value;
                },
                InitializeConnectionRequestWire.SHARED_MEMORY_SIZE_HINT_BYTES_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._shared_memory_size_hint_bytes = result.value;
                },
                InitializeConnectionRequestWire.PRODUCER_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._producer_name = result.value;
                },
                InitializeConnectionRequestWire.SMB_SCRAPING_MODE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._smb_scraping_mode = @enumFromInt(result.value);
                },
                InitializeConnectionRequestWire.PRODUCER_PROVIDED_SHMEM_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._producer_provided_shmem = result.value;
                },
                InitializeConnectionRequestWire.SDK_VERSION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._sdk_version = result.value;
                },
                InitializeConnectionRequestWire.SHM_KEY_WINDOWS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._shm_key_windows = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getSharedMemoryPageSizeHintBytes(self: *const InitializeConnectionRequestReader) u32 {
        return self._shared_memory_page_size_hint_bytes;
    }
    pub inline fn getSharedMemorySizeHintBytes(self: *const InitializeConnectionRequestReader) u32 {
        return self._shared_memory_size_hint_bytes;
    }
    pub inline fn getProducerName(self: *const InitializeConnectionRequestReader) []const u8 {
        return self._producer_name orelse &[_]u8{};
    }
    pub inline fn getSmbScrapingMode(self: *const InitializeConnectionRequestReader) InitializeConnectionRequest.ProducerSMBScrapingMode {
        return self._smb_scraping_mode;
    }
    pub inline fn getProducerProvidedShmem(self: *const InitializeConnectionRequestReader) bool {
        return self._producer_provided_shmem;
    }
    pub inline fn getSdkVersion(self: *const InitializeConnectionRequestReader) []const u8 {
        return self._sdk_version orelse &[_]u8{};
    }
    pub inline fn getShmKeyWindows(self: *const InitializeConnectionRequestReader) []const u8 {
        return self._shm_key_windows orelse &[_]u8{};
    }
};
const InitializeConnectionResponseWire = struct {
    const USING_SHMEM_PROVIDED_BY_PRODUCER_WIRE: gremlin.ProtoWireNumber = 1;
    const DIRECT_SMB_PATCHING_SUPPORTED_WIRE: gremlin.ProtoWireNumber = 2;
    const USE_SHMEM_EMULATION_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const InitializeConnectionResponse = struct {
    // fields
    using_shmem_provided_by_producer: bool = false,
    direct_smb_patching_supported: bool = false,
    use_shmem_emulation: bool = false,
    pub fn calcProtobufSize(self: *const InitializeConnectionResponse) usize {
        var res: usize = 0;
        if (self.using_shmem_provided_by_producer != false) {
            res += gremlin.sizes.sizeWireNumber(InitializeConnectionResponseWire.USING_SHMEM_PROVIDED_BY_PRODUCER_WIRE) + gremlin.sizes.sizeBool(self.using_shmem_provided_by_producer);
        }
        if (self.direct_smb_patching_supported != false) {
            res += gremlin.sizes.sizeWireNumber(InitializeConnectionResponseWire.DIRECT_SMB_PATCHING_SUPPORTED_WIRE) + gremlin.sizes.sizeBool(self.direct_smb_patching_supported);
        }
        if (self.use_shmem_emulation != false) {
            res += gremlin.sizes.sizeWireNumber(InitializeConnectionResponseWire.USE_SHMEM_EMULATION_WIRE) + gremlin.sizes.sizeBool(self.use_shmem_emulation);
        }
        return res;
    }
    pub fn encode(self: *const InitializeConnectionResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InitializeConnectionResponse, target: *gremlin.Writer) void {
        if (self.using_shmem_provided_by_producer != false) {
            target.appendBool(InitializeConnectionResponseWire.USING_SHMEM_PROVIDED_BY_PRODUCER_WIRE, self.using_shmem_provided_by_producer);
        }
        if (self.direct_smb_patching_supported != false) {
            target.appendBool(InitializeConnectionResponseWire.DIRECT_SMB_PATCHING_SUPPORTED_WIRE, self.direct_smb_patching_supported);
        }
        if (self.use_shmem_emulation != false) {
            target.appendBool(InitializeConnectionResponseWire.USE_SHMEM_EMULATION_WIRE, self.use_shmem_emulation);
        }
    }
};
pub const InitializeConnectionResponseReader = struct {
    buf: gremlin.Reader,
    _using_shmem_provided_by_producer: bool = false,
    _direct_smb_patching_supported: bool = false,
    _use_shmem_emulation: bool = false,
    pub fn init(src: []const u8) gremlin.Error!InitializeConnectionResponseReader {
        const buf = gremlin.Reader.init(src);
        var res = InitializeConnectionResponseReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InitializeConnectionResponseWire.USING_SHMEM_PROVIDED_BY_PRODUCER_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._using_shmem_provided_by_producer = result.value;
                },
                InitializeConnectionResponseWire.DIRECT_SMB_PATCHING_SUPPORTED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._direct_smb_patching_supported = result.value;
                },
                InitializeConnectionResponseWire.USE_SHMEM_EMULATION_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._use_shmem_emulation = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getUsingShmemProvidedByProducer(self: *const InitializeConnectionResponseReader) bool {
        return self._using_shmem_provided_by_producer;
    }
    pub inline fn getDirectSmbPatchingSupported(self: *const InitializeConnectionResponseReader) bool {
        return self._direct_smb_patching_supported;
    }
    pub inline fn getUseShmemEmulation(self: *const InitializeConnectionResponseReader) bool {
        return self._use_shmem_emulation;
    }
};
const RegisterDataSourceRequestWire = struct {
    const DATA_SOURCE_DESCRIPTOR_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const RegisterDataSourceRequest = struct {
    // fields
    data_source_descriptor: ?data_source_descriptor.DataSourceDescriptor = null,
    pub fn calcProtobufSize(self: *const RegisterDataSourceRequest) usize {
        var res: usize = 0;
        if (self.data_source_descriptor) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(RegisterDataSourceRequestWire.DATA_SOURCE_DESCRIPTOR_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const RegisterDataSourceRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegisterDataSourceRequest, target: *gremlin.Writer) void {
        if (self.data_source_descriptor) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(RegisterDataSourceRequestWire.DATA_SOURCE_DESCRIPTOR_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const RegisterDataSourceRequestReader = struct {
    buf: gremlin.Reader,
    _data_source_descriptor_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RegisterDataSourceRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = RegisterDataSourceRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegisterDataSourceRequestWire.DATA_SOURCE_DESCRIPTOR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._data_source_descriptor_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getDataSourceDescriptor(self: *const RegisterDataSourceRequestReader) gremlin.Error!data_source_descriptor.DataSourceDescriptorReader {
        if (self._data_source_descriptor_buf) |buf| {
            return try data_source_descriptor.DataSourceDescriptorReader.init(buf);
        }
        return try data_source_descriptor.DataSourceDescriptorReader.init(&[_]u8{});
    }
};
const RegisterDataSourceResponseWire = struct {
    const ERROR_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const RegisterDataSourceResponse = struct {
    // fields
    error_: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RegisterDataSourceResponse) usize {
        var res: usize = 0;
        if (self.error_) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RegisterDataSourceResponseWire.ERROR_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const RegisterDataSourceResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegisterDataSourceResponse, target: *gremlin.Writer) void {
        if (self.error_) |v| {
            if (v.len > 0) {
                target.appendBytes(RegisterDataSourceResponseWire.ERROR_WIRE, v);
            }
        }
    }
};
pub const RegisterDataSourceResponseReader = struct {
    buf: gremlin.Reader,
    _error_: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RegisterDataSourceResponseReader {
        const buf = gremlin.Reader.init(src);
        var res = RegisterDataSourceResponseReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegisterDataSourceResponseWire.ERROR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._error_ = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getError(self: *const RegisterDataSourceResponseReader) []const u8 {
        return self._error_ orelse &[_]u8{};
    }
};
const UpdateDataSourceRequestWire = struct {
    const DATA_SOURCE_DESCRIPTOR_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const UpdateDataSourceRequest = struct {
    // fields
    data_source_descriptor: ?data_source_descriptor.DataSourceDescriptor = null,
    pub fn calcProtobufSize(self: *const UpdateDataSourceRequest) usize {
        var res: usize = 0;
        if (self.data_source_descriptor) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(UpdateDataSourceRequestWire.DATA_SOURCE_DESCRIPTOR_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const UpdateDataSourceRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const UpdateDataSourceRequest, target: *gremlin.Writer) void {
        if (self.data_source_descriptor) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(UpdateDataSourceRequestWire.DATA_SOURCE_DESCRIPTOR_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const UpdateDataSourceRequestReader = struct {
    buf: gremlin.Reader,
    _data_source_descriptor_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!UpdateDataSourceRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = UpdateDataSourceRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                UpdateDataSourceRequestWire.DATA_SOURCE_DESCRIPTOR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._data_source_descriptor_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getDataSourceDescriptor(self: *const UpdateDataSourceRequestReader) gremlin.Error!data_source_descriptor.DataSourceDescriptorReader {
        if (self._data_source_descriptor_buf) |buf| {
            return try data_source_descriptor.DataSourceDescriptorReader.init(buf);
        }
        return try data_source_descriptor.DataSourceDescriptorReader.init(&[_]u8{});
    }
};
pub const UpdateDataSourceResponse = struct {
    pub fn calcProtobufSize(_: *const UpdateDataSourceResponse) usize {
        return 0;
    }
    pub fn encode(self: *const UpdateDataSourceResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const UpdateDataSourceResponse, _: *gremlin.Writer) void {}
};
pub const UpdateDataSourceResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!UpdateDataSourceResponseReader {
        const buf = gremlin.Reader.init(src);
        return UpdateDataSourceResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const UnregisterDataSourceRequestWire = struct {
    const DATA_SOURCE_NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const UnregisterDataSourceRequest = struct {
    // fields
    data_source_name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const UnregisterDataSourceRequest) usize {
        var res: usize = 0;
        if (self.data_source_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(UnregisterDataSourceRequestWire.DATA_SOURCE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const UnregisterDataSourceRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const UnregisterDataSourceRequest, target: *gremlin.Writer) void {
        if (self.data_source_name) |v| {
            if (v.len > 0) {
                target.appendBytes(UnregisterDataSourceRequestWire.DATA_SOURCE_NAME_WIRE, v);
            }
        }
    }
};
pub const UnregisterDataSourceRequestReader = struct {
    buf: gremlin.Reader,
    _data_source_name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!UnregisterDataSourceRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = UnregisterDataSourceRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                UnregisterDataSourceRequestWire.DATA_SOURCE_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._data_source_name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDataSourceName(self: *const UnregisterDataSourceRequestReader) []const u8 {
        return self._data_source_name orelse &[_]u8{};
    }
};
pub const UnregisterDataSourceResponse = struct {
    pub fn calcProtobufSize(_: *const UnregisterDataSourceResponse) usize {
        return 0;
    }
    pub fn encode(self: *const UnregisterDataSourceResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const UnregisterDataSourceResponse, _: *gremlin.Writer) void {}
};
pub const UnregisterDataSourceResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!UnregisterDataSourceResponseReader {
        const buf = gremlin.Reader.init(src);
        return UnregisterDataSourceResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const RegisterTraceWriterRequestWire = struct {
    const TRACE_WRITER_ID_WIRE: gremlin.ProtoWireNumber = 1;
    const TARGET_BUFFER_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const RegisterTraceWriterRequest = struct {
    // fields
    trace_writer_id: u32 = 0,
    target_buffer: u32 = 0,
    pub fn calcProtobufSize(self: *const RegisterTraceWriterRequest) usize {
        var res: usize = 0;
        if (self.trace_writer_id != 0) {
            res += gremlin.sizes.sizeWireNumber(RegisterTraceWriterRequestWire.TRACE_WRITER_ID_WIRE) + gremlin.sizes.sizeU32(self.trace_writer_id);
        }
        if (self.target_buffer != 0) {
            res += gremlin.sizes.sizeWireNumber(RegisterTraceWriterRequestWire.TARGET_BUFFER_WIRE) + gremlin.sizes.sizeU32(self.target_buffer);
        }
        return res;
    }
    pub fn encode(self: *const RegisterTraceWriterRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RegisterTraceWriterRequest, target: *gremlin.Writer) void {
        if (self.trace_writer_id != 0) {
            target.appendUint32(RegisterTraceWriterRequestWire.TRACE_WRITER_ID_WIRE, self.trace_writer_id);
        }
        if (self.target_buffer != 0) {
            target.appendUint32(RegisterTraceWriterRequestWire.TARGET_BUFFER_WIRE, self.target_buffer);
        }
    }
};
pub const RegisterTraceWriterRequestReader = struct {
    buf: gremlin.Reader,
    _trace_writer_id: u32 = 0,
    _target_buffer: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!RegisterTraceWriterRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = RegisterTraceWriterRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RegisterTraceWriterRequestWire.TRACE_WRITER_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._trace_writer_id = result.value;
                },
                RegisterTraceWriterRequestWire.TARGET_BUFFER_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._target_buffer = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTraceWriterId(self: *const RegisterTraceWriterRequestReader) u32 {
        return self._trace_writer_id;
    }
    pub inline fn getTargetBuffer(self: *const RegisterTraceWriterRequestReader) u32 {
        return self._target_buffer;
    }
};
pub const RegisterTraceWriterResponse = struct {
    pub fn calcProtobufSize(_: *const RegisterTraceWriterResponse) usize {
        return 0;
    }
    pub fn encode(self: *const RegisterTraceWriterResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const RegisterTraceWriterResponse, _: *gremlin.Writer) void {}
};
pub const RegisterTraceWriterResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!RegisterTraceWriterResponseReader {
        const buf = gremlin.Reader.init(src);
        return RegisterTraceWriterResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const UnregisterTraceWriterRequestWire = struct {
    const TRACE_WRITER_ID_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const UnregisterTraceWriterRequest = struct {
    // fields
    trace_writer_id: u32 = 0,
    pub fn calcProtobufSize(self: *const UnregisterTraceWriterRequest) usize {
        var res: usize = 0;
        if (self.trace_writer_id != 0) {
            res += gremlin.sizes.sizeWireNumber(UnregisterTraceWriterRequestWire.TRACE_WRITER_ID_WIRE) + gremlin.sizes.sizeU32(self.trace_writer_id);
        }
        return res;
    }
    pub fn encode(self: *const UnregisterTraceWriterRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const UnregisterTraceWriterRequest, target: *gremlin.Writer) void {
        if (self.trace_writer_id != 0) {
            target.appendUint32(UnregisterTraceWriterRequestWire.TRACE_WRITER_ID_WIRE, self.trace_writer_id);
        }
    }
};
pub const UnregisterTraceWriterRequestReader = struct {
    buf: gremlin.Reader,
    _trace_writer_id: u32 = 0,
    pub fn init(src: []const u8) gremlin.Error!UnregisterTraceWriterRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = UnregisterTraceWriterRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                UnregisterTraceWriterRequestWire.TRACE_WRITER_ID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._trace_writer_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTraceWriterId(self: *const UnregisterTraceWriterRequestReader) u32 {
        return self._trace_writer_id;
    }
};
pub const UnregisterTraceWriterResponse = struct {
    pub fn calcProtobufSize(_: *const UnregisterTraceWriterResponse) usize {
        return 0;
    }
    pub fn encode(self: *const UnregisterTraceWriterResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const UnregisterTraceWriterResponse, _: *gremlin.Writer) void {}
};
pub const UnregisterTraceWriterResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!UnregisterTraceWriterResponseReader {
        const buf = gremlin.Reader.init(src);
        return UnregisterTraceWriterResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
pub const CommitDataResponse = struct {
    pub fn calcProtobufSize(_: *const CommitDataResponse) usize {
        return 0;
    }
    pub fn encode(self: *const CommitDataResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const CommitDataResponse, _: *gremlin.Writer) void {}
};
pub const CommitDataResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!CommitDataResponseReader {
        const buf = gremlin.Reader.init(src);
        return CommitDataResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const NotifyDataSourceStartedRequestWire = struct {
    const DATA_SOURCE_ID_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const NotifyDataSourceStartedRequest = struct {
    // fields
    data_source_id: u64 = 0,
    pub fn calcProtobufSize(self: *const NotifyDataSourceStartedRequest) usize {
        var res: usize = 0;
        if (self.data_source_id != 0) {
            res += gremlin.sizes.sizeWireNumber(NotifyDataSourceStartedRequestWire.DATA_SOURCE_ID_WIRE) + gremlin.sizes.sizeU64(self.data_source_id);
        }
        return res;
    }
    pub fn encode(self: *const NotifyDataSourceStartedRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const NotifyDataSourceStartedRequest, target: *gremlin.Writer) void {
        if (self.data_source_id != 0) {
            target.appendUint64(NotifyDataSourceStartedRequestWire.DATA_SOURCE_ID_WIRE, self.data_source_id);
        }
    }
};
pub const NotifyDataSourceStartedRequestReader = struct {
    buf: gremlin.Reader,
    _data_source_id: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!NotifyDataSourceStartedRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = NotifyDataSourceStartedRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                NotifyDataSourceStartedRequestWire.DATA_SOURCE_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._data_source_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDataSourceId(self: *const NotifyDataSourceStartedRequestReader) u64 {
        return self._data_source_id;
    }
};
pub const NotifyDataSourceStartedResponse = struct {
    pub fn calcProtobufSize(_: *const NotifyDataSourceStartedResponse) usize {
        return 0;
    }
    pub fn encode(self: *const NotifyDataSourceStartedResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const NotifyDataSourceStartedResponse, _: *gremlin.Writer) void {}
};
pub const NotifyDataSourceStartedResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!NotifyDataSourceStartedResponseReader {
        const buf = gremlin.Reader.init(src);
        return NotifyDataSourceStartedResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const NotifyDataSourceStoppedRequestWire = struct {
    const DATA_SOURCE_ID_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const NotifyDataSourceStoppedRequest = struct {
    // fields
    data_source_id: u64 = 0,
    pub fn calcProtobufSize(self: *const NotifyDataSourceStoppedRequest) usize {
        var res: usize = 0;
        if (self.data_source_id != 0) {
            res += gremlin.sizes.sizeWireNumber(NotifyDataSourceStoppedRequestWire.DATA_SOURCE_ID_WIRE) + gremlin.sizes.sizeU64(self.data_source_id);
        }
        return res;
    }
    pub fn encode(self: *const NotifyDataSourceStoppedRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const NotifyDataSourceStoppedRequest, target: *gremlin.Writer) void {
        if (self.data_source_id != 0) {
            target.appendUint64(NotifyDataSourceStoppedRequestWire.DATA_SOURCE_ID_WIRE, self.data_source_id);
        }
    }
};
pub const NotifyDataSourceStoppedRequestReader = struct {
    buf: gremlin.Reader,
    _data_source_id: u64 = 0,
    pub fn init(src: []const u8) gremlin.Error!NotifyDataSourceStoppedRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = NotifyDataSourceStoppedRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                NotifyDataSourceStoppedRequestWire.DATA_SOURCE_ID_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._data_source_id = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getDataSourceId(self: *const NotifyDataSourceStoppedRequestReader) u64 {
        return self._data_source_id;
    }
};
pub const NotifyDataSourceStoppedResponse = struct {
    pub fn calcProtobufSize(_: *const NotifyDataSourceStoppedResponse) usize {
        return 0;
    }
    pub fn encode(self: *const NotifyDataSourceStoppedResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const NotifyDataSourceStoppedResponse, _: *gremlin.Writer) void {}
};
pub const NotifyDataSourceStoppedResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!NotifyDataSourceStoppedResponseReader {
        const buf = gremlin.Reader.init(src);
        return NotifyDataSourceStoppedResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const ActivateTriggersRequestWire = struct {
    const TRIGGER_NAMES_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ActivateTriggersRequest = struct {
    // fields
    trigger_names: ?[]const ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ActivateTriggersRequest) usize {
        var res: usize = 0;
        if (self.trigger_names) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ActivateTriggersRequestWire.TRIGGER_NAMES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ActivateTriggersRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ActivateTriggersRequest, target: *gremlin.Writer) void {
        if (self.trigger_names) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(ActivateTriggersRequestWire.TRIGGER_NAMES_WIRE, v);
                } else {
                    target.appendBytesTag(ActivateTriggersRequestWire.TRIGGER_NAMES_WIRE, 0);
                }
            }
        }
    }
};
pub const ActivateTriggersRequestReader = struct {
    buf: gremlin.Reader,
    _trigger_names_offset: ?usize = null,
    _trigger_names_last_offset: ?usize = null,
    _trigger_names_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ActivateTriggersRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = ActivateTriggersRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ActivateTriggersRequestWire.TRIGGER_NAMES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._trigger_names_offset == null) {
                        res._trigger_names_offset = offset - result.size;
                    }
                    res._trigger_names_last_offset = offset;
                    res._trigger_names_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn triggerNamesCount(self: *const ActivateTriggersRequestReader) usize {
        return self._trigger_names_cnt;
    }
    pub fn triggerNamesNext(self: *ActivateTriggersRequestReader) ?[]const u8 {
        if (self._trigger_names_offset == null) return null;
        const current_offset = self._trigger_names_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._trigger_names_last_offset != null and current_offset >= self._trigger_names_last_offset.?) {
            self._trigger_names_offset = null;
            return result.value;
        }
        if (self._trigger_names_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._trigger_names_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ActivateTriggersRequestWire.TRIGGER_NAMES_WIRE) {
                self._trigger_names_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._trigger_names_offset = null;
        return result.value;
    }
};
pub const ActivateTriggersResponse = struct {
    pub fn calcProtobufSize(_: *const ActivateTriggersResponse) usize {
        return 0;
    }
    pub fn encode(self: *const ActivateTriggersResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const ActivateTriggersResponse, _: *gremlin.Writer) void {}
};
pub const ActivateTriggersResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!ActivateTriggersResponseReader {
        const buf = gremlin.Reader.init(src);
        return ActivateTriggersResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
pub const GetAsyncCommandRequest = struct {
    pub fn calcProtobufSize(_: *const GetAsyncCommandRequest) usize {
        return 0;
    }
    pub fn encode(self: *const GetAsyncCommandRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const GetAsyncCommandRequest, _: *gremlin.Writer) void {}
};
pub const GetAsyncCommandRequestReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!GetAsyncCommandRequestReader {
        const buf = gremlin.Reader.init(src);
        return GetAsyncCommandRequestReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const GetAsyncCommandResponseWire = struct {
    const SETUP_TRACING_WIRE: gremlin.ProtoWireNumber = 3;
    const SETUP_DATA_SOURCE_WIRE: gremlin.ProtoWireNumber = 6;
    const START_DATA_SOURCE_WIRE: gremlin.ProtoWireNumber = 1;
    const STOP_DATA_SOURCE_WIRE: gremlin.ProtoWireNumber = 2;
    const FLUSH_WIRE: gremlin.ProtoWireNumber = 5;
    const CLEAR_INCREMENTAL_STATE_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const GetAsyncCommandResponse = struct {
    // nested structs
    const SetupDataSourceWire = struct {
        const NEW_INSTANCE_ID_WIRE: gremlin.ProtoWireNumber = 1;
        const CONFIG_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const SetupDataSource = struct {
        // fields
        new_instance_id: u64 = 0,
        config: ?data_source_config.DataSourceConfig = null,
        pub fn calcProtobufSize(self: *const GetAsyncCommandResponse.SetupDataSource) usize {
            var res: usize = 0;
            if (self.new_instance_id != 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.SetupDataSourceWire.NEW_INSTANCE_ID_WIRE) + gremlin.sizes.sizeU64(self.new_instance_id);
            }
            if (self.config) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.SetupDataSourceWire.CONFIG_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            return res;
        }
        pub fn encode(self: *const GetAsyncCommandResponse.SetupDataSource, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GetAsyncCommandResponse.SetupDataSource, target: *gremlin.Writer) void {
            if (self.new_instance_id != 0) {
                target.appendUint64(GetAsyncCommandResponse.SetupDataSourceWire.NEW_INSTANCE_ID_WIRE, self.new_instance_id);
            }
            if (self.config) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(GetAsyncCommandResponse.SetupDataSourceWire.CONFIG_WIRE, size);
                    v.encodeTo(target);
                }
            }
        }
    };
    pub const SetupDataSourceReader = struct {
        buf: gremlin.Reader,
        _new_instance_id: u64 = 0,
        _config_buf: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!GetAsyncCommandResponse.SetupDataSourceReader {
            const buf = gremlin.Reader.init(src);
            var res = GetAsyncCommandResponse.SetupDataSourceReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GetAsyncCommandResponse.SetupDataSourceWire.NEW_INSTANCE_ID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._new_instance_id = result.value;
                    },
                    GetAsyncCommandResponse.SetupDataSourceWire.CONFIG_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._config_buf = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getNewInstanceId(self: *const GetAsyncCommandResponse.SetupDataSourceReader) u64 {
            return self._new_instance_id;
        }
        pub fn getConfig(self: *const GetAsyncCommandResponse.SetupDataSourceReader) gremlin.Error!data_source_config.DataSourceConfigReader {
            if (self._config_buf) |buf| {
                return try data_source_config.DataSourceConfigReader.init(buf);
            }
            return try data_source_config.DataSourceConfigReader.init(&[_]u8{});
        }
    };
    const StartDataSourceWire = struct {
        const NEW_INSTANCE_ID_WIRE: gremlin.ProtoWireNumber = 1;
        const CONFIG_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const StartDataSource = struct {
        // fields
        new_instance_id: u64 = 0,
        config: ?data_source_config.DataSourceConfig = null,
        pub fn calcProtobufSize(self: *const GetAsyncCommandResponse.StartDataSource) usize {
            var res: usize = 0;
            if (self.new_instance_id != 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.StartDataSourceWire.NEW_INSTANCE_ID_WIRE) + gremlin.sizes.sizeU64(self.new_instance_id);
            }
            if (self.config) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.StartDataSourceWire.CONFIG_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            return res;
        }
        pub fn encode(self: *const GetAsyncCommandResponse.StartDataSource, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GetAsyncCommandResponse.StartDataSource, target: *gremlin.Writer) void {
            if (self.new_instance_id != 0) {
                target.appendUint64(GetAsyncCommandResponse.StartDataSourceWire.NEW_INSTANCE_ID_WIRE, self.new_instance_id);
            }
            if (self.config) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(GetAsyncCommandResponse.StartDataSourceWire.CONFIG_WIRE, size);
                    v.encodeTo(target);
                }
            }
        }
    };
    pub const StartDataSourceReader = struct {
        buf: gremlin.Reader,
        _new_instance_id: u64 = 0,
        _config_buf: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!GetAsyncCommandResponse.StartDataSourceReader {
            const buf = gremlin.Reader.init(src);
            var res = GetAsyncCommandResponse.StartDataSourceReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GetAsyncCommandResponse.StartDataSourceWire.NEW_INSTANCE_ID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._new_instance_id = result.value;
                    },
                    GetAsyncCommandResponse.StartDataSourceWire.CONFIG_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._config_buf = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getNewInstanceId(self: *const GetAsyncCommandResponse.StartDataSourceReader) u64 {
            return self._new_instance_id;
        }
        pub fn getConfig(self: *const GetAsyncCommandResponse.StartDataSourceReader) gremlin.Error!data_source_config.DataSourceConfigReader {
            if (self._config_buf) |buf| {
                return try data_source_config.DataSourceConfigReader.init(buf);
            }
            return try data_source_config.DataSourceConfigReader.init(&[_]u8{});
        }
    };
    const StopDataSourceWire = struct {
        const INSTANCE_ID_WIRE: gremlin.ProtoWireNumber = 1;
    };
    pub const StopDataSource = struct {
        // fields
        instance_id: u64 = 0,
        pub fn calcProtobufSize(self: *const GetAsyncCommandResponse.StopDataSource) usize {
            var res: usize = 0;
            if (self.instance_id != 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.StopDataSourceWire.INSTANCE_ID_WIRE) + gremlin.sizes.sizeU64(self.instance_id);
            }
            return res;
        }
        pub fn encode(self: *const GetAsyncCommandResponse.StopDataSource, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GetAsyncCommandResponse.StopDataSource, target: *gremlin.Writer) void {
            if (self.instance_id != 0) {
                target.appendUint64(GetAsyncCommandResponse.StopDataSourceWire.INSTANCE_ID_WIRE, self.instance_id);
            }
        }
    };
    pub const StopDataSourceReader = struct {
        buf: gremlin.Reader,
        _instance_id: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!GetAsyncCommandResponse.StopDataSourceReader {
            const buf = gremlin.Reader.init(src);
            var res = GetAsyncCommandResponse.StopDataSourceReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GetAsyncCommandResponse.StopDataSourceWire.INSTANCE_ID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._instance_id = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getInstanceId(self: *const GetAsyncCommandResponse.StopDataSourceReader) u64 {
            return self._instance_id;
        }
    };
    const SetupTracingWire = struct {
        const SHARED_BUFFER_PAGE_SIZE_KB_WIRE: gremlin.ProtoWireNumber = 1;
        const SHM_KEY_WINDOWS_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const SetupTracing = struct {
        // fields
        shared_buffer_page_size_kb: u32 = 0,
        shm_key_windows: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const GetAsyncCommandResponse.SetupTracing) usize {
            var res: usize = 0;
            if (self.shared_buffer_page_size_kb != 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.SetupTracingWire.SHARED_BUFFER_PAGE_SIZE_KB_WIRE) + gremlin.sizes.sizeU32(self.shared_buffer_page_size_kb);
            }
            if (self.shm_key_windows) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.SetupTracingWire.SHM_KEY_WINDOWS_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const GetAsyncCommandResponse.SetupTracing, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GetAsyncCommandResponse.SetupTracing, target: *gremlin.Writer) void {
            if (self.shared_buffer_page_size_kb != 0) {
                target.appendUint32(GetAsyncCommandResponse.SetupTracingWire.SHARED_BUFFER_PAGE_SIZE_KB_WIRE, self.shared_buffer_page_size_kb);
            }
            if (self.shm_key_windows) |v| {
                if (v.len > 0) {
                    target.appendBytes(GetAsyncCommandResponse.SetupTracingWire.SHM_KEY_WINDOWS_WIRE, v);
                }
            }
        }
    };
    pub const SetupTracingReader = struct {
        buf: gremlin.Reader,
        _shared_buffer_page_size_kb: u32 = 0,
        _shm_key_windows: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!GetAsyncCommandResponse.SetupTracingReader {
            const buf = gremlin.Reader.init(src);
            var res = GetAsyncCommandResponse.SetupTracingReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GetAsyncCommandResponse.SetupTracingWire.SHARED_BUFFER_PAGE_SIZE_KB_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._shared_buffer_page_size_kb = result.value;
                    },
                    GetAsyncCommandResponse.SetupTracingWire.SHM_KEY_WINDOWS_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._shm_key_windows = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getSharedBufferPageSizeKb(self: *const GetAsyncCommandResponse.SetupTracingReader) u32 {
            return self._shared_buffer_page_size_kb;
        }
        pub inline fn getShmKeyWindows(self: *const GetAsyncCommandResponse.SetupTracingReader) []const u8 {
            return self._shm_key_windows orelse &[_]u8{};
        }
    };
    const FlushWire = struct {
        const DATA_SOURCE_IDS_WIRE: gremlin.ProtoWireNumber = 1;
        const REQUEST_ID_WIRE: gremlin.ProtoWireNumber = 2;
        const FLAGS_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const Flush = struct {
        // fields
        data_source_ids: ?[]const u64 = null,
        request_id: u64 = 0,
        flags: u64 = 0,
        pub fn calcProtobufSize(self: *const GetAsyncCommandResponse.Flush) usize {
            var res: usize = 0;
            if (self.data_source_ids) |arr| {
                if (arr.len == 0) {} else if (arr.len == 1) {
                    res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.FlushWire.DATA_SOURCE_IDS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
                } else {
                    var packed_size: usize = 0;
                    for (arr) |v| {
                        packed_size += gremlin.sizes.sizeU64(v);
                    }
                    res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.FlushWire.DATA_SOURCE_IDS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
                }
            }
            if (self.request_id != 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.FlushWire.REQUEST_ID_WIRE) + gremlin.sizes.sizeU64(self.request_id);
            }
            if (self.flags != 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.FlushWire.FLAGS_WIRE) + gremlin.sizes.sizeU64(self.flags);
            }
            return res;
        }
        pub fn encode(self: *const GetAsyncCommandResponse.Flush, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GetAsyncCommandResponse.Flush, target: *gremlin.Writer) void {
            if (self.data_source_ids) |arr| {
                if (arr.len == 0) {} else if (arr.len == 1) {
                    target.appendUint64(GetAsyncCommandResponse.FlushWire.DATA_SOURCE_IDS_WIRE, arr[0]);
                } else {
                    var packed_size: usize = 0;
                    for (arr) |v| {
                        packed_size += gremlin.sizes.sizeU64(v);
                    }
                    target.appendBytesTag(GetAsyncCommandResponse.FlushWire.DATA_SOURCE_IDS_WIRE, packed_size);
                    for (arr) |v| {
                        target.appendUint64WithoutTag(v);
                    }
                }
            }
            if (self.request_id != 0) {
                target.appendUint64(GetAsyncCommandResponse.FlushWire.REQUEST_ID_WIRE, self.request_id);
            }
            if (self.flags != 0) {
                target.appendUint64(GetAsyncCommandResponse.FlushWire.FLAGS_WIRE, self.flags);
            }
        }
    };
    pub const FlushReader = struct {
        buf: gremlin.Reader,
        _data_source_ids_offset: ?usize = null,
        _data_source_ids_last_offset: ?usize = null,
        _data_source_ids_packed: bool = false,
        _request_id: u64 = 0,
        _flags: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!GetAsyncCommandResponse.FlushReader {
            const buf = gremlin.Reader.init(src);
            var res = GetAsyncCommandResponse.FlushReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GetAsyncCommandResponse.FlushWire.DATA_SOURCE_IDS_WIRE => {
                        if (res._data_source_ids_offset == null) {
                            res._data_source_ids_offset = offset;
                        }
                        if (tag.wire == gremlin.ProtoWireType.bytes) {
                            res._data_source_ids_packed = true;
                            const length_result = try buf.readVarInt(offset);
                            res._data_source_ids_offset = offset + length_result.size;
                            res._data_source_ids_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                            offset = res._data_source_ids_last_offset.?;
                        } else {
                            const result = try buf.readUInt64(offset);
                            offset += result.size;
                            res._data_source_ids_last_offset = offset;
                        }
                    },
                    GetAsyncCommandResponse.FlushWire.REQUEST_ID_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._request_id = result.value;
                    },
                    GetAsyncCommandResponse.FlushWire.FLAGS_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._flags = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub fn dataSourceIdsNext(self: *GetAsyncCommandResponse.FlushReader) gremlin.Error!?u64 {
            if (self._data_source_ids_offset == null) return null;
            const current_offset = self._data_source_ids_offset.?;
            if (current_offset >= self._data_source_ids_last_offset.?) {
                self._data_source_ids_offset = null;
                return null;
            }
            if (self._data_source_ids_packed) {
                const value_result = try self.buf.readUInt64(current_offset);
                self._data_source_ids_offset = current_offset + value_result.size;
                if (self._data_source_ids_offset.? >= self._data_source_ids_last_offset.?) {
                    self._data_source_ids_offset = null;
                }
                return value_result.value;
            } else {
                const value_result = try self.buf.readUInt64(current_offset);
                var next_offset = current_offset + value_result.size;
                const max_offset = self._data_source_ids_last_offset.?;
                // Search for the next occurrence of this field
                while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                    const next_tag = try self.buf.readTagAt(next_offset);
                    next_offset += next_tag.size;
                    if (next_tag.number == GetAsyncCommandResponse.FlushWire.DATA_SOURCE_IDS_WIRE) {
                        self._data_source_ids_offset = next_offset;
                        return value_result.value;
                    } else {
                        next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                    }
                }
                self._data_source_ids_offset = null;
                return value_result.value;
            }
        }
        pub inline fn getRequestId(self: *const GetAsyncCommandResponse.FlushReader) u64 {
            return self._request_id;
        }
        pub inline fn getFlags(self: *const GetAsyncCommandResponse.FlushReader) u64 {
            return self._flags;
        }
    };
    const ClearIncrementalStateWire = struct {
        const DATA_SOURCE_IDS_WIRE: gremlin.ProtoWireNumber = 1;
    };
    pub const ClearIncrementalState = struct {
        // fields
        data_source_ids: ?[]const u64 = null,
        pub fn calcProtobufSize(self: *const GetAsyncCommandResponse.ClearIncrementalState) usize {
            var res: usize = 0;
            if (self.data_source_ids) |arr| {
                if (arr.len == 0) {} else if (arr.len == 1) {
                    res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.ClearIncrementalStateWire.DATA_SOURCE_IDS_WIRE) + gremlin.sizes.sizeU64(arr[0]);
                } else {
                    var packed_size: usize = 0;
                    for (arr) |v| {
                        packed_size += gremlin.sizes.sizeU64(v);
                    }
                    res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponse.ClearIncrementalStateWire.DATA_SOURCE_IDS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
                }
            }
            return res;
        }
        pub fn encode(self: *const GetAsyncCommandResponse.ClearIncrementalState, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const GetAsyncCommandResponse.ClearIncrementalState, target: *gremlin.Writer) void {
            if (self.data_source_ids) |arr| {
                if (arr.len == 0) {} else if (arr.len == 1) {
                    target.appendUint64(GetAsyncCommandResponse.ClearIncrementalStateWire.DATA_SOURCE_IDS_WIRE, arr[0]);
                } else {
                    var packed_size: usize = 0;
                    for (arr) |v| {
                        packed_size += gremlin.sizes.sizeU64(v);
                    }
                    target.appendBytesTag(GetAsyncCommandResponse.ClearIncrementalStateWire.DATA_SOURCE_IDS_WIRE, packed_size);
                    for (arr) |v| {
                        target.appendUint64WithoutTag(v);
                    }
                }
            }
        }
    };
    pub const ClearIncrementalStateReader = struct {
        buf: gremlin.Reader,
        _data_source_ids_offset: ?usize = null,
        _data_source_ids_last_offset: ?usize = null,
        _data_source_ids_packed: bool = false,
        pub fn init(src: []const u8) gremlin.Error!GetAsyncCommandResponse.ClearIncrementalStateReader {
            const buf = gremlin.Reader.init(src);
            var res = GetAsyncCommandResponse.ClearIncrementalStateReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    GetAsyncCommandResponse.ClearIncrementalStateWire.DATA_SOURCE_IDS_WIRE => {
                        if (res._data_source_ids_offset == null) {
                            res._data_source_ids_offset = offset;
                        }
                        if (tag.wire == gremlin.ProtoWireType.bytes) {
                            res._data_source_ids_packed = true;
                            const length_result = try buf.readVarInt(offset);
                            res._data_source_ids_offset = offset + length_result.size;
                            res._data_source_ids_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                            offset = res._data_source_ids_last_offset.?;
                        } else {
                            const result = try buf.readUInt64(offset);
                            offset += result.size;
                            res._data_source_ids_last_offset = offset;
                        }
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub fn dataSourceIdsNext(self: *GetAsyncCommandResponse.ClearIncrementalStateReader) gremlin.Error!?u64 {
            if (self._data_source_ids_offset == null) return null;
            const current_offset = self._data_source_ids_offset.?;
            if (current_offset >= self._data_source_ids_last_offset.?) {
                self._data_source_ids_offset = null;
                return null;
            }
            if (self._data_source_ids_packed) {
                const value_result = try self.buf.readUInt64(current_offset);
                self._data_source_ids_offset = current_offset + value_result.size;
                if (self._data_source_ids_offset.? >= self._data_source_ids_last_offset.?) {
                    self._data_source_ids_offset = null;
                }
                return value_result.value;
            } else {
                const value_result = try self.buf.readUInt64(current_offset);
                var next_offset = current_offset + value_result.size;
                const max_offset = self._data_source_ids_last_offset.?;
                // Search for the next occurrence of this field
                while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                    const next_tag = try self.buf.readTagAt(next_offset);
                    next_offset += next_tag.size;
                    if (next_tag.number == GetAsyncCommandResponse.ClearIncrementalStateWire.DATA_SOURCE_IDS_WIRE) {
                        self._data_source_ids_offset = next_offset;
                        return value_result.value;
                    } else {
                        next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                    }
                }
                self._data_source_ids_offset = null;
                return value_result.value;
            }
        }
    };
    // fields
    setup_tracing: ?GetAsyncCommandResponse.SetupTracing = null,
    setup_data_source: ?GetAsyncCommandResponse.SetupDataSource = null,
    start_data_source: ?GetAsyncCommandResponse.StartDataSource = null,
    stop_data_source: ?GetAsyncCommandResponse.StopDataSource = null,
    flush: ?GetAsyncCommandResponse.Flush = null,
    clear_incremental_state: ?GetAsyncCommandResponse.ClearIncrementalState = null,
    pub fn calcProtobufSize(self: *const GetAsyncCommandResponse) usize {
        var res: usize = 0;
        if (self.setup_tracing) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponseWire.SETUP_TRACING_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.setup_data_source) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponseWire.SETUP_DATA_SOURCE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.start_data_source) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponseWire.START_DATA_SOURCE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.stop_data_source) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponseWire.STOP_DATA_SOURCE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.flush) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponseWire.FLUSH_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.clear_incremental_state) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(GetAsyncCommandResponseWire.CLEAR_INCREMENTAL_STATE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const GetAsyncCommandResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GetAsyncCommandResponse, target: *gremlin.Writer) void {
        if (self.setup_tracing) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GetAsyncCommandResponseWire.SETUP_TRACING_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.setup_data_source) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GetAsyncCommandResponseWire.SETUP_DATA_SOURCE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.start_data_source) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GetAsyncCommandResponseWire.START_DATA_SOURCE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.stop_data_source) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GetAsyncCommandResponseWire.STOP_DATA_SOURCE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.flush) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GetAsyncCommandResponseWire.FLUSH_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.clear_incremental_state) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(GetAsyncCommandResponseWire.CLEAR_INCREMENTAL_STATE_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const GetAsyncCommandResponseReader = struct {
    buf: gremlin.Reader,
    _setup_tracing_buf: ?[]const u8 = null,
    _setup_data_source_buf: ?[]const u8 = null,
    _start_data_source_buf: ?[]const u8 = null,
    _stop_data_source_buf: ?[]const u8 = null,
    _flush_buf: ?[]const u8 = null,
    _clear_incremental_state_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!GetAsyncCommandResponseReader {
        const buf = gremlin.Reader.init(src);
        var res = GetAsyncCommandResponseReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GetAsyncCommandResponseWire.SETUP_TRACING_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._setup_tracing_buf = result.value;
                },
                GetAsyncCommandResponseWire.SETUP_DATA_SOURCE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._setup_data_source_buf = result.value;
                },
                GetAsyncCommandResponseWire.START_DATA_SOURCE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._start_data_source_buf = result.value;
                },
                GetAsyncCommandResponseWire.STOP_DATA_SOURCE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._stop_data_source_buf = result.value;
                },
                GetAsyncCommandResponseWire.FLUSH_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._flush_buf = result.value;
                },
                GetAsyncCommandResponseWire.CLEAR_INCREMENTAL_STATE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._clear_incremental_state_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getSetupTracing(self: *const GetAsyncCommandResponseReader) gremlin.Error!GetAsyncCommandResponse.SetupTracingReader {
        if (self._setup_tracing_buf) |buf| {
            return try GetAsyncCommandResponse.SetupTracingReader.init(buf);
        }
        return try GetAsyncCommandResponse.SetupTracingReader.init(&[_]u8{});
    }
    pub fn getSetupDataSource(self: *const GetAsyncCommandResponseReader) gremlin.Error!GetAsyncCommandResponse.SetupDataSourceReader {
        if (self._setup_data_source_buf) |buf| {
            return try GetAsyncCommandResponse.SetupDataSourceReader.init(buf);
        }
        return try GetAsyncCommandResponse.SetupDataSourceReader.init(&[_]u8{});
    }
    pub fn getStartDataSource(self: *const GetAsyncCommandResponseReader) gremlin.Error!GetAsyncCommandResponse.StartDataSourceReader {
        if (self._start_data_source_buf) |buf| {
            return try GetAsyncCommandResponse.StartDataSourceReader.init(buf);
        }
        return try GetAsyncCommandResponse.StartDataSourceReader.init(&[_]u8{});
    }
    pub fn getStopDataSource(self: *const GetAsyncCommandResponseReader) gremlin.Error!GetAsyncCommandResponse.StopDataSourceReader {
        if (self._stop_data_source_buf) |buf| {
            return try GetAsyncCommandResponse.StopDataSourceReader.init(buf);
        }
        return try GetAsyncCommandResponse.StopDataSourceReader.init(&[_]u8{});
    }
    pub fn getFlush(self: *const GetAsyncCommandResponseReader) gremlin.Error!GetAsyncCommandResponse.FlushReader {
        if (self._flush_buf) |buf| {
            return try GetAsyncCommandResponse.FlushReader.init(buf);
        }
        return try GetAsyncCommandResponse.FlushReader.init(&[_]u8{});
    }
    pub fn getClearIncrementalState(self: *const GetAsyncCommandResponseReader) gremlin.Error!GetAsyncCommandResponse.ClearIncrementalStateReader {
        if (self._clear_incremental_state_buf) |buf| {
            return try GetAsyncCommandResponse.ClearIncrementalStateReader.init(buf);
        }
        return try GetAsyncCommandResponse.ClearIncrementalStateReader.init(&[_]u8{});
    }
};
pub const SyncRequest = struct {
    pub fn calcProtobufSize(_: *const SyncRequest) usize {
        return 0;
    }
    pub fn encode(self: *const SyncRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const SyncRequest, _: *gremlin.Writer) void {}
};
pub const SyncRequestReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!SyncRequestReader {
        const buf = gremlin.Reader.init(src);
        return SyncRequestReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
pub const SyncResponse = struct {
    pub fn calcProtobufSize(_: *const SyncResponse) usize {
        return 0;
    }
    pub fn encode(self: *const SyncResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const SyncResponse, _: *gremlin.Writer) void {}
};
pub const SyncResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!SyncResponseReader {
        const buf = gremlin.Reader.init(src);
        return SyncResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
