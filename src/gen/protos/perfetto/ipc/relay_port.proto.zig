// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const system_info = @import("src/gen/protos/perfetto/common/system_info.proto.zig");
// structs
const InitRelayRequestWire = struct {
    const SYSTEM_INFO_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const InitRelayRequest = struct {
    // fields
    system_info: ?system_info.SystemInfo = null,
    pub fn calcProtobufSize(self: *const InitRelayRequest) usize {
        var res: usize = 0;
        if (self.system_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(InitRelayRequestWire.SYSTEM_INFO_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const InitRelayRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const InitRelayRequest, target: *gremlin.Writer) void {
        if (self.system_info) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(InitRelayRequestWire.SYSTEM_INFO_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const InitRelayRequestReader = struct {
    buf: gremlin.Reader,
    _system_info_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!InitRelayRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = InitRelayRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                InitRelayRequestWire.SYSTEM_INFO_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._system_info_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn getSystemInfo(self: *const InitRelayRequestReader) gremlin.Error!system_info.SystemInfoReader {
        if (self._system_info_buf) |buf| {
            return try system_info.SystemInfoReader.init(buf);
        }
        return try system_info.SystemInfoReader.init(&[_]u8{});
    }
};
pub const InitRelayResponse = struct {
    pub fn calcProtobufSize(_: *const InitRelayResponse) usize {
        return 0;
    }
    pub fn encode(self: *const InitRelayResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const InitRelayResponse, _: *gremlin.Writer) void {}
};
pub const InitRelayResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!InitRelayResponseReader {
        const buf = gremlin.Reader.init(src);
        return InitRelayResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
const SyncClockRequestWire = struct {
    const PHASE_WIRE: gremlin.ProtoWireNumber = 1;
    const CLOCKS_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const SyncClockRequest = struct {
    // nested enums
    pub const Phase = enum(i32) {
        PING = 1,
        UPDATE = 2,
        _PROTOBUF_UNKNOWN = 0,
    };
    // nested structs
    const ClockWire = struct {
        const CLOCK_ID_WIRE: gremlin.ProtoWireNumber = 1;
        const TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const Clock = struct {
        // fields
        clock_id: u32 = 0,
        timestamp: u64 = 0,
        pub fn calcProtobufSize(self: *const SyncClockRequest.Clock) usize {
            var res: usize = 0;
            if (self.clock_id != 0) {
                res += gremlin.sizes.sizeWireNumber(SyncClockRequest.ClockWire.CLOCK_ID_WIRE) + gremlin.sizes.sizeU32(self.clock_id);
            }
            if (self.timestamp != 0) {
                res += gremlin.sizes.sizeWireNumber(SyncClockRequest.ClockWire.TIMESTAMP_WIRE) + gremlin.sizes.sizeU64(self.timestamp);
            }
            return res;
        }
        pub fn encode(self: *const SyncClockRequest.Clock, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const SyncClockRequest.Clock, target: *gremlin.Writer) void {
            if (self.clock_id != 0) {
                target.appendUint32(SyncClockRequest.ClockWire.CLOCK_ID_WIRE, self.clock_id);
            }
            if (self.timestamp != 0) {
                target.appendUint64(SyncClockRequest.ClockWire.TIMESTAMP_WIRE, self.timestamp);
            }
        }
    };
    pub const ClockReader = struct {
        buf: gremlin.Reader,
        _clock_id: u32 = 0,
        _timestamp: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!SyncClockRequest.ClockReader {
            const buf = gremlin.Reader.init(src);
            var res = SyncClockRequest.ClockReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    SyncClockRequest.ClockWire.CLOCK_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._clock_id = result.value;
                    },
                    SyncClockRequest.ClockWire.TIMESTAMP_WIRE => {
                        const result = try buf.readUInt64(offset);
                        offset += result.size;
                        res._timestamp = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getClockId(self: *const SyncClockRequest.ClockReader) u32 {
            return self._clock_id;
        }
        pub inline fn getTimestamp(self: *const SyncClockRequest.ClockReader) u64 {
            return self._timestamp;
        }
    };
    // fields
    phase: SyncClockRequest.Phase = @enumFromInt(0),
    clocks: ?[]const ?SyncClockRequest.Clock = null,
    pub fn calcProtobufSize(self: *const SyncClockRequest) usize {
        var res: usize = 0;
        if (@intFromEnum(self.phase) != 0) {
            res += gremlin.sizes.sizeWireNumber(SyncClockRequestWire.PHASE_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.phase));
        }
        if (self.clocks) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(SyncClockRequestWire.CLOCKS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const SyncClockRequest, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SyncClockRequest, target: *gremlin.Writer) void {
        if (@intFromEnum(self.phase) != 0) {
            target.appendInt32(SyncClockRequestWire.PHASE_WIRE, @intFromEnum(self.phase));
        }
        if (self.clocks) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(SyncClockRequestWire.CLOCKS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(SyncClockRequestWire.CLOCKS_WIRE, 0);
                }
            }
        }
    }
};
pub const SyncClockRequestReader = struct {
    buf: gremlin.Reader,
    _phase: SyncClockRequest.Phase = @enumFromInt(0),
    _clocks_offset: ?usize = null,
    _clocks_last_offset: ?usize = null,
    _clocks_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!SyncClockRequestReader {
        const buf = gremlin.Reader.init(src);
        var res = SyncClockRequestReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SyncClockRequestWire.PHASE_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._phase = @enumFromInt(result.value);
                },
                SyncClockRequestWire.CLOCKS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._clocks_offset == null) {
                        res._clocks_offset = offset - result.size;
                    }
                    res._clocks_last_offset = offset;
                    res._clocks_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getPhase(self: *const SyncClockRequestReader) SyncClockRequest.Phase {
        return self._phase;
    }
    pub fn clocksCount(self: *const SyncClockRequestReader) usize {
        return self._clocks_cnt;
    }
    pub fn clocksNext(self: *SyncClockRequestReader) ?SyncClockRequest.ClockReader {
        if (self._clocks_offset == null) return null;
        const current_offset = self._clocks_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = SyncClockRequest.ClockReader.init(result.value) catch return null;
        if (self._clocks_last_offset != null and current_offset >= self._clocks_last_offset.?) {
            self._clocks_offset = null;
            return msg;
        }
        if (self._clocks_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._clocks_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == SyncClockRequestWire.CLOCKS_WIRE) {
                self._clocks_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._clocks_offset = null;
        return msg;
    }
};
pub const SyncClockResponse = struct {
    pub fn calcProtobufSize(_: *const SyncClockResponse) usize {
        return 0;
    }
    pub fn encode(self: *const SyncClockResponse, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(_: *const SyncClockResponse, _: *gremlin.Writer) void {}
};
pub const SyncClockResponseReader = struct {
    buf: gremlin.Reader,
    pub fn init(src: []const u8) gremlin.Error!SyncClockResponseReader {
        const buf = gremlin.Reader.init(src);
        return SyncClockResponseReader{ .buf = buf };
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
};
