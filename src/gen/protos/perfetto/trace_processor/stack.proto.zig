// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const StackWire = struct {
    const ENTRIES_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const Stack = struct {
    // nested structs
    const EntryWire = struct {
        const NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const CALLSITE_ID_WIRE: gremlin.ProtoWireNumber = 2;
        const ANNOTATED_CALLSITE_ID_WIRE: gremlin.ProtoWireNumber = 3;
        const FRAME_ID_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const Entry = struct {
        // fields
        name: ?[]const u8 = null,
        callsite_id: u32 = 0,
        annotated_callsite_id: u32 = 0,
        frame_id: u32 = 0,
        pub fn calcProtobufSize(self: *const Stack.Entry) usize {
            var res: usize = 0;
            if (self.name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(Stack.EntryWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.callsite_id != 0) {
                res += gremlin.sizes.sizeWireNumber(Stack.EntryWire.CALLSITE_ID_WIRE) + gremlin.sizes.sizeU32(self.callsite_id);
            }
            if (self.annotated_callsite_id != 0) {
                res += gremlin.sizes.sizeWireNumber(Stack.EntryWire.ANNOTATED_CALLSITE_ID_WIRE) + gremlin.sizes.sizeU32(self.annotated_callsite_id);
            }
            if (self.frame_id != 0) {
                res += gremlin.sizes.sizeWireNumber(Stack.EntryWire.FRAME_ID_WIRE) + gremlin.sizes.sizeU32(self.frame_id);
            }
            return res;
        }
        pub fn encode(self: *const Stack.Entry, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const Stack.Entry, target: *gremlin.Writer) void {
            if (self.name) |v| {
                if (v.len > 0) {
                    target.appendBytes(Stack.EntryWire.NAME_WIRE, v);
                }
            }
            if (self.callsite_id != 0) {
                target.appendUint32(Stack.EntryWire.CALLSITE_ID_WIRE, self.callsite_id);
            }
            if (self.annotated_callsite_id != 0) {
                target.appendUint32(Stack.EntryWire.ANNOTATED_CALLSITE_ID_WIRE, self.annotated_callsite_id);
            }
            if (self.frame_id != 0) {
                target.appendUint32(Stack.EntryWire.FRAME_ID_WIRE, self.frame_id);
            }
        }
    };
    pub const EntryReader = struct {
        buf: gremlin.Reader,
        _name: ?[]const u8 = null,
        _callsite_id: u32 = 0,
        _annotated_callsite_id: u32 = 0,
        _frame_id: u32 = 0,
        pub fn init(src: []const u8) gremlin.Error!Stack.EntryReader {
            const buf = gremlin.Reader.init(src);
            var res = Stack.EntryReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    Stack.EntryWire.NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._name = result.value;
                    },
                    Stack.EntryWire.CALLSITE_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._callsite_id = result.value;
                    },
                    Stack.EntryWire.ANNOTATED_CALLSITE_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._annotated_callsite_id = result.value;
                    },
                    Stack.EntryWire.FRAME_ID_WIRE => {
                        const result = try buf.readUInt32(offset);
                        offset += result.size;
                        res._frame_id = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getName(self: *const Stack.EntryReader) []const u8 {
            return self._name orelse &[_]u8{};
        }
        pub inline fn getCallsiteId(self: *const Stack.EntryReader) u32 {
            return self._callsite_id;
        }
        pub inline fn getAnnotatedCallsiteId(self: *const Stack.EntryReader) u32 {
            return self._annotated_callsite_id;
        }
        pub inline fn getFrameId(self: *const Stack.EntryReader) u32 {
            return self._frame_id;
        }
    };
    // fields
    entries: ?[]const ?Stack.Entry = null,
    pub fn calcProtobufSize(self: *const Stack) usize {
        var res: usize = 0;
        if (self.entries) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(StackWire.ENTRIES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const Stack, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const Stack, target: *gremlin.Writer) void {
        if (self.entries) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(StackWire.ENTRIES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(StackWire.ENTRIES_WIRE, 0);
                }
            }
        }
    }
};
pub const StackReader = struct {
    buf: gremlin.Reader,
    _entries_offset: ?usize = null,
    _entries_last_offset: ?usize = null,
    _entries_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!StackReader {
        const buf = gremlin.Reader.init(src);
        var res = StackReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                StackWire.ENTRIES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._entries_offset == null) {
                        res._entries_offset = offset - result.size;
                    }
                    res._entries_last_offset = offset;
                    res._entries_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn entriesCount(self: *const StackReader) usize {
        return self._entries_cnt;
    }
    pub fn entriesNext(self: *StackReader) ?Stack.EntryReader {
        if (self._entries_offset == null) return null;
        const current_offset = self._entries_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = Stack.EntryReader.init(result.value) catch return null;
        if (self._entries_last_offset != null and current_offset >= self._entries_last_offset.?) {
            self._entries_offset = null;
            return msg;
        }
        if (self._entries_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._entries_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == StackWire.ENTRIES_WIRE) {
                self._entries_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._entries_offset = null;
        return msg;
    }
};
