// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ProtoBuilderResultWire = struct {
    const IS_REPEATED_WIRE: gremlin.ProtoWireNumber = 1;
    const SINGLE_WIRE: gremlin.ProtoWireNumber = 2;
    const REPEATED_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ProtoBuilderResult = struct {
    // fields
    is_repeated: bool = false,
    single: ?SingleBuilderResult = null,
    repeated: ?RepeatedBuilderResult = null,
    pub fn calcProtobufSize(self: *const ProtoBuilderResult) usize {
        var res: usize = 0;
        if (self.is_repeated != false) {
            res += gremlin.sizes.sizeWireNumber(ProtoBuilderResultWire.IS_REPEATED_WIRE) + gremlin.sizes.sizeBool(self.is_repeated);
        }
        if (self.single) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(ProtoBuilderResultWire.SINGLE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.repeated) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(ProtoBuilderResultWire.REPEATED_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const ProtoBuilderResult, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ProtoBuilderResult, target: *gremlin.Writer) void {
        if (self.is_repeated != false) {
            target.appendBool(ProtoBuilderResultWire.IS_REPEATED_WIRE, self.is_repeated);
        }
        if (self.single) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(ProtoBuilderResultWire.SINGLE_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.repeated) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(ProtoBuilderResultWire.REPEATED_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const ProtoBuilderResultReader = struct {
    buf: gremlin.Reader,
    _is_repeated: bool = false,
    _single_buf: ?[]const u8 = null,
    _repeated_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ProtoBuilderResultReader {
        const buf = gremlin.Reader.init(src);
        var res = ProtoBuilderResultReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ProtoBuilderResultWire.IS_REPEATED_WIRE => {
                    const result = try buf.readBool(offset);
                    offset += result.size;
                    res._is_repeated = result.value;
                },
                ProtoBuilderResultWire.SINGLE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._single_buf = result.value;
                },
                ProtoBuilderResultWire.REPEATED_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._repeated_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIsRepeated(self: *const ProtoBuilderResultReader) bool {
        return self._is_repeated;
    }
    pub fn getSingle(self: *const ProtoBuilderResultReader) gremlin.Error!SingleBuilderResultReader {
        if (self._single_buf) |buf| {
            return try SingleBuilderResultReader.init(buf);
        }
        return try SingleBuilderResultReader.init(&[_]u8{});
    }
    pub fn getRepeated(self: *const ProtoBuilderResultReader) gremlin.Error!RepeatedBuilderResultReader {
        if (self._repeated_buf) |buf| {
            return try RepeatedBuilderResultReader.init(buf);
        }
        return try RepeatedBuilderResultReader.init(&[_]u8{});
    }
};
const RepeatedBuilderResultWire = struct {
    const INT_VALUES_WIRE: gremlin.ProtoWireNumber = 1;
    const DOUBLE_VALUES_WIRE: gremlin.ProtoWireNumber = 2;
    const STRING_VALUES_WIRE: gremlin.ProtoWireNumber = 3;
    const BYTE_VALUES_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const RepeatedBuilderResult = struct {
    // fields
    int_values: ?[]const i64 = null,
    double_values: ?[]const f64 = null,
    string_values: ?[]const ?[]const u8 = null,
    byte_values: ?[]const ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RepeatedBuilderResult) usize {
        var res: usize = 0;
        if (self.int_values) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(RepeatedBuilderResultWire.INT_VALUES_WIRE) + gremlin.sizes.sizeSFixed64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeSFixed64(v);
                }
                res += gremlin.sizes.sizeWireNumber(RepeatedBuilderResultWire.INT_VALUES_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.double_values) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(RepeatedBuilderResultWire.DOUBLE_VALUES_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(RepeatedBuilderResultWire.DOUBLE_VALUES_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.string_values) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(RepeatedBuilderResultWire.STRING_VALUES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.byte_values) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(RepeatedBuilderResultWire.BYTE_VALUES_WIRE);
                if (maybe_v) |v| {
                    res += gremlin.sizes.sizeUsize(v.len) + v.len;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const RepeatedBuilderResult, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RepeatedBuilderResult, target: *gremlin.Writer) void {
        if (self.int_values) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendSfixed64(RepeatedBuilderResultWire.INT_VALUES_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeSFixed64(v);
                }
                target.appendBytesTag(RepeatedBuilderResultWire.INT_VALUES_WIRE, packed_size);
                for (arr) |v| {
                    target.appendSfixed64WithoutTag(v);
                }
            }
        }
        if (self.double_values) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(RepeatedBuilderResultWire.DOUBLE_VALUES_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(RepeatedBuilderResultWire.DOUBLE_VALUES_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.string_values) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(RepeatedBuilderResultWire.STRING_VALUES_WIRE, v);
                } else {
                    target.appendBytesTag(RepeatedBuilderResultWire.STRING_VALUES_WIRE, 0);
                }
            }
        }
        if (self.byte_values) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    target.appendBytes(RepeatedBuilderResultWire.BYTE_VALUES_WIRE, v);
                } else {
                    target.appendBytesTag(RepeatedBuilderResultWire.BYTE_VALUES_WIRE, 0);
                }
            }
        }
    }
};
pub const RepeatedBuilderResultReader = struct {
    buf: gremlin.Reader,
    _int_values_offset: ?usize = null,
    _int_values_last_offset: ?usize = null,
    _int_values_packed: bool = false,
    _double_values_offset: ?usize = null,
    _double_values_last_offset: ?usize = null,
    _double_values_packed: bool = false,
    _string_values_offset: ?usize = null,
    _string_values_last_offset: ?usize = null,
    _string_values_cnt: usize = 0,
    _byte_values_offset: ?usize = null,
    _byte_values_last_offset: ?usize = null,
    _byte_values_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!RepeatedBuilderResultReader {
        const buf = gremlin.Reader.init(src);
        var res = RepeatedBuilderResultReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RepeatedBuilderResultWire.INT_VALUES_WIRE => {
                    if (res._int_values_offset == null) {
                        res._int_values_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._int_values_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._int_values_offset = offset + length_result.size;
                        res._int_values_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._int_values_last_offset.?;
                    } else {
                        const result = try buf.readSFixed64(offset);
                        offset += result.size;
                        res._int_values_last_offset = offset;
                    }
                },
                RepeatedBuilderResultWire.DOUBLE_VALUES_WIRE => {
                    if (res._double_values_offset == null) {
                        res._double_values_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._double_values_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._double_values_offset = offset + length_result.size;
                        res._double_values_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._double_values_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._double_values_last_offset = offset;
                    }
                },
                RepeatedBuilderResultWire.STRING_VALUES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._string_values_offset == null) {
                        res._string_values_offset = offset - result.size;
                    }
                    res._string_values_last_offset = offset;
                    res._string_values_cnt += 1;
                },
                RepeatedBuilderResultWire.BYTE_VALUES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._byte_values_offset == null) {
                        res._byte_values_offset = offset - result.size;
                    }
                    res._byte_values_last_offset = offset;
                    res._byte_values_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn intValuesNext(self: *RepeatedBuilderResultReader) gremlin.Error!?i64 {
        if (self._int_values_offset == null) return null;
        const current_offset = self._int_values_offset.?;
        if (current_offset >= self._int_values_last_offset.?) {
            self._int_values_offset = null;
            return null;
        }
        if (self._int_values_packed) {
            const value_result = try self.buf.readSFixed64(current_offset);
            self._int_values_offset = current_offset + value_result.size;
            if (self._int_values_offset.? >= self._int_values_last_offset.?) {
                self._int_values_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readSFixed64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._int_values_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == RepeatedBuilderResultWire.INT_VALUES_WIRE) {
                    self._int_values_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._int_values_offset = null;
            return value_result.value;
        }
    }
    pub fn doubleValuesNext(self: *RepeatedBuilderResultReader) gremlin.Error!?f64 {
        if (self._double_values_offset == null) return null;
        const current_offset = self._double_values_offset.?;
        if (current_offset >= self._double_values_last_offset.?) {
            self._double_values_offset = null;
            return null;
        }
        if (self._double_values_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._double_values_offset = current_offset + value_result.size;
            if (self._double_values_offset.? >= self._double_values_last_offset.?) {
                self._double_values_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._double_values_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == RepeatedBuilderResultWire.DOUBLE_VALUES_WIRE) {
                    self._double_values_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._double_values_offset = null;
            return value_result.value;
        }
    }
    pub fn stringValuesCount(self: *const RepeatedBuilderResultReader) usize {
        return self._string_values_cnt;
    }
    pub fn stringValuesNext(self: *RepeatedBuilderResultReader) ?[]const u8 {
        if (self._string_values_offset == null) return null;
        const current_offset = self._string_values_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._string_values_last_offset != null and current_offset >= self._string_values_last_offset.?) {
            self._string_values_offset = null;
            return result.value;
        }
        if (self._string_values_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._string_values_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == RepeatedBuilderResultWire.STRING_VALUES_WIRE) {
                self._string_values_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._string_values_offset = null;
        return result.value;
    }
    pub fn byteValuesCount(self: *const RepeatedBuilderResultReader) usize {
        return self._byte_values_cnt;
    }
    pub fn byteValuesNext(self: *RepeatedBuilderResultReader) ?[]const u8 {
        if (self._byte_values_offset == null) return null;
        const current_offset = self._byte_values_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        if (self._byte_values_last_offset != null and current_offset >= self._byte_values_last_offset.?) {
            self._byte_values_offset = null;
            return result.value;
        }
        if (self._byte_values_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._byte_values_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == RepeatedBuilderResultWire.BYTE_VALUES_WIRE) {
                self._byte_values_offset = next_offset;
                return result.value;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._byte_values_offset = null;
        return result.value;
    }
};
const SingleBuilderResultWire = struct {
    const TYPE_WIRE: gremlin.ProtoWireNumber = 1;
    const TYPE_NAME_WIRE: gremlin.ProtoWireNumber = 2;
    const PROTOBUF_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const SingleBuilderResult = struct {
    // fields
    type: u32 = 0,
    type_name: ?[]const u8 = null,
    protobuf: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const SingleBuilderResult) usize {
        var res: usize = 0;
        if (self.type != 0) {
            res += gremlin.sizes.sizeWireNumber(SingleBuilderResultWire.TYPE_WIRE) + gremlin.sizes.sizeU32(self.type);
        }
        if (self.type_name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SingleBuilderResultWire.TYPE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.protobuf) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SingleBuilderResultWire.PROTOBUF_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const SingleBuilderResult, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SingleBuilderResult, target: *gremlin.Writer) void {
        if (self.type != 0) {
            target.appendUint32(SingleBuilderResultWire.TYPE_WIRE, self.type);
        }
        if (self.type_name) |v| {
            if (v.len > 0) {
                target.appendBytes(SingleBuilderResultWire.TYPE_NAME_WIRE, v);
            }
        }
        if (self.protobuf) |v| {
            if (v.len > 0) {
                target.appendBytes(SingleBuilderResultWire.PROTOBUF_WIRE, v);
            }
        }
    }
};
pub const SingleBuilderResultReader = struct {
    buf: gremlin.Reader,
    _type: u32 = 0,
    _type_name: ?[]const u8 = null,
    _protobuf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!SingleBuilderResultReader {
        const buf = gremlin.Reader.init(src);
        var res = SingleBuilderResultReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SingleBuilderResultWire.TYPE_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._type = result.value;
                },
                SingleBuilderResultWire.TYPE_NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._type_name = result.value;
                },
                SingleBuilderResultWire.PROTOBUF_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._protobuf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getType(self: *const SingleBuilderResultReader) u32 {
        return self._type;
    }
    pub inline fn getTypeName(self: *const SingleBuilderResultReader) []const u8 {
        return self._type_name orelse &[_]u8{};
    }
    pub inline fn getProtobuf(self: *const SingleBuilderResultReader) []const u8 {
        return self._protobuf orelse &[_]u8{};
    }
};
