// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const v2_metric = @import("v2_metric.proto.zig");
const structured_query = @import("src/gen/protos/perfetto/perfetto_sql/structured_query.proto.zig");
// structs
const TraceSummarySpecWire = struct {
    const METRIC_SPEC_WIRE: gremlin.ProtoWireNumber = 1;
    const QUERY_WIRE: gremlin.ProtoWireNumber = 2;
    const METRIC_TEMPLATE_SPEC_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const TraceSummarySpec = struct {
    // fields
    metric_spec: ?[]const ?v2_metric.TraceMetricV2Spec = null,
    query: ?[]const ?structured_query.PerfettoSqlStructuredQuery = null,
    metric_template_spec: ?[]const ?v2_metric.TraceMetricV2TemplateSpec = null,
    pub fn calcProtobufSize(self: *const TraceSummarySpec) usize {
        var res: usize = 0;
        if (self.metric_spec) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(TraceSummarySpecWire.METRIC_SPEC_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.query) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(TraceSummarySpecWire.QUERY_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.metric_template_spec) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(TraceSummarySpecWire.METRIC_TEMPLATE_SPEC_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const TraceSummarySpec, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TraceSummarySpec, target: *gremlin.Writer) void {
        if (self.metric_spec) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(TraceSummarySpecWire.METRIC_SPEC_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(TraceSummarySpecWire.METRIC_SPEC_WIRE, 0);
                }
            }
        }
        if (self.query) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(TraceSummarySpecWire.QUERY_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(TraceSummarySpecWire.QUERY_WIRE, 0);
                }
            }
        }
        if (self.metric_template_spec) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(TraceSummarySpecWire.METRIC_TEMPLATE_SPEC_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(TraceSummarySpecWire.METRIC_TEMPLATE_SPEC_WIRE, 0);
                }
            }
        }
    }
};
pub const TraceSummarySpecReader = struct {
    buf: gremlin.Reader,
    _metric_spec_offset: ?usize = null,
    _metric_spec_last_offset: ?usize = null,
    _metric_spec_cnt: usize = 0,
    _query_offset: ?usize = null,
    _query_last_offset: ?usize = null,
    _query_cnt: usize = 0,
    _metric_template_spec_offset: ?usize = null,
    _metric_template_spec_last_offset: ?usize = null,
    _metric_template_spec_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!TraceSummarySpecReader {
        const buf = gremlin.Reader.init(src);
        var res = TraceSummarySpecReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TraceSummarySpecWire.METRIC_SPEC_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._metric_spec_offset == null) {
                        res._metric_spec_offset = offset - result.size;
                    }
                    res._metric_spec_last_offset = offset;
                    res._metric_spec_cnt += 1;
                },
                TraceSummarySpecWire.QUERY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._query_offset == null) {
                        res._query_offset = offset - result.size;
                    }
                    res._query_last_offset = offset;
                    res._query_cnt += 1;
                },
                TraceSummarySpecWire.METRIC_TEMPLATE_SPEC_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._metric_template_spec_offset == null) {
                        res._metric_template_spec_offset = offset - result.size;
                    }
                    res._metric_template_spec_last_offset = offset;
                    res._metric_template_spec_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn metricSpecCount(self: *const TraceSummarySpecReader) usize {
        return self._metric_spec_cnt;
    }
    pub fn metricSpecNext(self: *TraceSummarySpecReader) ?v2_metric.TraceMetricV2SpecReader {
        if (self._metric_spec_offset == null) return null;
        const current_offset = self._metric_spec_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = v2_metric.TraceMetricV2SpecReader.init(result.value) catch return null;
        if (self._metric_spec_last_offset != null and current_offset >= self._metric_spec_last_offset.?) {
            self._metric_spec_offset = null;
            return msg;
        }
        if (self._metric_spec_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._metric_spec_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == TraceSummarySpecWire.METRIC_SPEC_WIRE) {
                self._metric_spec_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._metric_spec_offset = null;
        return msg;
    }
    pub fn queryCount(self: *const TraceSummarySpecReader) usize {
        return self._query_cnt;
    }
    pub fn queryNext(self: *TraceSummarySpecReader) ?structured_query.PerfettoSqlStructuredQueryReader {
        if (self._query_offset == null) return null;
        const current_offset = self._query_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = structured_query.PerfettoSqlStructuredQueryReader.init(result.value) catch return null;
        if (self._query_last_offset != null and current_offset >= self._query_last_offset.?) {
            self._query_offset = null;
            return msg;
        }
        if (self._query_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._query_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == TraceSummarySpecWire.QUERY_WIRE) {
                self._query_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._query_offset = null;
        return msg;
    }
    pub fn metricTemplateSpecCount(self: *const TraceSummarySpecReader) usize {
        return self._metric_template_spec_cnt;
    }
    pub fn metricTemplateSpecNext(self: *TraceSummarySpecReader) ?v2_metric.TraceMetricV2TemplateSpecReader {
        if (self._metric_template_spec_offset == null) return null;
        const current_offset = self._metric_template_spec_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = v2_metric.TraceMetricV2TemplateSpecReader.init(result.value) catch return null;
        if (self._metric_template_spec_last_offset != null and current_offset >= self._metric_template_spec_last_offset.?) {
            self._metric_template_spec_offset = null;
            return msg;
        }
        if (self._metric_template_spec_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._metric_template_spec_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == TraceSummarySpecWire.METRIC_TEMPLATE_SPEC_WIRE) {
                self._metric_template_spec_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._metric_template_spec_offset = null;
        return msg;
    }
};
const TraceSummaryWire = struct {
    const METRIC_BUNDLES_WIRE: gremlin.ProtoWireNumber = 3;
    const METADATA_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const TraceSummary = struct {
    // nested structs
    const MetadataWire = struct {
        const KEY_WIRE: gremlin.ProtoWireNumber = 1;
        const VALUE_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const Metadata = struct {
        // fields
        key: ?[]const u8 = null,
        value: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const TraceSummary.Metadata) usize {
            var res: usize = 0;
            if (self.key) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(TraceSummary.MetadataWire.KEY_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.value) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(TraceSummary.MetadataWire.VALUE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const TraceSummary.Metadata, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const TraceSummary.Metadata, target: *gremlin.Writer) void {
            if (self.key) |v| {
                if (v.len > 0) {
                    target.appendBytes(TraceSummary.MetadataWire.KEY_WIRE, v);
                }
            }
            if (self.value) |v| {
                if (v.len > 0) {
                    target.appendBytes(TraceSummary.MetadataWire.VALUE_WIRE, v);
                }
            }
        }
    };
    pub const MetadataReader = struct {
        buf: gremlin.Reader,
        _key: ?[]const u8 = null,
        _value: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!TraceSummary.MetadataReader {
            const buf = gremlin.Reader.init(src);
            var res = TraceSummary.MetadataReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    TraceSummary.MetadataWire.KEY_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._key = result.value;
                    },
                    TraceSummary.MetadataWire.VALUE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._value = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getKey(self: *const TraceSummary.MetadataReader) []const u8 {
            return self._key orelse &[_]u8{};
        }
        pub inline fn getValue(self: *const TraceSummary.MetadataReader) []const u8 {
            return self._value orelse &[_]u8{};
        }
    };
    // fields
    metric_bundles: ?[]const ?v2_metric.TraceMetricV2Bundle = null,
    metadata: ?[]const ?TraceSummary.Metadata = null,
    pub fn calcProtobufSize(self: *const TraceSummary) usize {
        var res: usize = 0;
        if (self.metric_bundles) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(TraceSummaryWire.METRIC_BUNDLES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        if (self.metadata) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(TraceSummaryWire.METADATA_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const TraceSummary, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TraceSummary, target: *gremlin.Writer) void {
        if (self.metric_bundles) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(TraceSummaryWire.METRIC_BUNDLES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(TraceSummaryWire.METRIC_BUNDLES_WIRE, 0);
                }
            }
        }
        if (self.metadata) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(TraceSummaryWire.METADATA_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(TraceSummaryWire.METADATA_WIRE, 0);
                }
            }
        }
    }
};
pub const TraceSummaryReader = struct {
    buf: gremlin.Reader,
    _metric_bundles_offset: ?usize = null,
    _metric_bundles_last_offset: ?usize = null,
    _metric_bundles_cnt: usize = 0,
    _metadata_offset: ?usize = null,
    _metadata_last_offset: ?usize = null,
    _metadata_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!TraceSummaryReader {
        const buf = gremlin.Reader.init(src);
        var res = TraceSummaryReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TraceSummaryWire.METRIC_BUNDLES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._metric_bundles_offset == null) {
                        res._metric_bundles_offset = offset - result.size;
                    }
                    res._metric_bundles_last_offset = offset;
                    res._metric_bundles_cnt += 1;
                },
                TraceSummaryWire.METADATA_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._metadata_offset == null) {
                        res._metadata_offset = offset - result.size;
                    }
                    res._metadata_last_offset = offset;
                    res._metadata_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn metricBundlesCount(self: *const TraceSummaryReader) usize {
        return self._metric_bundles_cnt;
    }
    pub fn metricBundlesNext(self: *TraceSummaryReader) ?v2_metric.TraceMetricV2BundleReader {
        if (self._metric_bundles_offset == null) return null;
        const current_offset = self._metric_bundles_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = v2_metric.TraceMetricV2BundleReader.init(result.value) catch return null;
        if (self._metric_bundles_last_offset != null and current_offset >= self._metric_bundles_last_offset.?) {
            self._metric_bundles_offset = null;
            return msg;
        }
        if (self._metric_bundles_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._metric_bundles_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == TraceSummaryWire.METRIC_BUNDLES_WIRE) {
                self._metric_bundles_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._metric_bundles_offset = null;
        return msg;
    }
    pub fn metadataCount(self: *const TraceSummaryReader) usize {
        return self._metadata_cnt;
    }
    pub fn metadataNext(self: *TraceSummaryReader) ?TraceSummary.MetadataReader {
        if (self._metadata_offset == null) return null;
        const current_offset = self._metadata_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = TraceSummary.MetadataReader.init(result.value) catch return null;
        if (self._metadata_last_offset != null and current_offset >= self._metadata_last_offset.?) {
            self._metadata_offset = null;
            return msg;
        }
        if (self._metadata_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._metadata_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == TraceSummaryWire.METADATA_WIRE) {
                self._metadata_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._metadata_offset = null;
        return msg;
    }
};
