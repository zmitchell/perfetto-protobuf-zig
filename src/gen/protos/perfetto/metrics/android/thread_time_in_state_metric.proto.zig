// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const process_metadata = @import("process_metadata.proto.zig");
// structs
const AndroidThreadTimeInStateMetricWire = struct {
    const PROCESSES_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const AndroidThreadTimeInStateMetric = struct {
    // nested structs
    const MetricsByCoreTypeWire = struct {
        const TIME_IN_STATE_CPU_WIRE: gremlin.ProtoWireNumber = 5;
        const CORE_TYPE_WIRE: gremlin.ProtoWireNumber = 1;
        const RUNTIME_MS_WIRE: gremlin.ProtoWireNumber = 2;
        const MCYCLES_WIRE: gremlin.ProtoWireNumber = 3;
        const POWER_PROFILE_MAH_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const MetricsByCoreType = struct {
        // fields
        time_in_state_cpu: i32 = 0,
        core_type: ?[]const u8 = null,
        runtime_ms: i64 = 0,
        mcycles: i64 = 0,
        power_profile_mah: f64 = 0.0,
        pub fn calcProtobufSize(self: *const AndroidThreadTimeInStateMetric.MetricsByCoreType) usize {
            var res: usize = 0;
            if (self.time_in_state_cpu != 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.TIME_IN_STATE_CPU_WIRE) + gremlin.sizes.sizeI32(self.time_in_state_cpu);
            }
            if (self.core_type) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.CORE_TYPE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.runtime_ms != 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.RUNTIME_MS_WIRE) + gremlin.sizes.sizeI64(self.runtime_ms);
            }
            if (self.mcycles != 0) {
                res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.MCYCLES_WIRE) + gremlin.sizes.sizeI64(self.mcycles);
            }
            if (self.power_profile_mah != 0.0) {
                res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.POWER_PROFILE_MAH_WIRE) + gremlin.sizes.sizeDouble(self.power_profile_mah);
            }
            return res;
        }
        pub fn encode(self: *const AndroidThreadTimeInStateMetric.MetricsByCoreType, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const AndroidThreadTimeInStateMetric.MetricsByCoreType, target: *gremlin.Writer) void {
            if (self.time_in_state_cpu != 0) {
                target.appendInt32(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.TIME_IN_STATE_CPU_WIRE, self.time_in_state_cpu);
            }
            if (self.core_type) |v| {
                if (v.len > 0) {
                    target.appendBytes(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.CORE_TYPE_WIRE, v);
                }
            }
            if (self.runtime_ms != 0) {
                target.appendInt64(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.RUNTIME_MS_WIRE, self.runtime_ms);
            }
            if (self.mcycles != 0) {
                target.appendInt64(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.MCYCLES_WIRE, self.mcycles);
            }
            if (self.power_profile_mah != 0.0) {
                target.appendFloat64(AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.POWER_PROFILE_MAH_WIRE, self.power_profile_mah);
            }
        }
    };
    pub const MetricsByCoreTypeReader = struct {
        buf: gremlin.Reader,
        _time_in_state_cpu: i32 = 0,
        _core_type: ?[]const u8 = null,
        _runtime_ms: i64 = 0,
        _mcycles: i64 = 0,
        _power_profile_mah: f64 = 0.0,
        pub fn init(src: []const u8) gremlin.Error!AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader {
            const buf = gremlin.Reader.init(src);
            var res = AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.TIME_IN_STATE_CPU_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._time_in_state_cpu = result.value;
                    },
                    AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.CORE_TYPE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._core_type = result.value;
                    },
                    AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.RUNTIME_MS_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._runtime_ms = result.value;
                    },
                    AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.MCYCLES_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._mcycles = result.value;
                    },
                    AndroidThreadTimeInStateMetric.MetricsByCoreTypeWire.POWER_PROFILE_MAH_WIRE => {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._power_profile_mah = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getTimeInStateCpu(self: *const AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader) i32 {
            return self._time_in_state_cpu;
        }
        pub inline fn getCoreType(self: *const AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader) []const u8 {
            return self._core_type orelse &[_]u8{};
        }
        pub inline fn getRuntimeMs(self: *const AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader) i64 {
            return self._runtime_ms;
        }
        pub inline fn getMcycles(self: *const AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader) i64 {
            return self._mcycles;
        }
        pub inline fn getPowerProfileMah(self: *const AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader) f64 {
            return self._power_profile_mah;
        }
    };
    const ThreadWire = struct {
        const NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const MAIN_THREAD_WIRE: gremlin.ProtoWireNumber = 3;
        const METRICS_BY_CORE_TYPE_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const Thread = struct {
        // fields
        name: ?[]const u8 = null,
        main_thread: bool = false,
        metrics_by_core_type: ?[]const ?AndroidThreadTimeInStateMetric.MetricsByCoreType = null,
        pub fn calcProtobufSize(self: *const AndroidThreadTimeInStateMetric.Thread) usize {
            var res: usize = 0;
            if (self.name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.ThreadWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.main_thread != false) {
                res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.ThreadWire.MAIN_THREAD_WIRE) + gremlin.sizes.sizeBool(self.main_thread);
            }
            if (self.metrics_by_core_type) |arr| {
                for (arr) |maybe_v| {
                    res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.ThreadWire.METRICS_BY_CORE_TYPE_WIRE);
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        res += gremlin.sizes.sizeUsize(size) + size;
                    } else {
                        res += gremlin.sizes.sizeUsize(0);
                    }
                }
            }
            return res;
        }
        pub fn encode(self: *const AndroidThreadTimeInStateMetric.Thread, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const AndroidThreadTimeInStateMetric.Thread, target: *gremlin.Writer) void {
            if (self.name) |v| {
                if (v.len > 0) {
                    target.appendBytes(AndroidThreadTimeInStateMetric.ThreadWire.NAME_WIRE, v);
                }
            }
            if (self.main_thread != false) {
                target.appendBool(AndroidThreadTimeInStateMetric.ThreadWire.MAIN_THREAD_WIRE, self.main_thread);
            }
            if (self.metrics_by_core_type) |arr| {
                for (arr) |maybe_v| {
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        target.appendBytesTag(AndroidThreadTimeInStateMetric.ThreadWire.METRICS_BY_CORE_TYPE_WIRE, size);
                        v.encodeTo(target);
                    } else {
                        target.appendBytesTag(AndroidThreadTimeInStateMetric.ThreadWire.METRICS_BY_CORE_TYPE_WIRE, 0);
                    }
                }
            }
        }
    };
    pub const ThreadReader = struct {
        buf: gremlin.Reader,
        _name: ?[]const u8 = null,
        _main_thread: bool = false,
        _metrics_by_core_type_offset: ?usize = null,
        _metrics_by_core_type_last_offset: ?usize = null,
        _metrics_by_core_type_cnt: usize = 0,
        pub fn init(src: []const u8) gremlin.Error!AndroidThreadTimeInStateMetric.ThreadReader {
            const buf = gremlin.Reader.init(src);
            var res = AndroidThreadTimeInStateMetric.ThreadReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    AndroidThreadTimeInStateMetric.ThreadWire.NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._name = result.value;
                    },
                    AndroidThreadTimeInStateMetric.ThreadWire.MAIN_THREAD_WIRE => {
                        const result = try buf.readBool(offset);
                        offset += result.size;
                        res._main_thread = result.value;
                    },
                    AndroidThreadTimeInStateMetric.ThreadWire.METRICS_BY_CORE_TYPE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        if (res._metrics_by_core_type_offset == null) {
                            res._metrics_by_core_type_offset = offset - result.size;
                        }
                        res._metrics_by_core_type_last_offset = offset;
                        res._metrics_by_core_type_cnt += 1;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getName(self: *const AndroidThreadTimeInStateMetric.ThreadReader) []const u8 {
            return self._name orelse &[_]u8{};
        }
        pub inline fn getMainThread(self: *const AndroidThreadTimeInStateMetric.ThreadReader) bool {
            return self._main_thread;
        }
        pub fn metricsByCoreTypeCount(self: *const AndroidThreadTimeInStateMetric.ThreadReader) usize {
            return self._metrics_by_core_type_cnt;
        }
        pub fn metricsByCoreTypeNext(self: *AndroidThreadTimeInStateMetric.ThreadReader) ?AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader {
            if (self._metrics_by_core_type_offset == null) return null;
            const current_offset = self._metrics_by_core_type_offset.?;
            const result = self.buf.readBytes(current_offset) catch return null;
            const msg = AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader.init(result.value) catch return null;
            if (self._metrics_by_core_type_last_offset != null and current_offset >= self._metrics_by_core_type_last_offset.?) {
                self._metrics_by_core_type_offset = null;
                return msg;
            }
            if (self._metrics_by_core_type_last_offset == null) unreachable;
            var next_offset = current_offset + result.size;
            const max_offset = self._metrics_by_core_type_last_offset.?;
            while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
                const tag = self.buf.readTagAt(next_offset) catch break;
                next_offset += tag.size;
                if (tag.number == AndroidThreadTimeInStateMetric.ThreadWire.METRICS_BY_CORE_TYPE_WIRE) {
                    self._metrics_by_core_type_offset = next_offset;
                    return msg;
                } else {
                    next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
                }
            }
            self._metrics_by_core_type_offset = null;
            return msg;
        }
    };
    const ProcessWire = struct {
        const METADATA_WIRE: gremlin.ProtoWireNumber = 1;
        const METRICS_BY_CORE_TYPE_WIRE: gremlin.ProtoWireNumber = 2;
        const THREADS_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const Process = struct {
        // fields
        metadata: ?process_metadata.AndroidProcessMetadata = null,
        metrics_by_core_type: ?[]const ?AndroidThreadTimeInStateMetric.MetricsByCoreType = null,
        threads: ?[]const ?AndroidThreadTimeInStateMetric.Thread = null,
        pub fn calcProtobufSize(self: *const AndroidThreadTimeInStateMetric.Process) usize {
            var res: usize = 0;
            if (self.metadata) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.ProcessWire.METADATA_WIRE) + gremlin.sizes.sizeUsize(size) + size;
                }
            }
            if (self.metrics_by_core_type) |arr| {
                for (arr) |maybe_v| {
                    res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.ProcessWire.METRICS_BY_CORE_TYPE_WIRE);
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        res += gremlin.sizes.sizeUsize(size) + size;
                    } else {
                        res += gremlin.sizes.sizeUsize(0);
                    }
                }
            }
            if (self.threads) |arr| {
                for (arr) |maybe_v| {
                    res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetric.ProcessWire.THREADS_WIRE);
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        res += gremlin.sizes.sizeUsize(size) + size;
                    } else {
                        res += gremlin.sizes.sizeUsize(0);
                    }
                }
            }
            return res;
        }
        pub fn encode(self: *const AndroidThreadTimeInStateMetric.Process, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const AndroidThreadTimeInStateMetric.Process, target: *gremlin.Writer) void {
            if (self.metadata) |v| {
                const size = v.calcProtobufSize();
                if (size > 0) {
                    target.appendBytesTag(AndroidThreadTimeInStateMetric.ProcessWire.METADATA_WIRE, size);
                    v.encodeTo(target);
                }
            }
            if (self.metrics_by_core_type) |arr| {
                for (arr) |maybe_v| {
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        target.appendBytesTag(AndroidThreadTimeInStateMetric.ProcessWire.METRICS_BY_CORE_TYPE_WIRE, size);
                        v.encodeTo(target);
                    } else {
                        target.appendBytesTag(AndroidThreadTimeInStateMetric.ProcessWire.METRICS_BY_CORE_TYPE_WIRE, 0);
                    }
                }
            }
            if (self.threads) |arr| {
                for (arr) |maybe_v| {
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        target.appendBytesTag(AndroidThreadTimeInStateMetric.ProcessWire.THREADS_WIRE, size);
                        v.encodeTo(target);
                    } else {
                        target.appendBytesTag(AndroidThreadTimeInStateMetric.ProcessWire.THREADS_WIRE, 0);
                    }
                }
            }
        }
    };
    pub const ProcessReader = struct {
        buf: gremlin.Reader,
        _metadata_buf: ?[]const u8 = null,
        _metrics_by_core_type_offset: ?usize = null,
        _metrics_by_core_type_last_offset: ?usize = null,
        _metrics_by_core_type_cnt: usize = 0,
        _threads_offset: ?usize = null,
        _threads_last_offset: ?usize = null,
        _threads_cnt: usize = 0,
        pub fn init(src: []const u8) gremlin.Error!AndroidThreadTimeInStateMetric.ProcessReader {
            const buf = gremlin.Reader.init(src);
            var res = AndroidThreadTimeInStateMetric.ProcessReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    AndroidThreadTimeInStateMetric.ProcessWire.METADATA_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._metadata_buf = result.value;
                    },
                    AndroidThreadTimeInStateMetric.ProcessWire.METRICS_BY_CORE_TYPE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        if (res._metrics_by_core_type_offset == null) {
                            res._metrics_by_core_type_offset = offset - result.size;
                        }
                        res._metrics_by_core_type_last_offset = offset;
                        res._metrics_by_core_type_cnt += 1;
                    },
                    AndroidThreadTimeInStateMetric.ProcessWire.THREADS_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        if (res._threads_offset == null) {
                            res._threads_offset = offset - result.size;
                        }
                        res._threads_last_offset = offset;
                        res._threads_cnt += 1;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub fn getMetadata(self: *const AndroidThreadTimeInStateMetric.ProcessReader) gremlin.Error!process_metadata.AndroidProcessMetadataReader {
            if (self._metadata_buf) |buf| {
                return try process_metadata.AndroidProcessMetadataReader.init(buf);
            }
            return try process_metadata.AndroidProcessMetadataReader.init(&[_]u8{});
        }
        pub fn metricsByCoreTypeCount(self: *const AndroidThreadTimeInStateMetric.ProcessReader) usize {
            return self._metrics_by_core_type_cnt;
        }
        pub fn metricsByCoreTypeNext(self: *AndroidThreadTimeInStateMetric.ProcessReader) ?AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader {
            if (self._metrics_by_core_type_offset == null) return null;
            const current_offset = self._metrics_by_core_type_offset.?;
            const result = self.buf.readBytes(current_offset) catch return null;
            const msg = AndroidThreadTimeInStateMetric.MetricsByCoreTypeReader.init(result.value) catch return null;
            if (self._metrics_by_core_type_last_offset != null and current_offset >= self._metrics_by_core_type_last_offset.?) {
                self._metrics_by_core_type_offset = null;
                return msg;
            }
            if (self._metrics_by_core_type_last_offset == null) unreachable;
            var next_offset = current_offset + result.size;
            const max_offset = self._metrics_by_core_type_last_offset.?;
            while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
                const tag = self.buf.readTagAt(next_offset) catch break;
                next_offset += tag.size;
                if (tag.number == AndroidThreadTimeInStateMetric.ProcessWire.METRICS_BY_CORE_TYPE_WIRE) {
                    self._metrics_by_core_type_offset = next_offset;
                    return msg;
                } else {
                    next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
                }
            }
            self._metrics_by_core_type_offset = null;
            return msg;
        }
        pub fn threadsCount(self: *const AndroidThreadTimeInStateMetric.ProcessReader) usize {
            return self._threads_cnt;
        }
        pub fn threadsNext(self: *AndroidThreadTimeInStateMetric.ProcessReader) ?AndroidThreadTimeInStateMetric.ThreadReader {
            if (self._threads_offset == null) return null;
            const current_offset = self._threads_offset.?;
            const result = self.buf.readBytes(current_offset) catch return null;
            const msg = AndroidThreadTimeInStateMetric.ThreadReader.init(result.value) catch return null;
            if (self._threads_last_offset != null and current_offset >= self._threads_last_offset.?) {
                self._threads_offset = null;
                return msg;
            }
            if (self._threads_last_offset == null) unreachable;
            var next_offset = current_offset + result.size;
            const max_offset = self._threads_last_offset.?;
            while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
                const tag = self.buf.readTagAt(next_offset) catch break;
                next_offset += tag.size;
                if (tag.number == AndroidThreadTimeInStateMetric.ProcessWire.THREADS_WIRE) {
                    self._threads_offset = next_offset;
                    return msg;
                } else {
                    next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
                }
            }
            self._threads_offset = null;
            return msg;
        }
    };
    // fields
    processes: ?[]const ?AndroidThreadTimeInStateMetric.Process = null,
    pub fn calcProtobufSize(self: *const AndroidThreadTimeInStateMetric) usize {
        var res: usize = 0;
        if (self.processes) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(AndroidThreadTimeInStateMetricWire.PROCESSES_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const AndroidThreadTimeInStateMetric, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const AndroidThreadTimeInStateMetric, target: *gremlin.Writer) void {
        if (self.processes) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(AndroidThreadTimeInStateMetricWire.PROCESSES_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(AndroidThreadTimeInStateMetricWire.PROCESSES_WIRE, 0);
                }
            }
        }
    }
};
pub const AndroidThreadTimeInStateMetricReader = struct {
    buf: gremlin.Reader,
    _processes_offset: ?usize = null,
    _processes_last_offset: ?usize = null,
    _processes_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!AndroidThreadTimeInStateMetricReader {
        const buf = gremlin.Reader.init(src);
        var res = AndroidThreadTimeInStateMetricReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                AndroidThreadTimeInStateMetricWire.PROCESSES_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._processes_offset == null) {
                        res._processes_offset = offset - result.size;
                    }
                    res._processes_last_offset = offset;
                    res._processes_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn processesCount(self: *const AndroidThreadTimeInStateMetricReader) usize {
        return self._processes_cnt;
    }
    pub fn processesNext(self: *AndroidThreadTimeInStateMetricReader) ?AndroidThreadTimeInStateMetric.ProcessReader {
        if (self._processes_offset == null) return null;
        const current_offset = self._processes_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = AndroidThreadTimeInStateMetric.ProcessReader.init(result.value) catch return null;
        if (self._processes_last_offset != null and current_offset >= self._processes_last_offset.?) {
            self._processes_offset = null;
            return msg;
        }
        if (self._processes_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._processes_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == AndroidThreadTimeInStateMetricWire.PROCESSES_WIRE) {
                self._processes_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._processes_offset = null;
        return msg;
    }
};
