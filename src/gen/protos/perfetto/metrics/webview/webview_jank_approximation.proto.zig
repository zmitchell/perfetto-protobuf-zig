// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const WebViewJankApproximationWire = struct {
    const WEBVIEW_JANKS_WIRE: gremlin.ProtoWireNumber = 1;
    const WEBVIEW_JANKS_WITHOUT_STARTUP_WIRE: gremlin.ProtoWireNumber = 2;
    const WEBVIEW_APP_JANKS_WIRE: gremlin.ProtoWireNumber = 3;
    const WEBVIEW_TOTAL_JANKS_WIRE: gremlin.ProtoWireNumber = 4;
    const TOTAL_JANKS_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const WebViewJankApproximation = struct {
    // fields
    webview_janks: i32 = 0,
    webview_janks_without_startup: i32 = 0,
    webview_app_janks: i32 = 0,
    webview_total_janks: i32 = 0,
    total_janks: i32 = 0,
    pub fn calcProtobufSize(self: *const WebViewJankApproximation) usize {
        var res: usize = 0;
        if (self.webview_janks != 0) {
            res += gremlin.sizes.sizeWireNumber(WebViewJankApproximationWire.WEBVIEW_JANKS_WIRE) + gremlin.sizes.sizeI32(self.webview_janks);
        }
        if (self.webview_janks_without_startup != 0) {
            res += gremlin.sizes.sizeWireNumber(WebViewJankApproximationWire.WEBVIEW_JANKS_WITHOUT_STARTUP_WIRE) + gremlin.sizes.sizeI32(self.webview_janks_without_startup);
        }
        if (self.webview_app_janks != 0) {
            res += gremlin.sizes.sizeWireNumber(WebViewJankApproximationWire.WEBVIEW_APP_JANKS_WIRE) + gremlin.sizes.sizeI32(self.webview_app_janks);
        }
        if (self.webview_total_janks != 0) {
            res += gremlin.sizes.sizeWireNumber(WebViewJankApproximationWire.WEBVIEW_TOTAL_JANKS_WIRE) + gremlin.sizes.sizeI32(self.webview_total_janks);
        }
        if (self.total_janks != 0) {
            res += gremlin.sizes.sizeWireNumber(WebViewJankApproximationWire.TOTAL_JANKS_WIRE) + gremlin.sizes.sizeI32(self.total_janks);
        }
        return res;
    }
    pub fn encode(self: *const WebViewJankApproximation, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const WebViewJankApproximation, target: *gremlin.Writer) void {
        if (self.webview_janks != 0) {
            target.appendInt32(WebViewJankApproximationWire.WEBVIEW_JANKS_WIRE, self.webview_janks);
        }
        if (self.webview_janks_without_startup != 0) {
            target.appendInt32(WebViewJankApproximationWire.WEBVIEW_JANKS_WITHOUT_STARTUP_WIRE, self.webview_janks_without_startup);
        }
        if (self.webview_app_janks != 0) {
            target.appendInt32(WebViewJankApproximationWire.WEBVIEW_APP_JANKS_WIRE, self.webview_app_janks);
        }
        if (self.webview_total_janks != 0) {
            target.appendInt32(WebViewJankApproximationWire.WEBVIEW_TOTAL_JANKS_WIRE, self.webview_total_janks);
        }
        if (self.total_janks != 0) {
            target.appendInt32(WebViewJankApproximationWire.TOTAL_JANKS_WIRE, self.total_janks);
        }
    }
};
pub const WebViewJankApproximationReader = struct {
    buf: gremlin.Reader,
    _webview_janks: i32 = 0,
    _webview_janks_without_startup: i32 = 0,
    _webview_app_janks: i32 = 0,
    _webview_total_janks: i32 = 0,
    _total_janks: i32 = 0,
    pub fn init(src: []const u8) gremlin.Error!WebViewJankApproximationReader {
        const buf = gremlin.Reader.init(src);
        var res = WebViewJankApproximationReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                WebViewJankApproximationWire.WEBVIEW_JANKS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._webview_janks = result.value;
                },
                WebViewJankApproximationWire.WEBVIEW_JANKS_WITHOUT_STARTUP_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._webview_janks_without_startup = result.value;
                },
                WebViewJankApproximationWire.WEBVIEW_APP_JANKS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._webview_app_janks = result.value;
                },
                WebViewJankApproximationWire.WEBVIEW_TOTAL_JANKS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._webview_total_janks = result.value;
                },
                WebViewJankApproximationWire.TOTAL_JANKS_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._total_janks = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getWebviewJanks(self: *const WebViewJankApproximationReader) i32 {
        return self._webview_janks;
    }
    pub inline fn getWebviewJanksWithoutStartup(self: *const WebViewJankApproximationReader) i32 {
        return self._webview_janks_without_startup;
    }
    pub inline fn getWebviewAppJanks(self: *const WebViewJankApproximationReader) i32 {
        return self._webview_app_janks;
    }
    pub inline fn getWebviewTotalJanks(self: *const WebViewJankApproximationReader) i32 {
        return self._webview_total_janks;
    }
    pub inline fn getTotalJanks(self: *const WebViewJankApproximationReader) i32 {
        return self._total_janks;
    }
};
