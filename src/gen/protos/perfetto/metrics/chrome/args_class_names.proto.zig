// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeArgsClassNamesWire = struct {
    const CLASS_NAMES_PER_VERSION_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ChromeArgsClassNames = struct {
    // nested structs
    const ChromeArgsClassNamesPerVersionWire = struct {
        const PACKAGE_NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const VERSION_CODE_WIRE: gremlin.ProtoWireNumber = 2;
        const CLASS_NAME_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const ChromeArgsClassNamesPerVersion = struct {
        // fields
        package_name: ?[]const u8 = null,
        version_code: i64 = 0,
        class_name: ?[]const ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const ChromeArgsClassNames.ChromeArgsClassNamesPerVersion) usize {
            var res: usize = 0;
            if (self.package_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.PACKAGE_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.version_code != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.VERSION_CODE_WIRE) + gremlin.sizes.sizeI64(self.version_code);
            }
            if (self.class_name) |arr| {
                for (arr) |maybe_v| {
                    res += gremlin.sizes.sizeWireNumber(ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.CLASS_NAME_WIRE);
                    if (maybe_v) |v| {
                        res += gremlin.sizes.sizeUsize(v.len) + v.len;
                    } else {
                        res += gremlin.sizes.sizeUsize(0);
                    }
                }
            }
            return res;
        }
        pub fn encode(self: *const ChromeArgsClassNames.ChromeArgsClassNamesPerVersion, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ChromeArgsClassNames.ChromeArgsClassNamesPerVersion, target: *gremlin.Writer) void {
            if (self.package_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.PACKAGE_NAME_WIRE, v);
                }
            }
            if (self.version_code != 0) {
                target.appendInt64(ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.VERSION_CODE_WIRE, self.version_code);
            }
            if (self.class_name) |arr| {
                for (arr) |maybe_v| {
                    if (maybe_v) |v| {
                        target.appendBytes(ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.CLASS_NAME_WIRE, v);
                    } else {
                        target.appendBytesTag(ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.CLASS_NAME_WIRE, 0);
                    }
                }
            }
        }
    };
    pub const ChromeArgsClassNamesPerVersionReader = struct {
        buf: gremlin.Reader,
        _package_name: ?[]const u8 = null,
        _version_code: i64 = 0,
        _class_name_offset: ?usize = null,
        _class_name_last_offset: ?usize = null,
        _class_name_cnt: usize = 0,
        pub fn init(src: []const u8) gremlin.Error!ChromeArgsClassNames.ChromeArgsClassNamesPerVersionReader {
            const buf = gremlin.Reader.init(src);
            var res = ChromeArgsClassNames.ChromeArgsClassNamesPerVersionReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.PACKAGE_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._package_name = result.value;
                    },
                    ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.VERSION_CODE_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._version_code = result.value;
                    },
                    ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.CLASS_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        if (res._class_name_offset == null) {
                            res._class_name_offset = offset - result.size;
                        }
                        res._class_name_last_offset = offset;
                        res._class_name_cnt += 1;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getPackageName(self: *const ChromeArgsClassNames.ChromeArgsClassNamesPerVersionReader) []const u8 {
            return self._package_name orelse &[_]u8{};
        }
        pub inline fn getVersionCode(self: *const ChromeArgsClassNames.ChromeArgsClassNamesPerVersionReader) i64 {
            return self._version_code;
        }
        pub fn classNameCount(self: *const ChromeArgsClassNames.ChromeArgsClassNamesPerVersionReader) usize {
            return self._class_name_cnt;
        }
        pub fn classNameNext(self: *ChromeArgsClassNames.ChromeArgsClassNamesPerVersionReader) ?[]const u8 {
            if (self._class_name_offset == null) return null;
            const current_offset = self._class_name_offset.?;
            const result = self.buf.readBytes(current_offset) catch return null;
            if (self._class_name_last_offset != null and current_offset >= self._class_name_last_offset.?) {
                self._class_name_offset = null;
                return result.value;
            }
            if (self._class_name_last_offset == null) unreachable;
            var next_offset = current_offset + result.size;
            const max_offset = self._class_name_last_offset.?;
            while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
                const tag = self.buf.readTagAt(next_offset) catch break;
                next_offset += tag.size;
                if (tag.number == ChromeArgsClassNames.ChromeArgsClassNamesPerVersionWire.CLASS_NAME_WIRE) {
                    self._class_name_offset = next_offset;
                    return result.value;
                } else {
                    next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
                }
            }
            self._class_name_offset = null;
            return result.value;
        }
    };
    // fields
    class_names_per_version: ?[]const ?ChromeArgsClassNames.ChromeArgsClassNamesPerVersion = null,
    pub fn calcProtobufSize(self: *const ChromeArgsClassNames) usize {
        var res: usize = 0;
        if (self.class_names_per_version) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ChromeArgsClassNamesWire.CLASS_NAMES_PER_VERSION_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ChromeArgsClassNames, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeArgsClassNames, target: *gremlin.Writer) void {
        if (self.class_names_per_version) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ChromeArgsClassNamesWire.CLASS_NAMES_PER_VERSION_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ChromeArgsClassNamesWire.CLASS_NAMES_PER_VERSION_WIRE, 0);
                }
            }
        }
    }
};
pub const ChromeArgsClassNamesReader = struct {
    buf: gremlin.Reader,
    _class_names_per_version_offset: ?usize = null,
    _class_names_per_version_last_offset: ?usize = null,
    _class_names_per_version_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeArgsClassNamesReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeArgsClassNamesReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeArgsClassNamesWire.CLASS_NAMES_PER_VERSION_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._class_names_per_version_offset == null) {
                        res._class_names_per_version_offset = offset - result.size;
                    }
                    res._class_names_per_version_last_offset = offset;
                    res._class_names_per_version_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn classNamesPerVersionCount(self: *const ChromeArgsClassNamesReader) usize {
        return self._class_names_per_version_cnt;
    }
    pub fn classNamesPerVersionNext(self: *ChromeArgsClassNamesReader) ?ChromeArgsClassNames.ChromeArgsClassNamesPerVersionReader {
        if (self._class_names_per_version_offset == null) return null;
        const current_offset = self._class_names_per_version_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ChromeArgsClassNames.ChromeArgsClassNamesPerVersionReader.init(result.value) catch return null;
        if (self._class_names_per_version_last_offset != null and current_offset >= self._class_names_per_version_last_offset.?) {
            self._class_names_per_version_offset = null;
            return msg;
        }
        if (self._class_names_per_version_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._class_names_per_version_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ChromeArgsClassNamesWire.CLASS_NAMES_PER_VERSION_WIRE) {
                self._class_names_per_version_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._class_names_per_version_offset = null;
        return msg;
    }
};
