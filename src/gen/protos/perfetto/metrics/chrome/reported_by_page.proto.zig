// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ReportedByPageWire = struct {
    const TIME_TO_VIEWABLE_WIRE: gremlin.ProtoWireNumber = 1;
    const TIME_TO_INTERACTIVE_WIRE: gremlin.ProtoWireNumber = 2;
    const BENCHMARK_TIME_WIRE: gremlin.ProtoWireNumber = 3;
};
pub const ReportedByPage = struct {
    // fields
    time_to_viewable: ?[]const f64 = null,
    time_to_interactive: ?[]const f64 = null,
    benchmark_time: ?[]const f64 = null,
    pub fn calcProtobufSize(self: *const ReportedByPage) usize {
        var res: usize = 0;
        if (self.time_to_viewable) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(ReportedByPageWire.TIME_TO_VIEWABLE_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(ReportedByPageWire.TIME_TO_VIEWABLE_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.time_to_interactive) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(ReportedByPageWire.TIME_TO_INTERACTIVE_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(ReportedByPageWire.TIME_TO_INTERACTIVE_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.benchmark_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(ReportedByPageWire.BENCHMARK_TIME_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(ReportedByPageWire.BENCHMARK_TIME_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        return res;
    }
    pub fn encode(self: *const ReportedByPage, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ReportedByPage, target: *gremlin.Writer) void {
        if (self.time_to_viewable) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(ReportedByPageWire.TIME_TO_VIEWABLE_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(ReportedByPageWire.TIME_TO_VIEWABLE_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.time_to_interactive) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(ReportedByPageWire.TIME_TO_INTERACTIVE_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(ReportedByPageWire.TIME_TO_INTERACTIVE_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.benchmark_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(ReportedByPageWire.BENCHMARK_TIME_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(ReportedByPageWire.BENCHMARK_TIME_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
    }
};
pub const ReportedByPageReader = struct {
    buf: gremlin.Reader,
    _time_to_viewable_offset: ?usize = null,
    _time_to_viewable_last_offset: ?usize = null,
    _time_to_viewable_packed: bool = false,
    _time_to_interactive_offset: ?usize = null,
    _time_to_interactive_last_offset: ?usize = null,
    _time_to_interactive_packed: bool = false,
    _benchmark_time_offset: ?usize = null,
    _benchmark_time_last_offset: ?usize = null,
    _benchmark_time_packed: bool = false,
    pub fn init(src: []const u8) gremlin.Error!ReportedByPageReader {
        const buf = gremlin.Reader.init(src);
        var res = ReportedByPageReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ReportedByPageWire.TIME_TO_VIEWABLE_WIRE => {
                    if (res._time_to_viewable_offset == null) {
                        res._time_to_viewable_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._time_to_viewable_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._time_to_viewable_offset = offset + length_result.size;
                        res._time_to_viewable_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._time_to_viewable_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._time_to_viewable_last_offset = offset;
                    }
                },
                ReportedByPageWire.TIME_TO_INTERACTIVE_WIRE => {
                    if (res._time_to_interactive_offset == null) {
                        res._time_to_interactive_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._time_to_interactive_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._time_to_interactive_offset = offset + length_result.size;
                        res._time_to_interactive_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._time_to_interactive_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._time_to_interactive_last_offset = offset;
                    }
                },
                ReportedByPageWire.BENCHMARK_TIME_WIRE => {
                    if (res._benchmark_time_offset == null) {
                        res._benchmark_time_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._benchmark_time_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._benchmark_time_offset = offset + length_result.size;
                        res._benchmark_time_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._benchmark_time_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._benchmark_time_last_offset = offset;
                    }
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn timeToViewableNext(self: *ReportedByPageReader) gremlin.Error!?f64 {
        if (self._time_to_viewable_offset == null) return null;
        const current_offset = self._time_to_viewable_offset.?;
        if (current_offset >= self._time_to_viewable_last_offset.?) {
            self._time_to_viewable_offset = null;
            return null;
        }
        if (self._time_to_viewable_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._time_to_viewable_offset = current_offset + value_result.size;
            if (self._time_to_viewable_offset.? >= self._time_to_viewable_last_offset.?) {
                self._time_to_viewable_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._time_to_viewable_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == ReportedByPageWire.TIME_TO_VIEWABLE_WIRE) {
                    self._time_to_viewable_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._time_to_viewable_offset = null;
            return value_result.value;
        }
    }
    pub fn timeToInteractiveNext(self: *ReportedByPageReader) gremlin.Error!?f64 {
        if (self._time_to_interactive_offset == null) return null;
        const current_offset = self._time_to_interactive_offset.?;
        if (current_offset >= self._time_to_interactive_last_offset.?) {
            self._time_to_interactive_offset = null;
            return null;
        }
        if (self._time_to_interactive_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._time_to_interactive_offset = current_offset + value_result.size;
            if (self._time_to_interactive_offset.? >= self._time_to_interactive_last_offset.?) {
                self._time_to_interactive_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._time_to_interactive_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == ReportedByPageWire.TIME_TO_INTERACTIVE_WIRE) {
                    self._time_to_interactive_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._time_to_interactive_offset = null;
            return value_result.value;
        }
    }
    pub fn benchmarkTimeNext(self: *ReportedByPageReader) gremlin.Error!?f64 {
        if (self._benchmark_time_offset == null) return null;
        const current_offset = self._benchmark_time_offset.?;
        if (current_offset >= self._benchmark_time_last_offset.?) {
            self._benchmark_time_offset = null;
            return null;
        }
        if (self._benchmark_time_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._benchmark_time_offset = current_offset + value_result.size;
            if (self._benchmark_time_offset.? >= self._benchmark_time_last_offset.?) {
                self._benchmark_time_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._benchmark_time_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == ReportedByPageWire.BENCHMARK_TIME_WIRE) {
                    self._benchmark_time_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._benchmark_time_offset = null;
            return value_result.value;
        }
    }
};
