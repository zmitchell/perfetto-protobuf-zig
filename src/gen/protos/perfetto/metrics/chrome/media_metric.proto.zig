// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const MediaMetricWire = struct {
    const TIME_TO_VIDEO_PLAY_WIRE: gremlin.ProtoWireNumber = 1;
    const TIME_TO_AUDIO_PLAY_WIRE: gremlin.ProtoWireNumber = 2;
    const DROPPED_FRAME_COUNT_WIRE: gremlin.ProtoWireNumber = 3;
    const BUFFERING_TIME_WIRE: gremlin.ProtoWireNumber = 4;
    const ROUGHNESS_WIRE: gremlin.ProtoWireNumber = 5;
    const FREEZING_WIRE: gremlin.ProtoWireNumber = 6;
    const SEEK_TIME_WIRE: gremlin.ProtoWireNumber = 8;
    const PIPELINE_SEEK_TIME_WIRE: gremlin.ProtoWireNumber = 9;
};
pub const MediaMetric = struct {
    // fields
    time_to_video_play: ?[]const f64 = null,
    time_to_audio_play: ?[]const f64 = null,
    dropped_frame_count: ?[]const i64 = null,
    buffering_time: ?[]const f64 = null,
    roughness: ?[]const f64 = null,
    freezing: ?[]const f64 = null,
    seek_time: ?[]const f64 = null,
    pipeline_seek_time: ?[]const f64 = null,
    pub fn calcProtobufSize(self: *const MediaMetric) usize {
        var res: usize = 0;
        if (self.time_to_video_play) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.TIME_TO_VIDEO_PLAY_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.TIME_TO_VIDEO_PLAY_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.time_to_audio_play) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.TIME_TO_AUDIO_PLAY_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.TIME_TO_AUDIO_PLAY_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.dropped_frame_count) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.DROPPED_FRAME_COUNT_WIRE) + gremlin.sizes.sizeI64(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI64(v);
                }
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.DROPPED_FRAME_COUNT_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.buffering_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.BUFFERING_TIME_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.BUFFERING_TIME_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.roughness) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.ROUGHNESS_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.ROUGHNESS_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.freezing) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.FREEZING_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.FREEZING_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.seek_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.SEEK_TIME_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.SEEK_TIME_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.pipeline_seek_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.PIPELINE_SEEK_TIME_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(MediaMetricWire.PIPELINE_SEEK_TIME_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        return res;
    }
    pub fn encode(self: *const MediaMetric, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const MediaMetric, target: *gremlin.Writer) void {
        if (self.time_to_video_play) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(MediaMetricWire.TIME_TO_VIDEO_PLAY_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(MediaMetricWire.TIME_TO_VIDEO_PLAY_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.time_to_audio_play) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(MediaMetricWire.TIME_TO_AUDIO_PLAY_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(MediaMetricWire.TIME_TO_AUDIO_PLAY_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.dropped_frame_count) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendInt64(MediaMetricWire.DROPPED_FRAME_COUNT_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeI64(v);
                }
                target.appendBytesTag(MediaMetricWire.DROPPED_FRAME_COUNT_WIRE, packed_size);
                for (arr) |v| {
                    target.appendInt64WithoutTag(v);
                }
            }
        }
        if (self.buffering_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(MediaMetricWire.BUFFERING_TIME_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(MediaMetricWire.BUFFERING_TIME_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.roughness) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(MediaMetricWire.ROUGHNESS_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(MediaMetricWire.ROUGHNESS_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.freezing) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(MediaMetricWire.FREEZING_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(MediaMetricWire.FREEZING_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.seek_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(MediaMetricWire.SEEK_TIME_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(MediaMetricWire.SEEK_TIME_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.pipeline_seek_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(MediaMetricWire.PIPELINE_SEEK_TIME_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(MediaMetricWire.PIPELINE_SEEK_TIME_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
    }
};
pub const MediaMetricReader = struct {
    buf: gremlin.Reader,
    _time_to_video_play_offset: ?usize = null,
    _time_to_video_play_last_offset: ?usize = null,
    _time_to_video_play_packed: bool = false,
    _time_to_audio_play_offset: ?usize = null,
    _time_to_audio_play_last_offset: ?usize = null,
    _time_to_audio_play_packed: bool = false,
    _dropped_frame_count_offset: ?usize = null,
    _dropped_frame_count_last_offset: ?usize = null,
    _dropped_frame_count_packed: bool = false,
    _buffering_time_offset: ?usize = null,
    _buffering_time_last_offset: ?usize = null,
    _buffering_time_packed: bool = false,
    _roughness_offset: ?usize = null,
    _roughness_last_offset: ?usize = null,
    _roughness_packed: bool = false,
    _freezing_offset: ?usize = null,
    _freezing_last_offset: ?usize = null,
    _freezing_packed: bool = false,
    _seek_time_offset: ?usize = null,
    _seek_time_last_offset: ?usize = null,
    _seek_time_packed: bool = false,
    _pipeline_seek_time_offset: ?usize = null,
    _pipeline_seek_time_last_offset: ?usize = null,
    _pipeline_seek_time_packed: bool = false,
    pub fn init(src: []const u8) gremlin.Error!MediaMetricReader {
        const buf = gremlin.Reader.init(src);
        var res = MediaMetricReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                MediaMetricWire.TIME_TO_VIDEO_PLAY_WIRE => {
                    if (res._time_to_video_play_offset == null) {
                        res._time_to_video_play_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._time_to_video_play_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._time_to_video_play_offset = offset + length_result.size;
                        res._time_to_video_play_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._time_to_video_play_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._time_to_video_play_last_offset = offset;
                    }
                },
                MediaMetricWire.TIME_TO_AUDIO_PLAY_WIRE => {
                    if (res._time_to_audio_play_offset == null) {
                        res._time_to_audio_play_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._time_to_audio_play_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._time_to_audio_play_offset = offset + length_result.size;
                        res._time_to_audio_play_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._time_to_audio_play_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._time_to_audio_play_last_offset = offset;
                    }
                },
                MediaMetricWire.DROPPED_FRAME_COUNT_WIRE => {
                    if (res._dropped_frame_count_offset == null) {
                        res._dropped_frame_count_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._dropped_frame_count_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._dropped_frame_count_offset = offset + length_result.size;
                        res._dropped_frame_count_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._dropped_frame_count_last_offset.?;
                    } else {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._dropped_frame_count_last_offset = offset;
                    }
                },
                MediaMetricWire.BUFFERING_TIME_WIRE => {
                    if (res._buffering_time_offset == null) {
                        res._buffering_time_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._buffering_time_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._buffering_time_offset = offset + length_result.size;
                        res._buffering_time_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._buffering_time_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._buffering_time_last_offset = offset;
                    }
                },
                MediaMetricWire.ROUGHNESS_WIRE => {
                    if (res._roughness_offset == null) {
                        res._roughness_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._roughness_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._roughness_offset = offset + length_result.size;
                        res._roughness_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._roughness_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._roughness_last_offset = offset;
                    }
                },
                MediaMetricWire.FREEZING_WIRE => {
                    if (res._freezing_offset == null) {
                        res._freezing_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._freezing_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._freezing_offset = offset + length_result.size;
                        res._freezing_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._freezing_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._freezing_last_offset = offset;
                    }
                },
                MediaMetricWire.SEEK_TIME_WIRE => {
                    if (res._seek_time_offset == null) {
                        res._seek_time_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._seek_time_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._seek_time_offset = offset + length_result.size;
                        res._seek_time_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._seek_time_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._seek_time_last_offset = offset;
                    }
                },
                MediaMetricWire.PIPELINE_SEEK_TIME_WIRE => {
                    if (res._pipeline_seek_time_offset == null) {
                        res._pipeline_seek_time_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._pipeline_seek_time_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._pipeline_seek_time_offset = offset + length_result.size;
                        res._pipeline_seek_time_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._pipeline_seek_time_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._pipeline_seek_time_last_offset = offset;
                    }
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn timeToVideoPlayNext(self: *MediaMetricReader) gremlin.Error!?f64 {
        if (self._time_to_video_play_offset == null) return null;
        const current_offset = self._time_to_video_play_offset.?;
        if (current_offset >= self._time_to_video_play_last_offset.?) {
            self._time_to_video_play_offset = null;
            return null;
        }
        if (self._time_to_video_play_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._time_to_video_play_offset = current_offset + value_result.size;
            if (self._time_to_video_play_offset.? >= self._time_to_video_play_last_offset.?) {
                self._time_to_video_play_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._time_to_video_play_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == MediaMetricWire.TIME_TO_VIDEO_PLAY_WIRE) {
                    self._time_to_video_play_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._time_to_video_play_offset = null;
            return value_result.value;
        }
    }
    pub fn timeToAudioPlayNext(self: *MediaMetricReader) gremlin.Error!?f64 {
        if (self._time_to_audio_play_offset == null) return null;
        const current_offset = self._time_to_audio_play_offset.?;
        if (current_offset >= self._time_to_audio_play_last_offset.?) {
            self._time_to_audio_play_offset = null;
            return null;
        }
        if (self._time_to_audio_play_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._time_to_audio_play_offset = current_offset + value_result.size;
            if (self._time_to_audio_play_offset.? >= self._time_to_audio_play_last_offset.?) {
                self._time_to_audio_play_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._time_to_audio_play_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == MediaMetricWire.TIME_TO_AUDIO_PLAY_WIRE) {
                    self._time_to_audio_play_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._time_to_audio_play_offset = null;
            return value_result.value;
        }
    }
    pub fn droppedFrameCountNext(self: *MediaMetricReader) gremlin.Error!?i64 {
        if (self._dropped_frame_count_offset == null) return null;
        const current_offset = self._dropped_frame_count_offset.?;
        if (current_offset >= self._dropped_frame_count_last_offset.?) {
            self._dropped_frame_count_offset = null;
            return null;
        }
        if (self._dropped_frame_count_packed) {
            const value_result = try self.buf.readInt64(current_offset);
            self._dropped_frame_count_offset = current_offset + value_result.size;
            if (self._dropped_frame_count_offset.? >= self._dropped_frame_count_last_offset.?) {
                self._dropped_frame_count_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readInt64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._dropped_frame_count_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == MediaMetricWire.DROPPED_FRAME_COUNT_WIRE) {
                    self._dropped_frame_count_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._dropped_frame_count_offset = null;
            return value_result.value;
        }
    }
    pub fn bufferingTimeNext(self: *MediaMetricReader) gremlin.Error!?f64 {
        if (self._buffering_time_offset == null) return null;
        const current_offset = self._buffering_time_offset.?;
        if (current_offset >= self._buffering_time_last_offset.?) {
            self._buffering_time_offset = null;
            return null;
        }
        if (self._buffering_time_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._buffering_time_offset = current_offset + value_result.size;
            if (self._buffering_time_offset.? >= self._buffering_time_last_offset.?) {
                self._buffering_time_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._buffering_time_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == MediaMetricWire.BUFFERING_TIME_WIRE) {
                    self._buffering_time_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._buffering_time_offset = null;
            return value_result.value;
        }
    }
    pub fn roughnessNext(self: *MediaMetricReader) gremlin.Error!?f64 {
        if (self._roughness_offset == null) return null;
        const current_offset = self._roughness_offset.?;
        if (current_offset >= self._roughness_last_offset.?) {
            self._roughness_offset = null;
            return null;
        }
        if (self._roughness_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._roughness_offset = current_offset + value_result.size;
            if (self._roughness_offset.? >= self._roughness_last_offset.?) {
                self._roughness_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._roughness_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == MediaMetricWire.ROUGHNESS_WIRE) {
                    self._roughness_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._roughness_offset = null;
            return value_result.value;
        }
    }
    pub fn freezingNext(self: *MediaMetricReader) gremlin.Error!?f64 {
        if (self._freezing_offset == null) return null;
        const current_offset = self._freezing_offset.?;
        if (current_offset >= self._freezing_last_offset.?) {
            self._freezing_offset = null;
            return null;
        }
        if (self._freezing_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._freezing_offset = current_offset + value_result.size;
            if (self._freezing_offset.? >= self._freezing_last_offset.?) {
                self._freezing_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._freezing_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == MediaMetricWire.FREEZING_WIRE) {
                    self._freezing_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._freezing_offset = null;
            return value_result.value;
        }
    }
    pub fn seekTimeNext(self: *MediaMetricReader) gremlin.Error!?f64 {
        if (self._seek_time_offset == null) return null;
        const current_offset = self._seek_time_offset.?;
        if (current_offset >= self._seek_time_last_offset.?) {
            self._seek_time_offset = null;
            return null;
        }
        if (self._seek_time_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._seek_time_offset = current_offset + value_result.size;
            if (self._seek_time_offset.? >= self._seek_time_last_offset.?) {
                self._seek_time_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._seek_time_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == MediaMetricWire.SEEK_TIME_WIRE) {
                    self._seek_time_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._seek_time_offset = null;
            return value_result.value;
        }
    }
    pub fn pipelineSeekTimeNext(self: *MediaMetricReader) gremlin.Error!?f64 {
        if (self._pipeline_seek_time_offset == null) return null;
        const current_offset = self._pipeline_seek_time_offset.?;
        if (current_offset >= self._pipeline_seek_time_last_offset.?) {
            self._pipeline_seek_time_offset = null;
            return null;
        }
        if (self._pipeline_seek_time_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._pipeline_seek_time_offset = current_offset + value_result.size;
            if (self._pipeline_seek_time_offset.? >= self._pipeline_seek_time_last_offset.?) {
                self._pipeline_seek_time_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._pipeline_seek_time_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == MediaMetricWire.PIPELINE_SEEK_TIME_WIRE) {
                    self._pipeline_seek_time_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._pipeline_seek_time_offset = null;
            return value_result.value;
        }
    }
};
