// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeUnsymbolizedArgsWire = struct {
    const ARGS_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ChromeUnsymbolizedArgs = struct {
    // nested structs
    const ArgWire = struct {
        const MODULE_WIRE: gremlin.ProtoWireNumber = 1;
        const BUILD_ID_WIRE: gremlin.ProtoWireNumber = 2;
        const ADDRESS_WIRE: gremlin.ProtoWireNumber = 3;
        const GOOGLE_LOOKUP_ID_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const Arg = struct {
        // fields
        module: ?[]const u8 = null,
        build_id: ?[]const u8 = null,
        address: i64 = 0,
        google_lookup_id: ?[]const u8 = null,
        pub fn calcProtobufSize(self: *const ChromeUnsymbolizedArgs.Arg) usize {
            var res: usize = 0;
            if (self.module) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ChromeUnsymbolizedArgs.ArgWire.MODULE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.build_id) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ChromeUnsymbolizedArgs.ArgWire.BUILD_ID_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.address != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeUnsymbolizedArgs.ArgWire.ADDRESS_WIRE) + gremlin.sizes.sizeI64(self.address);
            }
            if (self.google_lookup_id) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ChromeUnsymbolizedArgs.ArgWire.GOOGLE_LOOKUP_ID_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            return res;
        }
        pub fn encode(self: *const ChromeUnsymbolizedArgs.Arg, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ChromeUnsymbolizedArgs.Arg, target: *gremlin.Writer) void {
            if (self.module) |v| {
                if (v.len > 0) {
                    target.appendBytes(ChromeUnsymbolizedArgs.ArgWire.MODULE_WIRE, v);
                }
            }
            if (self.build_id) |v| {
                if (v.len > 0) {
                    target.appendBytes(ChromeUnsymbolizedArgs.ArgWire.BUILD_ID_WIRE, v);
                }
            }
            if (self.address != 0) {
                target.appendInt64(ChromeUnsymbolizedArgs.ArgWire.ADDRESS_WIRE, self.address);
            }
            if (self.google_lookup_id) |v| {
                if (v.len > 0) {
                    target.appendBytes(ChromeUnsymbolizedArgs.ArgWire.GOOGLE_LOOKUP_ID_WIRE, v);
                }
            }
        }
    };
    pub const ArgReader = struct {
        buf: gremlin.Reader,
        _module: ?[]const u8 = null,
        _build_id: ?[]const u8 = null,
        _address: i64 = 0,
        _google_lookup_id: ?[]const u8 = null,
        pub fn init(src: []const u8) gremlin.Error!ChromeUnsymbolizedArgs.ArgReader {
            const buf = gremlin.Reader.init(src);
            var res = ChromeUnsymbolizedArgs.ArgReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ChromeUnsymbolizedArgs.ArgWire.MODULE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._module = result.value;
                    },
                    ChromeUnsymbolizedArgs.ArgWire.BUILD_ID_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._build_id = result.value;
                    },
                    ChromeUnsymbolizedArgs.ArgWire.ADDRESS_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._address = result.value;
                    },
                    ChromeUnsymbolizedArgs.ArgWire.GOOGLE_LOOKUP_ID_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._google_lookup_id = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getModule(self: *const ChromeUnsymbolizedArgs.ArgReader) []const u8 {
            return self._module orelse &[_]u8{};
        }
        pub inline fn getBuildId(self: *const ChromeUnsymbolizedArgs.ArgReader) []const u8 {
            return self._build_id orelse &[_]u8{};
        }
        pub inline fn getAddress(self: *const ChromeUnsymbolizedArgs.ArgReader) i64 {
            return self._address;
        }
        pub inline fn getGoogleLookupId(self: *const ChromeUnsymbolizedArgs.ArgReader) []const u8 {
            return self._google_lookup_id orelse &[_]u8{};
        }
    };
    // fields
    args: ?[]const ?ChromeUnsymbolizedArgs.Arg = null,
    pub fn calcProtobufSize(self: *const ChromeUnsymbolizedArgs) usize {
        var res: usize = 0;
        if (self.args) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ChromeUnsymbolizedArgsWire.ARGS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ChromeUnsymbolizedArgs, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeUnsymbolizedArgs, target: *gremlin.Writer) void {
        if (self.args) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ChromeUnsymbolizedArgsWire.ARGS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ChromeUnsymbolizedArgsWire.ARGS_WIRE, 0);
                }
            }
        }
    }
};
pub const ChromeUnsymbolizedArgsReader = struct {
    buf: gremlin.Reader,
    _args_offset: ?usize = null,
    _args_last_offset: ?usize = null,
    _args_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeUnsymbolizedArgsReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeUnsymbolizedArgsReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeUnsymbolizedArgsWire.ARGS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._args_offset == null) {
                        res._args_offset = offset - result.size;
                    }
                    res._args_last_offset = offset;
                    res._args_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn argsCount(self: *const ChromeUnsymbolizedArgsReader) usize {
        return self._args_cnt;
    }
    pub fn argsNext(self: *ChromeUnsymbolizedArgsReader) ?ChromeUnsymbolizedArgs.ArgReader {
        if (self._args_offset == null) return null;
        const current_offset = self._args_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ChromeUnsymbolizedArgs.ArgReader.init(result.value) catch return null;
        if (self._args_last_offset != null and current_offset >= self._args_last_offset.?) {
            self._args_offset = null;
            return msg;
        }
        if (self._args_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._args_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ChromeUnsymbolizedArgsWire.ARGS_WIRE) {
                self._args_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._args_offset = null;
        return msg;
    }
};
