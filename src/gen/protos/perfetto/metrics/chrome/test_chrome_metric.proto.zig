// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TestChromeMetricWire = struct {
    const TEST_VALUE_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const TestChromeMetric = struct {
    // fields
    test_value: i64 = 0,
    pub fn calcProtobufSize(self: *const TestChromeMetric) usize {
        var res: usize = 0;
        if (self.test_value != 0) {
            res += gremlin.sizes.sizeWireNumber(TestChromeMetricWire.TEST_VALUE_WIRE) + gremlin.sizes.sizeI64(self.test_value);
        }
        return res;
    }
    pub fn encode(self: *const TestChromeMetric, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TestChromeMetric, target: *gremlin.Writer) void {
        if (self.test_value != 0) {
            target.appendInt64(TestChromeMetricWire.TEST_VALUE_WIRE, self.test_value);
        }
    }
};
pub const TestChromeMetricReader = struct {
    buf: gremlin.Reader,
    _test_value: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!TestChromeMetricReader {
        const buf = gremlin.Reader.init(src);
        var res = TestChromeMetricReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TestChromeMetricWire.TEST_VALUE_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._test_value = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTestValue(self: *const TestChromeMetricReader) i64 {
        return self._test_value;
    }
};
