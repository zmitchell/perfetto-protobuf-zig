// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ScrollJankWire = struct {
    const SCROLL_JANK_PERCENTAGE_WIRE: gremlin.ProtoWireNumber = 1;
    const SCROLL_MS_WIRE: gremlin.ProtoWireNumber = 2;
    const SCROLL_PROCESSING_MS_WIRE: gremlin.ProtoWireNumber = 3;
    const SCROLL_JANK_PROCESSING_MS_WIRE: gremlin.ProtoWireNumber = 4;
    const NUM_SCROLL_UPDATE_COUNT_WIRE: gremlin.ProtoWireNumber = 5;
    const NUM_SCROLL_UPDATE_JANK_COUNT_WIRE: gremlin.ProtoWireNumber = 6;
    const SCROLL_JANK_BUDGET_MS_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const ScrollJank = struct {
    // fields
    scroll_jank_percentage: f64 = 0.0,
    scroll_ms: f64 = 0.0,
    scroll_processing_ms: f64 = 0.0,
    scroll_jank_processing_ms: f64 = 0.0,
    num_scroll_update_count: i64 = 0,
    num_scroll_update_jank_count: i64 = 0,
    scroll_jank_budget_ms: f64 = 0.0,
    pub fn calcProtobufSize(self: *const ScrollJank) usize {
        var res: usize = 0;
        if (self.scroll_jank_percentage != 0.0) {
            res += gremlin.sizes.sizeWireNumber(ScrollJankWire.SCROLL_JANK_PERCENTAGE_WIRE) + gremlin.sizes.sizeDouble(self.scroll_jank_percentage);
        }
        if (self.scroll_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(ScrollJankWire.SCROLL_MS_WIRE) + gremlin.sizes.sizeDouble(self.scroll_ms);
        }
        if (self.scroll_processing_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(ScrollJankWire.SCROLL_PROCESSING_MS_WIRE) + gremlin.sizes.sizeDouble(self.scroll_processing_ms);
        }
        if (self.scroll_jank_processing_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(ScrollJankWire.SCROLL_JANK_PROCESSING_MS_WIRE) + gremlin.sizes.sizeDouble(self.scroll_jank_processing_ms);
        }
        if (self.num_scroll_update_count != 0) {
            res += gremlin.sizes.sizeWireNumber(ScrollJankWire.NUM_SCROLL_UPDATE_COUNT_WIRE) + gremlin.sizes.sizeI64(self.num_scroll_update_count);
        }
        if (self.num_scroll_update_jank_count != 0) {
            res += gremlin.sizes.sizeWireNumber(ScrollJankWire.NUM_SCROLL_UPDATE_JANK_COUNT_WIRE) + gremlin.sizes.sizeI64(self.num_scroll_update_jank_count);
        }
        if (self.scroll_jank_budget_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(ScrollJankWire.SCROLL_JANK_BUDGET_MS_WIRE) + gremlin.sizes.sizeDouble(self.scroll_jank_budget_ms);
        }
        return res;
    }
    pub fn encode(self: *const ScrollJank, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ScrollJank, target: *gremlin.Writer) void {
        if (self.scroll_jank_percentage != 0.0) {
            target.appendFloat64(ScrollJankWire.SCROLL_JANK_PERCENTAGE_WIRE, self.scroll_jank_percentage);
        }
        if (self.scroll_ms != 0.0) {
            target.appendFloat64(ScrollJankWire.SCROLL_MS_WIRE, self.scroll_ms);
        }
        if (self.scroll_processing_ms != 0.0) {
            target.appendFloat64(ScrollJankWire.SCROLL_PROCESSING_MS_WIRE, self.scroll_processing_ms);
        }
        if (self.scroll_jank_processing_ms != 0.0) {
            target.appendFloat64(ScrollJankWire.SCROLL_JANK_PROCESSING_MS_WIRE, self.scroll_jank_processing_ms);
        }
        if (self.num_scroll_update_count != 0) {
            target.appendInt64(ScrollJankWire.NUM_SCROLL_UPDATE_COUNT_WIRE, self.num_scroll_update_count);
        }
        if (self.num_scroll_update_jank_count != 0) {
            target.appendInt64(ScrollJankWire.NUM_SCROLL_UPDATE_JANK_COUNT_WIRE, self.num_scroll_update_jank_count);
        }
        if (self.scroll_jank_budget_ms != 0.0) {
            target.appendFloat64(ScrollJankWire.SCROLL_JANK_BUDGET_MS_WIRE, self.scroll_jank_budget_ms);
        }
    }
};
pub const ScrollJankReader = struct {
    buf: gremlin.Reader,
    _scroll_jank_percentage: f64 = 0.0,
    _scroll_ms: f64 = 0.0,
    _scroll_processing_ms: f64 = 0.0,
    _scroll_jank_processing_ms: f64 = 0.0,
    _num_scroll_update_count: i64 = 0,
    _num_scroll_update_jank_count: i64 = 0,
    _scroll_jank_budget_ms: f64 = 0.0,
    pub fn init(src: []const u8) gremlin.Error!ScrollJankReader {
        const buf = gremlin.Reader.init(src);
        var res = ScrollJankReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ScrollJankWire.SCROLL_JANK_PERCENTAGE_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._scroll_jank_percentage = result.value;
                },
                ScrollJankWire.SCROLL_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._scroll_ms = result.value;
                },
                ScrollJankWire.SCROLL_PROCESSING_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._scroll_processing_ms = result.value;
                },
                ScrollJankWire.SCROLL_JANK_PROCESSING_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._scroll_jank_processing_ms = result.value;
                },
                ScrollJankWire.NUM_SCROLL_UPDATE_COUNT_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._num_scroll_update_count = result.value;
                },
                ScrollJankWire.NUM_SCROLL_UPDATE_JANK_COUNT_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._num_scroll_update_jank_count = result.value;
                },
                ScrollJankWire.SCROLL_JANK_BUDGET_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._scroll_jank_budget_ms = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getScrollJankPercentage(self: *const ScrollJankReader) f64 {
        return self._scroll_jank_percentage;
    }
    pub inline fn getScrollMs(self: *const ScrollJankReader) f64 {
        return self._scroll_ms;
    }
    pub inline fn getScrollProcessingMs(self: *const ScrollJankReader) f64 {
        return self._scroll_processing_ms;
    }
    pub inline fn getScrollJankProcessingMs(self: *const ScrollJankReader) f64 {
        return self._scroll_jank_processing_ms;
    }
    pub inline fn getNumScrollUpdateCount(self: *const ScrollJankReader) i64 {
        return self._num_scroll_update_count;
    }
    pub inline fn getNumScrollUpdateJankCount(self: *const ScrollJankReader) i64 {
        return self._num_scroll_update_jank_count;
    }
    pub inline fn getScrollJankBudgetMs(self: *const ScrollJankReader) f64 {
        return self._scroll_jank_budget_ms;
    }
};
