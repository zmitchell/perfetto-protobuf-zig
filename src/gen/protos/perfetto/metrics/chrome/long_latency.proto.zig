// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeLongLatencyWire = struct {
    const LONG_LATENCY_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ChromeLongLatency = struct {
    // nested structs
    const LongLatencyWire = struct {
        const TS_WIRE: gremlin.ProtoWireNumber = 1;
        const EVENT_TYPE_WIRE: gremlin.ProtoWireNumber = 2;
        const PROCESS_NAME_WIRE: gremlin.ProtoWireNumber = 3;
        const PID_WIRE: gremlin.ProtoWireNumber = 4;
    };
    pub const LongLatency = struct {
        // fields
        ts: i64 = 0,
        event_type: ?[]const u8 = null,
        process_name: ?[]const u8 = null,
        pid: i64 = 0,
        pub fn calcProtobufSize(self: *const ChromeLongLatency.LongLatency) usize {
            var res: usize = 0;
            if (self.ts != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeLongLatency.LongLatencyWire.TS_WIRE) + gremlin.sizes.sizeI64(self.ts);
            }
            if (self.event_type) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ChromeLongLatency.LongLatencyWire.EVENT_TYPE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.process_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ChromeLongLatency.LongLatencyWire.PROCESS_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.pid != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeLongLatency.LongLatencyWire.PID_WIRE) + gremlin.sizes.sizeI64(self.pid);
            }
            return res;
        }
        pub fn encode(self: *const ChromeLongLatency.LongLatency, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ChromeLongLatency.LongLatency, target: *gremlin.Writer) void {
            if (self.ts != 0) {
                target.appendInt64(ChromeLongLatency.LongLatencyWire.TS_WIRE, self.ts);
            }
            if (self.event_type) |v| {
                if (v.len > 0) {
                    target.appendBytes(ChromeLongLatency.LongLatencyWire.EVENT_TYPE_WIRE, v);
                }
            }
            if (self.process_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(ChromeLongLatency.LongLatencyWire.PROCESS_NAME_WIRE, v);
                }
            }
            if (self.pid != 0) {
                target.appendInt64(ChromeLongLatency.LongLatencyWire.PID_WIRE, self.pid);
            }
        }
    };
    pub const LongLatencyReader = struct {
        buf: gremlin.Reader,
        _ts: i64 = 0,
        _event_type: ?[]const u8 = null,
        _process_name: ?[]const u8 = null,
        _pid: i64 = 0,
        pub fn init(src: []const u8) gremlin.Error!ChromeLongLatency.LongLatencyReader {
            const buf = gremlin.Reader.init(src);
            var res = ChromeLongLatency.LongLatencyReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ChromeLongLatency.LongLatencyWire.TS_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._ts = result.value;
                    },
                    ChromeLongLatency.LongLatencyWire.EVENT_TYPE_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._event_type = result.value;
                    },
                    ChromeLongLatency.LongLatencyWire.PROCESS_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._process_name = result.value;
                    },
                    ChromeLongLatency.LongLatencyWire.PID_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._pid = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getTs(self: *const ChromeLongLatency.LongLatencyReader) i64 {
            return self._ts;
        }
        pub inline fn getEventType(self: *const ChromeLongLatency.LongLatencyReader) []const u8 {
            return self._event_type orelse &[_]u8{};
        }
        pub inline fn getProcessName(self: *const ChromeLongLatency.LongLatencyReader) []const u8 {
            return self._process_name orelse &[_]u8{};
        }
        pub inline fn getPid(self: *const ChromeLongLatency.LongLatencyReader) i64 {
            return self._pid;
        }
    };
    // fields
    long_latency: ?[]const ?ChromeLongLatency.LongLatency = null,
    pub fn calcProtobufSize(self: *const ChromeLongLatency) usize {
        var res: usize = 0;
        if (self.long_latency) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ChromeLongLatencyWire.LONG_LATENCY_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ChromeLongLatency, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeLongLatency, target: *gremlin.Writer) void {
        if (self.long_latency) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ChromeLongLatencyWire.LONG_LATENCY_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ChromeLongLatencyWire.LONG_LATENCY_WIRE, 0);
                }
            }
        }
    }
};
pub const ChromeLongLatencyReader = struct {
    buf: gremlin.Reader,
    _long_latency_offset: ?usize = null,
    _long_latency_last_offset: ?usize = null,
    _long_latency_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeLongLatencyReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeLongLatencyReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeLongLatencyWire.LONG_LATENCY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._long_latency_offset == null) {
                        res._long_latency_offset = offset - result.size;
                    }
                    res._long_latency_last_offset = offset;
                    res._long_latency_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn longLatencyCount(self: *const ChromeLongLatencyReader) usize {
        return self._long_latency_cnt;
    }
    pub fn longLatencyNext(self: *ChromeLongLatencyReader) ?ChromeLongLatency.LongLatencyReader {
        if (self._long_latency_offset == null) return null;
        const current_offset = self._long_latency_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ChromeLongLatency.LongLatencyReader.init(result.value) catch return null;
        if (self._long_latency_last_offset != null and current_offset >= self._long_latency_last_offset.?) {
            self._long_latency_offset = null;
            return msg;
        }
        if (self._long_latency_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._long_latency_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ChromeLongLatencyWire.LONG_LATENCY_WIRE) {
                self._long_latency_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._long_latency_offset = null;
        return msg;
    }
};
