// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TouchJankWire = struct {
    const TOUCH_JANK_PERCENTAGE_WIRE: gremlin.ProtoWireNumber = 1;
    const TOUCH_MS_WIRE: gremlin.ProtoWireNumber = 2;
    const TOUCH_PROCESSING_MS_WIRE: gremlin.ProtoWireNumber = 3;
    const TOUCH_JANK_PROCESSING_MS_WIRE: gremlin.ProtoWireNumber = 4;
    const NUM_TOUCH_UPDATE_COUNT_WIRE: gremlin.ProtoWireNumber = 5;
    const NUM_TOUCH_UPDATE_JANK_COUNT_WIRE: gremlin.ProtoWireNumber = 6;
    const TOUCH_JANK_BUDGET_MS_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const TouchJank = struct {
    // fields
    touch_jank_percentage: f64 = 0.0,
    touch_ms: f64 = 0.0,
    touch_processing_ms: f64 = 0.0,
    touch_jank_processing_ms: f64 = 0.0,
    num_touch_update_count: i64 = 0,
    num_touch_update_jank_count: i64 = 0,
    touch_jank_budget_ms: f64 = 0.0,
    pub fn calcProtobufSize(self: *const TouchJank) usize {
        var res: usize = 0;
        if (self.touch_jank_percentage != 0.0) {
            res += gremlin.sizes.sizeWireNumber(TouchJankWire.TOUCH_JANK_PERCENTAGE_WIRE) + gremlin.sizes.sizeDouble(self.touch_jank_percentage);
        }
        if (self.touch_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(TouchJankWire.TOUCH_MS_WIRE) + gremlin.sizes.sizeDouble(self.touch_ms);
        }
        if (self.touch_processing_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(TouchJankWire.TOUCH_PROCESSING_MS_WIRE) + gremlin.sizes.sizeDouble(self.touch_processing_ms);
        }
        if (self.touch_jank_processing_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(TouchJankWire.TOUCH_JANK_PROCESSING_MS_WIRE) + gremlin.sizes.sizeDouble(self.touch_jank_processing_ms);
        }
        if (self.num_touch_update_count != 0) {
            res += gremlin.sizes.sizeWireNumber(TouchJankWire.NUM_TOUCH_UPDATE_COUNT_WIRE) + gremlin.sizes.sizeI64(self.num_touch_update_count);
        }
        if (self.num_touch_update_jank_count != 0) {
            res += gremlin.sizes.sizeWireNumber(TouchJankWire.NUM_TOUCH_UPDATE_JANK_COUNT_WIRE) + gremlin.sizes.sizeI64(self.num_touch_update_jank_count);
        }
        if (self.touch_jank_budget_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(TouchJankWire.TOUCH_JANK_BUDGET_MS_WIRE) + gremlin.sizes.sizeDouble(self.touch_jank_budget_ms);
        }
        return res;
    }
    pub fn encode(self: *const TouchJank, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TouchJank, target: *gremlin.Writer) void {
        if (self.touch_jank_percentage != 0.0) {
            target.appendFloat64(TouchJankWire.TOUCH_JANK_PERCENTAGE_WIRE, self.touch_jank_percentage);
        }
        if (self.touch_ms != 0.0) {
            target.appendFloat64(TouchJankWire.TOUCH_MS_WIRE, self.touch_ms);
        }
        if (self.touch_processing_ms != 0.0) {
            target.appendFloat64(TouchJankWire.TOUCH_PROCESSING_MS_WIRE, self.touch_processing_ms);
        }
        if (self.touch_jank_processing_ms != 0.0) {
            target.appendFloat64(TouchJankWire.TOUCH_JANK_PROCESSING_MS_WIRE, self.touch_jank_processing_ms);
        }
        if (self.num_touch_update_count != 0) {
            target.appendInt64(TouchJankWire.NUM_TOUCH_UPDATE_COUNT_WIRE, self.num_touch_update_count);
        }
        if (self.num_touch_update_jank_count != 0) {
            target.appendInt64(TouchJankWire.NUM_TOUCH_UPDATE_JANK_COUNT_WIRE, self.num_touch_update_jank_count);
        }
        if (self.touch_jank_budget_ms != 0.0) {
            target.appendFloat64(TouchJankWire.TOUCH_JANK_BUDGET_MS_WIRE, self.touch_jank_budget_ms);
        }
    }
};
pub const TouchJankReader = struct {
    buf: gremlin.Reader,
    _touch_jank_percentage: f64 = 0.0,
    _touch_ms: f64 = 0.0,
    _touch_processing_ms: f64 = 0.0,
    _touch_jank_processing_ms: f64 = 0.0,
    _num_touch_update_count: i64 = 0,
    _num_touch_update_jank_count: i64 = 0,
    _touch_jank_budget_ms: f64 = 0.0,
    pub fn init(src: []const u8) gremlin.Error!TouchJankReader {
        const buf = gremlin.Reader.init(src);
        var res = TouchJankReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TouchJankWire.TOUCH_JANK_PERCENTAGE_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._touch_jank_percentage = result.value;
                },
                TouchJankWire.TOUCH_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._touch_ms = result.value;
                },
                TouchJankWire.TOUCH_PROCESSING_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._touch_processing_ms = result.value;
                },
                TouchJankWire.TOUCH_JANK_PROCESSING_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._touch_jank_processing_ms = result.value;
                },
                TouchJankWire.NUM_TOUCH_UPDATE_COUNT_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._num_touch_update_count = result.value;
                },
                TouchJankWire.NUM_TOUCH_UPDATE_JANK_COUNT_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._num_touch_update_jank_count = result.value;
                },
                TouchJankWire.TOUCH_JANK_BUDGET_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._touch_jank_budget_ms = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTouchJankPercentage(self: *const TouchJankReader) f64 {
        return self._touch_jank_percentage;
    }
    pub inline fn getTouchMs(self: *const TouchJankReader) f64 {
        return self._touch_ms;
    }
    pub inline fn getTouchProcessingMs(self: *const TouchJankReader) f64 {
        return self._touch_processing_ms;
    }
    pub inline fn getTouchJankProcessingMs(self: *const TouchJankReader) f64 {
        return self._touch_jank_processing_ms;
    }
    pub inline fn getNumTouchUpdateCount(self: *const TouchJankReader) i64 {
        return self._num_touch_update_count;
    }
    pub inline fn getNumTouchUpdateJankCount(self: *const TouchJankReader) i64 {
        return self._num_touch_update_jank_count;
    }
    pub inline fn getTouchJankBudgetMs(self: *const TouchJankReader) f64 {
        return self._touch_jank_budget_ms;
    }
};
