// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const HistogramSummaryWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
    const MEAN_WIRE: gremlin.ProtoWireNumber = 2;
    const COUNT_WIRE: gremlin.ProtoWireNumber = 3;
    const SUM_WIRE: gremlin.ProtoWireNumber = 4;
    const MAX_WIRE: gremlin.ProtoWireNumber = 5;
    const P90_WIRE: gremlin.ProtoWireNumber = 6;
    const P50_WIRE: gremlin.ProtoWireNumber = 7;
};
pub const HistogramSummary = struct {
    // fields
    name: ?[]const u8 = null,
    mean: i64 = 0,
    count: u32 = 0,
    sum: i64 = 0,
    max: i64 = 0,
    p90: i64 = 0,
    p50: i64 = 0,
    pub fn calcProtobufSize(self: *const HistogramSummary) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(HistogramSummaryWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.mean != 0) {
            res += gremlin.sizes.sizeWireNumber(HistogramSummaryWire.MEAN_WIRE) + gremlin.sizes.sizeI64(self.mean);
        }
        if (self.count != 0) {
            res += gremlin.sizes.sizeWireNumber(HistogramSummaryWire.COUNT_WIRE) + gremlin.sizes.sizeU32(self.count);
        }
        if (self.sum != 0) {
            res += gremlin.sizes.sizeWireNumber(HistogramSummaryWire.SUM_WIRE) + gremlin.sizes.sizeI64(self.sum);
        }
        if (self.max != 0) {
            res += gremlin.sizes.sizeWireNumber(HistogramSummaryWire.MAX_WIRE) + gremlin.sizes.sizeI64(self.max);
        }
        if (self.p90 != 0) {
            res += gremlin.sizes.sizeWireNumber(HistogramSummaryWire.P90_WIRE) + gremlin.sizes.sizeI64(self.p90);
        }
        if (self.p50 != 0) {
            res += gremlin.sizes.sizeWireNumber(HistogramSummaryWire.P50_WIRE) + gremlin.sizes.sizeI64(self.p50);
        }
        return res;
    }
    pub fn encode(self: *const HistogramSummary, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const HistogramSummary, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(HistogramSummaryWire.NAME_WIRE, v);
            }
        }
        if (self.mean != 0) {
            target.appendInt64(HistogramSummaryWire.MEAN_WIRE, self.mean);
        }
        if (self.count != 0) {
            target.appendUint32(HistogramSummaryWire.COUNT_WIRE, self.count);
        }
        if (self.sum != 0) {
            target.appendInt64(HistogramSummaryWire.SUM_WIRE, self.sum);
        }
        if (self.max != 0) {
            target.appendInt64(HistogramSummaryWire.MAX_WIRE, self.max);
        }
        if (self.p90 != 0) {
            target.appendInt64(HistogramSummaryWire.P90_WIRE, self.p90);
        }
        if (self.p50 != 0) {
            target.appendInt64(HistogramSummaryWire.P50_WIRE, self.p50);
        }
    }
};
pub const HistogramSummaryReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    _mean: i64 = 0,
    _count: u32 = 0,
    _sum: i64 = 0,
    _max: i64 = 0,
    _p90: i64 = 0,
    _p50: i64 = 0,
    pub fn init(src: []const u8) gremlin.Error!HistogramSummaryReader {
        const buf = gremlin.Reader.init(src);
        var res = HistogramSummaryReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                HistogramSummaryWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                HistogramSummaryWire.MEAN_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._mean = result.value;
                },
                HistogramSummaryWire.COUNT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._count = result.value;
                },
                HistogramSummaryWire.SUM_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._sum = result.value;
                },
                HistogramSummaryWire.MAX_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._max = result.value;
                },
                HistogramSummaryWire.P90_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._p90 = result.value;
                },
                HistogramSummaryWire.P50_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._p50 = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const HistogramSummaryReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
    pub inline fn getMean(self: *const HistogramSummaryReader) i64 {
        return self._mean;
    }
    pub inline fn getCount(self: *const HistogramSummaryReader) u32 {
        return self._count;
    }
    pub inline fn getSum(self: *const HistogramSummaryReader) i64 {
        return self._sum;
    }
    pub inline fn getMax(self: *const HistogramSummaryReader) i64 {
        return self._max;
    }
    pub inline fn getP90(self: *const HistogramSummaryReader) i64 {
        return self._p90;
    }
    pub inline fn getP50(self: *const HistogramSummaryReader) i64 {
        return self._p50;
    }
};
const ChromeHistogramSummariesWire = struct {
    const HISTOGRAM_SUMMARY_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ChromeHistogramSummaries = struct {
    // fields
    histogram_summary: ?[]const ?HistogramSummary = null,
    pub fn calcProtobufSize(self: *const ChromeHistogramSummaries) usize {
        var res: usize = 0;
        if (self.histogram_summary) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ChromeHistogramSummariesWire.HISTOGRAM_SUMMARY_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ChromeHistogramSummaries, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeHistogramSummaries, target: *gremlin.Writer) void {
        if (self.histogram_summary) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ChromeHistogramSummariesWire.HISTOGRAM_SUMMARY_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ChromeHistogramSummariesWire.HISTOGRAM_SUMMARY_WIRE, 0);
                }
            }
        }
    }
};
pub const ChromeHistogramSummariesReader = struct {
    buf: gremlin.Reader,
    _histogram_summary_offset: ?usize = null,
    _histogram_summary_last_offset: ?usize = null,
    _histogram_summary_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeHistogramSummariesReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeHistogramSummariesReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeHistogramSummariesWire.HISTOGRAM_SUMMARY_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._histogram_summary_offset == null) {
                        res._histogram_summary_offset = offset - result.size;
                    }
                    res._histogram_summary_last_offset = offset;
                    res._histogram_summary_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn histogramSummaryCount(self: *const ChromeHistogramSummariesReader) usize {
        return self._histogram_summary_cnt;
    }
    pub fn histogramSummaryNext(self: *ChromeHistogramSummariesReader) ?HistogramSummaryReader {
        if (self._histogram_summary_offset == null) return null;
        const current_offset = self._histogram_summary_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = HistogramSummaryReader.init(result.value) catch return null;
        if (self._histogram_summary_last_offset != null and current_offset >= self._histogram_summary_last_offset.?) {
            self._histogram_summary_offset = null;
            return msg;
        }
        if (self._histogram_summary_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._histogram_summary_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ChromeHistogramSummariesWire.HISTOGRAM_SUMMARY_WIRE) {
                self._histogram_summary_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._histogram_summary_offset = null;
        return msg;
    }
};
