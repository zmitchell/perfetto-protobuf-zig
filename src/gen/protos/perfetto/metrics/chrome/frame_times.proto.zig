// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const FrameTimesWire = struct {
    const FRAME_TIME_WIRE: gremlin.ProtoWireNumber = 1;
    const EXP_FRAME_TIME_WIRE: gremlin.ProtoWireNumber = 2;
    const AVG_SURFACE_FPS_WIRE: gremlin.ProtoWireNumber = 3;
    const EXP_AVG_SURFACE_FPS_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const FrameTimes = struct {
    // fields
    frame_time: ?[]const f64 = null,
    exp_frame_time: ?[]const f64 = null,
    avg_surface_fps: f64 = 0.0,
    exp_avg_surface_fps: f64 = 0.0,
    pub fn calcProtobufSize(self: *const FrameTimes) usize {
        var res: usize = 0;
        if (self.frame_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(FrameTimesWire.FRAME_TIME_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(FrameTimesWire.FRAME_TIME_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.exp_frame_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                res += gremlin.sizes.sizeWireNumber(FrameTimesWire.EXP_FRAME_TIME_WIRE) + gremlin.sizes.sizeDouble(arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                res += gremlin.sizes.sizeWireNumber(FrameTimesWire.EXP_FRAME_TIME_WIRE) + gremlin.sizes.sizeUsize(packed_size) + packed_size;
            }
        }
        if (self.avg_surface_fps != 0.0) {
            res += gremlin.sizes.sizeWireNumber(FrameTimesWire.AVG_SURFACE_FPS_WIRE) + gremlin.sizes.sizeDouble(self.avg_surface_fps);
        }
        if (self.exp_avg_surface_fps != 0.0) {
            res += gremlin.sizes.sizeWireNumber(FrameTimesWire.EXP_AVG_SURFACE_FPS_WIRE) + gremlin.sizes.sizeDouble(self.exp_avg_surface_fps);
        }
        return res;
    }
    pub fn encode(self: *const FrameTimes, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FrameTimes, target: *gremlin.Writer) void {
        if (self.frame_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(FrameTimesWire.FRAME_TIME_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(FrameTimesWire.FRAME_TIME_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.exp_frame_time) |arr| {
            if (arr.len == 0) {} else if (arr.len == 1) {
                target.appendFloat64(FrameTimesWire.EXP_FRAME_TIME_WIRE, arr[0]);
            } else {
                var packed_size: usize = 0;
                for (arr) |v| {
                    packed_size += gremlin.sizes.sizeDouble(v);
                }
                target.appendBytesTag(FrameTimesWire.EXP_FRAME_TIME_WIRE, packed_size);
                for (arr) |v| {
                    target.appendFloat64WithoutTag(v);
                }
            }
        }
        if (self.avg_surface_fps != 0.0) {
            target.appendFloat64(FrameTimesWire.AVG_SURFACE_FPS_WIRE, self.avg_surface_fps);
        }
        if (self.exp_avg_surface_fps != 0.0) {
            target.appendFloat64(FrameTimesWire.EXP_AVG_SURFACE_FPS_WIRE, self.exp_avg_surface_fps);
        }
    }
};
pub const FrameTimesReader = struct {
    buf: gremlin.Reader,
    _frame_time_offset: ?usize = null,
    _frame_time_last_offset: ?usize = null,
    _frame_time_packed: bool = false,
    _exp_frame_time_offset: ?usize = null,
    _exp_frame_time_last_offset: ?usize = null,
    _exp_frame_time_packed: bool = false,
    _avg_surface_fps: f64 = 0.0,
    _exp_avg_surface_fps: f64 = 0.0,
    pub fn init(src: []const u8) gremlin.Error!FrameTimesReader {
        const buf = gremlin.Reader.init(src);
        var res = FrameTimesReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FrameTimesWire.FRAME_TIME_WIRE => {
                    if (res._frame_time_offset == null) {
                        res._frame_time_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._frame_time_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._frame_time_offset = offset + length_result.size;
                        res._frame_time_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._frame_time_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._frame_time_last_offset = offset;
                    }
                },
                FrameTimesWire.EXP_FRAME_TIME_WIRE => {
                    if (res._exp_frame_time_offset == null) {
                        res._exp_frame_time_offset = offset;
                    }
                    if (tag.wire == gremlin.ProtoWireType.bytes) {
                        res._exp_frame_time_packed = true;
                        const length_result = try buf.readVarInt(offset);
                        res._exp_frame_time_offset = offset + length_result.size;
                        res._exp_frame_time_last_offset = offset + length_result.size + @as(usize, @intCast(length_result.value));
                        offset = res._exp_frame_time_last_offset.?;
                    } else {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._exp_frame_time_last_offset = offset;
                    }
                },
                FrameTimesWire.AVG_SURFACE_FPS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._avg_surface_fps = result.value;
                },
                FrameTimesWire.EXP_AVG_SURFACE_FPS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._exp_avg_surface_fps = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn frameTimeNext(self: *FrameTimesReader) gremlin.Error!?f64 {
        if (self._frame_time_offset == null) return null;
        const current_offset = self._frame_time_offset.?;
        if (current_offset >= self._frame_time_last_offset.?) {
            self._frame_time_offset = null;
            return null;
        }
        if (self._frame_time_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._frame_time_offset = current_offset + value_result.size;
            if (self._frame_time_offset.? >= self._frame_time_last_offset.?) {
                self._frame_time_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._frame_time_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == FrameTimesWire.FRAME_TIME_WIRE) {
                    self._frame_time_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._frame_time_offset = null;
            return value_result.value;
        }
    }
    pub fn expFrameTimeNext(self: *FrameTimesReader) gremlin.Error!?f64 {
        if (self._exp_frame_time_offset == null) return null;
        const current_offset = self._exp_frame_time_offset.?;
        if (current_offset >= self._exp_frame_time_last_offset.?) {
            self._exp_frame_time_offset = null;
            return null;
        }
        if (self._exp_frame_time_packed) {
            const value_result = try self.buf.readFloat64(current_offset);
            self._exp_frame_time_offset = current_offset + value_result.size;
            if (self._exp_frame_time_offset.? >= self._exp_frame_time_last_offset.?) {
                self._exp_frame_time_offset = null;
            }
            return value_result.value;
        } else {
            const value_result = try self.buf.readFloat64(current_offset);
            var next_offset = current_offset + value_result.size;
            const max_offset = self._exp_frame_time_last_offset.?;
            // Search for the next occurrence of this field
            while (next_offset < max_offset and self.buf.hasNext(next_offset, 0)) {
                const next_tag = try self.buf.readTagAt(next_offset);
                next_offset += next_tag.size;
                if (next_tag.number == FrameTimesWire.EXP_FRAME_TIME_WIRE) {
                    self._exp_frame_time_offset = next_offset;
                    return value_result.value;
                } else {
                    next_offset = try self.buf.skipData(next_offset, next_tag.wire);
                }
            }
            self._exp_frame_time_offset = null;
            return value_result.value;
        }
    }
    pub inline fn getAvgSurfaceFps(self: *const FrameTimesReader) f64 {
        return self._avg_surface_fps;
    }
    pub inline fn getExpAvgSurfaceFps(self: *const FrameTimesReader) f64 {
        return self._exp_avg_surface_fps;
    }
};
