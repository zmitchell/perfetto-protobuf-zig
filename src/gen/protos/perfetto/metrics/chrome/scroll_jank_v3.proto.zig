// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeScrollJankV3Wire = struct {
    const TRACE_NUM_FRAMES_WIRE: gremlin.ProtoWireNumber = 1;
    const TRACE_NUM_JANKY_FRAMES_WIRE: gremlin.ProtoWireNumber = 2;
    const TRACE_SCROLL_JANK_PERCENTAGE_WIRE: gremlin.ProtoWireNumber = 3;
    const VSYNC_INTERVAL_MS_WIRE: gremlin.ProtoWireNumber = 4;
    const SCROLLS_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const ChromeScrollJankV3 = struct {
    // nested structs
    const ScrollWire = struct {
        const NUM_FRAMES_WIRE: gremlin.ProtoWireNumber = 1;
        const NUM_JANKY_FRAMES_WIRE: gremlin.ProtoWireNumber = 2;
        const SCROLL_JANK_PERCENTAGE_WIRE: gremlin.ProtoWireNumber = 3;
        const MAX_DELAY_SINCE_LAST_FRAME_WIRE: gremlin.ProtoWireNumber = 4;
        const SCROLL_JANK_CAUSES_WIRE: gremlin.ProtoWireNumber = 5;
    };
    pub const Scroll = struct {
        // nested structs
        const ScrollJankCauseWire = struct {
            const CAUSE_WIRE: gremlin.ProtoWireNumber = 1;
            const SUB_CAUSE_WIRE: gremlin.ProtoWireNumber = 2;
            const DELAY_SINCE_LAST_FRAME_WIRE: gremlin.ProtoWireNumber = 3;
        };
        pub const ScrollJankCause = struct {
            // fields
            cause: ?[]const u8 = null,
            sub_cause: ?[]const u8 = null,
            delay_since_last_frame: f64 = 0.0,
            pub fn calcProtobufSize(self: *const ChromeScrollJankV3.Scroll.ScrollJankCause) usize {
                var res: usize = 0;
                if (self.cause) |v| {
                    if (v.len > 0) {
                        res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3.Scroll.ScrollJankCauseWire.CAUSE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                    }
                }
                if (self.sub_cause) |v| {
                    if (v.len > 0) {
                        res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3.Scroll.ScrollJankCauseWire.SUB_CAUSE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                    }
                }
                if (self.delay_since_last_frame != 0.0) {
                    res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3.Scroll.ScrollJankCauseWire.DELAY_SINCE_LAST_FRAME_WIRE) + gremlin.sizes.sizeDouble(self.delay_since_last_frame);
                }
                return res;
            }
            pub fn encode(self: *const ChromeScrollJankV3.Scroll.ScrollJankCause, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
                const size = self.calcProtobufSize();
                if (size == 0) {
                    return &[_]u8{};
                }
                const buf = try allocator.alloc(u8, self.calcProtobufSize());
                var writer = gremlin.Writer.init(buf);
                self.encodeTo(&writer);
                return buf;
            }
            pub fn encodeTo(self: *const ChromeScrollJankV3.Scroll.ScrollJankCause, target: *gremlin.Writer) void {
                if (self.cause) |v| {
                    if (v.len > 0) {
                        target.appendBytes(ChromeScrollJankV3.Scroll.ScrollJankCauseWire.CAUSE_WIRE, v);
                    }
                }
                if (self.sub_cause) |v| {
                    if (v.len > 0) {
                        target.appendBytes(ChromeScrollJankV3.Scroll.ScrollJankCauseWire.SUB_CAUSE_WIRE, v);
                    }
                }
                if (self.delay_since_last_frame != 0.0) {
                    target.appendFloat64(ChromeScrollJankV3.Scroll.ScrollJankCauseWire.DELAY_SINCE_LAST_FRAME_WIRE, self.delay_since_last_frame);
                }
            }
        };
        pub const ScrollJankCauseReader = struct {
            buf: gremlin.Reader,
            _cause: ?[]const u8 = null,
            _sub_cause: ?[]const u8 = null,
            _delay_since_last_frame: f64 = 0.0,
            pub fn init(src: []const u8) gremlin.Error!ChromeScrollJankV3.Scroll.ScrollJankCauseReader {
                const buf = gremlin.Reader.init(src);
                var res = ChromeScrollJankV3.Scroll.ScrollJankCauseReader{ .buf = buf };
                if (buf.buf.len == 0) {
                    return res;
                }
                var offset: usize = 0;
                while (buf.hasNext(offset, 0)) {
                    const tag = try buf.readTagAt(offset);
                    offset += tag.size;
                    switch (tag.number) {
                        ChromeScrollJankV3.Scroll.ScrollJankCauseWire.CAUSE_WIRE => {
                            const result = try buf.readBytes(offset);
                            offset += result.size;
                            res._cause = result.value;
                        },
                        ChromeScrollJankV3.Scroll.ScrollJankCauseWire.SUB_CAUSE_WIRE => {
                            const result = try buf.readBytes(offset);
                            offset += result.size;
                            res._sub_cause = result.value;
                        },
                        ChromeScrollJankV3.Scroll.ScrollJankCauseWire.DELAY_SINCE_LAST_FRAME_WIRE => {
                            const result = try buf.readFloat64(offset);
                            offset += result.size;
                            res._delay_since_last_frame = result.value;
                        },
                        else => {
                            offset = try buf.skipData(offset, tag.wire);
                        },
                    }
                }
                return res;
            }
            pub fn sourceBytes(self: *const @This()) []const u8 {
                return self.buf.buf;
            }
            pub inline fn getCause(self: *const ChromeScrollJankV3.Scroll.ScrollJankCauseReader) []const u8 {
                return self._cause orelse &[_]u8{};
            }
            pub inline fn getSubCause(self: *const ChromeScrollJankV3.Scroll.ScrollJankCauseReader) []const u8 {
                return self._sub_cause orelse &[_]u8{};
            }
            pub inline fn getDelaySinceLastFrame(self: *const ChromeScrollJankV3.Scroll.ScrollJankCauseReader) f64 {
                return self._delay_since_last_frame;
            }
        };
        // fields
        num_frames: i64 = 0,
        num_janky_frames: i64 = 0,
        scroll_jank_percentage: f64 = 0.0,
        max_delay_since_last_frame: f64 = 0.0,
        scroll_jank_causes: ?[]const ?ChromeScrollJankV3.Scroll.ScrollJankCause = null,
        pub fn calcProtobufSize(self: *const ChromeScrollJankV3.Scroll) usize {
            var res: usize = 0;
            if (self.num_frames != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3.ScrollWire.NUM_FRAMES_WIRE) + gremlin.sizes.sizeI64(self.num_frames);
            }
            if (self.num_janky_frames != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3.ScrollWire.NUM_JANKY_FRAMES_WIRE) + gremlin.sizes.sizeI64(self.num_janky_frames);
            }
            if (self.scroll_jank_percentage != 0.0) {
                res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3.ScrollWire.SCROLL_JANK_PERCENTAGE_WIRE) + gremlin.sizes.sizeDouble(self.scroll_jank_percentage);
            }
            if (self.max_delay_since_last_frame != 0.0) {
                res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3.ScrollWire.MAX_DELAY_SINCE_LAST_FRAME_WIRE) + gremlin.sizes.sizeDouble(self.max_delay_since_last_frame);
            }
            if (self.scroll_jank_causes) |arr| {
                for (arr) |maybe_v| {
                    res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3.ScrollWire.SCROLL_JANK_CAUSES_WIRE);
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        res += gremlin.sizes.sizeUsize(size) + size;
                    } else {
                        res += gremlin.sizes.sizeUsize(0);
                    }
                }
            }
            return res;
        }
        pub fn encode(self: *const ChromeScrollJankV3.Scroll, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ChromeScrollJankV3.Scroll, target: *gremlin.Writer) void {
            if (self.num_frames != 0) {
                target.appendInt64(ChromeScrollJankV3.ScrollWire.NUM_FRAMES_WIRE, self.num_frames);
            }
            if (self.num_janky_frames != 0) {
                target.appendInt64(ChromeScrollJankV3.ScrollWire.NUM_JANKY_FRAMES_WIRE, self.num_janky_frames);
            }
            if (self.scroll_jank_percentage != 0.0) {
                target.appendFloat64(ChromeScrollJankV3.ScrollWire.SCROLL_JANK_PERCENTAGE_WIRE, self.scroll_jank_percentage);
            }
            if (self.max_delay_since_last_frame != 0.0) {
                target.appendFloat64(ChromeScrollJankV3.ScrollWire.MAX_DELAY_SINCE_LAST_FRAME_WIRE, self.max_delay_since_last_frame);
            }
            if (self.scroll_jank_causes) |arr| {
                for (arr) |maybe_v| {
                    if (maybe_v) |v| {
                        const size = v.calcProtobufSize();
                        target.appendBytesTag(ChromeScrollJankV3.ScrollWire.SCROLL_JANK_CAUSES_WIRE, size);
                        v.encodeTo(target);
                    } else {
                        target.appendBytesTag(ChromeScrollJankV3.ScrollWire.SCROLL_JANK_CAUSES_WIRE, 0);
                    }
                }
            }
        }
    };
    pub const ScrollReader = struct {
        buf: gremlin.Reader,
        _num_frames: i64 = 0,
        _num_janky_frames: i64 = 0,
        _scroll_jank_percentage: f64 = 0.0,
        _max_delay_since_last_frame: f64 = 0.0,
        _scroll_jank_causes_offset: ?usize = null,
        _scroll_jank_causes_last_offset: ?usize = null,
        _scroll_jank_causes_cnt: usize = 0,
        pub fn init(src: []const u8) gremlin.Error!ChromeScrollJankV3.ScrollReader {
            const buf = gremlin.Reader.init(src);
            var res = ChromeScrollJankV3.ScrollReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ChromeScrollJankV3.ScrollWire.NUM_FRAMES_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._num_frames = result.value;
                    },
                    ChromeScrollJankV3.ScrollWire.NUM_JANKY_FRAMES_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._num_janky_frames = result.value;
                    },
                    ChromeScrollJankV3.ScrollWire.SCROLL_JANK_PERCENTAGE_WIRE => {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._scroll_jank_percentage = result.value;
                    },
                    ChromeScrollJankV3.ScrollWire.MAX_DELAY_SINCE_LAST_FRAME_WIRE => {
                        const result = try buf.readFloat64(offset);
                        offset += result.size;
                        res._max_delay_since_last_frame = result.value;
                    },
                    ChromeScrollJankV3.ScrollWire.SCROLL_JANK_CAUSES_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        if (res._scroll_jank_causes_offset == null) {
                            res._scroll_jank_causes_offset = offset - result.size;
                        }
                        res._scroll_jank_causes_last_offset = offset;
                        res._scroll_jank_causes_cnt += 1;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getNumFrames(self: *const ChromeScrollJankV3.ScrollReader) i64 {
            return self._num_frames;
        }
        pub inline fn getNumJankyFrames(self: *const ChromeScrollJankV3.ScrollReader) i64 {
            return self._num_janky_frames;
        }
        pub inline fn getScrollJankPercentage(self: *const ChromeScrollJankV3.ScrollReader) f64 {
            return self._scroll_jank_percentage;
        }
        pub inline fn getMaxDelaySinceLastFrame(self: *const ChromeScrollJankV3.ScrollReader) f64 {
            return self._max_delay_since_last_frame;
        }
        pub fn scrollJankCausesCount(self: *const ChromeScrollJankV3.ScrollReader) usize {
            return self._scroll_jank_causes_cnt;
        }
        pub fn scrollJankCausesNext(self: *ChromeScrollJankV3.ScrollReader) ?ChromeScrollJankV3.Scroll.ScrollJankCauseReader {
            if (self._scroll_jank_causes_offset == null) return null;
            const current_offset = self._scroll_jank_causes_offset.?;
            const result = self.buf.readBytes(current_offset) catch return null;
            const msg = ChromeScrollJankV3.Scroll.ScrollJankCauseReader.init(result.value) catch return null;
            if (self._scroll_jank_causes_last_offset != null and current_offset >= self._scroll_jank_causes_last_offset.?) {
                self._scroll_jank_causes_offset = null;
                return msg;
            }
            if (self._scroll_jank_causes_last_offset == null) unreachable;
            var next_offset = current_offset + result.size;
            const max_offset = self._scroll_jank_causes_last_offset.?;
            while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
                const tag = self.buf.readTagAt(next_offset) catch break;
                next_offset += tag.size;
                if (tag.number == ChromeScrollJankV3.ScrollWire.SCROLL_JANK_CAUSES_WIRE) {
                    self._scroll_jank_causes_offset = next_offset;
                    return msg;
                } else {
                    next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
                }
            }
            self._scroll_jank_causes_offset = null;
            return msg;
        }
    };
    // fields
    trace_num_frames: i64 = 0,
    trace_num_janky_frames: i64 = 0,
    trace_scroll_jank_percentage: f64 = 0.0,
    vsync_interval_ms: f64 = 0.0,
    scrolls: ?[]const ?ChromeScrollJankV3.Scroll = null,
    pub fn calcProtobufSize(self: *const ChromeScrollJankV3) usize {
        var res: usize = 0;
        if (self.trace_num_frames != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3Wire.TRACE_NUM_FRAMES_WIRE) + gremlin.sizes.sizeI64(self.trace_num_frames);
        }
        if (self.trace_num_janky_frames != 0) {
            res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3Wire.TRACE_NUM_JANKY_FRAMES_WIRE) + gremlin.sizes.sizeI64(self.trace_num_janky_frames);
        }
        if (self.trace_scroll_jank_percentage != 0.0) {
            res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3Wire.TRACE_SCROLL_JANK_PERCENTAGE_WIRE) + gremlin.sizes.sizeDouble(self.trace_scroll_jank_percentage);
        }
        if (self.vsync_interval_ms != 0.0) {
            res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3Wire.VSYNC_INTERVAL_MS_WIRE) + gremlin.sizes.sizeDouble(self.vsync_interval_ms);
        }
        if (self.scrolls) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ChromeScrollJankV3Wire.SCROLLS_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ChromeScrollJankV3, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeScrollJankV3, target: *gremlin.Writer) void {
        if (self.trace_num_frames != 0) {
            target.appendInt64(ChromeScrollJankV3Wire.TRACE_NUM_FRAMES_WIRE, self.trace_num_frames);
        }
        if (self.trace_num_janky_frames != 0) {
            target.appendInt64(ChromeScrollJankV3Wire.TRACE_NUM_JANKY_FRAMES_WIRE, self.trace_num_janky_frames);
        }
        if (self.trace_scroll_jank_percentage != 0.0) {
            target.appendFloat64(ChromeScrollJankV3Wire.TRACE_SCROLL_JANK_PERCENTAGE_WIRE, self.trace_scroll_jank_percentage);
        }
        if (self.vsync_interval_ms != 0.0) {
            target.appendFloat64(ChromeScrollJankV3Wire.VSYNC_INTERVAL_MS_WIRE, self.vsync_interval_ms);
        }
        if (self.scrolls) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ChromeScrollJankV3Wire.SCROLLS_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ChromeScrollJankV3Wire.SCROLLS_WIRE, 0);
                }
            }
        }
    }
};
pub const ChromeScrollJankV3Reader = struct {
    buf: gremlin.Reader,
    _trace_num_frames: i64 = 0,
    _trace_num_janky_frames: i64 = 0,
    _trace_scroll_jank_percentage: f64 = 0.0,
    _vsync_interval_ms: f64 = 0.0,
    _scrolls_offset: ?usize = null,
    _scrolls_last_offset: ?usize = null,
    _scrolls_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeScrollJankV3Reader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeScrollJankV3Reader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeScrollJankV3Wire.TRACE_NUM_FRAMES_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._trace_num_frames = result.value;
                },
                ChromeScrollJankV3Wire.TRACE_NUM_JANKY_FRAMES_WIRE => {
                    const result = try buf.readInt64(offset);
                    offset += result.size;
                    res._trace_num_janky_frames = result.value;
                },
                ChromeScrollJankV3Wire.TRACE_SCROLL_JANK_PERCENTAGE_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._trace_scroll_jank_percentage = result.value;
                },
                ChromeScrollJankV3Wire.VSYNC_INTERVAL_MS_WIRE => {
                    const result = try buf.readFloat64(offset);
                    offset += result.size;
                    res._vsync_interval_ms = result.value;
                },
                ChromeScrollJankV3Wire.SCROLLS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._scrolls_offset == null) {
                        res._scrolls_offset = offset - result.size;
                    }
                    res._scrolls_last_offset = offset;
                    res._scrolls_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getTraceNumFrames(self: *const ChromeScrollJankV3Reader) i64 {
        return self._trace_num_frames;
    }
    pub inline fn getTraceNumJankyFrames(self: *const ChromeScrollJankV3Reader) i64 {
        return self._trace_num_janky_frames;
    }
    pub inline fn getTraceScrollJankPercentage(self: *const ChromeScrollJankV3Reader) f64 {
        return self._trace_scroll_jank_percentage;
    }
    pub inline fn getVsyncIntervalMs(self: *const ChromeScrollJankV3Reader) f64 {
        return self._vsync_interval_ms;
    }
    pub fn scrollsCount(self: *const ChromeScrollJankV3Reader) usize {
        return self._scrolls_cnt;
    }
    pub fn scrollsNext(self: *ChromeScrollJankV3Reader) ?ChromeScrollJankV3.ScrollReader {
        if (self._scrolls_offset == null) return null;
        const current_offset = self._scrolls_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ChromeScrollJankV3.ScrollReader.init(result.value) catch return null;
        if (self._scrolls_last_offset != null and current_offset >= self._scrolls_last_offset.?) {
            self._scrolls_offset = null;
            return msg;
        }
        if (self._scrolls_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._scrolls_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ChromeScrollJankV3Wire.SCROLLS_WIRE) {
                self._scrolls_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._scrolls_offset = null;
        return msg;
    }
};
