// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const ChromeDroppedFramesWire = struct {
    const DROPPED_FRAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const ChromeDroppedFrames = struct {
    // nested structs
    const DroppedFrameWire = struct {
        const TS_WIRE: gremlin.ProtoWireNumber = 1;
        const PROCESS_NAME_WIRE: gremlin.ProtoWireNumber = 2;
        const PID_WIRE: gremlin.ProtoWireNumber = 3;
    };
    pub const DroppedFrame = struct {
        // fields
        ts: i64 = 0,
        process_name: ?[]const u8 = null,
        pid: i64 = 0,
        pub fn calcProtobufSize(self: *const ChromeDroppedFrames.DroppedFrame) usize {
            var res: usize = 0;
            if (self.ts != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeDroppedFrames.DroppedFrameWire.TS_WIRE) + gremlin.sizes.sizeI64(self.ts);
            }
            if (self.process_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(ChromeDroppedFrames.DroppedFrameWire.PROCESS_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.pid != 0) {
                res += gremlin.sizes.sizeWireNumber(ChromeDroppedFrames.DroppedFrameWire.PID_WIRE) + gremlin.sizes.sizeI64(self.pid);
            }
            return res;
        }
        pub fn encode(self: *const ChromeDroppedFrames.DroppedFrame, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const ChromeDroppedFrames.DroppedFrame, target: *gremlin.Writer) void {
            if (self.ts != 0) {
                target.appendInt64(ChromeDroppedFrames.DroppedFrameWire.TS_WIRE, self.ts);
            }
            if (self.process_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(ChromeDroppedFrames.DroppedFrameWire.PROCESS_NAME_WIRE, v);
                }
            }
            if (self.pid != 0) {
                target.appendInt64(ChromeDroppedFrames.DroppedFrameWire.PID_WIRE, self.pid);
            }
        }
    };
    pub const DroppedFrameReader = struct {
        buf: gremlin.Reader,
        _ts: i64 = 0,
        _process_name: ?[]const u8 = null,
        _pid: i64 = 0,
        pub fn init(src: []const u8) gremlin.Error!ChromeDroppedFrames.DroppedFrameReader {
            const buf = gremlin.Reader.init(src);
            var res = ChromeDroppedFrames.DroppedFrameReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    ChromeDroppedFrames.DroppedFrameWire.TS_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._ts = result.value;
                    },
                    ChromeDroppedFrames.DroppedFrameWire.PROCESS_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._process_name = result.value;
                    },
                    ChromeDroppedFrames.DroppedFrameWire.PID_WIRE => {
                        const result = try buf.readInt64(offset);
                        offset += result.size;
                        res._pid = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getTs(self: *const ChromeDroppedFrames.DroppedFrameReader) i64 {
            return self._ts;
        }
        pub inline fn getProcessName(self: *const ChromeDroppedFrames.DroppedFrameReader) []const u8 {
            return self._process_name orelse &[_]u8{};
        }
        pub inline fn getPid(self: *const ChromeDroppedFrames.DroppedFrameReader) i64 {
            return self._pid;
        }
    };
    // fields
    dropped_frame: ?[]const ?ChromeDroppedFrames.DroppedFrame = null,
    pub fn calcProtobufSize(self: *const ChromeDroppedFrames) usize {
        var res: usize = 0;
        if (self.dropped_frame) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(ChromeDroppedFramesWire.DROPPED_FRAME_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const ChromeDroppedFrames, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ChromeDroppedFrames, target: *gremlin.Writer) void {
        if (self.dropped_frame) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(ChromeDroppedFramesWire.DROPPED_FRAME_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(ChromeDroppedFramesWire.DROPPED_FRAME_WIRE, 0);
                }
            }
        }
    }
};
pub const ChromeDroppedFramesReader = struct {
    buf: gremlin.Reader,
    _dropped_frame_offset: ?usize = null,
    _dropped_frame_last_offset: ?usize = null,
    _dropped_frame_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!ChromeDroppedFramesReader {
        const buf = gremlin.Reader.init(src);
        var res = ChromeDroppedFramesReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ChromeDroppedFramesWire.DROPPED_FRAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._dropped_frame_offset == null) {
                        res._dropped_frame_offset = offset - result.size;
                    }
                    res._dropped_frame_last_offset = offset;
                    res._dropped_frame_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub fn droppedFrameCount(self: *const ChromeDroppedFramesReader) usize {
        return self._dropped_frame_cnt;
    }
    pub fn droppedFrameNext(self: *ChromeDroppedFramesReader) ?ChromeDroppedFrames.DroppedFrameReader {
        if (self._dropped_frame_offset == null) return null;
        const current_offset = self._dropped_frame_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = ChromeDroppedFrames.DroppedFrameReader.init(result.value) catch return null;
        if (self._dropped_frame_last_offset != null and current_offset >= self._dropped_frame_last_offset.?) {
            self._dropped_frame_offset = null;
            return msg;
        }
        if (self._dropped_frame_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._dropped_frame_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == ChromeDroppedFramesWire.DROPPED_FRAME_WIRE) {
                self._dropped_frame_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._dropped_frame_offset = null;
        return msg;
    }
};
