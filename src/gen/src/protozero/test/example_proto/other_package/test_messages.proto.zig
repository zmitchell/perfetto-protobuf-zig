// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// enums
pub const Enum = enum(i32) {
    A = 10,
    B = 11,
    _PROTOBUF_UNKNOWN = 0,
};
// structs
const MessageWire = struct {
    const FIELD_INT32_WIRE: gremlin.ProtoWireNumber = 1;
    const FIELD_ENUM_WIRE: gremlin.ProtoWireNumber = 2;
    const FIELD_NESTED_ENUM_WIRE: gremlin.ProtoWireNumber = 3;
    const FIELD_NESTED_MESSAGE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const Message = struct {
    // nested enums
    pub const NestedEnum = enum(i32) {
        C = 12,
        D = 13,
        _PROTOBUF_UNKNOWN = 0,
    };
    // nested structs
    const NestedMessageWire = struct {
        const FIELD_INT32_WIRE: gremlin.ProtoWireNumber = 1;
    };
    pub const NestedMessage = struct {
        // fields
        field_int32: i32 = 0,
        pub fn calcProtobufSize(self: *const Message.NestedMessage) usize {
            var res: usize = 0;
            if (self.field_int32 != 0) {
                res += gremlin.sizes.sizeWireNumber(Message.NestedMessageWire.FIELD_INT32_WIRE) + gremlin.sizes.sizeI32(self.field_int32);
            }
            return res;
        }
        pub fn encode(self: *const Message.NestedMessage, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const Message.NestedMessage, target: *gremlin.Writer) void {
            if (self.field_int32 != 0) {
                target.appendInt32(Message.NestedMessageWire.FIELD_INT32_WIRE, self.field_int32);
            }
        }
    };
    pub const NestedMessageReader = struct {
        buf: gremlin.Reader,
        _field_int32: i32 = 0,
        pub fn init(src: []const u8) gremlin.Error!Message.NestedMessageReader {
            const buf = gremlin.Reader.init(src);
            var res = Message.NestedMessageReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    Message.NestedMessageWire.FIELD_INT32_WIRE => {
                        const result = try buf.readInt32(offset);
                        offset += result.size;
                        res._field_int32 = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getFieldInt32(self: *const Message.NestedMessageReader) i32 {
            return self._field_int32;
        }
    };
    // fields
    field_int32: i32 = 0,
    field_enum: Enum = @enumFromInt(0),
    field_nested_enum: Message.NestedEnum = @enumFromInt(0),
    field_nested_message: ?Message.NestedMessage = null,
    pub fn calcProtobufSize(self: *const Message) usize {
        var res: usize = 0;
        if (self.field_int32 != 0) {
            res += gremlin.sizes.sizeWireNumber(MessageWire.FIELD_INT32_WIRE) + gremlin.sizes.sizeI32(self.field_int32);
        }
        if (@intFromEnum(self.field_enum) != 0) {
            res += gremlin.sizes.sizeWireNumber(MessageWire.FIELD_ENUM_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.field_enum));
        }
        if (@intFromEnum(self.field_nested_enum) != 0) {
            res += gremlin.sizes.sizeWireNumber(MessageWire.FIELD_NESTED_ENUM_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.field_nested_enum));
        }
        if (self.field_nested_message) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(MessageWire.FIELD_NESTED_MESSAGE_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const Message, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const Message, target: *gremlin.Writer) void {
        if (self.field_int32 != 0) {
            target.appendInt32(MessageWire.FIELD_INT32_WIRE, self.field_int32);
        }
        if (@intFromEnum(self.field_enum) != 0) {
            target.appendInt32(MessageWire.FIELD_ENUM_WIRE, @intFromEnum(self.field_enum));
        }
        if (@intFromEnum(self.field_nested_enum) != 0) {
            target.appendInt32(MessageWire.FIELD_NESTED_ENUM_WIRE, @intFromEnum(self.field_nested_enum));
        }
        if (self.field_nested_message) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(MessageWire.FIELD_NESTED_MESSAGE_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const MessageReader = struct {
    buf: gremlin.Reader,
    _field_int32: i32 = 0,
    _field_enum: Enum = @enumFromInt(0),
    _field_nested_enum: Message.NestedEnum = @enumFromInt(0),
    _field_nested_message_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!MessageReader {
        const buf = gremlin.Reader.init(src);
        var res = MessageReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                MessageWire.FIELD_INT32_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._field_int32 = result.value;
                },
                MessageWire.FIELD_ENUM_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._field_enum = @enumFromInt(result.value);
                },
                MessageWire.FIELD_NESTED_ENUM_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._field_nested_enum = @enumFromInt(result.value);
                },
                MessageWire.FIELD_NESTED_MESSAGE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._field_nested_message_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFieldInt32(self: *const MessageReader) i32 {
        return self._field_int32;
    }
    pub inline fn getFieldEnum(self: *const MessageReader) Enum {
        return self._field_enum;
    }
    pub inline fn getFieldNestedEnum(self: *const MessageReader) Message.NestedEnum {
        return self._field_nested_enum;
    }
    pub fn getFieldNestedMessage(self: *const MessageReader) gremlin.Error!Message.NestedMessageReader {
        if (self._field_nested_message_buf) |buf| {
            return try Message.NestedMessageReader.init(buf);
        }
        return try Message.NestedMessageReader.init(&[_]u8{});
    }
};
