// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const galaxies = @import("src/gen/src/protozero/test/example_proto/library_internals/galaxies.proto.zig");
// structs
const TransgalacticMessageWire = struct {
    const ORIGIN_GALAXY_WIRE: gremlin.ProtoWireNumber = 1;
    const ORIGIN_PLANET_WIRE: gremlin.ProtoWireNumber = 2;
    const DESTINATION_GALAXY_WIRE: gremlin.ProtoWireNumber = 3;
    const DESTINATION_PLANET_WIRE: gremlin.ProtoWireNumber = 4;
    const PROTO_MESSAGE_WIRE: gremlin.ProtoWireNumber = 5;
};
pub const TransgalacticMessage = struct {
    // nested enums
    pub const MessageType = enum(i32) {
        REGULAR = 0,
        EXPRESS = 1,
    };
    // fields
    origin_galaxy: galaxies.Galaxy = @enumFromInt(0),
    origin_planet: ?[]const u8 = null,
    destination_galaxy: galaxies.Galaxy = @enumFromInt(0),
    destination_planet: ?[]const u8 = null,
    proto_message: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const TransgalacticMessage) usize {
        var res: usize = 0;
        if (@intFromEnum(self.origin_galaxy) != 0) {
            res += gremlin.sizes.sizeWireNumber(TransgalacticMessageWire.ORIGIN_GALAXY_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.origin_galaxy));
        }
        if (self.origin_planet) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TransgalacticMessageWire.ORIGIN_PLANET_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (@intFromEnum(self.destination_galaxy) != 0) {
            res += gremlin.sizes.sizeWireNumber(TransgalacticMessageWire.DESTINATION_GALAXY_WIRE) + gremlin.sizes.sizeI32(@intFromEnum(self.destination_galaxy));
        }
        if (self.destination_planet) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TransgalacticMessageWire.DESTINATION_PLANET_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.proto_message) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TransgalacticMessageWire.PROTO_MESSAGE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const TransgalacticMessage, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TransgalacticMessage, target: *gremlin.Writer) void {
        if (@intFromEnum(self.origin_galaxy) != 0) {
            target.appendInt32(TransgalacticMessageWire.ORIGIN_GALAXY_WIRE, @intFromEnum(self.origin_galaxy));
        }
        if (self.origin_planet) |v| {
            if (v.len > 0) {
                target.appendBytes(TransgalacticMessageWire.ORIGIN_PLANET_WIRE, v);
            }
        }
        if (@intFromEnum(self.destination_galaxy) != 0) {
            target.appendInt32(TransgalacticMessageWire.DESTINATION_GALAXY_WIRE, @intFromEnum(self.destination_galaxy));
        }
        if (self.destination_planet) |v| {
            if (v.len > 0) {
                target.appendBytes(TransgalacticMessageWire.DESTINATION_PLANET_WIRE, v);
            }
        }
        if (self.proto_message) |v| {
            if (v.len > 0) {
                target.appendBytes(TransgalacticMessageWire.PROTO_MESSAGE_WIRE, v);
            }
        }
    }
};
pub const TransgalacticMessageReader = struct {
    buf: gremlin.Reader,
    _origin_galaxy: galaxies.Galaxy = @enumFromInt(0),
    _origin_planet: ?[]const u8 = null,
    _destination_galaxy: galaxies.Galaxy = @enumFromInt(0),
    _destination_planet: ?[]const u8 = null,
    _proto_message: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!TransgalacticMessageReader {
        const buf = gremlin.Reader.init(src);
        var res = TransgalacticMessageReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TransgalacticMessageWire.ORIGIN_GALAXY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._origin_galaxy = @enumFromInt(result.value);
                },
                TransgalacticMessageWire.ORIGIN_PLANET_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._origin_planet = result.value;
                },
                TransgalacticMessageWire.DESTINATION_GALAXY_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._destination_galaxy = @enumFromInt(result.value);
                },
                TransgalacticMessageWire.DESTINATION_PLANET_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._destination_planet = result.value;
                },
                TransgalacticMessageWire.PROTO_MESSAGE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._proto_message = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getOriginGalaxy(self: *const TransgalacticMessageReader) galaxies.Galaxy {
        return self._origin_galaxy;
    }
    pub inline fn getOriginPlanet(self: *const TransgalacticMessageReader) []const u8 {
        return self._origin_planet orelse &[_]u8{};
    }
    pub inline fn getDestinationGalaxy(self: *const TransgalacticMessageReader) galaxies.Galaxy {
        return self._destination_galaxy;
    }
    pub inline fn getDestinationPlanet(self: *const TransgalacticMessageReader) []const u8 {
        return self._destination_planet orelse &[_]u8{};
    }
    pub inline fn getProtoMessage(self: *const TransgalacticMessageReader) []const u8 {
        return self._proto_message orelse &[_]u8{};
    }
};
