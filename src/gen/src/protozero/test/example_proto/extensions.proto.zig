// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
const extensions = @import("extensions.proto.zig");
// structs
const RealFakeEventWire = struct {
    const BASE_INT_WIRE: gremlin.ProtoWireNumber = 1;
    const BASE_STRING_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const RealFakeEvent = struct {
    // fields
    base_int: u32 = 0,
    base_string: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RealFakeEvent) usize {
        var res: usize = 0;
        if (self.base_int != 0) {
            res += gremlin.sizes.sizeWireNumber(RealFakeEventWire.BASE_INT_WIRE) + gremlin.sizes.sizeU32(self.base_int);
        }
        if (self.base_string) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RealFakeEventWire.BASE_STRING_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const RealFakeEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RealFakeEvent, target: *gremlin.Writer) void {
        if (self.base_int != 0) {
            target.appendUint32(RealFakeEventWire.BASE_INT_WIRE, self.base_int);
        }
        if (self.base_string) |v| {
            if (v.len > 0) {
                target.appendBytes(RealFakeEventWire.BASE_STRING_WIRE, v);
            }
        }
    }
};
pub const RealFakeEventReader = struct {
    buf: gremlin.Reader,
    _base_int: u32 = 0,
    _base_string: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RealFakeEventReader {
        const buf = gremlin.Reader.init(src);
        var res = RealFakeEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RealFakeEventWire.BASE_INT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._base_int = result.value;
                },
                RealFakeEventWire.BASE_STRING_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._base_string = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBaseInt(self: *const RealFakeEventReader) u32 {
        return self._base_int;
    }
    pub inline fn getBaseString(self: *const RealFakeEventReader) []const u8 {
        return self._base_string orelse &[_]u8{};
    }
};
const SystemAWire = struct {
    const INT_A_WIRE: gremlin.ProtoWireNumber = 1;
    const STRING_A_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const SystemA = struct {
    // fields
    int_a: u32 = 0,
    string_a: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const SystemA) usize {
        var res: usize = 0;
        if (self.int_a != 0) {
            res += gremlin.sizes.sizeWireNumber(SystemAWire.INT_A_WIRE) + gremlin.sizes.sizeU32(self.int_a);
        }
        if (self.string_a) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SystemAWire.STRING_A_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const SystemA, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SystemA, target: *gremlin.Writer) void {
        if (self.int_a != 0) {
            target.appendUint32(SystemAWire.INT_A_WIRE, self.int_a);
        }
        if (self.string_a) |v| {
            if (v.len > 0) {
                target.appendBytes(SystemAWire.STRING_A_WIRE, v);
            }
        }
    }
};
pub const SystemAReader = struct {
    buf: gremlin.Reader,
    _int_a: u32 = 0,
    _string_a: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!SystemAReader {
        const buf = gremlin.Reader.init(src);
        var res = SystemAReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SystemAWire.INT_A_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._int_a = result.value;
                },
                SystemAWire.STRING_A_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._string_a = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIntA(self: *const SystemAReader) u32 {
        return self._int_a;
    }
    pub inline fn getStringA(self: *const SystemAReader) []const u8 {
        return self._string_a orelse &[_]u8{};
    }
};
const SystemBWire = struct {
    const INT_B_WIRE: gremlin.ProtoWireNumber = 1;
    const STRING_B_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const SystemB = struct {
    // fields
    int_b: u32 = 0,
    string_b: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const SystemB) usize {
        var res: usize = 0;
        if (self.int_b != 0) {
            res += gremlin.sizes.sizeWireNumber(SystemBWire.INT_B_WIRE) + gremlin.sizes.sizeU32(self.int_b);
        }
        if (self.string_b) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(SystemBWire.STRING_B_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const SystemB, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const SystemB, target: *gremlin.Writer) void {
        if (self.int_b != 0) {
            target.appendUint32(SystemBWire.INT_B_WIRE, self.int_b);
        }
        if (self.string_b) |v| {
            if (v.len > 0) {
                target.appendBytes(SystemBWire.STRING_B_WIRE, v);
            }
        }
    }
};
pub const SystemBReader = struct {
    buf: gremlin.Reader,
    _int_b: u32 = 0,
    _string_b: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!SystemBReader {
        const buf = gremlin.Reader.init(src);
        var res = SystemBReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                SystemBWire.INT_B_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._int_b = result.value;
                },
                SystemBWire.STRING_B_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._string_b = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getIntB(self: *const SystemBReader) u32 {
        return self._int_b;
    }
    pub inline fn getStringB(self: *const SystemBReader) []const u8 {
        return self._string_b orelse &[_]u8{};
    }
};
const BrowserExtensionWire = struct {
    const BASE_INT_WIRE: gremlin.ProtoWireNumber = 1;
    const BASE_STRING_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const BrowserExtension = struct {
    // fields
    base_int: u32 = 0,
    base_string: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const BrowserExtension) usize {
        var res: usize = 0;
        if (self.base_int != 0) {
            res += gremlin.sizes.sizeWireNumber(BrowserExtensionWire.BASE_INT_WIRE) + gremlin.sizes.sizeU32(self.base_int);
        }
        if (self.base_string) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(BrowserExtensionWire.BASE_STRING_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const BrowserExtension, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const BrowserExtension, target: *gremlin.Writer) void {
        if (self.base_int != 0) {
            target.appendUint32(BrowserExtensionWire.BASE_INT_WIRE, self.base_int);
        }
        if (self.base_string) |v| {
            if (v.len > 0) {
                target.appendBytes(BrowserExtensionWire.BASE_STRING_WIRE, v);
            }
        }
    }
};
pub const BrowserExtensionReader = struct {
    buf: gremlin.Reader,
    _base_int: u32 = 0,
    _base_string: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!BrowserExtensionReader {
        const buf = gremlin.Reader.init(src);
        var res = BrowserExtensionReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                BrowserExtensionWire.BASE_INT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._base_int = result.value;
                },
                BrowserExtensionWire.BASE_STRING_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._base_string = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBaseInt(self: *const BrowserExtensionReader) u32 {
        return self._base_int;
    }
    pub inline fn getBaseString(self: *const BrowserExtensionReader) []const u8 {
        return self._base_string orelse &[_]u8{};
    }
};
const LegacyFakeEventWire = struct {
    const BASE_INT_WIRE: gremlin.ProtoWireNumber = 1;
    const BASE_STRING_WIRE: gremlin.ProtoWireNumber = 2;
    const EXTENSION_A_WIRE: gremlin.ProtoWireNumber = 10;
    const EXTENSION_B_WIRE: gremlin.ProtoWireNumber = 11;
};
pub const LegacyFakeEvent = struct {
    // fields
    base_int: u32 = 0,
    base_string: ?[]const u8 = null,
    extension_a: ?SystemA = null,
    extension_b: ?SystemB = null,
    pub fn calcProtobufSize(self: *const LegacyFakeEvent) usize {
        var res: usize = 0;
        if (self.base_int != 0) {
            res += gremlin.sizes.sizeWireNumber(LegacyFakeEventWire.BASE_INT_WIRE) + gremlin.sizes.sizeU32(self.base_int);
        }
        if (self.base_string) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(LegacyFakeEventWire.BASE_STRING_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.extension_a) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(LegacyFakeEventWire.EXTENSION_A_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        if (self.extension_b) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(LegacyFakeEventWire.EXTENSION_B_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const LegacyFakeEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const LegacyFakeEvent, target: *gremlin.Writer) void {
        if (self.base_int != 0) {
            target.appendUint32(LegacyFakeEventWire.BASE_INT_WIRE, self.base_int);
        }
        if (self.base_string) |v| {
            if (v.len > 0) {
                target.appendBytes(LegacyFakeEventWire.BASE_STRING_WIRE, v);
            }
        }
        if (self.extension_a) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(LegacyFakeEventWire.EXTENSION_A_WIRE, size);
                v.encodeTo(target);
            }
        }
        if (self.extension_b) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(LegacyFakeEventWire.EXTENSION_B_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const LegacyFakeEventReader = struct {
    buf: gremlin.Reader,
    _base_int: u32 = 0,
    _base_string: ?[]const u8 = null,
    _extension_a_buf: ?[]const u8 = null,
    _extension_b_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!LegacyFakeEventReader {
        const buf = gremlin.Reader.init(src);
        var res = LegacyFakeEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                LegacyFakeEventWire.BASE_INT_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._base_int = result.value;
                },
                LegacyFakeEventWire.BASE_STRING_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._base_string = result.value;
                },
                LegacyFakeEventWire.EXTENSION_A_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._extension_a_buf = result.value;
                },
                LegacyFakeEventWire.EXTENSION_B_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._extension_b_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getBaseInt(self: *const LegacyFakeEventReader) u32 {
        return self._base_int;
    }
    pub inline fn getBaseString(self: *const LegacyFakeEventReader) []const u8 {
        return self._base_string orelse &[_]u8{};
    }
    pub fn getExtensionA(self: *const LegacyFakeEventReader) gremlin.Error!SystemAReader {
        if (self._extension_a_buf) |buf| {
            return try SystemAReader.init(buf);
        }
        return try SystemAReader.init(&[_]u8{});
    }
    pub fn getExtensionB(self: *const LegacyFakeEventReader) gremlin.Error!SystemBReader {
        if (self._extension_b_buf) |buf| {
            return try SystemBReader.init(buf);
        }
        return try SystemBReader.init(&[_]u8{});
    }
};
