// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const FakeFtraceEventWire = struct {
    const COMMON_FIELD_WIRE: gremlin.ProtoWireNumber = 1;
    const COMMON_PID_WIRE: gremlin.ProtoWireNumber = 2;
    const ALL_FIELDS_WIRE: gremlin.ProtoWireNumber = 42;
};
pub const FakeFtraceEvent = struct {
    // fields
    common_field: u32 = 0,
    common_pid: u32 = 0,
    all_fields: ?FakeAllFieldsFtraceEvent = null,
    pub fn calcProtobufSize(self: *const FakeFtraceEvent) usize {
        var res: usize = 0;
        if (self.common_field != 0) {
            res += gremlin.sizes.sizeWireNumber(FakeFtraceEventWire.COMMON_FIELD_WIRE) + gremlin.sizes.sizeU32(self.common_field);
        }
        if (self.common_pid != 0) {
            res += gremlin.sizes.sizeWireNumber(FakeFtraceEventWire.COMMON_PID_WIRE) + gremlin.sizes.sizeU32(self.common_pid);
        }
        if (self.all_fields) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                res += gremlin.sizes.sizeWireNumber(FakeFtraceEventWire.ALL_FIELDS_WIRE) + gremlin.sizes.sizeUsize(size) + size;
            }
        }
        return res;
    }
    pub fn encode(self: *const FakeFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FakeFtraceEvent, target: *gremlin.Writer) void {
        if (self.common_field != 0) {
            target.appendUint32(FakeFtraceEventWire.COMMON_FIELD_WIRE, self.common_field);
        }
        if (self.common_pid != 0) {
            target.appendUint32(FakeFtraceEventWire.COMMON_PID_WIRE, self.common_pid);
        }
        if (self.all_fields) |v| {
            const size = v.calcProtobufSize();
            if (size > 0) {
                target.appendBytesTag(FakeFtraceEventWire.ALL_FIELDS_WIRE, size);
                v.encodeTo(target);
            }
        }
    }
};
pub const FakeFtraceEventReader = struct {
    buf: gremlin.Reader,
    _common_field: u32 = 0,
    _common_pid: u32 = 0,
    _all_fields_buf: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!FakeFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FakeFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FakeFtraceEventWire.COMMON_FIELD_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._common_field = result.value;
                },
                FakeFtraceEventWire.COMMON_PID_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._common_pid = result.value;
                },
                FakeFtraceEventWire.ALL_FIELDS_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._all_fields_buf = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getCommonField(self: *const FakeFtraceEventReader) u32 {
        return self._common_field;
    }
    pub inline fn getCommonPid(self: *const FakeFtraceEventReader) u32 {
        return self._common_pid;
    }
    pub fn getAllFields(self: *const FakeFtraceEventReader) gremlin.Error!FakeAllFieldsFtraceEventReader {
        if (self._all_fields_buf) |buf| {
            return try FakeAllFieldsFtraceEventReader.init(buf);
        }
        return try FakeAllFieldsFtraceEventReader.init(&[_]u8{});
    }
};
const FakeAllFieldsFtraceEventWire = struct {
    const FIELD_UINT32_WIRE: gremlin.ProtoWireNumber = 1;
    const FIELD_PID_WIRE: gremlin.ProtoWireNumber = 2;
    const FIELD_DEV_32_WIRE: gremlin.ProtoWireNumber = 3;
    const FIELD_INODE_32_WIRE: gremlin.ProtoWireNumber = 4;
    const FIELD_DEV_64_WIRE: gremlin.ProtoWireNumber = 5;
    const FIELD_INODE_64_WIRE: gremlin.ProtoWireNumber = 6;
    const FIELD_CHAR_16_WIRE: gremlin.ProtoWireNumber = 500;
    const FIELD_CHAR_WIRE: gremlin.ProtoWireNumber = 501;
    const FIELD_DATA_LOC_WIRE: gremlin.ProtoWireNumber = 502;
    const FIELD_CHAR_STAR_WIRE: gremlin.ProtoWireNumber = 503;
};
pub const FakeAllFieldsFtraceEvent = struct {
    // fields
    field_uint32: u32 = 0,
    field_pid: i32 = 0,
    field_dev_32: u64 = 0,
    field_inode_32: u64 = 0,
    field_dev_64: u64 = 0,
    field_inode_64: u64 = 0,
    field_char_16: ?[]const u8 = null,
    field_char: ?[]const u8 = null,
    field_data_loc: ?[]const u8 = null,
    field_char_star: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const FakeAllFieldsFtraceEvent) usize {
        var res: usize = 0;
        if (self.field_uint32 != 0) {
            res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_UINT32_WIRE) + gremlin.sizes.sizeU32(self.field_uint32);
        }
        if (self.field_pid != 0) {
            res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_PID_WIRE) + gremlin.sizes.sizeI32(self.field_pid);
        }
        if (self.field_dev_32 != 0) {
            res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_DEV_32_WIRE) + gremlin.sizes.sizeU64(self.field_dev_32);
        }
        if (self.field_inode_32 != 0) {
            res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_INODE_32_WIRE) + gremlin.sizes.sizeU64(self.field_inode_32);
        }
        if (self.field_dev_64 != 0) {
            res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_DEV_64_WIRE) + gremlin.sizes.sizeU64(self.field_dev_64);
        }
        if (self.field_inode_64 != 0) {
            res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_INODE_64_WIRE) + gremlin.sizes.sizeU64(self.field_inode_64);
        }
        if (self.field_char_16) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_CHAR_16_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.field_char) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_CHAR_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.field_data_loc) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_DATA_LOC_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        if (self.field_char_star) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(FakeAllFieldsFtraceEventWire.FIELD_CHAR_STAR_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const FakeAllFieldsFtraceEvent, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const FakeAllFieldsFtraceEvent, target: *gremlin.Writer) void {
        if (self.field_uint32 != 0) {
            target.appendUint32(FakeAllFieldsFtraceEventWire.FIELD_UINT32_WIRE, self.field_uint32);
        }
        if (self.field_pid != 0) {
            target.appendInt32(FakeAllFieldsFtraceEventWire.FIELD_PID_WIRE, self.field_pid);
        }
        if (self.field_dev_32 != 0) {
            target.appendUint64(FakeAllFieldsFtraceEventWire.FIELD_DEV_32_WIRE, self.field_dev_32);
        }
        if (self.field_inode_32 != 0) {
            target.appendUint64(FakeAllFieldsFtraceEventWire.FIELD_INODE_32_WIRE, self.field_inode_32);
        }
        if (self.field_dev_64 != 0) {
            target.appendUint64(FakeAllFieldsFtraceEventWire.FIELD_DEV_64_WIRE, self.field_dev_64);
        }
        if (self.field_inode_64 != 0) {
            target.appendUint64(FakeAllFieldsFtraceEventWire.FIELD_INODE_64_WIRE, self.field_inode_64);
        }
        if (self.field_char_16) |v| {
            if (v.len > 0) {
                target.appendBytes(FakeAllFieldsFtraceEventWire.FIELD_CHAR_16_WIRE, v);
            }
        }
        if (self.field_char) |v| {
            if (v.len > 0) {
                target.appendBytes(FakeAllFieldsFtraceEventWire.FIELD_CHAR_WIRE, v);
            }
        }
        if (self.field_data_loc) |v| {
            if (v.len > 0) {
                target.appendBytes(FakeAllFieldsFtraceEventWire.FIELD_DATA_LOC_WIRE, v);
            }
        }
        if (self.field_char_star) |v| {
            if (v.len > 0) {
                target.appendBytes(FakeAllFieldsFtraceEventWire.FIELD_CHAR_STAR_WIRE, v);
            }
        }
    }
};
pub const FakeAllFieldsFtraceEventReader = struct {
    buf: gremlin.Reader,
    _field_uint32: u32 = 0,
    _field_pid: i32 = 0,
    _field_dev_32: u64 = 0,
    _field_inode_32: u64 = 0,
    _field_dev_64: u64 = 0,
    _field_inode_64: u64 = 0,
    _field_char_16: ?[]const u8 = null,
    _field_char: ?[]const u8 = null,
    _field_data_loc: ?[]const u8 = null,
    _field_char_star: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!FakeAllFieldsFtraceEventReader {
        const buf = gremlin.Reader.init(src);
        var res = FakeAllFieldsFtraceEventReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                FakeAllFieldsFtraceEventWire.FIELD_UINT32_WIRE => {
                    const result = try buf.readUInt32(offset);
                    offset += result.size;
                    res._field_uint32 = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_PID_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._field_pid = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_DEV_32_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._field_dev_32 = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_INODE_32_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._field_inode_32 = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_DEV_64_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._field_dev_64 = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_INODE_64_WIRE => {
                    const result = try buf.readUInt64(offset);
                    offset += result.size;
                    res._field_inode_64 = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_CHAR_16_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._field_char_16 = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_CHAR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._field_char = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_DATA_LOC_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._field_data_loc = result.value;
                },
                FakeAllFieldsFtraceEventWire.FIELD_CHAR_STAR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._field_char_star = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFieldUint32(self: *const FakeAllFieldsFtraceEventReader) u32 {
        return self._field_uint32;
    }
    pub inline fn getFieldPid(self: *const FakeAllFieldsFtraceEventReader) i32 {
        return self._field_pid;
    }
    pub inline fn getFieldDev32(self: *const FakeAllFieldsFtraceEventReader) u64 {
        return self._field_dev_32;
    }
    pub inline fn getFieldInode32(self: *const FakeAllFieldsFtraceEventReader) u64 {
        return self._field_inode_32;
    }
    pub inline fn getFieldDev64(self: *const FakeAllFieldsFtraceEventReader) u64 {
        return self._field_dev_64;
    }
    pub inline fn getFieldInode64(self: *const FakeAllFieldsFtraceEventReader) u64 {
        return self._field_inode_64;
    }
    pub inline fn getFieldChar16(self: *const FakeAllFieldsFtraceEventReader) []const u8 {
        return self._field_char_16 orelse &[_]u8{};
    }
    pub inline fn getFieldChar(self: *const FakeAllFieldsFtraceEventReader) []const u8 {
        return self._field_char orelse &[_]u8{};
    }
    pub inline fn getFieldDataLoc(self: *const FakeAllFieldsFtraceEventReader) []const u8 {
        return self._field_data_loc orelse &[_]u8{};
    }
    pub inline fn getFieldCharStar(self: *const FakeAllFieldsFtraceEventReader) []const u8 {
        return self._field_char_star orelse &[_]u8{};
    }
};
