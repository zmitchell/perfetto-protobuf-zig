// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const PerfettoCmdStateWire = struct {
    const FIRST_TRACE_TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 1;
    const LAST_TRACE_TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 2;
    const TOTAL_BYTES_UPLOADED_WIRE: gremlin.ProtoWireNumber = 3;
    const SESSION_STATE_WIRE: gremlin.ProtoWireNumber = 4;
};
pub const PerfettoCmdState = struct {
    // nested structs
    const PerSessionStateWire = struct {
        const SESSION_NAME_WIRE: gremlin.ProtoWireNumber = 1;
        const TOTAL_BYTES_UPLOADED_WIRE: gremlin.ProtoWireNumber = 3;
        const LAST_TRACE_TIMESTAMP_WIRE: gremlin.ProtoWireNumber = 2;
    };
    pub const PerSessionState = struct {
        // fields
        session_name: ?[]const u8 = null,
        total_bytes_uploaded: u64 = 0,
        last_trace_timestamp: u64 = 0,
        pub fn calcProtobufSize(self: *const PerfettoCmdState.PerSessionState) usize {
            var res: usize = 0;
            if (self.session_name) |v| {
                if (v.len > 0) {
                    res += gremlin.sizes.sizeWireNumber(PerfettoCmdState.PerSessionStateWire.SESSION_NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
                }
            }
            if (self.total_bytes_uploaded != 0) {
                res += gremlin.sizes.sizeWireNumber(PerfettoCmdState.PerSessionStateWire.TOTAL_BYTES_UPLOADED_WIRE) + gremlin.sizes.sizeFixed64(self.total_bytes_uploaded);
            }
            if (self.last_trace_timestamp != 0) {
                res += gremlin.sizes.sizeWireNumber(PerfettoCmdState.PerSessionStateWire.LAST_TRACE_TIMESTAMP_WIRE) + gremlin.sizes.sizeFixed64(self.last_trace_timestamp);
            }
            return res;
        }
        pub fn encode(self: *const PerfettoCmdState.PerSessionState, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
            const size = self.calcProtobufSize();
            if (size == 0) {
                return &[_]u8{};
            }
            const buf = try allocator.alloc(u8, self.calcProtobufSize());
            var writer = gremlin.Writer.init(buf);
            self.encodeTo(&writer);
            return buf;
        }
        pub fn encodeTo(self: *const PerfettoCmdState.PerSessionState, target: *gremlin.Writer) void {
            if (self.session_name) |v| {
                if (v.len > 0) {
                    target.appendBytes(PerfettoCmdState.PerSessionStateWire.SESSION_NAME_WIRE, v);
                }
            }
            if (self.total_bytes_uploaded != 0) {
                target.appendFixed64(PerfettoCmdState.PerSessionStateWire.TOTAL_BYTES_UPLOADED_WIRE, self.total_bytes_uploaded);
            }
            if (self.last_trace_timestamp != 0) {
                target.appendFixed64(PerfettoCmdState.PerSessionStateWire.LAST_TRACE_TIMESTAMP_WIRE, self.last_trace_timestamp);
            }
        }
    };
    pub const PerSessionStateReader = struct {
        buf: gremlin.Reader,
        _session_name: ?[]const u8 = null,
        _total_bytes_uploaded: u64 = 0,
        _last_trace_timestamp: u64 = 0,
        pub fn init(src: []const u8) gremlin.Error!PerfettoCmdState.PerSessionStateReader {
            const buf = gremlin.Reader.init(src);
            var res = PerfettoCmdState.PerSessionStateReader{ .buf = buf };
            if (buf.buf.len == 0) {
                return res;
            }
            var offset: usize = 0;
            while (buf.hasNext(offset, 0)) {
                const tag = try buf.readTagAt(offset);
                offset += tag.size;
                switch (tag.number) {
                    PerfettoCmdState.PerSessionStateWire.SESSION_NAME_WIRE => {
                        const result = try buf.readBytes(offset);
                        offset += result.size;
                        res._session_name = result.value;
                    },
                    PerfettoCmdState.PerSessionStateWire.TOTAL_BYTES_UPLOADED_WIRE => {
                        const result = try buf.readFixed64(offset);
                        offset += result.size;
                        res._total_bytes_uploaded = result.value;
                    },
                    PerfettoCmdState.PerSessionStateWire.LAST_TRACE_TIMESTAMP_WIRE => {
                        const result = try buf.readFixed64(offset);
                        offset += result.size;
                        res._last_trace_timestamp = result.value;
                    },
                    else => {
                        offset = try buf.skipData(offset, tag.wire);
                    },
                }
            }
            return res;
        }
        pub fn sourceBytes(self: *const @This()) []const u8 {
            return self.buf.buf;
        }
        pub inline fn getSessionName(self: *const PerfettoCmdState.PerSessionStateReader) []const u8 {
            return self._session_name orelse &[_]u8{};
        }
        pub inline fn getTotalBytesUploaded(self: *const PerfettoCmdState.PerSessionStateReader) u64 {
            return self._total_bytes_uploaded;
        }
        pub inline fn getLastTraceTimestamp(self: *const PerfettoCmdState.PerSessionStateReader) u64 {
            return self._last_trace_timestamp;
        }
    };
    // fields
    first_trace_timestamp: u64 = 0,
    last_trace_timestamp: u64 = 0,
    total_bytes_uploaded: u64 = 0,
    session_state: ?[]const ?PerfettoCmdState.PerSessionState = null,
    pub fn calcProtobufSize(self: *const PerfettoCmdState) usize {
        var res: usize = 0;
        if (self.first_trace_timestamp != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoCmdStateWire.FIRST_TRACE_TIMESTAMP_WIRE) + gremlin.sizes.sizeFixed64(self.first_trace_timestamp);
        }
        if (self.last_trace_timestamp != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoCmdStateWire.LAST_TRACE_TIMESTAMP_WIRE) + gremlin.sizes.sizeFixed64(self.last_trace_timestamp);
        }
        if (self.total_bytes_uploaded != 0) {
            res += gremlin.sizes.sizeWireNumber(PerfettoCmdStateWire.TOTAL_BYTES_UPLOADED_WIRE) + gremlin.sizes.sizeFixed64(self.total_bytes_uploaded);
        }
        if (self.session_state) |arr| {
            for (arr) |maybe_v| {
                res += gremlin.sizes.sizeWireNumber(PerfettoCmdStateWire.SESSION_STATE_WIRE);
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    res += gremlin.sizes.sizeUsize(size) + size;
                } else {
                    res += gremlin.sizes.sizeUsize(0);
                }
            }
        }
        return res;
    }
    pub fn encode(self: *const PerfettoCmdState, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const PerfettoCmdState, target: *gremlin.Writer) void {
        if (self.first_trace_timestamp != 0) {
            target.appendFixed64(PerfettoCmdStateWire.FIRST_TRACE_TIMESTAMP_WIRE, self.first_trace_timestamp);
        }
        if (self.last_trace_timestamp != 0) {
            target.appendFixed64(PerfettoCmdStateWire.LAST_TRACE_TIMESTAMP_WIRE, self.last_trace_timestamp);
        }
        if (self.total_bytes_uploaded != 0) {
            target.appendFixed64(PerfettoCmdStateWire.TOTAL_BYTES_UPLOADED_WIRE, self.total_bytes_uploaded);
        }
        if (self.session_state) |arr| {
            for (arr) |maybe_v| {
                if (maybe_v) |v| {
                    const size = v.calcProtobufSize();
                    target.appendBytesTag(PerfettoCmdStateWire.SESSION_STATE_WIRE, size);
                    v.encodeTo(target);
                } else {
                    target.appendBytesTag(PerfettoCmdStateWire.SESSION_STATE_WIRE, 0);
                }
            }
        }
    }
};
pub const PerfettoCmdStateReader = struct {
    buf: gremlin.Reader,
    _first_trace_timestamp: u64 = 0,
    _last_trace_timestamp: u64 = 0,
    _total_bytes_uploaded: u64 = 0,
    _session_state_offset: ?usize = null,
    _session_state_last_offset: ?usize = null,
    _session_state_cnt: usize = 0,
    pub fn init(src: []const u8) gremlin.Error!PerfettoCmdStateReader {
        const buf = gremlin.Reader.init(src);
        var res = PerfettoCmdStateReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                PerfettoCmdStateWire.FIRST_TRACE_TIMESTAMP_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._first_trace_timestamp = result.value;
                },
                PerfettoCmdStateWire.LAST_TRACE_TIMESTAMP_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._last_trace_timestamp = result.value;
                },
                PerfettoCmdStateWire.TOTAL_BYTES_UPLOADED_WIRE => {
                    const result = try buf.readFixed64(offset);
                    offset += result.size;
                    res._total_bytes_uploaded = result.value;
                },
                PerfettoCmdStateWire.SESSION_STATE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    if (res._session_state_offset == null) {
                        res._session_state_offset = offset - result.size;
                    }
                    res._session_state_last_offset = offset;
                    res._session_state_cnt += 1;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getFirstTraceTimestamp(self: *const PerfettoCmdStateReader) u64 {
        return self._first_trace_timestamp;
    }
    pub inline fn getLastTraceTimestamp(self: *const PerfettoCmdStateReader) u64 {
        return self._last_trace_timestamp;
    }
    pub inline fn getTotalBytesUploaded(self: *const PerfettoCmdStateReader) u64 {
        return self._total_bytes_uploaded;
    }
    pub fn sessionStateCount(self: *const PerfettoCmdStateReader) usize {
        return self._session_state_cnt;
    }
    pub fn sessionStateNext(self: *PerfettoCmdStateReader) ?PerfettoCmdState.PerSessionStateReader {
        if (self._session_state_offset == null) return null;
        const current_offset = self._session_state_offset.?;
        const result = self.buf.readBytes(current_offset) catch return null;
        const msg = PerfettoCmdState.PerSessionStateReader.init(result.value) catch return null;
        if (self._session_state_last_offset != null and current_offset >= self._session_state_last_offset.?) {
            self._session_state_offset = null;
            return msg;
        }
        if (self._session_state_last_offset == null) unreachable;
        var next_offset = current_offset + result.size;
        const max_offset = self._session_state_last_offset.?;
        while (next_offset <= max_offset and self.buf.hasNext(next_offset, 0)) {
            const tag = self.buf.readTagAt(next_offset) catch break;
            next_offset += tag.size;
            if (tag.number == PerfettoCmdStateWire.SESSION_STATE_WIRE) {
                self._session_state_offset = next_offset;
                return msg;
            } else {
                next_offset = self.buf.skipData(next_offset, tag.wire) catch break;
            }
        }
        self._session_state_offset = null;
        return msg;
    }
};
