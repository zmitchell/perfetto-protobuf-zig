// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const TestMessageWire = struct {
    const NUM_WIRE: gremlin.ProtoWireNumber = 1;
    const STR_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const TestMessage = struct {
    // fields
    num: i32 = 0,
    str: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const TestMessage) usize {
        var res: usize = 0;
        if (self.num != 0) {
            res += gremlin.sizes.sizeWireNumber(TestMessageWire.NUM_WIRE) + gremlin.sizes.sizeI32(self.num);
        }
        if (self.str) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(TestMessageWire.STR_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const TestMessage, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const TestMessage, target: *gremlin.Writer) void {
        if (self.num != 0) {
            target.appendInt32(TestMessageWire.NUM_WIRE, self.num);
        }
        if (self.str) |v| {
            if (v.len > 0) {
                target.appendBytes(TestMessageWire.STR_WIRE, v);
            }
        }
    }
};
pub const TestMessageReader = struct {
    buf: gremlin.Reader,
    _num: i32 = 0,
    _str: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!TestMessageReader {
        const buf = gremlin.Reader.init(src);
        var res = TestMessageReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                TestMessageWire.NUM_WIRE => {
                    const result = try buf.readInt32(offset);
                    offset += result.size;
                    res._num = result.value;
                },
                TestMessageWire.STR_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._str = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getNum(self: *const TestMessageReader) i32 {
        return self._num;
    }
    pub inline fn getStr(self: *const TestMessageReader) []const u8 {
        return self._str orelse &[_]u8{};
    }
};
