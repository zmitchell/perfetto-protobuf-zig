// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const GreeterRequestMsgWire = struct {
    const NAME_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const GreeterRequestMsg = struct {
    // fields
    name: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const GreeterRequestMsg) usize {
        var res: usize = 0;
        if (self.name) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(GreeterRequestMsgWire.NAME_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const GreeterRequestMsg, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GreeterRequestMsg, target: *gremlin.Writer) void {
        if (self.name) |v| {
            if (v.len > 0) {
                target.appendBytes(GreeterRequestMsgWire.NAME_WIRE, v);
            }
        }
    }
};
pub const GreeterRequestMsgReader = struct {
    buf: gremlin.Reader,
    _name: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!GreeterRequestMsgReader {
        const buf = gremlin.Reader.init(src);
        var res = GreeterRequestMsgReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GreeterRequestMsgWire.NAME_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._name = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getName(self: *const GreeterRequestMsgReader) []const u8 {
        return self._name orelse &[_]u8{};
    }
};
const GreeterReplyMsgWire = struct {
    const MESSAGE_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const GreeterReplyMsg = struct {
    // fields
    message: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const GreeterReplyMsg) usize {
        var res: usize = 0;
        if (self.message) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(GreeterReplyMsgWire.MESSAGE_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const GreeterReplyMsg, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const GreeterReplyMsg, target: *gremlin.Writer) void {
        if (self.message) |v| {
            if (v.len > 0) {
                target.appendBytes(GreeterReplyMsgWire.MESSAGE_WIRE, v);
            }
        }
    }
};
pub const GreeterReplyMsgReader = struct {
    buf: gremlin.Reader,
    _message: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!GreeterReplyMsgReader {
        const buf = gremlin.Reader.init(src);
        var res = GreeterReplyMsgReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                GreeterReplyMsgWire.MESSAGE_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._message = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getMessage(self: *const GreeterReplyMsgReader) []const u8 {
        return self._message orelse &[_]u8{};
    }
};
