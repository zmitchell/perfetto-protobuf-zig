// =============================================================================
// DO NOT EDIT - This file is automatically generated by gremlin.zig
// =============================================================================
const std = @import("std");
const gremlin = @import("gremlin");
// structs
const RequestProtoWire = struct {
    const DATA_WIRE: gremlin.ProtoWireNumber = 1;
};
pub const RequestProto = struct {
    // fields
    data: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const RequestProto) usize {
        var res: usize = 0;
        if (self.data) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(RequestProtoWire.DATA_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const RequestProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const RequestProto, target: *gremlin.Writer) void {
        if (self.data) |v| {
            if (v.len > 0) {
                target.appendBytes(RequestProtoWire.DATA_WIRE, v);
            }
        }
    }
};
pub const RequestProtoReader = struct {
    buf: gremlin.Reader,
    _data: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!RequestProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = RequestProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                RequestProtoWire.DATA_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._data = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getData(self: *const RequestProtoReader) []const u8 {
        return self._data orelse &[_]u8{};
    }
};
const ReplyProtoWire = struct {
    const DATA_WIRE: gremlin.ProtoWireNumber = 2;
};
pub const ReplyProto = struct {
    // fields
    data: ?[]const u8 = null,
    pub fn calcProtobufSize(self: *const ReplyProto) usize {
        var res: usize = 0;
        if (self.data) |v| {
            if (v.len > 0) {
                res += gremlin.sizes.sizeWireNumber(ReplyProtoWire.DATA_WIRE) + gremlin.sizes.sizeUsize(v.len) + v.len;
            }
        }
        return res;
    }
    pub fn encode(self: *const ReplyProto, allocator: std.mem.Allocator) gremlin.Error![]const u8 {
        const size = self.calcProtobufSize();
        if (size == 0) {
            return &[_]u8{};
        }
        const buf = try allocator.alloc(u8, self.calcProtobufSize());
        var writer = gremlin.Writer.init(buf);
        self.encodeTo(&writer);
        return buf;
    }
    pub fn encodeTo(self: *const ReplyProto, target: *gremlin.Writer) void {
        if (self.data) |v| {
            if (v.len > 0) {
                target.appendBytes(ReplyProtoWire.DATA_WIRE, v);
            }
        }
    }
};
pub const ReplyProtoReader = struct {
    buf: gremlin.Reader,
    _data: ?[]const u8 = null,
    pub fn init(src: []const u8) gremlin.Error!ReplyProtoReader {
        const buf = gremlin.Reader.init(src);
        var res = ReplyProtoReader{ .buf = buf };
        if (buf.buf.len == 0) {
            return res;
        }
        var offset: usize = 0;
        while (buf.hasNext(offset, 0)) {
            const tag = try buf.readTagAt(offset);
            offset += tag.size;
            switch (tag.number) {
                ReplyProtoWire.DATA_WIRE => {
                    const result = try buf.readBytes(offset);
                    offset += result.size;
                    res._data = result.value;
                },
                else => {
                    offset = try buf.skipData(offset, tag.wire);
                },
            }
        }
        return res;
    }
    pub fn sourceBytes(self: *const @This()) []const u8 {
        return self.buf.buf;
    }
    pub inline fn getData(self: *const ReplyProtoReader) []const u8 {
        return self._data orelse &[_]u8{};
    }
};
